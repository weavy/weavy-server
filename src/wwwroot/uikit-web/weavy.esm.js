var r0 = Object.defineProperty;
var tm = (s) => {
  throw TypeError(s);
};
var n = (s, t) => r0(s, "name", { value: t, configurable: !0 });
var Dp = (s, t, e) => t.has(s) || tm("Cannot " + e);
var p = (s, t, e) => (Dp(s, t, "read from private field"), e ? e.call(s) : t.get(s)), T = (s, t, e) => t.has(s) ? tm("Cannot add the same private member more than once") : t instanceof WeakSet ? t.add(s) : t.set(s, e), P = (s, t, e, i) => (Dp(s, t, "write to private field"), i ? i.call(s, e) : t.set(s, e), e), dt = (s, t, e) => (Dp(s, t, "access private method"), e);
var gh = (s, t, e, i) => ({
  set _(r) {
    P(s, t, r, e);
  },
  get _() {
    return p(s, t, i);
  }
});
import { observeConnected as n0, throwOnDomNotAvailable as $e, isDomAvailable as dw, whenConnected as _v, whenParentsDefined as gi, defaultVisibilityCheckOptions as a0, isInShadowDom as em, isModifiedClick as vu, isPopoverPolyfilled as Vn, inOverlay as o0, autofocusRef as hw, whenElementVisible as ny, whenDocumentVisible as c0 } from "./es/editor-CI6f7aKN.js";
import { configureLocalization as l0, e as d0, i as j, i$1 as nt, m as h0, b as u0, r as p0, S as y0, T as h, Z as w0, A as g, E as gu, b$1 as Bh, msg as x, localized as ut, str as gt } from "./es/locales/sv-SE-D-8dijzH.js";
const s0 = "esm/dynamic";
function Nh(s) {
  return Object.prototype.toString.call(s) === "[object Object]";
}
n(Nh, "isObject");
function Ze(s) {
  if (Nh(s) === !1) return !1;
  const t = s.constructor;
  if (t === void 0) return !0;
  const e = t.prototype;
  return !(Nh(e) === !1 || Object.prototype.hasOwnProperty.call(e, "isPrototypeOf") === !1);
}
n(Ze, "isPlainObject$1");
function ay(s) {
  return typeof s.toJSON == "function";
}
n(ay, "hasToJSON");
function f0(s) {
  return typeof s == "boolean" || typeof s == "number" || typeof s == "string" || s === null || Ze(s) || Array.isArray(s);
}
n(f0, "isJSONSerializable");
function Hn(s, t, e = !1) {
  s = s || {}, t = t || {};
  const i = {};
  for (const r in s)
    Object.prototype.hasOwnProperty.call(s, r) && (i[r] = s[r]);
  for (const r in t)
    Object.prototype.hasOwnProperty.call(t, r) && (e && i[r] && Ze(i[r]) && Ze(t[r]) ? i[r] = Hn(i[r], t[r], e) : i[r] = t[r]);
  return i;
}
n(Hn, "assign");
function uw(s) {
  return s ? Array.isArray(s) ? s : [s] : [];
}
n(uw, "asArray");
async function m0(s, t) {
  for (const e of s)
    if (await t(e))
      return e;
}
n(m0, "findAsyncSequential");
function zs(s, t, e = !1, i = !1) {
  if (!i && (!Ze(s) || !Ze(t)) || i && (!Nh(s) || !Nh(t)))
    return !1;
  const r = Object.getOwnPropertyNames(s), a = Object.getOwnPropertyNames(t);
  if (!e && r.length !== a.length)
    return !1;
  for (let o = 0; o < r.length; o++) {
    const c = r[o], l = s[c], u = t[c];
    if (l !== u && !zs(l, u, e))
      return !1;
  }
  return !0;
}
n(zs, "eqObjects");
function Pv(s) {
  return Object.entries(s);
}
n(Pv, "objectAsIterable");
function Qd(s, t = !1) {
  return Object.entries(s).reduce((e, [i, r]) => {
    let a = r;
    return Array.isArray(a) && (a = a.filter((o) => o), Array.isArray(a) && a.length === 0) || t && Ze(a) && (a = Qd(a, !0), Object.keys(a).length === 0) || a && (e[i] = a), e;
  }, {});
}
n(Qd, "cleanFalsyProperties");
function pw(s, t = "") {
  const e = [];
  for (const [i, r] of Object.entries(s)) {
    const a = t ? `${t}.${i}` : i;
    if (r != null)
      if (Array.isArray(r))
        for (const o of r)
          o != null && e.push([a, String(o)]);
      else Ze(r) ? e.push(...pw(r, a)) : e.push([a, String(r ?? "")]);
  }
  return e;
}
n(pw, "flattenToSearchParams");
function Sv(s) {
  return s.charAt(0).toUpperCase() + s.substring(1).toLowerCase();
}
n(Sv, "toUpperCaseFirst");
const v0 = '\\s,.:;"', g0 = new RegExp(`^|[${v0}]|$`, "g"), im = ".!?", b0 = new RegExp(`[^${im}]+[${im}]?`, "g");
function Mv(s) {
  return s.length > 0 ? s.replace(/([a-z\d])([A-Z]+)/g, "$1-$2").replace(/_|\s+/g, "-").toLowerCase() : s;
}
n(Mv, "toKebabCase");
function x0(s, t = 2) {
  if (!s)
    return null;
  let e = "";
  const i = s.split(g0).filter((r) => r);
  return i.length == 1 ? e = i[0] : i.forEach((r) => {
    e += r.charAt(0);
  }), e.substring(0, t).toUpperCase();
}
n(x0, "getInitials");
function Ev(s) {
  const t = s.match(b0);
  return t && t.length ? t[0].trim() : s;
}
n(Ev, "getTitleFromText");
function Av(s, t = 256) {
  return s.length > t && (s = s.substring(0, t - 1) + "…"), s;
}
n(Av, "truncateText");
const C0 = /^[\p{Extended_Pictographic}\p{Emoji_Presentation}]+$/u;
function $0(s) {
  return !!s?.match(C0)?.length;
}
n($0, "checkOnlyEmojis");
const k0 = /[\s]+/, _0 = {
  //mode: 'cors', // no-cors, *cors, same-origin
  // cache: 'default' means the server is in control of the caching which is preferred instead of using 'reload'
  //cache: 'default', // *default, no-cache, reload, force-cache, only-if-cached
  credentials: "omit",
  // include, *same-origin, omit
  headers: {
    // https://stackoverflow.com/questions/8163703/cross-domain-ajax-doesnt-send-x-requested-with-header
    "X-Requested-With": "XMLHttpRequest"
  },
  redirect: "manual"
  // manual, *follow, error
  //referrerPolicy: 'no-referrer-when-downgrade', // no-referrer, *no-referrer-when-downgrade, origin, origin-when-cross-origin, same-origin, strict-origin, strict-origin-when-cross-origin, unsafe-url
};
function P0() {
  const s = /* @__PURE__ */ new WeakSet(), t = /* @__PURE__ */ n(function(e, i) {
    if (ay(i) && (i = i.toJSON()), !(i !== null && typeof i == "object"))
      return i;
    if (s.has(i))
      return "[Circular]";
    let r;
    if (s.add(i), Ze(i)) {
      const a = {};
      Object.keys(i).forEach((o) => {
        a[o] = t(o, i[o]);
      }), r = a;
    } else Array.isArray(i) && (r = i.map((a, o) => t(o, a)));
    return s.delete(i), r;
  }, "replacer");
  return t;
}
n(P0, "getCircularReferenceReplacer");
function Bl() {
  return ((1 + Math.random()) * 65536 | 0).toString(16).substring(1);
}
n(Bl, "S4");
function S0(s) {
  if (s && s.ok && s.body) {
    const t = s.body.getReader();
    return new ReadableStream({
      start(e) {
        const i = /* @__PURE__ */ n(() => t.read().then(({ done: r, value: a }) => {
          if (r) {
            e.close();
            return;
          }
          return e.enqueue(a), i();
        }), "pump");
        return i();
      }
    });
  } else
    throw new Error("Could not parse text stream");
}
n(S0, "getTextStreamFromResponse");
function Rv(s) {
  let t;
  try {
    if (t = window[s], t) {
      const e = "__storage_test__";
      t.setItem(e, e), t.removeItem(e);
    }
  } catch (e) {
    e instanceof DOMException && e.name === "QuotaExceededError" && // acknowledge QuotaExceededError only if there's something already stored
    t && t.length !== 0 && console.error("Storage not available:", s);
  }
  return t;
}
n(Rv, "getStorage");
function cn(s) {
  return !!s;
}
n(cn, "onlyValues");
const rf = class rf extends Error {
  constructor() {
    super("Instance destroyed"), this.name = "DestroyError";
  }
};
n(rf, "DestroyError");
let Ut = rf;
const M0 = /* @__PURE__ */ n((s) => {
  var t;
  return t = class extends s {
    // AUTHENTICATION
    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    constructor(...i) {
      super(...i), this._whenTokenFactory = new Promise((r) => {
        this._resolveTokenFactory = r;
      }), this._whenUrlAndTokenFactory = new Promise((r) => {
        this._resolveUrlAndTokenFactory = (a) => {
          r(a), this.configurationState = "configured";
        };
      }), this._whenTokenIsValid = new Promise((r) => {
        this._resolveTokenIsValid = r;
      }), this._tokenPromise = null, this._token = "", this._validTokenFromFactory = async (r = !1) => {
        const a = [this.whenTokenFactory()];
        this.tokenFactoryRetryDelay >= 0 && this.tokenFactoryRetryDelay < 1 / 0 && a.push(new Promise((c) => setTimeout(c, this.tokenFactoryRetryDelay))), await Promise.race(a);
        const o = await this.tokenFactory?.(r) ?? "";
        if (this._validateToken(o)) {
          if (r && o === this._token && this.tokenFactoryRetryDelay >= 0 && this.tokenFactoryRetryDelay < 1 / 0)
            return await new Promise((c) => setTimeout(c, this.tokenFactoryRetryDelay)), await this._validTokenFromFactory(!0);
        } else if (this._whenTokenFactory = new Promise((c) => {
          this._resolveTokenFactory = c;
        }), !r)
          return await this._validTokenFromFactory(!1);
        if (!o)
          throw new TypeError("Could not get a valid token from tokenFactory.");
        return this._resolveTokenFactory?.(!0), this.whenUrl().then(this._resolveUrlAndTokenFactory), o;
      }, Promise.all([this.whenUrl(), this.whenTokenFactory()]).then(() => {
        this.url && this.tokenFactory && this._resolveUrlAndTokenFactory?.(!0);
      });
    }
    async whenTokenFactory() {
      await this._whenTokenFactory;
    }
    async whenUrlAndTokenFactory() {
      await this._whenUrlAndTokenFactory;
    }
    async whenTokenIsValid() {
      await this._whenTokenIsValid;
    }
    /**
     * Async function returning an `access_token` string for _your_ authenticated user. A boolean `refresh` parameter is provided to let you now if a fresh token is needed from Weavy.
     */
    get tokenFactory() {
      return this._tokenFactory;
    }
    set tokenFactory(i) {
      if (this.isDestroyed)
        throw new Ut();
      this._tokenFactory && this._tokenFactory !== i && this.whenTokenFactory().then(() => {
        this.queryClient.refetchQueries({ stale: !0 });
      }), this._tokenFactory = i ?? void 0, this._tokenFactory && queueMicrotask(() => {
        this._resolveTokenFactory?.(!0);
      });
    }
    /**
     * An URL to an endpoint returning an JSON data containing an `access_token` string property for _your_ authenticated user. A boolean `refresh=true` query parameter is provided in the request to let you now if when a fresh token is needed from Weavy.
     */
    get tokenUrl() {
      return this._tokenUrl;
    }
    set tokenUrl(i) {
      if (this.isDestroyed)
        throw new Ut();
      try {
        if (typeof i == "string")
          i && (this._tokenUrl = new URL(i, window.location.toString()));
        else if (i instanceof URL)
          this._tokenUrl = i;
        else if (i == null)
          this._tokenUrl = void 0;
        else
          throw new Error();
      } catch (r) {
        throw new Error("Invalid url", r);
      }
      this._tokenUrl && !this.tokenFactory && (this.tokenFactory = async (r) => {
        if (!this.tokenUrl)
          throw new Error("tokenURL property is not valid");
        const a = new URL(this.tokenUrl);
        r ? a.searchParams.set("refresh", "true") : a.searchParams.delete("refresh");
        const o = await fetch(a);
        if (o.ok) {
          const c = await o.json();
          if (c.access_token === void 0)
            throw new Error("Token response does not contain required property: access_token");
          return c.access_token;
        } else
          throw new Error("Could not get access token from server!");
      });
    }
    _validateToken(i) {
      if (!i)
        return !1;
      if (typeof i != "string")
        throw new TypeError(`You have provided an invalid string access token of type ${typeof i}.`);
      if (typeof i == "string" && !i.startsWith("wyu_"))
        throw i.startsWith("wys_") ? new TypeError("You have provided an API key for authentication. Provide a user access token instead.") : new TypeError("You have provided an invalid string as access token.");
      return this._resolveTokenIsValid?.(i), !0;
    }
    async getToken(i = !1) {
      if (this.isDestroyed)
        throw new Ut();
      if (this._token && !i)
        return this._token;
      if (this._tokenPromise)
        return await this._tokenPromise;
      this._tokenPromise = new Promise((r, a) => {
        this._validTokenFromFactory(i).then(r).catch(a), this.tokenFactoryTimeout >= 0 && this.tokenFactoryTimeout < 1 / 0 && setTimeout(() => a(new Error("Token factory timeout.")), this.tokenFactoryTimeout), window.addEventListener("offline", () => a(new Error("Network changed.")), { once: !0 }), window.addEventListener("online", () => a(new Error("Network changed.")), { once: !0 });
      });
      try {
        const r = await this._tokenPromise;
        return this._tokenPromise = null, this._token = r, this._token;
      } catch (r) {
        throw this._tokenPromise = null, r;
      }
    }
  }, n(t, "WeavyAuthentication"), t;
}, "WeavyAuthenticationMixin");
var bh = typeof globalThis < "u" ? globalThis : typeof window < "u" ? window : typeof global < "u" ? global : typeof self < "u" ? self : {};
function E0(s) {
  return s && s.__esModule && Object.prototype.hasOwnProperty.call(s, "default") ? s.default : s;
}
n(E0, "getDefaultExportFromCjs");
function Vp(s) {
  throw new Error('Could not dynamically require "' + s + '". Please configure the dynamicRequireTargets or/and ignoreDynamicRequires option of @rollup/plugin-commonjs appropriately for this require call to work.');
}
n(Vp, "commonjsRequire");
var Hp = { exports: {} }, rm;
function A0() {
  return rm || (rm = 1, (function(s, t) {
    var e;
    e = /* @__PURE__ */ n(() => (() => {
      var i = { d: /* @__PURE__ */ n((N, d) => {
        for (var w in d) i.o(d, w) && !i.o(N, w) && Object.defineProperty(N, w, { enumerable: !0, get: d[w] });
      }, "d") };
      i.g = (function() {
        if (typeof globalThis == "object") return globalThis;
        try {
          return this || new Function("return this")();
        } catch {
          if (typeof window == "object") return window;
        }
      })(), i.o = (N, d) => Object.prototype.hasOwnProperty.call(N, d), i.r = (N) => {
        typeof Symbol < "u" && Symbol.toStringTag && Object.defineProperty(N, Symbol.toStringTag, { value: "Module" }), Object.defineProperty(N, "t", { value: !0 });
      };
      var r, a = {};
      i.r(a), i.d(a, { AbortError: /* @__PURE__ */ n(() => l, "AbortError"), DefaultHttpClient: /* @__PURE__ */ n(() => At, "DefaultHttpClient"), HttpClient: /* @__PURE__ */ n(() => $, "HttpClient"), HttpError: /* @__PURE__ */ n(() => o, "HttpError"), HttpResponse: /* @__PURE__ */ n(() => m, "HttpResponse"), HttpTransportType: /* @__PURE__ */ n(() => Et, "HttpTransportType"), HubConnection: /* @__PURE__ */ n(() => qi, "HubConnection"), HubConnectionBuilder: /* @__PURE__ */ n(() => np, "HubConnectionBuilder"), HubConnectionState: /* @__PURE__ */ n(() => $t, "HubConnectionState"), JsonHubProtocol: /* @__PURE__ */ n(() => fh, "JsonHubProtocol"), LogLevel: /* @__PURE__ */ n(() => r, "LogLevel"), MessageType: /* @__PURE__ */ n(() => lt, "MessageType"), NullLogger: /* @__PURE__ */ n(() => E, "NullLogger"), Subject: /* @__PURE__ */ n(() => ci, "Subject"), TimeoutError: /* @__PURE__ */ n(() => c, "TimeoutError"), TransferFormat: /* @__PURE__ */ n(() => ie, "TransferFormat"), VERSION: /* @__PURE__ */ n(() => M, "VERSION") });
      const ap = class ap extends Error {
        constructor(d, w) {
          const k = new.target.prototype;
          super(`${d}: Status code '${w}'`), this.statusCode = w, this.__proto__ = k;
        }
      };
      n(ap, "i");
      let o = ap;
      const op = class op extends Error {
        constructor(d = "A timeout occurred.") {
          const w = new.target.prototype;
          super(d), this.__proto__ = w;
        }
      };
      n(op, "n");
      let c = op;
      const cp = class cp extends Error {
        constructor(d = "An abort occurred.") {
          const w = new.target.prototype;
          super(d), this.__proto__ = w;
        }
      };
      n(cp, "r");
      let l = cp;
      const lp = class lp extends Error {
        constructor(d, w) {
          const k = new.target.prototype;
          super(d), this.transport = w, this.errorType = "UnsupportedTransportError", this.__proto__ = k;
        }
      };
      n(lp, "o");
      let u = lp;
      const dp = class dp extends Error {
        constructor(d, w) {
          const k = new.target.prototype;
          super(d), this.transport = w, this.errorType = "DisabledTransportError", this.__proto__ = k;
        }
      };
      n(dp, "h");
      let b = dp;
      const hp = class hp extends Error {
        constructor(d, w) {
          const k = new.target.prototype;
          super(d), this.transport = w, this.errorType = "FailedToStartTransportError", this.__proto__ = k;
        }
      };
      n(hp, "c");
      let C = hp;
      const up = class up extends Error {
        constructor(d) {
          const w = new.target.prototype;
          super(d), this.errorType = "FailedToNegotiateWithServerError", this.__proto__ = w;
        }
      };
      n(up, "a");
      let v = up;
      const pp = class pp extends Error {
        constructor(d, w) {
          const k = new.target.prototype;
          super(d), this.innerErrors = w, this.__proto__ = k;
        }
      };
      n(pp, "l");
      let f = pp;
      const yp = class yp {
        constructor(d, w, k) {
          this.statusCode = d, this.statusText = w, this.content = k;
        }
      };
      n(yp, "u");
      let m = yp;
      const wp = class wp {
        get(d, w) {
          return this.send({ ...w, method: "GET", url: d });
        }
        post(d, w) {
          return this.send({ ...w, method: "POST", url: d });
        }
        delete(d, w) {
          return this.send({ ...w, method: "DELETE", url: d });
        }
        getCookieString(d) {
          return "";
        }
      };
      n(wp, "d");
      let $ = wp;
      (function(N) {
        N[N.Trace = 0] = "Trace", N[N.Debug = 1] = "Debug", N[N.Information = 2] = "Information", N[N.Warning = 3] = "Warning", N[N.Error = 4] = "Error", N[N.Critical = 5] = "Critical", N[N.None = 6] = "None";
      })(r || (r = {}));
      const fp = class fp {
        constructor() {
        }
        log(d, w) {
        }
      };
      n(fp, "f");
      let E = fp;
      E.instance = new E();
      const M = "9.0.6", mp = class mp {
        static isRequired(d, w) {
          if (d == null) throw new Error(`The '${w}' argument is required.`);
        }
        static isNotEmpty(d, w) {
          if (!d || d.match(/^\s*$/)) throw new Error(`The '${w}' argument should not be empty.`);
        }
        static isIn(d, w, k) {
          if (!(d in w)) throw new Error(`Unknown ${k} value: ${d}.`);
        }
      };
      n(mp, "w");
      let A = mp;
      const gn = class gn {
        static get isBrowser() {
          return !gn.isNode && typeof window == "object" && typeof window.document == "object";
        }
        static get isWebWorker() {
          return !gn.isNode && typeof self == "object" && "importScripts" in self;
        }
        static get isReactNative() {
          return !gn.isNode && typeof window == "object" && window.document === void 0;
        }
        static get isNode() {
          return typeof process < "u" && process.release && process.release.name === "node";
        }
      };
      n(gn, "g");
      let H = gn;
      function D(N, d) {
        let w = "";
        return I(N) ? (w = `Binary data of length ${N.byteLength}`, d && (w += `. Content: '${(function(k) {
          const S = new Uint8Array(k);
          let L = "";
          return S.forEach(((z) => {
            L += `0x${z < 16 ? "0" : ""}${z.toString(16)} `;
          })), L.substr(0, L.length - 1);
        })(N)}'`)) : typeof N == "string" && (w = `String data of length ${N.length}`, d && (w += `. Content: '${N}'`)), w;
      }
      n(D, "m");
      function I(N) {
        return N && typeof ArrayBuffer < "u" && (N instanceof ArrayBuffer || N.constructor && N.constructor.name === "ArrayBuffer");
      }
      n(I, "y");
      async function B(N, d, w, k, S, L) {
        const z = {}, [Z, X] = ct();
        z[Z] = X, N.log(r.Trace, `(${d} transport) sending data. ${D(S, L.logMessageContent)}.`);
        const ft = I(S) ? "arraybuffer" : "text", Rt = await w.post(k, { content: S, headers: { ...z, ...L.headers }, responseType: ft, timeout: L.timeout, withCredentials: L.withCredentials });
        N.log(r.Trace, `(${d} transport) request complete. Response status: ${Rt.statusCode}.`);
      }
      n(B, "b");
      const vp = class vp {
        constructor(d, w) {
          this.i = d, this.h = w;
        }
        dispose() {
          const d = this.i.observers.indexOf(this.h);
          d > -1 && this.i.observers.splice(d, 1), this.i.observers.length === 0 && this.i.cancelCallback && this.i.cancelCallback().catch(((w) => {
          }));
        }
      };
      n(vp, "v");
      let Q = vp;
      const gp = class gp {
        constructor(d) {
          this.l = d, this.out = console;
        }
        log(d, w) {
          if (d >= this.l) {
            const k = `[${(/* @__PURE__ */ new Date()).toISOString()}] ${r[d]}: ${w}`;
            switch (d) {
              case r.Critical:
              case r.Error:
                this.out.error(k);
                break;
              case r.Warning:
                this.out.warn(k);
                break;
              case r.Information:
                this.out.info(k);
                break;
              default:
                this.out.log(k);
            }
          }
        }
      };
      n(gp, "E");
      let it = gp;
      function ct() {
        let N = "X-SignalR-User-Agent";
        return H.isNode && (N = "User-Agent"), [N, st(M, Ot(), H.isNode ? "NodeJS" : "Browser", zt())];
      }
      n(ct, "$");
      function st(N, d, w, k) {
        let S = "Microsoft SignalR/";
        const L = N.split(".");
        return S += `${L[0]}.${L[1]}`, S += ` (${N}; `, S += d && d !== "" ? `${d}; ` : "Unknown OS; ", S += `${w}`, S += k ? `; ${k}` : "; Unknown Runtime Version", S += ")", S;
      }
      n(st, "C");
      function Ot() {
        if (!H.isNode) return "";
        switch (process.platform) {
          case "win32":
            return "Windows NT";
          case "darwin":
            return "macOS";
          case "linux":
            return "Linux";
          default:
            return process.platform;
        }
      }
      n(Ot, "S");
      function zt() {
        if (H.isNode) return process.versions.node;
      }
      n(zt, "k");
      function Mt(N) {
        return N.stack ? N.stack : N.message ? N.message : `${N}`;
      }
      n(Mt, "P");
      const bp = class bp extends $ {
        constructor(d) {
          if (super(), this.u = d, typeof fetch > "u" || H.isNode) {
            const w = Vp;
            this.p = new (w("tough-cookie")).CookieJar(), typeof fetch > "u" ? this.m = w("node-fetch") : this.m = fetch, this.m = w("fetch-cookie")(this.m, this.p);
          } else this.m = fetch.bind((function() {
            if (typeof globalThis < "u") return globalThis;
            if (typeof self < "u") return self;
            if (typeof window < "u") return window;
            if (i.g !== void 0) return i.g;
            throw new Error("could not find global");
          })());
          if (typeof AbortController > "u") {
            const w = Vp;
            this.v = w("abort-controller");
          } else this.v = AbortController;
        }
        async send(d) {
          if (d.abortSignal && d.abortSignal.aborted) throw new l();
          if (!d.method) throw new Error("No method defined.");
          if (!d.url) throw new Error("No url defined.");
          const w = new this.v();
          let k;
          d.abortSignal && (d.abortSignal.onabort = () => {
            w.abort(), k = new l();
          });
          let S, L = null;
          if (d.timeout) {
            const X = d.timeout;
            L = setTimeout((() => {
              w.abort(), this.u.log(r.Warning, "Timeout from HTTP request."), k = new c();
            }), X);
          }
          d.content === "" && (d.content = void 0), d.content && (d.headers = d.headers || {}, I(d.content) ? d.headers["Content-Type"] = "application/octet-stream" : d.headers["Content-Type"] = "text/plain;charset=UTF-8");
          try {
            S = await this.m(d.url, { body: d.content, cache: "no-cache", credentials: d.withCredentials === !0 ? "include" : "same-origin", headers: { "X-Requested-With": "XMLHttpRequest", ...d.headers }, method: d.method, mode: "cors", redirect: "follow", signal: w.signal });
          } catch (X) {
            throw k || (this.u.log(r.Warning, `Error from HTTP request. ${X}.`), X);
          } finally {
            L && clearTimeout(L), d.abortSignal && (d.abortSignal.onabort = null);
          }
          if (!S.ok) {
            const X = await Dt(S, "text");
            throw new o(X || S.statusText, S.status);
          }
          const z = Dt(S, d.responseType), Z = await z;
          return new m(S.status, S.statusText, Z);
        }
        getCookieString(d) {
          let w = "";
          return H.isNode && this.p && this.p.getCookies(d, ((k, S) => w = S.join("; "))), w;
        }
      };
      n(bp, "T");
      let vt = bp;
      function Dt(N, d) {
        let w;
        switch (d) {
          case "arraybuffer":
            w = N.arrayBuffer();
            break;
          case "text":
          default:
            w = N.text();
            break;
          case "blob":
          case "document":
          case "json":
            throw new Error(`${d} is not supported.`);
        }
        return w;
      }
      n(Dt, "I");
      const xp = class xp extends $ {
        constructor(d) {
          super(), this.u = d;
        }
        send(d) {
          return d.abortSignal && d.abortSignal.aborted ? Promise.reject(new l()) : d.method ? d.url ? new Promise(((w, k) => {
            const S = new XMLHttpRequest();
            S.open(d.method, d.url, !0), S.withCredentials = d.withCredentials === void 0 || d.withCredentials, S.setRequestHeader("X-Requested-With", "XMLHttpRequest"), d.content === "" && (d.content = void 0), d.content && (I(d.content) ? S.setRequestHeader("Content-Type", "application/octet-stream") : S.setRequestHeader("Content-Type", "text/plain;charset=UTF-8"));
            const L = d.headers;
            L && Object.keys(L).forEach(((z) => {
              S.setRequestHeader(z, L[z]);
            })), d.responseType && (S.responseType = d.responseType), d.abortSignal && (d.abortSignal.onabort = () => {
              S.abort(), k(new l());
            }), d.timeout && (S.timeout = d.timeout), S.onload = () => {
              d.abortSignal && (d.abortSignal.onabort = null), S.status >= 200 && S.status < 300 ? w(new m(S.status, S.statusText, S.response || S.responseText)) : k(new o(S.response || S.responseText || S.statusText, S.status));
            }, S.onerror = () => {
              this.u.log(r.Warning, `Error from HTTP request. ${S.status}: ${S.statusText}.`), k(new o(S.statusText, S.status));
            }, S.ontimeout = () => {
              this.u.log(r.Warning, "Timeout from HTTP request."), k(new c());
            }, S.send(d.content);
          })) : Promise.reject(new Error("No url defined.")) : Promise.reject(new Error("No method defined."));
        }
      };
      n(xp, "_");
      let ge = xp;
      const Cp = class Cp extends $ {
        constructor(d) {
          if (super(), typeof fetch < "u" || H.isNode) this.$ = new vt(d);
          else {
            if (typeof XMLHttpRequest > "u") throw new Error("No usable HttpClient found.");
            this.$ = new ge(d);
          }
        }
        send(d) {
          return d.abortSignal && d.abortSignal.aborted ? Promise.reject(new l()) : d.method ? d.url ? this.$.send(d) : Promise.reject(new Error("No url defined.")) : Promise.reject(new Error("No method defined."));
        }
        getCookieString(d) {
          return this.$.getCookieString(d);
        }
      };
      n(Cp, "H");
      let At = Cp;
      const bn = class bn {
        static write(d) {
          return `${d}${bn.RecordSeparator}`;
        }
        static parse(d) {
          if (d[d.length - 1] !== bn.RecordSeparator) throw new Error("Message is incomplete.");
          const w = d.split(bn.RecordSeparator);
          return w.pop(), w;
        }
      };
      n(bn, "D");
      let xt = bn;
      xt.RecordSeparatorCode = 30, xt.RecordSeparator = String.fromCharCode(xt.RecordSeparatorCode);
      const $p = class $p {
        writeHandshakeRequest(d) {
          return xt.write(JSON.stringify(d));
        }
        parseHandshakeResponse(d) {
          let w, k;
          if (I(d)) {
            const z = new Uint8Array(d), Z = z.indexOf(xt.RecordSeparatorCode);
            if (Z === -1) throw new Error("Message is incomplete.");
            const X = Z + 1;
            w = String.fromCharCode.apply(null, Array.prototype.slice.call(z.slice(0, X))), k = z.byteLength > X ? z.slice(X).buffer : null;
          } else {
            const z = d, Z = z.indexOf(xt.RecordSeparator);
            if (Z === -1) throw new Error("Message is incomplete.");
            const X = Z + 1;
            w = z.substring(0, X), k = z.length > X ? z.substring(X) : null;
          }
          const S = xt.parse(w), L = JSON.parse(S[0]);
          if (L.type) throw new Error("Expected a handshake response from the server.");
          return [k, L];
        }
      };
      n($p, "R");
      let ee = $p;
      var lt, $t;
      (function(N) {
        N[N.Invocation = 1] = "Invocation", N[N.StreamItem = 2] = "StreamItem", N[N.Completion = 3] = "Completion", N[N.StreamInvocation = 4] = "StreamInvocation", N[N.CancelInvocation = 5] = "CancelInvocation", N[N.Ping = 6] = "Ping", N[N.Close = 7] = "Close", N[N.Ack = 8] = "Ack", N[N.Sequence = 9] = "Sequence";
      })(lt || (lt = {}));
      const kp = class kp {
        constructor() {
          this.observers = [];
        }
        next(d) {
          for (const w of this.observers) w.next(d);
        }
        error(d) {
          for (const w of this.observers) w.error && w.error(d);
        }
        complete() {
          for (const d of this.observers) d.complete && d.complete();
        }
        subscribe(d) {
          return this.observers.push(d), new Q(this, d);
        }
      };
      n(kp, "U");
      let ci = kp;
      const _p = class _p {
        constructor(d, w, k) {
          this.C = 1e5, this.S = [], this.k = 0, this.P = !1, this.T = 1, this.I = 0, this._ = 0, this.H = !1, this.D = d, this.R = w, this.C = k;
        }
        async A(d) {
          const w = this.D.writeMessage(d);
          let k = Promise.resolve();
          if (this.U(d)) {
            this.k++;
            let S = /* @__PURE__ */ n(() => {
            }, "t"), L = /* @__PURE__ */ n(() => {
            }, "i");
            I(w) ? this._ += w.byteLength : this._ += w.length, this._ >= this.C && (k = new Promise(((z, Z) => {
              S = z, L = Z;
            }))), this.S.push(new Ni(w, this.k, S, L));
          }
          try {
            this.H || await this.R.send(w);
          } catch {
            this.L();
          }
          await k;
        }
        N(d) {
          let w = -1;
          for (let k = 0; k < this.S.length; k++) {
            const S = this.S[k];
            if (S.q <= d.sequenceId) w = k, I(S.M) ? this._ -= S.M.byteLength : this._ -= S.M.length, S.j();
            else {
              if (!(this._ < this.C)) break;
              S.j();
            }
          }
          w !== -1 && (this.S = this.S.slice(w + 1));
        }
        W(d) {
          if (this.P) return d.type === lt.Sequence && (this.P = !1, !0);
          if (!this.U(d)) return !0;
          const w = this.T;
          return this.T++, w <= this.I ? (w === this.I && this.O(), !1) : (this.I = w, this.O(), !0);
        }
        F(d) {
          d.sequenceId > this.T ? this.R.stop(new Error("Sequence ID greater than amount of messages we've received.")) : this.T = d.sequenceId;
        }
        L() {
          this.H = !0, this.P = !0;
        }
        async B() {
          const d = this.S.length !== 0 ? this.S[0].q : this.k + 1;
          await this.R.send(this.D.writeMessage({ type: lt.Sequence, sequenceId: d }));
          const w = this.S;
          for (const k of w) await this.R.send(k.M);
          this.H = !1;
        }
        X(d) {
          d != null || (d = new Error("Unable to reconnect to server."));
          for (const w of this.S) w.J(d);
        }
        U(d) {
          switch (d.type) {
            case lt.Invocation:
            case lt.StreamItem:
            case lt.Completion:
            case lt.StreamInvocation:
            case lt.CancelInvocation:
              return !0;
            case lt.Close:
            case lt.Sequence:
            case lt.Ping:
            case lt.Ack:
              return !1;
          }
        }
        O() {
          this.V === void 0 && (this.V = setTimeout((async () => {
            try {
              this.H || await this.R.send(this.D.writeMessage({ type: lt.Ack, sequenceId: this.I }));
            } catch {
            }
            clearTimeout(this.V), this.V = void 0;
          }), 1e3));
        }
      };
      n(_p, "L");
      let Ke = _p;
      const Pp = class Pp {
        constructor(d, w, k, S) {
          this.M = d, this.q = w, this.j = k, this.J = S;
        }
      };
      n(Pp, "N");
      let Ni = Pp;
      (function(N) {
        N.Disconnected = "Disconnected", N.Connecting = "Connecting", N.Connected = "Connected", N.Disconnecting = "Disconnecting", N.Reconnecting = "Reconnecting";
      })($t || ($t = {}));
      const mh = class mh {
        static create(d, w, k, S, L, z, Z) {
          return new mh(d, w, k, S, L, z, Z);
        }
        constructor(d, w, k, S, L, z, Z) {
          this.K = 0, this.G = () => {
            this.u.log(r.Warning, "The page is being frozen, this will likely lead to the connection being closed and messages being lost. For more information see the docs at https://learn.microsoft.com/aspnet/core/signalr/javascript-client#bsleep");
          }, A.isRequired(d, "connection"), A.isRequired(w, "logger"), A.isRequired(k, "protocol"), this.serverTimeoutInMilliseconds = L ?? 3e4, this.keepAliveIntervalInMilliseconds = z ?? 15e3, this.Y = Z ?? 1e5, this.u = w, this.D = k, this.connection = d, this.Z = S, this.tt = new ee(), this.connection.onreceive = (X) => this.et(X), this.connection.onclose = (X) => this.st(X), this.it = {}, this.nt = {}, this.rt = [], this.ot = [], this.ht = [], this.ct = 0, this.lt = !1, this.ut = $t.Disconnected, this.dt = !1, this.ft = this.D.writeMessage({ type: lt.Ping });
        }
        get state() {
          return this.ut;
        }
        get connectionId() {
          return this.connection && this.connection.connectionId || null;
        }
        get baseUrl() {
          return this.connection.baseUrl || "";
        }
        set baseUrl(d) {
          if (this.ut !== $t.Disconnected && this.ut !== $t.Reconnecting) throw new Error("The HubConnection must be in the Disconnected or Reconnecting state to change the url.");
          if (!d) throw new Error("The HubConnection url must be a valid url.");
          this.connection.baseUrl = d;
        }
        start() {
          return this.wt = this.gt(), this.wt;
        }
        async gt() {
          if (this.ut !== $t.Disconnected) return Promise.reject(new Error("Cannot start a HubConnection that is not in the 'Disconnected' state."));
          this.ut = $t.Connecting, this.u.log(r.Debug, "Starting HubConnection.");
          try {
            await this.yt(), H.isBrowser && window.document.addEventListener("freeze", this.G), this.ut = $t.Connected, this.dt = !0, this.u.log(r.Debug, "HubConnection connected successfully.");
          } catch (d) {
            return this.ut = $t.Disconnected, this.u.log(r.Debug, `HubConnection failed to start successfully because of error '${d}'.`), Promise.reject(d);
          }
        }
        async yt() {
          this.bt = void 0, this.lt = !1;
          const d = new Promise(((w, k) => {
            this.vt = w, this.Et = k;
          }));
          await this.connection.start(this.D.transferFormat);
          try {
            let w = this.D.version;
            this.connection.features.reconnect || (w = 1);
            const k = { protocol: this.D.name, version: w };
            if (this.u.log(r.Debug, "Sending handshake request."), await this.$t(this.tt.writeHandshakeRequest(k)), this.u.log(r.Information, `Using HubProtocol '${this.D.name}'.`), this.Ct(), this.St(), this.kt(), await d, this.bt) throw this.bt;
            this.connection.features.reconnect && (this.Pt = new Ke(this.D, this.connection, this.Y), this.connection.features.disconnected = this.Pt.L.bind(this.Pt), this.connection.features.resend = () => {
              if (this.Pt) return this.Pt.B();
            }), this.connection.features.inherentKeepAlive || await this.$t(this.ft);
          } catch (w) {
            throw this.u.log(r.Debug, `Hub handshake failed with error '${w}' during start(). Stopping HubConnection.`), this.Ct(), this.Tt(), await this.connection.stop(w), w;
          }
        }
        async stop() {
          const d = this.wt;
          this.connection.features.reconnect = !1, this.It = this._t(), await this.It;
          try {
            await d;
          } catch {
          }
        }
        _t(d) {
          if (this.ut === $t.Disconnected) return this.u.log(r.Debug, `Call to HubConnection.stop(${d}) ignored because it is already in the disconnected state.`), Promise.resolve();
          if (this.ut === $t.Disconnecting) return this.u.log(r.Debug, `Call to HttpConnection.stop(${d}) ignored because the connection is already in the disconnecting state.`), this.It;
          const w = this.ut;
          return this.ut = $t.Disconnecting, this.u.log(r.Debug, "Stopping HubConnection."), this.Ht ? (this.u.log(r.Debug, "Connection stopped during reconnect delay. Done reconnecting."), clearTimeout(this.Ht), this.Ht = void 0, this.Dt(), Promise.resolve()) : (w === $t.Connected && this.Rt(), this.Ct(), this.Tt(), this.bt = d || new l("The connection was stopped before the hub handshake could complete."), this.connection.stop(d));
        }
        async Rt() {
          try {
            await this.xt(this.At());
          } catch {
          }
        }
        stream(d, ...w) {
          const [k, S] = this.Ut(w), L = this.Lt(d, w, S);
          let z;
          const Z = new ci();
          return Z.cancelCallback = () => {
            const X = this.Nt(L.invocationId);
            return delete this.it[L.invocationId], z.then((() => this.xt(X)));
          }, this.it[L.invocationId] = (X, ft) => {
            ft ? Z.error(ft) : X && (X.type === lt.Completion ? X.error ? Z.error(new Error(X.error)) : Z.complete() : Z.next(X.item));
          }, z = this.xt(L).catch(((X) => {
            Z.error(X), delete this.it[L.invocationId];
          })), this.qt(k, z), Z;
        }
        $t(d) {
          return this.kt(), this.connection.send(d);
        }
        xt(d) {
          return this.Pt ? this.Pt.A(d) : this.$t(this.D.writeMessage(d));
        }
        send(d, ...w) {
          const [k, S] = this.Ut(w), L = this.xt(this.Mt(d, w, !0, S));
          return this.qt(k, L), L;
        }
        invoke(d, ...w) {
          const [k, S] = this.Ut(w), L = this.Mt(d, w, !1, S);
          return new Promise(((z, Z) => {
            this.it[L.invocationId] = (ft, Rt) => {
              Rt ? Z(Rt) : ft && (ft.type === lt.Completion ? ft.error ? Z(new Error(ft.error)) : z(ft.result) : Z(new Error(`Unexpected message type: ${ft.type}`)));
            };
            const X = this.xt(L).catch(((ft) => {
              Z(ft), delete this.it[L.invocationId];
            }));
            this.qt(k, X);
          }));
        }
        on(d, w) {
          d && w && (d = d.toLowerCase(), this.nt[d] || (this.nt[d] = []), this.nt[d].indexOf(w) === -1 && this.nt[d].push(w));
        }
        off(d, w) {
          if (!d) return;
          d = d.toLowerCase();
          const k = this.nt[d];
          if (k) if (w) {
            const S = k.indexOf(w);
            S !== -1 && (k.splice(S, 1), k.length === 0 && delete this.nt[d]);
          } else delete this.nt[d];
        }
        onclose(d) {
          d && this.rt.push(d);
        }
        onreconnecting(d) {
          d && this.ot.push(d);
        }
        onreconnected(d) {
          d && this.ht.push(d);
        }
        et(d) {
          if (this.Ct(), this.lt || (d = this.jt(d), this.lt = !0), d) {
            const w = this.D.parseMessages(d, this.u);
            for (const k of w) if (!this.Pt || this.Pt.W(k)) switch (k.type) {
              case lt.Invocation:
                this.Wt(k).catch(((S) => {
                  this.u.log(r.Error, `Invoke client method threw error: ${Mt(S)}`);
                }));
                break;
              case lt.StreamItem:
              case lt.Completion: {
                const S = this.it[k.invocationId];
                if (S) {
                  k.type === lt.Completion && delete this.it[k.invocationId];
                  try {
                    S(k);
                  } catch (L) {
                    this.u.log(r.Error, `Stream callback threw error: ${Mt(L)}`);
                  }
                }
                break;
              }
              case lt.Ping:
                break;
              case lt.Close: {
                this.u.log(r.Information, "Close message received from server.");
                const S = k.error ? new Error("Server returned an error on close: " + k.error) : void 0;
                k.allowReconnect === !0 ? this.connection.stop(S) : this.It = this._t(S);
                break;
              }
              case lt.Ack:
                this.Pt && this.Pt.N(k);
                break;
              case lt.Sequence:
                this.Pt && this.Pt.F(k);
                break;
              default:
                this.u.log(r.Warning, `Invalid message type: ${k.type}.`);
            }
          }
          this.St();
        }
        jt(d) {
          let w, k;
          try {
            [k, w] = this.tt.parseHandshakeResponse(d);
          } catch (S) {
            const L = "Error parsing handshake response: " + S;
            this.u.log(r.Error, L);
            const z = new Error(L);
            throw this.Et(z), z;
          }
          if (w.error) {
            const S = "Server returned handshake error: " + w.error;
            this.u.log(r.Error, S);
            const L = new Error(S);
            throw this.Et(L), L;
          }
          return this.u.log(r.Debug, "Server handshake complete."), this.vt(), k;
        }
        kt() {
          this.connection.features.inherentKeepAlive || (this.K = (/* @__PURE__ */ new Date()).getTime() + this.keepAliveIntervalInMilliseconds, this.Tt());
        }
        St() {
          if (!(this.connection.features && this.connection.features.inherentKeepAlive || (this.Ot = setTimeout((() => this.serverTimeout()), this.serverTimeoutInMilliseconds), this.Ft !== void 0))) {
            let d = this.K - (/* @__PURE__ */ new Date()).getTime();
            d < 0 && (d = 0), this.Ft = setTimeout((async () => {
              if (this.ut === $t.Connected) try {
                await this.$t(this.ft);
              } catch {
                this.Tt();
              }
            }), d);
          }
        }
        serverTimeout() {
          this.connection.stop(new Error("Server timeout elapsed without receiving a message from the server."));
        }
        async Wt(d) {
          const w = d.target.toLowerCase(), k = this.nt[w];
          if (!k) return this.u.log(r.Warning, `No client method with the name '${w}' found.`), void (d.invocationId && (this.u.log(r.Warning, `No result given for '${w}' method and invocation ID '${d.invocationId}'.`), await this.xt(this.Bt(d.invocationId, "Client didn't provide a result.", null))));
          const S = k.slice(), L = !!d.invocationId;
          let z, Z, X;
          for (const ft of S) try {
            const Rt = z;
            z = await ft.apply(this, d.arguments), L && z && Rt && (this.u.log(r.Error, `Multiple results provided for '${w}'. Sending error to server.`), X = this.Bt(d.invocationId, "Client provided multiple results.", null)), Z = void 0;
          } catch (Rt) {
            Z = Rt, this.u.log(r.Error, `A callback for the method '${w}' threw error '${Rt}'.`);
          }
          X ? await this.xt(X) : L ? (Z ? X = this.Bt(d.invocationId, `${Z}`, null) : z !== void 0 ? X = this.Bt(d.invocationId, null, z) : (this.u.log(r.Warning, `No result given for '${w}' method and invocation ID '${d.invocationId}'.`), X = this.Bt(d.invocationId, "Client didn't provide a result.", null)), await this.xt(X)) : z && this.u.log(r.Error, `Result given for '${w}' method but server is not expecting a result.`);
        }
        st(d) {
          this.u.log(r.Debug, `HubConnection.connectionClosed(${d}) called while in state ${this.ut}.`), this.bt = this.bt || d || new l("The underlying connection was closed before the hub handshake could complete."), this.vt && this.vt(), this.Xt(d || new Error("Invocation canceled due to the underlying connection being closed.")), this.Ct(), this.Tt(), this.ut === $t.Disconnecting ? this.Dt(d) : this.ut === $t.Connected && this.Z ? this.Jt(d) : this.ut === $t.Connected && this.Dt(d);
        }
        Dt(d) {
          if (this.dt) {
            this.ut = $t.Disconnected, this.dt = !1, this.Pt && (this.Pt.X(d ?? new Error("Connection closed.")), this.Pt = void 0), H.isBrowser && window.document.removeEventListener("freeze", this.G);
            try {
              this.rt.forEach(((w) => w.apply(this, [d])));
            } catch (w) {
              this.u.log(r.Error, `An onclose callback called with error '${d}' threw error '${w}'.`);
            }
          }
        }
        async Jt(d) {
          const w = Date.now();
          let k = 0, S = d !== void 0 ? d : new Error("Attempting to reconnect due to a unknown error."), L = this.zt(k++, 0, S);
          if (L === null) return this.u.log(r.Debug, "Connection not reconnecting because the IRetryPolicy returned null on the first reconnect attempt."), void this.Dt(d);
          if (this.ut = $t.Reconnecting, d ? this.u.log(r.Information, `Connection reconnecting because of error '${d}'.`) : this.u.log(r.Information, "Connection reconnecting."), this.ot.length !== 0) {
            try {
              this.ot.forEach(((z) => z.apply(this, [d])));
            } catch (z) {
              this.u.log(r.Error, `An onreconnecting callback called with error '${d}' threw error '${z}'.`);
            }
            if (this.ut !== $t.Reconnecting) return void this.u.log(r.Debug, "Connection left the reconnecting state in onreconnecting callback. Done reconnecting.");
          }
          for (; L !== null; ) {
            if (this.u.log(r.Information, `Reconnect attempt number ${k} will start in ${L} ms.`), await new Promise(((z) => {
              this.Ht = setTimeout(z, L);
            })), this.Ht = void 0, this.ut !== $t.Reconnecting) return void this.u.log(r.Debug, "Connection left the reconnecting state during reconnect delay. Done reconnecting.");
            try {
              if (await this.yt(), this.ut = $t.Connected, this.u.log(r.Information, "HubConnection reconnected successfully."), this.ht.length !== 0) try {
                this.ht.forEach(((z) => z.apply(this, [this.connection.connectionId])));
              } catch (z) {
                this.u.log(r.Error, `An onreconnected callback called with connectionId '${this.connection.connectionId}; threw error '${z}'.`);
              }
              return;
            } catch (z) {
              if (this.u.log(r.Information, `Reconnect attempt failed because of error '${z}'.`), this.ut !== $t.Reconnecting) return this.u.log(r.Debug, `Connection moved to the '${this.ut}' from the reconnecting state during reconnect attempt. Done reconnecting.`), void (this.ut === $t.Disconnecting && this.Dt());
              S = z instanceof Error ? z : new Error(z.toString()), L = this.zt(k++, Date.now() - w, S);
            }
          }
          this.u.log(r.Information, `Reconnect retries have been exhausted after ${Date.now() - w} ms and ${k} failed attempts. Connection disconnecting.`), this.Dt();
        }
        zt(d, w, k) {
          try {
            return this.Z.nextRetryDelayInMilliseconds({ elapsedMilliseconds: w, previousRetryCount: d, retryReason: k });
          } catch (S) {
            return this.u.log(r.Error, `IRetryPolicy.nextRetryDelayInMilliseconds(${d}, ${w}) threw error '${S}'.`), null;
          }
        }
        Xt(d) {
          const w = this.it;
          this.it = {}, Object.keys(w).forEach(((k) => {
            const S = w[k];
            try {
              S(null, d);
            } catch (L) {
              this.u.log(r.Error, `Stream 'error' callback called with '${d}' threw error: ${Mt(L)}`);
            }
          }));
        }
        Tt() {
          this.Ft && (clearTimeout(this.Ft), this.Ft = void 0);
        }
        Ct() {
          this.Ot && clearTimeout(this.Ot);
        }
        Mt(d, w, k, S) {
          if (k) return S.length !== 0 ? { target: d, arguments: w, streamIds: S, type: lt.Invocation } : { target: d, arguments: w, type: lt.Invocation };
          {
            const L = this.ct;
            return this.ct++, S.length !== 0 ? { target: d, arguments: w, invocationId: L.toString(), streamIds: S, type: lt.Invocation } : { target: d, arguments: w, invocationId: L.toString(), type: lt.Invocation };
          }
        }
        qt(d, w) {
          if (d.length !== 0) {
            w || (w = Promise.resolve());
            for (const k in d) d[k].subscribe({ complete: /* @__PURE__ */ n(() => {
              w = w.then((() => this.xt(this.Bt(k))));
            }, "complete"), error: /* @__PURE__ */ n((S) => {
              let L;
              L = S instanceof Error ? S.message : S && S.toString ? S.toString() : "Unknown error", w = w.then((() => this.xt(this.Bt(k, L))));
            }, "error"), next: /* @__PURE__ */ n((S) => {
              w = w.then((() => this.xt(this.Vt(k, S))));
            }, "next") });
          }
        }
        Ut(d) {
          const w = [], k = [];
          for (let S = 0; S < d.length; S++) {
            const L = d[S];
            if (this.Kt(L)) {
              const z = this.ct;
              this.ct++, w[z] = L, k.push(z.toString()), d.splice(S, 1);
            }
          }
          return [w, k];
        }
        Kt(d) {
          return d && d.subscribe && typeof d.subscribe == "function";
        }
        Lt(d, w, k) {
          const S = this.ct;
          return this.ct++, k.length !== 0 ? { target: d, arguments: w, invocationId: S.toString(), streamIds: k, type: lt.StreamInvocation } : { target: d, arguments: w, invocationId: S.toString(), type: lt.StreamInvocation };
        }
        Nt(d) {
          return { invocationId: d, type: lt.CancelInvocation };
        }
        Vt(d, w) {
          return { invocationId: d, item: w, type: lt.StreamItem };
        }
        Bt(d, w, k) {
          return w ? { error: w, invocationId: d, type: lt.Completion } : { invocationId: d, result: k, type: lt.Completion };
        }
        At() {
          return { type: lt.Close };
        }
      };
      n(mh, "q");
      let qi = mh;
      const El = [0, 2e3, 1e4, 3e4, null], Sp = class Sp {
        constructor(d) {
          this.Gt = d !== void 0 ? [...d, null] : El;
        }
        nextRetryDelayInMilliseconds(d) {
          return this.Gt[d.previousRetryCount];
        }
      };
      n(Sp, "j");
      let Ci = Sp;
      const Mp = class Mp {
      };
      n(Mp, "W");
      let Ct = Mp;
      Ct.Authorization = "Authorization", Ct.Cookie = "Cookie";
      const Ep = class Ep extends $ {
        constructor(d, w) {
          super(), this.Qt = d, this.Yt = w;
        }
        async send(d) {
          let w = !0;
          this.Yt && (!this.Zt || d.url && d.url.indexOf("/negotiate?") > 0) && (w = !1, this.Zt = await this.Yt()), this.te(d);
          const k = await this.Qt.send(d);
          return w && k.statusCode === 401 && this.Yt ? (this.Zt = await this.Yt(), this.te(d), await this.Qt.send(d)) : k;
        }
        te(d) {
          d.headers || (d.headers = {}), this.Zt ? d.headers[Ct.Authorization] = `Bearer ${this.Zt}` : this.Yt && d.headers[Ct.Authorization] && delete d.headers[Ct.Authorization];
        }
        getCookieString(d) {
          return this.Qt.getCookieString(d);
        }
      };
      n(Ep, "O");
      let ze = Ep;
      var Et, ie;
      (function(N) {
        N[N.None = 0] = "None", N[N.WebSockets = 1] = "WebSockets", N[N.ServerSentEvents = 2] = "ServerSentEvents", N[N.LongPolling = 4] = "LongPolling";
      })(Et || (Et = {})), (function(N) {
        N[N.Text = 1] = "Text", N[N.Binary = 2] = "Binary";
      })(ie || (ie = {}));
      const Ap = class Ap {
        constructor() {
          this.ee = !1, this.onabort = null;
        }
        abort() {
          this.ee || (this.ee = !0, this.onabort && this.onabort());
        }
        get signal() {
          return this;
        }
        get aborted() {
          return this.ee;
        }
      };
      n(Ap, "X");
      let tp = Ap;
      const Rp = class Rp {
        get pollAborted() {
          return this.se.aborted;
        }
        constructor(d, w, k) {
          this.$ = d, this.u = w, this.se = new tp(), this.ie = k, this.ne = !1, this.onreceive = null, this.onclose = null;
        }
        async connect(d, w) {
          if (A.isRequired(d, "url"), A.isRequired(w, "transferFormat"), A.isIn(w, ie, "transferFormat"), this.re = d, this.u.log(r.Trace, "(LongPolling transport) Connecting."), w === ie.Binary && typeof XMLHttpRequest < "u" && typeof new XMLHttpRequest().responseType != "string") throw new Error("Binary protocols over XmlHttpRequest not implementing advanced features are not supported.");
          const [k, S] = ct(), L = { [k]: S, ...this.ie.headers }, z = { abortSignal: this.se.signal, headers: L, timeout: 1e5, withCredentials: this.ie.withCredentials };
          w === ie.Binary && (z.responseType = "arraybuffer");
          const Z = `${d}&_=${Date.now()}`;
          this.u.log(r.Trace, `(LongPolling transport) polling: ${Z}.`);
          const X = await this.$.get(Z, z);
          X.statusCode !== 200 ? (this.u.log(r.Error, `(LongPolling transport) Unexpected response code: ${X.statusCode}.`), this.oe = new o(X.statusText || "", X.statusCode), this.ne = !1) : this.ne = !0, this.he = this.ce(this.re, z);
        }
        async ce(d, w) {
          try {
            for (; this.ne; ) try {
              const k = `${d}&_=${Date.now()}`;
              this.u.log(r.Trace, `(LongPolling transport) polling: ${k}.`);
              const S = await this.$.get(k, w);
              S.statusCode === 204 ? (this.u.log(r.Information, "(LongPolling transport) Poll terminated by server."), this.ne = !1) : S.statusCode !== 200 ? (this.u.log(r.Error, `(LongPolling transport) Unexpected response code: ${S.statusCode}.`), this.oe = new o(S.statusText || "", S.statusCode), this.ne = !1) : S.content ? (this.u.log(r.Trace, `(LongPolling transport) data received. ${D(S.content, this.ie.logMessageContent)}.`), this.onreceive && this.onreceive(S.content)) : this.u.log(r.Trace, "(LongPolling transport) Poll timed out, reissuing.");
            } catch (k) {
              this.ne ? k instanceof c ? this.u.log(r.Trace, "(LongPolling transport) Poll timed out, reissuing.") : (this.oe = k, this.ne = !1) : this.u.log(r.Trace, `(LongPolling transport) Poll errored after shutdown: ${k.message}`);
            }
          } finally {
            this.u.log(r.Trace, "(LongPolling transport) Polling complete."), this.pollAborted || this.ae();
          }
        }
        async send(d) {
          return this.ne ? B(this.u, "LongPolling", this.$, this.re, d, this.ie) : Promise.reject(new Error("Cannot send until the transport is connected"));
        }
        async stop() {
          this.u.log(r.Trace, "(LongPolling transport) Stopping polling."), this.ne = !1, this.se.abort();
          try {
            await this.he, this.u.log(r.Trace, `(LongPolling transport) sending DELETE request to ${this.re}.`);
            const d = {}, [w, k] = ct();
            d[w] = k;
            const S = { headers: { ...d, ...this.ie.headers }, timeout: this.ie.timeout, withCredentials: this.ie.withCredentials };
            let L;
            try {
              await this.$.delete(this.re, S);
            } catch (z) {
              L = z;
            }
            L ? L instanceof o && (L.statusCode === 404 ? this.u.log(r.Trace, "(LongPolling transport) A 404 response was returned from sending a DELETE request.") : this.u.log(r.Trace, `(LongPolling transport) Error sending a DELETE request: ${L}`)) : this.u.log(r.Trace, "(LongPolling transport) DELETE request accepted.");
          } finally {
            this.u.log(r.Trace, "(LongPolling transport) Stop finished."), this.ae();
          }
        }
        ae() {
          if (this.onclose) {
            let d = "(LongPolling transport) Firing onclose event.";
            this.oe && (d += " Error: " + this.oe), this.u.log(r.Trace, d), this.onclose(this.oe);
          }
        }
      };
      n(Rp, "J");
      let wh = Rp;
      const Tp = class Tp {
        constructor(d, w, k, S) {
          this.$ = d, this.Zt = w, this.u = k, this.ie = S, this.onreceive = null, this.onclose = null;
        }
        async connect(d, w) {
          return A.isRequired(d, "url"), A.isRequired(w, "transferFormat"), A.isIn(w, ie, "transferFormat"), this.u.log(r.Trace, "(SSE transport) Connecting."), this.re = d, this.Zt && (d += (d.indexOf("?") < 0 ? "?" : "&") + `access_token=${encodeURIComponent(this.Zt)}`), new Promise(((k, S) => {
            let L, z = !1;
            if (w === ie.Text) {
              if (H.isBrowser || H.isWebWorker) L = new this.ie.EventSource(d, { withCredentials: this.ie.withCredentials });
              else {
                const Z = this.$.getCookieString(d), X = {};
                X.Cookie = Z;
                const [ft, Rt] = ct();
                X[ft] = Rt, L = new this.ie.EventSource(d, { withCredentials: this.ie.withCredentials, headers: { ...X, ...this.ie.headers } });
              }
              try {
                L.onmessage = (Z) => {
                  if (this.onreceive) try {
                    this.u.log(r.Trace, `(SSE transport) data received. ${D(Z.data, this.ie.logMessageContent)}.`), this.onreceive(Z.data);
                  } catch (X) {
                    return void this.le(X);
                  }
                }, L.onerror = (Z) => {
                  z ? this.le() : S(new Error("EventSource failed to connect. The connection could not be found on the server, either the connection ID is not present on the server, or a proxy is refusing/buffering the connection. If you have multiple servers check that sticky sessions are enabled."));
                }, L.onopen = () => {
                  this.u.log(r.Information, `SSE connected to ${this.re}`), this.ue = L, z = !0, k();
                };
              } catch (Z) {
                return void S(Z);
              }
            } else S(new Error("The Server-Sent Events transport only supports the 'Text' transfer format"));
          }));
        }
        async send(d) {
          return this.ue ? B(this.u, "SSE", this.$, this.re, d, this.ie) : Promise.reject(new Error("Cannot send until the transport is connected"));
        }
        stop() {
          return this.le(), Promise.resolve();
        }
        le(d) {
          this.ue && (this.ue.close(), this.ue = void 0, this.onclose && this.onclose(d));
        }
      };
      n(Tp, "z");
      let ep = Tp;
      const Lp = class Lp {
        constructor(d, w, k, S, L, z) {
          this.u = k, this.Yt = w, this.de = S, this.fe = L, this.$ = d, this.onreceive = null, this.onclose = null, this.pe = z;
        }
        async connect(d, w) {
          let k;
          return A.isRequired(d, "url"), A.isRequired(w, "transferFormat"), A.isIn(w, ie, "transferFormat"), this.u.log(r.Trace, "(WebSockets transport) Connecting."), this.Yt && (k = await this.Yt()), new Promise(((S, L) => {
            let z;
            d = d.replace(/^http/, "ws");
            const Z = this.$.getCookieString(d);
            let X = !1;
            if (H.isNode || H.isReactNative) {
              const ft = {}, [Rt, xn] = ct();
              ft[Rt] = xn, k && (ft[Ct.Authorization] = `Bearer ${k}`), Z && (ft[Ct.Cookie] = Z), z = new this.fe(d, void 0, { headers: { ...ft, ...this.pe } });
            } else k && (d += (d.indexOf("?") < 0 ? "?" : "&") + `access_token=${encodeURIComponent(k)}`);
            z || (z = new this.fe(d)), w === ie.Binary && (z.binaryType = "arraybuffer"), z.onopen = (ft) => {
              this.u.log(r.Information, `WebSocket connected to ${d}.`), this.we = z, X = !0, S();
            }, z.onerror = (ft) => {
              let Rt = null;
              Rt = typeof ErrorEvent < "u" && ft instanceof ErrorEvent ? ft.error : "There was an error with the transport", this.u.log(r.Information, `(WebSockets transport) ${Rt}.`);
            }, z.onmessage = (ft) => {
              if (this.u.log(r.Trace, `(WebSockets transport) data received. ${D(ft.data, this.de)}.`), this.onreceive) try {
                this.onreceive(ft.data);
              } catch (Rt) {
                return void this.le(Rt);
              }
            }, z.onclose = (ft) => {
              if (X) this.le(ft);
              else {
                let Rt = null;
                Rt = typeof ErrorEvent < "u" && ft instanceof ErrorEvent ? ft.error : "WebSocket failed to connect. The connection could not be found on the server, either the endpoint may not be a SignalR endpoint, the connection ID is not present on the server, or there is a proxy blocking WebSockets. If you have multiple servers check that sticky sessions are enabled.", L(new Error(Rt));
              }
            };
          }));
        }
        send(d) {
          return this.we && this.we.readyState === this.fe.OPEN ? (this.u.log(r.Trace, `(WebSockets transport) sending data. ${D(d, this.de)}.`), this.we.send(d), Promise.resolve()) : Promise.reject("WebSocket is not in the OPEN state");
        }
        stop() {
          return this.we && this.le(void 0), Promise.resolve();
        }
        le(d) {
          this.we && (this.we.onclose = () => {
          }, this.we.onmessage = () => {
          }, this.we.onerror = () => {
          }, this.we.close(), this.we = void 0), this.u.log(r.Trace, "(WebSockets transport) socket closed."), this.onclose && (!this.ge(d) || d.wasClean !== !1 && d.code === 1e3 ? d instanceof Error ? this.onclose(d) : this.onclose() : this.onclose(new Error(`WebSocket closed with status code: ${d.code} (${d.reason || "no reason given"}).`)));
        }
        ge(d) {
          return d && typeof d.wasClean == "boolean" && typeof d.code == "number";
        }
      };
      n(Lp, "V");
      let ip = Lp;
      const Op = class Op {
        constructor(d, w = {}) {
          var k;
          if (this.me = () => {
          }, this.features = {}, this.ye = 1, A.isRequired(d, "url"), this.u = (k = w.logger) === void 0 ? new it(r.Information) : k === null ? E.instance : k.log !== void 0 ? k : new it(k), this.baseUrl = this.be(d), (w = w || {}).logMessageContent = w.logMessageContent !== void 0 && w.logMessageContent, typeof w.withCredentials != "boolean" && w.withCredentials !== void 0) throw new Error("withCredentials option was not a 'boolean' or 'undefined' value");
          w.withCredentials = w.withCredentials === void 0 || w.withCredentials, w.timeout = w.timeout === void 0 ? 1e5 : w.timeout;
          let S = null, L = null;
          if (H.isNode) {
            const z = Vp;
            S = z("ws"), L = z("eventsource");
          }
          H.isNode || typeof WebSocket > "u" || w.WebSocket ? H.isNode && !w.WebSocket && S && (w.WebSocket = S) : w.WebSocket = WebSocket, H.isNode || typeof EventSource > "u" || w.EventSource ? H.isNode && !w.EventSource && L !== void 0 && (w.EventSource = L) : w.EventSource = EventSource, this.$ = new ze(w.httpClient || new At(this.u), w.accessTokenFactory), this.ut = "Disconnected", this.dt = !1, this.ie = w, this.onreceive = null, this.onclose = null;
        }
        async start(d) {
          if (d = d || ie.Binary, A.isIn(d, ie, "transferFormat"), this.u.log(r.Debug, `Starting connection with transfer format '${ie[d]}'.`), this.ut !== "Disconnected") return Promise.reject(new Error("Cannot start an HttpConnection that is not in the 'Disconnected' state."));
          if (this.ut = "Connecting", this.ve = this.yt(d), await this.ve, this.ut === "Disconnecting") {
            const w = "Failed to start the HttpConnection before stop() was called.";
            return this.u.log(r.Error, w), await this.It, Promise.reject(new l(w));
          }
          if (this.ut !== "Connected") {
            const w = "HttpConnection.startInternal completed gracefully but didn't enter the connection into the connected state!";
            return this.u.log(r.Error, w), Promise.reject(new l(w));
          }
          this.dt = !0;
        }
        send(d) {
          return this.ut !== "Connected" ? Promise.reject(new Error("Cannot send data if the connection is not in the 'Connected' State.")) : (this.Ee || (this.Ee = new sp(this.transport)), this.Ee.send(d));
        }
        async stop(d) {
          return this.ut === "Disconnected" ? (this.u.log(r.Debug, `Call to HttpConnection.stop(${d}) ignored because the connection is already in the disconnected state.`), Promise.resolve()) : this.ut === "Disconnecting" ? (this.u.log(r.Debug, `Call to HttpConnection.stop(${d}) ignored because the connection is already in the disconnecting state.`), this.It) : (this.ut = "Disconnecting", this.It = new Promise(((w) => {
            this.me = w;
          })), await this._t(d), void await this.It);
        }
        async _t(d) {
          this.$e = d;
          try {
            await this.ve;
          } catch {
          }
          if (this.transport) {
            try {
              await this.transport.stop();
            } catch (w) {
              this.u.log(r.Error, `HttpConnection.transport.stop() threw error '${w}'.`), this.Ce();
            }
            this.transport = void 0;
          } else this.u.log(r.Debug, "HttpConnection.transport is undefined in HttpConnection.stop() because start() failed.");
        }
        async yt(d) {
          let w = this.baseUrl;
          this.Yt = this.ie.accessTokenFactory, this.$.Yt = this.Yt;
          try {
            if (this.ie.skipNegotiation) {
              if (this.ie.transport !== Et.WebSockets) throw new Error("Negotiation can only be skipped when using the WebSocket transport directly.");
              this.transport = this.Se(Et.WebSockets), await this.ke(w, d);
            } else {
              let k = null, S = 0;
              do {
                if (k = await this.Pe(w), this.ut === "Disconnecting" || this.ut === "Disconnected") throw new l("The connection was stopped during negotiation.");
                if (k.error) throw new Error(k.error);
                if (k.ProtocolVersion) throw new Error("Detected a connection attempt to an ASP.NET SignalR Server. This client only supports connecting to an ASP.NET Core SignalR Server. See https://aka.ms/signalr-core-differences for details.");
                if (k.url && (w = k.url), k.accessToken) {
                  const L = k.accessToken;
                  this.Yt = () => L, this.$.Zt = L, this.$.Yt = void 0;
                }
                S++;
              } while (k.url && S < 100);
              if (S === 100 && k.url) throw new Error("Negotiate redirection limit exceeded.");
              await this.Te(w, this.ie.transport, k, d);
            }
            this.transport instanceof wh && (this.features.inherentKeepAlive = !0), this.ut === "Connecting" && (this.u.log(r.Debug, "The HttpConnection connected successfully."), this.ut = "Connected");
          } catch (k) {
            return this.u.log(r.Error, "Failed to start the connection: " + k), this.ut = "Disconnected", this.transport = void 0, this.me(), Promise.reject(k);
          }
        }
        async Pe(d) {
          const w = {}, [k, S] = ct();
          w[k] = S;
          const L = this.Ie(d);
          this.u.log(r.Debug, `Sending negotiation request: ${L}.`);
          try {
            const z = await this.$.post(L, { content: "", headers: { ...w, ...this.ie.headers }, timeout: this.ie.timeout, withCredentials: this.ie.withCredentials });
            if (z.statusCode !== 200) return Promise.reject(new Error(`Unexpected status code returned from negotiate '${z.statusCode}'`));
            const Z = JSON.parse(z.content);
            return (!Z.negotiateVersion || Z.negotiateVersion < 1) && (Z.connectionToken = Z.connectionId), Z.useStatefulReconnect && this.ie._e !== !0 ? Promise.reject(new v("Client didn't negotiate Stateful Reconnect but the server did.")) : Z;
          } catch (z) {
            let Z = "Failed to complete negotiation with the server: " + z;
            return z instanceof o && z.statusCode === 404 && (Z += " Either this is not a SignalR endpoint or there is a proxy blocking the connection."), this.u.log(r.Error, Z), Promise.reject(new v(Z));
          }
        }
        He(d, w) {
          return w ? d + (d.indexOf("?") === -1 ? "?" : "&") + `id=${w}` : d;
        }
        async Te(d, w, k, S) {
          let L = this.He(d, k.connectionToken);
          if (this.De(w)) return this.u.log(r.Debug, "Connection was provided an instance of ITransport, using that directly."), this.transport = w, await this.ke(L, S), void (this.connectionId = k.connectionId);
          const z = [], Z = k.availableTransports || [];
          let X = k;
          for (const ft of Z) {
            const Rt = this.Re(ft, w, S, X?.useStatefulReconnect === !0);
            if (Rt instanceof Error) z.push(`${ft.transport} failed:`), z.push(Rt);
            else if (this.De(Rt)) {
              if (this.transport = Rt, !X) {
                try {
                  X = await this.Pe(d);
                } catch (xn) {
                  return Promise.reject(xn);
                }
                L = this.He(d, X.connectionToken);
              }
              try {
                return await this.ke(L, S), void (this.connectionId = X.connectionId);
              } catch (xn) {
                if (this.u.log(r.Error, `Failed to start the transport '${ft.transport}': ${xn}`), X = void 0, z.push(new C(`${ft.transport} failed: ${xn}`, Et[ft.transport])), this.ut !== "Connecting") {
                  const Jf = "Failed to select transport before stop() was called.";
                  return this.u.log(r.Debug, Jf), Promise.reject(new l(Jf));
                }
              }
            }
          }
          return z.length > 0 ? Promise.reject(new f(`Unable to connect to the server with any of the available transports. ${z.join(" ")}`, z)) : Promise.reject(new Error("None of the transports supported by the client are supported by the server."));
        }
        Se(d) {
          switch (d) {
            case Et.WebSockets:
              if (!this.ie.WebSocket) throw new Error("'WebSocket' is not supported in your environment.");
              return new ip(this.$, this.Yt, this.u, this.ie.logMessageContent, this.ie.WebSocket, this.ie.headers || {});
            case Et.ServerSentEvents:
              if (!this.ie.EventSource) throw new Error("'EventSource' is not supported in your environment.");
              return new ep(this.$, this.$.Zt, this.u, this.ie);
            case Et.LongPolling:
              return new wh(this.$, this.u, this.ie);
            default:
              throw new Error(`Unknown transport: ${d}.`);
          }
        }
        ke(d, w) {
          return this.transport.onreceive = this.onreceive, this.features.reconnect ? this.transport.onclose = async (k) => {
            let S = !1;
            if (this.features.reconnect) {
              try {
                this.features.disconnected(), await this.transport.connect(d, w), await this.features.resend();
              } catch {
                S = !0;
              }
              S && this.Ce(k);
            } else this.Ce(k);
          } : this.transport.onclose = (k) => this.Ce(k), this.transport.connect(d, w);
        }
        Re(d, w, k, S) {
          const L = Et[d.transport];
          if (L == null) return this.u.log(r.Debug, `Skipping transport '${d.transport}' because it is not supported by this client.`), new Error(`Skipping transport '${d.transport}' because it is not supported by this client.`);
          if (!(function(z, Z) {
            return !z || !!(Z & z);
          })(w, L)) return this.u.log(r.Debug, `Skipping transport '${Et[L]}' because it was disabled by the client.`), new b(`'${Et[L]}' is disabled by the client.`, L);
          if (!(d.transferFormats.map(((z) => ie[z])).indexOf(k) >= 0)) return this.u.log(r.Debug, `Skipping transport '${Et[L]}' because it does not support the requested transfer format '${ie[k]}'.`), new Error(`'${Et[L]}' does not support ${ie[k]}.`);
          if (L === Et.WebSockets && !this.ie.WebSocket || L === Et.ServerSentEvents && !this.ie.EventSource) return this.u.log(r.Debug, `Skipping transport '${Et[L]}' because it is not supported in your environment.'`), new u(`'${Et[L]}' is not supported in your environment.`, L);
          this.u.log(r.Debug, `Selecting transport '${Et[L]}'.`);
          try {
            return this.features.reconnect = L === Et.WebSockets ? S : void 0, this.Se(L);
          } catch (z) {
            return z;
          }
        }
        De(d) {
          return d && typeof d == "object" && "connect" in d;
        }
        Ce(d) {
          if (this.u.log(r.Debug, `HttpConnection.stopConnection(${d}) called while in state ${this.ut}.`), this.transport = void 0, d = this.$e || d, this.$e = void 0, this.ut !== "Disconnected") {
            if (this.ut === "Connecting") throw this.u.log(r.Warning, `Call to HttpConnection.stopConnection(${d}) was ignored because the connection is still in the connecting state.`), new Error(`HttpConnection.stopConnection(${d}) was called while the connection is still in the connecting state.`);
            if (this.ut === "Disconnecting" && this.me(), d ? this.u.log(r.Error, `Connection disconnected with error '${d}'.`) : this.u.log(r.Information, "Connection disconnected."), this.Ee && (this.Ee.stop().catch(((w) => {
              this.u.log(r.Error, `TransportSendQueue.stop() threw error '${w}'.`);
            })), this.Ee = void 0), this.connectionId = void 0, this.ut = "Disconnected", this.dt) {
              this.dt = !1;
              try {
                this.onclose && this.onclose(d);
              } catch (w) {
                this.u.log(r.Error, `HttpConnection.onclose(${d}) threw error '${w}'.`);
              }
            }
          } else this.u.log(r.Debug, `Call to HttpConnection.stopConnection(${d}) was ignored because the connection is already in the disconnected state.`);
        }
        be(d) {
          if (d.lastIndexOf("https://", 0) === 0 || d.lastIndexOf("http://", 0) === 0) return d;
          if (!H.isBrowser) throw new Error(`Cannot resolve '${d}'.`);
          const w = window.document.createElement("a");
          return w.href = d, this.u.log(r.Information, `Normalizing '${d}' to '${w.href}'.`), w.href;
        }
        Ie(d) {
          const w = new URL(d);
          w.pathname.endsWith("/") ? w.pathname += "negotiate" : w.pathname += "/negotiate";
          const k = new URLSearchParams(w.searchParams);
          return k.has("negotiateVersion") || k.append("negotiateVersion", this.ye.toString()), k.has("useStatefulReconnect") ? k.get("useStatefulReconnect") === "true" && (this.ie._e = !0) : this.ie._e === !0 && k.append("useStatefulReconnect", "true"), w.search = k.toString(), w.toString();
        }
      };
      n(Op, "K");
      let rp = Op;
      const vh = class vh {
        constructor(d) {
          this.xe = d, this.Ae = [], this.Ue = !0, this.Le = new vn(), this.Ne = new vn(), this.qe = this.Me();
        }
        send(d) {
          return this.je(d), this.Ne || (this.Ne = new vn()), this.Ne.promise;
        }
        stop() {
          return this.Ue = !1, this.Le.resolve(), this.qe;
        }
        je(d) {
          if (this.Ae.length && typeof this.Ae[0] != typeof d) throw new Error(`Expected data to be of type ${typeof this.Ae} but was of type ${typeof d}`);
          this.Ae.push(d), this.Le.resolve();
        }
        async Me() {
          for (; ; ) {
            if (await this.Le.promise, !this.Ue) {
              this.Ne && this.Ne.reject("Connection stopped.");
              break;
            }
            this.Le = new vn();
            const d = this.Ne;
            this.Ne = void 0;
            const w = typeof this.Ae[0] == "string" ? this.Ae.join("") : vh.We(this.Ae);
            this.Ae.length = 0;
            try {
              await this.xe.send(w), d.resolve();
            } catch (k) {
              d.reject(k);
            }
          }
        }
        static We(d) {
          const w = d.map(((L) => L.byteLength)).reduce(((L, z) => L + z)), k = new Uint8Array(w);
          let S = 0;
          for (const L of d) k.set(new Uint8Array(L), S), S += L.byteLength;
          return k.buffer;
        }
      };
      n(vh, "G");
      let sp = vh;
      const zp = class zp {
        constructor() {
          this.promise = new Promise(((d, w) => [this.j, this.Oe] = [d, w]));
        }
        resolve() {
          this.j();
        }
        reject(d) {
          this.Oe(d);
        }
      };
      n(zp, "Q");
      let vn = zp;
      const Fp = class Fp {
        constructor() {
          this.name = "json", this.version = 2, this.transferFormat = ie.Text;
        }
        parseMessages(d, w) {
          if (typeof d != "string") throw new Error("Invalid input for JSON hub protocol. Expected a string.");
          if (!d) return [];
          w === null && (w = E.instance);
          const k = xt.parse(d), S = [];
          for (const L of k) {
            const z = JSON.parse(L);
            if (typeof z.type != "number") throw new Error("Invalid payload.");
            switch (z.type) {
              case lt.Invocation:
                this.U(z);
                break;
              case lt.StreamItem:
                this.Fe(z);
                break;
              case lt.Completion:
                this.Be(z);
                break;
              case lt.Ping:
              case lt.Close:
                break;
              case lt.Ack:
                this.Xe(z);
                break;
              case lt.Sequence:
                this.Je(z);
                break;
              default:
                w.log(r.Information, "Unknown message type '" + z.type + "' ignored.");
                continue;
            }
            S.push(z);
          }
          return S;
        }
        writeMessage(d) {
          return xt.write(JSON.stringify(d));
        }
        U(d) {
          this.ze(d.target, "Invalid payload for Invocation message."), d.invocationId !== void 0 && this.ze(d.invocationId, "Invalid payload for Invocation message.");
        }
        Fe(d) {
          if (this.ze(d.invocationId, "Invalid payload for StreamItem message."), d.item === void 0) throw new Error("Invalid payload for StreamItem message.");
        }
        Be(d) {
          if (d.result && d.error) throw new Error("Invalid payload for Completion message.");
          !d.result && d.error && this.ze(d.error, "Invalid payload for Completion message."), this.ze(d.invocationId, "Invalid payload for Completion message.");
        }
        Xe(d) {
          if (typeof d.sequenceId != "number") throw new Error("Invalid SequenceId for Ack message.");
        }
        Je(d) {
          if (typeof d.sequenceId != "number") throw new Error("Invalid SequenceId for Sequence message.");
        }
        ze(d, w) {
          if (typeof d != "string" || d === "") throw new Error(w);
        }
      };
      n(Fp, "Y");
      let fh = Fp;
      const i0 = { trace: r.Trace, debug: r.Debug, info: r.Information, information: r.Information, warn: r.Warning, warning: r.Warning, error: r.Error, critical: r.Critical, none: r.None }, Ip = class Ip {
        configureLogging(d) {
          if (A.isRequired(d, "logging"), d.log !== void 0) this.logger = d;
          else if (typeof d == "string") {
            const w = (function(k) {
              const S = i0[k.toLowerCase()];
              if (S !== void 0) return S;
              throw new Error(`Unknown log level: ${k}`);
            })(d);
            this.logger = new it(w);
          } else this.logger = new it(d);
          return this;
        }
        withUrl(d, w) {
          return A.isRequired(d, "url"), A.isNotEmpty(d, "url"), this.url = d, this.httpConnectionOptions = typeof w == "object" ? { ...this.httpConnectionOptions, ...w } : { ...this.httpConnectionOptions, transport: w }, this;
        }
        withHubProtocol(d) {
          return A.isRequired(d, "protocol"), this.protocol = d, this;
        }
        withAutomaticReconnect(d) {
          if (this.reconnectPolicy) throw new Error("A reconnectPolicy has already been set.");
          return d ? Array.isArray(d) ? this.reconnectPolicy = new Ci(d) : this.reconnectPolicy = d : this.reconnectPolicy = new Ci(), this;
        }
        withServerTimeout(d) {
          return A.isRequired(d, "milliseconds"), this.Ve = d, this;
        }
        withKeepAliveInterval(d) {
          return A.isRequired(d, "milliseconds"), this.Ke = d, this;
        }
        withStatefulReconnect(d) {
          return this.httpConnectionOptions === void 0 && (this.httpConnectionOptions = {}), this.httpConnectionOptions._e = !0, this.Y = d?.bufferSize, this;
        }
        build() {
          const d = this.httpConnectionOptions || {};
          if (d.logger === void 0 && (d.logger = this.logger), !this.url) throw new Error("The 'HubConnectionBuilder.withUrl' method must be called before building the connection.");
          const w = new rp(this.url, d);
          return qi.create(w, this.logger || E.instance, this.protocol || new fh(), this.reconnectPolicy, this.Ve, this.Ke, this.Y);
        }
      };
      n(Ip, "tt");
      let np = Ip;
      return Uint8Array.prototype.indexOf || Object.defineProperty(Uint8Array.prototype, "indexOf", { value: Array.prototype.indexOf, writable: !0 }), Uint8Array.prototype.slice || Object.defineProperty(Uint8Array.prototype, "slice", { value: /* @__PURE__ */ n(function(N, d) {
        return new Uint8Array(Array.prototype.slice.call(this, N, d));
      }, "value"), writable: !0 }), Uint8Array.prototype.forEach || Object.defineProperty(Uint8Array.prototype, "forEach", { value: Array.prototype.forEach, writable: !0 }), a;
    })(), "e"), s.exports = e();
  })(Hp)), Hp.exports;
}
n(A0, "requireSignalr_min");
var sm = /* @__PURE__ */ A0();
const R0 = /* @__PURE__ */ n((s) => {
  var t;
  return t = class extends s {
    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    constructor(...i) {
      super(...i), this._whenConnectionRequested = new Promise((r) => {
        this._resolveConnectionRequested = r;
      }), this._whenConnectionCreated = new Promise((r) => {
        this._resolveConnectionCreated = r;
      }), this._connectionEventListeners = [], this.signalRAccessTokenRefresh = !1, this._whenConnectionStarted = new Promise((r, a) => {
        this._whenConnectionStartedResolve = r, this._whenConnectionStartedReject = a;
      }), this.whenConnectionRequested().then(() => {
        this.isDestroyed || this.createConnection();
      });
    }
    async whenConnectionRequested() {
      await this._whenConnectionRequested;
    }
    async whenConnectionCreated() {
      return await this._whenConnectionCreated;
    }
    get rtmConnection() {
      return this._connection;
    }
    async whenConnectionStarted() {
      return await this._whenConnectionStarted;
    }
    async createConnection() {
      if (this.isDestroyed)
        throw new Ut();
      if (this.networkStateIsPending = !0, await this.whenUrlAndTokenFactory(), this._connection) {
        const i = new URL("/hubs/rtm", this.url);
        this._connection.baseUrl !== i.toString() && (this.connectionState = "reconnecting", console.info(
          this.weavyId,
          "Reconnecting due to changed url.",
          this._connection.baseUrl,
          "=>",
          i.toString()
        ), await this.disconnect(), this._connection.baseUrl = i.toString(), this.connect());
      } else {
        this.connectionState = "connecting";
        const i = new URL("/hubs/rtm", this.url);
        this._connection = new sm.HubConnectionBuilder().configureLogging(sm.LogLevel.None).withUrl(i.toString(), {
          accessTokenFactory: /* @__PURE__ */ n(async () => {
            try {
              if (this.signalRAccessTokenRefresh) {
                const r = await this.getToken(!0);
                return this.signalRAccessTokenRefresh = !1, r;
              } else
                return await this.getToken();
            } catch (r) {
              throw console.error(r), r;
            }
          }, "accessTokenFactory")
        }).withAutomaticReconnect({
          nextRetryDelayInMilliseconds: /* @__PURE__ */ n((r) => !this.isDestroyed && window.navigator.onLine && document?.visibilityState !== "hidden" && r.elapsedMilliseconds < 6e4 ? [0, 2e3, 1e4][r.previousRetryCount] || 1e4 : null, "nextRetryDelayInMilliseconds")
        }).build(), this._connection.onclose(() => {
          console.info(this.weavyId, "SignalR closed."), this.connectionState = "disconnected", !this.isDestroyed && (this.networkStateIsPending = !0, this._whenConnectionStarted = new Promise((r, a) => {
            this._whenConnectionStartedResolve = r, this._whenConnectionStartedReject = a;
          }), this.connect());
        }), this._connection.onreconnecting(() => {
          console.info(this.weavyId, "SignalR reconnecting..."), this.connectionState = "reconnecting";
        }), this._connection.onreconnected((r) => {
          console.info(this.weavyId, `SignalR reconnected ${r}`), this.connectionState = "connected", this.networkStateIsPending = !1;
          for (let a = 0; a < this._connectionEventListeners.length; a++)
            this._connection?.invoke("Subscribe", this._connectionEventListeners[a].name);
        }), this._resolveConnectionCreated?.(this._connection), this.connect();
      }
      return this._connection;
    }
    async disconnect() {
      this._connection && (await this._connection.stop(), this.connectionState = "disconnected");
    }
    async connect() {
      if (this.isDestroyed)
        throw new Ut();
      let i;
      this._connection ? i = this._connection : i = await this.whenConnectionCreated(), console.info(this.weavyId, "Connecting SignalR...");
      try {
        if (!window.navigator.onLine)
          throw new Error();
        await Promise.race([i.start(), this.whenConnectionStarted()]), this.signalRAccessTokenRefresh = !1, this.networkStateIsPending = !1, this.connectionState = "connected", this._whenConnectionStartedResolve?.(i), console.info(this.weavyId, `SignalR connected ${i.connectionId}`);
      } catch (r) {
        if (r instanceof Ut)
          return console.warn(this.weavyId, "SignalR connection aborted."), i;
        window.navigator.onLine ? !this.signalRAccessTokenRefresh && window.document.visibilityState !== "hidden" && r.toString().includes("Unauthorized") ? (console.info(this.weavyId, "Retrying SignalR connect with fresh token."), this.signalRAccessTokenRefresh = !0) : (console.info(
          this.weavyId,
          "Server is probably down, retrying SignalR connect after a delay or when window regains focus."
        ), this.connectionState = "reconnecting", await new Promise((a) => {
          setTimeout(a, 5e3), window.addEventListener("visibilitychange", a, { once: !0 }), window.addEventListener("offline", a, { once: !0 }), window.addEventListener("online", a, { once: !0 });
        })) : (this.networkStateIsPending = !1, console.info(this.weavyId, "Offline, reconnecting SignalR when online."), await new Promise((a) => {
          window.addEventListener("online", a, { once: !0 });
        })), window.navigator.onLine && document?.visibilityState !== "hidden" && await new Promise((a) => setTimeout(a, 1e3)), this.checkVersion(), this.networkStateIsPending = !0, await this.connect();
      }
      return i;
    }
    async subscribe(i, r, a) {
      if (this.isDestroyed)
        throw new Ut();
      this._resolveConnectionRequested?.(!0);
      const o = i ? i + ":" + r : r;
      try {
        if (this._connectionEventListeners || await new Promise((l) => queueMicrotask(() => l(!0))), this._connectionEventListeners.some((l) => l.name === o && l.callback === a))
          throw new Error("Duplicate subscribe: " + o);
        if (this._connectionEventListeners.push({ name: o, callback: a }), await this.whenConnectionStarted(), !this._connection)
          throw new Error("Connection not created");
        if (this._connection.on(o, a), await this._connection.invoke("Subscribe", o) === !1)
          throw new Error("Could not subscribe to " + o);
        return !0;
      } catch (c) {
        c instanceof Ut || console.error(this.weavyId, "Error in Subscribe:", c);
        const l = this._connectionEventListeners.findIndex((u) => u.name === o && u.callback === a);
        return l !== -1 && (this._connectionEventListeners.splice(l, 1), this._connection?.off(o, a)), !1;
      }
    }
    async unsubscribe(i, r, a) {
      if (this.isDestroyed)
        throw new Ut();
      try {
        const o = i ? i + ":" + r : r, c = this._connectionEventListeners.findIndex((l) => l.name === o && l.callback === a);
        if (c !== -1) {
          if (this._connectionEventListeners.splice(c, 1), await this.whenConnectionStarted(), !this._connection)
            throw new Error("Connection not created");
          this._connection?.off(o, a), this._connectionEventListeners.some((l) => l.name === o) || await this._connection.invoke("Unsubscribe", o);
        }
      } catch (o) {
        o instanceof Ut || console.error(this.weavyId, "Error in Unsubscribe:", o);
      }
    }
    destroy() {
      super.destroy(), this.disconnect(), this._whenConnectionStartedReject && (this._whenConnectionStarted.catch(() => {
      }), this._whenConnectionStartedReject(new Ut()));
    }
  }, n(t, "WeavyConnection"), t;
}, "WeavyConnectionMixin");
var qh = /* @__PURE__ */ ((s) => (s.JSON = "application/json;charset=utf-8", s.FormData = "multipart/form-data;charset=utf-8", s.URLEncoded = "application/x-www-form-urlencoded;charset=utf-8", s.Text = "text/plain;charset=utf-8", s.Auto = "", s))(qh || {});
const T0 = /* @__PURE__ */ n((s) => {
  var t;
  return t = class extends s {
    // FETCH
    async fetchOptions(i = {}, r = !0) {
      if (this.isDestroyed)
        throw new Ut();
      const a = {
        headers: {
          "X-Weavy-Source": `${Qt.sourceName}@${Qt.version}`,
          "Content-Type": qh.JSON
        },
        method: "GET"
      }, o = Hn(Hn(_0, a, !0), i, !0);
      return r ? Hn(
        o,
        {
          headers: {
            Authorization: "Bearer " + await this.getToken()
          }
        },
        !0
      ) : o;
    }
    async fetch(i, r, a = !0) {
      if (this.isDestroyed)
        throw new Ut();
      const o = await this.fetchOptions(r);
      this.networkStateIsPending = !0;
      const c = await fetch(new URL(i, this.url), o);
      if (c.ok)
        this.networkStateIsPending = !1, this.serverState = "ok";
      else if (c.status === 401 || c.status === 403) {
        if (a)
          return await this.getToken(!0), await this.fetch(i, r, !1);
        this.networkStateIsPending = !1, this.serverState = "unauthorized";
      } else
        this.networkStateIsPending = !1;
      return c;
    }
    async upload(i, r, a, o = qh.JSON, c, l, u = !0) {
      if (this.isDestroyed)
        throw new Ut();
      const b = await this.getToken();
      return await new Promise((C, v) => {
        const f = new XMLHttpRequest();
        f.open(r, new URL(i, this.url), !0), f.setRequestHeader("Authorization", "Bearer " + b), f.setRequestHeader("X-Weavy-Source", `${Qt.sourceName}@${Qt.version}`), o && f.setRequestHeader("Content-Type", o), c && f.upload.addEventListener("progress", (m) => {
          c(m.loaded / m.total * 100 || 100);
        }), f.onload = (m) => {
          u && (f.status === 401 || f.status === 401) ? this.getToken(!0).then(() => this.upload(i, r, a, o, c, l, !1)).then(C).catch(v) : C(new Response(f.response, { status: f.status, statusText: f.statusText }));
        }, f.onerror = v, f.onabort = v, l?.addEventListener("abort", () => {
          f.abort();
        }), f.send(a);
      });
    }
    // DEPRECATED
    async get(i) {
      return console.warn(`weavy.get() is deprecated, use weavy.fetch("${i}") instead.`), this.fetch(i);
    }
    // DEPRECATED
    async post(i, r, a, o) {
      console.warn(
        `weavy.post() is deprecated, use weavy.fetch("${i}", { method: "${r}"}) instead.`
      );
      const c = o ? { "Content-Type": o } : void 0;
      return this.fetch(i, { method: r, body: a, headers: c });
    }
  }, n(t, "WeavyFetch"), t;
}, "WeavyFetchMixin"), Tv = "en", L0 = /* @__PURE__ */ n((s) => {
  var t;
  return t = class extends s {
    constructor() {
      super(...arguments), this._locales = /* @__PURE__ */ new Map([
        ["sv-SE", () => import(
          /* webpackIgnore: true */
          /* @vite-ignore */
          new URL("./es/locales/sv-SE-D-8dijzH.js", typeof WEAVY_IMPORT_URL == "string" && (!import.meta.url || !new URL(import.meta.url).href.startsWith(WEAVY_IMPORT_URL)) ? new URL(WEAVY_IMPORT_URL, globalThis.location.href) : import.meta.url).href
        ).then((i) => i.svSE)]
      ]), this._locale = t.sourceLocale;
    }
    /**
     * The locale used in the Weavy source.
     */
    static get sourceLocale() {
      try {
        return Tv;
      } catch {
        return "";
      }
    }
    get locales() {
      return Array.from(this._locales.entries());
    }
    set locales(i) {
      if (this.isDestroyed)
        throw new Ut();
      if (this.localization)
        throw new Error("Locales may only be configured once");
      if (i) {
        if (!Array.isArray(i))
          throw new TypeError("Provided locales have invalid format.");
        i.forEach((r) => {
          if (!Array.isArray(r) || r.length !== 2 || typeof r[0] != "string")
            throw new TypeError("Invalid locale provided: " + r[0]);
          this._locales.set(...r);
        }), this.configureLocalization();
      }
    }
    get localization() {
      return this._localization;
    }
    /**
     * Selected locale. The locale must be pre configured in `.locales`.
     */
    get locale() {
      return this._locale;
    }
    set locale(i) {
      if (this.isDestroyed)
        throw new Ut();
      !this._locale && !i || (i ||= t.sourceLocale, this._locale = i, this.localization ? this.localization.setLocale(this._locale) : queueMicrotask(() => {
        this.localization ? this.localization.setLocale(this._locale) : this._locale !== t.sourceLocale && (this._locales.has(this._locale) && this.configureLocalization(), this.localization ? this.localization.setLocale(this._locale) : console.error(
          this.weavyId,
          `You need to configure additional languages in config to use '${i}'.`
        ));
      }));
    }
    async loadLocale(i) {
      if (this.isDestroyed)
        throw new Ut();
      if (this._locales?.has(i)) {
        const r = this._locales.get(i);
        return console.info(
          this.weavyId,
          typeof r == "function" ? "loading locale" : "preloaded locale",
          i
        ), await (typeof r == "function" ? r() : r);
      } else
        throw new Error("The requested locale is not configured");
    }
    configureLocalization() {
      if (this.isDestroyed)
        throw new Ut();
      if (this._locales?.size && !this.localization) {
        const i = this._locales.keys();
        console.info(this.weavyId, "Configuring locales", i);
        const { getLocale: r, setLocale: a } = l0({
          sourceLocale: t.sourceLocale,
          targetLocales: i,
          loadLocale: /* @__PURE__ */ n((o) => this.loadLocale(o), "loadLocale")
        });
        this._localization = {
          getLocale: r,
          setLocale: a
        };
      }
    }
  }, n(t, "WeavyLocalization"), t;
}, "WeavyLocalizationMixin"), O0 = /* @__PURE__ */ n((s) => {
  var t;
  return t = class extends s {
    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    constructor(...i) {
      super(...i), this._configurationState = "pending", this._networkEvents = /* @__PURE__ */ new Set(), this._connectionState = "connecting", this._serverState = "ok", this._networkState = window.navigator.onLine ? "online" : "offline", this._networkStateIsPending = !1, window.addEventListener("online", () => {
        this.networkState = "online";
      }), window.addEventListener("offline", () => {
        this.networkState = "offline", this.networkStateIsPending = !1;
      }), queueMicrotask(() => this.requestConfigurationCheck());
    }
    get configurationState() {
      return this._configurationState;
    }
    set configurationState(i) {
      this._configurationState = i, this._configurationTimer && i === "configured" && (window.clearTimeout(this._configurationTimer), this._configurationTimer = null), this.triggerNetworkChange();
    }
    get networkState() {
      return this._networkState;
    }
    set networkState(i) {
      this._networkState = i, this.triggerNetworkChange();
    }
    get serverState() {
      return this._serverState;
    }
    set serverState(i) {
      this._serverState = i, this.triggerNetworkChange();
    }
    get connectionState() {
      return this._connectionState;
    }
    set connectionState(i) {
      this._connectionState = i, this.triggerNetworkChange();
    }
    get networkStateIsPending() {
      return this._networkStateIsPending;
    }
    set networkStateIsPending(i) {
      this._networkStateIsPending = i, this.triggerNetworkChange();
    }
    get network() {
      return {
        state: this._networkState === "online" ? (this._connectionState === "connected" || this._serverState === "ok") && this.configurationState !== "uninitialized" ? "online" : "unreachable" : "offline",
        isPending: this._networkStateIsPending
      };
    }
    requestConfigurationCheck() {
      !this._configurationTimer && this.configurationTimeout >= 0 && this.configurationTimeout < 1 / 0 && (this._configurationTimer = window.setTimeout(() => {
        this.configurationState === "pending" && (this.configurationState = "uninitialized", console.error("Weavy was not configured with required url and tokenFactory/tokenUrl within a reasonable time. Please check your configuration!")), this._configurationTimer = null;
      }, this.configurationTimeout));
    }
    triggerNetworkChange() {
      const i = this.network;
      this._networkEvents.forEach((r) => {
        r(i);
      });
    }
    addNetworkListener(i) {
      this._networkEvents.add(i);
    }
    removeNetworkListener(i) {
      this._networkEvents.delete(i);
    }
  }, n(t, "WeavyNetwork"), t;
}, "WeavyNetworkMixin");
var Bn, pl = (Bn = class {
  constructor() {
    this.listeners = /* @__PURE__ */ new Set(), this.subscribe = this.subscribe.bind(this);
  }
  subscribe(t) {
    return this.listeners.add(t), this.onSubscribe(), () => {
      this.listeners.delete(t), this.onUnsubscribe();
    };
  }
  hasListeners() {
    return this.listeners.size > 0;
  }
  onSubscribe() {
  }
  onUnsubscribe() {
  }
}, n(Bn, "Subscribable"), Bn), z0 = {
  // We need the wrapper function syntax below instead of direct references to
  // global setTimeout etc.
  //
  // BAD: `setTimeout: setTimeout`
  // GOOD: `setTimeout: (cb, delay) => setTimeout(cb, delay)`
  //
  // If we use direct references here, then anything that wants to spy on or
  // replace the global setTimeout (like tests) won't work since we'll already
  // have a hard reference to the original implementation at the time when this
  // file was imported.
  setTimeout: /* @__PURE__ */ n((s, t) => setTimeout(s, t), "setTimeout"),
  clearTimeout: /* @__PURE__ */ n((s) => clearTimeout(s), "clearTimeout"),
  setInterval: /* @__PURE__ */ n((s, t) => setInterval(s, t), "setInterval"),
  clearInterval: /* @__PURE__ */ n((s) => clearInterval(s), "clearInterval")
}, Lr, lw, Nn, F0 = (Nn = class {
  constructor() {
    // We cannot have TimeoutManager<T> as we must instantiate it with a concrete
    // type at app boot; and if we leave that type, then any new timer provider
    // would need to support ReturnType<typeof setTimeout>, which is infeasible.
    //
    // We settle for type safety for the TimeoutProvider type, and accept that
    // this class is unsafe internally to allow for extension.
    T(this, Lr, z0);
    T(this, lw, !1);
  }
  setTimeoutProvider(t) {
    P(this, Lr, t);
  }
  setTimeout(t, e) {
    return p(this, Lr).setTimeout(t, e);
  }
  clearTimeout(t) {
    p(this, Lr).clearTimeout(t);
  }
  setInterval(t, e) {
    return p(this, Lr).setInterval(t, e);
  }
  clearInterval(t) {
    p(this, Lr).clearInterval(t);
  }
}, Lr = new WeakMap(), lw = new WeakMap(), n(Nn, "TimeoutManager"), Nn), jr = new F0();
function I0(s) {
  setTimeout(s, 0);
}
n(I0, "systemSetTimeoutZero");
var qc = typeof window > "u" || "Deno" in globalThis;
function Me() {
}
n(Me, "noop$1");
function D0(s, t) {
  return typeof s == "function" ? s(t) : s;
}
n(D0, "functionalUpdate");
function oy(s) {
  return typeof s == "number" && s >= 0 && s !== 1 / 0;
}
n(oy, "isValidTimeout");
function Lv(s, t) {
  return Math.max(s + (t || 0) - Date.now(), 0);
}
n(Lv, "timeUntilStale");
function Gr(s, t) {
  return typeof s == "function" ? s(t) : s;
}
n(Gr, "resolveStaleTime");
function ii(s, t) {
  return typeof s == "function" ? s(t) : s;
}
n(ii, "resolveEnabled");
function nm(s, t) {
  const {
    type: e = "all",
    exact: i,
    fetchStatus: r,
    predicate: a,
    queryKey: o,
    stale: c
  } = s;
  if (o) {
    if (i) {
      if (t.queryHash !== yw(o, t.options))
        return !1;
    } else if (!Nl(t.queryKey, o))
      return !1;
  }
  if (e !== "all") {
    const l = t.isActive();
    if (e === "active" && !l || e === "inactive" && l)
      return !1;
  }
  return !(typeof c == "boolean" && t.isStale() !== c || r && r !== t.state.fetchStatus || a && !a(t));
}
n(nm, "matchQuery");
function am(s, t) {
  const { exact: e, status: i, predicate: r, mutationKey: a } = s;
  if (a) {
    if (!t.options.mutationKey)
      return !1;
    if (e) {
      if (Ds(t.options.mutationKey) !== Ds(a))
        return !1;
    } else if (!Nl(t.options.mutationKey, a))
      return !1;
  }
  return !(i && t.state.status !== i || r && !r(t));
}
n(am, "matchMutation");
function yw(s, t) {
  return (t?.queryKeyHashFn || Ds)(s);
}
n(yw, "hashQueryKeyByOptions");
function Ds(s) {
  return JSON.stringify(
    s,
    (t, e) => cy(e) ? Object.keys(e).sort().reduce((i, r) => (i[r] = e[r], i), {}) : e
  );
}
n(Ds, "hashKey");
function Nl(s, t) {
  return s === t ? !0 : typeof s != typeof t ? !1 : s && t && typeof s == "object" && typeof t == "object" ? Object.keys(t).every((e) => Nl(s[e], t[e])) : !1;
}
n(Nl, "partialMatchKey");
var V0 = Object.prototype.hasOwnProperty;
function Gd(s, t, e = 0) {
  if (s === t)
    return s;
  if (e > 500) return t;
  const i = om(s) && om(t);
  if (!i && !(cy(s) && cy(t))) return t;
  const a = (i ? s : Object.keys(s)).length, o = i ? t : Object.keys(t), c = o.length, l = i ? new Array(c) : {};
  let u = 0;
  for (let b = 0; b < c; b++) {
    const C = i ? b : o[b], v = s[C], f = t[C];
    if (v === f) {
      l[C] = v, (i ? b < a : V0.call(s, C)) && u++;
      continue;
    }
    if (v === null || f === null || typeof v != "object" || typeof f != "object") {
      l[C] = f;
      continue;
    }
    const m = Gd(v, f, e + 1);
    l[C] = m, m === v && u++;
  }
  return a === c && u === a ? s : l;
}
n(Gd, "replaceEqualDeep");
function Wh(s, t) {
  if (!t || Object.keys(s).length !== Object.keys(t).length)
    return !1;
  for (const e in s)
    if (s[e] !== t[e])
      return !1;
  return !0;
}
n(Wh, "shallowEqualObjects");
function om(s) {
  return Array.isArray(s) && s.length === Object.keys(s).length;
}
n(om, "isPlainArray");
function cy(s) {
  if (!cm(s))
    return !1;
  const t = s.constructor;
  if (t === void 0)
    return !0;
  const e = t.prototype;
  return !(!cm(e) || !e.hasOwnProperty("isPrototypeOf") || Object.getPrototypeOf(s) !== Object.prototype);
}
n(cy, "isPlainObject");
function cm(s) {
  return Object.prototype.toString.call(s) === "[object Object]";
}
n(cm, "hasObjectPrototype");
function H0(s) {
  return new Promise((t) => {
    jr.setTimeout(t, s);
  });
}
n(H0, "sleep");
function ly(s, t, e) {
  return typeof e.structuralSharing == "function" ? e.structuralSharing(s, t) : e.structuralSharing !== !1 ? Gd(s, t) : t;
}
n(ly, "replaceData");
function U0(s, t, e = 0) {
  const i = [...s, t];
  return e && i.length > e ? i.slice(1) : i;
}
n(U0, "addToEnd");
function B0(s, t, e = 0) {
  const i = [t, ...s];
  return e && i.length > e ? i.slice(0, -1) : i;
}
n(B0, "addToStart");
var ww = /* @__PURE__ */ Symbol();
function Ov(s, t) {
  return !s.queryFn && t?.initialPromise ? () => t.initialPromise : !s.queryFn || s.queryFn === ww ? () => Promise.reject(new Error(`Missing queryFn: '${s.queryHash}'`)) : s.queryFn;
}
n(Ov, "ensureQueryFn");
function N0(s, t, e) {
  let i = !1, r;
  return Object.defineProperty(s, "signal", {
    enumerable: !0,
    get: /* @__PURE__ */ n(() => (r ??= t(), i || (i = !0, r.aborted ? e() : r.addEventListener("abort", e, { once: !0 })), r), "get")
  }), s;
}
n(N0, "addConsumeAwareSignal");
var gs, Or, qn, Wn, q0 = (Wn = class extends pl {
  constructor() {
    super();
    T(this, gs);
    T(this, Or);
    T(this, qn);
    P(this, qn, (e) => {
      if (!qc && window.addEventListener) {
        const i = /* @__PURE__ */ n(() => e(), "listener");
        return window.addEventListener("visibilitychange", i, !1), () => {
          window.removeEventListener("visibilitychange", i);
        };
      }
    });
  }
  onSubscribe() {
    p(this, Or) || this.setEventListener(p(this, qn));
  }
  onUnsubscribe() {
    var e;
    this.hasListeners() || ((e = p(this, Or)) == null || e.call(this), P(this, Or, void 0));
  }
  setEventListener(e) {
    var i;
    P(this, qn, e), (i = p(this, Or)) == null || i.call(this), P(this, Or, e((r) => {
      typeof r == "boolean" ? this.setFocused(r) : this.onFocus();
    }));
  }
  setFocused(e) {
    p(this, gs) !== e && (P(this, gs, e), this.onFocus());
  }
  onFocus() {
    const e = this.isFocused();
    this.listeners.forEach((i) => {
      i(e);
    });
  }
  isFocused() {
    return typeof p(this, gs) == "boolean" ? p(this, gs) : globalThis.document?.visibilityState !== "hidden";
  }
}, gs = new WeakMap(), Or = new WeakMap(), qn = new WeakMap(), n(Wn, "FocusManager"), Wn), fw = new q0();
function dy() {
  let s, t;
  const e = new Promise((r, a) => {
    s = r, t = a;
  });
  e.status = "pending", e.catch(() => {
  });
  function i(r) {
    Object.assign(e, r), delete e.resolve, delete e.reject;
  }
  return n(i, "finalize"), e.resolve = (r) => {
    i({
      status: "fulfilled",
      value: r
    }), s(r);
  }, e.reject = (r) => {
    i({
      status: "rejected",
      reason: r
    }), t(r);
  }, e;
}
n(dy, "pendingThenable");
function W0(s) {
  let t;
  if (s.then((e) => (t = e, e), Me)?.catch(Me), t !== void 0)
    return { data: t };
}
n(W0, "tryResolveSync");
function zv(s) {
  return s;
}
n(zv, "defaultTransformerFn");
function j0(s) {
  return {
    mutationKey: s.options.mutationKey,
    state: s.state,
    ...s.options.scope && { scope: s.options.scope },
    ...s.meta && { meta: s.meta }
  };
}
n(j0, "dehydrateMutation");
function K0(s, t, e) {
  const i = /* @__PURE__ */ n(() => {
    const r = s.promise?.then(t).catch((a) => e(a) ? Promise.reject(new Error("redacted")) : Promise.reject(a));
    return r?.catch(Me), r;
  }, "dehydratePromise");
  return {
    dehydratedAt: Date.now(),
    state: {
      ...s.state,
      ...s.state.data !== void 0 && {
        data: t(s.state.data)
      }
    },
    queryKey: s.queryKey,
    queryHash: s.queryHash,
    ...s.state.status === "pending" && {
      promise: i()
    },
    ...s.meta && { meta: s.meta }
  };
}
n(K0, "dehydrateQuery");
function Q0(s) {
  return s.state.isPaused;
}
n(Q0, "defaultShouldDehydrateMutation");
function G0(s) {
  return s.state.status === "success";
}
n(G0, "defaultShouldDehydrateQuery");
function Z0(s) {
  return !0;
}
n(Z0, "defaultShouldRedactErrors");
function Y0(s, t = {}) {
  const e = t.shouldDehydrateMutation ?? s.getDefaultOptions().dehydrate?.shouldDehydrateMutation ?? Q0, i = s.getMutationCache().getAll().flatMap(
    (l) => e(l) ? [j0(l)] : []
  ), r = t.shouldDehydrateQuery ?? s.getDefaultOptions().dehydrate?.shouldDehydrateQuery ?? G0, a = t.shouldRedactErrors ?? s.getDefaultOptions().dehydrate?.shouldRedactErrors ?? Z0, o = t.serializeData ?? s.getDefaultOptions().dehydrate?.serializeData ?? zv, c = s.getQueryCache().getAll().flatMap(
    (l) => r(l) ? [K0(l, o, a)] : []
  );
  return { mutations: i, queries: c };
}
n(Y0, "dehydrate");
function X0(s, t, e) {
  if (typeof t != "object" || t === null)
    return;
  const i = s.getMutationCache(), r = s.getQueryCache(), a = s.getDefaultOptions().hydrate?.deserializeData ?? zv, o = t.mutations || [], c = t.queries || [];
  o.forEach(({ state: l, ...u }) => {
    i.build(
      s,
      {
        ...s.getDefaultOptions().hydrate?.mutations,
        ...e?.defaultOptions?.mutations,
        ...u
      },
      l
    );
  }), c.forEach(
    ({ queryKey: l, state: u, queryHash: b, meta: C, promise: v, dehydratedAt: f }) => {
      const m = v ? W0(v) : void 0, $ = u.data === void 0 ? m?.data : u.data, E = $ === void 0 ? $ : a($);
      let M = r.get(b);
      const A = M?.state.status === "pending", H = M?.state.fetchStatus === "fetching";
      if (M) {
        const D = m && // We only need this undefined check to handle older dehydration
        // payloads that might not have dehydratedAt
        f !== void 0 && f > M.state.dataUpdatedAt;
        if (u.dataUpdatedAt > M.state.dataUpdatedAt || D) {
          const { fetchStatus: I, ...B } = u;
          M.setState({
            ...B,
            data: E
          });
        }
      } else
        M = r.build(
          s,
          {
            ...s.getDefaultOptions().hydrate?.queries,
            ...e?.defaultOptions?.queries,
            queryKey: l,
            queryHash: b,
            meta: C
          },
          // Reset fetch status to idle to avoid
          // query being stuck in fetching state upon hydration
          {
            ...u,
            data: E,
            fetchStatus: "idle",
            status: E !== void 0 ? "success" : u.status
          }
        );
      v && !A && !H && // Only hydrate if dehydration is newer than any existing data,
      // this is always true for new queries
      (f === void 0 || f > M.state.dataUpdatedAt) && M.fetch(void 0, {
        // RSC transformed promises are not thenable
        initialPromise: Promise.resolve(v).then(a)
      }).catch(Me);
    }
  );
}
n(X0, "hydrate");
var J0 = I0;
function tb() {
  let s = [], t = 0, e = /* @__PURE__ */ n((c) => {
    c();
  }, "notifyFn"), i = /* @__PURE__ */ n((c) => {
    c();
  }, "batchNotifyFn"), r = J0;
  const a = /* @__PURE__ */ n((c) => {
    t ? s.push(c) : r(() => {
      e(c);
    });
  }, "schedule"), o = /* @__PURE__ */ n(() => {
    const c = s;
    s = [], c.length && r(() => {
      i(() => {
        c.forEach((l) => {
          e(l);
        });
      });
    });
  }, "flush");
  return {
    batch: /* @__PURE__ */ n((c) => {
      let l;
      t++;
      try {
        l = c();
      } finally {
        t--, t || o();
      }
      return l;
    }, "batch"),
    /**
     * All calls to the wrapped function will be batched.
     */
    batchCalls: /* @__PURE__ */ n((c) => (...l) => {
      a(() => {
        c(...l);
      });
    }, "batchCalls"),
    schedule: a,
    /**
     * Use this method to set a custom notify function.
     * This can be used to for example wrap notifications with `React.act` while running tests.
     */
    setNotifyFunction: /* @__PURE__ */ n((c) => {
      e = c;
    }, "setNotifyFunction"),
    /**
     * Use this method to set a custom function to batch notifications together into a single tick.
     * By default React Query will use the batch function provided by ReactDOM or React Native.
     */
    setBatchNotifyFunction: /* @__PURE__ */ n((c) => {
      i = c;
    }, "setBatchNotifyFunction"),
    setScheduler: /* @__PURE__ */ n((c) => {
      r = c;
    }, "setScheduler")
  };
}
n(tb, "createNotifyManager");
var Ce = tb(), jn, zr, Kn, Qn, eb = (Qn = class extends pl {
  constructor() {
    super();
    T(this, jn, !0);
    T(this, zr);
    T(this, Kn);
    P(this, Kn, (e) => {
      if (!qc && window.addEventListener) {
        const i = /* @__PURE__ */ n(() => e(!0), "onlineListener"), r = /* @__PURE__ */ n(() => e(!1), "offlineListener");
        return window.addEventListener("online", i, !1), window.addEventListener("offline", r, !1), () => {
          window.removeEventListener("online", i), window.removeEventListener("offline", r);
        };
      }
    });
  }
  onSubscribe() {
    p(this, zr) || this.setEventListener(p(this, Kn));
  }
  onUnsubscribe() {
    var e;
    this.hasListeners() || ((e = p(this, zr)) == null || e.call(this), P(this, zr, void 0));
  }
  setEventListener(e) {
    var i;
    P(this, Kn, e), (i = p(this, zr)) == null || i.call(this), P(this, zr, e(this.setOnline.bind(this)));
  }
  setOnline(e) {
    p(this, jn) !== e && (P(this, jn, e), this.listeners.forEach((r) => {
      r(e);
    }));
  }
  isOnline() {
    return p(this, jn);
  }
}, jn = new WeakMap(), zr = new WeakMap(), Kn = new WeakMap(), n(Qn, "OnlineManager"), Qn), jh = new eb();
function ib(s) {
  return Math.min(1e3 * 2 ** s, 3e4);
}
n(ib, "defaultRetryDelay");
function Fv(s) {
  return (s ?? "online") === "online" ? jh.isOnline() : !0;
}
n(Fv, "canFetch");
var Gn, hy = (Gn = class extends Error {
  constructor(t) {
    super("CancelledError"), this.revert = t?.revert, this.silent = t?.silent;
  }
}, n(Gn, "CancelledError"), Gn);
function Iv(s) {
  let t = !1, e = 0, i;
  const r = dy(), a = /* @__PURE__ */ n(() => r.status !== "pending", "isResolved"), o = /* @__PURE__ */ n(($) => {
    if (!a()) {
      const E = new hy($);
      v(E), s.onCancel?.(E);
    }
  }, "cancel"), c = /* @__PURE__ */ n(() => {
    t = !0;
  }, "cancelRetry"), l = /* @__PURE__ */ n(() => {
    t = !1;
  }, "continueRetry"), u = /* @__PURE__ */ n(() => fw.isFocused() && (s.networkMode === "always" || jh.isOnline()) && s.canRun(), "canContinue"), b = /* @__PURE__ */ n(() => Fv(s.networkMode) && s.canRun(), "canStart"), C = /* @__PURE__ */ n(($) => {
    a() || (i?.(), r.resolve($));
  }, "resolve"), v = /* @__PURE__ */ n(($) => {
    a() || (i?.(), r.reject($));
  }, "reject"), f = /* @__PURE__ */ n(() => new Promise(($) => {
    i = /* @__PURE__ */ n((E) => {
      (a() || u()) && $(E);
    }, "continueFn"), s.onPause?.();
  }).then(() => {
    i = void 0, a() || s.onContinue?.();
  }), "pause"), m = /* @__PURE__ */ n(() => {
    if (a())
      return;
    let $;
    const E = e === 0 ? s.initialPromise : void 0;
    try {
      $ = E ?? s.fn();
    } catch (M) {
      $ = Promise.reject(M);
    }
    Promise.resolve($).then(C).catch((M) => {
      if (a())
        return;
      const A = s.retry ?? (qc ? 0 : 3), H = s.retryDelay ?? ib, D = typeof H == "function" ? H(e, M) : H, I = A === !0 || typeof A == "number" && e < A || typeof A == "function" && A(e, M);
      if (t || !I) {
        v(M);
        return;
      }
      e++, s.onFail?.(e, M), H0(D).then(() => u() ? void 0 : f()).then(() => {
        t ? v(M) : m();
      });
    });
  }, "run");
  return {
    promise: r,
    status: /* @__PURE__ */ n(() => r.status, "status"),
    cancel: o,
    continue: /* @__PURE__ */ n(() => (i?.(), r), "continue"),
    cancelRetry: c,
    continueRetry: l,
    canStart: b,
    start: /* @__PURE__ */ n(() => (b() ? m() : f().then(m), r), "start")
  };
}
n(Iv, "createRetryer");
var bs, Zn, Dv = (Zn = class {
  constructor() {
    T(this, bs);
  }
  destroy() {
    this.clearGcTimeout();
  }
  scheduleGc() {
    this.clearGcTimeout(), oy(this.gcTime) && P(this, bs, jr.setTimeout(() => {
      this.optionalRemove();
    }, this.gcTime));
  }
  updateGcTime(t) {
    this.gcTime = Math.max(
      this.gcTime || 0,
      t ?? (qc ? 1 / 0 : 300 * 1e3)
    );
  }
  clearGcTimeout() {
    p(this, bs) && (jr.clearTimeout(p(this, bs)), P(this, bs, void 0));
  }
}, bs = new WeakMap(), n(Zn, "Removable"), Zn), xs, Yn, ti, Cs, we, bd, $s, li, ji, Xn, rb = (Xn = class extends Dv {
  constructor(e) {
    super();
    T(this, li);
    T(this, xs);
    T(this, Yn);
    T(this, ti);
    T(this, Cs);
    T(this, we);
    T(this, bd);
    T(this, $s);
    P(this, $s, !1), P(this, bd, e.defaultOptions), this.setOptions(e.options), this.observers = [], P(this, Cs, e.client), P(this, ti, p(this, Cs).getQueryCache()), this.queryKey = e.queryKey, this.queryHash = e.queryHash, P(this, xs, dm(this.options)), this.state = e.state ?? p(this, xs), this.scheduleGc();
  }
  get meta() {
    return this.options.meta;
  }
  get promise() {
    return p(this, we)?.promise;
  }
  setOptions(e) {
    if (this.options = { ...p(this, bd), ...e }, this.updateGcTime(this.options.gcTime), this.state && this.state.data === void 0) {
      const i = dm(this.options);
      i.data !== void 0 && (this.setState(
        lm(i.data, i.dataUpdatedAt)
      ), P(this, xs, i));
    }
  }
  optionalRemove() {
    !this.observers.length && this.state.fetchStatus === "idle" && p(this, ti).remove(this);
  }
  setData(e, i) {
    const r = ly(this.state.data, e, this.options);
    return dt(this, li, ji).call(this, {
      data: r,
      type: "success",
      dataUpdatedAt: i?.updatedAt,
      manual: i?.manual
    }), r;
  }
  setState(e, i) {
    dt(this, li, ji).call(this, { type: "setState", state: e, setStateOptions: i });
  }
  cancel(e) {
    const i = p(this, we)?.promise;
    return p(this, we)?.cancel(e), i ? i.then(Me).catch(Me) : Promise.resolve();
  }
  destroy() {
    super.destroy(), this.cancel({ silent: !0 });
  }
  reset() {
    this.destroy(), this.setState(p(this, xs));
  }
  isActive() {
    return this.observers.some(
      (e) => ii(e.options.enabled, this) !== !1
    );
  }
  isDisabled() {
    return this.getObserversCount() > 0 ? !this.isActive() : this.options.queryFn === ww || this.state.dataUpdateCount + this.state.errorUpdateCount === 0;
  }
  isStatic() {
    return this.getObserversCount() > 0 ? this.observers.some(
      (e) => Gr(e.options.staleTime, this) === "static"
    ) : !1;
  }
  isStale() {
    return this.getObserversCount() > 0 ? this.observers.some(
      (e) => e.getCurrentResult().isStale
    ) : this.state.data === void 0 || this.state.isInvalidated;
  }
  isStaleByTime(e = 0) {
    return this.state.data === void 0 ? !0 : e === "static" ? !1 : this.state.isInvalidated ? !0 : !Lv(this.state.dataUpdatedAt, e);
  }
  onFocus() {
    this.observers.find((i) => i.shouldFetchOnWindowFocus())?.refetch({ cancelRefetch: !1 }), p(this, we)?.continue();
  }
  onOnline() {
    this.observers.find((i) => i.shouldFetchOnReconnect())?.refetch({ cancelRefetch: !1 }), p(this, we)?.continue();
  }
  addObserver(e) {
    this.observers.includes(e) || (this.observers.push(e), this.clearGcTimeout(), p(this, ti).notify({ type: "observerAdded", query: this, observer: e }));
  }
  removeObserver(e) {
    this.observers.includes(e) && (this.observers = this.observers.filter((i) => i !== e), this.observers.length || (p(this, we) && (p(this, $s) ? p(this, we).cancel({ revert: !0 }) : p(this, we).cancelRetry()), this.scheduleGc()), p(this, ti).notify({ type: "observerRemoved", query: this, observer: e }));
  }
  getObserversCount() {
    return this.observers.length;
  }
  invalidate() {
    this.state.isInvalidated || dt(this, li, ji).call(this, { type: "invalidate" });
  }
  async fetch(e, i) {
    if (this.state.fetchStatus !== "idle" && // If the promise in the retryer is already rejected, we have to definitely
    // re-start the fetch; there is a chance that the query is still in a
    // pending state when that happens
    p(this, we)?.status() !== "rejected") {
      if (this.state.data !== void 0 && i?.cancelRefetch)
        this.cancel({ silent: !0 });
      else if (p(this, we))
        return p(this, we).continueRetry(), p(this, we).promise;
    }
    if (e && this.setOptions(e), !this.options.queryFn) {
      const u = this.observers.find((b) => b.options.queryFn);
      u && this.setOptions(u.options);
    }
    const r = new AbortController(), a = /* @__PURE__ */ n((u) => {
      Object.defineProperty(u, "signal", {
        enumerable: !0,
        get: /* @__PURE__ */ n(() => (P(this, $s, !0), r.signal), "get")
      });
    }, "addSignalProperty"), o = /* @__PURE__ */ n(() => {
      const u = Ov(this.options, i), C = (/* @__PURE__ */ n(() => {
        const v = {
          client: p(this, Cs),
          queryKey: this.queryKey,
          meta: this.meta
        };
        return a(v), v;
      }, "createQueryFnContext"))();
      return P(this, $s, !1), this.options.persister ? this.options.persister(
        u,
        C,
        this
      ) : u(C);
    }, "fetchFn"), l = (/* @__PURE__ */ n(() => {
      const u = {
        fetchOptions: i,
        options: this.options,
        queryKey: this.queryKey,
        client: p(this, Cs),
        state: this.state,
        fetchFn: o
      };
      return a(u), u;
    }, "createFetchContext"))();
    this.options.behavior?.onFetch(l, this), P(this, Yn, this.state), (this.state.fetchStatus === "idle" || this.state.fetchMeta !== l.fetchOptions?.meta) && dt(this, li, ji).call(this, { type: "fetch", meta: l.fetchOptions?.meta }), P(this, we, Iv({
      initialPromise: i?.initialPromise,
      fn: l.fetchFn,
      onCancel: /* @__PURE__ */ n((u) => {
        u instanceof hy && u.revert && this.setState({
          ...p(this, Yn),
          fetchStatus: "idle"
        }), r.abort();
      }, "onCancel"),
      onFail: /* @__PURE__ */ n((u, b) => {
        dt(this, li, ji).call(this, { type: "failed", failureCount: u, error: b });
      }, "onFail"),
      onPause: /* @__PURE__ */ n(() => {
        dt(this, li, ji).call(this, { type: "pause" });
      }, "onPause"),
      onContinue: /* @__PURE__ */ n(() => {
        dt(this, li, ji).call(this, { type: "continue" });
      }, "onContinue"),
      retry: l.options.retry,
      retryDelay: l.options.retryDelay,
      networkMode: l.options.networkMode,
      canRun: /* @__PURE__ */ n(() => !0, "canRun")
    }));
    try {
      const u = await p(this, we).start();
      if (u === void 0)
        throw new Error(`${this.queryHash} data is undefined`);
      return this.setData(u), p(this, ti).config.onSuccess?.(u, this), p(this, ti).config.onSettled?.(
        u,
        this.state.error,
        this
      ), u;
    } catch (u) {
      if (u instanceof hy) {
        if (u.silent)
          return p(this, we).promise;
        if (u.revert) {
          if (this.state.data === void 0)
            throw u;
          return this.state.data;
        }
      }
      throw dt(this, li, ji).call(this, {
        type: "error",
        error: u
      }), p(this, ti).config.onError?.(
        u,
        this
      ), p(this, ti).config.onSettled?.(
        this.state.data,
        u,
        this
      ), u;
    } finally {
      this.scheduleGc();
    }
  }
}, xs = new WeakMap(), Yn = new WeakMap(), ti = new WeakMap(), Cs = new WeakMap(), we = new WeakMap(), bd = new WeakMap(), $s = new WeakMap(), li = new WeakSet(), ji = /* @__PURE__ */ n(function(e) {
  const i = /* @__PURE__ */ n((r) => {
    switch (e.type) {
      case "failed":
        return {
          ...r,
          fetchFailureCount: e.failureCount,
          fetchFailureReason: e.error
        };
      case "pause":
        return {
          ...r,
          fetchStatus: "paused"
        };
      case "continue":
        return {
          ...r,
          fetchStatus: "fetching"
        };
      case "fetch":
        return {
          ...r,
          ...Vv(r.data, this.options),
          fetchMeta: e.meta ?? null
        };
      case "success":
        const a = {
          ...r,
          ...lm(e.data, e.dataUpdatedAt),
          dataUpdateCount: r.dataUpdateCount + 1,
          ...!e.manual && {
            fetchStatus: "idle",
            fetchFailureCount: 0,
            fetchFailureReason: null
          }
        };
        return P(this, Yn, e.manual ? a : void 0), a;
      case "error":
        const o = e.error;
        return {
          ...r,
          error: o,
          errorUpdateCount: r.errorUpdateCount + 1,
          errorUpdatedAt: Date.now(),
          fetchFailureCount: r.fetchFailureCount + 1,
          fetchFailureReason: o,
          fetchStatus: "idle",
          status: "error",
          // flag existing data as invalidated if we get a background error
          // note that "no data" always means stale so we can set unconditionally here
          isInvalidated: !0
        };
      case "invalidate":
        return {
          ...r,
          isInvalidated: !0
        };
      case "setState":
        return {
          ...r,
          ...e.state
        };
    }
  }, "reducer");
  this.state = i(this.state), Ce.batch(() => {
    this.observers.forEach((r) => {
      r.onQueryUpdate();
    }), p(this, ti).notify({ query: this, type: "updated", action: e });
  });
}, "#dispatch"), n(Xn, "Query"), Xn);
function Vv(s, t) {
  return {
    fetchFailureCount: 0,
    fetchFailureReason: null,
    fetchStatus: Fv(t.networkMode) ? "fetching" : "paused",
    ...s === void 0 && {
      error: null,
      status: "pending"
    }
  };
}
n(Vv, "fetchState");
function lm(s, t) {
  return {
    data: s,
    dataUpdatedAt: t ?? Date.now(),
    error: null,
    isInvalidated: !1,
    status: "success"
  };
}
n(lm, "successState");
function dm(s) {
  const t = typeof s.initialData == "function" ? s.initialData() : s.initialData, e = t !== void 0, i = e ? typeof s.initialDataUpdatedAt == "function" ? s.initialDataUpdatedAt() : s.initialDataUpdatedAt : 0;
  return {
    data: t,
    dataUpdateCount: 0,
    dataUpdatedAt: e ? i ?? Date.now() : 0,
    error: null,
    errorUpdateCount: 0,
    errorUpdatedAt: 0,
    fetchFailureCount: 0,
    fetchFailureReason: null,
    fetchMeta: null,
    isInvalidated: !1,
    status: e ? "success" : "pending",
    fetchStatus: "idle"
  };
}
n(dm, "getDefaultState$1");
var Ie, bt, xd, Le, ks, Jn, Qi, Fr, Cd, ta, ea, _s, Ps, Ir, ia, Pt, Ol, uy, py, yy, wy, fy, my, vy, Uv, ra, Hv = (ra = class extends pl {
  constructor(e, i) {
    super();
    T(this, Pt);
    T(this, Ie);
    T(this, bt);
    T(this, xd);
    T(this, Le);
    T(this, ks);
    T(this, Jn);
    T(this, Qi);
    T(this, Fr);
    T(this, Cd);
    T(this, ta);
    // This property keeps track of the last query with defined data.
    // It will be used to pass the previous data and query to the placeholder function between renders.
    T(this, ea);
    T(this, _s);
    T(this, Ps);
    T(this, Ir);
    T(this, ia, /* @__PURE__ */ new Set());
    this.options = i, P(this, Ie, e), P(this, Fr, null), P(this, Qi, dy()), this.bindMethods(), this.setOptions(i);
  }
  bindMethods() {
    this.refetch = this.refetch.bind(this);
  }
  onSubscribe() {
    this.listeners.size === 1 && (p(this, bt).addObserver(this), hm(p(this, bt), this.options) ? dt(this, Pt, Ol).call(this) : this.updateResult(), dt(this, Pt, wy).call(this));
  }
  onUnsubscribe() {
    this.hasListeners() || this.destroy();
  }
  shouldFetchOnReconnect() {
    return gy(
      p(this, bt),
      this.options,
      this.options.refetchOnReconnect
    );
  }
  shouldFetchOnWindowFocus() {
    return gy(
      p(this, bt),
      this.options,
      this.options.refetchOnWindowFocus
    );
  }
  destroy() {
    this.listeners = /* @__PURE__ */ new Set(), dt(this, Pt, fy).call(this), dt(this, Pt, my).call(this), p(this, bt).removeObserver(this);
  }
  setOptions(e) {
    const i = this.options, r = p(this, bt);
    if (this.options = p(this, Ie).defaultQueryOptions(e), this.options.enabled !== void 0 && typeof this.options.enabled != "boolean" && typeof this.options.enabled != "function" && typeof ii(this.options.enabled, p(this, bt)) != "boolean")
      throw new Error(
        "Expected enabled to be a boolean or a callback that returns a boolean"
      );
    dt(this, Pt, vy).call(this), p(this, bt).setOptions(this.options), i._defaulted && !Wh(this.options, i) && p(this, Ie).getQueryCache().notify({
      type: "observerOptionsUpdated",
      query: p(this, bt),
      observer: this
    });
    const a = this.hasListeners();
    a && um(
      p(this, bt),
      r,
      this.options,
      i
    ) && dt(this, Pt, Ol).call(this), this.updateResult(), a && (p(this, bt) !== r || ii(this.options.enabled, p(this, bt)) !== ii(i.enabled, p(this, bt)) || Gr(this.options.staleTime, p(this, bt)) !== Gr(i.staleTime, p(this, bt))) && dt(this, Pt, uy).call(this);
    const o = dt(this, Pt, py).call(this);
    a && (p(this, bt) !== r || ii(this.options.enabled, p(this, bt)) !== ii(i.enabled, p(this, bt)) || o !== p(this, Ir)) && dt(this, Pt, yy).call(this, o);
  }
  getOptimisticResult(e) {
    const i = p(this, Ie).getQueryCache().build(p(this, Ie), e), r = this.createResult(i, e);
    return nb(this, r) && (P(this, Le, r), P(this, Jn, this.options), P(this, ks, p(this, bt).state)), r;
  }
  getCurrentResult() {
    return p(this, Le);
  }
  trackResult(e, i) {
    return new Proxy(e, {
      get: /* @__PURE__ */ n((r, a) => (this.trackProp(a), i?.(a), a === "promise" && (this.trackProp("data"), !this.options.experimental_prefetchInRender && p(this, Qi).status === "pending" && p(this, Qi).reject(
        new Error(
          "experimental_prefetchInRender feature flag is not enabled"
        )
      )), Reflect.get(r, a)), "get")
    });
  }
  trackProp(e) {
    p(this, ia).add(e);
  }
  getCurrentQuery() {
    return p(this, bt);
  }
  refetch({ ...e } = {}) {
    return this.fetch({
      ...e
    });
  }
  fetchOptimistic(e) {
    const i = p(this, Ie).defaultQueryOptions(e), r = p(this, Ie).getQueryCache().build(p(this, Ie), i);
    return r.fetch().then(() => this.createResult(r, i));
  }
  fetch(e) {
    return dt(this, Pt, Ol).call(this, {
      ...e,
      cancelRefetch: e.cancelRefetch ?? !0
    }).then(() => (this.updateResult(), p(this, Le)));
  }
  createResult(e, i) {
    const r = p(this, bt), a = this.options, o = p(this, Le), c = p(this, ks), l = p(this, Jn), b = e !== r ? e.state : p(this, xd), { state: C } = e;
    let v = { ...C }, f = !1, m;
    if (i._optimisticResults) {
      const st = this.hasListeners(), Ot = !st && hm(e, i), zt = st && um(e, r, i, a);
      (Ot || zt) && (v = {
        ...v,
        ...Vv(C.data, e.options)
      }), i._optimisticResults === "isRestoring" && (v.fetchStatus = "idle");
    }
    let { error: $, errorUpdatedAt: E, status: M } = v;
    m = v.data;
    let A = !1;
    if (i.placeholderData !== void 0 && m === void 0 && M === "pending") {
      let st;
      o?.isPlaceholderData && i.placeholderData === l?.placeholderData ? (st = o.data, A = !0) : st = typeof i.placeholderData == "function" ? i.placeholderData(
        p(this, ea)?.state.data,
        p(this, ea)
      ) : i.placeholderData, st !== void 0 && (M = "success", m = ly(
        o?.data,
        st,
        i
      ), f = !0);
    }
    if (i.select && m !== void 0 && !A)
      if (o && m === c?.data && i.select === p(this, Cd))
        m = p(this, ta);
      else
        try {
          P(this, Cd, i.select), m = i.select(m), m = ly(o?.data, m, i), P(this, ta, m), P(this, Fr, null);
        } catch (st) {
          P(this, Fr, st);
        }
    p(this, Fr) && ($ = p(this, Fr), m = p(this, ta), E = Date.now(), M = "error");
    const H = v.fetchStatus === "fetching", D = M === "pending", I = M === "error", B = D && H, Q = m !== void 0, ct = {
      status: M,
      fetchStatus: v.fetchStatus,
      isPending: D,
      isSuccess: M === "success",
      isError: I,
      isInitialLoading: B,
      isLoading: B,
      data: m,
      dataUpdatedAt: v.dataUpdatedAt,
      error: $,
      errorUpdatedAt: E,
      failureCount: v.fetchFailureCount,
      failureReason: v.fetchFailureReason,
      errorUpdateCount: v.errorUpdateCount,
      isFetched: v.dataUpdateCount > 0 || v.errorUpdateCount > 0,
      isFetchedAfterMount: v.dataUpdateCount > b.dataUpdateCount || v.errorUpdateCount > b.errorUpdateCount,
      isFetching: H,
      isRefetching: H && !D,
      isLoadingError: I && !Q,
      isPaused: v.fetchStatus === "paused",
      isPlaceholderData: f,
      isRefetchError: I && Q,
      isStale: mw(e, i),
      refetch: this.refetch,
      promise: p(this, Qi),
      isEnabled: ii(i.enabled, e) !== !1
    };
    if (this.options.experimental_prefetchInRender) {
      const st = ct.data !== void 0, Ot = ct.status === "error" && !st, zt = /* @__PURE__ */ n((Dt) => {
        Ot ? Dt.reject(ct.error) : st && Dt.resolve(ct.data);
      }, "finalizeThenableIfPossible"), Mt = /* @__PURE__ */ n(() => {
        const Dt = P(this, Qi, ct.promise = dy());
        zt(Dt);
      }, "recreateThenable"), vt = p(this, Qi);
      switch (vt.status) {
        case "pending":
          e.queryHash === r.queryHash && zt(vt);
          break;
        case "fulfilled":
          (Ot || ct.data !== vt.value) && Mt();
          break;
        case "rejected":
          (!Ot || ct.error !== vt.reason) && Mt();
          break;
      }
    }
    return ct;
  }
  updateResult() {
    const e = p(this, Le), i = this.createResult(p(this, bt), this.options);
    if (P(this, ks, p(this, bt).state), P(this, Jn, this.options), p(this, ks).data !== void 0 && P(this, ea, p(this, bt)), Wh(i, e))
      return;
    P(this, Le, i);
    const r = /* @__PURE__ */ n(() => {
      if (!e)
        return !0;
      const { notifyOnChangeProps: a } = this.options, o = typeof a == "function" ? a() : a;
      if (o === "all" || !o && !p(this, ia).size)
        return !0;
      const c = new Set(
        o ?? p(this, ia)
      );
      return this.options.throwOnError && c.add("error"), Object.keys(p(this, Le)).some((l) => {
        const u = l;
        return p(this, Le)[u] !== e[u] && c.has(u);
      });
    }, "shouldNotifyListeners");
    dt(this, Pt, Uv).call(this, { listeners: r() });
  }
  onQueryUpdate() {
    this.updateResult(), this.hasListeners() && dt(this, Pt, wy).call(this);
  }
}, Ie = new WeakMap(), bt = new WeakMap(), xd = new WeakMap(), Le = new WeakMap(), ks = new WeakMap(), Jn = new WeakMap(), Qi = new WeakMap(), Fr = new WeakMap(), Cd = new WeakMap(), ta = new WeakMap(), ea = new WeakMap(), _s = new WeakMap(), Ps = new WeakMap(), Ir = new WeakMap(), ia = new WeakMap(), Pt = new WeakSet(), Ol = /* @__PURE__ */ n(function(e) {
  dt(this, Pt, vy).call(this);
  let i = p(this, bt).fetch(
    this.options,
    e
  );
  return e?.throwOnError || (i = i.catch(Me)), i;
}, "#executeFetch"), uy = /* @__PURE__ */ n(function() {
  dt(this, Pt, fy).call(this);
  const e = Gr(
    this.options.staleTime,
    p(this, bt)
  );
  if (qc || p(this, Le).isStale || !oy(e))
    return;
  const r = Lv(p(this, Le).dataUpdatedAt, e) + 1;
  P(this, _s, jr.setTimeout(() => {
    p(this, Le).isStale || this.updateResult();
  }, r));
}, "#updateStaleTimeout"), py = /* @__PURE__ */ n(function() {
  return (typeof this.options.refetchInterval == "function" ? this.options.refetchInterval(p(this, bt)) : this.options.refetchInterval) ?? !1;
}, "#computeRefetchInterval"), yy = /* @__PURE__ */ n(function(e) {
  dt(this, Pt, my).call(this), P(this, Ir, e), !(qc || ii(this.options.enabled, p(this, bt)) === !1 || !oy(p(this, Ir)) || p(this, Ir) === 0) && P(this, Ps, jr.setInterval(() => {
    (this.options.refetchIntervalInBackground || fw.isFocused()) && dt(this, Pt, Ol).call(this);
  }, p(this, Ir)));
}, "#updateRefetchInterval"), wy = /* @__PURE__ */ n(function() {
  dt(this, Pt, uy).call(this), dt(this, Pt, yy).call(this, dt(this, Pt, py).call(this));
}, "#updateTimers"), fy = /* @__PURE__ */ n(function() {
  p(this, _s) && (jr.clearTimeout(p(this, _s)), P(this, _s, void 0));
}, "#clearStaleTimeout"), my = /* @__PURE__ */ n(function() {
  p(this, Ps) && (jr.clearInterval(p(this, Ps)), P(this, Ps, void 0));
}, "#clearRefetchInterval"), vy = /* @__PURE__ */ n(function() {
  const e = p(this, Ie).getQueryCache().build(p(this, Ie), this.options);
  if (e === p(this, bt))
    return;
  const i = p(this, bt);
  P(this, bt, e), P(this, xd, e.state), this.hasListeners() && (i?.removeObserver(this), e.addObserver(this));
}, "#updateQuery"), Uv = /* @__PURE__ */ n(function(e) {
  Ce.batch(() => {
    e.listeners && this.listeners.forEach((i) => {
      i(p(this, Le));
    }), p(this, Ie).getQueryCache().notify({
      query: p(this, bt),
      type: "observerResultsUpdated"
    });
  });
}, "#notify"), n(ra, "QueryObserver"), ra);
function sb(s, t) {
  return ii(t.enabled, s) !== !1 && s.state.data === void 0 && !(s.state.status === "error" && t.retryOnMount === !1);
}
n(sb, "shouldLoadOnMount");
function hm(s, t) {
  return sb(s, t) || s.state.data !== void 0 && gy(s, t, t.refetchOnMount);
}
n(hm, "shouldFetchOnMount");
function gy(s, t, e) {
  if (ii(t.enabled, s) !== !1 && Gr(t.staleTime, s) !== "static") {
    const i = typeof e == "function" ? e(s) : e;
    return i === "always" || i !== !1 && mw(s, t);
  }
  return !1;
}
n(gy, "shouldFetchOn");
function um(s, t, e, i) {
  return (s !== t || ii(i.enabled, s) === !1) && (!e.suspense || s.state.status !== "error") && mw(s, e);
}
n(um, "shouldFetchOptionally");
function mw(s, t) {
  return ii(t.enabled, s) !== !1 && s.isStaleByTime(Gr(t.staleTime, s));
}
n(mw, "isStale");
function nb(s, t) {
  return !Wh(s.getCurrentResult(), t);
}
n(nb, "shouldAssignObserverCurrentProperties");
function Kh(s) {
  return {
    onFetch: /* @__PURE__ */ n((t, e) => {
      const i = t.options, r = t.fetchOptions?.meta?.fetchMore?.direction, a = t.state.data?.pages || [], o = t.state.data?.pageParams || [];
      let c = { pages: [], pageParams: [] }, l = 0;
      const u = /* @__PURE__ */ n(async () => {
        let b = !1;
        const C = /* @__PURE__ */ n((m) => {
          N0(
            m,
            () => t.signal,
            () => b = !0
          );
        }, "addSignalProperty"), v = Ov(t.options, t.fetchOptions), f = /* @__PURE__ */ n(async (m, $, E) => {
          if (b)
            return Promise.reject();
          if ($ == null && m.pages.length)
            return Promise.resolve(m);
          const A = (/* @__PURE__ */ n(() => {
            const B = {
              client: t.client,
              queryKey: t.queryKey,
              pageParam: $,
              direction: E ? "backward" : "forward",
              meta: t.options.meta
            };
            return C(B), B;
          }, "createQueryFnContext"))(), H = await v(A), { maxPages: D } = t.options, I = E ? B0 : U0;
          return {
            pages: I(m.pages, H, D),
            pageParams: I(m.pageParams, $, D)
          };
        }, "fetchPage");
        if (r && a.length) {
          const m = r === "backward", $ = m ? Bv : by, E = {
            pages: a,
            pageParams: o
          }, M = $(i, E);
          c = await f(E, M, m);
        } else {
          const m = s ?? a.length;
          do {
            const $ = l === 0 ? o[0] ?? i.initialPageParam : by(i, c);
            if (l > 0 && $ == null)
              break;
            c = await f(c, $), l++;
          } while (l < m);
        }
        return c;
      }, "fetchFn");
      t.options.persister ? t.fetchFn = () => t.options.persister?.(
        u,
        {
          client: t.client,
          queryKey: t.queryKey,
          meta: t.options.meta,
          signal: t.signal
        },
        e
      ) : t.fetchFn = u;
    }, "onFetch")
  };
}
n(Kh, "infiniteQueryBehavior");
function by(s, { pages: t, pageParams: e }) {
  const i = t.length - 1;
  return t.length > 0 ? s.getNextPageParam(
    t[i],
    t,
    e[i],
    e
  ) : void 0;
}
n(by, "getNextPageParam");
function Bv(s, { pages: t, pageParams: e }) {
  return t.length > 0 ? s.getPreviousPageParam?.(t[0], t, e[0], e) : void 0;
}
n(Bv, "getPreviousPageParam");
function ab(s, t) {
  return t ? by(s, t) != null : !1;
}
n(ab, "hasNextPage");
function ob(s, t) {
  return !t || !s.getPreviousPageParam ? !1 : Bv(s, t) != null;
}
n(ob, "hasPreviousPage");
var sa, cb = (sa = class extends Hv {
  constructor(t, e) {
    super(t, e);
  }
  bindMethods() {
    super.bindMethods(), this.fetchNextPage = this.fetchNextPage.bind(this), this.fetchPreviousPage = this.fetchPreviousPage.bind(this);
  }
  setOptions(t) {
    super.setOptions({
      ...t,
      behavior: Kh()
    });
  }
  getOptimisticResult(t) {
    return t.behavior = Kh(), super.getOptimisticResult(t);
  }
  fetchNextPage(t) {
    return this.fetch({
      ...t,
      meta: {
        fetchMore: { direction: "forward" }
      }
    });
  }
  fetchPreviousPage(t) {
    return this.fetch({
      ...t,
      meta: {
        fetchMore: { direction: "backward" }
      }
    });
  }
  createResult(t, e) {
    const { state: i } = t, r = super.createResult(t, e), { isFetching: a, isRefetching: o, isError: c, isRefetchError: l } = r, u = i.fetchMeta?.fetchMore?.direction, b = c && u === "forward", C = a && u === "forward", v = c && u === "backward", f = a && u === "backward";
    return {
      ...r,
      fetchNextPage: this.fetchNextPage,
      fetchPreviousPage: this.fetchPreviousPage,
      hasNextPage: ab(e, i.data),
      hasPreviousPage: ob(e, i.data),
      isFetchNextPageError: b,
      isFetchingNextPage: C,
      isFetchPreviousPageError: v,
      isFetchingPreviousPage: f,
      isRefetchError: l && !b && !v,
      isRefetching: o && !C && !f
    };
  }
}, n(sa, "InfiniteQueryObserver"), sa), $d, _i, Se, Ss, Pi, Er, na, lb = (na = class extends Dv {
  constructor(e) {
    super();
    T(this, Pi);
    T(this, $d);
    T(this, _i);
    T(this, Se);
    T(this, Ss);
    P(this, $d, e.client), this.mutationId = e.mutationId, P(this, Se, e.mutationCache), P(this, _i, []), this.state = e.state || Nv(), this.setOptions(e.options), this.scheduleGc();
  }
  setOptions(e) {
    this.options = e, this.updateGcTime(this.options.gcTime);
  }
  get meta() {
    return this.options.meta;
  }
  addObserver(e) {
    p(this, _i).includes(e) || (p(this, _i).push(e), this.clearGcTimeout(), p(this, Se).notify({
      type: "observerAdded",
      mutation: this,
      observer: e
    }));
  }
  removeObserver(e) {
    P(this, _i, p(this, _i).filter((i) => i !== e)), this.scheduleGc(), p(this, Se).notify({
      type: "observerRemoved",
      mutation: this,
      observer: e
    });
  }
  optionalRemove() {
    p(this, _i).length || (this.state.status === "pending" ? this.scheduleGc() : p(this, Se).remove(this));
  }
  continue() {
    return p(this, Ss)?.continue() ?? // continuing a mutation assumes that variables are set, mutation must have been dehydrated before
    this.execute(this.state.variables);
  }
  async execute(e) {
    const i = /* @__PURE__ */ n(() => {
      dt(this, Pi, Er).call(this, { type: "continue" });
    }, "onContinue"), r = {
      client: p(this, $d),
      meta: this.options.meta,
      mutationKey: this.options.mutationKey
    };
    P(this, Ss, Iv({
      fn: /* @__PURE__ */ n(() => this.options.mutationFn ? this.options.mutationFn(e, r) : Promise.reject(new Error("No mutationFn found")), "fn"),
      onFail: /* @__PURE__ */ n((c, l) => {
        dt(this, Pi, Er).call(this, { type: "failed", failureCount: c, error: l });
      }, "onFail"),
      onPause: /* @__PURE__ */ n(() => {
        dt(this, Pi, Er).call(this, { type: "pause" });
      }, "onPause"),
      onContinue: i,
      retry: this.options.retry ?? 0,
      retryDelay: this.options.retryDelay,
      networkMode: this.options.networkMode,
      canRun: /* @__PURE__ */ n(() => p(this, Se).canRun(this), "canRun")
    }));
    const a = this.state.status === "pending", o = !p(this, Ss).canStart();
    try {
      if (a)
        i();
      else {
        dt(this, Pi, Er).call(this, { type: "pending", variables: e, isPaused: o }), p(this, Se).config.onMutate && await p(this, Se).config.onMutate(
          e,
          this,
          r
        );
        const l = await this.options.onMutate?.(
          e,
          r
        );
        l !== this.state.context && dt(this, Pi, Er).call(this, {
          type: "pending",
          context: l,
          variables: e,
          isPaused: o
        });
      }
      const c = await p(this, Ss).start();
      return await p(this, Se).config.onSuccess?.(
        c,
        e,
        this.state.context,
        this,
        r
      ), await this.options.onSuccess?.(
        c,
        e,
        this.state.context,
        r
      ), await p(this, Se).config.onSettled?.(
        c,
        null,
        this.state.variables,
        this.state.context,
        this,
        r
      ), await this.options.onSettled?.(
        c,
        null,
        e,
        this.state.context,
        r
      ), dt(this, Pi, Er).call(this, { type: "success", data: c }), c;
    } catch (c) {
      try {
        await p(this, Se).config.onError?.(
          c,
          e,
          this.state.context,
          this,
          r
        );
      } catch (l) {
        Promise.reject(l);
      }
      try {
        await this.options.onError?.(
          c,
          e,
          this.state.context,
          r
        );
      } catch (l) {
        Promise.reject(l);
      }
      try {
        await p(this, Se).config.onSettled?.(
          void 0,
          c,
          this.state.variables,
          this.state.context,
          this,
          r
        );
      } catch (l) {
        Promise.reject(l);
      }
      try {
        await this.options.onSettled?.(
          void 0,
          c,
          e,
          this.state.context,
          r
        );
      } catch (l) {
        Promise.reject(l);
      }
      throw dt(this, Pi, Er).call(this, { type: "error", error: c }), c;
    } finally {
      p(this, Se).runNext(this);
    }
  }
}, $d = new WeakMap(), _i = new WeakMap(), Se = new WeakMap(), Ss = new WeakMap(), Pi = new WeakSet(), Er = /* @__PURE__ */ n(function(e) {
  const i = /* @__PURE__ */ n((r) => {
    switch (e.type) {
      case "failed":
        return {
          ...r,
          failureCount: e.failureCount,
          failureReason: e.error
        };
      case "pause":
        return {
          ...r,
          isPaused: !0
        };
      case "continue":
        return {
          ...r,
          isPaused: !1
        };
      case "pending":
        return {
          ...r,
          context: e.context,
          data: void 0,
          failureCount: 0,
          failureReason: null,
          error: null,
          isPaused: e.isPaused,
          status: "pending",
          variables: e.variables,
          submittedAt: Date.now()
        };
      case "success":
        return {
          ...r,
          data: e.data,
          failureCount: 0,
          failureReason: null,
          error: null,
          status: "success",
          isPaused: !1
        };
      case "error":
        return {
          ...r,
          data: void 0,
          error: e.error,
          failureCount: r.failureCount + 1,
          failureReason: e.error,
          isPaused: !1,
          status: "error"
        };
    }
  }, "reducer");
  this.state = i(this.state), Ce.batch(() => {
    p(this, _i).forEach((r) => {
      r.onMutationUpdate(e);
    }), p(this, Se).notify({
      mutation: this,
      type: "updated",
      action: e
    });
  });
}, "#dispatch"), n(na, "Mutation"), na);
function Nv() {
  return {
    context: void 0,
    data: void 0,
    error: null,
    failureCount: 0,
    failureReason: null,
    isPaused: !1,
    status: "idle",
    variables: void 0,
    submittedAt: 0
  };
}
n(Nv, "getDefaultState");
var Gi, di, kd, aa, db = (aa = class extends pl {
  constructor(e = {}) {
    super();
    T(this, Gi);
    T(this, di);
    T(this, kd);
    this.config = e, P(this, Gi, /* @__PURE__ */ new Set()), P(this, di, /* @__PURE__ */ new Map()), P(this, kd, 0);
  }
  build(e, i, r) {
    const a = new lb({
      client: e,
      mutationCache: this,
      mutationId: ++gh(this, kd)._,
      options: e.defaultMutationOptions(i),
      state: r
    });
    return this.add(a), a;
  }
  add(e) {
    p(this, Gi).add(e);
    const i = xh(e);
    if (typeof i == "string") {
      const r = p(this, di).get(i);
      r ? r.push(e) : p(this, di).set(i, [e]);
    }
    this.notify({ type: "added", mutation: e });
  }
  remove(e) {
    if (p(this, Gi).delete(e)) {
      const i = xh(e);
      if (typeof i == "string") {
        const r = p(this, di).get(i);
        if (r)
          if (r.length > 1) {
            const a = r.indexOf(e);
            a !== -1 && r.splice(a, 1);
          } else r[0] === e && p(this, di).delete(i);
      }
    }
    this.notify({ type: "removed", mutation: e });
  }
  canRun(e) {
    const i = xh(e);
    if (typeof i == "string") {
      const a = p(this, di).get(i)?.find(
        (o) => o.state.status === "pending"
      );
      return !a || a === e;
    } else
      return !0;
  }
  runNext(e) {
    const i = xh(e);
    return typeof i == "string" ? p(this, di).get(i)?.find((a) => a !== e && a.state.isPaused)?.continue() ?? Promise.resolve() : Promise.resolve();
  }
  clear() {
    Ce.batch(() => {
      p(this, Gi).forEach((e) => {
        this.notify({ type: "removed", mutation: e });
      }), p(this, Gi).clear(), p(this, di).clear();
    });
  }
  getAll() {
    return Array.from(p(this, Gi));
  }
  find(e) {
    const i = { exact: !0, ...e };
    return this.getAll().find(
      (r) => am(i, r)
    );
  }
  findAll(e = {}) {
    return this.getAll().filter((i) => am(e, i));
  }
  notify(e) {
    Ce.batch(() => {
      this.listeners.forEach((i) => {
        i(e);
      });
    });
  }
  resumePausedMutations() {
    const e = this.getAll().filter((i) => i.state.isPaused);
    return Ce.batch(
      () => Promise.all(
        e.map((i) => i.continue().catch(Me))
      )
    );
  }
}, Gi = new WeakMap(), di = new WeakMap(), kd = new WeakMap(), n(aa, "MutationCache"), aa);
function xh(s) {
  return s.options.scope?.id;
}
n(xh, "scopeFor");
var Zi, Dr, De, Yi, sr, Ph, xy, oa, St = (oa = class extends pl {
  constructor(e, i) {
    super();
    T(this, sr);
    T(this, Zi);
    T(this, Dr);
    T(this, De);
    T(this, Yi);
    P(this, Zi, e), this.setOptions(i), this.bindMethods(), dt(this, sr, Ph).call(this);
  }
  bindMethods() {
    this.mutate = this.mutate.bind(this), this.reset = this.reset.bind(this);
  }
  setOptions(e) {
    const i = this.options;
    this.options = p(this, Zi).defaultMutationOptions(e), Wh(this.options, i) || p(this, Zi).getMutationCache().notify({
      type: "observerOptionsUpdated",
      mutation: p(this, De),
      observer: this
    }), i?.mutationKey && this.options.mutationKey && Ds(i.mutationKey) !== Ds(this.options.mutationKey) ? this.reset() : p(this, De)?.state.status === "pending" && p(this, De).setOptions(this.options);
  }
  onUnsubscribe() {
    this.hasListeners() || p(this, De)?.removeObserver(this);
  }
  onMutationUpdate(e) {
    dt(this, sr, Ph).call(this), dt(this, sr, xy).call(this, e);
  }
  getCurrentResult() {
    return p(this, Dr);
  }
  reset() {
    p(this, De)?.removeObserver(this), P(this, De, void 0), dt(this, sr, Ph).call(this), dt(this, sr, xy).call(this);
  }
  mutate(e, i) {
    return P(this, Yi, i), p(this, De)?.removeObserver(this), P(this, De, p(this, Zi).getMutationCache().build(p(this, Zi), this.options)), p(this, De).addObserver(this), p(this, De).execute(e);
  }
}, Zi = new WeakMap(), Dr = new WeakMap(), De = new WeakMap(), Yi = new WeakMap(), sr = new WeakSet(), Ph = /* @__PURE__ */ n(function() {
  const e = p(this, De)?.state ?? Nv();
  P(this, Dr, {
    ...e,
    isPending: e.status === "pending",
    isSuccess: e.status === "success",
    isError: e.status === "error",
    isIdle: e.status === "idle",
    mutate: this.mutate,
    reset: this.reset
  });
}, "#updateResult"), xy = /* @__PURE__ */ n(function(e) {
  Ce.batch(() => {
    if (p(this, Yi) && this.hasListeners()) {
      const i = p(this, Dr).variables, r = p(this, Dr).context, a = {
        client: p(this, Zi),
        meta: this.options.meta,
        mutationKey: this.options.mutationKey
      };
      if (e?.type === "success") {
        try {
          p(this, Yi).onSuccess?.(
            e.data,
            i,
            r,
            a
          );
        } catch (o) {
          Promise.reject(o);
        }
        try {
          p(this, Yi).onSettled?.(
            e.data,
            null,
            i,
            r,
            a
          );
        } catch (o) {
          Promise.reject(o);
        }
      } else if (e?.type === "error") {
        try {
          p(this, Yi).onError?.(
            e.error,
            i,
            r,
            a
          );
        } catch (o) {
          Promise.reject(o);
        }
        try {
          p(this, Yi).onSettled?.(
            void 0,
            e.error,
            i,
            r,
            a
          );
        } catch (o) {
          Promise.reject(o);
        }
      }
    }
    this.listeners.forEach((i) => {
      i(p(this, Dr));
    });
  });
}, "#notify"), n(oa, "MutationObserver"), oa), Si, ca, hb = (ca = class extends pl {
  constructor(e = {}) {
    super();
    T(this, Si);
    this.config = e, P(this, Si, /* @__PURE__ */ new Map());
  }
  build(e, i, r) {
    const a = i.queryKey, o = i.queryHash ?? yw(a, i);
    let c = this.get(o);
    return c || (c = new rb({
      client: e,
      queryKey: a,
      queryHash: o,
      options: e.defaultQueryOptions(i),
      state: r,
      defaultOptions: e.getQueryDefaults(a)
    }), this.add(c)), c;
  }
  add(e) {
    p(this, Si).has(e.queryHash) || (p(this, Si).set(e.queryHash, e), this.notify({
      type: "added",
      query: e
    }));
  }
  remove(e) {
    const i = p(this, Si).get(e.queryHash);
    i && (e.destroy(), i === e && p(this, Si).delete(e.queryHash), this.notify({ type: "removed", query: e }));
  }
  clear() {
    Ce.batch(() => {
      this.getAll().forEach((e) => {
        this.remove(e);
      });
    });
  }
  get(e) {
    return p(this, Si).get(e);
  }
  getAll() {
    return [...p(this, Si).values()];
  }
  find(e) {
    const i = { exact: !0, ...e };
    return this.getAll().find(
      (r) => nm(i, r)
    );
  }
  findAll(e = {}) {
    const i = this.getAll();
    return Object.keys(e).length > 0 ? i.filter((r) => nm(e, r)) : i;
  }
  notify(e) {
    Ce.batch(() => {
      this.listeners.forEach((i) => {
        i(e);
      });
    });
  }
  onFocus() {
    Ce.batch(() => {
      this.getAll().forEach((e) => {
        e.onFocus();
      });
    });
  }
  onOnline() {
    Ce.batch(() => {
      this.getAll().forEach((e) => {
        e.onOnline();
      });
    });
  }
}, Si = new WeakMap(), n(ca, "QueryCache"), ca), Ht, Vr, Hr, la, da, Ur, ha, ua, pa, ub = (pa = class {
  constructor(t = {}) {
    T(this, Ht);
    T(this, Vr);
    T(this, Hr);
    T(this, la);
    T(this, da);
    T(this, Ur);
    T(this, ha);
    T(this, ua);
    P(this, Ht, t.queryCache || new hb()), P(this, Vr, t.mutationCache || new db()), P(this, Hr, t.defaultOptions || {}), P(this, la, /* @__PURE__ */ new Map()), P(this, da, /* @__PURE__ */ new Map()), P(this, Ur, 0);
  }
  mount() {
    gh(this, Ur)._++, p(this, Ur) === 1 && (P(this, ha, fw.subscribe(async (t) => {
      t && (await this.resumePausedMutations(), p(this, Ht).onFocus());
    })), P(this, ua, jh.subscribe(async (t) => {
      t && (await this.resumePausedMutations(), p(this, Ht).onOnline());
    })));
  }
  unmount() {
    var t, e;
    gh(this, Ur)._--, p(this, Ur) === 0 && ((t = p(this, ha)) == null || t.call(this), P(this, ha, void 0), (e = p(this, ua)) == null || e.call(this), P(this, ua, void 0));
  }
  isFetching(t) {
    return p(this, Ht).findAll({ ...t, fetchStatus: "fetching" }).length;
  }
  isMutating(t) {
    return p(this, Vr).findAll({ ...t, status: "pending" }).length;
  }
  /**
   * Imperative (non-reactive) way to retrieve data for a QueryKey.
   * Should only be used in callbacks or functions where reading the latest data is necessary, e.g. for optimistic updates.
   *
   * Hint: Do not use this function inside a component, because it won't receive updates.
   * Use `useQuery` to create a `QueryObserver` that subscribes to changes.
   */
  getQueryData(t) {
    const e = this.defaultQueryOptions({ queryKey: t });
    return p(this, Ht).get(e.queryHash)?.state.data;
  }
  ensureQueryData(t) {
    const e = this.defaultQueryOptions(t), i = p(this, Ht).build(this, e), r = i.state.data;
    return r === void 0 ? this.fetchQuery(t) : (t.revalidateIfStale && i.isStaleByTime(Gr(e.staleTime, i)) && this.prefetchQuery(e), Promise.resolve(r));
  }
  getQueriesData(t) {
    return p(this, Ht).findAll(t).map(({ queryKey: e, state: i }) => {
      const r = i.data;
      return [e, r];
    });
  }
  setQueryData(t, e, i) {
    const r = this.defaultQueryOptions({ queryKey: t }), o = p(this, Ht).get(
      r.queryHash
    )?.state.data, c = D0(e, o);
    if (c !== void 0)
      return p(this, Ht).build(this, r).setData(c, { ...i, manual: !0 });
  }
  setQueriesData(t, e, i) {
    return Ce.batch(
      () => p(this, Ht).findAll(t).map(({ queryKey: r }) => [
        r,
        this.setQueryData(r, e, i)
      ])
    );
  }
  getQueryState(t) {
    const e = this.defaultQueryOptions({ queryKey: t });
    return p(this, Ht).get(
      e.queryHash
    )?.state;
  }
  removeQueries(t) {
    const e = p(this, Ht);
    Ce.batch(() => {
      e.findAll(t).forEach((i) => {
        e.remove(i);
      });
    });
  }
  resetQueries(t, e) {
    const i = p(this, Ht);
    return Ce.batch(() => (i.findAll(t).forEach((r) => {
      r.reset();
    }), this.refetchQueries(
      {
        type: "active",
        ...t
      },
      e
    )));
  }
  cancelQueries(t, e = {}) {
    const i = { revert: !0, ...e }, r = Ce.batch(
      () => p(this, Ht).findAll(t).map((a) => a.cancel(i))
    );
    return Promise.all(r).then(Me).catch(Me);
  }
  invalidateQueries(t, e = {}) {
    return Ce.batch(() => (p(this, Ht).findAll(t).forEach((i) => {
      i.invalidate();
    }), t?.refetchType === "none" ? Promise.resolve() : this.refetchQueries(
      {
        ...t,
        type: t?.refetchType ?? t?.type ?? "active"
      },
      e
    )));
  }
  refetchQueries(t, e = {}) {
    const i = {
      ...e,
      cancelRefetch: e.cancelRefetch ?? !0
    }, r = Ce.batch(
      () => p(this, Ht).findAll(t).filter((a) => !a.isDisabled() && !a.isStatic()).map((a) => {
        let o = a.fetch(void 0, i);
        return i.throwOnError || (o = o.catch(Me)), a.state.fetchStatus === "paused" ? Promise.resolve() : o;
      })
    );
    return Promise.all(r).then(Me);
  }
  fetchQuery(t) {
    const e = this.defaultQueryOptions(t);
    e.retry === void 0 && (e.retry = !1);
    const i = p(this, Ht).build(this, e);
    return i.isStaleByTime(
      Gr(e.staleTime, i)
    ) ? i.fetch(e) : Promise.resolve(i.state.data);
  }
  prefetchQuery(t) {
    return this.fetchQuery(t).then(Me).catch(Me);
  }
  fetchInfiniteQuery(t) {
    return t.behavior = Kh(t.pages), this.fetchQuery(t);
  }
  prefetchInfiniteQuery(t) {
    return this.fetchInfiniteQuery(t).then(Me).catch(Me);
  }
  ensureInfiniteQueryData(t) {
    return t.behavior = Kh(t.pages), this.ensureQueryData(t);
  }
  resumePausedMutations() {
    return jh.isOnline() ? p(this, Vr).resumePausedMutations() : Promise.resolve();
  }
  getQueryCache() {
    return p(this, Ht);
  }
  getMutationCache() {
    return p(this, Vr);
  }
  getDefaultOptions() {
    return p(this, Hr);
  }
  setDefaultOptions(t) {
    P(this, Hr, t);
  }
  setQueryDefaults(t, e) {
    p(this, la).set(Ds(t), {
      queryKey: t,
      defaultOptions: e
    });
  }
  getQueryDefaults(t) {
    const e = [...p(this, la).values()], i = {};
    return e.forEach((r) => {
      Nl(t, r.queryKey) && Object.assign(i, r.defaultOptions);
    }), i;
  }
  setMutationDefaults(t, e) {
    p(this, da).set(Ds(t), {
      mutationKey: t,
      defaultOptions: e
    });
  }
  getMutationDefaults(t) {
    const e = [...p(this, da).values()], i = {};
    return e.forEach((r) => {
      Nl(t, r.mutationKey) && Object.assign(i, r.defaultOptions);
    }), i;
  }
  defaultQueryOptions(t) {
    if (t._defaulted)
      return t;
    const e = {
      ...p(this, Hr).queries,
      ...this.getQueryDefaults(t.queryKey),
      ...t,
      _defaulted: !0
    };
    return e.queryHash || (e.queryHash = yw(
      e.queryKey,
      e
    )), e.refetchOnReconnect === void 0 && (e.refetchOnReconnect = e.networkMode !== "always"), e.throwOnError === void 0 && (e.throwOnError = !!e.suspense), !e.networkMode && e.persister && (e.networkMode = "offlineFirst"), e.queryFn === ww && (e.enabled = !1), e;
  }
  defaultMutationOptions(t) {
    return t?._defaulted ? t : {
      ...p(this, Hr).mutations,
      ...t?.mutationKey && this.getMutationDefaults(t.mutationKey),
      ...t,
      _defaulted: !0
    };
  }
  clear() {
    p(this, Ht).clear(), p(this, Vr).clear();
  }
}, Ht = new WeakMap(), Vr = new WeakMap(), Hr = new WeakMap(), la = new WeakMap(), da = new WeakMap(), Ur = new WeakMap(), ha = new WeakMap(), ua = new WeakMap(), n(pa, "QueryClient"), pa);
function Up() {
}
n(Up, "noop");
function pb({
  storage: s,
  key: t = "REACT_QUERY_OFFLINE_CACHE",
  throttleTime: e = 1e3,
  serialize: i = JSON.stringify,
  deserialize: r = JSON.parse,
  retry: a
}) {
  if (s) {
    const o = /* @__PURE__ */ n((c) => {
      try {
        s.setItem(t, i(c));
        return;
      } catch (l) {
        return l;
      }
    }, "trySave");
    return {
      persistClient: yb((c) => {
        let l = c, u = o(l), b = 0;
        for (; u && l; )
          b++, l = a?.({
            persistedClient: l,
            error: u,
            errorCount: b
          }), l && (u = o(l));
      }, e),
      restoreClient: /* @__PURE__ */ n(() => {
        const c = s.getItem(t);
        if (c)
          return r(c);
      }, "restoreClient"),
      removeClient: /* @__PURE__ */ n(() => {
        s.removeItem(t);
      }, "removeClient")
    };
  }
  return {
    persistClient: Up,
    restoreClient: Up,
    removeClient: Up
  };
}
n(pb, "createSyncStoragePersister");
function yb(s, t = 100) {
  let e = null, i;
  return function(...r) {
    i = r, e === null && (e = jr.setTimeout(() => {
      s(...i), e = null;
    }, t));
  };
}
n(yb, "throttle$1");
var wb = ["added", "removed", "updated"];
function pm(s) {
  return wb.includes(s);
}
n(pm, "isCacheEventType");
async function fb({
  queryClient: s,
  persister: t,
  maxAge: e = 1e3 * 60 * 60 * 24,
  buster: i = "",
  hydrateOptions: r
}) {
  try {
    const a = await t.restoreClient();
    if (a)
      if (a.timestamp) {
        const o = Date.now() - a.timestamp > e, c = a.buster !== i;
        if (o || c)
          return t.removeClient();
        X0(s, a.clientState, r);
      } else
        return t.removeClient();
  } catch (a) {
    throw await t.removeClient(), a;
  }
}
n(fb, "persistQueryClientRestore");
async function ym({
  queryClient: s,
  persister: t,
  buster: e = "",
  dehydrateOptions: i
}) {
  const r = {
    buster: e,
    timestamp: Date.now(),
    clientState: Y0(s, i)
  };
  await t.persistClient(r);
}
n(ym, "persistQueryClientSave");
function mb(s) {
  const t = s.queryClient.getQueryCache().subscribe((i) => {
    pm(i.type) && ym(s);
  }), e = s.queryClient.getMutationCache().subscribe((i) => {
    pm(i.type) && ym(s);
  });
  return () => {
    t(), e();
  };
}
n(mb, "persistQueryClientSubscribe");
const vb = /* @__PURE__ */ n((s) => {
  var t;
  return t = class extends s {
    // QUERY CLIENT
    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    constructor(...i) {
      super(...i), this._queryClient = new ub({
        defaultOptions: {
          queries: {
            staleTime: this.staleTime,
            gcTime: this.gcTime
          }
        }
      }), this.initQueryClient();
    }
    get queryClient() {
      return this._queryClient;
    }
    async initQueryClient() {
      if (this.isDestroyed)
        throw new Ut();
      await this.whenUrl();
      try {
        this._sessionStoragePersister = pb({
          key: "WEAVY_QUERY_OFFLINE_CACHE",
          storage: window.sessionStorage,
          throttleTime: this.staleTime
        });
        const i = {
          queryClient: this._queryClient,
          persister: this._sessionStoragePersister,
          maxAge: this.gcTime,
          // 24h - should match gcTime
          buster: this.cachePrefix,
          // Cache busting parameter (build hash or similar)
          hydrateOptions: void 0,
          dehydrateOptions: {
            shouldDehydrateMutation: /* @__PURE__ */ n((r) => {
              const a = r.state.context?.status?.state === "pending";
              return !!(r.state.context && !a || r.state.isPaused);
            }, "shouldDehydrateMutation")
          }
        };
        await fb(i), this._unsubscribeQueryClient = mb(i);
      } catch {
        console.warn(this.weavyId, "Query cache persister not available.");
      }
      this.host.isConnected && this._queryClient.mount(), this._hostIsConnectedObserver = n0(this.host, (i) => {
        this.isDestroyed || (i ? (console.info(this.weavyId, "Query client mounted"), this._queryClient.mount()) : (console.info(this.weavyId, "Query client unmounted"), this._queryClient.unmount()));
      });
    }
    async reset() {
      await super.reset(), await this._queryClient.cancelQueries(), await this._sessionStoragePersister?.removeClient(), await this.queryClient.resetQueries();
    }
    async disconnectQueryClient() {
      console.info(this.weavyId, "Query client disconnected"), await this._queryClient.cancelQueries(), await this._sessionStoragePersister?.removeClient(), this._unsubscribeQueryClient?.(), this._queryClient.unmount(), this._queryClient.clear();
    }
    destroy() {
      super.destroy(), this._hostIsConnectedObserver?.disconnect(), this.disconnectQueryClient();
    }
  }, n(t, "WeavyQuery"), t;
}, "WeavyQueryMixin"), gb = /* @__PURE__ */ n((s) => {
  var t;
  return t = class extends s {
    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    constructor(...i) {
      super(...i);
    }
    set annotations(i) {
      this._annotations = i, this.updateContext();
    }
    get annotations() {
      return this._annotations ?? Qt.defaults.annotations;
    }
    set enterToSend(i) {
      this._enterToSend = i, this.updateContext();
    }
    get enterToSend() {
      return this._enterToSend ?? Qt.defaults.enterToSend;
    }
    set reactions(i) {
      this._reactions = i, this.updateContext();
    }
    get reactions() {
      return this._reactions ?? Qt.defaults.reactions;
    }
  }, n(t, "WeavySettings"), t;
}, "WeavySettingsMixin");
function Fe(s) {
  return s < 0 ? -1 : s === 0 ? 0 : 1;
}
n(Fe, "signum");
function Il(s, t, e) {
  return (1 - e) * s + e * t;
}
n(Il, "lerp");
function bb(s, t, e) {
  return e < s ? s : e > t ? t : e;
}
n(bb, "clampInt");
function xe(s, t, e) {
  return e < s ? s : e > t ? t : e;
}
n(xe, "clampDouble");
function wm(s) {
  return s = s % 360, s < 0 && (s = s + 360), s;
}
n(wm, "sanitizeDegreesInt");
function Ve(s) {
  return s = s % 360, s < 0 && (s = s + 360), s;
}
n(Ve, "sanitizeDegreesDouble");
function Cy(s, t) {
  const e = s[0] * t[0][0] + s[1] * t[0][1] + s[2] * t[0][2], i = s[0] * t[1][0] + s[1] * t[1][1] + s[2] * t[1][2], r = s[0] * t[2][0] + s[1] * t[2][1] + s[2] * t[2][2];
  return [e, i, r];
}
n(Cy, "matrixMultiply");
const qv = [
  [0.41233895, 0.35762064, 0.18051042],
  [0.2126, 0.7152, 0.0722],
  [0.01932141, 0.11916382, 0.95034478]
], xb = [
  [
    3.2413774792388685,
    -1.5376652402851851,
    -0.49885366846268053
  ],
  [
    -0.9691452513005321,
    1.8758853451067872,
    0.04156585616912061
  ],
  [
    0.05562093689691305,
    -0.20395524564742123,
    1.0571799111220335
  ]
], Wv = [95.047, 100, 108.883];
function vw(s, t, e) {
  return (255 << 24 | (s & 255) << 16 | (t & 255) << 8 | e & 255) >>> 0;
}
n(vw, "argbFromRgb");
function fm(s) {
  const t = Fs(s[0]), e = Fs(s[1]), i = Fs(s[2]);
  return vw(t, e, i);
}
n(fm, "argbFromLinrgb");
function Cb(s) {
  return s >> 24 & 255;
}
n(Cb, "alphaFromArgb");
function gw(s) {
  return s >> 16 & 255;
}
n(gw, "redFromArgb");
function bw(s) {
  return s >> 8 & 255;
}
n(bw, "greenFromArgb");
function xw(s) {
  return s & 255;
}
n(xw, "blueFromArgb");
function $b(s, t, e) {
  const i = xb, r = i[0][0] * s + i[0][1] * t + i[0][2] * e, a = i[1][0] * s + i[1][1] * t + i[1][2] * e, o = i[2][0] * s + i[2][1] * t + i[2][2] * e, c = Fs(r), l = Fs(a), u = Fs(o);
  return vw(c, l, u);
}
n($b, "argbFromXyz");
function kb(s) {
  const t = ir(gw(s)), e = ir(bw(s)), i = ir(xw(s));
  return Cy([t, e, i], qv);
}
n(kb, "xyzFromArgb");
function _b(s) {
  const t = ir(gw(s)), e = ir(bw(s)), i = ir(xw(s)), r = qv, a = r[0][0] * t + r[0][1] * e + r[0][2] * i, o = r[1][0] * t + r[1][1] * e + r[1][2] * i, c = r[2][0] * t + r[2][1] * e + r[2][2] * i, l = Wv, u = a / l[0], b = o / l[1], C = c / l[2], v = Dl(u), f = Dl(b), m = Dl(C), $ = 116 * f - 16, E = 500 * (v - f), M = 200 * (f - m);
  return [$, E, M];
}
n(_b, "labFromArgb");
function Pb(s) {
  const t = Kr(s), e = Fs(t);
  return vw(e, e, e);
}
n(Pb, "argbFromLstar");
function mm(s) {
  const t = kb(s)[1];
  return 116 * Dl(t / 100) - 16;
}
n(mm, "lstarFromArgb");
function Kr(s) {
  return 100 * Mb((s + 16) / 116);
}
n(Kr, "yFromLstar");
function $y(s) {
  return Dl(s / 100) * 116 - 16;
}
n($y, "lstarFromY");
function ir(s) {
  const t = s / 255;
  return t <= 0.040449936 ? t / 12.92 * 100 : Math.pow((t + 0.055) / 1.055, 2.4) * 100;
}
n(ir, "linearized");
function Fs(s) {
  const t = s / 100;
  let e = 0;
  return t <= 31308e-7 ? e = t * 12.92 : e = 1.055 * Math.pow(t, 1 / 2.4) - 0.055, bb(0, 255, Math.round(e * 255));
}
n(Fs, "delinearized");
function Sb() {
  return Wv;
}
n(Sb, "whitePointD65");
function Dl(s) {
  const t = 0.008856451679035631, e = 24389 / 27;
  return s > t ? Math.pow(s, 1 / 3) : (e * s + 16) / 116;
}
n(Dl, "labF");
function Mb(s) {
  const t = 0.008856451679035631, e = 24389 / 27, i = s * s * s;
  return i > t ? i : (116 * s - 16) / e;
}
n(Mb, "labInvf");
const yu = class yu {
  /**
   * Create ViewingConditions from a simple, physically relevant, set of
   * parameters.
   *
   * @param whitePoint White point, measured in the XYZ color space.
   *     default = D65, or sunny day afternoon
   * @param adaptingLuminance The luminance of the adapting field. Informally,
   *     how bright it is in the room where the color is viewed. Can be
   *     calculated from lux by multiplying lux by 0.0586. default = 11.72,
   *     or 200 lux.
   * @param backgroundLstar The lightness of the area surrounding the color.
   *     measured by L* in L*a*b*. default = 50.0
   * @param surround A general description of the lighting surrounding the
   *     color. 0 is pitch dark, like watching a movie in a theater. 1.0 is a
   *     dimly light room, like watching TV at home at night. 2.0 means there
   *     is no difference between the lighting on the color and around it.
   *     default = 2.0
   * @param discountingIlluminant Whether the eye accounts for the tint of the
   *     ambient lighting, such as knowing an apple is still red in green light.
   *     default = false, the eye does not perform this process on
   *       self-luminous objects like displays.
   */
  static make(t = Sb(), e = 200 / Math.PI * Kr(50) / 100, i = 50, r = 2, a = !1) {
    const o = t, c = o[0] * 0.401288 + o[1] * 0.650173 + o[2] * -0.051461, l = o[0] * -0.250268 + o[1] * 1.204414 + o[2] * 0.045854, u = o[0] * -2079e-6 + o[1] * 0.048952 + o[2] * 0.953127, b = 0.8 + r / 10, C = b >= 0.9 ? Il(0.59, 0.69, (b - 0.9) * 10) : Il(0.525, 0.59, (b - 0.8) * 10);
    let v = a ? 1 : b * (1 - 1 / 3.6 * Math.exp((-e - 42) / 92));
    v = v > 1 ? 1 : v < 0 ? 0 : v;
    const f = b, m = [
      v * (100 / c) + 1 - v,
      v * (100 / l) + 1 - v,
      v * (100 / u) + 1 - v
    ], $ = 1 / (5 * e + 1), E = $ * $ * $ * $, M = 1 - E, A = E * e + 0.1 * M * M * Math.cbrt(5 * e), H = Kr(i) / t[1], D = 1.48 + Math.sqrt(H), I = 0.725 / Math.pow(H, 0.2), B = I, Q = [
      Math.pow(A * m[0] * c / 100, 0.42),
      Math.pow(A * m[1] * l / 100, 0.42),
      Math.pow(A * m[2] * u / 100, 0.42)
    ], it = [
      400 * Q[0] / (Q[0] + 27.13),
      400 * Q[1] / (Q[1] + 27.13),
      400 * Q[2] / (Q[2] + 27.13)
    ], ct = (2 * it[0] + it[1] + 0.05 * it[2]) * I;
    return new yu(H, ct, I, B, C, f, m, A, Math.pow(A, 0.25), D);
  }
  /**
   * Parameters are intermediate values of the CAM16 conversion process. Their
   * names are shorthand for technical color science terminology, this class
   * would not benefit from documenting them individually. A brief overview
   * is available in the CAM16 specification, and a complete overview requires
   * a color science textbook, such as Fairchild's Color Appearance Models.
   */
  constructor(t, e, i, r, a, o, c, l, u, b) {
    this.n = t, this.aw = e, this.nbb = i, this.ncb = r, this.c = a, this.nc = o, this.rgbD = c, this.fl = l, this.fLRoot = u, this.z = b;
  }
};
n(yu, "ViewingConditions");
let Ai = yu;
Ai.DEFAULT = Ai.make();
const ki = class ki {
  /**
   * All of the CAM16 dimensions can be calculated from 3 of the dimensions, in
   * the following combinations:
   *      -  {j or q} and {c, m, or s} and hue
   *      - jstar, astar, bstar
   * Prefer using a static method that constructs from 3 of those dimensions.
   * This constructor is intended for those methods to use to return all
   * possible dimensions.
   *
   * @param hue
   * @param chroma informally, colorfulness / color intensity. like saturation
   *     in HSL, except perceptually accurate.
   * @param j lightness
   * @param q brightness; ratio of lightness to white point's lightness
   * @param m colorfulness
   * @param s saturation; ratio of chroma to white point's chroma
   * @param jstar CAM16-UCS J coordinate
   * @param astar CAM16-UCS a coordinate
   * @param bstar CAM16-UCS b coordinate
   */
  constructor(t, e, i, r, a, o, c, l, u) {
    this.hue = t, this.chroma = e, this.j = i, this.q = r, this.m = a, this.s = o, this.jstar = c, this.astar = l, this.bstar = u;
  }
  /**
   * CAM16 instances also have coordinates in the CAM16-UCS space, called J*,
   * a*, b*, or jstar, astar, bstar in code. CAM16-UCS is included in the CAM16
   * specification, and is used to measure distances between colors.
   */
  distance(t) {
    const e = this.jstar - t.jstar, i = this.astar - t.astar, r = this.bstar - t.bstar, a = Math.sqrt(e * e + i * i + r * r);
    return 1.41 * Math.pow(a, 0.63);
  }
  /**
   * @param argb ARGB representation of a color.
   * @return CAM16 color, assuming the color was viewed in default viewing
   *     conditions.
   */
  static fromInt(t) {
    return ki.fromIntInViewingConditions(t, Ai.DEFAULT);
  }
  /**
   * @param argb ARGB representation of a color.
   * @param viewingConditions Information about the environment where the color
   *     was observed.
   * @return CAM16 color.
   */
  static fromIntInViewingConditions(t, e) {
    const i = (t & 16711680) >> 16, r = (t & 65280) >> 8, a = t & 255, o = ir(i), c = ir(r), l = ir(a), u = 0.41233895 * o + 0.35762064 * c + 0.18051042 * l, b = 0.2126 * o + 0.7152 * c + 0.0722 * l, C = 0.01932141 * o + 0.11916382 * c + 0.95034478 * l, v = 0.401288 * u + 0.650173 * b - 0.051461 * C, f = -0.250268 * u + 1.204414 * b + 0.045854 * C, m = -2079e-6 * u + 0.048952 * b + 0.953127 * C, $ = e.rgbD[0] * v, E = e.rgbD[1] * f, M = e.rgbD[2] * m, A = Math.pow(e.fl * Math.abs($) / 100, 0.42), H = Math.pow(e.fl * Math.abs(E) / 100, 0.42), D = Math.pow(e.fl * Math.abs(M) / 100, 0.42), I = Fe($) * 400 * A / (A + 27.13), B = Fe(E) * 400 * H / (H + 27.13), Q = Fe(M) * 400 * D / (D + 27.13), it = (11 * I + -12 * B + Q) / 11, ct = (I + B - 2 * Q) / 9, st = (20 * I + 20 * B + 21 * Q) / 20, Ot = (40 * I + 20 * B + Q) / 20, Mt = Math.atan2(ct, it) * 180 / Math.PI, vt = Ve(Mt), Dt = vt * Math.PI / 180, ge = Ot * e.nbb, At = 100 * Math.pow(ge / e.aw, e.c * e.z), xt = 4 / e.c * Math.sqrt(At / 100) * (e.aw + 4) * e.fLRoot, ee = vt < 20.14 ? vt + 360 : vt, lt = 0.25 * (Math.cos(ee * Math.PI / 180 + 2) + 3.8), ci = 5e4 / 13 * lt * e.nc * e.ncb * Math.sqrt(it * it + ct * ct) / (st + 0.305), Ke = Math.pow(ci, 0.9) * Math.pow(1.64 - Math.pow(0.29, e.n), 0.73), Ni = Ke * Math.sqrt(At / 100), qi = Ni * e.fLRoot, El = 50 * Math.sqrt(Ke * e.c / (e.aw + 4)), Ci = (1 + 100 * 7e-3) * At / (1 + 7e-3 * At), Ct = 1 / 0.0228 * Math.log(1 + 0.0228 * qi), ze = Ct * Math.cos(Dt), Et = Ct * Math.sin(Dt);
    return new ki(vt, Ni, At, xt, qi, El, Ci, ze, Et);
  }
  /**
   * @param j CAM16 lightness
   * @param c CAM16 chroma
   * @param h CAM16 hue
   */
  static fromJch(t, e, i) {
    return ki.fromJchInViewingConditions(t, e, i, Ai.DEFAULT);
  }
  /**
   * @param j CAM16 lightness
   * @param c CAM16 chroma
   * @param h CAM16 hue
   * @param viewingConditions Information about the environment where the color
   *     was observed.
   */
  static fromJchInViewingConditions(t, e, i, r) {
    const a = 4 / r.c * Math.sqrt(t / 100) * (r.aw + 4) * r.fLRoot, o = e * r.fLRoot, c = e / Math.sqrt(t / 100), l = 50 * Math.sqrt(c * r.c / (r.aw + 4)), u = i * Math.PI / 180, b = (1 + 100 * 7e-3) * t / (1 + 7e-3 * t), C = 1 / 0.0228 * Math.log(1 + 0.0228 * o), v = C * Math.cos(u), f = C * Math.sin(u);
    return new ki(i, e, t, a, o, l, b, v, f);
  }
  /**
   * @param jstar CAM16-UCS lightness.
   * @param astar CAM16-UCS a dimension. Like a* in L*a*b*, it is a Cartesian
   *     coordinate on the Y axis.
   * @param bstar CAM16-UCS b dimension. Like a* in L*a*b*, it is a Cartesian
   *     coordinate on the X axis.
   */
  static fromUcs(t, e, i) {
    return ki.fromUcsInViewingConditions(t, e, i, Ai.DEFAULT);
  }
  /**
   * @param jstar CAM16-UCS lightness.
   * @param astar CAM16-UCS a dimension. Like a* in L*a*b*, it is a Cartesian
   *     coordinate on the Y axis.
   * @param bstar CAM16-UCS b dimension. Like a* in L*a*b*, it is a Cartesian
   *     coordinate on the X axis.
   * @param viewingConditions Information about the environment where the color
   *     was observed.
   */
  static fromUcsInViewingConditions(t, e, i, r) {
    const a = e, o = i, c = Math.sqrt(a * a + o * o), u = (Math.exp(c * 0.0228) - 1) / 0.0228 / r.fLRoot;
    let b = Math.atan2(o, a) * (180 / Math.PI);
    b < 0 && (b += 360);
    const C = t / (1 - (t - 100) * 7e-3);
    return ki.fromJchInViewingConditions(C, u, b, r);
  }
  /**
   *  @return ARGB representation of color, assuming the color was viewed in
   *     default viewing conditions, which are near-identical to the default
   *     viewing conditions for sRGB.
   */
  toInt() {
    return this.viewed(Ai.DEFAULT);
  }
  /**
   * @param viewingConditions Information about the environment where the color
   *     will be viewed.
   * @return ARGB representation of color
   */
  viewed(t) {
    const e = this.chroma === 0 || this.j === 0 ? 0 : this.chroma / Math.sqrt(this.j / 100), i = Math.pow(e / Math.pow(1.64 - Math.pow(0.29, t.n), 0.73), 1 / 0.9), r = this.hue * Math.PI / 180, a = 0.25 * (Math.cos(r + 2) + 3.8), o = t.aw * Math.pow(this.j / 100, 1 / t.c / t.z), c = a * (5e4 / 13) * t.nc * t.ncb, l = o / t.nbb, u = Math.sin(r), b = Math.cos(r), C = 23 * (l + 0.305) * i / (23 * c + 11 * i * b + 108 * i * u), v = C * b, f = C * u, m = (460 * l + 451 * v + 288 * f) / 1403, $ = (460 * l - 891 * v - 261 * f) / 1403, E = (460 * l - 220 * v - 6300 * f) / 1403, M = Math.max(0, 27.13 * Math.abs(m) / (400 - Math.abs(m))), A = Fe(m) * (100 / t.fl) * Math.pow(M, 1 / 0.42), H = Math.max(0, 27.13 * Math.abs($) / (400 - Math.abs($))), D = Fe($) * (100 / t.fl) * Math.pow(H, 1 / 0.42), I = Math.max(0, 27.13 * Math.abs(E) / (400 - Math.abs(E))), B = Fe(E) * (100 / t.fl) * Math.pow(I, 1 / 0.42), Q = A / t.rgbD[0], it = D / t.rgbD[1], ct = B / t.rgbD[2], st = 1.86206786 * Q - 1.01125463 * it + 0.14918677 * ct, Ot = 0.38752654 * Q + 0.62144744 * it - 897398e-8 * ct, zt = -0.0158415 * Q - 0.03412294 * it + 1.04996444 * ct;
    return $b(st, Ot, zt);
  }
  /// Given color expressed in XYZ and viewed in [viewingConditions], convert to
  /// CAM16.
  static fromXyzInViewingConditions(t, e, i, r) {
    const a = 0.401288 * t + 0.650173 * e - 0.051461 * i, o = -0.250268 * t + 1.204414 * e + 0.045854 * i, c = -2079e-6 * t + 0.048952 * e + 0.953127 * i, l = r.rgbD[0] * a, u = r.rgbD[1] * o, b = r.rgbD[2] * c, C = Math.pow(r.fl * Math.abs(l) / 100, 0.42), v = Math.pow(r.fl * Math.abs(u) / 100, 0.42), f = Math.pow(r.fl * Math.abs(b) / 100, 0.42), m = Fe(l) * 400 * C / (C + 27.13), $ = Fe(u) * 400 * v / (v + 27.13), E = Fe(b) * 400 * f / (f + 27.13), M = (11 * m + -12 * $ + E) / 11, A = (m + $ - 2 * E) / 9, H = (20 * m + 20 * $ + 21 * E) / 20, D = (40 * m + 20 * $ + E) / 20, B = Math.atan2(A, M) * 180 / Math.PI, Q = B < 0 ? B + 360 : B >= 360 ? B - 360 : B, it = Q * Math.PI / 180, ct = D * r.nbb, st = 100 * Math.pow(ct / r.aw, r.c * r.z), Ot = 4 / r.c * Math.sqrt(st / 100) * (r.aw + 4) * r.fLRoot, zt = Q < 20.14 ? Q + 360 : Q, Mt = 1 / 4 * (Math.cos(zt * Math.PI / 180 + 2) + 3.8), Dt = 5e4 / 13 * Mt * r.nc * r.ncb * Math.sqrt(M * M + A * A) / (H + 0.305), ge = Math.pow(Dt, 0.9) * Math.pow(1.64 - Math.pow(0.29, r.n), 0.73), At = ge * Math.sqrt(st / 100), xt = At * r.fLRoot, ee = 50 * Math.sqrt(ge * r.c / (r.aw + 4)), lt = (1 + 100 * 7e-3) * st / (1 + 7e-3 * st), $t = Math.log(1 + 0.0228 * xt) / 0.0228, ci = $t * Math.cos(it), Ke = $t * Math.sin(it);
    return new ki(Q, At, st, Ot, xt, ee, lt, ci, Ke);
  }
  /// XYZ representation of CAM16 seen in [viewingConditions].
  xyzInViewingConditions(t) {
    const e = this.chroma === 0 || this.j === 0 ? 0 : this.chroma / Math.sqrt(this.j / 100), i = Math.pow(e / Math.pow(1.64 - Math.pow(0.29, t.n), 0.73), 1 / 0.9), r = this.hue * Math.PI / 180, a = 0.25 * (Math.cos(r + 2) + 3.8), o = t.aw * Math.pow(this.j / 100, 1 / t.c / t.z), c = a * (5e4 / 13) * t.nc * t.ncb, l = o / t.nbb, u = Math.sin(r), b = Math.cos(r), C = 23 * (l + 0.305) * i / (23 * c + 11 * i * b + 108 * i * u), v = C * b, f = C * u, m = (460 * l + 451 * v + 288 * f) / 1403, $ = (460 * l - 891 * v - 261 * f) / 1403, E = (460 * l - 220 * v - 6300 * f) / 1403, M = Math.max(0, 27.13 * Math.abs(m) / (400 - Math.abs(m))), A = Fe(m) * (100 / t.fl) * Math.pow(M, 1 / 0.42), H = Math.max(0, 27.13 * Math.abs($) / (400 - Math.abs($))), D = Fe($) * (100 / t.fl) * Math.pow(H, 1 / 0.42), I = Math.max(0, 27.13 * Math.abs(E) / (400 - Math.abs(E))), B = Fe(E) * (100 / t.fl) * Math.pow(I, 1 / 0.42), Q = A / t.rgbD[0], it = D / t.rgbD[1], ct = B / t.rgbD[2], st = 1.86206786 * Q - 1.01125463 * it + 0.14918677 * ct, Ot = 0.38752654 * Q + 0.62144744 * it - 897398e-8 * ct, zt = -0.0158415 * Q - 0.03412294 * it + 1.04996444 * ct;
    return [st, Ot, zt];
  }
};
n(ki, "Cam16");
let vs = ki;
const wt = class wt {
  /**
   * Sanitizes a small enough angle in radians.
   *
   * @param angle An angle in radians; must not deviate too much
   * from 0.
   * @return A coterminal angle between 0 and 2pi.
   */
  static sanitizeRadians(t) {
    return (t + Math.PI * 8) % (Math.PI * 2);
  }
  /**
   * Delinearizes an RGB component, returning a floating-point
   * number.
   *
   * @param rgbComponent 0.0 <= rgb_component <= 100.0, represents
   * linear R/G/B channel
   * @return 0.0 <= output <= 255.0, color channel converted to
   * regular RGB space
   */
  static trueDelinearized(t) {
    const e = t / 100;
    let i = 0;
    return e <= 31308e-7 ? i = e * 12.92 : i = 1.055 * Math.pow(e, 1 / 2.4) - 0.055, i * 255;
  }
  static chromaticAdaptation(t) {
    const e = Math.pow(Math.abs(t), 0.42);
    return Fe(t) * 400 * e / (e + 27.13);
  }
  /**
   * Returns the hue of a linear RGB color in CAM16.
   *
   * @param linrgb The linear RGB coordinates of a color.
   * @return The hue of the color in CAM16, in radians.
   */
  static hueOf(t) {
    const e = Cy(t, wt.SCALED_DISCOUNT_FROM_LINRGB), i = wt.chromaticAdaptation(e[0]), r = wt.chromaticAdaptation(e[1]), a = wt.chromaticAdaptation(e[2]), o = (11 * i + -12 * r + a) / 11, c = (i + r - 2 * a) / 9;
    return Math.atan2(c, o);
  }
  static areInCyclicOrder(t, e, i) {
    const r = wt.sanitizeRadians(e - t), a = wt.sanitizeRadians(i - t);
    return r < a;
  }
  /**
   * Solves the lerp equation.
   *
   * @param source The starting number.
   * @param mid The number in the middle.
   * @param target The ending number.
   * @return A number t such that lerp(source, target, t) = mid.
   */
  static intercept(t, e, i) {
    return (e - t) / (i - t);
  }
  static lerpPoint(t, e, i) {
    return [
      t[0] + (i[0] - t[0]) * e,
      t[1] + (i[1] - t[1]) * e,
      t[2] + (i[2] - t[2]) * e
    ];
  }
  /**
   * Intersects a segment with a plane.
   *
   * @param source The coordinates of point A.
   * @param coordinate The R-, G-, or B-coordinate of the plane.
   * @param target The coordinates of point B.
   * @param axis The axis the plane is perpendicular with. (0: R, 1:
   * G, 2: B)
   * @return The intersection point of the segment AB with the plane
   * R=coordinate, G=coordinate, or B=coordinate
   */
  static setCoordinate(t, e, i, r) {
    const a = wt.intercept(t[r], e, i[r]);
    return wt.lerpPoint(t, a, i);
  }
  static isBounded(t) {
    return 0 <= t && t <= 100;
  }
  /**
   * Returns the nth possible vertex of the polygonal intersection.
   *
   * @param y The Y value of the plane.
   * @param n The zero-based index of the point. 0 <= n <= 11.
   * @return The nth possible vertex of the polygonal intersection
   * of the y plane and the RGB cube, in linear RGB coordinates, if
   * it exists. If this possible vertex lies outside of the cube,
   * [-1.0, -1.0, -1.0] is returned.
   */
  static nthVertex(t, e) {
    const i = wt.Y_FROM_LINRGB[0], r = wt.Y_FROM_LINRGB[1], a = wt.Y_FROM_LINRGB[2], o = e % 4 <= 1 ? 0 : 100, c = e % 2 === 0 ? 0 : 100;
    if (e < 4) {
      const l = o, u = c, b = (t - l * r - u * a) / i;
      return wt.isBounded(b) ? [b, l, u] : [-1, -1, -1];
    } else if (e < 8) {
      const l = o, u = c, b = (t - u * i - l * a) / r;
      return wt.isBounded(b) ? [u, b, l] : [-1, -1, -1];
    } else {
      const l = o, u = c, b = (t - l * i - u * r) / a;
      return wt.isBounded(b) ? [l, u, b] : [-1, -1, -1];
    }
  }
  /**
   * Finds the segment containing the desired color.
   *
   * @param y The Y value of the color.
   * @param targetHue The hue of the color.
   * @return A list of two sets of linear RGB coordinates, each
   * corresponding to an endpoint of the segment containing the
   * desired color.
   */
  static bisectToSegment(t, e) {
    let i = [-1, -1, -1], r = i, a = 0, o = 0, c = !1, l = !0;
    for (let u = 0; u < 12; u++) {
      const b = wt.nthVertex(t, u);
      if (b[0] < 0)
        continue;
      const C = wt.hueOf(b);
      if (!c) {
        i = b, r = b, a = C, o = C, c = !0;
        continue;
      }
      (l || wt.areInCyclicOrder(a, C, o)) && (l = !1, wt.areInCyclicOrder(a, e, C) ? (r = b, o = C) : (i = b, a = C));
    }
    return [i, r];
  }
  static midpoint(t, e) {
    return [
      (t[0] + e[0]) / 2,
      (t[1] + e[1]) / 2,
      (t[2] + e[2]) / 2
    ];
  }
  static criticalPlaneBelow(t) {
    return Math.floor(t - 0.5);
  }
  static criticalPlaneAbove(t) {
    return Math.ceil(t - 0.5);
  }
  /**
   * Finds a color with the given Y and hue on the boundary of the
   * cube.
   *
   * @param y The Y value of the color.
   * @param targetHue The hue of the color.
   * @return The desired color, in linear RGB coordinates.
   */
  static bisectToLimit(t, e) {
    const i = wt.bisectToSegment(t, e);
    let r = i[0], a = wt.hueOf(r), o = i[1];
    for (let c = 0; c < 3; c++)
      if (r[c] !== o[c]) {
        let l = -1, u = 255;
        r[c] < o[c] ? (l = wt.criticalPlaneBelow(wt.trueDelinearized(r[c])), u = wt.criticalPlaneAbove(wt.trueDelinearized(o[c]))) : (l = wt.criticalPlaneAbove(wt.trueDelinearized(r[c])), u = wt.criticalPlaneBelow(wt.trueDelinearized(o[c])));
        for (let b = 0; b < 8 && !(Math.abs(u - l) <= 1); b++) {
          const C = Math.floor((l + u) / 2), v = wt.CRITICAL_PLANES[C], f = wt.setCoordinate(r, v, o, c), m = wt.hueOf(f);
          wt.areInCyclicOrder(a, e, m) ? (o = f, u = C) : (r = f, a = m, l = C);
        }
      }
    return wt.midpoint(r, o);
  }
  static inverseChromaticAdaptation(t) {
    const e = Math.abs(t), i = Math.max(0, 27.13 * e / (400 - e));
    return Fe(t) * Math.pow(i, 1 / 0.42);
  }
  /**
   * Finds a color with the given hue, chroma, and Y.
   *
   * @param hueRadians The desired hue in radians.
   * @param chroma The desired chroma.
   * @param y The desired Y.
   * @return The desired color as a hexadecimal integer, if found; 0
   * otherwise.
   */
  static findResultByJ(t, e, i) {
    let r = Math.sqrt(i) * 11;
    const a = Ai.DEFAULT, o = 1 / Math.pow(1.64 - Math.pow(0.29, a.n), 0.73), l = 0.25 * (Math.cos(t + 2) + 3.8) * (5e4 / 13) * a.nc * a.ncb, u = Math.sin(t), b = Math.cos(t);
    for (let C = 0; C < 5; C++) {
      const v = r / 100, f = e === 0 || r === 0 ? 0 : e / Math.sqrt(v), m = Math.pow(f * o, 1 / 0.9), E = a.aw * Math.pow(v, 1 / a.c / a.z) / a.nbb, M = 23 * (E + 0.305) * m / (23 * l + 11 * m * b + 108 * m * u), A = M * b, H = M * u, D = (460 * E + 451 * A + 288 * H) / 1403, I = (460 * E - 891 * A - 261 * H) / 1403, B = (460 * E - 220 * A - 6300 * H) / 1403, Q = wt.inverseChromaticAdaptation(D), it = wt.inverseChromaticAdaptation(I), ct = wt.inverseChromaticAdaptation(B), st = Cy([Q, it, ct], wt.LINRGB_FROM_SCALED_DISCOUNT);
      if (st[0] < 0 || st[1] < 0 || st[2] < 0)
        return 0;
      const Ot = wt.Y_FROM_LINRGB[0], zt = wt.Y_FROM_LINRGB[1], Mt = wt.Y_FROM_LINRGB[2], vt = Ot * st[0] + zt * st[1] + Mt * st[2];
      if (vt <= 0)
        return 0;
      if (C === 4 || Math.abs(vt - i) < 2e-3)
        return st[0] > 100.01 || st[1] > 100.01 || st[2] > 100.01 ? 0 : fm(st);
      r = r - (vt - i) * r / (2 * vt);
    }
    return 0;
  }
  /**
   * Finds an sRGB color with the given hue, chroma, and L*, if
   * possible.
   *
   * @param hueDegrees The desired hue, in degrees.
   * @param chroma The desired chroma.
   * @param lstar The desired L*.
   * @return A hexadecimal representing the sRGB color. The color
   * has sufficiently close hue, chroma, and L* to the desired
   * values, if possible; otherwise, the hue and L* will be
   * sufficiently close, and chroma will be maximized.
   */
  static solveToInt(t, e, i) {
    if (e < 1e-4 || i < 1e-4 || i > 99.9999)
      return Pb(i);
    t = Ve(t);
    const r = t / 180 * Math.PI, a = Kr(i), o = wt.findResultByJ(r, e, a);
    if (o !== 0)
      return o;
    const c = wt.bisectToLimit(a, r);
    return fm(c);
  }
  /**
   * Finds an sRGB color with the given hue, chroma, and L*, if
   * possible.
   *
   * @param hueDegrees The desired hue, in degrees.
   * @param chroma The desired chroma.
   * @param lstar The desired L*.
   * @return An CAM16 object representing the sRGB color. The color
   * has sufficiently close hue, chroma, and L* to the desired
   * values, if possible; otherwise, the hue and L* will be
   * sufficiently close, and chroma will be maximized.
   */
  static solveToCam(t, e, i) {
    return vs.fromInt(wt.solveToInt(t, e, i));
  }
};
n(wt, "HctSolver");
let Ri = wt;
Ri.SCALED_DISCOUNT_FROM_LINRGB = [
  [
    0.001200833568784504,
    0.002389694492170889,
    2795742885861124e-19
  ],
  [
    5891086651375999e-19,
    0.0029785502573438758,
    3270666104008398e-19
  ],
  [
    10146692491640572e-20,
    5364214359186694e-19,
    0.0032979401770712076
  ]
];
Ri.LINRGB_FROM_SCALED_DISCOUNT = [
  [
    1373.2198709594231,
    -1100.4251190754821,
    -7.278681089101213
  ],
  [
    -271.815969077903,
    559.6580465940733,
    -32.46047482791194
  ],
  [
    1.9622899599665666,
    -57.173814538844006,
    308.7233197812385
  ]
];
Ri.Y_FROM_LINRGB = [0.2126, 0.7152, 0.0722];
Ri.CRITICAL_PLANES = [
  0.015176349177441876,
  0.045529047532325624,
  0.07588174588720938,
  0.10623444424209313,
  0.13658714259697685,
  0.16693984095186062,
  0.19729253930674434,
  0.2276452376616281,
  0.2579979360165119,
  0.28835063437139563,
  0.3188300904430532,
  0.350925934958123,
  0.3848314933096426,
  0.42057480301049466,
  0.458183274052838,
  0.4976837250274023,
  0.5391024159806381,
  0.5824650784040898,
  0.6277969426914107,
  0.6751227633498623,
  0.7244668422128921,
  0.775853049866786,
  0.829304845476233,
  0.8848452951698498,
  0.942497089126609,
  1.0022825574869039,
  1.0642236851973577,
  1.1283421258858297,
  1.1946592148522128,
  1.2631959812511864,
  1.3339731595349034,
  1.407011200216447,
  1.4823302800086415,
  1.5599503113873272,
  1.6398909516233677,
  1.7221716113234105,
  1.8068114625156377,
  1.8938294463134073,
  1.9832442801866852,
  2.075074464868551,
  2.1693382909216234,
  2.2660538449872063,
  2.36523901573795,
  2.4669114995532007,
  2.5710888059345764,
  2.6777882626779785,
  2.7870270208169257,
  2.898822059350997,
  3.0131901897720907,
  3.1301480604002863,
  3.2497121605402226,
  3.3718988244681087,
  3.4967242352587946,
  3.624204428461639,
  3.754355295633311,
  3.887192587735158,
  4.022731918402185,
  4.160988767090289,
  4.301978482107941,
  4.445716283538092,
  4.592217266055746,
  4.741496401646282,
  4.893568542229298,
  5.048448422192488,
  5.20615066083972,
  5.3666897647573375,
  5.5300801301023865,
  5.696336044816294,
  5.865471690767354,
  6.037501145825082,
  6.212438385869475,
  6.390297286737924,
  6.571091626112461,
  6.7548350853498045,
  6.941541251256611,
  7.131223617812143,
  7.323895587840543,
  7.5195704746346665,
  7.7182615035334345,
  7.919981813454504,
  8.124744458384042,
  8.332562408825165,
  8.543448553206703,
  8.757415699253682,
  8.974476575321063,
  9.194643831691977,
  9.417930041841839,
  9.644347703669503,
  9.873909240696694,
  10.106627003236781,
  10.342513269534024,
  10.58158024687427,
  10.8238400726681,
  11.069304815507364,
  11.317986476196008,
  11.569896988756009,
  11.825048221409341,
  12.083451977536606,
  12.345119996613247,
  12.610063955123938,
  12.878295467455942,
  13.149826086772048,
  13.42466730586372,
  13.702830557985108,
  13.984327217668513,
  14.269168601521828,
  14.55736596900856,
  14.848930523210871,
  15.143873411576273,
  15.44220572664832,
  15.743938506781891,
  16.04908273684337,
  16.35764934889634,
  16.66964922287304,
  16.985093187232053,
  17.30399201960269,
  17.62635644741625,
  17.95219714852476,
  18.281524751807332,
  18.614349837764564,
  18.95068293910138,
  19.290534541298456,
  19.633915083172692,
  19.98083495742689,
  20.331304511189067,
  20.685334046541502,
  21.042933821039977,
  21.404114048223256,
  21.76888489811322,
  22.137256497705877,
  22.50923893145328,
  22.884842241736916,
  23.264076429332462,
  23.6469514538663,
  24.033477234264016,
  24.42366364919083,
  24.817520537484558,
  25.21505769858089,
  25.61628489293138,
  26.021211842414342,
  26.429848230738664,
  26.842203703840827,
  27.258287870275353,
  27.678110301598522,
  28.10168053274597,
  28.529008062403893,
  28.96010235337422,
  29.39497283293396,
  29.83362889318845,
  30.276079891419332,
  30.722335150426627,
  31.172403958865512,
  31.62629557157785,
  32.08401920991837,
  32.54558406207592,
  33.010999283389665,
  33.4802739966603,
  33.953417292456834,
  34.430438229418264,
  34.911345834551085,
  35.39614910352207,
  35.88485700094671,
  36.37747846067349,
  36.87402238606382,
  37.37449765026789,
  37.87891309649659,
  38.38727753828926,
  38.89959975977785,
  39.41588851594697,
  39.93615253289054,
  40.460400508064545,
  40.98864111053629,
  41.520882981230194,
  42.05713473317016,
  42.597404951718396,
  43.141702194811224,
  43.6900349931913,
  44.24241185063697,
  44.798841244188324,
  45.35933162437017,
  45.92389141541209,
  46.49252901546552,
  47.065252796817916,
  47.64207110610409,
  48.22299226451468,
  48.808024568002054,
  49.3971762874833,
  49.9904556690408,
  50.587870934119984,
  51.189430279724725,
  51.79514187861014,
  52.40501387947288,
  53.0190544071392,
  53.637271562750364,
  54.259673423945976,
  54.88626804504493,
  55.517063457223934,
  56.15206766869424,
  56.79128866487574,
  57.43473440856916,
  58.08241284012621,
  58.734331877617365,
  59.39049941699807,
  60.05092333227251,
  60.715611475655585,
  61.38457167773311,
  62.057811747619894,
  62.7353394731159,
  63.417162620860914,
  64.10328893648692,
  64.79372614476921,
  65.48848194977529,
  66.18756403501224,
  66.89098006357258,
  67.59873767827808,
  68.31084450182222,
  69.02730813691093,
  69.74813616640164,
  70.47333615344107,
  71.20291564160104,
  71.93688215501312,
  72.67524319850172,
  73.41800625771542,
  74.16517879925733,
  74.9167682708136,
  75.67278210128072,
  76.43322770089146,
  77.1981124613393,
  77.96744375590167,
  78.74122893956174,
  79.51947534912904,
  80.30219030335869,
  81.08938110306934,
  81.88105503125999,
  82.67721935322541,
  83.4778813166706,
  84.28304815182372,
  85.09272707154808,
  85.90692527145302,
  86.72564993000343,
  87.54890820862819,
  88.3767072518277,
  89.2090541872801,
  90.04595612594655,
  90.88742016217518,
  91.73345337380438,
  92.58406282226491,
  93.43925555268066,
  94.29903859396902,
  95.16341895893969,
  96.03240364439274,
  96.9059996312159,
  97.78421388448044,
  98.6670533535366,
  99.55452497210776
];
const Fn = class Fn {
  static from(t, e, i) {
    return new Fn(Ri.solveToInt(t, e, i));
  }
  /**
   * @param argb ARGB representation of a color.
   * @return HCT representation of a color in default viewing conditions
   */
  static fromInt(t) {
    return new Fn(t);
  }
  toInt() {
    return this.argb;
  }
  /**
   * A number, in degrees, representing ex. red, orange, yellow, etc.
   * Ranges from 0 <= hue < 360.
   */
  get hue() {
    return this.internalHue;
  }
  /**
   * @param newHue 0 <= newHue < 360; invalid values are corrected.
   * Chroma may decrease because chroma has a different maximum for any given
   * hue and tone.
   */
  set hue(t) {
    this.setInternalState(Ri.solveToInt(t, this.internalChroma, this.internalTone));
  }
  get chroma() {
    return this.internalChroma;
  }
  /**
   * @param newChroma 0 <= newChroma < ?
   * Chroma may decrease because chroma has a different maximum for any given
   * hue and tone.
   */
  set chroma(t) {
    this.setInternalState(Ri.solveToInt(this.internalHue, t, this.internalTone));
  }
  /** Lightness. Ranges from 0 to 100. */
  get tone() {
    return this.internalTone;
  }
  /**
   * @param newTone 0 <= newTone <= 100; invalid valids are corrected.
   * Chroma may decrease because chroma has a different maximum for any given
   * hue and tone.
   */
  set tone(t) {
    this.setInternalState(Ri.solveToInt(this.internalHue, this.internalChroma, t));
  }
  /** Sets a property of the Hct object. */
  setValue(t, e) {
    this[t] = e;
  }
  toString() {
    return `HCT(${this.hue.toFixed(0)}, ${this.chroma.toFixed(0)}, ${this.tone.toFixed(0)})`;
  }
  static isBlue(t) {
    return t >= 250 && t < 270;
  }
  static isYellow(t) {
    return t >= 105 && t < 125;
  }
  static isCyan(t) {
    return t >= 170 && t < 207;
  }
  constructor(t) {
    this.argb = t;
    const e = vs.fromInt(t);
    this.internalHue = e.hue, this.internalChroma = e.chroma, this.internalTone = mm(t), this.argb = t;
  }
  setInternalState(t) {
    const e = vs.fromInt(t);
    this.internalHue = e.hue, this.internalChroma = e.chroma, this.internalTone = mm(t), this.argb = t;
  }
  /**
   * Translates a color into different [ViewingConditions].
   *
   * Colors change appearance. They look different with lights on versus off,
   * the same color, as in hex code, on white looks different when on black.
   * This is called color relativity, most famously explicated by Josef Albers
   * in Interaction of Color.
   *
   * In color science, color appearance models can account for this and
   * calculate the appearance of a color in different settings. HCT is based on
   * CAM16, a color appearance model, and uses it to make these calculations.
   *
   * See [ViewingConditions.make] for parameters affecting color appearance.
   */
  inViewingConditions(t) {
    const i = vs.fromInt(this.toInt()).xyzInViewingConditions(t), r = vs.fromXyzInViewingConditions(i[0], i[1], i[2], Ai.make());
    return Fn.from(r.hue, r.chroma, $y(i[1]));
  }
};
n(Fn, "Hct");
let yt = Fn;
const Tr = class Tr {
  /**
   * Returns a contrast ratio, which ranges from 1 to 21.
   *
   * @param toneA Tone between 0 and 100. Values outside will be clamped.
   * @param toneB Tone between 0 and 100. Values outside will be clamped.
   */
  static ratioOfTones(t, e) {
    return t = xe(0, 100, t), e = xe(0, 100, e), Tr.ratioOfYs(Kr(t), Kr(e));
  }
  static ratioOfYs(t, e) {
    const i = t > e ? t : e, r = i === e ? t : e;
    return (i + 5) / (r + 5);
  }
  /**
   * Returns a tone >= tone parameter that ensures ratio parameter.
   * Return value is between 0 and 100.
   * Returns -1 if ratio cannot be achieved with tone parameter.
   *
   * @param tone Tone return value must contrast with.
   * Range is 0 to 100. Invalid values will result in -1 being returned.
   * @param ratio Contrast ratio of return value and tone.
   * Range is 1 to 21, invalid values have undefined behavior.
   */
  static lighter(t, e) {
    if (t < 0 || t > 100)
      return -1;
    const i = Kr(t), r = e * (i + 5) - 5, a = Tr.ratioOfYs(r, i), o = Math.abs(a - e);
    if (a < e && o > 0.04)
      return -1;
    const c = $y(r) + 0.4;
    return c < 0 || c > 100 ? -1 : c;
  }
  /**
   * Returns a tone <= tone parameter that ensures ratio parameter.
   * Return value is between 0 and 100.
   * Returns -1 if ratio cannot be achieved with tone parameter.
   *
   * @param tone Tone return value must contrast with.
   * Range is 0 to 100. Invalid values will result in -1 being returned.
   * @param ratio Contrast ratio of return value and tone.
   * Range is 1 to 21, invalid values have undefined behavior.
   */
  static darker(t, e) {
    if (t < 0 || t > 100)
      return -1;
    const i = Kr(t), r = (i + 5) / e - 5, a = Tr.ratioOfYs(i, r), o = Math.abs(a - e);
    if (a < e && o > 0.04)
      return -1;
    const c = $y(r) - 0.4;
    return c < 0 || c > 100 ? -1 : c;
  }
  /**
   * Returns a tone >= tone parameter that ensures ratio parameter.
   * Return value is between 0 and 100.
   * Returns 100 if ratio cannot be achieved with tone parameter.
   *
   * This method is unsafe because the returned value is guaranteed to be in
   * bounds for tone, i.e. between 0 and 100. However, that value may not reach
   * the ratio with tone. For example, there is no color lighter than T100.
   *
   * @param tone Tone return value must contrast with.
   * Range is 0 to 100. Invalid values will result in 100 being returned.
   * @param ratio Desired contrast ratio of return value and tone parameter.
   * Range is 1 to 21, invalid values have undefined behavior.
   */
  static lighterUnsafe(t, e) {
    const i = Tr.lighter(t, e);
    return i < 0 ? 100 : i;
  }
  /**
   * Returns a tone >= tone parameter that ensures ratio parameter.
   * Return value is between 0 and 100.
   * Returns 100 if ratio cannot be achieved with tone parameter.
   *
   * This method is unsafe because the returned value is guaranteed to be in
   * bounds for tone, i.e. between 0 and 100. However, that value may not reach
   * the [ratio with [tone]. For example, there is no color darker than T0.
   *
   * @param tone Tone return value must contrast with.
   * Range is 0 to 100. Invalid values will result in 0 being returned.
   * @param ratio Desired contrast ratio of return value and tone parameter.
   * Range is 1 to 21, invalid values have undefined behavior.
   */
  static darkerUnsafe(t, e) {
    const i = Tr.darker(t, e);
    return i < 0 ? 0 : i;
  }
};
n(Tr, "Contrast");
let se = Tr;
const wu = class wu {
  /**
   * Returns true if a color is disliked.
   *
   * @param hct A color to be judged.
   * @return Whether the color is disliked.
   *
   * Disliked is defined as a dark yellow-green that is not neutral.
   */
  static isDisliked(t) {
    const e = Math.round(t.hue) >= 90 && Math.round(t.hue) <= 111, i = Math.round(t.chroma) > 16, r = Math.round(t.tone) < 65;
    return e && i && r;
  }
  /**
   * If a color is disliked, lighten it to make it likable.
   *
   * @param hct A color to be judged.
   * @return A new color if the original color is disliked, or the original
   *   color if it is acceptable.
   */
  static fixIfDisliked(t) {
    return wu.isDisliked(t) ? yt.from(t.hue, t.chroma, 70) : t;
  }
};
n(wu, "DislikeAnalyzer");
let ql = wu;
function Eb(s, t, e) {
  if (s.name !== e.name)
    throw new Error(`Attempting to extend color ${s.name} with color ${e.name} of different name for spec version ${t}.`);
  if (s.isBackground !== e.isBackground)
    throw new Error(`Attempting to extend color ${s.name} as a ${s.isBackground ? "background" : "foreground"} with color ${e.name} as a ${e.isBackground ? "background" : "foreground"} for spec version ${t}.`);
}
n(Eb, "validateExtendedColor");
function pt(s, t, e) {
  return Eb(s, t, e), O.fromPalette({
    name: s.name,
    palette: /* @__PURE__ */ n((i) => i.specVersion === t ? e.palette(i) : s.palette(i), "palette"),
    tone: /* @__PURE__ */ n((i) => i.specVersion === t ? e.tone(i) : s.tone(i), "tone"),
    isBackground: s.isBackground,
    chromaMultiplier: /* @__PURE__ */ n((i) => {
      const r = i.specVersion === t ? e.chromaMultiplier : s.chromaMultiplier;
      return r !== void 0 ? r(i) : 1;
    }, "chromaMultiplier"),
    background: /* @__PURE__ */ n((i) => {
      const r = i.specVersion === t ? e.background : s.background;
      return r !== void 0 ? r(i) : void 0;
    }, "background"),
    secondBackground: /* @__PURE__ */ n((i) => {
      const r = i.specVersion === t ? e.secondBackground : s.secondBackground;
      return r !== void 0 ? r(i) : void 0;
    }, "secondBackground"),
    contrastCurve: /* @__PURE__ */ n((i) => {
      const r = i.specVersion === t ? e.contrastCurve : s.contrastCurve;
      return r !== void 0 ? r(i) : void 0;
    }, "contrastCurve"),
    toneDeltaPair: /* @__PURE__ */ n((i) => {
      const r = i.specVersion === t ? e.toneDeltaPair : s.toneDeltaPair;
      return r !== void 0 ? r(i) : void 0;
    }, "toneDeltaPair")
  });
}
n(pt, "extendSpecVersion");
const Ki = class Ki {
  /**
   * Create a DynamicColor defined by a TonalPalette and HCT tone.
   *
   * @param args Functions with DynamicScheme as input. Must provide a palette
   *     and tone. May provide a background DynamicColor and ToneDeltaPair.
   */
  static fromPalette(t) {
    return new Ki(t.name ?? "", t.palette, t.tone ?? Ki.getInitialToneFromBackground(t.background), t.isBackground ?? !1, t.chromaMultiplier, t.background, t.secondBackground, t.contrastCurve, t.toneDeltaPair);
  }
  static getInitialToneFromBackground(t) {
    return t === void 0 ? (e) => 50 : (e) => t(e) ? t(e).getTone(e) : 50;
  }
  /**
   * The base constructor for DynamicColor.
   *
   * _Strongly_ prefer using one of the convenience constructors. This class is
   * arguably too flexible to ensure it can support any scenario. Functional
   * arguments allow  overriding without risks that come with subclasses.
   *
   * For example, the default behavior of adjust tone at max contrast
   * to be at a 7.0 ratio with its background is principled and
   * matches accessibility guidance. That does not mean it's the desired
   * approach for _every_ design system, and every color pairing,
   * always, in every case.
   *
   * @param name The name of the dynamic color. Defaults to empty.
   * @param palette Function that provides a TonalPalette given DynamicScheme. A
   *     TonalPalette is defined by a hue and chroma, so this replaces the need
   *     to specify hue/chroma. By providing a tonal palette, when contrast
   *     adjustments are made, intended chroma can be preserved.
   * @param tone Function that provides a tone, given a DynamicScheme.
   * @param isBackground Whether this dynamic color is a background, with some
   *     other color as the foreground. Defaults to false.
   * @param chromaMultiplier A factor that multiplies the chroma for this color.
   * @param background The background of the dynamic color (as a function of a
   *     `DynamicScheme`), if it exists.
   * @param secondBackground A second background of the dynamic color (as a
   *     function of a `DynamicScheme`), if it exists.
   * @param contrastCurve A `ContrastCurve` object specifying how its contrast
   *     against its background should behave in various contrast levels
   *     options.
   * @param toneDeltaPair A `ToneDeltaPair` object specifying a tone delta
   *     constraint between two colors. One of them must be the color being
   *     constructed.
   */
  constructor(t, e, i, r, a, o, c, l, u) {
    if (this.name = t, this.palette = e, this.tone = i, this.isBackground = r, this.chromaMultiplier = a, this.background = o, this.secondBackground = c, this.contrastCurve = l, this.toneDeltaPair = u, this.hctCache = /* @__PURE__ */ new Map(), !o && c)
      throw new Error(`Color ${t} has secondBackgrounddefined, but background is not defined.`);
    if (!o && l)
      throw new Error(`Color ${t} has contrastCurvedefined, but background is not defined.`);
    if (o && !l)
      throw new Error(`Color ${t} has backgrounddefined, but contrastCurve is not defined.`);
  }
  /**
   * Returns a deep copy of this DynamicColor.
   */
  clone() {
    return Ki.fromPalette({
      name: this.name,
      palette: this.palette,
      tone: this.tone,
      isBackground: this.isBackground,
      chromaMultiplier: this.chromaMultiplier,
      background: this.background,
      secondBackground: this.secondBackground,
      contrastCurve: this.contrastCurve,
      toneDeltaPair: this.toneDeltaPair
    });
  }
  /**
   * Clears the cache of HCT values for this color. For testing or debugging
   * purposes.
   */
  clearCache() {
    this.hctCache.clear();
  }
  /**
   * Returns a ARGB integer (i.e. a hex code).
   *
   * @param scheme Defines the conditions of the user interface, for example,
   *     whether or not it is dark mode or light mode, and what the desired
   *     contrast level is.
   */
  getArgb(t) {
    return this.getHct(t).toInt();
  }
  /**
   * Returns a color, expressed in the HCT color space, that this
   * DynamicColor is under the conditions in scheme.
   *
   * @param scheme Defines the conditions of the user interface, for example,
   *     whether or not it is dark mode or light mode, and what the desired
   *     contrast level is.
   */
  getHct(t) {
    const e = this.hctCache.get(t);
    if (e != null)
      return e;
    const i = vm(t.specVersion).getHct(t, this);
    return this.hctCache.size > 4 && this.hctCache.clear(), this.hctCache.set(t, i), i;
  }
  /**
   * Returns a tone, T in the HCT color space, that this DynamicColor is under
   * the conditions in scheme.
   *
   * @param scheme Defines the conditions of the user interface, for example,
   *     whether or not it is dark mode or light mode, and what the desired
   *     contrast level is.
   */
  getTone(t) {
    return vm(t.specVersion).getTone(t, this);
  }
  /**
   * Given a background tone, finds a foreground tone, while ensuring they reach
   * a contrast ratio that is as close to [ratio] as possible.
   *
   * @param bgTone Tone in HCT. Range is 0 to 100, undefined behavior when it
   *     falls outside that range.
   * @param ratio The contrast ratio desired between bgTone and the return
   *     value.
   */
  static foregroundTone(t, e) {
    const i = se.lighterUnsafe(t, e), r = se.darkerUnsafe(t, e), a = se.ratioOfTones(i, t), o = se.ratioOfTones(r, t);
    if (Ki.tonePrefersLightForeground(t)) {
      const l = Math.abs(a - o) < 0.1 && a < e && o < e;
      return a >= e || a >= o || l ? i : r;
    } else
      return o >= e || o >= a ? r : i;
  }
  /**
   * Returns whether [tone] prefers a light foreground.
   *
   * People prefer white foregrounds on ~T60-70. Observed over time, and also
   * by Andrew Somers during research for APCA.
   *
   * T60 used as to create the smallest discontinuity possible when skipping
   * down to T49 in order to ensure light foregrounds.
   * Since `tertiaryContainer` in dark monochrome scheme requires a tone of
   * 60, it should not be adjusted. Therefore, 60 is excluded here.
   */
  static tonePrefersLightForeground(t) {
    return Math.round(t) < 60;
  }
  /**
   * Returns whether [tone] can reach a contrast ratio of 4.5 with a lighter
   * color.
   */
  static toneAllowsLightForeground(t) {
    return Math.round(t) <= 49;
  }
  /**
   * Adjusts a tone such that white has 4.5 contrast, if the tone is
   * reasonably close to supporting it.
   */
  static enableLightForeground(t) {
    return Ki.tonePrefersLightForeground(t) && !Ki.toneAllowsLightForeground(t) ? 49 : t;
  }
};
n(Ki, "DynamicColor");
let O = Ki;
const sf = class sf {
  getHct(t, e) {
    const i = e.getTone(t);
    return e.palette(t).getHct(i);
  }
  getTone(t, e) {
    const i = t.contrastLevel < 0, r = e.toneDeltaPair ? e.toneDeltaPair(t) : void 0;
    if (r) {
      const a = r.roleA, o = r.roleB, c = r.delta, l = r.polarity, u = r.stayTogether, b = l === "nearer" || l === "lighter" && !t.isDark || l === "darker" && t.isDark, C = b ? a : o, v = b ? o : a, f = e.name === C.name, m = t.isDark ? 1 : -1;
      let $ = C.tone(t), E = v.tone(t);
      if (e.background && C.contrastCurve && v.contrastCurve) {
        const M = e.background(t), A = C.contrastCurve(t), H = v.contrastCurve(t);
        if (M && A && H) {
          const D = M.getTone(t), I = A.get(t.contrastLevel), B = H.get(t.contrastLevel);
          se.ratioOfTones(D, $) < I && ($ = O.foregroundTone(D, I)), se.ratioOfTones(D, E) < B && (E = O.foregroundTone(D, B)), i && ($ = O.foregroundTone(D, I), E = O.foregroundTone(D, B));
        }
      }
      return (E - $) * m < c && (E = xe(0, 100, $ + c * m), (E - $) * m >= c || ($ = xe(0, 100, E - c * m))), 50 <= $ && $ < 60 ? m > 0 ? ($ = 60, E = Math.max(E, $ + c * m)) : ($ = 49, E = Math.min(E, $ + c * m)) : 50 <= E && E < 60 && (u ? m > 0 ? ($ = 60, E = Math.max(E, $ + c * m)) : ($ = 49, E = Math.min(E, $ + c * m)) : m > 0 ? E = 60 : E = 49), f ? $ : E;
    } else {
      let a = e.tone(t);
      if (e.background == null || e.background(t) === void 0 || e.contrastCurve == null || e.contrastCurve(t) === void 0)
        return a;
      const o = e.background(t).getTone(t), c = e.contrastCurve(t).get(t.contrastLevel);
      if (se.ratioOfTones(o, a) >= c || (a = O.foregroundTone(o, c)), i && (a = O.foregroundTone(o, c)), e.isBackground && 50 <= a && a < 60 && (se.ratioOfTones(49, o) >= c ? a = 49 : a = 60), e.secondBackground == null || e.secondBackground(t) === void 0)
        return a;
      const [l, u] = [e.background, e.secondBackground], [b, C] = [l(t).getTone(t), u(t).getTone(t)], [v, f] = [Math.max(b, C), Math.min(b, C)];
      if (se.ratioOfTones(v, a) >= c && se.ratioOfTones(f, a) >= c)
        return a;
      const m = se.lighter(v, c), $ = se.darker(f, c), E = [];
      return m !== -1 && E.push(m), $ !== -1 && E.push($), O.tonePrefersLightForeground(b) || O.tonePrefersLightForeground(C) ? m < 0 ? 100 : m : E.length === 1 ? E[0] : $ < 0 ? 0 : $;
    }
  }
};
n(sf, "ColorCalculationDelegateImpl2021");
let ky = sf;
const nf = class nf {
  getHct(t, e) {
    const i = e.palette(t), r = e.getTone(t), a = i.hue, o = i.chroma * (e.chromaMultiplier ? e.chromaMultiplier(t) : 1);
    return yt.from(a, o, r);
  }
  getTone(t, e) {
    const i = e.toneDeltaPair ? e.toneDeltaPair(t) : void 0;
    if (i) {
      const r = i.roleA, a = i.roleB, o = i.polarity, c = i.constraint, l = o === "darker" || o === "relative_lighter" && t.isDark || o === "relative_darker" && !t.isDark ? -i.delta : i.delta, u = e.name === r.name, b = u ? r : a, C = u ? a : r;
      let v = b.tone(t), f = C.getTone(t);
      const m = l * (u ? 1 : -1);
      if (c === "exact" ? v = xe(0, 100, f + m) : c === "nearer" ? m > 0 ? v = xe(0, 100, xe(f, f + m, v)) : v = xe(0, 100, xe(f + m, f, v)) : c === "farther" && (m > 0 ? v = xe(f + m, 100, v) : v = xe(0, f + m, v)), e.background && e.contrastCurve) {
        const $ = e.background(t), E = e.contrastCurve(t);
        if ($ && E) {
          const M = $.getTone(t), A = E.get(t.contrastLevel);
          v = se.ratioOfTones(M, v) >= A && t.contrastLevel >= 0 ? v : O.foregroundTone(M, A);
        }
      }
      return e.isBackground && !e.name.endsWith("_fixed_dim") && (v >= 57 ? v = xe(65, 100, v) : v = xe(0, 49, v)), v;
    } else {
      let r = e.tone(t);
      if (e.background == null || e.background(t) === void 0 || e.contrastCurve == null || e.contrastCurve(t) === void 0)
        return r;
      const a = e.background(t).getTone(t), o = e.contrastCurve(t).get(t.contrastLevel);
      if (r = se.ratioOfTones(a, r) >= o && t.contrastLevel >= 0 ? r : O.foregroundTone(a, o), e.isBackground && !e.name.endsWith("_fixed_dim") && (r >= 57 ? r = xe(65, 100, r) : r = xe(0, 49, r)), e.secondBackground == null || e.secondBackground(t) === void 0)
        return r;
      const [c, l] = [e.background, e.secondBackground], [u, b] = [c(t).getTone(t), l(t).getTone(t)], [C, v] = [Math.max(u, b), Math.min(u, b)];
      if (se.ratioOfTones(C, r) >= o && se.ratioOfTones(v, r) >= o)
        return r;
      const f = se.lighter(C, o), m = se.darker(v, o), $ = [];
      return f !== -1 && $.push(f), m !== -1 && $.push(m), O.tonePrefersLightForeground(u) || O.tonePrefersLightForeground(b) ? f < 0 ? 100 : f : $.length === 1 ? $[0] : m < 0 ? 0 : m;
    }
  }
};
n(nf, "ColorCalculationDelegateImpl2025");
let _y = nf;
const Ab = new ky(), Rb = new _y();
function vm(s) {
  return s === "2025" ? Rb : Ab;
}
n(vm, "getSpec$1");
const In = class In {
  /**
   * @param argb ARGB representation of a color
   * @return Tones matching that color's hue and chroma.
   */
  static fromInt(t) {
    const e = yt.fromInt(t);
    return In.fromHct(e);
  }
  /**
   * @param hct Hct
   * @return Tones matching that color's hue and chroma.
   */
  static fromHct(t) {
    return new In(t.hue, t.chroma, t);
  }
  /**
   * @param hue HCT hue
   * @param chroma HCT chroma
   * @return Tones matching hue and chroma.
   */
  static fromHueAndChroma(t, e) {
    const i = new Py(t, e).create();
    return new In(t, e, i);
  }
  constructor(t, e, i) {
    this.hue = t, this.chroma = e, this.keyColor = i, this.cache = /* @__PURE__ */ new Map();
  }
  /**
   * @param tone HCT tone, measured from 0 to 100.
   * @return ARGB representation of a color with that tone.
   */
  tone(t) {
    let e = this.cache.get(t);
    return e === void 0 && (t == 99 && yt.isYellow(this.hue) ? e = this.averageArgb(this.tone(98), this.tone(100)) : e = yt.from(this.hue, this.chroma, t).toInt(), this.cache.set(t, e)), e;
  }
  /**
   * @param tone HCT tone.
   * @return HCT representation of a color with that tone.
   */
  getHct(t) {
    return yt.fromInt(this.tone(t));
  }
  averageArgb(t, e) {
    const i = t >>> 16 & 255, r = t >>> 8 & 255, a = t & 255, o = e >>> 16 & 255, c = e >>> 8 & 255, l = e & 255, u = Math.round((i + o) / 2), b = Math.round((r + c) / 2), C = Math.round((a + l) / 2);
    return (255 << 24 | (u & 255) << 16 | (b & 255) << 8 | C & 255) >>> 0;
  }
};
n(In, "TonalPalette");
let q = In;
const af = class af {
  constructor(t, e) {
    this.hue = t, this.requestedChroma = e, this.chromaCache = /* @__PURE__ */ new Map(), this.maxChromaValue = 200;
  }
  /**
   * Creates a key color from a [hue] and a [chroma].
   * The key color is the first tone, starting from T50, matching the given hue
   * and chroma.
   *
   * @return Key color [Hct]
   */
  create() {
    let r = 0, a = 100;
    for (; r < a; ) {
      const o = Math.floor((r + a) / 2), c = this.maxChroma(o) < this.maxChroma(o + 1);
      if (this.maxChroma(o) >= this.requestedChroma - 0.01)
        if (Math.abs(r - 50) < Math.abs(a - 50))
          a = o;
        else {
          if (r === o)
            return yt.from(this.hue, this.requestedChroma, r);
          r = o;
        }
      else
        c ? r = o + 1 : a = o;
    }
    return yt.from(this.hue, this.requestedChroma, r);
  }
  // Find the maximum chroma for a given tone
  maxChroma(t) {
    if (this.chromaCache.has(t))
      return this.chromaCache.get(t);
    const e = yt.from(this.hue, this.maxChromaValue, t).chroma;
    return this.chromaCache.set(t, e), e;
  }
};
n(af, "KeyColor");
let Py = af;
const Dn = class Dn {
  constructor(t) {
    this.input = t, this.hctsByTempCache = [], this.hctsByHueCache = [], this.tempsByHctCache = /* @__PURE__ */ new Map(), this.inputRelativeTemperatureCache = -1, this.complementCache = null;
  }
  get hctsByTemp() {
    if (this.hctsByTempCache.length > 0)
      return this.hctsByTempCache;
    const t = this.hctsByHue.concat([this.input]), e = this.tempsByHct;
    return t.sort((i, r) => e.get(i) - e.get(r)), this.hctsByTempCache = t, t;
  }
  get warmest() {
    return this.hctsByTemp[this.hctsByTemp.length - 1];
  }
  get coldest() {
    return this.hctsByTemp[0];
  }
  /**
   * A set of colors with differing hues, equidistant in temperature.
   *
   * In art, this is usually described as a set of 5 colors on a color wheel
   * divided into 12 sections. This method allows provision of either of those
   * values.
   *
   * Behavior is undefined when [count] or [divisions] is 0.
   * When divisions < count, colors repeat.
   *
   * [count] The number of colors to return, includes the input color.
   * [divisions] The number of divisions on the color wheel.
   */
  analogous(t = 5, e = 12) {
    const i = Math.round(this.input.hue), r = this.hctsByHue[i];
    let a = this.relativeTemperature(r);
    const o = [r];
    let c = 0;
    for (let m = 0; m < 360; m++) {
      const $ = wm(i + m), E = this.hctsByHue[$], M = this.relativeTemperature(E), A = Math.abs(M - a);
      a = M, c += A;
    }
    let l = 1;
    const u = c / e;
    let b = 0;
    for (a = this.relativeTemperature(r); o.length < e; ) {
      const m = wm(i + l), $ = this.hctsByHue[m], E = this.relativeTemperature($), M = Math.abs(E - a);
      b += M;
      const A = o.length * u;
      let H = b >= A, D = 1;
      for (; H && o.length < e; ) {
        o.push($);
        const I = (o.length + D) * u;
        H = b >= I, D++;
      }
      if (a = E, l++, l > 360) {
        for (; o.length < e; )
          o.push($);
        break;
      }
    }
    const C = [this.input], v = Math.floor((t - 1) / 2);
    for (let m = 1; m < v + 1; m++) {
      let $ = 0 - m;
      for (; $ < 0; )
        $ = o.length + $;
      $ >= o.length && ($ = $ % o.length), C.splice(0, 0, o[$]);
    }
    const f = t - v - 1;
    for (let m = 1; m < f + 1; m++) {
      let $ = m;
      for (; $ < 0; )
        $ = o.length + $;
      $ >= o.length && ($ = $ % o.length), C.push(o[$]);
    }
    return C;
  }
  /**
   * A color that complements the input color aesthetically.
   *
   * In art, this is usually described as being across the color wheel.
   * History of this shows intent as a color that is just as cool-warm as the
   * input color is warm-cool.
   */
  get complement() {
    if (this.complementCache != null)
      return this.complementCache;
    const t = this.coldest.hue, e = this.tempsByHct.get(this.coldest), i = this.warmest.hue, a = this.tempsByHct.get(this.warmest) - e, o = Dn.isBetween(this.input.hue, t, i), c = o ? i : t, l = o ? t : i, u = 1;
    let b = 1e3, C = this.hctsByHue[Math.round(this.input.hue)];
    const v = 1 - this.inputRelativeTemperature;
    for (let f = 0; f <= 360; f += 1) {
      const m = Ve(c + u * f);
      if (!Dn.isBetween(m, c, l))
        continue;
      const $ = this.hctsByHue[Math.round(m)], E = (this.tempsByHct.get($) - e) / a, M = Math.abs(v - E);
      M < b && (b = M, C = $);
    }
    return this.complementCache = C, this.complementCache;
  }
  /**
   * Temperature relative to all colors with the same chroma and tone.
   * Value on a scale from 0 to 1.
   */
  relativeTemperature(t) {
    const e = this.tempsByHct.get(this.warmest) - this.tempsByHct.get(this.coldest), i = this.tempsByHct.get(t) - this.tempsByHct.get(this.coldest);
    return e === 0 ? 0.5 : i / e;
  }
  /** Relative temperature of the input color. See [relativeTemperature]. */
  get inputRelativeTemperature() {
    return this.inputRelativeTemperatureCache >= 0 ? this.inputRelativeTemperatureCache : (this.inputRelativeTemperatureCache = this.relativeTemperature(this.input), this.inputRelativeTemperatureCache);
  }
  /** A Map with keys of HCTs in [hctsByTemp], values of raw temperature. */
  get tempsByHct() {
    if (this.tempsByHctCache.size > 0)
      return this.tempsByHctCache;
    const t = this.hctsByHue.concat([this.input]), e = /* @__PURE__ */ new Map();
    for (const i of t)
      e.set(i, Dn.rawTemperature(i));
    return this.tempsByHctCache = e, e;
  }
  /**
   * HCTs for all hues, with the same chroma/tone as the input.
   * Sorted ascending, hue 0 to 360.
   */
  get hctsByHue() {
    if (this.hctsByHueCache.length > 0)
      return this.hctsByHueCache;
    const t = [];
    for (let e = 0; e <= 360; e += 1) {
      const i = yt.from(e, this.input.chroma, this.input.tone);
      t.push(i);
    }
    return this.hctsByHueCache = t, this.hctsByHueCache;
  }
  /** Determines if an angle is between two other angles, rotating clockwise. */
  static isBetween(t, e, i) {
    return e < i ? e <= t && t <= i : e <= t || t <= i;
  }
  /**
   * Value representing cool-warm factor of a color.
   * Values below 0 are considered cool, above, warm.
   *
   * Color science has researched emotion and harmony, which art uses to select
   * colors. Warm-cool is the foundation of analogous and complementary colors.
   * See:
   * - Li-Chen Ou's Chapter 19 in Handbook of Color Psychology (2015).
   * - Josef Albers' Interaction of Color chapters 19 and 21.
   *
   * Implementation of Ou, Woodcock and Wright's algorithm, which uses
   * L*a*b* / LCH color space.
   * Return value has these properties:
   * - Values below 0 are cool, above 0 are warm.
   * - Lower bound: -0.52 - (chroma ^ 1.07 / 20). L*a*b* chroma is infinite.
   *   Assuming max of 130 chroma, -9.66.
   * - Upper bound: -0.52 + (chroma ^ 1.07 / 20). L*a*b* chroma is infinite.
   *   Assuming max of 130 chroma, 8.61.
   */
  static rawTemperature(t) {
    const e = _b(t.toInt()), i = Ve(Math.atan2(e[2], e[1]) * 180 / Math.PI), r = Math.sqrt(e[1] * e[1] + e[2] * e[2]);
    return -0.5 + 0.02 * Math.pow(r, 1.07) * Math.cos(Ve(i - 50) * Math.PI / 180);
  }
};
n(Dn, "TemperatureCache");
let Qh = Dn;
const of = class of {
  /**
   * Creates a `ContrastCurve` object.
   *
   * @param low Value for contrast level -1.0
   * @param normal Value for contrast level 0.0
   * @param medium Value for contrast level 0.5
   * @param high Value for contrast level 1.0
   */
  constructor(t, e, i, r) {
    this.low = t, this.normal = e, this.medium = i, this.high = r;
  }
  /**
   * Returns the value at a given contrast level.
   *
   * @param contrastLevel The contrast level. 0.0 is the default (normal); -1.0
   *     is the lowest; 1.0 is the highest.
   * @return The value. For contrast ratios, a number between 1.0 and 21.0.
   */
  get(t) {
    return t <= -1 ? this.low : t < 0 ? Il(this.low, this.normal, (t - -1) / 1) : t < 0.5 ? Il(this.normal, this.medium, (t - 0) / 0.5) : t < 1 ? Il(this.medium, this.high, (t - 0.5) / 0.5) : this.high;
  }
};
n(of, "ContrastCurve");
let et = of;
const cf = class cf {
  /**
   * Documents a constraint in tone distance between two DynamicColors.
   *
   * The polarity is an adjective that describes "A", compared to "B".
   *
   * For instance, ToneDeltaPair(A, B, 15, 'darker', 'exact') states that
   * A's tone should be exactly 15 darker than B's.
   *
   * 'relative_darker' and 'relative_lighter' describes the tone adjustment
   * relative to the surface color trend (white in light mode; black in dark
   * mode). For instance, ToneDeltaPair(A, B, 10, 'relative_lighter',
   * 'farther') states that A should be at least 10 lighter than B in light
   * mode, and at least 10 darker than B in dark mode.
   *
   * @param roleA The first role in a pair.
   * @param roleB The second role in a pair.
   * @param delta Required difference between tones. Absolute value, negative
   * values have undefined behavior.
   * @param polarity The relative relation between tones of roleA and roleB,
   * as described above.
   * @param constraint How to fulfill the tone delta pair constraint.
   * @param stayTogether Whether these two roles should stay on the same side
   * of the "awkward zone" (T50-59). This is necessary for certain cases where
   * one role has two backgrounds.
   */
  constructor(t, e, i, r, a, o) {
    this.roleA = t, this.roleB = e, this.delta = i, this.polarity = r, this.stayTogether = a, this.constraint = o, this.constraint = o ?? "exact";
  }
};
n(cf, "ToneDeltaPair");
let kt = cf;
var R;
(function(s) {
  s[s.MONOCHROME = 0] = "MONOCHROME", s[s.NEUTRAL = 1] = "NEUTRAL", s[s.TONAL_SPOT = 2] = "TONAL_SPOT", s[s.VIBRANT = 3] = "VIBRANT", s[s.EXPRESSIVE = 4] = "EXPRESSIVE", s[s.FIDELITY = 5] = "FIDELITY", s[s.CONTENT = 6] = "CONTENT", s[s.RAINBOW = 7] = "RAINBOW", s[s.FRUIT_SALAD = 8] = "FRUIT_SALAD";
})(R || (R = {}));
function Cn(s) {
  return s.variant === R.FIDELITY || s.variant === R.CONTENT;
}
n(Cn, "isFidelity");
function Ft(s) {
  return s.variant === R.MONOCHROME;
}
n(Ft, "isMonochrome");
function Tb(s, t, e, i) {
  let r = e, a = yt.from(s, t, e);
  if (a.chroma < t) {
    let o = a.chroma;
    for (; a.chroma < t; ) {
      r += i ? -1 : 1;
      const c = yt.from(s, t, r);
      if (o > c.chroma || Math.abs(c.chroma - t) < 0.4)
        break;
      const l = Math.abs(c.chroma - t), u = Math.abs(a.chroma - t);
      l < u && (a = c), o = Math.max(o, c.chroma);
    }
  }
  return r;
}
n(Tb, "findDesiredChromaByTone");
const lf = class lf {
  ////////////////////////////////////////////////////////////////
  // Main Palettes                                              //
  ////////////////////////////////////////////////////////////////
  primaryPaletteKeyColor() {
    return O.fromPalette({
      name: "primary_palette_key_color",
      palette: /* @__PURE__ */ n((t) => t.primaryPalette, "palette"),
      tone: /* @__PURE__ */ n((t) => t.primaryPalette.keyColor.tone, "tone")
    });
  }
  secondaryPaletteKeyColor() {
    return O.fromPalette({
      name: "secondary_palette_key_color",
      palette: /* @__PURE__ */ n((t) => t.secondaryPalette, "palette"),
      tone: /* @__PURE__ */ n((t) => t.secondaryPalette.keyColor.tone, "tone")
    });
  }
  tertiaryPaletteKeyColor() {
    return O.fromPalette({
      name: "tertiary_palette_key_color",
      palette: /* @__PURE__ */ n((t) => t.tertiaryPalette, "palette"),
      tone: /* @__PURE__ */ n((t) => t.tertiaryPalette.keyColor.tone, "tone")
    });
  }
  neutralPaletteKeyColor() {
    return O.fromPalette({
      name: "neutral_palette_key_color",
      palette: /* @__PURE__ */ n((t) => t.neutralPalette, "palette"),
      tone: /* @__PURE__ */ n((t) => t.neutralPalette.keyColor.tone, "tone")
    });
  }
  neutralVariantPaletteKeyColor() {
    return O.fromPalette({
      name: "neutral_variant_palette_key_color",
      palette: /* @__PURE__ */ n((t) => t.neutralVariantPalette, "palette"),
      tone: /* @__PURE__ */ n((t) => t.neutralVariantPalette.keyColor.tone, "tone")
    });
  }
  errorPaletteKeyColor() {
    return O.fromPalette({
      name: "error_palette_key_color",
      palette: /* @__PURE__ */ n((t) => t.errorPalette, "palette"),
      tone: /* @__PURE__ */ n((t) => t.errorPalette.keyColor.tone, "tone")
    });
  }
  ////////////////////////////////////////////////////////////////
  // Surfaces [S]                                               //
  ////////////////////////////////////////////////////////////////
  background() {
    return O.fromPalette({
      name: "background",
      palette: /* @__PURE__ */ n((t) => t.neutralPalette, "palette"),
      tone: /* @__PURE__ */ n((t) => t.isDark ? 6 : 98, "tone"),
      isBackground: !0
    });
  }
  onBackground() {
    return O.fromPalette({
      name: "on_background",
      palette: /* @__PURE__ */ n((t) => t.neutralPalette, "palette"),
      tone: /* @__PURE__ */ n((t) => t.isDark ? 90 : 10, "tone"),
      background: /* @__PURE__ */ n((t) => this.background(), "background"),
      contrastCurve: /* @__PURE__ */ n((t) => new et(3, 3, 4.5, 7), "contrastCurve")
    });
  }
  surface() {
    return O.fromPalette({
      name: "surface",
      palette: /* @__PURE__ */ n((t) => t.neutralPalette, "palette"),
      tone: /* @__PURE__ */ n((t) => t.isDark ? 6 : 98, "tone"),
      isBackground: !0
    });
  }
  surfaceDim() {
    return O.fromPalette({
      name: "surface_dim",
      palette: /* @__PURE__ */ n((t) => t.neutralPalette, "palette"),
      tone: /* @__PURE__ */ n((t) => t.isDark ? 6 : new et(87, 87, 80, 75).get(t.contrastLevel), "tone"),
      isBackground: !0
    });
  }
  surfaceBright() {
    return O.fromPalette({
      name: "surface_bright",
      palette: /* @__PURE__ */ n((t) => t.neutralPalette, "palette"),
      tone: /* @__PURE__ */ n((t) => t.isDark ? new et(24, 24, 29, 34).get(t.contrastLevel) : 98, "tone"),
      isBackground: !0
    });
  }
  surfaceContainerLowest() {
    return O.fromPalette({
      name: "surface_container_lowest",
      palette: /* @__PURE__ */ n((t) => t.neutralPalette, "palette"),
      tone: /* @__PURE__ */ n((t) => t.isDark ? new et(4, 4, 2, 0).get(t.contrastLevel) : 100, "tone"),
      isBackground: !0
    });
  }
  surfaceContainerLow() {
    return O.fromPalette({
      name: "surface_container_low",
      palette: /* @__PURE__ */ n((t) => t.neutralPalette, "palette"),
      tone: /* @__PURE__ */ n((t) => t.isDark ? new et(10, 10, 11, 12).get(t.contrastLevel) : new et(96, 96, 96, 95).get(t.contrastLevel), "tone"),
      isBackground: !0
    });
  }
  surfaceContainer() {
    return O.fromPalette({
      name: "surface_container",
      palette: /* @__PURE__ */ n((t) => t.neutralPalette, "palette"),
      tone: /* @__PURE__ */ n((t) => t.isDark ? new et(12, 12, 16, 20).get(t.contrastLevel) : new et(94, 94, 92, 90).get(t.contrastLevel), "tone"),
      isBackground: !0
    });
  }
  surfaceContainerHigh() {
    return O.fromPalette({
      name: "surface_container_high",
      palette: /* @__PURE__ */ n((t) => t.neutralPalette, "palette"),
      tone: /* @__PURE__ */ n((t) => t.isDark ? new et(17, 17, 21, 25).get(t.contrastLevel) : new et(92, 92, 88, 85).get(t.contrastLevel), "tone"),
      isBackground: !0
    });
  }
  surfaceContainerHighest() {
    return O.fromPalette({
      name: "surface_container_highest",
      palette: /* @__PURE__ */ n((t) => t.neutralPalette, "palette"),
      tone: /* @__PURE__ */ n((t) => t.isDark ? new et(22, 22, 26, 30).get(t.contrastLevel) : new et(90, 90, 84, 80).get(t.contrastLevel), "tone"),
      isBackground: !0
    });
  }
  onSurface() {
    return O.fromPalette({
      name: "on_surface",
      palette: /* @__PURE__ */ n((t) => t.neutralPalette, "palette"),
      tone: /* @__PURE__ */ n((t) => t.isDark ? 90 : 10, "tone"),
      background: /* @__PURE__ */ n((t) => this.highestSurface(t), "background"),
      contrastCurve: /* @__PURE__ */ n((t) => new et(4.5, 7, 11, 21), "contrastCurve")
    });
  }
  surfaceVariant() {
    return O.fromPalette({
      name: "surface_variant",
      palette: /* @__PURE__ */ n((t) => t.neutralVariantPalette, "palette"),
      tone: /* @__PURE__ */ n((t) => t.isDark ? 30 : 90, "tone"),
      isBackground: !0
    });
  }
  onSurfaceVariant() {
    return O.fromPalette({
      name: "on_surface_variant",
      palette: /* @__PURE__ */ n((t) => t.neutralVariantPalette, "palette"),
      tone: /* @__PURE__ */ n((t) => t.isDark ? 80 : 30, "tone"),
      background: /* @__PURE__ */ n((t) => this.highestSurface(t), "background"),
      contrastCurve: /* @__PURE__ */ n((t) => new et(3, 4.5, 7, 11), "contrastCurve")
    });
  }
  inverseSurface() {
    return O.fromPalette({
      name: "inverse_surface",
      palette: /* @__PURE__ */ n((t) => t.neutralPalette, "palette"),
      tone: /* @__PURE__ */ n((t) => t.isDark ? 90 : 20, "tone"),
      isBackground: !0
    });
  }
  inverseOnSurface() {
    return O.fromPalette({
      name: "inverse_on_surface",
      palette: /* @__PURE__ */ n((t) => t.neutralPalette, "palette"),
      tone: /* @__PURE__ */ n((t) => t.isDark ? 20 : 95, "tone"),
      background: /* @__PURE__ */ n((t) => this.inverseSurface(), "background"),
      contrastCurve: /* @__PURE__ */ n((t) => new et(4.5, 7, 11, 21), "contrastCurve")
    });
  }
  outline() {
    return O.fromPalette({
      name: "outline",
      palette: /* @__PURE__ */ n((t) => t.neutralVariantPalette, "palette"),
      tone: /* @__PURE__ */ n((t) => t.isDark ? 60 : 50, "tone"),
      background: /* @__PURE__ */ n((t) => this.highestSurface(t), "background"),
      contrastCurve: /* @__PURE__ */ n((t) => new et(1.5, 3, 4.5, 7), "contrastCurve")
    });
  }
  outlineVariant() {
    return O.fromPalette({
      name: "outline_variant",
      palette: /* @__PURE__ */ n((t) => t.neutralVariantPalette, "palette"),
      tone: /* @__PURE__ */ n((t) => t.isDark ? 30 : 80, "tone"),
      background: /* @__PURE__ */ n((t) => this.highestSurface(t), "background"),
      contrastCurve: /* @__PURE__ */ n((t) => new et(1, 1, 3, 4.5), "contrastCurve")
    });
  }
  shadow() {
    return O.fromPalette({
      name: "shadow",
      palette: /* @__PURE__ */ n((t) => t.neutralPalette, "palette"),
      tone: /* @__PURE__ */ n((t) => 0, "tone")
    });
  }
  scrim() {
    return O.fromPalette({
      name: "scrim",
      palette: /* @__PURE__ */ n((t) => t.neutralPalette, "palette"),
      tone: /* @__PURE__ */ n((t) => 0, "tone")
    });
  }
  surfaceTint() {
    return O.fromPalette({
      name: "surface_tint",
      palette: /* @__PURE__ */ n((t) => t.primaryPalette, "palette"),
      tone: /* @__PURE__ */ n((t) => t.isDark ? 80 : 40, "tone"),
      isBackground: !0
    });
  }
  ////////////////////////////////////////////////////////////////
  // Primary [P].                                               //
  ////////////////////////////////////////////////////////////////
  primary() {
    return O.fromPalette({
      name: "primary",
      palette: /* @__PURE__ */ n((t) => t.primaryPalette, "palette"),
      tone: /* @__PURE__ */ n((t) => Ft(t) ? t.isDark ? 100 : 0 : t.isDark ? 80 : 40, "tone"),
      isBackground: !0,
      background: /* @__PURE__ */ n((t) => this.highestSurface(t), "background"),
      contrastCurve: /* @__PURE__ */ n((t) => new et(3, 4.5, 7, 7), "contrastCurve"),
      toneDeltaPair: /* @__PURE__ */ n((t) => new kt(this.primaryContainer(), this.primary(), 10, "nearer", !1), "toneDeltaPair")
    });
  }
  primaryDim() {
  }
  onPrimary() {
    return O.fromPalette({
      name: "on_primary",
      palette: /* @__PURE__ */ n((t) => t.primaryPalette, "palette"),
      tone: /* @__PURE__ */ n((t) => Ft(t) ? t.isDark ? 10 : 90 : t.isDark ? 20 : 100, "tone"),
      background: /* @__PURE__ */ n((t) => this.primary(), "background"),
      contrastCurve: /* @__PURE__ */ n((t) => new et(4.5, 7, 11, 21), "contrastCurve")
    });
  }
  primaryContainer() {
    return O.fromPalette({
      name: "primary_container",
      palette: /* @__PURE__ */ n((t) => t.primaryPalette, "palette"),
      tone: /* @__PURE__ */ n((t) => Cn(t) ? t.sourceColorHct.tone : Ft(t) ? t.isDark ? 85 : 25 : t.isDark ? 30 : 90, "tone"),
      isBackground: !0,
      background: /* @__PURE__ */ n((t) => this.highestSurface(t), "background"),
      contrastCurve: /* @__PURE__ */ n((t) => new et(1, 1, 3, 4.5), "contrastCurve"),
      toneDeltaPair: /* @__PURE__ */ n((t) => new kt(this.primaryContainer(), this.primary(), 10, "nearer", !1), "toneDeltaPair")
    });
  }
  onPrimaryContainer() {
    return O.fromPalette({
      name: "on_primary_container",
      palette: /* @__PURE__ */ n((t) => t.primaryPalette, "palette"),
      tone: /* @__PURE__ */ n((t) => Cn(t) ? O.foregroundTone(this.primaryContainer().tone(t), 4.5) : Ft(t) ? t.isDark ? 0 : 100 : t.isDark ? 90 : 30, "tone"),
      background: /* @__PURE__ */ n((t) => this.primaryContainer(), "background"),
      contrastCurve: /* @__PURE__ */ n((t) => new et(3, 4.5, 7, 11), "contrastCurve")
    });
  }
  inversePrimary() {
    return O.fromPalette({
      name: "inverse_primary",
      palette: /* @__PURE__ */ n((t) => t.primaryPalette, "palette"),
      tone: /* @__PURE__ */ n((t) => t.isDark ? 40 : 80, "tone"),
      background: /* @__PURE__ */ n((t) => this.inverseSurface(), "background"),
      contrastCurve: /* @__PURE__ */ n((t) => new et(3, 4.5, 7, 7), "contrastCurve")
    });
  }
  /////////////////////////////////////////////////////////////////
  // Secondary [Q].                                              //
  /////////////////////////////////////////////////////////////////
  secondary() {
    return O.fromPalette({
      name: "secondary",
      palette: /* @__PURE__ */ n((t) => t.secondaryPalette, "palette"),
      tone: /* @__PURE__ */ n((t) => t.isDark ? 80 : 40, "tone"),
      isBackground: !0,
      background: /* @__PURE__ */ n((t) => this.highestSurface(t), "background"),
      contrastCurve: /* @__PURE__ */ n((t) => new et(3, 4.5, 7, 7), "contrastCurve"),
      toneDeltaPair: /* @__PURE__ */ n((t) => new kt(this.secondaryContainer(), this.secondary(), 10, "nearer", !1), "toneDeltaPair")
    });
  }
  secondaryDim() {
  }
  onSecondary() {
    return O.fromPalette({
      name: "on_secondary",
      palette: /* @__PURE__ */ n((t) => t.secondaryPalette, "palette"),
      tone: /* @__PURE__ */ n((t) => Ft(t) ? t.isDark ? 10 : 100 : t.isDark ? 20 : 100, "tone"),
      background: /* @__PURE__ */ n((t) => this.secondary(), "background"),
      contrastCurve: /* @__PURE__ */ n((t) => new et(4.5, 7, 11, 21), "contrastCurve")
    });
  }
  secondaryContainer() {
    return O.fromPalette({
      name: "secondary_container",
      palette: /* @__PURE__ */ n((t) => t.secondaryPalette, "palette"),
      tone: /* @__PURE__ */ n((t) => {
        const e = t.isDark ? 30 : 90;
        return Ft(t) ? t.isDark ? 30 : 85 : Cn(t) ? Tb(t.secondaryPalette.hue, t.secondaryPalette.chroma, e, !t.isDark) : e;
      }, "tone"),
      isBackground: !0,
      background: /* @__PURE__ */ n((t) => this.highestSurface(t), "background"),
      contrastCurve: /* @__PURE__ */ n((t) => new et(1, 1, 3, 4.5), "contrastCurve"),
      toneDeltaPair: /* @__PURE__ */ n((t) => new kt(this.secondaryContainer(), this.secondary(), 10, "nearer", !1), "toneDeltaPair")
    });
  }
  onSecondaryContainer() {
    return O.fromPalette({
      name: "on_secondary_container",
      palette: /* @__PURE__ */ n((t) => t.secondaryPalette, "palette"),
      tone: /* @__PURE__ */ n((t) => Ft(t) ? t.isDark ? 90 : 10 : Cn(t) ? O.foregroundTone(this.secondaryContainer().tone(t), 4.5) : t.isDark ? 90 : 30, "tone"),
      background: /* @__PURE__ */ n((t) => this.secondaryContainer(), "background"),
      contrastCurve: /* @__PURE__ */ n((t) => new et(3, 4.5, 7, 11), "contrastCurve")
    });
  }
  /////////////////////////////////////////////////////////////////
  // Tertiary [T].                                               //
  /////////////////////////////////////////////////////////////////
  tertiary() {
    return O.fromPalette({
      name: "tertiary",
      palette: /* @__PURE__ */ n((t) => t.tertiaryPalette, "palette"),
      tone: /* @__PURE__ */ n((t) => Ft(t) ? t.isDark ? 90 : 25 : t.isDark ? 80 : 40, "tone"),
      isBackground: !0,
      background: /* @__PURE__ */ n((t) => this.highestSurface(t), "background"),
      contrastCurve: /* @__PURE__ */ n((t) => new et(3, 4.5, 7, 7), "contrastCurve"),
      toneDeltaPair: /* @__PURE__ */ n((t) => new kt(this.tertiaryContainer(), this.tertiary(), 10, "nearer", !1), "toneDeltaPair")
    });
  }
  tertiaryDim() {
  }
  onTertiary() {
    return O.fromPalette({
      name: "on_tertiary",
      palette: /* @__PURE__ */ n((t) => t.tertiaryPalette, "palette"),
      tone: /* @__PURE__ */ n((t) => Ft(t) ? t.isDark ? 10 : 90 : t.isDark ? 20 : 100, "tone"),
      background: /* @__PURE__ */ n((t) => this.tertiary(), "background"),
      contrastCurve: /* @__PURE__ */ n((t) => new et(4.5, 7, 11, 21), "contrastCurve")
    });
  }
  tertiaryContainer() {
    return O.fromPalette({
      name: "tertiary_container",
      palette: /* @__PURE__ */ n((t) => t.tertiaryPalette, "palette"),
      tone: /* @__PURE__ */ n((t) => {
        if (Ft(t))
          return t.isDark ? 60 : 49;
        if (!Cn(t))
          return t.isDark ? 30 : 90;
        const e = t.tertiaryPalette.getHct(t.sourceColorHct.tone);
        return ql.fixIfDisliked(e).tone;
      }, "tone"),
      isBackground: !0,
      background: /* @__PURE__ */ n((t) => this.highestSurface(t), "background"),
      contrastCurve: /* @__PURE__ */ n((t) => new et(1, 1, 3, 4.5), "contrastCurve"),
      toneDeltaPair: /* @__PURE__ */ n((t) => new kt(this.tertiaryContainer(), this.tertiary(), 10, "nearer", !1), "toneDeltaPair")
    });
  }
  onTertiaryContainer() {
    return O.fromPalette({
      name: "on_tertiary_container",
      palette: /* @__PURE__ */ n((t) => t.tertiaryPalette, "palette"),
      tone: /* @__PURE__ */ n((t) => Ft(t) ? t.isDark ? 0 : 100 : Cn(t) ? O.foregroundTone(this.tertiaryContainer().tone(t), 4.5) : t.isDark ? 90 : 30, "tone"),
      background: /* @__PURE__ */ n((t) => this.tertiaryContainer(), "background"),
      contrastCurve: /* @__PURE__ */ n((t) => new et(3, 4.5, 7, 11), "contrastCurve")
    });
  }
  //////////////////////////////////////////////////////////////////
  // Error [E].                                                   //
  //////////////////////////////////////////////////////////////////
  error() {
    return O.fromPalette({
      name: "error",
      palette: /* @__PURE__ */ n((t) => t.errorPalette, "palette"),
      tone: /* @__PURE__ */ n((t) => t.isDark ? 80 : 40, "tone"),
      isBackground: !0,
      background: /* @__PURE__ */ n((t) => this.highestSurface(t), "background"),
      contrastCurve: /* @__PURE__ */ n((t) => new et(3, 4.5, 7, 7), "contrastCurve"),
      toneDeltaPair: /* @__PURE__ */ n((t) => new kt(this.errorContainer(), this.error(), 10, "nearer", !1), "toneDeltaPair")
    });
  }
  errorDim() {
  }
  onError() {
    return O.fromPalette({
      name: "on_error",
      palette: /* @__PURE__ */ n((t) => t.errorPalette, "palette"),
      tone: /* @__PURE__ */ n((t) => t.isDark ? 20 : 100, "tone"),
      background: /* @__PURE__ */ n((t) => this.error(), "background"),
      contrastCurve: /* @__PURE__ */ n((t) => new et(4.5, 7, 11, 21), "contrastCurve")
    });
  }
  errorContainer() {
    return O.fromPalette({
      name: "error_container",
      palette: /* @__PURE__ */ n((t) => t.errorPalette, "palette"),
      tone: /* @__PURE__ */ n((t) => t.isDark ? 30 : 90, "tone"),
      isBackground: !0,
      background: /* @__PURE__ */ n((t) => this.highestSurface(t), "background"),
      contrastCurve: /* @__PURE__ */ n((t) => new et(1, 1, 3, 4.5), "contrastCurve"),
      toneDeltaPair: /* @__PURE__ */ n((t) => new kt(this.errorContainer(), this.error(), 10, "nearer", !1), "toneDeltaPair")
    });
  }
  onErrorContainer() {
    return O.fromPalette({
      name: "on_error_container",
      palette: /* @__PURE__ */ n((t) => t.errorPalette, "palette"),
      tone: /* @__PURE__ */ n((t) => Ft(t) ? t.isDark ? 90 : 10 : t.isDark ? 90 : 30, "tone"),
      background: /* @__PURE__ */ n((t) => this.errorContainer(), "background"),
      contrastCurve: /* @__PURE__ */ n((t) => new et(3, 4.5, 7, 11), "contrastCurve")
    });
  }
  //////////////////////////////////////////////////////////////////
  // Primary Fixed [PF]                                           //
  //////////////////////////////////////////////////////////////////
  primaryFixed() {
    return O.fromPalette({
      name: "primary_fixed",
      palette: /* @__PURE__ */ n((t) => t.primaryPalette, "palette"),
      tone: /* @__PURE__ */ n((t) => Ft(t) ? 40 : 90, "tone"),
      isBackground: !0,
      background: /* @__PURE__ */ n((t) => this.highestSurface(t), "background"),
      contrastCurve: /* @__PURE__ */ n((t) => new et(1, 1, 3, 4.5), "contrastCurve"),
      toneDeltaPair: /* @__PURE__ */ n((t) => new kt(this.primaryFixed(), this.primaryFixedDim(), 10, "lighter", !0), "toneDeltaPair")
    });
  }
  primaryFixedDim() {
    return O.fromPalette({
      name: "primary_fixed_dim",
      palette: /* @__PURE__ */ n((t) => t.primaryPalette, "palette"),
      tone: /* @__PURE__ */ n((t) => Ft(t) ? 30 : 80, "tone"),
      isBackground: !0,
      background: /* @__PURE__ */ n((t) => this.highestSurface(t), "background"),
      contrastCurve: /* @__PURE__ */ n((t) => new et(1, 1, 3, 4.5), "contrastCurve"),
      toneDeltaPair: /* @__PURE__ */ n((t) => new kt(this.primaryFixed(), this.primaryFixedDim(), 10, "lighter", !0), "toneDeltaPair")
    });
  }
  onPrimaryFixed() {
    return O.fromPalette({
      name: "on_primary_fixed",
      palette: /* @__PURE__ */ n((t) => t.primaryPalette, "palette"),
      tone: /* @__PURE__ */ n((t) => Ft(t) ? 100 : 10, "tone"),
      background: /* @__PURE__ */ n((t) => this.primaryFixedDim(), "background"),
      secondBackground: /* @__PURE__ */ n((t) => this.primaryFixed(), "secondBackground"),
      contrastCurve: /* @__PURE__ */ n((t) => new et(4.5, 7, 11, 21), "contrastCurve")
    });
  }
  onPrimaryFixedVariant() {
    return O.fromPalette({
      name: "on_primary_fixed_variant",
      palette: /* @__PURE__ */ n((t) => t.primaryPalette, "palette"),
      tone: /* @__PURE__ */ n((t) => Ft(t) ? 90 : 30, "tone"),
      background: /* @__PURE__ */ n((t) => this.primaryFixedDim(), "background"),
      secondBackground: /* @__PURE__ */ n((t) => this.primaryFixed(), "secondBackground"),
      contrastCurve: /* @__PURE__ */ n((t) => new et(3, 4.5, 7, 11), "contrastCurve")
    });
  }
  ///////////////////////////////////////////////////////////////////
  // Secondary Fixed [QF]                                          //
  ///////////////////////////////////////////////////////////////////
  secondaryFixed() {
    return O.fromPalette({
      name: "secondary_fixed",
      palette: /* @__PURE__ */ n((t) => t.secondaryPalette, "palette"),
      tone: /* @__PURE__ */ n((t) => Ft(t) ? 80 : 90, "tone"),
      isBackground: !0,
      background: /* @__PURE__ */ n((t) => this.highestSurface(t), "background"),
      contrastCurve: /* @__PURE__ */ n((t) => new et(1, 1, 3, 4.5), "contrastCurve"),
      toneDeltaPair: /* @__PURE__ */ n((t) => new kt(this.secondaryFixed(), this.secondaryFixedDim(), 10, "lighter", !0), "toneDeltaPair")
    });
  }
  secondaryFixedDim() {
    return O.fromPalette({
      name: "secondary_fixed_dim",
      palette: /* @__PURE__ */ n((t) => t.secondaryPalette, "palette"),
      tone: /* @__PURE__ */ n((t) => Ft(t) ? 70 : 80, "tone"),
      isBackground: !0,
      background: /* @__PURE__ */ n((t) => this.highestSurface(t), "background"),
      contrastCurve: /* @__PURE__ */ n((t) => new et(1, 1, 3, 4.5), "contrastCurve"),
      toneDeltaPair: /* @__PURE__ */ n((t) => new kt(this.secondaryFixed(), this.secondaryFixedDim(), 10, "lighter", !0), "toneDeltaPair")
    });
  }
  onSecondaryFixed() {
    return O.fromPalette({
      name: "on_secondary_fixed",
      palette: /* @__PURE__ */ n((t) => t.secondaryPalette, "palette"),
      tone: /* @__PURE__ */ n((t) => 10, "tone"),
      background: /* @__PURE__ */ n((t) => this.secondaryFixedDim(), "background"),
      secondBackground: /* @__PURE__ */ n((t) => this.secondaryFixed(), "secondBackground"),
      contrastCurve: /* @__PURE__ */ n((t) => new et(4.5, 7, 11, 21), "contrastCurve")
    });
  }
  onSecondaryFixedVariant() {
    return O.fromPalette({
      name: "on_secondary_fixed_variant",
      palette: /* @__PURE__ */ n((t) => t.secondaryPalette, "palette"),
      tone: /* @__PURE__ */ n((t) => Ft(t) ? 25 : 30, "tone"),
      background: /* @__PURE__ */ n((t) => this.secondaryFixedDim(), "background"),
      secondBackground: /* @__PURE__ */ n((t) => this.secondaryFixed(), "secondBackground"),
      contrastCurve: /* @__PURE__ */ n((t) => new et(3, 4.5, 7, 11), "contrastCurve")
    });
  }
  /////////////////////////////////////////////////////////////////
  // Tertiary Fixed [TF]                                         //
  /////////////////////////////////////////////////////////////////
  tertiaryFixed() {
    return O.fromPalette({
      name: "tertiary_fixed",
      palette: /* @__PURE__ */ n((t) => t.tertiaryPalette, "palette"),
      tone: /* @__PURE__ */ n((t) => Ft(t) ? 40 : 90, "tone"),
      isBackground: !0,
      background: /* @__PURE__ */ n((t) => this.highestSurface(t), "background"),
      contrastCurve: /* @__PURE__ */ n((t) => new et(1, 1, 3, 4.5), "contrastCurve"),
      toneDeltaPair: /* @__PURE__ */ n((t) => new kt(this.tertiaryFixed(), this.tertiaryFixedDim(), 10, "lighter", !0), "toneDeltaPair")
    });
  }
  tertiaryFixedDim() {
    return O.fromPalette({
      name: "tertiary_fixed_dim",
      palette: /* @__PURE__ */ n((t) => t.tertiaryPalette, "palette"),
      tone: /* @__PURE__ */ n((t) => Ft(t) ? 30 : 80, "tone"),
      isBackground: !0,
      background: /* @__PURE__ */ n((t) => this.highestSurface(t), "background"),
      contrastCurve: /* @__PURE__ */ n((t) => new et(1, 1, 3, 4.5), "contrastCurve"),
      toneDeltaPair: /* @__PURE__ */ n((t) => new kt(this.tertiaryFixed(), this.tertiaryFixedDim(), 10, "lighter", !0), "toneDeltaPair")
    });
  }
  onTertiaryFixed() {
    return O.fromPalette({
      name: "on_tertiary_fixed",
      palette: /* @__PURE__ */ n((t) => t.tertiaryPalette, "palette"),
      tone: /* @__PURE__ */ n((t) => Ft(t) ? 100 : 10, "tone"),
      background: /* @__PURE__ */ n((t) => this.tertiaryFixedDim(), "background"),
      secondBackground: /* @__PURE__ */ n((t) => this.tertiaryFixed(), "secondBackground"),
      contrastCurve: /* @__PURE__ */ n((t) => new et(4.5, 7, 11, 21), "contrastCurve")
    });
  }
  onTertiaryFixedVariant() {
    return O.fromPalette({
      name: "on_tertiary_fixed_variant",
      palette: /* @__PURE__ */ n((t) => t.tertiaryPalette, "palette"),
      tone: /* @__PURE__ */ n((t) => Ft(t) ? 90 : 30, "tone"),
      background: /* @__PURE__ */ n((t) => this.tertiaryFixedDim(), "background"),
      secondBackground: /* @__PURE__ */ n((t) => this.tertiaryFixed(), "secondBackground"),
      contrastCurve: /* @__PURE__ */ n((t) => new et(3, 4.5, 7, 11), "contrastCurve")
    });
  }
  ////////////////////////////////////////////////////////////////
  // Other                                                      //
  ////////////////////////////////////////////////////////////////
  highestSurface(t) {
    return t.isDark ? this.surfaceBright() : this.surfaceDim();
  }
};
n(lf, "ColorSpecDelegateImpl2021");
let Sy = lf;
function mt(s, t = 0, e = 100, i = 1) {
  let r = jv(s.hue, s.chroma * i, 100, !0);
  return xe(t, e, r);
}
n(mt, "tMaxC");
function Mr(s, t = 0, e = 100) {
  let i = jv(s.hue, s.chroma, 0, !1);
  return xe(t, e, i);
}
n(Mr, "tMinC");
function jv(s, t, e, i) {
  let r = e, a = yt.from(s, t, r);
  for (; a.chroma < t && !(e < 0 || e > 100); ) {
    e += i ? -1 : 1;
    const o = yt.from(s, t, e);
    a.chroma < o.chroma && (a = o, r = e);
  }
  return r;
}
n(jv, "findBestToneForChroma");
function at(s) {
  return s === 1.5 ? new et(1.5, 1.5, 3, 5.5) : s === 3 ? new et(3, 3, 4.5, 7) : s === 4.5 ? new et(4.5, 4.5, 7, 11) : s === 6 ? new et(6, 6, 7, 11) : s === 7 ? new et(7, 7, 11, 21) : s === 9 ? new et(9, 9, 11, 21) : s === 11 ? new et(11, 11, 21, 21) : s === 21 ? new et(21, 21, 21, 21) : new et(s, s, 7, 21);
}
n(at, "getCurve");
const df = class df extends Sy {
  ////////////////////////////////////////////////////////////////
  // Surfaces [S]                                               //
  ////////////////////////////////////////////////////////////////
  surface() {
    const t = O.fromPalette({
      name: "surface",
      palette: /* @__PURE__ */ n((e) => e.neutralPalette, "palette"),
      tone: /* @__PURE__ */ n((e) => (super.surface().tone(e), e.platform === "phone" ? e.isDark ? 4 : yt.isYellow(e.neutralPalette.hue) ? 99 : e.variant === R.VIBRANT ? 97 : 98 : 0), "tone"),
      isBackground: !0
    });
    return pt(super.surface(), "2025", t);
  }
  surfaceDim() {
    const t = O.fromPalette({
      name: "surface_dim",
      palette: /* @__PURE__ */ n((e) => e.neutralPalette, "palette"),
      tone: /* @__PURE__ */ n((e) => e.isDark ? 4 : yt.isYellow(e.neutralPalette.hue) ? 90 : e.variant === R.VIBRANT ? 85 : 87, "tone"),
      isBackground: !0,
      chromaMultiplier: /* @__PURE__ */ n((e) => {
        if (!e.isDark) {
          if (e.variant === R.NEUTRAL)
            return 2.5;
          if (e.variant === R.TONAL_SPOT)
            return 1.7;
          if (e.variant === R.EXPRESSIVE)
            return yt.isYellow(e.neutralPalette.hue) ? 2.7 : 1.75;
          if (e.variant === R.VIBRANT)
            return 1.36;
        }
        return 1;
      }, "chromaMultiplier")
    });
    return pt(super.surfaceDim(), "2025", t);
  }
  surfaceBright() {
    const t = O.fromPalette({
      name: "surface_bright",
      palette: /* @__PURE__ */ n((e) => e.neutralPalette, "palette"),
      tone: /* @__PURE__ */ n((e) => e.isDark ? 18 : yt.isYellow(e.neutralPalette.hue) ? 99 : e.variant === R.VIBRANT ? 97 : 98, "tone"),
      isBackground: !0,
      chromaMultiplier: /* @__PURE__ */ n((e) => {
        if (e.isDark) {
          if (e.variant === R.NEUTRAL)
            return 2.5;
          if (e.variant === R.TONAL_SPOT)
            return 1.7;
          if (e.variant === R.EXPRESSIVE)
            return yt.isYellow(e.neutralPalette.hue) ? 2.7 : 1.75;
          if (e.variant === R.VIBRANT)
            return 1.36;
        }
        return 1;
      }, "chromaMultiplier")
    });
    return pt(super.surfaceBright(), "2025", t);
  }
  surfaceContainerLowest() {
    const t = O.fromPalette({
      name: "surface_container_lowest",
      palette: /* @__PURE__ */ n((e) => e.neutralPalette, "palette"),
      tone: /* @__PURE__ */ n((e) => e.isDark ? 0 : 100, "tone"),
      isBackground: !0
    });
    return pt(super.surfaceContainerLowest(), "2025", t);
  }
  surfaceContainerLow() {
    const t = O.fromPalette({
      name: "surface_container_low",
      palette: /* @__PURE__ */ n((e) => e.neutralPalette, "palette"),
      tone: /* @__PURE__ */ n((e) => e.platform === "phone" ? e.isDark ? 6 : yt.isYellow(e.neutralPalette.hue) ? 98 : e.variant === R.VIBRANT ? 95 : 96 : 15, "tone"),
      isBackground: !0,
      chromaMultiplier: /* @__PURE__ */ n((e) => {
        if (e.platform === "phone") {
          if (e.variant === R.NEUTRAL)
            return 1.3;
          if (e.variant === R.TONAL_SPOT)
            return 1.25;
          if (e.variant === R.EXPRESSIVE)
            return yt.isYellow(e.neutralPalette.hue) ? 1.3 : 1.15;
          if (e.variant === R.VIBRANT)
            return 1.08;
        }
        return 1;
      }, "chromaMultiplier")
    });
    return pt(super.surfaceContainerLow(), "2025", t);
  }
  surfaceContainer() {
    const t = O.fromPalette({
      name: "surface_container",
      palette: /* @__PURE__ */ n((e) => e.neutralPalette, "palette"),
      tone: /* @__PURE__ */ n((e) => e.platform === "phone" ? e.isDark ? 9 : yt.isYellow(e.neutralPalette.hue) ? 96 : e.variant === R.VIBRANT ? 92 : 94 : 20, "tone"),
      isBackground: !0,
      chromaMultiplier: /* @__PURE__ */ n((e) => {
        if (e.platform === "phone") {
          if (e.variant === R.NEUTRAL)
            return 1.6;
          if (e.variant === R.TONAL_SPOT)
            return 1.4;
          if (e.variant === R.EXPRESSIVE)
            return yt.isYellow(e.neutralPalette.hue) ? 1.6 : 1.3;
          if (e.variant === R.VIBRANT)
            return 1.15;
        }
        return 1;
      }, "chromaMultiplier")
    });
    return pt(super.surfaceContainer(), "2025", t);
  }
  surfaceContainerHigh() {
    const t = O.fromPalette({
      name: "surface_container_high",
      palette: /* @__PURE__ */ n((e) => e.neutralPalette, "palette"),
      tone: /* @__PURE__ */ n((e) => e.platform === "phone" ? e.isDark ? 12 : yt.isYellow(e.neutralPalette.hue) ? 94 : e.variant === R.VIBRANT ? 90 : 92 : 25, "tone"),
      isBackground: !0,
      chromaMultiplier: /* @__PURE__ */ n((e) => {
        if (e.platform === "phone") {
          if (e.variant === R.NEUTRAL)
            return 1.9;
          if (e.variant === R.TONAL_SPOT)
            return 1.5;
          if (e.variant === R.EXPRESSIVE)
            return yt.isYellow(e.neutralPalette.hue) ? 1.95 : 1.45;
          if (e.variant === R.VIBRANT)
            return 1.22;
        }
        return 1;
      }, "chromaMultiplier")
    });
    return pt(super.surfaceContainerHigh(), "2025", t);
  }
  surfaceContainerHighest() {
    const t = O.fromPalette({
      name: "surface_container_highest",
      palette: /* @__PURE__ */ n((e) => e.neutralPalette, "palette"),
      tone: /* @__PURE__ */ n((e) => e.isDark ? 15 : yt.isYellow(e.neutralPalette.hue) ? 92 : e.variant === R.VIBRANT ? 88 : 90, "tone"),
      isBackground: !0,
      chromaMultiplier: /* @__PURE__ */ n((e) => e.variant === R.NEUTRAL ? 2.2 : e.variant === R.TONAL_SPOT ? 1.7 : e.variant === R.EXPRESSIVE ? yt.isYellow(e.neutralPalette.hue) ? 2.3 : 1.6 : e.variant === R.VIBRANT ? 1.29 : 1, "chromaMultiplier")
    });
    return pt(super.surfaceContainerHighest(), "2025", t);
  }
  onSurface() {
    const t = O.fromPalette({
      name: "on_surface",
      palette: /* @__PURE__ */ n((e) => e.neutralPalette, "palette"),
      tone: /* @__PURE__ */ n((e) => e.variant === R.VIBRANT ? mt(e.neutralPalette, 0, 100, 1.1) : O.getInitialToneFromBackground((i) => i.platform === "phone" ? this.highestSurface(i) : this.surfaceContainerHigh())(e), "tone"),
      chromaMultiplier: /* @__PURE__ */ n((e) => {
        if (e.platform === "phone") {
          if (e.variant === R.NEUTRAL)
            return 2.2;
          if (e.variant === R.TONAL_SPOT)
            return 1.7;
          if (e.variant === R.EXPRESSIVE)
            return yt.isYellow(e.neutralPalette.hue) ? e.isDark ? 3 : 2.3 : 1.6;
        }
        return 1;
      }, "chromaMultiplier"),
      background: /* @__PURE__ */ n((e) => e.platform === "phone" ? this.highestSurface(e) : this.surfaceContainerHigh(), "background"),
      contrastCurve: /* @__PURE__ */ n((e) => e.isDark && e.platform === "phone" ? at(11) : at(9), "contrastCurve")
    });
    return pt(super.onSurface(), "2025", t);
  }
  onSurfaceVariant() {
    const t = O.fromPalette({
      name: "on_surface_variant",
      palette: /* @__PURE__ */ n((e) => e.neutralPalette, "palette"),
      chromaMultiplier: /* @__PURE__ */ n((e) => {
        if (e.platform === "phone") {
          if (e.variant === R.NEUTRAL)
            return 2.2;
          if (e.variant === R.TONAL_SPOT)
            return 1.7;
          if (e.variant === R.EXPRESSIVE)
            return yt.isYellow(e.neutralPalette.hue) ? e.isDark ? 3 : 2.3 : 1.6;
        }
        return 1;
      }, "chromaMultiplier"),
      background: /* @__PURE__ */ n((e) => e.platform === "phone" ? this.highestSurface(e) : this.surfaceContainerHigh(), "background"),
      contrastCurve: /* @__PURE__ */ n((e) => e.platform === "phone" ? e.isDark ? at(6) : at(4.5) : at(7), "contrastCurve")
    });
    return pt(super.onSurfaceVariant(), "2025", t);
  }
  outline() {
    const t = O.fromPalette({
      name: "outline",
      palette: /* @__PURE__ */ n((e) => e.neutralPalette, "palette"),
      chromaMultiplier: /* @__PURE__ */ n((e) => {
        if (e.platform === "phone") {
          if (e.variant === R.NEUTRAL)
            return 2.2;
          if (e.variant === R.TONAL_SPOT)
            return 1.7;
          if (e.variant === R.EXPRESSIVE)
            return yt.isYellow(e.neutralPalette.hue) ? e.isDark ? 3 : 2.3 : 1.6;
        }
        return 1;
      }, "chromaMultiplier"),
      background: /* @__PURE__ */ n((e) => e.platform === "phone" ? this.highestSurface(e) : this.surfaceContainerHigh(), "background"),
      contrastCurve: /* @__PURE__ */ n((e) => e.platform === "phone" ? at(3) : at(4.5), "contrastCurve")
    });
    return pt(super.outline(), "2025", t);
  }
  outlineVariant() {
    const t = O.fromPalette({
      name: "outline_variant",
      palette: /* @__PURE__ */ n((e) => e.neutralPalette, "palette"),
      chromaMultiplier: /* @__PURE__ */ n((e) => {
        if (e.platform === "phone") {
          if (e.variant === R.NEUTRAL)
            return 2.2;
          if (e.variant === R.TONAL_SPOT)
            return 1.7;
          if (e.variant === R.EXPRESSIVE)
            return yt.isYellow(e.neutralPalette.hue) ? e.isDark ? 3 : 2.3 : 1.6;
        }
        return 1;
      }, "chromaMultiplier"),
      background: /* @__PURE__ */ n((e) => e.platform === "phone" ? this.highestSurface(e) : this.surfaceContainerHigh(), "background"),
      contrastCurve: /* @__PURE__ */ n((e) => e.platform === "phone" ? at(1.5) : at(3), "contrastCurve")
    });
    return pt(super.outlineVariant(), "2025", t);
  }
  inverseSurface() {
    const t = O.fromPalette({
      name: "inverse_surface",
      palette: /* @__PURE__ */ n((e) => e.neutralPalette, "palette"),
      tone: /* @__PURE__ */ n((e) => e.isDark ? 98 : 4, "tone"),
      isBackground: !0
    });
    return pt(super.inverseSurface(), "2025", t);
  }
  inverseOnSurface() {
    const t = O.fromPalette({
      name: "inverse_on_surface",
      palette: /* @__PURE__ */ n((e) => e.neutralPalette, "palette"),
      background: /* @__PURE__ */ n((e) => this.inverseSurface(), "background"),
      contrastCurve: /* @__PURE__ */ n((e) => at(7), "contrastCurve")
    });
    return pt(super.inverseOnSurface(), "2025", t);
  }
  ////////////////////////////////////////////////////////////////
  // Primaries [P]                                              //
  ////////////////////////////////////////////////////////////////
  primary() {
    const t = O.fromPalette({
      name: "primary",
      palette: /* @__PURE__ */ n((e) => e.primaryPalette, "palette"),
      tone: /* @__PURE__ */ n((e) => e.variant === R.NEUTRAL ? e.platform === "phone" ? e.isDark ? 80 : 40 : 90 : e.variant === R.TONAL_SPOT ? e.platform === "phone" ? e.isDark ? 80 : mt(e.primaryPalette) : mt(e.primaryPalette, 0, 90) : e.variant === R.EXPRESSIVE ? e.platform === "phone" ? mt(e.primaryPalette, 0, yt.isYellow(e.primaryPalette.hue) ? 25 : yt.isCyan(e.primaryPalette.hue) ? 88 : 98) : mt(e.primaryPalette) : e.platform === "phone" ? mt(e.primaryPalette, 0, yt.isCyan(e.primaryPalette.hue) ? 88 : 98) : mt(e.primaryPalette), "tone"),
      isBackground: !0,
      background: /* @__PURE__ */ n((e) => e.platform === "phone" ? this.highestSurface(e) : this.surfaceContainerHigh(), "background"),
      contrastCurve: /* @__PURE__ */ n((e) => e.platform === "phone" ? at(4.5) : at(7), "contrastCurve"),
      toneDeltaPair: /* @__PURE__ */ n((e) => e.platform === "phone" ? new kt(this.primaryContainer(), this.primary(), 5, "relative_lighter", !0, "farther") : void 0, "toneDeltaPair")
    });
    return pt(super.primary(), "2025", t);
  }
  primaryDim() {
    return O.fromPalette({
      name: "primary_dim",
      palette: /* @__PURE__ */ n((t) => t.primaryPalette, "palette"),
      tone: /* @__PURE__ */ n((t) => t.variant === R.NEUTRAL ? 85 : t.variant === R.TONAL_SPOT ? mt(t.primaryPalette, 0, 90) : mt(t.primaryPalette), "tone"),
      isBackground: !0,
      background: /* @__PURE__ */ n((t) => this.surfaceContainerHigh(), "background"),
      contrastCurve: /* @__PURE__ */ n((t) => at(4.5), "contrastCurve"),
      toneDeltaPair: /* @__PURE__ */ n((t) => new kt(this.primaryDim(), this.primary(), 5, "darker", !0, "farther"), "toneDeltaPair")
    });
  }
  onPrimary() {
    const t = O.fromPalette({
      name: "on_primary",
      palette: /* @__PURE__ */ n((e) => e.primaryPalette, "palette"),
      background: /* @__PURE__ */ n((e) => e.platform === "phone" ? this.primary() : this.primaryDim(), "background"),
      contrastCurve: /* @__PURE__ */ n((e) => e.platform === "phone" ? at(6) : at(7), "contrastCurve")
    });
    return pt(super.onPrimary(), "2025", t);
  }
  primaryContainer() {
    const t = O.fromPalette({
      name: "primary_container",
      palette: /* @__PURE__ */ n((e) => e.primaryPalette, "palette"),
      tone: /* @__PURE__ */ n((e) => e.platform === "watch" ? 30 : e.variant === R.NEUTRAL ? e.isDark ? 30 : 90 : e.variant === R.TONAL_SPOT ? e.isDark ? Mr(e.primaryPalette, 35, 93) : mt(e.primaryPalette, 0, 90) : e.variant === R.EXPRESSIVE ? e.isDark ? mt(e.primaryPalette, 30, 93) : mt(e.primaryPalette, 78, yt.isCyan(e.primaryPalette.hue) ? 88 : 90) : e.isDark ? Mr(e.primaryPalette, 66, 93) : mt(e.primaryPalette, 66, yt.isCyan(e.primaryPalette.hue) ? 88 : 93), "tone"),
      isBackground: !0,
      background: /* @__PURE__ */ n((e) => e.platform === "phone" ? this.highestSurface(e) : void 0, "background"),
      toneDeltaPair: /* @__PURE__ */ n((e) => e.platform === "phone" ? void 0 : new kt(this.primaryContainer(), this.primaryDim(), 10, "darker", !0, "farther"), "toneDeltaPair"),
      contrastCurve: /* @__PURE__ */ n((e) => e.platform === "phone" && e.contrastLevel > 0 ? at(1.5) : void 0, "contrastCurve")
    });
    return pt(super.primaryContainer(), "2025", t);
  }
  onPrimaryContainer() {
    const t = O.fromPalette({
      name: "on_primary_container",
      palette: /* @__PURE__ */ n((e) => e.primaryPalette, "palette"),
      background: /* @__PURE__ */ n((e) => this.primaryContainer(), "background"),
      contrastCurve: /* @__PURE__ */ n((e) => e.platform === "phone" ? at(6) : at(7), "contrastCurve")
    });
    return pt(super.onPrimaryContainer(), "2025", t);
  }
  primaryFixed() {
    const t = O.fromPalette({
      name: "primary_fixed",
      palette: /* @__PURE__ */ n((e) => e.primaryPalette, "palette"),
      tone: /* @__PURE__ */ n((e) => {
        let i = Object.assign({}, e, { isDark: !1, contrastLevel: 0 });
        return this.primaryContainer().getTone(i);
      }, "tone"),
      isBackground: !0,
      background: /* @__PURE__ */ n((e) => e.platform === "phone" ? this.highestSurface(e) : void 0, "background"),
      contrastCurve: /* @__PURE__ */ n((e) => e.platform === "phone" && e.contrastLevel > 0 ? at(1.5) : void 0, "contrastCurve")
    });
    return pt(super.primaryFixed(), "2025", t);
  }
  primaryFixedDim() {
    const t = O.fromPalette({
      name: "primary_fixed_dim",
      palette: /* @__PURE__ */ n((e) => e.primaryPalette, "palette"),
      tone: /* @__PURE__ */ n((e) => this.primaryFixed().getTone(e), "tone"),
      isBackground: !0,
      toneDeltaPair: /* @__PURE__ */ n((e) => new kt(this.primaryFixedDim(), this.primaryFixed(), 5, "darker", !0, "exact"), "toneDeltaPair")
    });
    return pt(super.primaryFixedDim(), "2025", t);
  }
  onPrimaryFixed() {
    const t = O.fromPalette({
      name: "on_primary_fixed",
      palette: /* @__PURE__ */ n((e) => e.primaryPalette, "palette"),
      background: /* @__PURE__ */ n((e) => this.primaryFixedDim(), "background"),
      contrastCurve: /* @__PURE__ */ n((e) => at(7), "contrastCurve")
    });
    return pt(super.onPrimaryFixed(), "2025", t);
  }
  onPrimaryFixedVariant() {
    const t = O.fromPalette({
      name: "on_primary_fixed_variant",
      palette: /* @__PURE__ */ n((e) => e.primaryPalette, "palette"),
      background: /* @__PURE__ */ n((e) => this.primaryFixedDim(), "background"),
      contrastCurve: /* @__PURE__ */ n((e) => at(4.5), "contrastCurve")
    });
    return pt(super.onPrimaryFixedVariant(), "2025", t);
  }
  inversePrimary() {
    const t = O.fromPalette({
      name: "inverse_primary",
      palette: /* @__PURE__ */ n((e) => e.primaryPalette, "palette"),
      tone: /* @__PURE__ */ n((e) => mt(e.primaryPalette), "tone"),
      background: /* @__PURE__ */ n((e) => this.inverseSurface(), "background"),
      contrastCurve: /* @__PURE__ */ n((e) => e.platform === "phone" ? at(6) : at(7), "contrastCurve")
    });
    return pt(super.inversePrimary(), "2025", t);
  }
  ////////////////////////////////////////////////////////////////
  // Secondaries [Q]                                            //
  ////////////////////////////////////////////////////////////////
  secondary() {
    const t = O.fromPalette({
      name: "secondary",
      palette: /* @__PURE__ */ n((e) => e.secondaryPalette, "palette"),
      tone: /* @__PURE__ */ n((e) => e.platform === "watch" ? e.variant === R.NEUTRAL ? 90 : mt(e.secondaryPalette, 0, 90) : e.variant === R.NEUTRAL ? e.isDark ? Mr(e.secondaryPalette, 0, 98) : mt(e.secondaryPalette) : e.variant === R.VIBRANT ? mt(e.secondaryPalette, 0, e.isDark ? 90 : 98) : e.isDark ? 80 : mt(e.secondaryPalette), "tone"),
      isBackground: !0,
      background: /* @__PURE__ */ n((e) => e.platform === "phone" ? this.highestSurface(e) : this.surfaceContainerHigh(), "background"),
      contrastCurve: /* @__PURE__ */ n((e) => e.platform === "phone" ? at(4.5) : at(7), "contrastCurve"),
      toneDeltaPair: /* @__PURE__ */ n((e) => e.platform === "phone" ? new kt(this.secondaryContainer(), this.secondary(), 5, "relative_lighter", !0, "farther") : void 0, "toneDeltaPair")
    });
    return pt(super.secondary(), "2025", t);
  }
  secondaryDim() {
    return O.fromPalette({
      name: "secondary_dim",
      palette: /* @__PURE__ */ n((t) => t.secondaryPalette, "palette"),
      tone: /* @__PURE__ */ n((t) => t.variant === R.NEUTRAL ? 85 : mt(t.secondaryPalette, 0, 90), "tone"),
      isBackground: !0,
      background: /* @__PURE__ */ n((t) => this.surfaceContainerHigh(), "background"),
      contrastCurve: /* @__PURE__ */ n((t) => at(4.5), "contrastCurve"),
      toneDeltaPair: /* @__PURE__ */ n((t) => new kt(this.secondaryDim(), this.secondary(), 5, "darker", !0, "farther"), "toneDeltaPair")
    });
  }
  onSecondary() {
    const t = O.fromPalette({
      name: "on_secondary",
      palette: /* @__PURE__ */ n((e) => e.secondaryPalette, "palette"),
      background: /* @__PURE__ */ n((e) => e.platform === "phone" ? this.secondary() : this.secondaryDim(), "background"),
      contrastCurve: /* @__PURE__ */ n((e) => e.platform === "phone" ? at(6) : at(7), "contrastCurve")
    });
    return pt(super.onSecondary(), "2025", t);
  }
  secondaryContainer() {
    const t = O.fromPalette({
      name: "secondary_container",
      palette: /* @__PURE__ */ n((e) => e.secondaryPalette, "palette"),
      tone: /* @__PURE__ */ n((e) => e.platform === "watch" ? 30 : e.variant === R.VIBRANT ? e.isDark ? Mr(e.secondaryPalette, 30, 40) : mt(e.secondaryPalette, 84, 90) : e.variant === R.EXPRESSIVE ? e.isDark ? 15 : mt(e.secondaryPalette, 90, 95) : e.isDark ? 25 : 90, "tone"),
      isBackground: !0,
      background: /* @__PURE__ */ n((e) => e.platform === "phone" ? this.highestSurface(e) : void 0, "background"),
      toneDeltaPair: /* @__PURE__ */ n((e) => e.platform === "watch" ? new kt(this.secondaryContainer(), this.secondaryDim(), 10, "darker", !0, "farther") : void 0, "toneDeltaPair"),
      contrastCurve: /* @__PURE__ */ n((e) => e.platform === "phone" && e.contrastLevel > 0 ? at(1.5) : void 0, "contrastCurve")
    });
    return pt(super.secondaryContainer(), "2025", t);
  }
  onSecondaryContainer() {
    const t = O.fromPalette({
      name: "on_secondary_container",
      palette: /* @__PURE__ */ n((e) => e.secondaryPalette, "palette"),
      background: /* @__PURE__ */ n((e) => this.secondaryContainer(), "background"),
      contrastCurve: /* @__PURE__ */ n((e) => e.platform === "phone" ? at(6) : at(7), "contrastCurve")
    });
    return pt(super.onSecondaryContainer(), "2025", t);
  }
  secondaryFixed() {
    const t = O.fromPalette({
      name: "secondary_fixed",
      palette: /* @__PURE__ */ n((e) => e.secondaryPalette, "palette"),
      tone: /* @__PURE__ */ n((e) => {
        let i = Object.assign({}, e, { isDark: !1, contrastLevel: 0 });
        return this.secondaryContainer().getTone(i);
      }, "tone"),
      isBackground: !0,
      background: /* @__PURE__ */ n((e) => e.platform === "phone" ? this.highestSurface(e) : void 0, "background"),
      contrastCurve: /* @__PURE__ */ n((e) => e.platform === "phone" && e.contrastLevel > 0 ? at(1.5) : void 0, "contrastCurve")
    });
    return pt(super.secondaryFixed(), "2025", t);
  }
  secondaryFixedDim() {
    const t = O.fromPalette({
      name: "secondary_fixed_dim",
      palette: /* @__PURE__ */ n((e) => e.secondaryPalette, "palette"),
      tone: /* @__PURE__ */ n((e) => this.secondaryFixed().getTone(e), "tone"),
      isBackground: !0,
      toneDeltaPair: /* @__PURE__ */ n((e) => new kt(this.secondaryFixedDim(), this.secondaryFixed(), 5, "darker", !0, "exact"), "toneDeltaPair")
    });
    return pt(super.secondaryFixedDim(), "2025", t);
  }
  onSecondaryFixed() {
    const t = O.fromPalette({
      name: "on_secondary_fixed",
      palette: /* @__PURE__ */ n((e) => e.secondaryPalette, "palette"),
      background: /* @__PURE__ */ n((e) => this.secondaryFixedDim(), "background"),
      contrastCurve: /* @__PURE__ */ n((e) => at(7), "contrastCurve")
    });
    return pt(super.onSecondaryFixed(), "2025", t);
  }
  onSecondaryFixedVariant() {
    const t = O.fromPalette({
      name: "on_secondary_fixed_variant",
      palette: /* @__PURE__ */ n((e) => e.secondaryPalette, "palette"),
      background: /* @__PURE__ */ n((e) => this.secondaryFixedDim(), "background"),
      contrastCurve: /* @__PURE__ */ n((e) => at(4.5), "contrastCurve")
    });
    return pt(super.onSecondaryFixedVariant(), "2025", t);
  }
  ////////////////////////////////////////////////////////////////
  // Tertiaries [T]                                             //
  ////////////////////////////////////////////////////////////////
  tertiary() {
    const t = O.fromPalette({
      name: "tertiary",
      palette: /* @__PURE__ */ n((e) => e.tertiaryPalette, "palette"),
      tone: /* @__PURE__ */ n((e) => e.platform === "watch" ? e.variant === R.TONAL_SPOT ? mt(e.tertiaryPalette, 0, 90) : mt(e.tertiaryPalette) : e.variant === R.EXPRESSIVE || e.variant === R.VIBRANT ? mt(e.tertiaryPalette, 0, yt.isCyan(e.tertiaryPalette.hue) ? 88 : e.isDark ? 98 : 100) : e.isDark ? mt(e.tertiaryPalette, 0, 98) : mt(e.tertiaryPalette), "tone"),
      isBackground: !0,
      background: /* @__PURE__ */ n((e) => e.platform === "phone" ? this.highestSurface(e) : this.surfaceContainerHigh(), "background"),
      contrastCurve: /* @__PURE__ */ n((e) => e.platform === "phone" ? at(4.5) : at(7), "contrastCurve"),
      toneDeltaPair: /* @__PURE__ */ n((e) => e.platform === "phone" ? new kt(this.tertiaryContainer(), this.tertiary(), 5, "relative_lighter", !0, "farther") : void 0, "toneDeltaPair")
    });
    return pt(super.tertiary(), "2025", t);
  }
  tertiaryDim() {
    return O.fromPalette({
      name: "tertiary_dim",
      palette: /* @__PURE__ */ n((t) => t.tertiaryPalette, "palette"),
      tone: /* @__PURE__ */ n((t) => t.variant === R.TONAL_SPOT ? mt(t.tertiaryPalette, 0, 90) : mt(t.tertiaryPalette), "tone"),
      isBackground: !0,
      background: /* @__PURE__ */ n((t) => this.surfaceContainerHigh(), "background"),
      contrastCurve: /* @__PURE__ */ n((t) => at(4.5), "contrastCurve"),
      toneDeltaPair: /* @__PURE__ */ n((t) => new kt(this.tertiaryDim(), this.tertiary(), 5, "darker", !0, "farther"), "toneDeltaPair")
    });
  }
  onTertiary() {
    const t = O.fromPalette({
      name: "on_tertiary",
      palette: /* @__PURE__ */ n((e) => e.tertiaryPalette, "palette"),
      background: /* @__PURE__ */ n((e) => e.platform === "phone" ? this.tertiary() : this.tertiaryDim(), "background"),
      contrastCurve: /* @__PURE__ */ n((e) => e.platform === "phone" ? at(6) : at(7), "contrastCurve")
    });
    return pt(super.onTertiary(), "2025", t);
  }
  tertiaryContainer() {
    const t = O.fromPalette({
      name: "tertiary_container",
      palette: /* @__PURE__ */ n((e) => e.tertiaryPalette, "palette"),
      tone: /* @__PURE__ */ n((e) => e.platform === "watch" ? e.variant === R.TONAL_SPOT ? mt(e.tertiaryPalette, 0, 90) : mt(e.tertiaryPalette) : e.variant === R.NEUTRAL ? e.isDark ? mt(e.tertiaryPalette, 0, 93) : mt(e.tertiaryPalette, 0, 96) : e.variant === R.TONAL_SPOT ? mt(e.tertiaryPalette, 0, e.isDark ? 93 : 100) : e.variant === R.EXPRESSIVE ? mt(e.tertiaryPalette, 75, yt.isCyan(e.tertiaryPalette.hue) ? 88 : e.isDark ? 93 : 100) : e.isDark ? mt(e.tertiaryPalette, 0, 93) : mt(e.tertiaryPalette, 72, 100), "tone"),
      isBackground: !0,
      background: /* @__PURE__ */ n((e) => e.platform === "phone" ? this.highestSurface(e) : void 0, "background"),
      toneDeltaPair: /* @__PURE__ */ n((e) => e.platform === "watch" ? new kt(this.tertiaryContainer(), this.tertiaryDim(), 10, "darker", !0, "farther") : void 0, "toneDeltaPair"),
      contrastCurve: /* @__PURE__ */ n((e) => e.platform === "phone" && e.contrastLevel > 0 ? at(1.5) : void 0, "contrastCurve")
    });
    return pt(super.tertiaryContainer(), "2025", t);
  }
  onTertiaryContainer() {
    const t = O.fromPalette({
      name: "on_tertiary_container",
      palette: /* @__PURE__ */ n((e) => e.tertiaryPalette, "palette"),
      background: /* @__PURE__ */ n((e) => this.tertiaryContainer(), "background"),
      contrastCurve: /* @__PURE__ */ n((e) => e.platform === "phone" ? at(6) : at(7), "contrastCurve")
    });
    return pt(super.onTertiaryContainer(), "2025", t);
  }
  tertiaryFixed() {
    const t = O.fromPalette({
      name: "tertiary_fixed",
      palette: /* @__PURE__ */ n((e) => e.tertiaryPalette, "palette"),
      tone: /* @__PURE__ */ n((e) => {
        let i = Object.assign({}, e, { isDark: !1, contrastLevel: 0 });
        return this.tertiaryContainer().getTone(i);
      }, "tone"),
      isBackground: !0,
      background: /* @__PURE__ */ n((e) => e.platform === "phone" ? this.highestSurface(e) : void 0, "background"),
      contrastCurve: /* @__PURE__ */ n((e) => e.platform === "phone" && e.contrastLevel > 0 ? at(1.5) : void 0, "contrastCurve")
    });
    return pt(super.tertiaryFixed(), "2025", t);
  }
  tertiaryFixedDim() {
    const t = O.fromPalette({
      name: "tertiary_fixed_dim",
      palette: /* @__PURE__ */ n((e) => e.tertiaryPalette, "palette"),
      tone: /* @__PURE__ */ n((e) => this.tertiaryFixed().getTone(e), "tone"),
      isBackground: !0,
      toneDeltaPair: /* @__PURE__ */ n((e) => new kt(this.tertiaryFixedDim(), this.tertiaryFixed(), 5, "darker", !0, "exact"), "toneDeltaPair")
    });
    return pt(super.tertiaryFixedDim(), "2025", t);
  }
  onTertiaryFixed() {
    const t = O.fromPalette({
      name: "on_tertiary_fixed",
      palette: /* @__PURE__ */ n((e) => e.tertiaryPalette, "palette"),
      background: /* @__PURE__ */ n((e) => this.tertiaryFixedDim(), "background"),
      contrastCurve: /* @__PURE__ */ n((e) => at(7), "contrastCurve")
    });
    return pt(super.onTertiaryFixed(), "2025", t);
  }
  onTertiaryFixedVariant() {
    const t = O.fromPalette({
      name: "on_tertiary_fixed_variant",
      palette: /* @__PURE__ */ n((e) => e.tertiaryPalette, "palette"),
      background: /* @__PURE__ */ n((e) => this.tertiaryFixedDim(), "background"),
      contrastCurve: /* @__PURE__ */ n((e) => at(4.5), "contrastCurve")
    });
    return pt(super.onTertiaryFixedVariant(), "2025", t);
  }
  ////////////////////////////////////////////////////////////////
  // Errors [E]                                                 //
  ////////////////////////////////////////////////////////////////
  error() {
    const t = O.fromPalette({
      name: "error",
      palette: /* @__PURE__ */ n((e) => e.errorPalette, "palette"),
      tone: /* @__PURE__ */ n((e) => e.platform === "phone" ? e.isDark ? Mr(e.errorPalette, 0, 98) : mt(e.errorPalette) : Mr(e.errorPalette), "tone"),
      isBackground: !0,
      background: /* @__PURE__ */ n((e) => e.platform === "phone" ? this.highestSurface(e) : this.surfaceContainerHigh(), "background"),
      contrastCurve: /* @__PURE__ */ n((e) => e.platform === "phone" ? at(4.5) : at(7), "contrastCurve"),
      toneDeltaPair: /* @__PURE__ */ n((e) => e.platform === "phone" ? new kt(this.errorContainer(), this.error(), 5, "relative_lighter", !0, "farther") : void 0, "toneDeltaPair")
    });
    return pt(super.error(), "2025", t);
  }
  errorDim() {
    return O.fromPalette({
      name: "error_dim",
      palette: /* @__PURE__ */ n((t) => t.errorPalette, "palette"),
      tone: /* @__PURE__ */ n((t) => Mr(t.errorPalette), "tone"),
      isBackground: !0,
      background: /* @__PURE__ */ n((t) => this.surfaceContainerHigh(), "background"),
      contrastCurve: /* @__PURE__ */ n((t) => at(4.5), "contrastCurve"),
      toneDeltaPair: /* @__PURE__ */ n((t) => new kt(this.errorDim(), this.error(), 5, "darker", !0, "farther"), "toneDeltaPair")
    });
  }
  onError() {
    const t = O.fromPalette({
      name: "on_error",
      palette: /* @__PURE__ */ n((e) => e.errorPalette, "palette"),
      background: /* @__PURE__ */ n((e) => e.platform === "phone" ? this.error() : this.errorDim(), "background"),
      contrastCurve: /* @__PURE__ */ n((e) => e.platform === "phone" ? at(6) : at(7), "contrastCurve")
    });
    return pt(super.onError(), "2025", t);
  }
  errorContainer() {
    const t = O.fromPalette({
      name: "error_container",
      palette: /* @__PURE__ */ n((e) => e.errorPalette, "palette"),
      tone: /* @__PURE__ */ n((e) => e.platform === "watch" ? 30 : e.isDark ? Mr(e.errorPalette, 30, 93) : mt(e.errorPalette, 0, 90), "tone"),
      isBackground: !0,
      background: /* @__PURE__ */ n((e) => e.platform === "phone" ? this.highestSurface(e) : void 0, "background"),
      toneDeltaPair: /* @__PURE__ */ n((e) => e.platform === "watch" ? new kt(this.errorContainer(), this.errorDim(), 10, "darker", !0, "farther") : void 0, "toneDeltaPair"),
      contrastCurve: /* @__PURE__ */ n((e) => e.platform === "phone" && e.contrastLevel > 0 ? at(1.5) : void 0, "contrastCurve")
    });
    return pt(super.errorContainer(), "2025", t);
  }
  onErrorContainer() {
    const t = O.fromPalette({
      name: "on_error_container",
      palette: /* @__PURE__ */ n((e) => e.errorPalette, "palette"),
      background: /* @__PURE__ */ n((e) => this.errorContainer(), "background"),
      contrastCurve: /* @__PURE__ */ n((e) => e.platform === "phone" ? at(4.5) : at(7), "contrastCurve")
    });
    return pt(super.onErrorContainer(), "2025", t);
  }
  /////////////////////////////////////////////////////////////////
  // Remapped Colors                                             //
  /////////////////////////////////////////////////////////////////
  surfaceVariant() {
    const t = Object.assign(this.surfaceContainerHighest().clone(), { name: "surface_variant" });
    return pt(super.surfaceVariant(), "2025", t);
  }
  surfaceTint() {
    const t = Object.assign(this.primary().clone(), { name: "surface_tint" });
    return pt(super.surfaceTint(), "2025", t);
  }
  background() {
    const t = Object.assign(this.surface().clone(), { name: "background" });
    return pt(super.background(), "2025", t);
  }
  onBackground() {
    const t = Object.assign(this.onSurface().clone(), {
      name: "on_background",
      tone: /* @__PURE__ */ n((e) => e.platform === "watch" ? 100 : this.onSurface().getTone(e), "tone")
    });
    return pt(super.onBackground(), "2025", t);
  }
};
n(df, "ColorSpecDelegateImpl2025");
let My = df;
const G = class G {
  constructor() {
    this.allColors = [
      this.background(),
      this.onBackground(),
      this.surface(),
      this.surfaceDim(),
      this.surfaceBright(),
      this.surfaceContainerLowest(),
      this.surfaceContainerLow(),
      this.surfaceContainer(),
      this.surfaceContainerHigh(),
      this.surfaceContainerHighest(),
      this.onSurface(),
      this.onSurfaceVariant(),
      this.outline(),
      this.outlineVariant(),
      this.inverseSurface(),
      this.inverseOnSurface(),
      this.primary(),
      this.primaryDim(),
      this.onPrimary(),
      this.primaryContainer(),
      this.onPrimaryContainer(),
      this.primaryFixed(),
      this.primaryFixedDim(),
      this.onPrimaryFixed(),
      this.onPrimaryFixedVariant(),
      this.inversePrimary(),
      this.secondary(),
      this.secondaryDim(),
      this.onSecondary(),
      this.secondaryContainer(),
      this.onSecondaryContainer(),
      this.secondaryFixed(),
      this.secondaryFixedDim(),
      this.onSecondaryFixed(),
      this.onSecondaryFixedVariant(),
      this.tertiary(),
      this.tertiaryDim(),
      this.onTertiary(),
      this.tertiaryContainer(),
      this.onTertiaryContainer(),
      this.tertiaryFixed(),
      this.tertiaryFixedDim(),
      this.onTertiaryFixed(),
      this.onTertiaryFixedVariant(),
      this.error(),
      this.errorDim(),
      this.onError(),
      this.errorContainer(),
      this.onErrorContainer()
    ].filter((t) => t !== void 0);
  }
  highestSurface(t) {
    return G.colorSpec.highestSurface(t);
  }
  ////////////////////////////////////////////////////////////////
  // Main Palettes                                              //
  ////////////////////////////////////////////////////////////////
  primaryPaletteKeyColor() {
    return G.colorSpec.primaryPaletteKeyColor();
  }
  secondaryPaletteKeyColor() {
    return G.colorSpec.secondaryPaletteKeyColor();
  }
  tertiaryPaletteKeyColor() {
    return G.colorSpec.tertiaryPaletteKeyColor();
  }
  neutralPaletteKeyColor() {
    return G.colorSpec.neutralPaletteKeyColor();
  }
  neutralVariantPaletteKeyColor() {
    return G.colorSpec.neutralVariantPaletteKeyColor();
  }
  errorPaletteKeyColor() {
    return G.colorSpec.errorPaletteKeyColor();
  }
  ////////////////////////////////////////////////////////////////
  // Surfaces [S]                                               //
  ////////////////////////////////////////////////////////////////
  background() {
    return G.colorSpec.background();
  }
  onBackground() {
    return G.colorSpec.onBackground();
  }
  surface() {
    return G.colorSpec.surface();
  }
  surfaceDim() {
    return G.colorSpec.surfaceDim();
  }
  surfaceBright() {
    return G.colorSpec.surfaceBright();
  }
  surfaceContainerLowest() {
    return G.colorSpec.surfaceContainerLowest();
  }
  surfaceContainerLow() {
    return G.colorSpec.surfaceContainerLow();
  }
  surfaceContainer() {
    return G.colorSpec.surfaceContainer();
  }
  surfaceContainerHigh() {
    return G.colorSpec.surfaceContainerHigh();
  }
  surfaceContainerHighest() {
    return G.colorSpec.surfaceContainerHighest();
  }
  onSurface() {
    return G.colorSpec.onSurface();
  }
  surfaceVariant() {
    return G.colorSpec.surfaceVariant();
  }
  onSurfaceVariant() {
    return G.colorSpec.onSurfaceVariant();
  }
  outline() {
    return G.colorSpec.outline();
  }
  outlineVariant() {
    return G.colorSpec.outlineVariant();
  }
  inverseSurface() {
    return G.colorSpec.inverseSurface();
  }
  inverseOnSurface() {
    return G.colorSpec.inverseOnSurface();
  }
  shadow() {
    return G.colorSpec.shadow();
  }
  scrim() {
    return G.colorSpec.scrim();
  }
  surfaceTint() {
    return G.colorSpec.surfaceTint();
  }
  ////////////////////////////////////////////////////////////////
  // Primaries [P]                                              //
  ////////////////////////////////////////////////////////////////
  primary() {
    return G.colorSpec.primary();
  }
  primaryDim() {
    return G.colorSpec.primaryDim();
  }
  onPrimary() {
    return G.colorSpec.onPrimary();
  }
  primaryContainer() {
    return G.colorSpec.primaryContainer();
  }
  onPrimaryContainer() {
    return G.colorSpec.onPrimaryContainer();
  }
  inversePrimary() {
    return G.colorSpec.inversePrimary();
  }
  /////////////////////////////////////////////////////////////////
  // Primary Fixed [PF]                                          //
  /////////////////////////////////////////////////////////////////
  primaryFixed() {
    return G.colorSpec.primaryFixed();
  }
  primaryFixedDim() {
    return G.colorSpec.primaryFixedDim();
  }
  onPrimaryFixed() {
    return G.colorSpec.onPrimaryFixed();
  }
  onPrimaryFixedVariant() {
    return G.colorSpec.onPrimaryFixedVariant();
  }
  ////////////////////////////////////////////////////////////////
  // Secondaries [Q]                                            //
  ////////////////////////////////////////////////////////////////
  secondary() {
    return G.colorSpec.secondary();
  }
  secondaryDim() {
    return G.colorSpec.secondaryDim();
  }
  onSecondary() {
    return G.colorSpec.onSecondary();
  }
  secondaryContainer() {
    return G.colorSpec.secondaryContainer();
  }
  onSecondaryContainer() {
    return G.colorSpec.onSecondaryContainer();
  }
  /////////////////////////////////////////////////////////////////
  // Secondary Fixed [QF]                                        //
  /////////////////////////////////////////////////////////////////
  secondaryFixed() {
    return G.colorSpec.secondaryFixed();
  }
  secondaryFixedDim() {
    return G.colorSpec.secondaryFixedDim();
  }
  onSecondaryFixed() {
    return G.colorSpec.onSecondaryFixed();
  }
  onSecondaryFixedVariant() {
    return G.colorSpec.onSecondaryFixedVariant();
  }
  ////////////////////////////////////////////////////////////////
  // Tertiaries [T]                                             //
  ////////////////////////////////////////////////////////////////
  tertiary() {
    return G.colorSpec.tertiary();
  }
  tertiaryDim() {
    return G.colorSpec.tertiaryDim();
  }
  onTertiary() {
    return G.colorSpec.onTertiary();
  }
  tertiaryContainer() {
    return G.colorSpec.tertiaryContainer();
  }
  onTertiaryContainer() {
    return G.colorSpec.onTertiaryContainer();
  }
  /////////////////////////////////////////////////////////////////
  // Tertiary Fixed [TF]                                         //
  /////////////////////////////////////////////////////////////////
  tertiaryFixed() {
    return G.colorSpec.tertiaryFixed();
  }
  tertiaryFixedDim() {
    return G.colorSpec.tertiaryFixedDim();
  }
  onTertiaryFixed() {
    return G.colorSpec.onTertiaryFixed();
  }
  onTertiaryFixedVariant() {
    return G.colorSpec.onTertiaryFixedVariant();
  }
  ////////////////////////////////////////////////////////////////
  // Errors [E]                                                 //
  ////////////////////////////////////////////////////////////////
  error() {
    return G.colorSpec.error();
  }
  errorDim() {
    return G.colorSpec.errorDim();
  }
  onError() {
    return G.colorSpec.onError();
  }
  errorContainer() {
    return G.colorSpec.errorContainer();
  }
  onErrorContainer() {
    return G.colorSpec.onErrorContainer();
  }
  // Static variables are deprecated. Use the instance methods to get correct
  // specs based on request.
  /** @deprecated Use highestSurface() instead. */
  static highestSurface(t) {
    return G.colorSpec.highestSurface(t);
  }
};
n(G, "MaterialDynamicColors");
let F = G;
F.contentAccentToneDelta = 15;
F.colorSpec = new My();
F.primaryPaletteKeyColor = F.colorSpec.primaryPaletteKeyColor();
F.secondaryPaletteKeyColor = F.colorSpec.secondaryPaletteKeyColor();
F.tertiaryPaletteKeyColor = F.colorSpec.tertiaryPaletteKeyColor();
F.neutralPaletteKeyColor = F.colorSpec.neutralPaletteKeyColor();
F.neutralVariantPaletteKeyColor = F.colorSpec.neutralVariantPaletteKeyColor();
F.background = F.colorSpec.background();
F.onBackground = F.colorSpec.onBackground();
F.surface = F.colorSpec.surface();
F.surfaceDim = F.colorSpec.surfaceDim();
F.surfaceBright = F.colorSpec.surfaceBright();
F.surfaceContainerLowest = F.colorSpec.surfaceContainerLowest();
F.surfaceContainerLow = F.colorSpec.surfaceContainerLow();
F.surfaceContainer = F.colorSpec.surfaceContainer();
F.surfaceContainerHigh = F.colorSpec.surfaceContainerHigh();
F.surfaceContainerHighest = F.colorSpec.surfaceContainerHighest();
F.onSurface = F.colorSpec.onSurface();
F.surfaceVariant = F.colorSpec.surfaceVariant();
F.onSurfaceVariant = F.colorSpec.onSurfaceVariant();
F.inverseSurface = F.colorSpec.inverseSurface();
F.inverseOnSurface = F.colorSpec.inverseOnSurface();
F.outline = F.colorSpec.outline();
F.outlineVariant = F.colorSpec.outlineVariant();
F.shadow = F.colorSpec.shadow();
F.scrim = F.colorSpec.scrim();
F.surfaceTint = F.colorSpec.surfaceTint();
F.primary = F.colorSpec.primary();
F.onPrimary = F.colorSpec.onPrimary();
F.primaryContainer = F.colorSpec.primaryContainer();
F.onPrimaryContainer = F.colorSpec.onPrimaryContainer();
F.inversePrimary = F.colorSpec.inversePrimary();
F.secondary = F.colorSpec.secondary();
F.onSecondary = F.colorSpec.onSecondary();
F.secondaryContainer = F.colorSpec.secondaryContainer();
F.onSecondaryContainer = F.colorSpec.onSecondaryContainer();
F.tertiary = F.colorSpec.tertiary();
F.onTertiary = F.colorSpec.onTertiary();
F.tertiaryContainer = F.colorSpec.tertiaryContainer();
F.onTertiaryContainer = F.colorSpec.onTertiaryContainer();
F.error = F.colorSpec.error();
F.onError = F.colorSpec.onError();
F.errorContainer = F.colorSpec.errorContainer();
F.onErrorContainer = F.colorSpec.onErrorContainer();
F.primaryFixed = F.colorSpec.primaryFixed();
F.primaryFixedDim = F.colorSpec.primaryFixedDim();
F.onPrimaryFixed = F.colorSpec.onPrimaryFixed();
F.onPrimaryFixedVariant = F.colorSpec.onPrimaryFixedVariant();
F.secondaryFixed = F.colorSpec.secondaryFixed();
F.secondaryFixedDim = F.colorSpec.secondaryFixedDim();
F.onSecondaryFixed = F.colorSpec.onSecondaryFixed();
F.onSecondaryFixedVariant = F.colorSpec.onSecondaryFixedVariant();
F.tertiaryFixed = F.colorSpec.tertiaryFixed();
F.tertiaryFixedDim = F.colorSpec.tertiaryFixedDim();
F.onTertiaryFixed = F.colorSpec.onTertiaryFixed();
F.onTertiaryFixedVariant = F.colorSpec.onTertiaryFixedVariant();
const Ul = class Ul {
  static maybeFallbackSpecVersion(t, e) {
    switch (e) {
      case R.EXPRESSIVE:
      case R.VIBRANT:
      case R.TONAL_SPOT:
      case R.NEUTRAL:
        return t;
      default:
        return "2021";
    }
  }
  constructor(t) {
    this.sourceColorArgb = t.sourceColorHct.toInt(), this.variant = t.variant, this.contrastLevel = t.contrastLevel, this.isDark = t.isDark, this.platform = t.platform ?? "phone", this.specVersion = Ul.maybeFallbackSpecVersion(t.specVersion ?? "2021", this.variant), this.sourceColorHct = t.sourceColorHct, this.primaryPalette = t.primaryPalette ?? $n(this.specVersion).getPrimaryPalette(this.variant, t.sourceColorHct, this.isDark, this.platform, this.contrastLevel), this.secondaryPalette = t.secondaryPalette ?? $n(this.specVersion).getSecondaryPalette(this.variant, t.sourceColorHct, this.isDark, this.platform, this.contrastLevel), this.tertiaryPalette = t.tertiaryPalette ?? $n(this.specVersion).getTertiaryPalette(this.variant, t.sourceColorHct, this.isDark, this.platform, this.contrastLevel), this.neutralPalette = t.neutralPalette ?? $n(this.specVersion).getNeutralPalette(this.variant, t.sourceColorHct, this.isDark, this.platform, this.contrastLevel), this.neutralVariantPalette = t.neutralVariantPalette ?? $n(this.specVersion).getNeutralVariantPalette(this.variant, t.sourceColorHct, this.isDark, this.platform, this.contrastLevel), this.errorPalette = t.errorPalette ?? $n(this.specVersion).getErrorPalette(this.variant, t.sourceColorHct, this.isDark, this.platform, this.contrastLevel) ?? q.fromHueAndChroma(25, 84), this.colors = new F();
  }
  toString() {
    return `Scheme: variant=${R[this.variant]}, mode=${this.isDark ? "dark" : "light"}, platform=${this.platform}, contrastLevel=${this.contrastLevel.toFixed(1)}, seed=${this.sourceColorHct.toString()}, specVersion=${this.specVersion}`;
  }
  /**
   * Returns a new hue based on a piecewise function and input color hue.
   *
   * For example, for the following function:
   * result = 26 if 0 <= hue < 101
   * result = 39 if 101 <= hue < 210
   * result = 28 if 210 <= hue < 360
   *
   * call the function as:
   *
   * const hueBreakpoints = [0, 101, 210, 360];
   * const hues = [26, 39, 28];
   * const result = scheme.piecewise(hue, hueBreakpoints, hues);
   *
   * @param sourceColorHct The input value.
   * @param hueBreakpoints The breakpoints, in sorted order. No default lower or
   *     upper bounds are assumed.
   * @param hues The hues that should be applied when source color's hue is >=
   *     the same index in hueBrakpoints array, and < the hue at the next index
   *     in hueBrakpoints array. Otherwise, the source color's hue is returned.
   */
  static getPiecewiseHue(t, e, i) {
    const r = Math.min(e.length - 1, i.length), a = t.hue;
    for (let o = 0; o < r; o++)
      if (a >= e[o] && a < e[o + 1])
        return Ve(i[o]);
    return a;
  }
  /**
   * Returns a shifted hue based on a piecewise function and input color hue.
   *
   * For example, for the following function:
   * result = hue + 26 if 0 <= hue < 101
   * result = hue - 39 if 101 <= hue < 210
   * result = hue + 28 if 210 <= hue < 360
   *
   * call the function as:
   *
   * const hueBreakpoints = [0, 101, 210, 360];
   * const hues = [26, -39, 28];
   * const result = scheme.getRotatedHue(hue, hueBreakpoints, hues);
   *
   * @param sourceColorHct the source color of the theme, in HCT.
   * @param hueBreakpoints The "breakpoints", i.e. the hues at which a rotation
   *     should be apply. No default lower or upper bounds are assumed.
   * @param rotations The rotation that should be applied when source color's
   *     hue is >= the same index in hues array, and < the hue at the next
   *     index in hues array. Otherwise, the source color's hue is returned.
   */
  static getRotatedHue(t, e, i) {
    let r = Ul.getPiecewiseHue(t, e, i);
    return Math.min(e.length - 1, i.length) <= 0 && (r = 0), Ve(t.hue + r);
  }
  getArgb(t) {
    return t.getArgb(this);
  }
  getHct(t) {
    return t.getHct(this);
  }
  // Palette key colors
  get primaryPaletteKeyColor() {
    return this.getArgb(this.colors.primaryPaletteKeyColor());
  }
  get secondaryPaletteKeyColor() {
    return this.getArgb(this.colors.secondaryPaletteKeyColor());
  }
  get tertiaryPaletteKeyColor() {
    return this.getArgb(this.colors.tertiaryPaletteKeyColor());
  }
  get neutralPaletteKeyColor() {
    return this.getArgb(this.colors.neutralPaletteKeyColor());
  }
  get neutralVariantPaletteKeyColor() {
    return this.getArgb(this.colors.neutralVariantPaletteKeyColor());
  }
  get errorPaletteKeyColor() {
    return this.getArgb(this.colors.errorPaletteKeyColor());
  }
  // Surface colors
  get background() {
    return this.getArgb(this.colors.background());
  }
  get onBackground() {
    return this.getArgb(this.colors.onBackground());
  }
  get surface() {
    return this.getArgb(this.colors.surface());
  }
  get surfaceDim() {
    return this.getArgb(this.colors.surfaceDim());
  }
  get surfaceBright() {
    return this.getArgb(this.colors.surfaceBright());
  }
  get surfaceContainerLowest() {
    return this.getArgb(this.colors.surfaceContainerLowest());
  }
  get surfaceContainerLow() {
    return this.getArgb(this.colors.surfaceContainerLow());
  }
  get surfaceContainer() {
    return this.getArgb(this.colors.surfaceContainer());
  }
  get surfaceContainerHigh() {
    return this.getArgb(this.colors.surfaceContainerHigh());
  }
  get surfaceContainerHighest() {
    return this.getArgb(this.colors.surfaceContainerHighest());
  }
  get onSurface() {
    return this.getArgb(this.colors.onSurface());
  }
  get surfaceVariant() {
    return this.getArgb(this.colors.surfaceVariant());
  }
  get onSurfaceVariant() {
    return this.getArgb(this.colors.onSurfaceVariant());
  }
  get inverseSurface() {
    return this.getArgb(this.colors.inverseSurface());
  }
  get inverseOnSurface() {
    return this.getArgb(this.colors.inverseOnSurface());
  }
  get outline() {
    return this.getArgb(this.colors.outline());
  }
  get outlineVariant() {
    return this.getArgb(this.colors.outlineVariant());
  }
  get shadow() {
    return this.getArgb(this.colors.shadow());
  }
  get scrim() {
    return this.getArgb(this.colors.scrim());
  }
  get surfaceTint() {
    return this.getArgb(this.colors.surfaceTint());
  }
  // Primary colors
  get primary() {
    return this.getArgb(this.colors.primary());
  }
  get primaryDim() {
    const t = this.colors.primaryDim();
    if (t === void 0)
      throw new Error("`primaryDim` color is undefined prior to 2025 spec.");
    return this.getArgb(t);
  }
  get onPrimary() {
    return this.getArgb(this.colors.onPrimary());
  }
  get primaryContainer() {
    return this.getArgb(this.colors.primaryContainer());
  }
  get onPrimaryContainer() {
    return this.getArgb(this.colors.onPrimaryContainer());
  }
  get primaryFixed() {
    return this.getArgb(this.colors.primaryFixed());
  }
  get primaryFixedDim() {
    return this.getArgb(this.colors.primaryFixedDim());
  }
  get onPrimaryFixed() {
    return this.getArgb(this.colors.onPrimaryFixed());
  }
  get onPrimaryFixedVariant() {
    return this.getArgb(this.colors.onPrimaryFixedVariant());
  }
  get inversePrimary() {
    return this.getArgb(this.colors.inversePrimary());
  }
  // Secondary colors
  get secondary() {
    return this.getArgb(this.colors.secondary());
  }
  get secondaryDim() {
    const t = this.colors.secondaryDim();
    if (t === void 0)
      throw new Error("`secondaryDim` color is undefined prior to 2025 spec.");
    return this.getArgb(t);
  }
  get onSecondary() {
    return this.getArgb(this.colors.onSecondary());
  }
  get secondaryContainer() {
    return this.getArgb(this.colors.secondaryContainer());
  }
  get onSecondaryContainer() {
    return this.getArgb(this.colors.onSecondaryContainer());
  }
  get secondaryFixed() {
    return this.getArgb(this.colors.secondaryFixed());
  }
  get secondaryFixedDim() {
    return this.getArgb(this.colors.secondaryFixedDim());
  }
  get onSecondaryFixed() {
    return this.getArgb(this.colors.onSecondaryFixed());
  }
  get onSecondaryFixedVariant() {
    return this.getArgb(this.colors.onSecondaryFixedVariant());
  }
  // Tertiary colors
  get tertiary() {
    return this.getArgb(this.colors.tertiary());
  }
  get tertiaryDim() {
    const t = this.colors.tertiaryDim();
    if (t === void 0)
      throw new Error("`tertiaryDim` color is undefined prior to 2025 spec.");
    return this.getArgb(t);
  }
  get onTertiary() {
    return this.getArgb(this.colors.onTertiary());
  }
  get tertiaryContainer() {
    return this.getArgb(this.colors.tertiaryContainer());
  }
  get onTertiaryContainer() {
    return this.getArgb(this.colors.onTertiaryContainer());
  }
  get tertiaryFixed() {
    return this.getArgb(this.colors.tertiaryFixed());
  }
  get tertiaryFixedDim() {
    return this.getArgb(this.colors.tertiaryFixedDim());
  }
  get onTertiaryFixed() {
    return this.getArgb(this.colors.onTertiaryFixed());
  }
  get onTertiaryFixedVariant() {
    return this.getArgb(this.colors.onTertiaryFixedVariant());
  }
  // Error colors
  get error() {
    return this.getArgb(this.colors.error());
  }
  get errorDim() {
    const t = this.colors.errorDim();
    if (t === void 0)
      throw new Error("`errorDim` color is undefined prior to 2025 spec.");
    return this.getArgb(t);
  }
  get onError() {
    return this.getArgb(this.colors.onError());
  }
  get errorContainer() {
    return this.getArgb(this.colors.errorContainer());
  }
  get onErrorContainer() {
    return this.getArgb(this.colors.onErrorContainer());
  }
};
n(Ul, "DynamicScheme");
let fe = Ul;
fe.DEFAULT_SPEC_VERSION = "2021";
fe.DEFAULT_PLATFORM = "phone";
const hf = class hf {
  //////////////////////////////////////////////////////////////////
  // Scheme Palettes                                              //
  //////////////////////////////////////////////////////////////////
  getPrimaryPalette(t, e, i, r, a) {
    switch (t) {
      case R.CONTENT:
      case R.FIDELITY:
        return q.fromHueAndChroma(e.hue, e.chroma);
      case R.FRUIT_SALAD:
        return q.fromHueAndChroma(Ve(e.hue - 50), 48);
      case R.MONOCHROME:
        return q.fromHueAndChroma(e.hue, 0);
      case R.NEUTRAL:
        return q.fromHueAndChroma(e.hue, 12);
      case R.RAINBOW:
        return q.fromHueAndChroma(e.hue, 48);
      case R.TONAL_SPOT:
        return q.fromHueAndChroma(e.hue, 36);
      case R.EXPRESSIVE:
        return q.fromHueAndChroma(Ve(e.hue + 240), 40);
      case R.VIBRANT:
        return q.fromHueAndChroma(e.hue, 200);
      default:
        throw new Error(`Unsupported variant: ${t}`);
    }
  }
  getSecondaryPalette(t, e, i, r, a) {
    switch (t) {
      case R.CONTENT:
      case R.FIDELITY:
        return q.fromHueAndChroma(e.hue, Math.max(e.chroma - 32, e.chroma * 0.5));
      case R.FRUIT_SALAD:
        return q.fromHueAndChroma(Ve(e.hue - 50), 36);
      case R.MONOCHROME:
        return q.fromHueAndChroma(e.hue, 0);
      case R.NEUTRAL:
        return q.fromHueAndChroma(e.hue, 8);
      case R.RAINBOW:
        return q.fromHueAndChroma(e.hue, 16);
      case R.TONAL_SPOT:
        return q.fromHueAndChroma(e.hue, 16);
      case R.EXPRESSIVE:
        return q.fromHueAndChroma(fe.getRotatedHue(e, [0, 21, 51, 121, 151, 191, 271, 321, 360], [45, 95, 45, 20, 45, 90, 45, 45, 45]), 24);
      case R.VIBRANT:
        return q.fromHueAndChroma(fe.getRotatedHue(e, [0, 41, 61, 101, 131, 181, 251, 301, 360], [18, 15, 10, 12, 15, 18, 15, 12, 12]), 24);
      default:
        throw new Error(`Unsupported variant: ${t}`);
    }
  }
  getTertiaryPalette(t, e, i, r, a) {
    switch (t) {
      case R.CONTENT:
        return q.fromHct(ql.fixIfDisliked(new Qh(e).analogous(
          /* count= */
          3,
          /* divisions= */
          6
        )[2]));
      case R.FIDELITY:
        return q.fromHct(ql.fixIfDisliked(new Qh(e).complement));
      case R.FRUIT_SALAD:
        return q.fromHueAndChroma(e.hue, 36);
      case R.MONOCHROME:
        return q.fromHueAndChroma(e.hue, 0);
      case R.NEUTRAL:
        return q.fromHueAndChroma(e.hue, 16);
      case R.RAINBOW:
      case R.TONAL_SPOT:
        return q.fromHueAndChroma(Ve(e.hue + 60), 24);
      case R.EXPRESSIVE:
        return q.fromHueAndChroma(fe.getRotatedHue(e, [0, 21, 51, 121, 151, 191, 271, 321, 360], [120, 120, 20, 45, 20, 15, 20, 120, 120]), 32);
      case R.VIBRANT:
        return q.fromHueAndChroma(fe.getRotatedHue(e, [0, 41, 61, 101, 131, 181, 251, 301, 360], [35, 30, 20, 25, 30, 35, 30, 25, 25]), 32);
      default:
        throw new Error(`Unsupported variant: ${t}`);
    }
  }
  getNeutralPalette(t, e, i, r, a) {
    switch (t) {
      case R.CONTENT:
      case R.FIDELITY:
        return q.fromHueAndChroma(e.hue, e.chroma / 8);
      case R.FRUIT_SALAD:
        return q.fromHueAndChroma(e.hue, 10);
      case R.MONOCHROME:
        return q.fromHueAndChroma(e.hue, 0);
      case R.NEUTRAL:
        return q.fromHueAndChroma(e.hue, 2);
      case R.RAINBOW:
        return q.fromHueAndChroma(e.hue, 0);
      case R.TONAL_SPOT:
        return q.fromHueAndChroma(e.hue, 6);
      case R.EXPRESSIVE:
        return q.fromHueAndChroma(Ve(e.hue + 15), 8);
      case R.VIBRANT:
        return q.fromHueAndChroma(e.hue, 10);
      default:
        throw new Error(`Unsupported variant: ${t}`);
    }
  }
  getNeutralVariantPalette(t, e, i, r, a) {
    switch (t) {
      case R.CONTENT:
        return q.fromHueAndChroma(e.hue, e.chroma / 8 + 4);
      case R.FIDELITY:
        return q.fromHueAndChroma(e.hue, e.chroma / 8 + 4);
      case R.FRUIT_SALAD:
        return q.fromHueAndChroma(e.hue, 16);
      case R.MONOCHROME:
        return q.fromHueAndChroma(e.hue, 0);
      case R.NEUTRAL:
        return q.fromHueAndChroma(e.hue, 2);
      case R.RAINBOW:
        return q.fromHueAndChroma(e.hue, 0);
      case R.TONAL_SPOT:
        return q.fromHueAndChroma(e.hue, 8);
      case R.EXPRESSIVE:
        return q.fromHueAndChroma(Ve(e.hue + 15), 12);
      case R.VIBRANT:
        return q.fromHueAndChroma(e.hue, 12);
      default:
        throw new Error(`Unsupported variant: ${t}`);
    }
  }
  getErrorPalette(t, e, i, r, a) {
  }
};
n(hf, "DynamicSchemePalettesDelegateImpl2021");
let Gh = hf;
const Qe = class Qe extends Gh {
  //////////////////////////////////////////////////////////////////
  // Scheme Palettes                                              //
  //////////////////////////////////////////////////////////////////
  getPrimaryPalette(t, e, i, r, a) {
    switch (t) {
      case R.NEUTRAL:
        return q.fromHueAndChroma(e.hue, r === "phone" ? yt.isBlue(e.hue) ? 12 : 8 : yt.isBlue(e.hue) ? 16 : 12);
      case R.TONAL_SPOT:
        return q.fromHueAndChroma(e.hue, r === "phone" && i ? 26 : 32);
      case R.EXPRESSIVE:
        return q.fromHueAndChroma(e.hue, r === "phone" ? i ? 36 : 48 : 40);
      case R.VIBRANT:
        return q.fromHueAndChroma(e.hue, r === "phone" ? 74 : 56);
      default:
        return super.getPrimaryPalette(t, e, i, r, a);
    }
  }
  getSecondaryPalette(t, e, i, r, a) {
    switch (t) {
      case R.NEUTRAL:
        return q.fromHueAndChroma(e.hue, r === "phone" ? yt.isBlue(e.hue) ? 6 : 4 : yt.isBlue(e.hue) ? 10 : 6);
      case R.TONAL_SPOT:
        return q.fromHueAndChroma(e.hue, 16);
      case R.EXPRESSIVE:
        return q.fromHueAndChroma(fe.getRotatedHue(e, [0, 105, 140, 204, 253, 278, 300, 333, 360], [-160, 155, -100, 96, -96, -156, -165, -160]), r === "phone" && i ? 16 : 24);
      case R.VIBRANT:
        return q.fromHueAndChroma(fe.getRotatedHue(e, [0, 38, 105, 140, 333, 360], [-14, 10, -14, 10, -14]), r === "phone" ? 56 : 36);
      default:
        return super.getSecondaryPalette(t, e, i, r, a);
    }
  }
  getTertiaryPalette(t, e, i, r, a) {
    switch (t) {
      case R.NEUTRAL:
        return q.fromHueAndChroma(fe.getRotatedHue(e, [0, 38, 105, 161, 204, 278, 333, 360], [-32, 26, 10, -39, 24, -15, -32]), r === "phone" ? 20 : 36);
      case R.TONAL_SPOT:
        return q.fromHueAndChroma(fe.getRotatedHue(e, [0, 20, 71, 161, 333, 360], [-40, 48, -32, 40, -32]), r === "phone" ? 28 : 32);
      case R.EXPRESSIVE:
        return q.fromHueAndChroma(fe.getRotatedHue(e, [0, 105, 140, 204, 253, 278, 300, 333, 360], [-165, 160, -105, 101, -101, -160, -170, -165]), 48);
      case R.VIBRANT:
        return q.fromHueAndChroma(fe.getRotatedHue(e, [0, 38, 71, 105, 140, 161, 253, 333, 360], [-72, 35, 24, -24, 62, 50, 62, -72]), 56);
      default:
        return super.getTertiaryPalette(t, e, i, r, a);
    }
  }
  static getExpressiveNeutralHue(t) {
    return fe.getRotatedHue(t, [0, 71, 124, 253, 278, 300, 360], [10, 0, 10, 0, 10, 0]);
  }
  static getExpressiveNeutralChroma(t, e, i) {
    const r = Qe.getExpressiveNeutralHue(t);
    return i === "phone" ? e ? yt.isYellow(r) ? 6 : 14 : 18 : 12;
  }
  static getVibrantNeutralHue(t) {
    return fe.getRotatedHue(t, [0, 38, 105, 140, 333, 360], [-14, 10, -14, 10, -14]);
  }
  static getVibrantNeutralChroma(t, e) {
    const i = Qe.getVibrantNeutralHue(t);
    return e === "phone" || yt.isBlue(i) ? 28 : 20;
  }
  getNeutralPalette(t, e, i, r, a) {
    switch (t) {
      case R.NEUTRAL:
        return q.fromHueAndChroma(e.hue, r === "phone" ? 1.4 : 6);
      case R.TONAL_SPOT:
        return q.fromHueAndChroma(e.hue, r === "phone" ? 5 : 10);
      case R.EXPRESSIVE:
        return q.fromHueAndChroma(Qe.getExpressiveNeutralHue(e), Qe.getExpressiveNeutralChroma(e, i, r));
      case R.VIBRANT:
        return q.fromHueAndChroma(Qe.getVibrantNeutralHue(e), Qe.getVibrantNeutralChroma(e, r));
      default:
        return super.getNeutralPalette(t, e, i, r, a);
    }
  }
  getNeutralVariantPalette(t, e, i, r, a) {
    switch (t) {
      case R.NEUTRAL:
        return q.fromHueAndChroma(e.hue, (r === "phone" ? 1.4 : 6) * 2.2);
      case R.TONAL_SPOT:
        return q.fromHueAndChroma(e.hue, (r === "phone" ? 5 : 10) * 1.7);
      case R.EXPRESSIVE:
        const o = Qe.getExpressiveNeutralHue(e), c = Qe.getExpressiveNeutralChroma(e, i, r);
        return q.fromHueAndChroma(o, c * (o >= 105 && o < 125 ? 1.6 : 2.3));
      case R.VIBRANT:
        const l = Qe.getVibrantNeutralHue(e), u = Qe.getVibrantNeutralChroma(e, r);
        return q.fromHueAndChroma(l, u * 1.29);
      default:
        return super.getNeutralVariantPalette(t, e, i, r, a);
    }
  }
  getErrorPalette(t, e, i, r, a) {
    const o = fe.getPiecewiseHue(e, [0, 3, 13, 23, 33, 43, 153, 273, 360], [12, 22, 32, 12, 22, 32, 22, 12]);
    switch (t) {
      case R.NEUTRAL:
        return q.fromHueAndChroma(o, r === "phone" ? 50 : 40);
      case R.TONAL_SPOT:
        return q.fromHueAndChroma(o, r === "phone" ? 60 : 48);
      case R.EXPRESSIVE:
        return q.fromHueAndChroma(o, r === "phone" ? 64 : 48);
      case R.VIBRANT:
        return q.fromHueAndChroma(o, r === "phone" ? 80 : 60);
      default:
        return super.getErrorPalette(t, e, i, r, a);
    }
  }
};
n(Qe, "DynamicSchemePalettesDelegateImpl2025");
let Ey = Qe;
const Lb = new Gh(), Ob = new Ey();
function $n(s) {
  return s === "2025" ? Ob : Lb;
}
n($n, "getSpec");
function zb(s) {
  s = s.replace("#", "");
  const t = s.length === 3, e = s.length === 6, i = s.length === 8;
  if (!t && !e && !i)
    throw new Error("unexpected hex " + s);
  let r = 0, a = 0, o = 0;
  return t ? (r = Wi(s.slice(0, 1).repeat(2)), a = Wi(s.slice(1, 2).repeat(2)), o = Wi(s.slice(2, 3).repeat(2))) : e ? (r = Wi(s.slice(0, 2)), a = Wi(s.slice(2, 4)), o = Wi(s.slice(4, 6))) : i && (r = Wi(s.slice(2, 4)), a = Wi(s.slice(4, 6)), o = Wi(s.slice(6, 8))), (255 << 24 | (r & 255) << 16 | (a & 255) << 8 | o & 255) >>> 0;
}
n(zb, "argbFromHex");
function Wi(s) {
  return parseInt(s, 16);
}
n(Wi, "parseIntHex");
function Fb(s, t = !1) {
  const e = s.match(/^rgba?\((\d+),\s*(\d+),\s*(\d+)(?:,\s*(\d+\.{0,1}\d*))?\)$/)?.slice(1).map((c, l) => l === 3 ? Math.round(parseFloat(c) * 255) : parseFloat(c));
  if (!e)
    throw new Error("Could not parse rgba color.");
  const [i, r, a, o] = e;
  return ((t ? 255 : o & 255) << 24 | (i & 255) << 16 | (r & 255) << 8 | a & 255) >>> 0;
}
n(Fb, "argbFromRgba");
function $i(s, t) {
  return Math.round(255 * t) << 24 | s & 16777215;
}
n($i, "addAlphaToArgb");
function Ib(s) {
  const t = gw(s), e = bw(s), i = xw(s), r = Cb(s), a = [t.toString(16), e.toString(16), i.toString(16)];
  r !== 255 && a.push(r.toString(16));
  for (const [o, c] of a.entries())
    c.length === 1 && (a[o] = "0" + c);
  return "#" + a.join("");
}
n(Ib, "hexWithAlphaFromArgb");
function Db(s) {
  $e();
  const t = document.createElement("wy-compute-styles");
  t.setAttribute("style", `color: ${s} !important;`), document.documentElement.append(t);
  const e = window.getComputedStyle(t).color;
  return t.remove(), e;
}
n(Db, "getComputedColor");
const uf = class uf extends fe {
  constructor(t, e, i, r = R.FIDELITY) {
    const a = t.chroma, o = Math.min(36, t.chroma * 16 / 36), c = o, l = Math.min(6, t.chroma * 6 / 36), u = Math.min(8, t.chroma * 8 / 36);
    super({
      sourceColorHct: t,
      variant: r,
      contrastLevel: i,
      isDark: e,
      primaryPalette: q.fromHueAndChroma(t.hue, a),
      secondaryPalette: q.fromHueAndChroma(t.hue, o),
      tertiaryPalette: q.fromHueAndChroma(Ve(t.hue - 6 * 22.5), c),
      neutralPalette: q.fromHueAndChroma(t.hue, l),
      neutralVariantPalette: q.fromHueAndChroma(t.hue, u)
    }), this.allTones = {
      //"100": 100,
      98: 98,
      // MD3
      95: 95,
      90: 90,
      80: 80,
      70: 70,
      60: 60,
      50: 50,
      40: 40,
      30: 30,
      20: 20,
      10: 10
      //"0": 0
    }, this.namedColorToneMap = {
      red: { light: 60, dark: 60 },
      "deep-orange": { light: 60, dark: 60 },
      orange: { light: 70, dark: 70 },
      amber: { light: 70, dark: 70 },
      yellow: { light: 70, dark: 80 },
      lime: { light: 70, dark: 70 },
      "light-green": { light: 70, dark: 70 },
      green: { light: 60, dark: 60 },
      teal: { light: 60, dark: 60 },
      cyan: { light: 50, dark: 60 },
      "light-blue": { light: 60, dark: 70 },
      blue: { light: 70, dark: 80 },
      indigo: { light: 60, dark: 60 },
      "deep-purple": { light: 60, dark: 70 },
      purple: { light: 60, dark: 70 },
      pink: { light: 60, dark: 70 },
      gray: { light: 50, dark: 60 }
    };
    const b = t.hue, C = t.chroma, v = Math.max(48, C), f = Math.min(v, 84), $ = 360 / 16, E = 27.4, M = -8, A = (b + 360 - E - M + $ / 2) % $ + E + M - $ / 2;
    this.errorPalette = q.fromHueAndChroma(A, 84), this.warningPalette = q.fromHueAndChroma(A + 4 * $, f), this.namedColorPalettes = {
      red: q.fromHueAndChroma(A, f),
      "deep-orange": q.fromHueAndChroma(A + 1 * $, f),
      orange: q.fromHueAndChroma(A + 2 * $, f),
      amber: q.fromHueAndChroma(A + 3 * $, f),
      yellow: q.fromHueAndChroma(A + 4 * $, f),
      lime: q.fromHueAndChroma(A + 5 * $, f),
      "light-green": q.fromHueAndChroma(A + 6 * $, f),
      green: q.fromHueAndChroma(A + 7 * $, f),
      teal: q.fromHueAndChroma(A + 8 * $, f),
      cyan: q.fromHueAndChroma(A + 9 * $, f),
      "light-blue": q.fromHueAndChroma(A + 10 * $, f),
      blue: q.fromHueAndChroma(A + 11 * $, f),
      indigo: q.fromHueAndChroma(A + 12 * $, f),
      "deep-purple": q.fromHueAndChroma(A + 13 * $, f),
      purple: q.fromHueAndChroma(A + 14 * $, f),
      pink: q.fromHueAndChroma(A + 15 * $, f),
      gray: q.fromHueAndChroma(b, 4)
    };
  }
  // Solid base colors
  get black() {
    return 0;
  }
  get white() {
    return 16777215;
  }
  // Custom transparency based surface container colors
  get surfaceLayerLowest() {
    return this.isDark ? $i(this.black, 0.5) : $i(this.white, 0.75);
  }
  get surfaceLayerLow() {
    return this.isDark ? $i(this.black, 0.25) : $i(this.white, 0.5);
  }
  get surfaceLayer() {
    return this.isDark ? $i(this.white, 0.05) : $i(this.black, 0.05);
  }
  get surfaceLayerHigh() {
    return this.isDark ? $i(this.white, 0.1) : $i(this.black, 0.075);
  }
  get surfaceLayerHighest() {
    return this.isDark ? $i(this.white, 0.15) : $i(this.black, 0.1);
  }
  // Custom Tokens
  get warning() {
    return this.warningPalette.tone(this.isDark ? 90 : 70);
  }
  get onWarning() {
    return this.warningPalette.tone(this.isDark ? 30 : 0);
  }
  get warningContainer() {
    return this.warningPalette.tone(this.isDark ? 50 : 90);
  }
  get onWarningContainer() {
    return this.warningPalette.tone(this.isDark ? 95 : 10);
  }
  get highlight() {
    return this.primaryPalette.tone(this.isDark ? 20 : 95);
  }
  get onHighlight() {
    return this.primaryPalette.tone(this.isDark ? 90 : 10);
  }
  // Named colors
  namedColor(t) {
    return this.namedColorPalettes[t].tone(this.namedColorToneMap[t][this.isDark ? "dark" : "light"]);
  }
  get red() {
    return this.namedColor(
      "red"
      /* Red */
    );
  }
  get deepOrange() {
    return this.namedColor(
      "deep-orange"
      /* DeepOrange */
    );
  }
  get orange() {
    return this.namedColor(
      "orange"
      /* Orange */
    );
  }
  get amber() {
    return this.namedColor(
      "amber"
      /* Amber */
    );
  }
  get yellow() {
    return this.namedColor(
      "yellow"
      /* Yellow */
    );
  }
  get lime() {
    return this.namedColor(
      "lime"
      /* Lime */
    );
  }
  get lightGreen() {
    return this.namedColor(
      "light-green"
      /* LightGreen */
    );
  }
  get green() {
    return this.namedColor(
      "green"
      /* Green */
    );
  }
  get teal() {
    return this.namedColor(
      "teal"
      /* Teal */
    );
  }
  get cyan() {
    return this.namedColor(
      "cyan"
      /* Cyan */
    );
  }
  get lightBlue() {
    return this.namedColor(
      "light-blue"
      /* LightBlue */
    );
  }
  get blue() {
    return this.namedColor(
      "blue"
      /* Blue */
    );
  }
  get indigo() {
    return this.namedColor(
      "indigo"
      /* Indigo */
    );
  }
  get deepPurple() {
    return this.namedColor(
      "deep-purple"
      /* DeepPurple */
    );
  }
  get purple() {
    return this.namedColor(
      "purple"
      /* Purple */
    );
  }
  get pink() {
    return this.namedColor(
      "pink"
      /* Pink */
    );
  }
  get gray() {
    return this.namedColor(
      "gray"
      /* Gray */
    );
  }
};
n(uf, "SchemeWeavy");
let Zh = uf;
function Ay(s) {
  return getComputedStyle(s).getPropertyValue("--wy-theme-color") || void 0;
}
n(Ay, "getCSSThemeColor");
function Vb(s, t) {
  let e = Ay(s);
  const i = new MutationObserver(() => {
    const o = Ay(s);
    o !== e && (e = o, t(o));
  }), r = {
    attributes: !0,
    attributeFilter: ["class", "style"]
  };
  let a = s;
  for (; a && a !== document; )
    i.observe(a, r), a = a.parentNode;
  return () => i.disconnect();
}
n(Vb, "observeCSSThemeColor");
function Ry() {
  return $e(), Array.from(document.head.querySelectorAll("meta[name='theme-color']")).filter((e) => {
    const i = e.getAttribute("media");
    return !i || window.matchMedia(i)?.matches;
  }).pop()?.getAttribute("content") || void 0;
}
n(Ry, "getMetaThemeColor");
function Hb(s) {
  $e();
  const t = Array.from(document.head.querySelectorAll("meta[name='theme-color']"));
  if (!t)
    return () => {
    };
  let e = Ry();
  const i = /* @__PURE__ */ n(() => {
    const c = Ry();
    c !== e && (e = c, s(c));
  }, "checkChangedColor"), r = new MutationObserver(i), a = {
    attributes: !0,
    attributeFilter: ["content"]
  }, o = [];
  return t.forEach((c) => {
    r.observe(c, a);
    const l = c.getAttribute("media");
    if (l) {
      const u = window.matchMedia(l);
      u.addEventListener("change", i), o.push(u);
    }
  }), () => {
    r.disconnect(), o.forEach((c) => c.removeEventListener("change", i));
  };
}
n(Hb, "observeMetaThemeColor");
const Al = [], Ub = 16;
function Bb(s, t = !1) {
  if (Al.some((l) => l.seedColor === s)) {
    const l = Al.find((u) => u.seedColor === s)?.colors;
    if (l)
      return l;
  }
  const e = [];
  t && e.push(`--wy-theme-color:${s};`);
  let i = s;
  !s.startsWith("#") && !s.startsWith("rgb") && (i = Db(s));
  const r = s.startsWith("#") ? zb(i) : Fb(i), a = yt.fromInt(r), o = {
    light: new Zh(a, !1, 0, R.FIDELITY),
    dark: new Zh(a, !0, 0, R.FIDELITY)
  }, c = {
    primary: "primary",
    onPrimary: "on-primary",
    primaryContainer: "primary-container",
    onPrimaryContainer: "on-primary-container",
    secondary: "secondary",
    onSecondary: "on-secondary",
    secondaryContainer: "secondary-container",
    onSecondaryContainer: "on-secondary-container",
    tertiary: "tertiary",
    onTertiary: "on-tertiary",
    tertiaryContainer: "tertiary-container",
    onTertiaryContainer: "on-tertiary-container",
    error: "error",
    onError: "on-error",
    errorContainer: "error-container",
    onErrorContainer: "on-error-container",
    background: "background",
    onBackground: "on-background",
    surface: "surface",
    onSurface: "on-surface",
    surfaceVariant: "surface-variant",
    onSurfaceVariant: "on-surface-variant",
    surfaceContainerLowest: "surface-container-lowest",
    surfaceContainerLow: "surface-container-low",
    surfaceContainer: "surface-container",
    surfaceContainerHigh: "surface-container-high",
    surfaceContainerHighest: "surface-container-highest",
    outline: "outline",
    outlineVariant: "outline-variant",
    shadow: "shadow",
    scrim: "scrim",
    // Custom surface layers
    surfaceLayerLowest: "surface-layer-lowest",
    surfaceLayerLow: "surface-layer-low",
    surfaceLayer: "surface-layer",
    surfaceLayerHigh: "surface-layer-high",
    surfaceLayerHighest: "surface-layer-highest",
    // Custom tokens
    warning: "warning",
    onWarning: "on-warning",
    warningContainer: "warning-container",
    onWarningContainer: "on-warning-container",
    highlight: "highlight",
    onHighlight: "on-highlight",
    // Named colors
    red: "red",
    deepOrange: "deep-orange",
    orange: "orange",
    amber: "amber",
    yellow: "yellow",
    lime: "lime",
    lightGreen: "light-green",
    green: "green",
    teal: "teal",
    cyan: "cyan",
    lightBlue: "light-blue",
    blue: "blue",
    indigo: "indigo",
    deepPurple: "deep-purple",
    purple: "purple",
    pink: "pink",
    gray: "gray"
  };
  for (const l in o) {
    const u = o[l];
    for (const b in c) {
      const C = c[b], v = Ib(u[b]);
      e.push(`--wy-${C}-${l}:${v};`);
    }
  }
  return Al.unshift({ seedColor: s, colors: e }), Al.length = Math.min(Al.length, Ub), e;
}
n(Bb, "generateThemeColors");
const Nb = /* @__PURE__ */ n((s) => {
  if ($e(), d0)
    document.adoptedStyleSheets = s.map((t) => t instanceof CSSStyleSheet ? t : t.styleSheet);
  else
    for (const t of s) {
      const e = document.createElement("style"), i = global.litNonce;
      i !== void 0 && e.setAttribute("nonce", i), e.textContent = t.cssText, (document.head || document.documentElement).appendChild(e);
    }
}, "adoptGlobalStyles"), Kt = j`@property --wy-color-scheme{syntax: \"normal | light | dark\"; inherits: true; initial-value: normal;}:is(.wy-light,[part~=wy-light]) :where(wy-chat),:is(.wy-light,[part~=wy-light]) :where(wy-comments),:is(.wy-light,[part~=wy-light]) :where(wy-component),:is(.wy-light,[part~=wy-light]) :where(wy-copilot),:is(.wy-light,[part~=wy-light]) :where(wy-feed),:is(.wy-light,[part~=wy-light]) :where(wy-files),:is(.wy-light,[part~=wy-light]) :where(wy-messenger-badge),:is(.wy-light,[part~=wy-light]) :where(wy-messenger-button),:is(.wy-light,[part~=wy-light]) :where(wy-messenger-conversation),:is(.wy-light,[part~=wy-light]) :where(wy-messenger-new),:is(.wy-light,[part~=wy-light]) :where(wy-messenger),:is(.wy-light,[part~=wy-light]) :where(wy-notification-badge),:is(.wy-light,[part~=wy-light]) :where(wy-notification-button),:is(.wy-light,[part~=wy-light]) :where(wy-notification-toasts),:is(.wy-light,[part~=wy-light]) :where(wy-notifications),:is(.wy-light,[part~=wy-light]) :where(wy-post-editor),:is(.wy-light,[part~=wy-light]) :where(wy-posts),:host(:is(.wy-light,[part~=wy-light])),:host :is(.wy-light,[part~=wy-light]),::slotted(:is(.wy-light,[part~=wy-light])){color-scheme:light;--wy-primary: var(--wy-primary-light, #2f628c);--wy-on-primary: var(--wy-on-primary-light, #ffffff);--wy-primary-container: var(--wy-primary-container-light, #cee5ff);--wy-on-primary-container: var(--wy-on-primary-container-light, #0d4a73);--wy-secondary: var(--wy-secondary-light, #51606f);--wy-on-secondary: var(--wy-on-secondary-light, #ffffff);--wy-secondary-container: var(--wy-secondary-container-light, #d5e4f7);--wy-on-secondary-container: var(--wy-on-secondary-container-light, #3a4857);--wy-tertiary: var(--wy-tertiary-light, #645f41);--wy-on-tertiary: var(--wy-on-tertiary-light, #ffffff);--wy-tertiary-container: var(--wy-tertiary-container-light, #ebe3bd);--wy-on-tertiary-container: var(--wy-on-tertiary-container-light, #4c472b);--wy-error: var(--wy-error-light, #ba1821);--wy-on-error: var(--wy-on-error-light, #ffffff);--wy-error-container: var(--wy-error-container-light, #ffdad6);--wy-on-error-container: var(--wy-on-error-container-light, #930012);--wy-background: var(--wy-background-light, #f7f9ff);--wy-on-background: var(--wy-on-background-light, #181c20);--wy-surface: var(--wy-surface-light, #f7f9ff);--wy-on-surface: var(--wy-on-surface-light, #181c20);--wy-surface-variant: var(--wy-surface-variant-light, #dee3eb);--wy-on-surface-variant: var(--wy-on-surface-variant-light, #42474e);--wy-outline: var(--wy-outline-light, #72777f);--wy-outline-variant: var(--wy-outline-variant-light, #c2c7cf);--wy-surface-container-highest: var(--wy-surface-container-highest-light, #e0e2e8);--wy-surface-container-high: var(--wy-surface-container-high-light, #e6e8ee);--wy-surface-container: var(--wy-surface-container-light, #eceef3);--wy-surface-container-low: var(--wy-surface-container-low-light, #f1f3f9);--wy-surface-container-lowest: var(--wy-surface-container-lowest-light, #ffffff);--wy-surface-layer-highest: var(--wy-surface-layer-highest-light, rgba(0, 0, 0, .1019607843));--wy-surface-layer-high: var(--wy-surface-layer-high-light, rgba(0, 0, 0, .0745098039));--wy-surface-layer: var(--wy-surface-layer-light, rgba(0, 0, 0, .0509803922));--wy-surface-layer-low: var(--wy-surface-layer-low-light, rgba(255, 255, 255, .5019607843));--wy-surface-layer-lowest: var(--wy-surface-layer-lowest-light, rgba(255, 255, 255, .7490196078));--wy-warning: var(--wy-warning-light, #adb140);--wy-on-warning: var(--wy-on-warning-light, #000000);--wy-warning-container: var(--wy-warning-container-light, #e5e972);--wy-on-warning-container: var(--wy-on-warning-container-light, #1c1d00);--wy-highlight: var(--wy-highlight-light, #e8f2ff);--wy-on-highlight: var(--wy-on-highlight-light, #001d32);--wy-link: var(--wy-primary-light, #2f628c);--wy-presence-active: var(--wy-green-light, #00a38d);--wy-shade: var(--wy-shade-light, rgba(0, 0, 0, .3));--wy-shade-invert: var(--wy-shade-invert-light, rgba(255, 255, 255, .15));--wy-shade-opaque: var(--wy-shade-opaque-light, white);--wy-blue: var(--wy-blue-light, #8aa9fc);--wy-indigo: var(--wy-indigo-light, #9285de);--wy-purple: var(--wy-purple-light, #c675b6);--wy-pink: var(--wy-pink-light, #d57193);--wy-red: var(--wy-red-light, #db726b);--wy-orange: var(--wy-orange-light, #e79b3a);--wy-yellow: var(--wy-yellow-light, #adb140);--wy-green: var(--wy-green-light, #00a38d);--wy-teal: var(--wy-teal-light, #00a0a9);--wy-cyan: var(--wy-cyan-light, #0081a2);--wy-gray: var(--wy-gray-light, #76777a);--wy-code-text: var(--wy-code-text-light, #3b3b3b);--wy-code-variable: var(--wy-code-variable-light, #001080);--wy-code-operator: var(--wy-code-operator-light, #000000);--wy-code-prolog: var(--wy-code-prolog-light, #000080);--wy-code-comment: var(--wy-code-comment-light, #008000);--wy-code-builtin: var(--wy-code-builtin-light, #0070C1);--wy-code-number: var(--wy-code-number-light, #098658);--wy-code-inserted: var(--wy-code-inserted-light, #098658);--wy-code-constant: var(--wy-code-constant-light, #811F3F);--wy-code-hexcode: var(--wy-code-hexcode-light, #811F3F);--wy-code-regex: var(--wy-code-regex-light, #811F3F);--wy-code-char: var(--wy-code-char-light, #811F3F);--wy-code-tag: var(--wy-code-tag-light, #800000);--wy-code-attr-name: var(--wy-code-attr-name-light, #E50000);--wy-code-selector: var(--wy-code-selector-light, #E50000);--wy-code-property: var(--wy-code-property-light, #E50000);--wy-code-deleted: var(--wy-code-deleted-light, #A31515);--wy-code-string: var(--wy-code-string-light, #A31515);--wy-code-changed: var(--wy-code-changed-light, #0451A5);--wy-code-punctuation: var(--wy-code-punctuation-light, #000000);--wy-code-function: var(--wy-code-function-light, #0000FF);--wy-code-keyword: var(--wy-code-keyword-light, #AF00DB);--wy-code-class-name: var(--wy-code-class-name-light, #267F99)}:is(.wy-dark,[part~=wy-dark]):not(.wy-light,[part~=wy-light]) :where(wy-chat),:is(.wy-dark,[part~=wy-dark]):not(.wy-light,[part~=wy-light]) :where(wy-comments),:is(.wy-dark,[part~=wy-dark]):not(.wy-light,[part~=wy-light]) :where(wy-component),:is(.wy-dark,[part~=wy-dark]):not(.wy-light,[part~=wy-light]) :where(wy-copilot),:is(.wy-dark,[part~=wy-dark]):not(.wy-light,[part~=wy-light]) :where(wy-feed),:is(.wy-dark,[part~=wy-dark]):not(.wy-light,[part~=wy-light]) :where(wy-files),:is(.wy-dark,[part~=wy-dark]):not(.wy-light,[part~=wy-light]) :where(wy-messenger-badge),:is(.wy-dark,[part~=wy-dark]):not(.wy-light,[part~=wy-light]) :where(wy-messenger-button),:is(.wy-dark,[part~=wy-dark]):not(.wy-light,[part~=wy-light]) :where(wy-messenger-conversation),:is(.wy-dark,[part~=wy-dark]):not(.wy-light,[part~=wy-light]) :where(wy-messenger-new),:is(.wy-dark,[part~=wy-dark]):not(.wy-light,[part~=wy-light]) :where(wy-messenger),:is(.wy-dark,[part~=wy-dark]):not(.wy-light,[part~=wy-light]) :where(wy-notification-badge),:is(.wy-dark,[part~=wy-dark]):not(.wy-light,[part~=wy-light]) :where(wy-notification-button),:is(.wy-dark,[part~=wy-dark]):not(.wy-light,[part~=wy-light]) :where(wy-notification-toasts),:is(.wy-dark,[part~=wy-dark]):not(.wy-light,[part~=wy-light]) :where(wy-notifications),:is(.wy-dark,[part~=wy-dark]):not(.wy-light,[part~=wy-light]) :where(wy-post-editor),:is(.wy-dark,[part~=wy-dark]):not(.wy-light,[part~=wy-light]) :where(wy-posts),:host(:is(.wy-dark,[part~=wy-dark]):not(.wy-light,[part~=wy-light])),:host :is(.wy-dark,[part~=wy-dark]):not(.wy-light,[part~=wy-light]),::slotted(:is(.wy-dark,[part~=wy-dark]):not(.wy-light,[part~=wy-light])){color-scheme:dark;--wy-primary: var(--wy-primary-dark, #9bcbfa);--wy-on-primary: var(--wy-on-primary-dark, #003353);--wy-primary-container: var(--wy-primary-container-dark, #0d4a73);--wy-on-primary-container: var(--wy-on-primary-container-dark, #cee5ff);--wy-secondary: var(--wy-secondary-dark, #b9c8da);--wy-on-secondary: var(--wy-on-secondary-dark, #233240);--wy-secondary-container: var(--wy-secondary-container-dark, #3a4857);--wy-on-secondary-container: var(--wy-on-secondary-container-dark, #d5e4f7);--wy-tertiary: var(--wy-tertiary-dark, #cfc7a2);--wy-on-tertiary: var(--wy-on-tertiary-dark, #353117);--wy-tertiary-container: var(--wy-tertiary-container-dark, #4c472b);--wy-on-tertiary-container: var(--wy-on-tertiary-container-dark, #ebe3bd);--wy-error: var(--wy-error-dark, #ffb3ad);--wy-on-error: var(--wy-on-error-dark, #680009);--wy-error-container: var(--wy-error-container-dark, #930012);--wy-on-error-container: var(--wy-on-error-container-dark, #ffdad6);--wy-background: var(--wy-background-dark, #101418);--wy-on-background: var(--wy-on-background-dark, #e0e2e8);--wy-surface: var(--wy-surface-dark, #101418);--wy-on-surface: var(--wy-on-surface-dark, #e0e2e8);--wy-surface-variant: var(--wy-surface-variant-dark, #42474e);--wy-on-surface-variant: var(--wy-on-surface-variant-dark, #c2c7cf);--wy-outline: var(--wy-outline-dark, #8c9198);--wy-outline-variant: var(--wy-outline-variant-dark, #42474e);--wy-surface-container-highest: var(--wy-surface-container-highest-dark, #323539);--wy-surface-container-high: var(--wy-surface-container-high-dark, #272a2f);--wy-surface-container: var(--wy-surface-container-dark, #1c2024);--wy-surface-container-low: var(--wy-surface-container-low-dark, #181c20);--wy-surface-container-lowest: var(--wy-surface-container-lowest-dark, #0b0f12);--wy-surface-layer-highest: var(--wy-surface-layer-highest-dark, rgba(255, 255, 255, .1490196078));--wy-surface-layer-high: var(--wy-surface-layer-high-dark, rgba(255, 255, 255, .1019607843));--wy-surface-layer: var(--wy-surface-layer-dark, rgba(255, 255, 255, .0509803922));--wy-surface-layer-low: var(--wy-surface-layer-low-dark, rgba(0, 0, 0, .2509803922));--wy-surface-layer-lowest: var(--wy-surface-layer-lowest-dark, rgba(0, 0, 0, .5019607843));--wy-warning: var(--wy-warning-dark, #e5e972);--wy-on-warning: var(--wy-on-warning-dark, #484a00);--wy-warning-container: var(--wy-warning-container-dark, #787c07);--wy-on-warning-container: var(--wy-on-warning-container-dark, #f4f87e);--wy-highlight: var(--wy-highlight-dark, #003353);--wy-on-highlight: var(--wy-on-highlight-dark, #cee5ff);--wy-link: var(--wy-primary-dark, #9bcbfa);--wy-presence-active: var(--wy-green-dark, #00a38d);--wy-shade: var(--wy-shade-dark, rgba(0, 0, 0, .5));--wy-shade-invert: var(--wy-shade-invert-dark, rgba(255, 255, 255, .25));--wy-shade-opaque: var(--wy-shade-opaque-dark, grey);--wy-blue: var(--wy-blue-dark, #b2c5ff);--wy-indigo: var(--wy-indigo-dark, #9285de);--wy-purple: var(--wy-purple-dark, #e48fd2);--wy-pink: var(--wy-pink-dark, #f58bad);--wy-red: var(--wy-red-dark, #db726b);--wy-orange: var(--wy-orange-dark, #e79b3a);--wy-yellow: var(--wy-yellow-dark, #c9cd59);--wy-green: var(--wy-green-dark, #00a38d);--wy-teal: var(--wy-teal-dark, #00a0a9);--wy-cyan: var(--wy-cyan-dark, #009dc4);--wy-gray: var(--wy-gray-dark, #909194);--wy-code-text: var(--wy-code-text-dark, #cccccc);--wy-code-variable: var(--wy-code-variable-dark, #9CDCFE);--wy-code-operator: var(--wy-code-operator-dark, #d4d4d4);--wy-code-prolog: var(--wy-code-prolog-dark, #569CD6);--wy-code-comment: var(--wy-code-comment-dark, #6a9955);--wy-code-builtin: var(--wy-code-builtin-dark, #4fc1ff);--wy-code-number: var(--wy-code-number-dark, #b5cea8);--wy-code-inserted: var(--wy-code-inserted-dark, #b5cea8);--wy-code-constant: var(--wy-code-constant-dark, #646695);--wy-code-hexcode: var(--wy-code-hexcode-dark, #646695);--wy-code-regex: var(--wy-code-regex-dark, #d16969);--wy-code-char: var(--wy-code-char-dark, #d16969);--wy-code-tag: var(--wy-code-tag-dark, #569cd6);--wy-code-attr-name: var(--wy-code-attr-name-dark, #9cdcfe);--wy-code-selector: var(--wy-code-selector-dark, #9cdcfe);--wy-code-property: var(--wy-code-property-dark, #9cdcfe);--wy-code-deleted: var(--wy-code-deleted-dark, #ce9178);--wy-code-string: var(--wy-code-string-dark, #ce9178);--wy-code-changed: var(--wy-code-changed-dark, #569cd6);--wy-code-punctuation: var(--wy-code-punctuation-dark, #D4D4D4);--wy-code-function: var(--wy-code-function-dark, #569cd6);--wy-code-keyword: var(--wy-code-keyword-dark, #C586C0);--wy-code-class-name: var(--wy-code-class-name-dark, #4ec9b0)}@container style(--wy-color-scheme: dark){html :where(wy-chat),html :where(wy-comments),html :where(wy-component),html :where(wy-copilot),html :where(wy-feed),html :where(wy-files),html :where(wy-messenger-badge),html :where(wy-messenger-button),html :where(wy-messenger-conversation),html :where(wy-messenger-new),html :where(wy-messenger),html :where(wy-notification-badge),html :where(wy-notification-button),html :where(wy-notification-toasts),html :where(wy-notifications),html :where(wy-post-editor),html :where(wy-posts){color-scheme:dark;--wy-primary: var(--wy-primary-dark, #9bcbfa);--wy-on-primary: var(--wy-on-primary-dark, #003353);--wy-primary-container: var(--wy-primary-container-dark, #0d4a73);--wy-on-primary-container: var(--wy-on-primary-container-dark, #cee5ff);--wy-secondary: var(--wy-secondary-dark, #b9c8da);--wy-on-secondary: var(--wy-on-secondary-dark, #233240);--wy-secondary-container: var(--wy-secondary-container-dark, #3a4857);--wy-on-secondary-container: var(--wy-on-secondary-container-dark, #d5e4f7);--wy-tertiary: var(--wy-tertiary-dark, #cfc7a2);--wy-on-tertiary: var(--wy-on-tertiary-dark, #353117);--wy-tertiary-container: var(--wy-tertiary-container-dark, #4c472b);--wy-on-tertiary-container: var(--wy-on-tertiary-container-dark, #ebe3bd);--wy-error: var(--wy-error-dark, #ffb3ad);--wy-on-error: var(--wy-on-error-dark, #680009);--wy-error-container: var(--wy-error-container-dark, #930012);--wy-on-error-container: var(--wy-on-error-container-dark, #ffdad6);--wy-background: var(--wy-background-dark, #101418);--wy-on-background: var(--wy-on-background-dark, #e0e2e8);--wy-surface: var(--wy-surface-dark, #101418);--wy-on-surface: var(--wy-on-surface-dark, #e0e2e8);--wy-surface-variant: var(--wy-surface-variant-dark, #42474e);--wy-on-surface-variant: var(--wy-on-surface-variant-dark, #c2c7cf);--wy-outline: var(--wy-outline-dark, #8c9198);--wy-outline-variant: var(--wy-outline-variant-dark, #42474e);--wy-surface-container-highest: var(--wy-surface-container-highest-dark, #323539);--wy-surface-container-high: var(--wy-surface-container-high-dark, #272a2f);--wy-surface-container: var(--wy-surface-container-dark, #1c2024);--wy-surface-container-low: var(--wy-surface-container-low-dark, #181c20);--wy-surface-container-lowest: var(--wy-surface-container-lowest-dark, #0b0f12);--wy-surface-layer-highest: var(--wy-surface-layer-highest-dark, rgba(255, 255, 255, .1490196078));--wy-surface-layer-high: var(--wy-surface-layer-high-dark, rgba(255, 255, 255, .1019607843));--wy-surface-layer: var(--wy-surface-layer-dark, rgba(255, 255, 255, .0509803922));--wy-surface-layer-low: var(--wy-surface-layer-low-dark, rgba(0, 0, 0, .2509803922));--wy-surface-layer-lowest: var(--wy-surface-layer-lowest-dark, rgba(0, 0, 0, .5019607843));--wy-warning: var(--wy-warning-dark, #e5e972);--wy-on-warning: var(--wy-on-warning-dark, #484a00);--wy-warning-container: var(--wy-warning-container-dark, #787c07);--wy-on-warning-container: var(--wy-on-warning-container-dark, #f4f87e);--wy-highlight: var(--wy-highlight-dark, #003353);--wy-on-highlight: var(--wy-on-highlight-dark, #cee5ff);--wy-link: var(--wy-primary-dark, #9bcbfa);--wy-presence-active: var(--wy-green-dark, #00a38d);--wy-shade: var(--wy-shade-dark, rgba(0, 0, 0, .5));--wy-shade-invert: var(--wy-shade-invert-dark, rgba(255, 255, 255, .25));--wy-shade-opaque: var(--wy-shade-opaque-dark, grey);--wy-blue: var(--wy-blue-dark, #b2c5ff);--wy-indigo: var(--wy-indigo-dark, #9285de);--wy-purple: var(--wy-purple-dark, #e48fd2);--wy-pink: var(--wy-pink-dark, #f58bad);--wy-red: var(--wy-red-dark, #db726b);--wy-orange: var(--wy-orange-dark, #e79b3a);--wy-yellow: var(--wy-yellow-dark, #c9cd59);--wy-green: var(--wy-green-dark, #00a38d);--wy-teal: var(--wy-teal-dark, #00a0a9);--wy-cyan: var(--wy-cyan-dark, #009dc4);--wy-gray: var(--wy-gray-dark, #909194);--wy-code-text: var(--wy-code-text-dark, #cccccc);--wy-code-variable: var(--wy-code-variable-dark, #9CDCFE);--wy-code-operator: var(--wy-code-operator-dark, #d4d4d4);--wy-code-prolog: var(--wy-code-prolog-dark, #569CD6);--wy-code-comment: var(--wy-code-comment-dark, #6a9955);--wy-code-builtin: var(--wy-code-builtin-dark, #4fc1ff);--wy-code-number: var(--wy-code-number-dark, #b5cea8);--wy-code-inserted: var(--wy-code-inserted-dark, #b5cea8);--wy-code-constant: var(--wy-code-constant-dark, #646695);--wy-code-hexcode: var(--wy-code-hexcode-dark, #646695);--wy-code-regex: var(--wy-code-regex-dark, #d16969);--wy-code-char: var(--wy-code-char-dark, #d16969);--wy-code-tag: var(--wy-code-tag-dark, #569cd6);--wy-code-attr-name: var(--wy-code-attr-name-dark, #9cdcfe);--wy-code-selector: var(--wy-code-selector-dark, #9cdcfe);--wy-code-property: var(--wy-code-property-dark, #9cdcfe);--wy-code-deleted: var(--wy-code-deleted-dark, #ce9178);--wy-code-string: var(--wy-code-string-dark, #ce9178);--wy-code-changed: var(--wy-code-changed-dark, #569cd6);--wy-code-punctuation: var(--wy-code-punctuation-dark, #D4D4D4);--wy-code-function: var(--wy-code-function-dark, #569cd6);--wy-code-keyword: var(--wy-code-keyword-dark, #C586C0);--wy-code-class-name: var(--wy-code-class-name-dark, #4ec9b0)}}`, qb = j`@property --wy-component-color{syntax: \"<color>\"; inherits: true; initial-value: currentColor;}@property --wy-component-background-color{syntax: \"<color>\"; inherits: true; initial-value: transparent;}`, Wb = /* @__PURE__ */ n((s) => {
  var t;
  return t = class extends s {
    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    constructor(...i) {
      super(...i), Nb([qb, Kt]);
    }
  }, n(t, "WeavyStyles"), t;
}, "WeavyStylesMixin"), jb = /* @__PURE__ */ n((s) => {
  var t, e;
  return e = class extends s {
    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    constructor(...a) {
      super(...a);
      T(this, t);
      this.version = Qt.version, this.whenUrl().then(() => {
        this.isDestroyed || this.checkVersion();
      });
    }
    /**
     * Checks the version of the Weavy Context against the Weavy Environment version.
     *
     */
    async checkVersion() {
      return p(this, t) || P(this, t, (async () => {
        await this.whenUrl(), this.networkStateIsPending = !0;
        let a;
        try {
          if (a = await fetch(new URL("/version", this.url), await this.fetchOptions({}, !1)), !a.ok)
            throw new Error(`Could not verify environment version. ${a.status} ${a.statusText}`, {
              cause: a.status
            });
          this.networkStateIsPending = !1, this.serverState = "ok";
        } catch (c) {
          return this.networkStateIsPending = !1, this.serverState = "unreachable", console.warn("Could not check Weavy version: " + c.toString()), {
            client: {
              name: Qt.sourceName,
              version: this.version
            },
            environment: {
              name: this.url?.hostname,
              version: void 0
            }
          };
        }
        const o = await a.text();
        if (!this.version || !o || this.version !== o)
          try {
            const c = this.version.split(".").slice(0, 2), l = o.split(".").slice(0, 2);
            if (c[0] !== l[0])
              throw new Error("Major version mismatch", {
                cause: "major"
              });
            if (c[1] !== l[1])
              throw new Error("Minor version mismatch", {
                cause: "minor"
              });
            if (c[2] !== l[2])
              throw new Error("Patch version mismatch", {
                cause: "patch"
              });
          } catch (c) {
            throw new Error(
              `Weavy version mismatch! ${Qt.sourceName}@${this.version} ≠ ${this.url?.hostname}@${o} - This will likely cause errors!`,
              {
                cause: {
                  mismatch: c.cause,
                  client: {
                    name: Qt.sourceName,
                    version: this.version
                  },
                  environment: {
                    name: this.url?.hostname,
                    version: o
                  }
                }
              }
            );
          } finally {
            P(this, t, void 0);
          }
        else
          console.info(`Weavy version ${this.version} ☑️`);
        return {
          client: {
            name: Qt.sourceName,
            version: this.version
          },
          environment: {
            name: this.url?.hostname,
            version: o
          }
        };
      })()), await p(this, t);
    }
  }, t = new WeakMap(), n(e, "WeavyVersion"), e;
}, "WeavyVersionMixin");
var ya;
let Kv = (ya = class extends Event {
  constructor(t, e, i, r) {
    super("context-request", { bubbles: !0, composed: !0 }), this.context = t, this.contextTarget = e, this.callback = i, this.subscribe = r ?? !1;
  }
}, n(ya, "s"), ya);
var wa;
let He = (wa = class {
  constructor(t, e, i, r) {
    if (this.subscribe = !1, this.provided = !1, this.value = void 0, this.t = (a, o) => {
      this.unsubscribe && (this.unsubscribe !== o && (this.provided = !1, this.unsubscribe()), this.subscribe || this.unsubscribe()), this.value = a, this.host.requestUpdate(), this.provided && !this.subscribe || (this.provided = !0, this.callback && this.callback(a, o)), this.unsubscribe = o;
    }, this.host = t, e.context !== void 0) {
      const a = e;
      this.context = a.context, this.callback = a.callback, this.subscribe = a.subscribe ?? !1;
    } else this.context = e, this.callback = i, this.subscribe = r ?? !1;
    this.host.addController(this);
  }
  hostConnected() {
    this.dispatchRequest();
  }
  hostDisconnected() {
    this.unsubscribe && (this.unsubscribe(), this.unsubscribe = void 0);
  }
  dispatchRequest() {
    this.host.dispatchEvent(new Kv(this.context, this.host, this.t, this.subscribe));
  }
}, n(wa, "s"), wa);
var fa;
let Kb = (fa = class {
  get value() {
    return this.o;
  }
  set value(t) {
    this.setValue(t);
  }
  setValue(t, e = !1) {
    const i = e || !Object.is(t, this.o);
    this.o = t, i && this.updateObservers();
  }
  constructor(t) {
    this.subscriptions = /* @__PURE__ */ new Map(), this.updateObservers = () => {
      for (const [e, { disposer: i }] of this.subscriptions) e(this.o, i);
    }, t !== void 0 && (this.value = t);
  }
  addCallback(t, e, i) {
    if (!i) return void t(this.value);
    this.subscriptions.has(t) || this.subscriptions.set(t, { disposer: /* @__PURE__ */ n(() => {
      this.subscriptions.delete(t);
    }, "disposer"), consumerHost: e });
    const { disposer: r } = this.subscriptions.get(t);
    t(this.value, r);
  }
  clearCallbacks() {
    this.subscriptions.clear();
  }
}, n(fa, "s"), fa);
var ma;
let Qb = (ma = class extends Event {
  constructor(t, e) {
    super("context-provider", { bubbles: !0, composed: !0 }), this.context = t, this.contextTarget = e;
  }
}, n(ma, "e"), ma);
var va;
let Ty = (va = class extends Kb {
  constructor(t, e, i) {
    super(e.context !== void 0 ? e.initialValue : i), this.onContextRequest = (r) => {
      if (r.context !== this.context) return;
      const a = r.contextTarget ?? r.composedPath()[0];
      a !== this.host && (r.stopPropagation(), this.addCallback(r.callback, a, r.subscribe));
    }, this.onProviderRequest = (r) => {
      if (r.context !== this.context || (r.contextTarget ?? r.composedPath()[0]) === this.host) return;
      const a = /* @__PURE__ */ new Set();
      for (const [o, { consumerHost: c }] of this.subscriptions) a.has(o) || (a.add(o), c.dispatchEvent(new Kv(this.context, c, o, !0)));
      r.stopPropagation();
    }, this.host = t, e.context !== void 0 ? this.context = e.context : this.context = e, this.attachListeners(), this.host.addController?.(this);
  }
  attachListeners() {
    this.host.addEventListener("context-request", this.onContextRequest), this.host.addEventListener("context-provider", this.onProviderRequest);
  }
  hostConnected() {
    this.host.dispatchEvent(new Qb(this.context, this.host));
  }
}, n(va, "i"), va);
function Ui({ context: s }) {
  return (t, e) => {
    const i = /* @__PURE__ */ new WeakMap();
    if (typeof e == "object") return { get() {
      return t.get.call(this);
    }, set(r) {
      return i.get(this).setValue(r), t.set.call(this, r);
    }, init(r) {
      return i.set(this, new Ty(this, { context: s, initialValue: r })), r;
    } };
    {
      t.constructor.addInitializer(((o) => {
        i.set(o, new Ty(o, { context: s }));
      }));
      const r = Object.getOwnPropertyDescriptor(t, e);
      let a;
      if (r === void 0) {
        const o = /* @__PURE__ */ new WeakMap();
        a = { get() {
          return o.get(this);
        }, set(c) {
          i.get(this).setValue(c), o.set(this, c);
        }, configurable: !0, enumerable: !0 };
      } else {
        const o = r.set;
        a = { ...r, set(c) {
          i.get(this).setValue(c), o?.call(this, c);
        } };
      }
      return void Object.defineProperty(t, e, a);
    }
  };
}
n(Ui, "e$4");
function he({ context: s, subscribe: t }) {
  return (e, i) => {
    typeof i == "object" ? i.addInitializer((function() {
      new He(this, { context: s, callback: /* @__PURE__ */ n((r) => {
        e.set.call(this, r);
      }, "callback"), subscribe: t });
    })) : e.constructor.addInitializer(((r) => {
      new He(r, { context: s, callback: /* @__PURE__ */ n((a) => {
        r[i] = a;
      }, "callback"), subscribe: t });
    }));
  };
}
n(he, "c$2");
const pf = class pf extends Event {
  /**
   *
   * @param context the context key to request
   * @param contextTarget the original context target of the requester
   * @param callback the callback that should be invoked when the context with the specified key is available
   * @param subscribe when, true indicates we want to subscribe to future updates
   */
  constructor(t, e, i, r) {
    super("context-request", { bubbles: !0, composed: !0 }), this.context = t, this.contextTarget = e, this.callback = i, this.subscribe = r ?? !1;
  }
};
n(pf, "ContextRequestEvent");
let Ly = pf;
const yf = class yf extends Event {
  /**
   *
   * @param context the context which this provider can provide
   * @param contextTarget the original context target of the provider
   */
  constructor(t, e) {
    super("context-provider", { bubbles: !0, composed: !0 }), this.context = t, this.contextTarget = e;
  }
};
n(yf, "ContextProviderEvent");
let Oy = yf;
const wf = class wf extends Ty {
  constructor(t, e, i) {
    e.context !== void 0 ? super(t, e) : super(t, e, i), e.context !== void 0 ? this._context = e.context : this._context = e, this.isAttached = !0, this.host instanceof nt || this.dispatchWhenConnected();
  }
  dispatchWhenConnected() {
    this.isAttached && (this.host.isConnected ? this.host.dispatchEvent(new Oy(this._context, this.host)) : requestAnimationFrame(() => this.dispatchWhenConnected()));
  }
  detachListeners() {
    this.isAttached = !1, this.host.removeEventListener("context-request", this.onContextRequest), this.host.removeEventListener("context-provider", this.onProviderRequest);
  }
};
n(wf, "WyContextProvider");
let Yh = wf;
const Xt = Symbol.for("weavy-client");
function Gb(s, t) {
  return $e(), new Yh(s, { context: Xt, initialValue: t });
}
n(Gb, "createWeavyContextProvider");
const Rl = dw() ? Gb(document.documentElement) : void 0, Zb = /* @__PURE__ */ n((s) => {
  var t, e;
  return e = class extends s {
    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    constructor(...a) {
      super(...a);
      // CONTEXT PROVIDER
      // SHOULD BE APPLIED LAST TO ENSURE EVERYTHING IS SET BEFORE CONTEXT GETS PROVIDED
      T(this, t);
      this.host !== document.documentElement ? (Rl?.detachListeners(), P(this, t, new Yh(this.host, {
        context: Xt,
        initialValue: this
      }))) : Rl?.setValue(this);
    }
    updateContext() {
      this.host !== document.documentElement ? p(this, t)?.updateObservers() : Rl?.updateObservers();
    }
    destroy() {
      super.destroy(), this.host !== document.documentElement ? p(this, t)?.detachListeners() : Rl?.value === this && Rl?.setValue(void 0);
    }
  }, t = new WeakMap(), n(e, "WeavyContextProvider"), e;
}, "WeavyContextProviderMixin");
var _d, Pd, Xi, ga;
const re = class re {
  // CONSTRUCTOR
  constructor(t) {
    // Promises
    // whenUrl
    T(this, _d);
    T(this, Pd);
    // Reactive options
    T(this, Xi);
    T(this, ga);
    this.weavySid = Bl(), this.weavyId = `${re.sourceName}#${this.weavySid}`, this.cloudFilePickerUrl = re.defaults.cloudFilePickerUrl, this.configurationTimeout = re.defaults.configurationTimeout, this.disableEnvironmentImports = re.defaults.disableEnvironmentImports, this.gcTime = re.defaults.gcTime, this.scrollBehavior = re.defaults.scrollBehavior, this.staleTime = re.defaults.staleTime, this.tokenFactoryRetryDelay = re.defaults.tokenFactoryRetryDelay, this.tokenFactoryTimeout = re.defaults.tokenFactoryTimeout, P(this, Pd, new Promise((i) => {
      P(this, _d, i);
    })), P(this, ga, !1), console.info(`${re.sourceName}@${re.version} (${re.sourceFormat}) #${this.weavySid}`), $e(), this.host = document.documentElement;
    const e = {};
    for (const i in t) {
      const r = i;
      t[r] !== void 0 && Object.assign(e, { [r]: t[r] });
    }
    e?.host && (this.host = e.host, delete e.host), e && Object.assign(this, e);
  }
  async whenUrl() {
    await p(this, Pd);
  }
  /**
   * The URL to the weavy environment.
   */
  get url() {
    return p(this, Xi);
  }
  set url(t) {
    var e;
    if (this.isDestroyed)
      throw new Ut();
    try {
      if (typeof t == "string")
        t && P(this, Xi, new URL(t, window.location.toString()));
      else if (t instanceof URL)
        P(this, Xi, t || void 0);
      else if (t == null)
        P(this, Xi, void 0);
      else
        throw new Error();
    } catch {
      throw new TypeError("Invalid url");
    }
    t && !this.disableEnvironmentImports && globalThis.WEAVY_IMPORT_URL === void 0 && (globalThis.WEAVY_IMPORT_URL = new URL(
      "./uikit-web/",
      t
    ).href), p(this, Xi) && ((e = p(this, _d)) == null || e.call(this, p(this, Xi)));
  }
  /**
   * Prefix to use for caches.
   */
  get cachePrefix() {
    return `${re.version}:${this.url}`;
  }
  // eslint-disable-next-line @typescript-eslint/require-await
  async reset() {
    console.info(this.weavyId, "is reset");
  }
  get isDestroyed() {
    return p(this, ga);
  }
  destroy() {
    P(this, ga, !0), console.info(this.weavyId, "was destroyed");
  }
};
_d = new WeakMap(), Pd = new WeakMap(), Xi = new WeakMap(), ga = new WeakMap(), n(re, "WeavyClient"), re.version = "31.0.0", re.sourceName = "@weavy/uikit-web", re.sourceFormat = s0 ?? "typescript", re.defaults = {
  // StrictWeavyOptions
  cloudFilePickerUrl: "https://filebrowser.weavy.io/v14/",
  configurationTimeout: 5e3,
  disableEnvironmentImports: !1,
  gcTime: 1e3 * 60 * 60 * 24,
  // 24h,
  locale: Tv,
  scrollBehavior: "auto",
  staleTime: 1e3 * 1,
  // 1s
  tokenFactoryRetryDelay: 2e3,
  tokenFactoryTimeout: 2e4,
  // WeavyComponentSettingProps
  annotations: "buttons-inline",
  enterToSend: "auto",
  reactions: "😍 😎 😉 😜 👍"
};
let Qt = re;
const ff = class ff extends Zb(
  L0(
    R0(
      O0(
        M0(
          vb(jb(T0(Wb(gb(Qt)))))
        )
      )
    )
  )
) {
};
n(ff, "Weavy");
let Vt = ff;
const bu = Symbol.for("weavy-app"), Qv = Symbol.for("weavy-apps"), Gv = Symbol.for("weavy-context-id"), Zv = Symbol.for("weavy-data-blobs");
var _ = /* @__PURE__ */ ((s) => (s.Attachments = "attachments", s.CloudFiles = "cloud_files", s.ContextData = "context_data", s.Comments = "comments", s.Embeds = "embeds", s.Follow = "follow", s.GoogleMeet = "google_meet", s.Meetings = "meetings", s.Mentions = "mentions", s.MicrosoftTeams = "microsoft_teams", s.Polls = "polls", s.Previews = "previews", s.Reactions = "reactions", s.Receipts = "receipts", s.Thumbnails = "thumbnails", s.Typing = "typing", s.Versions = "versions", s.WebDAV = "web_dav", s.ZoomMeetings = "zoom_meetings", s))(_ || {});
const xu = Symbol.for("weavy-features");
var Br, Nr, Mi;
const fu = class fu {
  /**
   * Creates a feature policy. All features are initially enabled unless `defaultFeatures` is specified.
   *
   * @constructor
   * @param componentFeatures - Available features.
   */
  constructor(t, e) {
    T(this, Br);
    T(this, Nr);
    T(this, Mi);
    P(this, Br, Object.keys(t)), P(this, Nr, Object.entries(t).reduce(
      (i, [r, a]) => (a && i.push(r), i),
      []
    )), P(this, Mi, e ?? p(this, Nr));
  }
  features() {
    return p(this, Br);
  }
  supportedFeature(...t) {
    return t.every((e) => p(this, Br).includes(e));
  }
  allowedFeatures() {
    return p(this, Mi);
  }
  allowsFeature(...t) {
    return t.every((e) => this.supportedFeature(e) ? p(this, Mi).includes(e) : !1);
  }
  allowsAnyFeature(...t) {
    return t.some((e) => this.supportedFeature(e) ? p(this, Mi).includes(e) : !1);
  }
  /**
   * Sets the allowed features.
   * @param allowedFeatures - Space separated string with features that will be enabled. Empty string will disable all features. `null` or `undefined` will enable all features.
   */
  setAllowedFeatures(t) {
    return P(this, Mi, typeof t == "string" ? Yv(t, p(this, Br), p(this, Nr)) : p(this, Nr)), p(this, Mi);
  }
  immutableCopy() {
    const t = Cw(p(this, Br), p(this, Nr));
    return new fu(t, p(this, Mi));
  }
};
Br = new WeakMap(), Nr = new WeakMap(), Mi = new WeakMap(), n(fu, "ComponentFeatures");
let Gt = fu;
function Yv(s, t, e) {
  let i = [...e];
  return s.split(" ").forEach((r) => {
    if (r)
      if (r.startsWith("-")) {
        const a = r.substring(1);
        i = i.filter((o) => o !== a);
      } else t.includes(r) ? i.includes(r) || i.push(r) : console.warn("Unknown feature provided:", r);
  }), i;
}
n(Yv, "featureListFromString");
function Cw(s, t) {
  return t ??= s, Object.fromEntries(s.map((e) => [e, t.includes(e)]));
}
n(Cw, "featureConfigFromList");
const Sh = Object.values(_), Xv = Symbol.for("weavy-link");
var Sd;
const mf = class mf {
  // PROPERTY INIT
  constructor(t) {
    T(this, Sd);
    this.enterToSend = Qt.defaults.enterToSend, this.reactions = Qt.defaults.reactions, this.annotations = Qt.defaults.annotations, P(this, Sd, t), Object.keys(this).forEach((i) => {
      i in t && Object.assign(this, { [i]: t[i] });
    });
  }
  /**
   * Provides a reference to the host.
   */
  get component() {
    return p(this, Sd);
  }
};
Sd = new WeakMap(), n(mf, "WeavyComponentSettings");
let Xh = mf;
const Jv = Symbol.for("weavy-component-settings"), Cu = Symbol.for("weavy-user"), vf = class vf {
  constructor(t, e, i, r, a) {
    if (this.subscribe = !1, this.provided = !1, this.value = void 0, this._callback = (o, c) => {
      this.unsubscribe && (this.unsubscribe !== c && (this.provided = !1, this.unsubscribe()), this.subscribe || this.unsubscribe()), this.value = o, this.host.requestUpdate(), (!this.provided || this.subscribe) && (this.provided = !0, this.callback && this.callback(o, c)), this.unsubscribe = c;
    }, this.host = t, e.context !== void 0) {
      const o = e;
      this.context = o.context, this.callback = o.callback, this.subscribe = o.subscribe ?? !1, this.ref = o.ref ?? t;
    } else
      this.context = e, this.callback = i, this.subscribe = r ?? !1, this.ref = a ?? t;
    this.host.addController?.(this);
  }
  hostConnected() {
    this.dispatchRequest();
  }
  hostDisconnected() {
    this.unsubscribe && (this.unsubscribe(), this.unsubscribe = void 0);
  }
  dispatchRequest() {
    this.ref.dispatchEvent(
      new Ly(
        this.context,
        this.host,
        this._callback,
        this.subscribe
      )
    );
  }
};
n(vf, "ContextConsumer");
let zy = vf;
const gf = class gf {
  /**
   * @param host - The host element
   * @param context - The Context to use. Should be constructed using createContext().
   */
  constructor(t, e, i) {
    this.whenRef = new Promise((r) => this.resolveRef = r), t.addController(this), this.host = t, this.setRef(i), this.setContext(e);
  }
  /**
   * The DOM connected element that can subscribe to a context
   */
  get ref() {
    return this._ref;
  }
  set ref(t) {
    this._ref = t, t && this.resolveRef?.(t);
  }
  /** 
   * Set the DOM connected element that can subscribe to a context.
   * Wrapper function for React linting compatibility. 
   */
  setRef(t) {
    this.ref = t;
  }
  async setContext(t) {
    this.whenContext = new Promise((i) => this.resolveContext = i);
    const e = await this.whenRef;
    this.context = new zy(this.host, {
      context: t,
      subscribe: !0,
      callback: /* @__PURE__ */ n((i, r) => {
        this.resolveContext?.(i), this.host.requestUpdate();
      }, "callback"),
      ref: e
    }), await _v(e), this.context.dispatchRequest();
  }
  hostUpdate() {
    this.context?.value && this.resolveContext?.(this.context.value);
  }
};
n(gf, "ContextController");
let gm = gf;
var rt = /* @__PURE__ */ ((s) => (s.Chat = "d65dd4bc-418e-403c-9f56-f9cf4da931ed", s.Comments = "88f96a08-c6c1-4eac-a0bd-5bf8fba1a3fd", s.Files = "523edd88-4bbf-4547-b60f-2859a6d2ddc1", s.Posts = "5ebfa152-de85-48da-82dd-30a1b560c313", s.ChatRoom = "edb400ac-839b-45a7-b2a8-6a01820d1c44", s.PrivateChat = "7e14f418-8f15-46f4-b182-f619b671e470", s.AgentChat = "2352a1c6-abc6-420e-8b85-ca7d5aed8779", s))(rt || {}), ui = /* @__PURE__ */ ((s) => (s.Chat = "chat", s.Comments = "comments", s.Files = "files", s.Posts = "posts", s.ChatRoom = "chat_room", s.PrivateChat = "private_chat", s.AgentChat = "agent_chat", s))(ui || {}), t1 = /* @__PURE__ */ ((s) => (s["d65dd4bc-418e-403c-9f56-f9cf4da931ed"] = "chat", s["88f96a08-c6c1-4eac-a0bd-5bf8fba1a3fd"] = "comments", s["523edd88-4bbf-4547-b60f-2859a6d2ddc1"] = "files", s["5ebfa152-de85-48da-82dd-30a1b560c313"] = "posts", s["edb400ac-839b-45a7-b2a8-6a01820d1c44"] = "chat_room", s["7e14f418-8f15-46f4-b182-f619b671e470"] = "private_chat", s["2352a1c6-abc6-420e-8b85-ca7d5aed8779"] = "agent_chat", s))(t1 || {}), e1 = /* @__PURE__ */ ((s) => (s.chat = "d65dd4bc-418e-403c-9f56-f9cf4da931ed", s.comments = "88f96a08-c6c1-4eac-a0bd-5bf8fba1a3fd", s.files = "523edd88-4bbf-4547-b60f-2859a6d2ddc1", s.posts = "5ebfa152-de85-48da-82dd-30a1b560c313", s.chat_room = "edb400ac-839b-45a7-b2a8-6a01820d1c44", s.private_chat = "7e14f418-8f15-46f4-b182-f619b671e470", s.agent_chat = "2352a1c6-abc6-420e-8b85-ca7d5aed8779", s))(e1 || {}), i1 = /* @__PURE__ */ ((s) => (s["2352a1c6-abc6-420e-8b85-ca7d5aed8779"] = "agent_chat", s))(i1 || {}), r1 = /* @__PURE__ */ ((s) => (s.agent_chat = "2352a1c6-abc6-420e-8b85-ca7d5aed8779", s))(r1 || {}), Rr = /* @__PURE__ */ ((s) => (s.None = "none", s.Read = "read", s.Write = "write", s.Admin = "admin", s))(Rr || {}), Vs = /* @__PURE__ */ ((s) => (s.List = "list", s.Read = "read", s.Create = "create", s.Update = "update", s.Delete = "delete", s.Admin = "admin", s))(Vs || {}), It = /* @__PURE__ */ ((s) => (s.App = "app", s.File = "file", s.Message = "message", s.User = "user", s.Comment = "comment", s.Post = "post", s))(It || {});
function nr(s, t, e, i, r, a = "GET") {
  return {
    // eslint-disable-next-line @tanstack/query/exhaustive-deps
    queryKey: t,
    queryFn: /* @__PURE__ */ n(async () => {
      const o = await s.fetch(e || "/api/" + t.join("/"), { method: a, body: r });
      if (o.ok)
        return await o.json();
      throw new Error(`Error calling ${e || "/api/" + t.join("/")}`);
    }, "queryFn"),
    ...i
  };
}
n(nr, "getApiOptions");
const s1 = /* @__PURE__ */ n((s) => (t, e) => {
  e !== void 0 ? e.addInitializer(() => {
    customElements.define(s, t);
  }) : customElements.define(s, t);
}, "t$3");
const Yb = { attribute: !0, type: String, converter: u0, reflect: !1, hasChanged: h0 }, Xb = /* @__PURE__ */ n((s = Yb, t, e) => {
  const { kind: i, metadata: r } = e;
  let a = globalThis.litPropertyMetadata.get(r);
  if (a === void 0 && globalThis.litPropertyMetadata.set(r, a = /* @__PURE__ */ new Map()), i === "setter" && ((s = Object.create(s)).wrapped = !0), a.set(e.name, s), i === "accessor") {
    const { name: o } = e;
    return { set(c) {
      const l = t.get.call(this);
      t.set.call(this, c), this.requestUpdate(o, l, s, !0, c);
    }, init(c) {
      return c !== void 0 && this.C(o, void 0, s, c), c;
    } };
  }
  if (i === "setter") {
    const { name: o } = e;
    return function(c) {
      const l = this[o];
      t.call(this, c), this.requestUpdate(o, l, s, !0, c);
    };
  }
  throw Error("Unsupported decorator location: " + i);
}, "r$3");
function y(s) {
  return (t, e) => typeof e == "object" ? Xb(s, t, e) : ((i, r, a) => {
    const o = r.hasOwnProperty(a);
    return r.constructor.createProperty(a, i), o ? Object.getOwnPropertyDescriptor(r, a) : void 0;
  })(s, t, e);
}
n(y, "n$3");
function V(s) {
  return y({ ...s, state: !0, attribute: !1 });
}
n(V, "r$2");
const Jb = /* @__PURE__ */ n((s, t, e) => (e.configurable = !0, e.enumerable = !0, Reflect.decorate && typeof t != "object" && Object.defineProperty(s, t, e), e), "e$3");
function yl(s) {
  return (t, e) => {
    const { slot: i, selector: r } = s ?? {}, a = "slot" + (i ? `[name=${i}]` : ":not([name])");
    return Jb(t, e, { get() {
      const o = this.renderRoot?.querySelector(a), c = o?.assignedElements(s) ?? [];
      return r === void 0 ? c : c.filter((l) => l.matches(r));
    } });
  };
}
n(yl, "o$6");
const bf = class bf {
  get result() {
    return this._result && this.observer ? this.observer.trackResult(this._result) : { isPending: !0 };
  }
  constructor(t) {
    t.addController(this), this.host = t, this.whenContext = new Promise((e) => this.resolveContext = e), this.whenQueryClient = new Promise((e) => this.resolveQueryClient = e), this.setContext();
  }
  async setContext() {
    await gi(this.host), this.context = new He(this.host, {
      context: Xt,
      subscribe: !0,
      callback: /* @__PURE__ */ n((t) => {
        t && (this.resolveContext?.(t), this.queryClient = t.queryClient, this.resolveQueryClient?.(t.queryClient));
      }, "callback")
    });
  }
  async trackQuery(t, e = !0) {
    this.observerUnsubscribe?.();
    const i = await this.whenQueryClient;
    if (!i)
      throw new Error("No QueryClient provided");
    const r = new Hv(i, t);
    this.observer = r, this.observerSubscribe(e);
  }
  observerSubscribe(t = !0) {
    if (this.queryClient && this.observer) {
      t ? this._result = this.observer.getOptimisticResult(this.observer.options) : this._result = this.observer.getCurrentResult(), this.observerUnsubscribe = this.observer.subscribe(() => {
        if (this.observer) {
          const i = Gd(this.result, this.observer.getCurrentResult());
          i !== this._result && (this._result = i, this.host.requestUpdate());
        }
      }), this.observer.updateResult(), this.host.requestUpdate();
      let e;
      t ? e = this.observer.fetchOptimistic(this.observer.options) : e = this.queryClient.getQueryCache().get(this.observer.options.queryHash)?.promise, e?.catch(() => {
      }).finally(() => {
        this.observer?.updateResult();
      });
    }
  }
  untrackQuery() {
    this.observerUnsubscribe?.(), this.observerUnsubscribe = void 0, this._result = void 0, this.observer = void 0, this.host.requestUpdate();
  }
  hostConnected() {
    this.observerSubscribe();
  }
  hostDisconnected() {
    this.observerUnsubscribe?.();
  }
};
n(bf, "QueryController");
let ve = bf;
function Fy(s) {
  return s.signal instanceof AbortSignal || typeof s.abort == "function";
}
n(Fy, "hasAbort");
const xf = class xf {
  constructor(t) {
    t.addController(this), this.host = t, this.whenContext = new Promise((e) => this.resolveContext = e), this.whenObserver = new Promise((e) => this.resolveObserver = e), this.setContext();
  }
  async setContext() {
    await gi(this.host), this.context = new He(this.host, { context: Xt, subscribe: !0 });
  }
  hostUpdate() {
    this.context?.value && this.resolveContext?.();
  }
  async trackMutation(t, e) {
    if (e || (await this.whenContext, e = this.context?.value?.queryClient), !e)
      throw new Error("No QueryClient provided");
    return this.observerUnsubscribe?.(), this.observer && (this.whenObserver = new Promise((i) => this.resolveObserver = i)), this.observer = new St(e, { ...t }), this.observerSubscribe(), this.resolveObserver?.(this.observer), this.observer;
  }
  observerSubscribe() {
    this.observer && (this.result = this.observer.getCurrentResult(), this.observerUnsubscribe ??= this.observer.subscribe(() => {
      if (this.observer) {
        const t = Gd(this.result, this.observer.getCurrentResult());
        this.result !== t && (this.result = t, this.host.requestUpdate());
      }
    }), this.host.requestUpdate());
  }
  untrackMutation() {
    this.observerUnsubscribe?.(), this.observerUnsubscribe = void 0, this.result = void 0, this.observer = void 0, this.host.requestUpdate();
  }
  async mutate(t, e) {
    const i = await this.whenObserver, r = new AbortController();
    return i.mutate(
      {
        ...t,
        signal: r.signal,
        abort: r.abort.bind(r)
      },
      e
    );
  }
  hostConnected() {
    this.observerSubscribe();
  }
  hostDisconnected() {
    this.observerUnsubscribe?.();
  }
};
n(xf, "MutationController");
let Ue = xf;
function $u(s) {
  if (s)
    return s.match(/^[0-9]+$/) ? parseInt(s) : s;
}
n($u, "toIntOrString");
const be = [];
for (let s = 0; s < 256; ++s)
  be.push((s + 256).toString(16).slice(1));
function t2(s, t = 0) {
  return (be[s[t + 0]] + be[s[t + 1]] + be[s[t + 2]] + be[s[t + 3]] + "-" + be[s[t + 4]] + be[s[t + 5]] + "-" + be[s[t + 6]] + be[s[t + 7]] + "-" + be[s[t + 8]] + be[s[t + 9]] + "-" + be[s[t + 10]] + be[s[t + 11]] + be[s[t + 12]] + be[s[t + 13]] + be[s[t + 14]] + be[s[t + 15]]).toLowerCase();
}
n(t2, "unsafeStringify");
let Bp;
const e2 = new Uint8Array(16);
function i2() {
  if (!Bp) {
    if (typeof crypto > "u" || !crypto.getRandomValues)
      throw new Error("crypto.getRandomValues() not supported. See https://github.com/uuidjs/uuid#getrandomvalues-not-supported");
    Bp = crypto.getRandomValues.bind(crypto);
  }
  return Bp(e2);
}
n(i2, "rng");
const r2 = typeof crypto < "u" && crypto.randomUUID && crypto.randomUUID.bind(crypto), bm = { randomUUID: r2 };
function s2(s, t, e) {
  s = s || {};
  const i = s.random ?? s.rng?.() ?? i2();
  if (i.length < 16)
    throw new Error("Random bytes length must be >= 16");
  return i[6] = i[6] & 15 | 64, i[8] = i[8] & 63 | 128, t2(i);
}
n(s2, "_v4");
function Vl(s, t, e) {
  return bm.randomUUID && !s ? bm.randomUUID() : s2(s);
}
n(Vl, "v4");
function n2(s) {
  let t;
  if (s instanceof URL)
    t = { type: "url", item: s };
  else if (s instanceof File)
    t = { type: "file", item: s };
  else if (s instanceof Blob)
    t = {
      type: "file",
      item: new File([s], `${Vl()}`, { type: s.type })
    };
  else if (typeof s == "string")
    t = {
      type: "file",
      item: new File([s], `${Vl()}.data.txt`, { type: "text/plain;charset=UTF-8" })
    };
  else if (f0(s) || ay(s))
    try {
      const e = ay(s) ? s.toJSON() : JSON.stringify(s, P0(), 2);
      t = {
        type: "file",
        item: new File([e], `${Vl()}.json.txt`, { type: "text/plain;charset=UTF-8" })
      };
    } catch (e) {
      console.error("Could not serialize context data to JSON.", e);
    }
  return t;
}
n(n2, "getContextDataRef");
const ku = /* @__PURE__ */ n((s, t = 3) => {
  const e = [" B", " KB", " MB", " GB", " TB", " PB", " EB", " ZB", " YB"];
  let i = s, r = 0;
  for (; r < e.length - 1 && i >= 1024; )
    i = 100 * i / 1024 / 100, r++;
  return i.toLocaleString(void 0, { maximumSignificantDigits: t }) + e[r];
}, "fileSizeAsString");
function ai(s) {
  return s.lastIndexOf(".") === -1 ? "." : (s.substring(s.lastIndexOf("."), s.length) || s).toLowerCase();
}
n(ai, "getExtension");
function n1(s) {
  switch (s) {
    case ".aac":
    case ".aif":
    case ".aiff":
    case ".au":
    case ".gsm":
    case ".m4a":
    case ".mid":
    case ".midi":
    case ".mka":
    case ".mp3":
    case ".oga":
    case ".ogg":
    case ".ra":
    case ".ram":
    case ".snd":
    case ".spx":
    case ".wav":
    case ".wma":
      return !0;
    default:
      return !1;
  }
}
n(n1, "isAudio");
function a2(s) {
  switch (s) {
    case ".ai":
    case ".apng":
    case ".bmp":
    case ".emf":
    case ".eps":
    case ".gif":
    case ".heic":
    case ".ico":
    case ".jpg":
    case ".jpeg":
    case ".png":
    case ".psd":
    case ".svg":
    case ".tif":
    case ".tiff":
    case ".webp":
    case ".wmf":
      return !0;
    default:
      return !1;
  }
}
n(a2, "isImage");
function o2(s) {
  switch (ai(s)) {
    case ".apng":
    case ".bmp":
    case ".gif":
    case ".ico":
    case ".jpg":
    case ".jpeg":
    case ".png":
    case ".svg":
    case ".webp":
      return !0;
    default:
      return !1;
  }
}
n(o2, "isWebImage");
function a1(s) {
  switch (s) {
    case ".3g2":
    case ".asx":
    case ".asf":
    case ".avi":
    case ".flv":
    case ".mk3d":
    case ".mkv":
    case ".mov":
    case ".mp4":
    case ".mpeg":
    case ".mpg":
    case ".ogm":
    case ".ogv":
    case ".ogx":
    case ".qt":
    case ".rm":
    case ".rv":
    case ".smi":
    case ".smil":
    case ".swf":
    case ".webm":
    case ".wmv":
    case ".xaml":
      return !0;
    default:
      return !1;
  }
}
n(a1, "isVideo");
function c2(s) {
  switch (s) {
    case ".markdown":
    case ".md":
      return !0;
    default:
      return !1;
  }
}
n(c2, "isMarkdown");
function o1(s) {
  switch (s) {
    case ".htm":
    case ".html":
    case ".xhtml":
    case ".xml":
      return !0;
    default:
      return !1;
  }
}
n(o1, "isMarkup");
function $w(s) {
  if (o1(s))
    return !0;
  switch (s) {
    case ".as":
    case ".as3":
    case ".asm":
    case ".aspx":
    case ".bat":
    case ".c":
    case ".cc":
    case ".cmake":
    case ".coffee":
    case ".cpp":
    case ".cs":
    case ".css":
    case ".cxx":
    case ".diff":
    case ".erb":
    case ".erl":
    case ".groovy":
    case ".gvy":
    case ".h":
    case ".haml":
    case ".hh":
    case ".hpp":
    case ".hxx":
    case ".java":
    case ".js":
    case ".json":
    case ".jsx":
    case ".less":
    case ".lst":
    case ".m":
    case ".make":
    case ".ml":
    case ".mm":
    case ".out":
    case ".patch":
    case ".php":
    case ".pl":
    case ".plist":
    case ".properties":
    case ".py":
    case ".rb":
    case ".sass":
    case ".scala":
    case ".scm":
    case ".script":
    case ".scss":
    case ".sh":
    case ".sml":
    case ".sql":
    case ".vb":
    case ".vi":
    case ".vim":
    case ".xsd":
    case ".xsl":
    case ".yaml":
    case ".yml":
      return !0;
    default:
      return !1;
  }
}
n($w, "isCode");
function c1(s) {
  if ($w(s) || c2(s))
    return !0;
  switch (s) {
    case ".txt":
      return !0;
    default:
      return !1;
  }
}
n(c1, "isText");
function cs(s) {
  const t = ai(s);
  if (t === "") return { icon: "file" };
  if (n1(t))
    return { icon: "file-music", color: "indigo" };
  if (a2(t))
    return { icon: "file-image", color: "cyan" };
  if (a1(t))
    return { icon: "file-video", color: "pink" };
  if (o1(t))
    return { icon: "file-xml", color: "purple" };
  if ($w(t))
    return { icon: "file-code", color: "purple" };
  if (c1(t))
    return { icon: "file-document" };
  switch (t) {
    case ".7z":
    case ".zip":
      return { icon: "file-compressed", color: "orange" };
    case ".doc":
    case ".docm":
    case ".docx":
    case ".dotm":
    case ".dotx":
      return { icon: "file-word", color: "native" };
    case ".eml":
    case ".msg":
      return { icon: "email", color: "" };
    case ".xls":
    case ".xlsm":
    case ".xlsx":
    case ".xltx":
      return { icon: "file-excel", color: "native" };
    case ".pdf":
      return { icon: "file-pdf", color: "native" };
    case ".ppt":
    case ".pptm":
    case ".pptx":
    case ".potx":
      return { icon: "file-powerpoint", color: "native" };
    case ".pages":
      return { icon: "file", color: "orange" };
    case ".numbers":
      return { icon: "file", color: "green" };
    case ".keynote":
      return { icon: "file", color: "blue" };
  }
  return { icon: "file" };
}
n(cs, "getIcon");
function l2(s) {
  const t = ai(s);
  return t === "" ? "none" : o2(t) ? "image" : $w(t) ? "code" : c1(t) ? "text" : n1(t) ? "audio" : a1(t) ? "video" : "none";
}
n(l2, "getWebPreviewFormat");
function Zd(s) {
  switch (s) {
    case "Google Drive":
      return "google-drive";
    case "Dropbox":
      return "dropbox";
    case "OneDrive":
      return "onedrive";
    case "Box":
      return "box";
    default:
      return "";
  }
}
n(Zd, "getProvider");
function kw(s) {
  let t = "file";
  switch (ai(s)) {
    case ".7z":
    case ".zip":
      t = "archive";
      break;
    case ".aac":
    case ".aif":
    case ".aiff":
    case ".au":
    case ".gsm":
    case ".m4a":
    case ".mid":
    case ".midi":
    case ".mka":
    case ".mp3":
    case ".oga":
    case ".ogg":
    case ".ra":
    case ".ram":
    case ".snd":
    case ".spx":
    case ".wav":
    case ".wma":
      t = "audio";
      break;
    case ".c":
    case ".cgi":
    case ".cpp":
    case ".cs":
    case ".cshtml":
    case ".css":
    case ".html":
    case ".java":
    case ".js":
    case ".json":
    case ".less":
    case ".php":
    case ".pl":
    case ".ps1":
    case ".py":
    case ".rb":
    case ".rs":
    case ".scss":
    case ".sh":
    case ".swift":
    case ".sql":
    case ".ts":
    case ".xml":
    case ".yaml":
    case ".yml":
      t = "code";
      break;
    case ".doc":
    case ".docm":
    case ".docx":
    case ".dotm":
    case ".dotx":
    case ".gdoc":
    case ".pages":
    case ".pdf":
      t = "document";
      break;
    case ".eml":
    case ".msg":
      t = "email";
      break;
    case ".gslides":
    case ".keynote":
    case ".ppt":
    case ".pptm":
    case ".pptx":
    case ".potx":
      t = "presentation";
      break;
    case ".gsheet":
    case ".numbers":
    case ".xls":
    case ".xlsm":
    case ".xlsx":
    case ".xltx":
      t = "spreadsheet";
      break;
    case ".ai":
    case ".apng":
    case ".bmp":
    case ".emf":
    case ".eps":
    case ".gif":
    case ".heic":
    case ".ico":
    case ".jpg":
    case ".jpeg":
    case ".png":
    case ".psd":
    case ".svg":
    case ".tif":
    case ".tiff":
    case ".webp":
    case ".wmf":
      t = "image";
      break;
    case ".md":
    case ".markdown":
    case ".txt":
      t = "text";
      break;
    case ".3g2":
    case ".asx":
    case ".asf":
    case ".avi":
    case ".flv":
    case ".mk3d":
    case ".mkv":
    case ".mov":
    case ".mp4":
    case ".mpeg":
    case ".mpg":
    case ".ogm":
    case ".ogv":
    case ".ogx":
    case ".qt":
    case ".rm":
    case ".rv":
    case ".smi":
    case ".smil":
    case ".swf":
    case ".webm":
    case ".wmv":
    case ".xaml":
      t = "video";
      break;
  }
  return t;
}
n(kw, "getKind");
function _w(s) {
  const t = s.target, e = t?.value.lastIndexOf(".");
  e === -1 ? t?.select() : e !== void 0 && t?.setSelectionRange(0, e);
}
n(_w, "handleSelectFilename");
async function Iy(s, t = "SHA-256") {
  return await new Promise((e) => {
    if (s) {
      const i = new FileReader();
      i.readAsArrayBuffer(s), i.onloadend = async () => {
        const r = await crypto.subtle.digest(t, i.result);
        let a = "";
        r && (a = Array.from(new Uint8Array(r)).map((c) => c.toString(16).padStart(2, "0")).join("")), e(a);
      };
    } else
      e("");
  });
}
n(Iy, "getHash");
function Np(s, t) {
  return s.findAll(t.filters).map((e) => t.select ? t.select(e) : e.state);
}
n(Np, "getResult");
const Cf = class Cf {
  constructor(t) {
    this.alwaysUpdate = !1, t.addController(this), this.host = t, this.setContext();
  }
  async setContext() {
    this.whenContext = new Promise((t) => this.resolveContext = t), await gi(this.host), this.context = new He(this.host, { context: Xt, subscribe: !0 });
  }
  hostUpdate() {
    this.context?.value && this.resolveContext?.();
  }
  async trackMutationState(t, e) {
    if (e || (await this.whenContext, e = this.context?.value?.queryClient), !e)
      throw new Error("No QueryClient provided");
    return this.mutationCacheUnsubscribe?.(), this.options = t, this.mutationCache = e.getMutationCache(), this.mutationCacheSubscribe(), this.result;
  }
  mutationCacheSubscribe() {
    this.mutationCache && this.options && (this.result = Np(this.mutationCache, this.options), this.mutationCacheUnsubscribe = this.mutationCache.subscribe((t) => {
      if (this.mutationCache && this.options && /added|removed|updated/.test(t.type)) {
        const e = Np(this.mutationCache, this.options);
        this.result !== e && !zs(this.result, e) && (this.result = e, this.host.requestUpdate());
      }
    }), this.host.requestUpdate());
  }
  get isMutating() {
    return !this.mutationCache || !this.options?.filters ? 0 : Np(this.mutationCache, { filters: { ...this.options.filters, status: "pending" } }).length;
  }
  untrackMutationState() {
    this.mutationCacheUnsubscribe?.(), this.mutationCacheUnsubscribe = void 0, this.result = void 0, this.options = void 0, this.mutationCache = void 0, this.host.requestUpdate();
  }
  hostConnected() {
    this.mutationCacheSubscribe();
  }
  hostDisconnected() {
    this.mutationCacheUnsubscribe?.();
  }
};
n(Cf, "MutationStateController");
let Wc = Cf;
function Bt(s, t, e, i) {
  const r = s.getMutationCache(), a = r.find({
    mutationKey: t,
    predicate: /* @__PURE__ */ n((o) => o.state.variables === e, "predicate")
  });
  if (a && a.state.context) {
    const o = { ...a.state.context };
    i(o);
    const c = { ...a.state, context: o };
    a.state = c, r.notify({
      mutation: a,
      type: "updated",
      action: {
        type: "pending",
        context: a.state.context,
        variables: a.state.variables,
        isPaused: !1
      }
    });
  }
}
n(Bt, "updateMutationContext");
function l1(s, t, e) {
  const i = s.getMutationCache().find({ mutationKey: t, exact: !0, predicate: e });
  i && s.getMutationCache().remove(i);
}
n(l1, "removeMutation");
function d2(s, t) {
  const e = s.getMutationCache();
  e.findAll({ mutationKey: t, exact: !0 }).forEach((r) => {
    e.remove(r);
  });
}
n(d2, "removeMutations");
const Pw = /* @__PURE__ */ n((s, t, e = 0, i = "application/octet-stream", r, a = Date.now()) => {
  const o = s && (s instanceof URL ? s.toString() : URL.createObjectURL(s));
  return {
    id: a,
    app: { id: -1 },
    name: t,
    kind: kw(t),
    size: e,
    media_type: i,
    embed_url: "",
    preview_format: l2(t),
    thumbnail_url: o,
    preview_url: o,
    download_url: o,
    rev: -1,
    created_by: r,
    created_at: (/* @__PURE__ */ new Date()).toUTCString(),
    is_subscribed: !1,
    is_trashed: !1,
    is_starred: !1
  };
}, "getTempFile");
function d1(s) {
  const t = s ? s.reduce(
    (e, i) => {
      const r = i.context?.file;
      return r && i.context?.status.progress && r.size ? {
        loaded: e.loaded + Math.floor(i.context.status.progress / 100 * r.size),
        total: e.total + r.size
      } : e;
    },
    { loaded: 0, total: 0 }
  ) : { loaded: 0, total: 0 };
  return {
    /** Loaded bytes */
    loaded: t.loaded,
    /** Total bytes */
    total: t.total,
    /** Progress of upload provided as 0-100 percent. */
    percent: t.total > 0 ? t.loaded / t.total * 100 : null
  };
}
n(d1, "getFileMutationsTotalProgress");
function Dy(s) {
  return s ? s.some((t) => t.context?.status.state === "conflict") ? "conflict" : s.some((t) => t.status === "error") ? "error" : s.every((t) => t.status === "success") ? "ok" : "pending" : "ok";
}
n(Dy, "getFileMutationsTotalStatus");
function xm(s) {
  return s?.filter((t) => t.context?.status.state === "pending") || [];
}
n(xm, "getPendingFileMutations");
function Cm(s) {
  return s?.filter((t) => t.context?.status.state === "conflict" || t.status === "error") || [];
}
n(Cm, "getFileMutationsByConflictOrError");
function h2(s, t) {
  const e = s.queryClient;
  e.getMutationCache().findAll({
    mutationKey: ["apps", t.id, "blobs"],
    exact: !0
  }).forEach((i) => {
    i.state.context?.status.state === "error" && e.getMutationCache().remove(i);
  }), e.getMutationCache().findAll({
    mutationKey: ["apps", t.id, "files"],
    exact: !0
  }).forEach((i) => {
    i.state.context?.status.state === "conflict" && e.getMutationCache().remove(i);
  });
}
n(h2, "removeErroredFileMutations");
function Vy(s, t, e) {
  const i = s.queryClient;
  i.getMutationCache().findAll({
    mutationKey: ["apps", t.id, "blobs"],
    exact: !0,
    predicate: /* @__PURE__ */ n((r) => /error|success/.test(r.state.status) && (!e || r.state.variables?.blob?.name === e), "predicate")
  }).forEach((r) => {
    i.getMutationCache().remove(r);
  }), i.getMutationCache().findAll({
    mutationKey: ["apps", t.id, "files"],
    exact: !0,
    predicate: /* @__PURE__ */ n((r) => /error|success/.test(r.state.status) && (!e || r.state.variables?.blob?.name === e), "predicate")
  }).forEach((r) => {
    i.getMutationCache().remove(r);
  });
}
n(Vy, "removeSettledFileMutations");
function u2(s, t, e) {
  const i = s.queryClient, r = ["apps", e.id, "files"];
  return {
    mutationFn: /* @__PURE__ */ n(async ({ blob: o, replace: c = !1 }) => {
      const l = await s.fetch("/api/apps/" + e.id + "/files", {
        method: "POST",
        body: JSON.stringify({ blob_id: o.id, replace: c })
      });
      if (!l.ok) {
        const u = await l.json();
        throw new Error(u.detail || u.title, { cause: u });
      }
      return await l.json();
    }, "mutationFn"),
    mutationKey: r,
    onMutate: /* @__PURE__ */ n(async (o) => {
      await i.cancelQueries({ queryKey: r, exact: !0 }), p2(s, e, o.blob.name), Vy(s, e, o.blob.name);
      let c;
      try {
        c = o.blob.thumbnail_url && new URL(o.blob.thumbnail_url) || void 0;
      } catch {
      }
      const l = Pw(c, o.blob.name, o.blob.size, o.blob.media_type, t);
      return {
        type: o.replace ? "replace" : "create",
        file: l,
        status: { state: "pending" }
      };
    }, "onMutate"),
    onSuccess: /* @__PURE__ */ n((o, c, l) => (Bt(i, r, c, (u) => {
      u && (u.status.state = "ok", u.status.progress = void 0, u.status.text = void 0);
    }), i.invalidateQueries({ queryKey: r })), "onSuccess"),
    onError(o, c, l) {
      o?.cause?.status === 409 ? Bt(i, r, c, (u) => {
        u && (u.status.progress = void 0, u.status.state = "conflict", u.status.text = o.message);
      }) : Bt(i, r, c, (u) => {
        u && (u.status.state = "error", u.status.progress = void 0, u.status.text = o.message);
      });
    }
  };
}
n(u2, "getCreateFileMutationOptions");
function p2(s, t, e, i, r = "blobs") {
  const a = s.queryClient;
  a.getMutationCache().findAll({
    mutationKey: ["apps", t.id, r],
    exact: !0,
    status: "success",
    predicate: /* @__PURE__ */ n((o) => o.state.data?.name === e, "predicate")
  }).forEach((o) => {
    a.getMutationCache().remove(o);
  });
}
n(p2, "removeSuccessfulUploadBlobMutations");
async function h1(s, t, e, i) {
  const r = new FormData();
  r.append("blob", t);
  const a = await s.upload(
    "/api/blobs",
    "POST",
    r,
    qh.Auto,
    (c) => {
      e && e({ progress: c });
    },
    i
  );
  if (!a.ok) {
    const c = await a.json();
    throw new Error(c.detail || c.title, { cause: c });
  }
  return await a.json();
}
n(h1, "uploadBlob");
function y2(s) {
  return {
    mutationFn: /* @__PURE__ */ n(async (e) => await h1(s, e.file, e.onProgress), "mutationFn")
    // TODO: implement onmutate, onsuccess, onerror...
  };
}
n(y2, "getSimpleUploadBlobMutationOptions");
function Sw(s, t, e, i, r = "blobs") {
  const a = s.queryClient, o = i ? ["apps", e, r, i] : ["apps", e, r];
  return {
    mutationFn: /* @__PURE__ */ n(async (l) => await h1(s, l.file, l.onProgress, l.signal), "mutationFn"),
    mutationKey: o,
    onMutate: /* @__PURE__ */ n(async (l) => {
      await a.cancelQueries({ queryKey: o, exact: !0 });
      const u = Pw(l.file, l.file.name, l.file.size, l.file.type, t);
      l.onProgress = ({ progress: C }) => {
        Bt(a, o, l, (v) => {
          v.status.state = "pending", v.status.progress = C;
        });
      };
      const b = await Iy(l.file);
      return { type: "upload", file: u, status: { state: "pending" }, sha256: b };
    }, "onMutate"),
    onSuccess: /* @__PURE__ */ n((l, u, b) => {
      Bt(a, o, u, (C) => {
        C.status.state = "ok", C.status.progress = void 0, C.status.text = void 0;
      });
    }, "onSuccess"),
    onError(l, u, b) {
      const C = l.cause;
      C && C.status === 409 ? Bt(a, o, u, (v) => {
        v.status.state = "conflict", v.status.progress = void 0, v.status.text = C.detail || C.title;
      }) : Bt(a, o, u, (v) => {
        v && (v.status.state = "error", v.status.progress = void 0, v.status.text = C ? C.detail || C.title : l.message);
      });
    }
  };
}
n(Sw, "getUploadBlobMutationOptions");
var w2 = Object.defineProperty, Mw = /* @__PURE__ */ n((s, t, e, i) => {
  for (var r = void 0, a = s.length - 1, o; a >= 0; a--)
    (o = s[a]) && (r = o(t, e, r) || r);
  return r && w2(t, e, r), r;
}, "__decorateClass$1p"), ba, xa, Ca, $a, ka, _a, Md, Ed, Ji;
const $f = class $f extends nt {
  constructor() {
    super(...arguments);
    T(this, ba);
    T(this, xa);
    T(this, Ca);
    T(this, $a);
    T(this, ka);
    T(this, _a);
    T(this, Md);
    T(this, Ed);
    T(this, Ji);
    this.contextId = Vl(), P(this, xa, new Promise((e) => {
      P(this, ba, e);
    })), P(this, $a, new Promise((e) => {
      P(this, Ca, e);
    })), P(this, _a, new Promise((e) => {
      P(this, ka, e);
    })), P(this, Ed, new Promise((e) => {
      P(this, Md, e);
    })), P(this, Ji, new ve(this));
  }
  // PROPERTIES
  // PROMISES
  // TODO: Switch to Promise.withResolvers() when allowed by typescript
  // Promise.withResolvers() is available in ES2024, that needs to be set in TSConfig
  /**
   * Resolves when a contextual id is available.
   *
   * @internal
   * @returns {Promise<ContextIdType>}
   */
  async whenContextId() {
    return await p(this, xa);
  }
  /**
   * Resolves when current user data is available.
   *
   * @returns {Promise<UserType>}
   */
  async whenUser() {
    return await p(this, $a);
  }
  /**
   * Resolves when a weavy context is available.
   *
   * @returns {Promise<WeavyType>}
   */
  async whenWeavy() {
    return await p(this, _a);
  }
  /**
   * Resolves when the component has rendered first time.
   *
   * @returns {Promise<WeavyComponent>}
   */
  async whenReady() {
    return await p(this, Ed);
  }
  connectedCallback() {
    super.connectedCallback(), this.contextId && this.requestUpdate("contextId"), this.user && this.requestUpdate("user"), this.weavy && this.requestUpdate("weavy");
  }
  async scheduleUpdate() {
    await gi(this), await super.scheduleUpdate();
  }
  async willUpdate(e) {
    var i, r, a;
    super.willUpdate(e), this.weavyContextConsumer ??= new He(this, { context: Xt, subscribe: !0 }), this.weavyContextConsumer?.value && this.weavy !== this.weavyContextConsumer?.value && (this.weavy = this.weavyContextConsumer?.value), e.has("weavy") && this.weavy && await p(this, Ji).trackQuery(nr(this.weavy, ["user"])), p(this, Ji).result?.isPending || (this.user && p(this, Ji).result.data && this.user.id !== p(this, Ji).result.data.id && (console.warn("User mismatch, resetting"), this.weavy?.reset()), this.user = p(this, Ji).result?.data), e.has("contextId") && this.contextId && (e.get("contextId") && P(this, xa, new Promise((o) => {
      P(this, ba, o);
    })), (i = p(this, ba)) == null || i.call(this, this.contextId)), e.has("user") && this.user && (e.get("user") && P(this, $a, new Promise((o) => {
      P(this, Ca, o);
    })), (r = p(this, Ca)) == null || r.call(this, this.user)), e.has("weavy") && this.weavy && (e.get("weavy") && P(this, _a, new Promise((o) => {
      P(this, ka, o);
    })), (a = p(this, ka)) == null || a.call(this, this.weavy));
  }
  firstUpdated(e) {
    var i;
    super.firstUpdated(e), (i = p(this, Md)) == null || i.call(this, this), this.dispatchEvent(
      new CustomEvent("ready", {
        bubbles: !0,
        composed: !0
      })
    );
  }
};
ba = new WeakMap(), xa = new WeakMap(), Ca = new WeakMap(), $a = new WeakMap(), ka = new WeakMap(), _a = new WeakMap(), Md = new WeakMap(), Ed = new WeakMap(), Ji = new WeakMap(), n($f, "WeavyComponent");
let jc = $f;
Mw([
  V()
], jc.prototype, "weavy");
Mw([
  Ui({ context: Gv }),
  V()
], jc.prototype, "contextId");
Mw([
  Ui({ context: Cu }),
  V()
], jc.prototype, "user");
var f2 = Object.defineProperty, m2 = Object.getOwnPropertyDescriptor, qe = /* @__PURE__ */ n((s, t, e, i) => {
  for (var r = i > 1 ? void 0 : i ? m2(t, e) : t, a = s.length - 1, o; a >= 0; a--)
    (o = s[a]) && (r = (i ? o(t, e, r) : o(r)) || r);
  return i && r && f2(t, e, r), r;
}, "__decorateClass$1o"), Ad, Rd, Td, Ld, Pa, Sa, Ma, Ea, Ms, Es, Aa, Ra, Ei, Ta, As;
const kf = class kf extends jc {
  constructor() {
    super();
    // DEPRECATED .data property
    T(this, Ad);
    // APP SUBSCRIBE
    // SETTINGS
    // annotations
    T(this, Rd);
    // enterToSend
    T(this, Td);
    // reactions
    T(this, Ld);
    T(this, Pa);
    T(this, Sa);
    T(this, Ma);
    T(this, Ea);
    T(this, Ms);
    T(this, Es);
    T(this, Aa);
    T(this, Ra);
    T(this, Ei);
    T(this, Ta);
    T(this, As);
    this.storage = Rv("localStorage"), this.storageLinkHandler = (e) => {
      e.storageArea === this.storage && e.key === "wy-link" && e.newValue && this.readStorageLink();
    }, this.linkEventHandler = async (e) => {
      !e.defaultPrevented && e.detail.link && (this.link && this.link.id === e.detail.link.id && (this.link = void 0, await this.updateComplete), this.link = e.detail.link, this.link || this.provideStorageLink(e.detail.link));
    }, this.notificationEventConsumer = (e) => {
      e.stopPropagation(), e.defaultPrevented || this.visibilityElement && this.visibilityElement.isConnected && this.visibilityElement.checkVisibility(a0) && this.matchesLink(e.detail.link) && e.preventDefault();
    }, P(this, Sa, new Promise((e) => {
      P(this, Pa, e);
    })), P(this, Ea, new Promise((e) => {
      P(this, Ma, e);
    })), P(this, Es, new Promise((e) => {
      P(this, Ms, e);
    })), P(this, Ra, new Promise((e) => {
      P(this, Aa, e);
    })), P(this, Ei, /* @__PURE__ */ new Map()), P(this, Ta, new Ue(this)), P(this, As, new Wc(this)), this.settings = new Xh(this);
  }
  /**
   * Checks if an entity matches the component configuration.
   *
   * @internal
   * @param link - Entity to check for a match.
   * @returns True if the entity targets this component.
   */
  matchesLink(e) {
    return (
      // Type app match, like the Messenger
      e?.app?.type && this.componentTypes?.includes(e.app.type) ? e.agent ? e.agent === this.agent : !0 : !1
    );
  }
  set link(e) {
    const i = this._link;
    this._link = this.matchesLink(e) ? e : void 0, this.requestUpdate("link", i);
  }
  get link() {
    return this._link;
  }
  /**
   * Clears the link and resets the promise.
   *
   * @internal
   */
  clearLink() {
    this.link && (P(this, Es, new Promise((e) => {
      P(this, Ms, e);
    })), this.link = void 0);
  }
  /**
   * Shares a link with other blocks that may consume it automatically.
   *
   * @param link - The entity to provide.
   * @internal
   */
  provideStorageLink(e) {
    this.storage?.setItem("wy-link", btoa(JSON.stringify(e)));
  }
  /**
   * Reads a link from storage and exposes it via the link property and context.
   *
   * @internal
   */
  readStorageLink() {
    if (!this.storage) {
      console.error("Storage not available");
      return;
    }
    const e = this.storage.getItem("wy-link");
    if (e)
      try {
        const i = JSON.parse(atob(e));
        i && (this.link = i);
      } catch (i) {
        console.error("Error parsing link", i);
      }
  }
  /**
   * Consumes a link in storage. Make sure to consume it after it has been used.
   *
   * @internal
   */
  consumeStorageLink() {
    this.storage?.removeItem("wy-link");
  }
  /**
   * Element to match visibility on.
   */
  get visibilityElement() {
    return this;
  }
  set agent(e) {
    this._agentUid = e || void 0;
  }
  get agent() {
    return this._agentUid;
  }
  set data(e) {
    console.warn(".data property array is deprecated. Use .contextualData string instead."), P(this, Ad, e);
  }
  /**
   * DEPRECATED: Use `.contextualData` property instead.
   *
   * Array with contextual data.
   *
   * *Note: Only the first item in the array is currently used.*
   * @internal
   * @deprecated
   */
  get data() {
    return p(this, Ad);
  }
  set annotations(e) {
    P(this, Rd, e);
  }
  get annotations() {
    return p(this, Rd) ?? this.weavy?.annotations ?? Qt.defaults.annotations;
  }
  set enterToSend(e) {
    P(this, Td, e);
  }
  get enterToSend() {
    return p(this, Td) ?? this.weavy?.enterToSend ?? Qt.defaults.enterToSend;
  }
  set reactions(e) {
    P(this, Ld, e);
  }
  get reactions() {
    return p(this, Ld) ?? this.weavy?.reactions ?? Qt.defaults.reactions;
  }
  // PROMISES
  // TODO: Switch to Promise.withResolvers() when allowed by typescript
  // Promise.withResolvers() is available in ES2024, that needs to be set in TSConfig
  // #resolveAgentUser?: (agent: AgentType) => void;
  // #whenAgentUser = new Promise<AgentType>((r) => {
  //   this.#resolveAgentUser = r;
  // });
  // async whenAgentUser() {
  //   return await this.#whenAgentUser;
  // }
  /**
   * Resolves when context data blob uploads has finished.
   *
   * @internal
   * @returns {Promise<ContextDataBlobsType>}
   */
  async whenContextDataBlobs() {
    return await p(this, Sa);
  }
  /**
   * Resolves when Weavy component features config is available.
   *
   * @internal
   * @returns {Promise<ComponentFeaturePolicy>}
   */
  async whenComponentFeatures() {
    return await p(this, Ea);
  }
  /**
   * Resolves when a provided link is available.
   *
   * @internal
   * @returns {Promise<LinkType>}
   */
  async whenLink() {
    return await p(this, Es);
  }
  /**
   * Resolves when Weavy component settings are available.
   *
   * @internal
   * @returns {Promise<WeavyComponentSettingsType>}
   */
  async whenSettings() {
    return await p(this, Ra);
  }
  connectedCallback() {
    super.connectedCallback(), document.addEventListener("wy-link", this.linkEventHandler), window.addEventListener("storage", this.storageLinkHandler), this.componentFeatures && this.requestUpdate("componentFeatures"), this.contextDataBlobs && this.requestUpdate("contextDataBlobs"), this.link && this.requestUpdate("link"), this.settings && this.requestUpdate("settings");
  }
  disconnectedCallback() {
    super.disconnectedCallback(), document.removeEventListener("wy-link", this.linkEventHandler), window.removeEventListener("storage", this.storageLinkHandler), this.weavy?.host.removeEventListener("wy-notification", this.notificationEventConsumer, { capture: !0 });
  }
  async willUpdate(e) {
    var r, a, o, c;
    await super.willUpdate(e), e.has("bot") && typeof this.bot == "string" && console.error(`Using .bot property is deprecated. Use .agent = "${this.bot}"; instead`);
    const i = Object.keys(this.settings);
    if ((e.has("weavy") || i.find((l) => e.has(l))) && (this.settings = new Xh(this)), e.has("features") && this.componentFeatures && (this.componentFeatures.setAllowedFeatures(this.features), this.componentFeatures instanceof Gt && (this.componentFeatures = this.componentFeatures.immutableCopy())), (e.has("weavy") || e.has("contextId") || e.has("user") || e.has("componentFeatures")) && this.weavy && this.contextId && this.user && this.componentFeatures?.allowsFeature(_.ContextData) && (await p(this, Ta).trackMutation(
      Sw(this.weavy, this.user, this.contextId, void 0, "data")
    ), await p(this, As).trackMutationState(
      {
        filters: {
          mutationKey: ["apps", this.contextId, "data"],
          exact: !0
        }
      },
      this.weavy.queryClient
    )), e.has("contextualData") || e.has("data") || e.has("componentFeatures")) {
      const l = this.contextualData ? [this.contextualData] : this.data ? this.data : [], u = p(this, Ei);
      if (P(this, Ei, /* @__PURE__ */ new Map()), l.forEach((b) => {
        const C = u.get(b);
        if (C)
          p(this, Ei).set(b, C);
        else {
          const v = n2(b);
          v && p(this, Ei).set(b, v);
        }
      }), p(this, Ei) && this.componentFeatures?.allowsFeature(_.ContextData)) {
        for (const f of Array.from(p(this, Ei).values()))
          if (f.type === "file") {
            const m = await Iy(f.item);
            if (!await m0(
              p(this, As).result ?? [],
              async (E) => (E.context?.sha256 ?? await Iy(E.variables?.file)) === m
            ))
              try {
                await p(this, Ta).mutate({ file: f.item });
              } catch {
                console.warn("Could not upload context data.");
              }
          }
        const b = p(this, As).result, C = b?.some((f) => f.status === "pending"), v = b?.map((f) => f.data?.id).filter(cn).reverse() ?? [];
        C || (this.contextDataBlobs = v);
      } else
        this.contextDataBlobs = [];
    }
    (e.has("componentTypes") || e.has("agent")) && this.componentTypes && this.readStorageLink(), e.has("link") && this.link && (console.info("Opening notification link"), this.consumeStorageLink()), e.has("contextDataBlobs") && this.contextDataBlobs && (e.get("contextDataBlobs") && P(this, Sa, new Promise((l) => {
      P(this, Pa, l);
    })), (r = p(this, Pa)) == null || r.call(this, this.contextDataBlobs)), e.has("componentFeatures") && this.componentFeatures && (e.get("componentFeatures") && P(this, Ea, new Promise((l) => {
      P(this, Ma, l);
    })), (a = p(this, Ma)) == null || a.call(this, this.componentFeatures)), e.has("link") && this.link && (e.get("link") && P(this, Es, new Promise((l) => {
      P(this, Ms, l);
    })), (o = p(this, Ms)) == null || o.call(this, this.link)), e.has("settings") && this.settings && (e.get("settings") && P(this, Ra, new Promise((l) => {
      P(this, Aa, l);
    })), (c = p(this, Aa)) == null || c.call(this, this.settings)), e.has("weavy") && this.weavy && this.weavy.host.addEventListener("wy-notification", this.notificationEventConsumer, { capture: !0 });
  }
};
Ad = new WeakMap(), Rd = new WeakMap(), Td = new WeakMap(), Ld = new WeakMap(), Pa = new WeakMap(), Sa = new WeakMap(), Ma = new WeakMap(), Ea = new WeakMap(), Ms = new WeakMap(), Es = new WeakMap(), Aa = new WeakMap(), Ra = new WeakMap(), Ei = new WeakMap(), Ta = new WeakMap(), As = new WeakMap(), n(kf, "WeavyTypeComponent");
let Nt = kf;
qe([
  Ui({ context: Zv }),
  V()
], Nt.prototype, "contextDataBlobs", 2);
qe([
  Ui({ context: xu }),
  V()
], Nt.prototype, "componentFeatures", 2);
qe([
  Ui({ context: Jv }),
  V()
], Nt.prototype, "settings", 2);
qe([
  Ui({ context: Xv })
], Nt.prototype, "_link", 2);
qe([
  y({ type: Object })
], Nt.prototype, "link", 1);
qe([
  V()
], Nt.prototype, "componentTypes", 2);
qe([
  y()
], Nt.prototype, "features", 2);
qe([
  y({ type: String })
], Nt.prototype, "agent", 1);
qe([
  y({
    attribute: !0,
    type: String
  })
], Nt.prototype, "contextualData", 2);
qe([
  y({
    attribute: !0,
    type: String,
    converter: {
      fromAttribute(s) {
        return uw(s);
      }
    }
  })
], Nt.prototype, "data", 1);
qe([
  y({ type: String })
], Nt.prototype, "annotations", 1);
qe([
  y({ type: String })
], Nt.prototype, "enterToSend", 1);
qe([
  y({ type: String })
], Nt.prototype, "reactions", 1);
qe([
  y()
], Nt.prototype, "bot", 2);
const v2 = new Map(Pv(t1)), g2 = new Map(Pv(e1)), Jh = new Map(Object.entries(i1)), Uk = new Map(Object.entries(r1));
var b2 = Object.defineProperty, x2 = Object.getOwnPropertyDescriptor, xr = /* @__PURE__ */ n((s, t, e, i) => {
  for (var r = i > 1 ? void 0 : i ? x2(t, e) : t, a = s.length - 1, o; a >= 0; a--)
    (o = s[a]) && (r = (i ? o(t, e, r) : o(r)) || r);
  return i && r && b2(t, e, r), r;
}, "__decorateClass$1n");
const Ti = "unknown";
var Od, hi, La, Oa, zd, Fd, qr, ei, Rs, zl;
const _f = class _f extends Nt {
  constructor() {
    super(...arguments);
    T(this, Rs);
    T(this, Od);
    T(this, hi);
    T(this, La);
    T(this, Oa);
    T(this, zd);
    T(this, Fd);
    T(this, qr);
    T(this, ei);
    P(this, hi, []), this.currentUid = void 0, this.appSubscribeMutation = new Ue(this), P(this, Oa, new Promise((e) => {
      P(this, La, e);
    })), P(this, Fd, new Promise((e) => {
      P(this, zd, e);
    })), P(this, qr, new ve(this)), P(this, ei, /* @__PURE__ */ new Map());
  }
  // @provide({ context: AgentContext })
  // @state()
  // agentUser: AgentType | undefined;
  /**
   * Sets the component to it's initial state and resets the app state.
   */
  reset() {
    this.apps && (this.apps = void 0), this._appName !== this._initialAppName && (this._appName = this._initialAppName);
  }
  /**
   * Checks if an entity matches the component configuration.
   *
   * @internal
   * @param link - Entity to check for a match.
   * @returns True if the entity targets this component.
   */
  matchesLink(e) {
    return this.appType ? (
      // Normal contextual app
      !!(e && e.app && this.appType !== Ti && (typeof this.uid == "string" && e.app?.uid === this.uid || // Normal app with app uid
      typeof this.uid == "number" && e.app?.id === this.uid || // Normal app with app id
      !this.uid && this.app && e.app.id === this.app.id))
    ) : super.matchesLink(e);
  }
  set link(e) {
    const i = this._link;
    !this.uid && this.appType && this.appType !== Ti ? this.whenApp().then(() => {
      this._link = this.matchesLink(e) ? e : void 0, this.requestUpdate("link", i);
    }) : (this._link = this.matchesLink(e) ? e : void 0, this.requestUpdate("link", i));
  }
  get link() {
    return this._link;
  }
  set uid(e) {
    P(this, Od, e), typeof e == "string" ? P(this, hi, e.split(k0).filter(cn).map((i) => $u(i))) : e ? P(this, hi, [e]) : P(this, hi, []);
  }
  get uid() {
    return p(this, Od);
  }
  /**
   * An array of parsed uids from the `uid` property.
   * @readonly
   */
  get uids() {
    return [...p(this, hi)];
  }
  set name(e) {
    this._initialAppName = e, this._appName = e;
  }
  get name() {
    return this._appName;
  }
  /**
   * Subscribes or unsubscribes to notification updates from the app. Check `.app.is_subscribed` to see current state.
   *
   * @param subscribe - Set to `false` to unsubscribe from updates. Defaults to `true`.
   * @returns {Promise<void>}
   *
   * @example <caption>Toggle app subscription</caption>
   * ```js
   * // Wait for app data
   * await myWeavyComponent.whenApp();
   *
   * // Get current app subscription state
   * const isSubscribed = myWeavyComponent.app.is_subscribed;
   *
   * // Toggle the app subscription state
   * myWeavyComponent.subscribe(!isSubscribed);
   * ```
   */
  async subscribe(e = !0) {
    if (!this.currentUid)
      throw new Error(`Cannot ${e ? "subscribe" : "unsubscribe"} without a uid.`);
    await this.whenApp(), this.app?.id && this.appSubscribeMutation.mutate({ subscribe: e });
  }
  // PROMISES
  // TODO: Switch to Promise.withResolvers() when allowed by typescript
  // Promise.withResolvers() is available in ES2024, that needs to be set in TSConfig
  /**
   * Resolves when app data is available.
   *
   * @returns {Promise<AppType>}
   */
  async whenApp() {
    return await p(this, Oa);
  }
  /**
   * Resolves when multiple apps is available.
   *
   * @returns {Promise<AppType[]>}
   */
  async whenApps() {
    return await p(this, Fd);
  }
  connectedCallback() {
    super.connectedCallback(), this.app && this.requestUpdate("app");
  }
  async willUpdate(e) {
    var r, a;
    if (await super.willUpdate(e), e.has("autoUid") && typeof this.autoUid == "string" && (console.error(`Using .autoUid property is deprecated. Use .generateUid = "${this.autoUid}"; instead`), this.generateUid = this.autoUid), (e.has("generateUid") || e.has("user") || e.has("agent")) && this.generateUid && this.user && (this.appType && Jh.has(this.appType) && this.agent || this.appType && !Jh.has(this.appType))) {
      const o = [this.generateUid];
      this.agent && o.push(this.agent), this.user && o.push(this.user.uid || this.user.id), this.uid = o.join("-");
    }
    if (e.has("uid") && this.uid && (!this.currentUid || this.currentUid && !p(this, hi).includes(this.currentUid))) {
      const o = p(this, hi)[0];
      this.currentUid = o;
    }
    const i = this.name;
    if ((e.has("appType") || e.has("uid") || e.has("agent") || e.has("weavy")) && this._appName !== this._initialAppName && (this._appName = this._initialAppName, this.requestUpdate("name", i)), e.has("appType") || e.has("currentUid") || e.has("agent") || e.has("name") || e.has("weavy"))
      if (this.appType && this.currentUid && this.weavy) {
        const o = p(this, hi).length === 1;
        !o && this.name && console.warn("Specifying a name for the app is not possible when using multiple uid.");
        const c = o && this.name ? { name: this.name } : void 0, l = this.agent ? [this.agent] : void 0;
        await p(this, qr).trackQuery(
          C2(this.weavy, this.currentUid, this.appType, l, c)
        );
      } else
        p(this, qr).untrackQuery();
    if (p(this, qr).result?.isPending || (this.app = p(this, qr).result?.data, this.app?.name && this._appName !== this.app.name && (this.name = this.app.name, this.requestUpdate("name", i))), (e.has("uid") || e.has("weavy")) && (this.uid || e.get("uid")) && this.weavy)
      if (this.uids.length) {
        for (const [o, c] of p(this, ei).entries())
          this.uids.includes(o) || (c.untrackQuery(), p(this, ei).delete(o));
        this.uids.forEach((o) => {
          if (!p(this, ei).has(o) && this.weavy) {
            const c = new ve(this);
            c.trackQuery(u1(this.weavy, o)), p(this, ei).set(o, c);
          }
        });
      } else p(this, ei).size && (p(this, ei).forEach((o) => o.untrackQuery()), p(this, ei).clear());
    if (!p(this, Rs, zl).result.isPending && (!this.apps || JSON.stringify(this.apps) !== JSON.stringify(p(this, Rs, zl).result.data?.data)))
      if (p(this, Rs, zl).result.data?.data) {
        const o = p(this, Rs, zl).result.data?.data, c = this;
        this.apps = o && Object.defineProperty([...o], "current", {
          set(l) {
            c.app = l || void 0, c.currentUid = l?.uid || l?.id;
          },
          get() {
            return c.app || void 0;
          }
        }), (r = p(this, zd)) == null || r.call(this, this.apps);
      } else
        this.apps = void 0;
    if (e.has("currentUid") && (this.currentUid || e.get("currentUid")) && this.currentUid !== e.get("currentUid") && this.clearLink(), !this.link && (e.has("currentUid") && this.currentUid || e.has("app") && this.app) && this.appType && this.appType !== Ti && this.readStorageLink(), e.has("app") && this.app) {
      const o = new CustomEvent("wy-app", {
        bubbles: !1,
        composed: !0,
        detail: {
          app: this.app
        }
      });
      this.dispatchEvent(o);
    }
    e.has("app") && this.app && (e.get("app") && P(this, Oa, new Promise((o) => {
      P(this, La, o);
    })), (a = p(this, La)) == null || a.call(this, this.app)), (e.has("weavy") || e.has("app")) && this.weavy && this.app && this.appSubscribeMutation.trackMutation(k2(this.weavy, this.app));
  }
};
Od = new WeakMap(), hi = new WeakMap(), La = new WeakMap(), Oa = new WeakMap(), zd = new WeakMap(), Fd = new WeakMap(), qr = new WeakMap(), ei = new WeakMap(), Rs = new WeakSet(), zl = /* @__PURE__ */ n(function() {
  return S2(p(this, ei));
}, "#appsQuery"), n(_f, "WeavyAppComponent");
let ae = _f;
xr([
  Ui({ context: bu }),
  V()
], ae.prototype, "app", 2);
xr([
  Ui({ context: Qv }),
  V()
], ae.prototype, "apps", 2);
xr([
  y({ type: Object })
], ae.prototype, "link", 1);
xr([
  V()
], ae.prototype, "appType", 2);
xr([
  y()
], ae.prototype, "generateUid", 2);
xr([
  y({ converter: $u })
], ae.prototype, "uid", 1);
xr([
  y()
], ae.prototype, "currentUid", 2);
xr([
  y({ type: String })
], ae.prototype, "name", 1);
xr([
  y()
], ae.prototype, "autoUid", 2);
function u1(s, t, e = Ti, i) {
  return e === Ti || typeof t == "number" ? nr(s, ["apps", t]) : nr(s, ["apps", t], void 0, void 0, JSON.stringify({ type: e, ...i }), "PUT");
}
n(u1, "getAppOptions");
function C2(s, t, e = Ti, i, r) {
  return {
    // eslint-disable-next-line @tanstack/query/exhaustive-deps
    queryKey: ["apps", t],
    queryFn: /* @__PURE__ */ n(async () => {
      const a = [];
      e === Ti || typeof t == "number" ? a.push(
        // Get existing app
        s.fetch(`/api/apps/${t}`)
      ) : i?.length ? (a.push(
        // Get existing app
        s.fetch(`/api/apps/${t}`)
      ), a.push(
        // Create app with members
        s.fetch("/api/apps", { method: "POST", body: JSON.stringify({ type: e, members: i, uid: t, ...r }) })
      )) : a.push(
        // Get, update or create app (upsert) using app uid
        s.fetch(`/api/apps/${t}`, { method: "PUT", body: JSON.stringify({ type: e, ...r }) })
      );
      const o = (await Promise.allSettled(a)).findLast(
        (l) => l.status === "fulfilled" && l.value?.ok
      ), c = o?.status === "fulfilled" && o.value;
      if (c)
        return await c.json();
      throw new Error(`Error getting or creating app ${t}`);
    }, "queryFn")
  };
}
n(C2, "getOrCreateAppOptions");
function $2(s) {
  return {
    mutationFn: /* @__PURE__ */ n(async ({ name: e, members: i, type: r, uid: a }) => await (await s.fetch("/api/apps", {
      method: "POST",
      body: JSON.stringify({
        name: e,
        members: i,
        type: r,
        uid: a
      })
    })).json(), "mutationFn"),
    onSettled: /* @__PURE__ */ n(async () => {
      await s.queryClient.invalidateQueries({ queryKey: ["apps"] });
    }, "onSettled")
  };
}
n($2, "getCreateAppMutationOptions");
function p1(s) {
  return new St(s.queryClient, $2(s));
}
n(p1, "getCreateAppMutation");
function k2(s, t) {
  const e = s.queryClient, i = ["apps", t.uid || t.id];
  return {
    mutationFn: /* @__PURE__ */ n(async ({ subscribe: a }) => {
      if (t.id >= 1) {
        const o = await s.fetch(`/api/apps/${t.id}/${a ? "subscribe" : "unsubscribe"}`, {
          method: "POST"
        });
        if (!o.ok)
          throw await o.json();
      } else
        throw new Error(`Could not subscribe to app ${t.uid || t.id}.`);
    }, "mutationFn"),
    onMutate: /* @__PURE__ */ n((a) => {
      let o;
      return e.setQueryData(i, (l) => (o = l.is_subscribed, { ...l, is_subscribed: a.subscribe })), {
        previousSubscribe: o,
        subscribe: a.subscribe
      };
    }, "onMutate"),
    onError(a, o, c) {
      c && c.previousSubscribe !== void 0 && e.setQueryData(i, (l) => ({ ...l, is_subscribed: c?.previousSubscribe }));
    }
  };
}
n(k2, "getAppSubscribeMutationOptions");
function _2(s, t = [rt.ChatRoom, rt.PrivateChat], e) {
  const i = new URLSearchParams({
    count_only: "true",
    unread: "true",
    uid: "false"
  });
  e && i.append("member", e), t?.forEach((o) => i.append("type", o));
  const r = `/api/apps?${i.toString()}`;
  return nr(s, ["apps", "unread", t, e], r);
}
n(_2, "getAppsUnreadOptions");
function P2(s, t = {}, e, i, r, a, o) {
  return {
    ...t,
    initialPageParam: 0,
    queryKey: ["apps", "list", e, i, a, o],
    queryFn: /* @__PURE__ */ n(async (c) => {
      const l = new URLSearchParams();
      i && l.append("member", i), c.pageParam && typeof c.pageParam == "number" && l.append("skip", c.pageParam?.toString()), e?.forEach((f) => l.append("type", f)), l.append("order_by", a);
      const u = r?.();
      u && l.append("q", u), l.append("uid", "false");
      const b = `/api/apps?${l.toString()}`, v = await (await s.fetch(b)).json();
      return v.data = v.data || [], v;
    }, "queryFn"),
    getNextPageParam: /* @__PURE__ */ n((c) => {
      if (c.end && c?.end < c?.count)
        return c.end;
    }, "getNextPageParam")
  };
}
n(P2, "getAppListOptions");
function S2(s) {
  const t = Array.from(s.values()).map((r) => r.result.data).filter(cn), e = s.size ? Array.from(s.values()).reduce((r, a) => r || a.result.isPending, !1) : !1;
  return {
    result: {
      data: s.size ? {
        data: t,
        count: t.length
      } : void 0,
      isPending: e
    }
  };
}
n(S2, "mapMultipleAppsAsAppList");
var me = /* @__PURE__ */ ((s) => (s.Default = "", s.Select = "select", s.Star = "star", s.Unstar = "unstar", s.Subscribe = "subscribe", s.Unsubscribe = "unsubscribe", s.Pin = "pin", s.Unpin = "unpin", s.Follow = "follow", s.Unfollow = "unfollow", s.Download = "download", s.Preview = "preview", s.Open = "open", s.Edit = "edit", s.Trash = "trash", s.DeleteForever = "delete-forever", s.Restore = "restore", s))(me || {});
const Pf = class Pf {
  get weavy() {
    return this.weavyContext?.value;
  }
  constructor(t) {
    t.addController(this), this.host = t, this.whenWeavyContext = new Promise((e) => this.resolveWeavyContext = e), this.setContexts();
  }
  /**
   * Initiates context consumers
   */
  async setContexts() {
    await gi(this.host), this.weavyContext = new He(this.host, { context: Xt, subscribe: !0 });
  }
  /**
   * Dispatch a `wy-action` event on the host.
   *
   * @fires {WyActionEventType} wy-action - Emitted when a conversation app is created and should be selected.
   */
  dispatchActionEvent(t) {
    const e = new CustomEvent("wy-action", {
      detail: { action: me.Select, app: t },
      bubbles: !0,
      composed: !0
    });
    return this.host.dispatchEvent(e);
  }
  /**
   * Create mutation controller.
   */
  async initMutation() {
    if (!this.addConversationMutation) {
      const t = await this.whenWeavyContext;
      this.addConversationMutation = p1(t);
    }
    return this.addConversationMutation;
  }
  /**
   * Create a conversation and trigger a `select` event.
   *
   * @param members - Array of members by id/uid for a new conversation.
   * @returns Whether the event was successful.
   * @fires {WyActionEventType} wy-action - Emitted when a conversation app is created and should be selected.
   */
  async create(t = []) {
    const e = await this.initMutation(), i = this.agent ?? this.host.agent, r = i ? { members: [i], type: ui.AgentChat } : { members: t, type: t.length === 1 ? ui.PrivateChat : ui.ChatRoom }, a = await e.mutate(r);
    return this.dispatchActionEvent(a), a;
  }
  hostUpdate() {
    this.weavyContext?.value && this.resolveWeavyContext?.(this.weavyContext?.value);
  }
};
n(Pf, "CreateConversationController");
let Kc = Pf;
var za;
const Sf = class Sf {
  constructor(t) {
    T(this, za, !1);
    t.addController(this), this.host = t, t.addEventListener("dragstart", () => this.isDragActive = !0), t.addEventListener("dragenter", () => this.isDragActive = !0), t.addEventListener("dragover", (e) => {
      e.preventDefault(), this.isDragActive = !0;
    }), t.addEventListener("dragleave", () => this.isDragActive = !1), t.addEventListener("dragend", () => this.isDragActive = !1), t.addEventListener("drop", (e) => this.handleDrop(e));
  }
  get isDragActive() {
    return p(this, za);
  }
  set isDragActive(t) {
    p(this, za) !== t && (P(this, za, t), this.host.requestUpdate());
  }
  handleDrop(t) {
    t.preventDefault();
    const e = [];
    t.dataTransfer?.items ? [...t.dataTransfer.items].forEach((i) => {
      if (i.kind === "file") {
        const r = i.getAsFile();
        r ? e.push(r) : console.error("Could not add file");
      }
    }) : t.dataTransfer?.files && [...t.dataTransfer.files].forEach((i) => {
      e.push(i);
    }), e.length && this.dispatchUploadFiles(e), this.isDragActive = !1;
  }
  dispatchUploadFiles(t) {
    const e = new CustomEvent("drop-files", { detail: { files: t } });
    return this.host.dispatchEvent(e);
  }
  hostDisconnected() {
  }
};
za = new WeakMap(), n(Sf, "DropZoneController");
let tu = Sf;
function Mh(s, t) {
  $e();
  const e = Hn({}, window.history.state, !0);
  if (!e.weavy || !e.weavy[s] || !Object.hasOwn(e.weavy[s], t))
    throw new Error("Property not found");
  return e.weavy[s][t];
}
n(Mh, "getBrowserStateProperty");
function Hy(s, t, e = "push", i) {
  if ($e(), t) {
    const r = Hn({}, window.history.state, !0);
    r.weavy ??= {}, r.weavy[s] = t, i = i && String(i) || window.location.href;
    try {
      e === "replace" ? window.history.replaceState(r, "", i) : window.history.pushState(r, "", i);
    } catch (a) {
      console.warn("history: Could not push history state.", a, t);
    }
  }
}
n(Hy, "setBrowserState");
function M2(s, t, e) {
  $e();
  const i = `${typeof s}:${t}`;
  e.forEach((r) => {
    try {
      const a = Mh(i, r), o = s[r];
      y1(o, a) && (s[r] = a);
    } catch {
    }
  });
}
n(M2, "restoreHistoryProperties");
function Uy(s, t, e, i = "push") {
  $e();
  const r = `${typeof s}:${t}`, a = {};
  e.forEach((o) => {
    a[o] = s[o];
  }), Hy(r, a, i);
}
n(Uy, "pushHistoryProperties");
function E2(s, t, e) {
  Uy(s, t, e, "replace");
}
n(E2, "updateHistoryProperties");
function y1(s, t) {
  return s && t && (Ze(s) || Ze(t)) ? s && Object.hasOwn(s, "id") || t && Object.hasOwn(t, "id") ? s.id !== t.id : !zs(s, t) : s !== t;
}
n(y1, "itemHasChanged");
const Mf = class Mf {
  constructor(t, e, i) {
    if (this._backCount = 0, this.restoreHistory = (r) => {
      if (this.prefixKey && this.properties) {
        M2(this.host, this.prefixKey, this.properties);
        try {
          this._backCount = Mh(this.prefixKey, "_backCount") || 0;
        } catch {
          this._backCount = 0;
        }
        this.updatePrevPropertyValues();
      }
    }, t.addController(this), this.host = t, i && (this.properties = i), e) {
      this.prefixKey = e;
      try {
        this._backCount = Mh(this.prefixKey, "_backCount") || this._backCount;
      } catch {
      }
    }
  }
  get hasBackNavigation() {
    return this._backCount > 0;
  }
  back() {
    window.history.back();
  }
  backAll() {
    window.history.go(this._backCount * -1);
  }
  observe(t, e) {
    if (this.properties = t, e) {
      this.prefixKey = e;
      try {
        this._backCount = Mh(this.prefixKey, "_hasBack") || this._backCount;
      } catch {
      }
    }
  }
  /**
   * Method to manually push history
   */
  pushCurrentState() {
    this.prefixKey && (Uy(this.host, this.prefixKey, this.properties), this._backCount++, Hy(this.prefixKey, { _backCount: this._backCount }, "replace"));
  }
  updatePrevPropertyValues() {
    this.properties && this.properties.forEach((t) => {
      this.prevPropertyValues ??= {};
      const e = this.host[t];
      this.prevPropertyValues[t] !== e && (this.prevPropertyValues[t] = e && JSON.parse(JSON.stringify(e)));
    });
  }
  hostConnected() {
    window.addEventListener("popstate", this.restoreHistory);
  }
  hostUpdated() {
    this.prefixKey && this.properties && (this.prevPropertyValues ? this.properties.some(
      (e) => {
        const i = this.prevPropertyValues?.[e], r = this.host[e];
        return y1(i, r);
      }
    ) && (Uy(
      this.prevPropertyValues,
      this.prefixKey,
      this.properties
    ), this._backCount++, Hy(this.prefixKey, { _backCount: this._backCount }, "replace")) : this.prevPropertyValues = {}, E2(this.host, this.prefixKey, this.properties), this.updatePrevPropertyValues());
  }
  hostDisconnected() {
    window.removeEventListener("popstate", this.restoreHistory);
  }
};
n(Mf, "HistoryController");
let $m = Mf;
const Ef = class Ef {
  get result() {
    return this._result && this.observer ? this.observer.trackResult(this._result) : this.observer?.getCurrentResult() ?? { isPending: !0 };
  }
  constructor(t) {
    t.addController(this), this.host = t, this.whenContext = new Promise((e) => this.resolveContext = e), this.whenQueryClient = new Promise((e) => this.resolveQueryClient = e), this.setContext();
  }
  async setContext() {
    await gi(this.host), this.context = new He(this.host, {
      context: Xt,
      subscribe: !0,
      callback: /* @__PURE__ */ n((t) => {
        t && (this.resolveContext?.(t), this.queryClient = t.queryClient, this.resolveQueryClient?.(t.queryClient));
      }, "callback")
    });
  }
  async trackInfiniteQuery(t, e = !0) {
    this.observerUnsubscribe?.();
    const i = await this.whenQueryClient;
    if (!i)
      throw new Error("No QueryClient provided");
    const r = new cb(i, t);
    this.observer = r, this.observerSubscribe(e);
  }
  observerSubscribe(t = !0) {
    if (this.queryClient && this.observer) {
      t ? this._result = this.observer.getOptimisticResult(
        this.observer.options
      ) : this._result = this.observer.getCurrentResult(), this.observerUnsubscribe = this.observer.subscribe(() => {
        if (this.observer) {
          const i = Gd(this.result, this.observer.getCurrentResult());
          i !== this._result && (this._result = i, this.host.requestUpdate());
        }
      }), this.observer.updateResult(), this.host.requestUpdate();
      let e;
      t ? e = this.observer.fetchOptimistic(this.observer.options) : e = this.queryClient.getQueryCache().get(
        this.observer.options.queryHash
      )?.promise, e?.catch(() => {
      }).finally(() => {
        this.observer?.updateResult();
      });
    }
  }
  untrackInfiniteQuery() {
    this.observerUnsubscribe?.(), this.observerUnsubscribe = void 0, this._result = void 0, this.observer = void 0, this.host.requestUpdate();
  }
  hostConnected() {
    this.observerSubscribe();
  }
  hostDisconnected() {
    this.observerUnsubscribe?.();
  }
};
n(Ef, "InfiniteQueryController");
let ar = Ef;
function By(s, t = !1) {
  for (s && !t && (s = s.nextElementSibling); s; ) {
    if (!(s instanceof HTMLElement))
      continue;
    const e = getComputedStyle(s);
    if (e.display !== "none") {
      if (e.display === "contents") {
        if (s.shadowRoot && s.shadowRoot.firstElementChild instanceof HTMLElement) {
          const i = By(s.shadowRoot.firstElementChild, !0);
          if (i)
            return i;
        }
        if (s.firstElementChild instanceof HTMLElement) {
          const i = By(s.firstElementChild, !0);
          if (i)
            return i;
        }
      }
      if (/absolute|sticky|fixed/.test(e.position) === !1)
        return s;
      s = s.nextElementSibling;
    }
  }
  return null;
}
n(By, "getNextPositionedChild");
function _u(s, t = !1) {
  if ($e(), s) {
    let e = getComputedStyle(s);
    const i = e.position === "absolute", r = t ? /(auto|scroll|overlay|hidden)/ : /(auto|overlay|scroll)/;
    if (e.position === "fixed" && document.scrollingElement)
      return document.scrollingElement;
    for (let a = s; a = a.parentElement || a.parentNode || a.host; )
      if (a instanceof Element && (e = getComputedStyle(a), !(i && e.position === "static") && r.test(e.overflow + e.overflowY + e.overflowX)))
        return a;
  }
  return document.scrollingElement || s;
}
n(_u, "getScrollParent");
function A2(s) {
  if (s && s.isConnected) {
    const t = _u(s);
    return t.clientHeight !== t.scrollHeight;
  }
  return !1;
}
n(A2, "hasScroll");
function R2(s, t = 32) {
  if (s) {
    const e = _u(s);
    return Math.abs(e.scrollTop + e.clientHeight - e.scrollHeight) < t;
  }
  return !1;
}
n(R2, "isParentAtBottom");
async function w1(s, t, e = !1) {
  if (t) {
    const i = _u(t);
    if (s === "top" ? i.scrollTop >= 1 : Math.abs(i.scrollTop + i.clientHeight - i.scrollHeight) > 1) {
      const a = s === "top" ? 0 : i.scrollHeight;
      e ? (await new Promise((o) => requestAnimationFrame(o)), i.scrollTo({
        top: a,
        left: 0,
        behavior: "smooth"
      })) : i.scrollTop = a;
    }
    await T2(s, i, e);
  }
}
n(w1, "scrollParentTo");
async function T2(s, t, e = !1) {
  await new Promise((i) => {
    let r = t.scrollTop, a = 0;
    const o = /* @__PURE__ */ n(() => {
      e && t.scrollTop === r && a++ === 1 && (t.scrollTop = s === "bottom" ? t.scrollHeight : 0), r = t.scrollTop, (s === "top" ? t.scrollTop >= 1 : Math.abs(t.scrollTop + t.clientHeight - t.scrollHeight) >= 1) ? requestAnimationFrame(o) : i(void 0);
    }, "scrollCheck");
    requestAnimationFrame(o);
  });
}
n(T2, "whenScrolledTo");
const L2 = 0, O2 = void 0;
function z2(s, t, e = !1) {
  $e(), t ??= /* @__PURE__ */ n(() => Promise.reject(new Error("No scroll function defined")), "whenNext");
  const i = e ? _u(s) : document.documentElement, r = i === document.documentElement ? document : i;
  let a = !1;
  const o = new IntersectionObserver(
    (c) => {
      c.forEach((l) => {
        if (l.isIntersecting && !a)
          if (a = !0, e && i && r instanceof HTMLElement) {
            const u = By(l.target) || l.target, b = i.scrollHeight, C = u.offsetTop, v = /* @__PURE__ */ n(() => {
              queueMicrotask(() => {
                if (b !== i.scrollHeight) {
                  if (u?.isConnected) {
                    const f = u.offsetTop - C;
                    i.scrollTop += f;
                  }
                  requestAnimationFrame(() => a = !1);
                } else
                  queueMicrotask(() => {
                    if (b !== i.scrollHeight) {
                      if (u?.isConnected) {
                        const f = u.offsetTop - C;
                        i.scrollTop += f;
                      }
                      requestAnimationFrame(() => a = !1);
                    } else
                      requestAnimationFrame(() => {
                        if (o.takeRecords().length && u?.isConnected) {
                          const f = u.offsetTop - C;
                          i.scrollTop += f;
                        }
                        requestAnimationFrame(() => a = !1);
                      });
                  });
              });
            }, "afterNext");
            t().then(v);
          } else
            t().then(() => {
              requestAnimationFrame(() => a = !1);
            });
      });
    },
    { root: r, threshold: L2, rootMargin: O2 }
  );
  return o.observe(s), o;
}
n(z2, "createScroller");
const Af = class Af {
  constructor(t, e = !1) {
    this.reverse = !1, this.isObservePending = !1, t.addController(this), this.host = t, this.reverse = e;
  }
  observe(t, e) {
    t && e && !t.isLoading && !this.isObservePending && (this.isObservePending = !0, requestAnimationFrame(() => {
      this.loadMoreRefElement = e, this.scroller?.disconnect(), this.scroller = z2(
        e,
        async () => {
          t.hasNextPage && !t.isFetching && (await t.fetchNextPage({ cancelRefetch: !1 }), this.reverse && await this.host.updateComplete);
        },
        this.reverse
      ), this.isObservePending = !1;
    }));
  }
  hostConnected() {
    this.loadMoreRefElement && this.scroller?.observe(this.loadMoreRefElement);
  }
  hostDisconnected() {
    this.scroller?.disconnect();
  }
};
n(Af, "InfiniteScrollController");
let or = Af;
const Rf = class Rf extends or {
  constructor(t) {
    super(t, !0);
  }
};
n(Rf, "ReverseInfiniteScrollController");
let Ny = Rf;
var tr, Ts;
const Tf = class Tf {
  constructor(t) {
    T(this, tr);
    T(this, Ts);
    if (P(this, tr, /* @__PURE__ */ new Map()), this.keyPrefix = "WEAVY_OFFLINE_CACHE", t)
      P(this, Ts, t);
    else
      try {
        $e(), P(this, Ts, window.sessionStorage);
      } catch {
        console.warn("Session storage not available.");
      }
  }
  resetPersistPropertiesCache() {
    p(this, tr).clear();
  }
  getStorageItem(t, e) {
    const i = p(this, Ts)?.getItem(`${t}-${e.toString()}`);
    if (i)
      return JSON.parse(i);
  }
  setStorageItem(t, e, i) {
    const r = JSON.stringify(i);
    r && p(this, Ts)?.setItem(`${t}-${e.toString()}`, r);
  }
  persistProperties(t, e, i, r, a) {
    const o = `${this.keyPrefix}:${r ? `${r}:` : ""}${typeof t}:${e}`, c = /* @__PURE__ */ new Map();
    for (const l of i) {
      if (!p(this, tr).has(l.name)) {
        const u = this.getStorageItem(o, l.name);
        u && (l.override || !t[l.name]) && (c.set(l.name, t[l.name]), t[l.name] = u), p(this, tr).set(l.name, u);
      }
      if (a?.(c), t[l.name] !== p(this, tr).get(l.name)) {
        const u = t[l.name];
        p(this, tr).set(l.name, u), this.setStorageItem(o, l.name, u);
      }
    }
  }
};
tr = new WeakMap(), Ts = new WeakMap(), n(Tf, "PersistStorageCache");
let qy = Tf;
var Ls, Fa;
const Lf = class Lf {
  constructor(t) {
    T(this, Ls);
    T(this, Fa);
    P(this, Ls, ""), this.properties = [], this.initialProperties = /* @__PURE__ */ new Map(), this.persistStorageCache = new qy(), t.addController(this), this.host = t;
  }
  get prefixKey() {
    return p(this, Ls);
  }
  set prefixKey(t) {
    t !== p(this, Ls) && (this.persistStorageCache.resetPersistPropertiesCache(), P(this, Ls, t), this.host.requestUpdate());
  }
  get cachePrefix() {
    return p(this, Fa);
  }
  set cachePrefix(t) {
    t !== p(this, Fa) && (this.persistStorageCache.resetPersistPropertiesCache(), P(this, Fa, t), this.host.requestUpdate());
  }
  observe(t, e, i, r) {
    this.properties = t, this.callback = r;
    const a = !!(this.prefixKey && this.prefixKey !== e || this.cachePrefix && this.cachePrefix !== i);
    this.properties.forEach((o) => {
      a && this.initialProperties.has(o.name) ? this.host[o.name] = this.initialProperties.get(o.name) : this.initialProperties.has(o.name) || this.initialProperties.set(o.name, this.host[o.name]);
    }), e && (this.prefixKey = e), i && (this.cachePrefix = i);
  }
  hostUpdate() {
    this.prefixKey && this.properties && this.host.weavy && this.persistStorageCache.persistProperties(this.host, this.prefixKey, this.properties, this.cachePrefix ? `${this.host.weavy.cachePrefix}:${this.cachePrefix}` : this.host.weavy.cachePrefix, this.callback);
  }
};
Ls = new WeakMap(), Fa = new WeakMap(), n(Lf, "PersistStateController");
let Hs = Lf;
const Of = class Of {
  constructor(t) {
    this.observers = [], this.conditions = {}, this.resizer = new ResizeObserver((e) => {
      let i = !1;
      for (const r of e)
        i = this.checkConditions(r) || i;
      i && this.host.requestUpdate();
    }), t.addController(this), this.host = t;
  }
  checkConditions(t) {
    let e = !1;
    return this.observers.forEach((i) => {
      if (t.target === i.target) {
        const r = this.conditions[i.name], a = i.condition(t);
        r !== a && (this.conditions[i.name] = a, e = !0);
      }
    }), e;
  }
  observe(t) {
    const e = t.target, i = e.getBoundingClientRect();
    this.observers.push(t), this.resizer.observe(e), this.checkConditions({
      contentBoxSize: [
        {
          inlineSize: e.clientWidth,
          blockSize: e.clientHeight
        }
      ],
      borderBoxSize: [
        {
          inlineSize: i.width,
          blockSize: i.height
        }
      ],
      devicePixelContentBoxSize: [
        {
          inlineSize: i.width * window.devicePixelRatio,
          blockSize: i.height * window.devicePixelRatio
        }
      ],
      contentRect: i,
      target: e
    });
  }
  reset() {
    this.conditions = {}, this.observers.length = 0, this.resizer.disconnect();
  }
  hostConnected() {
    this.observers.forEach((t) => {
      this.resizer.observe(t.target);
    });
  }
  hostDisconnected() {
    this.resizer.disconnect();
  }
};
n(Of, "ResizeController");
let km = Of;
const zf = class zf {
  constructor(t, e) {
    this.additionalTargets = /* @__PURE__ */ new Set(), this.setExportParts = (i) => {
      (!this.host.hasAttribute("exportparts") || this.shadowParts) && (this.shadowParts ??= /* @__PURE__ */ new Set(), i.forEach((r) => {
        r.part.forEach((a) => this.shadowParts?.add(a)), r.getAttribute("exportparts")?.split(", ").forEach((a) => this.shadowParts?.add(a));
      }), this.shadowParts.size && this.host.setAttribute("exportparts", Array.from(this.shadowParts.values()).join(", ")));
    }, t.addController(this), this.host = t, this.addLocalName = e;
  }
  addPartsFrom(t) {
    t && !this.additionalTargets.has(t) && (this.setExportParts([t]), this.observer?.observe(t, {
      attributeFilter: ["part", "exportparts"]
    }), this.additionalTargets.add(t));
  }
  async hostConnected() {
    if (await this.host.updateComplete, em(this.host) && this.host.shadowRoot) {
      const t = Array.from(this.host.shadowRoot.querySelectorAll("[part], [exportparts]"));
      this.setExportParts(t), this.observer = new MutationObserver((e) => {
        const i = e.flatMap((r) => r.type === "attributes" ? [r.target] : Array.from(r.addedNodes)).filter((r) => r instanceof Element);
        this.setExportParts(i);
      }), this.observer.observe(this.host.shadowRoot, {
        subtree: !0,
        childList: !0,
        attributeFilter: ["part", "exportparts"]
      }), Array.from(this.additionalTargets).forEach((e) => {
        this.observer?.observe(e, {
          attributeFilter: ["part", "exportparts"]
        });
      });
    }
  }
  hostUpdated() {
    em(this.host) && this.addLocalName !== !1 && !this.host.part.contains(this.host.localName) && (this.addLocalName || getComputedStyle(this.host).display !== "contents") && this.host.part.add(this.host.localName);
  }
  hostDisconnected() {
    this.observer?.disconnect();
  }
};
n(zf, "ShadowPartsController");
let W = zf;
const Ff = class Ff {
  constructor(t) {
    this.delay = NaN, this.sleep = !1, t.addController(this), this.host = t;
  }
  createObserver(t) {
    this.scrollObserver?.disconnect(), this.swipeElement = t, this.scrollObserver = new IntersectionObserver(
      (e) => {
        e.forEach(async (i) => {
          i.isIntersecting && !this.sleep && i.intersectionRatio >= 1 && (this.sleep = !0, Number.isNaN(this.delay) || await new Promise((r) => {
            setTimeout(r, this.delay);
          }), i.target === this.prevElement && this.whenPrev && await this.whenPrev(), i.target === this.nextElement && this.whenNext && await this.whenNext(), await this.host.updateComplete);
        });
      },
      { root: t, threshold: 1, rootMargin: "0px" }
    );
  }
  observe(t, e) {
    this.prevElement && (this.scrollObserver?.unobserve(this.prevElement), this.prevElement = void 0), this.nextElement && (this.scrollObserver?.unobserve(this.nextElement), this.nextElement = void 0), t && (this.scrollObserver?.observe(t), this.prevElement = t), e && (this.scrollObserver?.observe(e), this.nextElement = e), this.sleep = !1;
  }
  clearObserver() {
    this.prevElement = void 0, this.nextElement = void 0, this.scrollObserver?.disconnect(), this.sleep = !1;
  }
  hostConnected() {
    this.scrollObserver && (this.prevElement && this.scrollObserver.observe(this.prevElement), this.nextElement && this.scrollObserver.observe(this.nextElement), this.sleep = !1);
  }
  hostDisconnected() {
    this.scrollObserver?.disconnect();
  }
};
n(Ff, "SwipeScrollController");
let Wy = Ff;
var Ia, Id;
const If = class If {
  constructor(t, e) {
    T(this, Ia);
    T(this, Id);
    P(this, Ia, !0), this.styles = [], t.addController(this), this.host = t, e && (this.styles = e);
  }
  get themeColor() {
    return p(this, Id);
  }
  set themeColor(t) {
    P(this, Id, t), this.checkThemeUpdate();
  }
  checkThemeUpdate() {
    const t = this.themeColor || Ay(this.host) || Ry();
    if (t && t !== this._resolvedThemeColor) {
      this._resolvedThemeColor = t, console.info("Configuring theme", this._resolvedThemeColor);
      const e = Bb(this._resolvedThemeColor).join(""), i = j`
        :host {
          ${p0(e)};
        }
      `, r = this.host.renderRoot;
      y0(r, [...this.styles, i]);
    }
  }
  hostUpdate() {
    p(this, Ia) && (this.checkThemeUpdate(), P(this, Ia, !1));
  }
  hostConnected() {
    this.cssObserverDisconnect = Vb(this.host, () => this.checkThemeUpdate()), this.metaObserverDisconnect = Hb(() => this.checkThemeUpdate());
  }
  hostDisconnected() {
    this.cssObserverDisconnect?.(), this.metaObserverDisconnect?.();
  }
};
Ia = new WeakMap(), Id = new WeakMap(), n(If, "ThemeController");
let ce = If;
const Df = class Df {
  constructor(t) {
    this.registrationRequested = !1, this.typingTimeout = null, this.discardTime = 5 * 1e3, this.typingMembers = [], this.names = [], this.ellipsis = "", this.handleRealtimeTyping = (e) => {
      e.entity.id === this.appId && e.actor.id !== this.userId && (this.setTypers(e.actor), this.updateTyping());
    }, this.handleRealtimeStopTyping = (e) => {
      this.typingMembers.forEach((i, r) => {
        i.id === e.actor.id && this.typingMembers.splice(r, 1);
      }), this.updateTyping();
    }, t.addController(this), this.host = t, this.setContexts();
  }
  get weavy() {
    return this.weavyContext?.value;
  }
  get componentFeatures() {
    return this._componentFeatures;
  }
  get appId() {
    return this._appId;
  }
  set appId(t) {
    t !== this._appId && (this.typingMembers = [], this.unregisterRealtime(), this._appId = t, this.registerRealtime());
  }
  get userId() {
    return this._userId;
  }
  set userId(t) {
    t !== this._userId && (this.typingMembers = [], this.unregisterRealtime(), this._userId = t, this.registerRealtime());
  }
  async setContexts() {
    this.whenWeavyContext = new Promise((t) => this.resolveWeavyContext = t), this.whenComponentFeaturesContext = new Promise((t) => this.resolveComponentFeaturesContext = t), await gi(this.host), this.weavyContext = new He(this.host, { context: Xt, subscribe: !0 }), this.componentFeaturesContext = new He(this.host, {
      context: xu,
      subscribe: !0
    });
  }
  hostUpdate() {
    this.weavyContext?.value && this.resolveWeavyContext?.(this.weavyContext?.value);
    const t = this.componentFeaturesContext && this.componentFeaturesContext.value?.allowsFeature(_.Typing) !== this._componentFeatures?.allowsFeature(_.Typing);
    t && (this._componentFeatures = this.componentFeaturesContext?.value, this.componentFeaturesContext?.value && this.resolveComponentFeaturesContext?.(this.componentFeaturesContext.value), t && (this.unregisterRealtime(!0), this.registerRealtime()));
  }
  async registerRealtime() {
    !this.registrationRequested && this.appId && this._userId && (this.registrationRequested = !0, await Promise.all([this.whenWeavyContext, this.whenComponentFeaturesContext]), this.componentFeatures?.allowsFeature(_.Typing) && (this.weavy?.subscribe(`a${this.appId}`, "typing", this.handleRealtimeTyping), this.weavy?.subscribe(`a${this.appId}`, "message_created", this.handleRealtimeStopTyping)), this.registrationRequested = !1);
  }
  async unregisterRealtime(t = !1) {
    !this.registrationRequested && this.appId && this.userId && (!t && await this.whenWeavyContext, this.weavy?.unsubscribe(`a${this.appId}`, "typing", this.handleRealtimeTyping), this.weavy?.unsubscribe(`a${this.appId}`, "message_created", this.handleRealtimeStopTyping));
  }
  /**
   * @fires typing
   */
  updateTyping() {
    this.typingTimeout && (clearTimeout(this.typingTimeout), this.typingTimeout = null);
    const t = Date.now();
    if (this.typingMembers.forEach((i, r) => {
      t - i.time > this.discardTime && this.typingMembers.splice(r, 1);
    }), this.typingMembers.length) {
      const i = Date.now(), r = Math.round(
        (i - Math.max.apply(
          null,
          this.typingMembers.map((a) => a.time)
        )) / 1e3
      ) % 3 + 1;
      this.ellipsis = ".".repeat(r), this.names = this.typingMembers.map((a) => a.name).sort(), this.typingTimeout = window.setTimeout(() => this.updateTyping(), 1e3);
    } else
      this.names = [];
    this.host.requestUpdate();
    const e = new CustomEvent("typing", {
      bubbles: !0,
      composed: !1,
      detail: { count: this.typingMembers.length }
    });
    this.host.dispatchEvent(e);
  }
  setTypers(t) {
    this.typingMembers.forEach((i, r) => {
      i.id === t.id && this.typingMembers.splice(r, 1);
    });
    const e = { ...t, time: Date.now() };
    this.typingMembers.push(e);
  }
  hostDisconnected() {
    this.typingTimeout && (clearTimeout(this.typingTimeout), this.typingTimeout = null), this.typingMembers.length = 0, this.names.length = 0, this.weavy && this.unregisterRealtime(!0);
  }
};
n(Df, "TypingController");
let eu = Df;
var Da;
const Vf = class Vf {
  constructor(t) {
    T(this, Da);
    this.appTypes = [], P(this, Da, 0), this.registrationRequested = !1, this.handleRefresh = () => {
      this.unreadQuery.result.refetch();
    }, t.addController(this), this.host = t, this.unreadQuery = new ve(t), this.whenWeavyContext = new Promise((e) => this.resolveWeavyContext = e), this.setContexts(), this.registerRealtime();
  }
  /**
   * The number of unread items for the current scope
   */
  get unread() {
    return p(this, Da);
  }
  /**
   * Is the unread count pending?
   */
  get isUnreadPending() {
    return this.unreadQuery.result.isPending;
  }
  /**
   * The underlying query result for the unread count.
   */
  get unreadResult() {
    return this.unreadQuery.result;
  }
  get weavy() {
    return this.weavyContext?.value;
  }
  /**
   * Initiates context consumers
   */
  async setContexts() {
    await gi(this.host), this.weavyContext = new He(this.host, { context: Xt, subscribe: !0 });
  }
  /**
   * Dispatch a `wy-unread` event on the host.
   *
   * @fires {WyUnreadEventType} wy-unread - Emitted when the number of unread items change.
   */
  dispatchUnreadEvent() {
    const t = new CustomEvent("wy-unread", {
      detail: { unread: this.unread },
      bubbles: !1,
      composed: !0
    });
    this.host.dispatchEvent(t);
  }
  /**
   * Register realtime handlers.
   */
  async registerRealtime() {
    this.registrationRequested || (this.registrationRequested = !0, await this.whenWeavyContext, this.weavy?.subscribe(null, "message_created", this.handleRefresh), this.weavy?.subscribe(null, "app_marked", this.handleRefresh), this.registrationRequested = !1);
  }
  /**
   * Unregister realtime handlers.
   *
   * @param skipAwait - Skip waiting for any context.
   */
  async unregisterRealtime(t = !1) {
    this.registrationRequested || (!t && await this.whenWeavyContext, this.weavy?.unsubscribe(null, "message_created", this.handleRefresh), this.weavy?.unsubscribe(null, "app_marked", this.handleRefresh));
  }
  async track(t, e) {
    this.appTypes = t, this.agent = e;
    const i = await this.whenWeavyContext;
    await this.unreadQuery.trackQuery(_2(i, t, this.agent), !0);
  }
  hostUpdate() {
    this.weavyContext?.value && this.resolveWeavyContext?.(this.weavyContext?.value);
    const t = this.unreadQuery.result?.data?.count ?? 0;
    t !== this.unread && (P(this, Da, t), this.dispatchUnreadEvent(), this.host.requestUpdate());
  }
  hostDisconnected() {
    this.weavy && this.unregisterRealtime(!0);
  }
};
Da = new WeakMap(), n(Vf, "UnreadConversationsController");
let Qc = Vf;
var _t = /* @__PURE__ */ ((s) => (s.All = "", s.Activity = "activity", s.Mention = "mention", s.Reaction = "reaction", s))(_t || {});
function f1(s) {
  return !!(Ze(s) && (typeof s.q == "string" || typeof s.tag == "string" || typeof s.trashed == "boolean" || typeof s.order_by == "string"));
}
n(f1, "isQueryFilter");
function m1(s) {
  const t = s.at(-1);
  if (f1(t) && t.order_by) {
    const [e, i] = t.order_by.split(" ");
    return { by: e, descending: i === "desc" };
  }
  return {};
}
n(m1, "getQueryOrderBy");
function v1(s, t, e = {}, i) {
  if (s)
    if (s = JSON.parse(JSON.stringify(s)), s?.pages) {
      let r = -1;
      return {
        pages: [...s.pages.map((o, c) => {
          if (r >= 0)
            return o;
          const l = o.data || [], u = l.filter(
            (b) => b.id !== t.id && (!i || b.id !== i)
          );
          return e && e.by ? (r = u.findIndex((b) => {
            let C = e.by && b[e.by], v = e.by && t[e.by];
            if (e.by === "updated_at" && (C ??= b.created_at, v ??= t.created_at), typeof C == "string" && typeof v == "string") {
              const f = C.localeCompare(v, void 0, {
                sensitivity: "base",
                numeric: !0
              });
              return e.descending ? f < 0 : f > 0;
            }
            return e.by === "id" && typeof v == "number" && v < 0 && typeof C == "number" ? C < 0 ? e.descending ? C > v : C < v : e.descending : C && v && (e.descending ? C < v : C > v);
          }), r >= 0 ? (u.splice(r, 0, t), o.data = [...u], o.end && (o.end += 1 + u.length - l.length)) : s && c == s.pages.length - 1 && o.end === o.count ? (o.data = [...u, t], o.end && (o.end += 1 + u.length - l.length)) : o.data = [...u]) : c === 0 && (e.descending ? o.data = [t, ...u] : o.data = [...u, t], o.end && (o.end += 1 + u.length - l.length)), o;
        }) ?? []],
        pageParams: [...s.pageParams]
      };
    } else if (s?.data?.length) {
      let r = -1;
      const a = [
        ...s.data?.filter(
          (c) => c.id !== t.id && (!i || c.id !== i)
        ) || []
      ];
      let o = s.count;
      return e && e.by ? (r = a.findIndex((c) => {
        let l = c[e.by], u = t[e.by];
        if (e.by === "updated_at" && (l ??= c.created_at, u ??= t.created_at), typeof l == "string" && typeof u == "string") {
          const b = l.localeCompare(u, void 0, {
            sensitivity: "base",
            numeric: !0
          });
          return e.descending ? b < 0 : b > 0;
        }
        return e.by === "id" && typeof u == "number" && u < 0 && typeof l == "number" ? l < 0 ? e.descending ? l > u : l < u : e.descending : l && u && (e.descending ? l < u : l > u);
      }), r >= 0 ? a.splice(r, 0, t) : (a.push(t), o++)) : (e.descending ? a.unshift(t) : a.push(t), o++), {
        data: a,
        count: o
      };
    } else s?.count;
  return s;
}
n(v1, "addToQueryData");
function g1(s, t, e) {
  const i = t === void 0 ? () => !0 : t instanceof Function ? t : (r) => r.id === t;
  return s && (s = JSON.parse(JSON.stringify(s)), s.pages ? {
    pages: s.pages.map((a) => (a.data && (a.data = [
      ...a.data.map((o) => {
        if (i(o)) {
          const c = { ...o };
          o = e(c);
        }
        return o;
      })
    ]), a)) ?? [],
    pageParams: s.pageParams
  } : s.data?.length ? {
    ...s,
    data: [
      ...s.data?.map((r) => {
        if (i(r)) {
          const a = { ...r };
          r = e(a);
        }
        return r;
      }) || []
    ]
  } : s.data ? {
    data: [
      ...s.data.map((a) => {
        if (i(a)) {
          const o = { ...a };
          a = e(o);
        }
        return a;
      })
    ],
    count: s.count
  } : s);
}
n(g1, "updateQueryData");
function b1(s, t) {
  if (t !== void 0) {
    const e = t instanceof Function ? t : (i) => i.id === t;
    if (s) {
      if (s = JSON.parse(JSON.stringify(s)), s.pages)
        return {
          pages: s.pages.map((r) => (r.data && (r.data = [...r.data.filter((a) => !e(a))]), r)) ?? [],
          pageParams: s.pageParams
        };
      if (s.data?.length) {
        const i = s.data?.length;
        let r = s.count;
        const a = [...s.data?.filter((o) => !e(o)) || []];
        return i !== a.length && r--, {
          data: a,
          count: r
        };
      } else s?.count;
    }
  }
  return s;
}
n(b1, "removeQueryData");
const Wl = /* @__PURE__ */ n((s, t, e, i, r) => (i ??= m1(t), s.setQueryData(t, (a) => v1(a, e, i, r))), "addCacheItem"), _m = /* @__PURE__ */ n((s, t, e, i) => {
  uw(t).forEach((r) => {
    s.getQueryCache().findAll(r).forEach((o) => {
      const c = o.state.data, l = m1(o.queryKey);
      o.setData(v1(c, e, l, i));
    });
  });
}, "addCacheItems"), Ee = /* @__PURE__ */ n((s, t, e, i) => s.setQueryData(t, (r) => g1(
  r,
  e,
  i
)), "updateCacheItem"), ht = /* @__PURE__ */ n((s, t, e, i) => {
  uw(t).forEach((r) => {
    s.setQueriesData(r, (a) => g1(
      a,
      e,
      i
    ));
  });
}, "updateCacheItems"), Pu = /* @__PURE__ */ n((s, t, e) => s.setQueryData(t, (i) => b1(
  i,
  e
)), "removeCacheItem"), F2 = /* @__PURE__ */ n((s, t, e) => {
  s.setQueriesData(t, (i) => b1(i, e));
}, "removeCacheItems"), jl = /* @__PURE__ */ n((s, t, e) => {
  s.setQueriesData(t, (i) => {
    const { count: r } = i;
    return { count: e(r) };
  });
}, "updateCacheItemsCount");
function qp(s, t, e, i = 1) {
  if (!s)
    return;
  const r = s.getQueryData(t);
  r?.pages?.length && r.pages.length > 1 && s.setQueryData(
    t,
    (a) => ({
      pages: a.pages.slice(0, i),
      pageParams: a.pageParams.slice(0, i)
    }),
    e
  );
}
n(qp, "keepPages");
function Kl(s, t, e) {
  const i = s.getQueryCache().find({ queryKey: t });
  if (i && i.state.data) {
    const r = i.state.data.pages.flatMap((a) => a.data).filter((a) => a && a.id < 0).sort((a, o) => a && o ? a.id - o.id : 0);
    return r.length ? e ? r[r.length - 1] : r[0] : null;
  }
  return null;
}
n(Kl, "getPendingCacheItem");
function Yd(s, t, e) {
  const i = s.getQueryCache().find({ queryKey: t });
  return i && i.state.data ? i.state.data.pages.flatMap((r) => r.data).find((r) => r?.id === e) : null;
}
n(Yd, "getCacheItem");
function pi(s) {
  return (s?.pages.flatMap((t) => t.data) || []).filter((t) => t);
}
n(pi, "getFlatInfiniteResultData");
function Pm(s) {
  return !s || !s?.pages.some((t) => t.data?.length);
}
n(Pm, "isInfiniteResultDataEmpty");
function I2(s, t = _t.All, e, i = {}) {
  return {
    ...i,
    initialPageParam: 0,
    queryKey: ["notifications", "list", e, t],
    queryFn: /* @__PURE__ */ n(async (r) => {
      const a = new URLSearchParams({
        skip: r.pageParam?.toString() || "0",
        type: t
      }), o = `/api/${e ? `apps/${e.toString()}/` : ""}notifications?${a.toString()}`, l = await (await s.fetch(o)).json();
      return l.data = l.data || [], l;
    }, "queryFn"),
    getNextPageParam: /* @__PURE__ */ n((r) => {
      if (r.end && r.end < r.count)
        return r.end;
    }, "getNextPageParam")
  };
}
n(I2, "getNotificationsOptions");
function D2(s, t = _t.All, e) {
  const i = s.queryClient.getQueryData(["notifications", "list", e, t])?.pages.flatMap((a) => a.data);
  let r;
  return i?.forEach((a) => {
    r = r && a && r.id > a.id ? r : a;
  }), r;
}
n(D2, "getLastNotification");
function V2(s, t) {
  return {
    mutationFn: /* @__PURE__ */ n(async ({ notificationId: i }) => {
      const r = new URL(`/api/${t ? `apps/${t.toString()}/` : ""}notifications/mark`, s.url);
      i && r.searchParams.append("id", i.toString()), await s.fetch(r, { method: "PUT" });
    }, "mutationFn"),
    onMutate: /* @__PURE__ */ n((i) => {
      const r = [];
      return ht(
        s.queryClient,
        { queryKey: t ? ["notifications", "list", t] : ["notifications", "list"], exact: !1 },
        void 0,
        (a) => (r.push({ id: a.id, is_unread: a.is_unread }), { ...a, is_unread: !1 })
      ), t && r.length && ht(
        s.queryClient,
        { queryKey: ["notifications", "list"], exact: !1 },
        (a) => !!r.find((o) => o.id === a.id && a.is_unread),
        (a) => ({
          ...a,
          is_unread: !1
        })
      ), t || jl(
        s.queryClient,
        { queryKey: ["notifications", "unread"], exact: !1 },
        () => 0
      ), jl(
        s.queryClient,
        {
          queryKey: t ? ["apps", "notifications", "unread", t] : ["apps", "notifications", "unread"],
          exact: !1
        },
        () => 0
      ), { changedNotifications: r };
    }, "onMutate"),
    onSuccess: /* @__PURE__ */ n(async () => {
      t && await s.queryClient.invalidateQueries({ queryKey: ["notifications", "unread"], exact: !1 });
    }, "onSuccess"),
    onSettled: /* @__PURE__ */ n(async () => {
      await s.queryClient.invalidateQueries({ queryKey: ["notifications", "list"], exact: !1 }), await s.queryClient.invalidateQueries({ queryKey: ["notifications", "unread"], exact: !1 }), await s.queryClient.invalidateQueries({ queryKey: ["apps", "notifications", "unread"], exact: !1 });
    }, "onSettled"),
    onError: /* @__PURE__ */ n((i, r, a) => {
      console.error(i.message);
    }, "onError")
  };
}
n(V2, "getMarkNotificationsMutationOptions");
function H2(s, t) {
  return new St(s.queryClient, V2(s, t));
}
n(H2, "getMarkNotificationsMutation");
function U2(s) {
  return {
    mutationFn: /* @__PURE__ */ n(async ({ markAsRead: e, notificationId: i }) => {
      const r = `/api/notifications/${i}/mark`;
      await s.fetch(r, { method: e ? "PUT" : "DELETE" });
    }, "mutationFn"),
    onMutate: /* @__PURE__ */ n((e) => {
      const i = /* @__PURE__ */ new Map();
      ht(
        s.queryClient,
        { queryKey: ["notifications", "list"], exact: !1 },
        e.notificationId,
        (r) => (!!r.is_unread === e.markAsRead && i.set(r.id, r), { ...r, is_unread: !e.markAsRead })
      ), i.size && (jl(
        s.queryClient,
        {
          queryKey: ["notifications", "unread"],
          predicate: /* @__PURE__ */ n((r) => r.queryKey[3] === "" || r.queryKey[3] === i.values().next().value?.type, "predicate"),
          exact: !1
        },
        (r) => Math.max(0, r + (e.markAsRead ? -1 : 1))
      ), i.forEach((r) => {
        r.link?.app && jl(
          s.queryClient,
          {
            queryKey: ["apps", "notifications", "unread"],
            predicate: /* @__PURE__ */ n((a) => {
              const o = a.queryKey[3] === r.link?.app?.id || a.queryKey[3] === r.link?.app?.uid, c = a.queryKey[4] === "" || a.queryKey[4] === r.type;
              return o && c;
            }, "predicate"),
            exact: !1
          },
          (a) => Math.max(0, a + (e.markAsRead ? -1 : 1))
        );
      }));
    }, "onMutate"),
    onError: /* @__PURE__ */ n(async (e, i) => {
      ht(
        s.queryClient,
        { queryKey: ["notifications", "list"], exact: !1 },
        i.notificationId,
        (r) => ({
          ...r,
          is_unread: i.markAsRead
        })
      ), await s.queryClient.invalidateQueries({ queryKey: ["notifications", "unread"], exact: !1 }), await s.queryClient.invalidateQueries({ queryKey: ["apps", "notifications", "unread"], exact: !1 });
    }, "onError")
  };
}
n(U2, "getMarkNotificationMutationOptions");
function x1(s) {
  return new St(s.queryClient, U2(s));
}
n(x1, "getMarkNotificationMutation");
function B2(s, t = _t.All, e, i = {}) {
  const r = new URLSearchParams({
    type: t,
    count_only: "true",
    unread: "true"
  }), a = `/api/${e ? `apps/${e.toString()}/` : ""}notifications?${r.toString()}`;
  return nr(s, e ? ["apps", "notifications", "unread", e, t] : ["notifications", "unread", t], a, i);
}
n(B2, "getUnreadOptions");
var Va;
const Hf = class Hf {
  constructor(t) {
    T(this, Va);
    this.typeFilter = _t.All, P(this, Va, 0), this.registrationRequested = !1, this.handleRefresh = () => {
      this.unreadQuery.result.refetch();
    }, t.addController(this), this.host = t, this.unreadQuery = new ve(t), this.whenWeavyContext = new Promise((e) => this.resolveWeavyContext = e), this.setContexts(), this.registerRealtime();
  }
  /**
   * The number of unread notifications for the current scope
   */
  get unread() {
    return p(this, Va);
  }
  /**
   * Is the unread count pending?
   */
  get isUnreadPending() {
    return this.unreadQuery.result.isPending;
  }
  /**
   * The underlying query result for the unread count.
   */
  get unreadResult() {
    return this.unreadQuery.result;
  }
  get weavy() {
    return this.weavyContext?.value;
  }
  /**
   * Initiates context consumers
   */
  async setContexts() {
    await gi(this.host), this.weavyContext = new He(this.host, { context: Xt, subscribe: !0 });
  }
  /**
   * Dispatch a `wy-unread` event on the host.
   *
   * @fires {WyUnreadEventType} wy-unread - Emitted when the number of unread notifications change.
   */
  dispatchNotificationUnreadEvent() {
    const t = new CustomEvent("wy-unread", {
      detail: { unread: this.unread },
      bubbles: !1,
      composed: !0
    });
    this.host.dispatchEvent(t);
  }
  /**
   * Register realtime handlers.
   */
  async registerRealtime() {
    this.registrationRequested || (this.registrationRequested = !0, await this.whenWeavyContext, this.weavy?.subscribe(null, "notification_created", this.handleRefresh), this.weavy?.subscribe(null, "notification_updated", this.handleRefresh), this.weavy?.subscribe(null, "notifications_marked", this.handleRefresh), this.registrationRequested = !1);
  }
  /**
   * Unregister realtime handlers.
   *
   * @param skipAwait - Skip waiting for any context.
   */
  async unregisterRealtime(t = !1) {
    this.registrationRequested || (!t && await this.whenWeavyContext, this.weavy?.unsubscribe(null, "notification_created", this.handleRefresh), this.weavy?.unsubscribe(null, "notification_updated", this.handleRefresh), this.weavy?.unsubscribe(null, "notifications_marked", this.handleRefresh));
  }
  /**
   * Tracks unread data. Initiates the query data with the given filtering scope.
   *
   * @param typeFilter - The notification types to track.
   * @param appId - Optional app id for the filtering scope.
   */
  async track(t, e) {
    this.appId = e, this.typeFilter = t;
    const i = await this.whenWeavyContext;
    this.markNotificationsMutation = H2(i, this.appId), await this.unreadQuery.trackQuery(B2(i, this.typeFilter, this.appId), !0);
  }
  async markAllAsRead() {
    const t = await this.whenWeavyContext, e = D2(t, _t.All, this.appId)?.id;
    await this.markNotificationsMutation?.mutate({ notificationId: e });
  }
  hostUpdate() {
    this.weavyContext?.value && this.resolveWeavyContext?.(this.weavyContext?.value);
    const t = this.unreadQuery.result?.data?.count ?? 0;
    t !== this.unread && (P(this, Va, t), this.dispatchNotificationUnreadEvent(), this.host.requestUpdate());
  }
  hostDisconnected() {
    this.weavy && this.unregisterRealtime(!0);
  }
};
Va = new WeakMap(), n(Hf, "UnreadNotificationsController");
let Ql = Hf;
var N2 = Object.defineProperty, q2 = /* @__PURE__ */ n((s, t, e, i) => {
  for (var r = void 0, a = s.length - 1, o; a >= 0; a--)
    (o = s[a]) && (r = o(t, e, r) || r);
  return r && N2(t, e, r), r;
}, "__decorateClass$1m");
const Uf = class Uf extends ae {
  constructor() {
    super(...arguments), this.appType = Ti;
  }
};
n(Uf, "WeavyOptionalAppComponent");
let Us = Uf;
q2([
  V()
], Us.prototype, "appType");
var W2 = Object.defineProperty, Ew = /* @__PURE__ */ n((s, t, e, i) => {
  for (var r = void 0, a = s.length - 1, o; a >= 0; a--)
    (o = s[a]) && (r = o(t, e, r) || r);
  return r && W2(t, e, r), r;
}, "__decorateClass$1l"), Ha, Dd, Ua, Vd, Ba, Hd, Ud, mu;
const Bf = class Bf extends nt {
  constructor() {
    super(...arguments);
    T(this, Ha);
    T(this, Dd, new Promise((e) => {
      P(this, Ha, e);
    }));
    T(this, Ua);
    T(this, Vd, new Promise((e) => {
      P(this, Ua, e);
    }));
    T(this, Ba);
    T(this, Hd, new Promise((e) => {
      P(this, Ba, e);
    }));
    T(this, Ud);
    T(this, mu, new Promise((e) => {
      P(this, Ud, e);
    }));
  }
  // PROMISES
  // TODO: Switch to Promise.withResolvers() when allowed by typescript
  /**
   * Resolves when a contextual id is available.
   *
   * @internal
   * @returns {Promise<ContextIdType>}
   */
  async whenContextId() {
    return await p(this, Dd);
  }
  /**
   * Resolves when current user data is available.
   *
   * @internal
   * @returns {Promise<UserType>}
   */
  async whenUser() {
    return await p(this, Vd);
  }
  /**
   * Resolves when a weavy context is available.
   *
   * @internal
   * @returns {Promise<WeavyType>}
   */
  async whenWeavy() {
    return await p(this, Hd);
  }
  /**
   * Resolves when the component has rendered first time.
   *
   * @returns {Promise<WeavySubComponent>}
   */
  async whenReady() {
    return await p(this, mu);
  }
  willUpdate(e) {
    var i, r, a;
    super.willUpdate(e), e.has("contextId") && this.contextId && (e.get("contextId") && P(this, Dd, new Promise((o) => {
      P(this, Ha, o);
    })), (i = p(this, Ha)) == null || i.call(this, this.contextId)), e.has("user") && this.user && (e.get("user") && P(this, Vd, new Promise((o) => {
      P(this, Ua, o);
    })), (r = p(this, Ua)) == null || r.call(this, this.user)), e.has("weavy") && this.weavy && (e.get("weavy") && P(this, Hd, new Promise((o) => {
      P(this, Ba, o);
    })), (a = p(this, Ba)) == null || a.call(this, this.weavy));
  }
  connectedCallback() {
    super.connectedCallback(), this.contextId && this.requestUpdate("contextId"), this.user && this.requestUpdate("user"), this.weavy && this.requestUpdate("weavy");
  }
  firstUpdated(e) {
    var i;
    super.firstUpdated(e), (i = p(this, Ud)) == null || i.call(this, this), this.dispatchEvent(
      new CustomEvent("ready", {
        bubbles: !1,
        composed: !1
      })
    );
  }
};
Ha = new WeakMap(), Dd = new WeakMap(), Ua = new WeakMap(), Vd = new WeakMap(), Ba = new WeakMap(), Hd = new WeakMap(), Ud = new WeakMap(), mu = new WeakMap(), n(Bf, "WeavySubComponent");
let Ae = Bf;
Ew([
  he({ context: Gv, subscribe: !0 }),
  V()
], Ae.prototype, "contextId");
Ew([
  he({ context: Cu, subscribe: !0 }),
  V()
], Ae.prototype, "user");
Ew([
  he({ context: Xt, subscribe: !0 }),
  V()
], Ae.prototype, "weavy");
var j2 = Object.defineProperty, Su = /* @__PURE__ */ n((s, t, e, i) => {
  for (var r = void 0, a = s.length - 1, o; a >= 0; a--)
    (o = s[a]) && (r = o(t, e, r) || r);
  return r && j2(t, e, r), r;
}, "__decorateClass$1k"), Na, Bd, qa, Nd, Wa, qd, ja, Wd;
const Nf = class Nf extends Ae {
  constructor() {
    super(...arguments);
    T(this, Na);
    T(this, Bd, new Promise((e) => {
      P(this, Na, e);
    }));
    T(this, qa);
    T(this, Nd, new Promise((e) => {
      P(this, qa, e);
    }));
    T(this, Wa);
    T(this, qd, new Promise((e) => {
      P(this, Wa, e);
    }));
    T(this, ja);
    T(this, Wd, new Promise((e) => {
      P(this, ja, e);
    }));
  }
  // PROMISES
  // TODO: Switch to Promise.withResolvers() when allowed by typescript
  // #resolveAgentUser?: (agentUser: AgentType) => void;
  // #whenAgentUser = new Promise<AgentType>((r) => {
  //   this.#resolveAgentUser = r;
  // });
  // async whenAgentUser() {
  //   return await this.#whenAgentUser;
  // }
  /**
   * Resolves when context data blob uploads has finished.
   *
   * @internal
   * @returns {Promise<ContextDataBlobsType>}
   */
  async whenContextDataBlobs() {
    return await p(this, Bd);
  }
  /**
   * Resolves when weavy component features config is available.
   *
   * @internal
   * @returns {Promise<ComponentFeaturePolicy>}
   */
  async whenComponentFeatures() {
    return await p(this, Nd);
  }
  /**
   * Resolves when a provided link is available.
   *
   * @returns {Promise<LinkType>}
   */
  async whenLink() {
    return await p(this, qd);
  }
  /**
   * Resolves when weavy component settings are available.
   *
   * @internal
   * @returns {Promise<WeavyComponentSettingsType>}
   */
  async whenSettings() {
    return await p(this, Wd);
  }
  willUpdate(e) {
    var i, r, a, o;
    super.willUpdate(e), e.has("contextDataBlobs") && this.contextDataBlobs && (e.get("contextDataBlobs") && P(this, Bd, new Promise((c) => {
      P(this, Na, c);
    })), (i = p(this, Na)) == null || i.call(this, this.contextDataBlobs)), e.has("componentFeatures") && this.componentFeatures && (e.get("componentFeatures") && P(this, Nd, new Promise((c) => {
      P(this, qa, c);
    })), (r = p(this, qa)) == null || r.call(this, this.componentFeatures)), e.has("link") && this.link && (e.get("link") && P(this, qd, new Promise((c) => {
      P(this, Wa, c);
    })), (a = p(this, Wa)) == null || a.call(this, this.link)), e.has("settings") && this.settings && (e.get("settings") && P(this, Wd, new Promise((c) => {
      P(this, ja, c);
    })), (o = p(this, ja)) == null || o.call(this, this.settings));
  }
  connectedCallback() {
    super.connectedCallback(), this.contextDataBlobs && this.requestUpdate("contextDataBlobs"), this.componentFeatures && this.requestUpdate("componentFeatures"), this.link && this.requestUpdate("link"), this.settings && this.requestUpdate("settings");
  }
};
Na = new WeakMap(), Bd = new WeakMap(), qa = new WeakMap(), Nd = new WeakMap(), Wa = new WeakMap(), qd = new WeakMap(), ja = new WeakMap(), Wd = new WeakMap(), n(Nf, "WeavySubTypeComponent");
let cr = Nf;
Su([
  he({ context: Zv, subscribe: !0 }),
  V()
], cr.prototype, "contextDataBlobs");
Su([
  he({ context: xu, subscribe: !0 }),
  V()
], cr.prototype, "componentFeatures");
Su([
  he({ context: Xv, subscribe: !0 }),
  V()
], cr.prototype, "link");
Su([
  he({ context: Jv, subscribe: !0 }),
  V()
], cr.prototype, "settings");
var K2 = Object.defineProperty, C1 = /* @__PURE__ */ n((s, t, e, i) => {
  for (var r = void 0, a = s.length - 1, o; a >= 0; a--)
    (o = s[a]) && (r = o(t, e, r) || r);
  return r && K2(t, e, r), r;
}, "__decorateClass$1j"), Ka, jd, Qa, Kd;
const qf = class qf extends cr {
  constructor() {
    super(...arguments);
    T(this, Ka);
    T(this, jd, new Promise((e) => {
      P(this, Ka, e);
    }));
    T(this, Qa);
    T(this, Kd, new Promise((e) => {
      P(this, Qa, e);
    }));
  }
  // PROMISES
  // TODO: Switch to Promise.withResolvers() when allowed by typescript
  /**
   * Resolves when app data is available.
   *
   * @returns {Promise<AppType>}
   */
  async whenApp() {
    return await p(this, jd);
  }
  /**
   * Resolves when multiple app data is available.
   *
   * @returns {Promise<AppListType>}
   */
  async whenApps() {
    return await p(this, Kd);
  }
  willUpdate(e) {
    var i, r;
    super.willUpdate(e), e.has("app") && this.app && (e.get("app") && P(this, jd, new Promise((a) => {
      P(this, Ka, a);
    })), (i = p(this, Ka)) == null || i.call(this, this.app)), e.has("apps") && this.apps && (e.get("apps") && P(this, Kd, new Promise((a) => {
      P(this, Qa, a);
    })), (r = p(this, Qa)) == null || r.call(this, this.apps));
  }
  connectedCallback() {
    super.connectedCallback(), this.app && this.requestUpdate("app"), this.apps && this.requestUpdate("apps");
  }
};
Ka = new WeakMap(), jd = new WeakMap(), Qa = new WeakMap(), Kd = new WeakMap(), n(qf, "WeavySubAppComponent");
let qt = qf;
C1([
  he({ context: bu, subscribe: !0 }),
  V()
], qt.prototype, "app");
C1([
  he({ context: Qv, subscribe: !0 }),
  V()
], qt.prototype, "apps");
const Q2 = eval;
function G2(s) {
  return typeof s == "string" && s ? s = Q2?.(`"use strict";(${s})`) : s = void 0, s;
}
n(G2, "indirectEvalObject");
function Aw(s) {
  if (s)
    try {
      return new URL(s, window.location.toString());
    } catch {
      console.warn("Invalid url: ", s);
    }
}
n(Aw, "toUrl");
const U = /* @__PURE__ */ n((s) => (t, e) => {
  e !== void 0 ? e.addInitializer(() => {
    !customElements.get(s) && customElements.define(
      s,
      t
    );
  }) : !customElements.get(s) && customElements.define(s, t);
}, "customElement"), Y = j`:host{display:contents}`;
var Z2 = Object.defineProperty, Y2 = Object.getOwnPropertyDescriptor, ue = /* @__PURE__ */ n((s, t, e, i) => {
  for (var r = i > 1 ? void 0 : i ? Y2(t, e) : t, a = s.length - 1, o; a >= 0; a--)
    (o = s[a]) && (r = (i ? o(t, e, r) : o(r)) || r);
  return i && r && Z2(t, e, r), r;
}, "__decorateClass$1i");
function Sm(s) {
  return s != null && s !== !1;
}
n(Sm, "acceptedValue");
var Ga;
let Zt = (Ga = class extends nt {
  constructor() {
    super(), this.provider = !1, this.annotations = Vt.defaults.annotations, this.cloudFilePickerUrl = Vt.defaults.cloudFilePickerUrl, this.configurationTimeout = Vt.defaults.configurationTimeout, this.disableEnvironmentImports = Vt.defaults.disableEnvironmentImports, this.enterToSend = Vt.defaults.enterToSend, this.locale = Vt.defaults.locale, this.locales = Vt.defaults.locales, this.gcTime = Vt.defaults.gcTime, this.scrollBehavior = Vt.defaults.scrollBehavior, this.staleTime = Vt.defaults.staleTime, this.tokenFactory = Vt.defaults.tokenFactory, this.tokenFactoryRetryDelay = Vt.defaults.tokenFactoryRetryDelay, this.tokenFactoryTimeout = Vt.defaults.tokenFactoryTimeout, this.tokenUrl = Vt.defaults.tokenUrl, this.url = Vt.defaults.url, this.reactions = Vt.defaults.reactions, this.childElementCount && (this.provider = !0), this.weavy ??= new Vt({ host: this.provider ? this : void 0 });
  }
  /** The semver version of the package. */
  get version() {
    return Vt.version;
  }
  /** The Weavy source name; package name. */
  get sourceName() {
    return Vt.sourceName;
  }
  /** The Weavy source format; file format. */
  get sourceFormat() {
    return Vt.sourceFormat;
  }
  willUpdate(t) {
    if (super.willUpdate(t), this.weavy) {
      const e = {};
      Array.from(t.keys()).forEach((i) => {
        i !== "weavy" && (Sm(this[i]) || Sm(t.get(i))) && Object.assign(e, { [i]: this[i] });
      }), Object.assign(this.weavy, e);
    }
  }
  render() {
    return h` <slot></slot> `;
  }
}, n(Ga, "WyContext"), Ga);
Zt.styles = [Kt, Y];
ue([
  y({ attribute: !0, type: Boolean })
], Zt.prototype, "provider", 2);
ue([
  y()
], Zt.prototype, "annotations", 2);
ue([
  y({
    attribute: !0,
    converter: {
      fromAttribute: /* @__PURE__ */ n((s) => Aw(s), "fromAttribute")
    }
  })
], Zt.prototype, "cloudFilePickerUrl", 2);
ue([
  y({ type: Number, attribute: !0 })
], Zt.prototype, "configurationTimeout", 2);
ue([
  y({ type: Boolean })
], Zt.prototype, "disableEnvironmentImports", 2);
ue([
  y({ attribute: !0 })
], Zt.prototype, "enterToSend", 2);
ue([
  y({ attribute: !0 })
], Zt.prototype, "locale", 2);
ue([
  y({ attribute: !0, type: Array })
], Zt.prototype, "locales", 2);
ue([
  y({ attribute: !0, type: Number })
], Zt.prototype, "gcTime", 2);
ue([
  y({ attribute: !0 })
], Zt.prototype, "scrollBehavior", 2);
ue([
  y({ attribute: !0, type: Number })
], Zt.prototype, "staleTime", 2);
ue([
  y({
    attribute: !0,
    converter: {
      fromAttribute: /* @__PURE__ */ n((s) => G2(s), "fromAttribute")
    }
  })
], Zt.prototype, "tokenFactory", 2);
ue([
  y({ attribute: !0, type: Number })
], Zt.prototype, "tokenFactoryRetryDelay", 2);
ue([
  y({ attribute: !0, type: Number })
], Zt.prototype, "tokenFactoryTimeout", 2);
ue([
  y({
    attribute: !0,
    converter: {
      fromAttribute: /* @__PURE__ */ n((s) => Aw(s), "fromAttribute")
    }
  })
], Zt.prototype, "tokenUrl", 2);
ue([
  y({
    attribute: !0,
    converter: {
      fromAttribute: /* @__PURE__ */ n((s) => Aw(s), "fromAttribute")
    }
  })
], Zt.prototype, "url", 2);
ue([
  y({ attribute: !0 })
], Zt.prototype, "reactions", 2);
ue([
  V()
], Zt.prototype, "weavy", 2);
Zt = ue([
  U("wy-context")
], Zt);
const Pe = j`:host{font-family:var(--wy-font-family, unset)}`;
var X2 = Object.defineProperty, J2 = Object.getOwnPropertyDescriptor, Rw = /* @__PURE__ */ n((s, t, e, i) => {
  for (var r = i > 1 ? void 0 : i ? J2(t, e) : t, a = s.length - 1, o; a >= 0; a--)
    (o = s[a]) && (r = (i ? o(t, e, r) : o(r)) || r);
  return i && r && X2(t, e, r), r;
}, "__decorateClass$1h"), Za;
let Gc = (Za = class extends ae {
  constructor() {
    super(...arguments), this.appType = Ti, this.componentFeatures = new Gt(Cw(Sh)), this.theme = new ce(this, Gc.styles);
  }
  render() {
    return h`<slot></slot>`;
  }
}, n(Za, "WyComponent"), Za);
Gc.styles = [Kt, Y, Pe];
Rw([
  y({
    converter: {
      fromAttribute(s, t) {
        return g2.get(s) ?? s;
      }
    }
  })
], Gc.prototype, "appType", 2);
Rw([
  y({
    converter: {
      fromAttribute(s, t) {
        const e = typeof s == "string" ? Yv(s, Sh, Sh) : Sh;
        return new Gt(Cw(e));
      }
    }
  })
], Gc.prototype, "componentFeatures", 2);
Gc = Rw([
  U("wy-component")
], Gc);
const bi = j`:host{position:relative;display:flex;flex-direction:column;min-height:3rem;min-width:16rem;isolation:isolate;box-sizing:border-box;container-type:inline-size}`, Cr = j`:host{flex:1}`, $r = j`:host{padding:var(--wy-padding-outer, 0);border-radius:var(--wy-border-radius-outer, var(--wy-border-radius, calc(.5 * var(--wy-size, 1rem))))}`, ls = j`:host{overflow-y:auto;max-height:100%;overflow-x:hidden;overflow-anchor:none;touch-action:pan-y;will-change:scroll-position,opacity;backface-visibility:hidden}:host:not(body){-webkit-overflow-scrolling:touch}`;
function t5(s, t, e = {}) {
  return {
    ...e,
    initialPageParam: 0,
    queryKey: ["messages", t],
    queryFn: /* @__PURE__ */ n(async (i) => {
      const r = i.pageParam, a = "/api/apps/" + t + "/messages?order_by=id+desc&skip=" + r, c = await (await s.fetch(a)).json();
      return c.data = c.data?.reverse() || [], c;
    }, "queryFn"),
    getNextPageParam: /* @__PURE__ */ n((i) => {
      if (i?.end && i?.end < i?.count)
        return i.end;
    }, "getNextPageParam"),
    select: /* @__PURE__ */ n((i) => ({
      // reverse scroll
      pages: [...i.pages].reverse(),
      pageParams: [...i.pageParams].reverse()
    }), "select")
  };
}
n(t5, "getMessagesOptions");
function e5(s, t) {
  return {
    mutationFn: /* @__PURE__ */ n(async (i) => await (await s.fetch("/api/apps/" + i.app_id + "/messages", {
      method: "POST",
      body: JSON.stringify({
        text: i.text,
        blobs: i.blobs,
        embed_id: i.embed_id || null,
        meeting_id: i.meeting_id,
        options: i.poll_options.filter((a) => a.text.trim() !== "").map((a) => ({ text: a.text })),
        metadata: i.metadata || null,
        context: i.context
      })
    })).json(), "mutationFn"),
    mutationKey: t,
    onMutate: /* @__PURE__ */ n((i) => {
      const r = ["messages", i.app_id], a = Kl(s.queryClient, r, !1), o = {
        id: a ? a.id - 1 : -1,
        app: { id: i.app_id },
        text: i.text,
        html: i.text,
        plain: i.text,
        created_by: i.user,
        created_at: (/* @__PURE__ */ new Date()).toUTCString(),
        attachments: { count: 0 },
        reactions: { count: 0 },
        is_starred: !1,
        is_subscribed: !0,
        is_trashed: !1
      };
      Wl(s.queryClient, r, o);
    }, "onMutate"),
    onSuccess: /* @__PURE__ */ n((i) => {
      ht(
        s.queryClient,
        { queryKey: ["members", i.app.id] },
        i.created_by.id,
        (l) => ({
          ...l,
          marked_id: i.id,
          marked_at: i.created_at
        })
      );
      const r = ["messages", i.app.id], { queryClient: a } = s, o = Yd(a, r, i.id), c = o ? null : Kl(a, r, !0);
      o ? Ee(a, r, o.id, () => i) : c ? Ee(a, r, c.id, () => i) : Wl(a, r, i);
    }, "onSuccess")
  };
}
n(e5, "getAddMessageMutationOptions");
const { I: i5 } = w0, Mm = /* @__PURE__ */ n((s) => s, "i$3"), r5 = /* @__PURE__ */ n((s) => s.strings === void 0, "r$1"), Em = /* @__PURE__ */ n(() => document.createComment(""), "s$1"), Tl = /* @__PURE__ */ n((s, t, e) => {
  const i = s._$AA.parentNode, r = t === void 0 ? s._$AB : t._$AA;
  if (e === void 0) {
    const a = i.insertBefore(Em(), r), o = i.insertBefore(Em(), r);
    e = new i5(a, o, s, s.options);
  } else {
    const a = e._$AB.nextSibling, o = e._$AM, c = o !== s;
    if (c) {
      let l;
      e._$AQ?.(s), e._$AM = s, e._$AP !== void 0 && (l = s._$AU) !== o._$AU && e._$AP(l);
    }
    if (a !== r || c) {
      let l = e._$AA;
      for (; l !== a; ) {
        const u = Mm(l).nextSibling;
        Mm(i).insertBefore(l, r), l = u;
      }
    }
  }
  return e;
}, "v"), fs = /* @__PURE__ */ n((s, t, e = s) => (s._$AI(t, e), s), "u$1"), s5 = {}, $1 = /* @__PURE__ */ n((s, t = s5) => s._$AH = t, "p"), n5 = /* @__PURE__ */ n((s) => s._$AH, "M"), Wp = /* @__PURE__ */ n((s) => {
  s._$AR(), s._$AA.remove();
}, "h$2");
const Xd = { ATTRIBUTE: 1, CHILD: 2 }, ln = /* @__PURE__ */ n((s) => (...t) => ({ _$litDirective$: s, values: t }), "e$2");
var Ya;
let wl = (Ya = class {
  constructor(t) {
  }
  get _$AU() {
    return this._$AM._$AU;
  }
  _$AT(t, e, i) {
    this._$Ct = t, this._$AM = e, this._$Ci = i;
  }
  _$AS(t, e) {
    return this.update(t, e);
  }
  update(t, e) {
    return this.render(...e);
  }
}, n(Ya, "i"), Ya);
const Hl = /* @__PURE__ */ n((s, t) => {
  const e = s._$AN;
  if (e === void 0) return !1;
  for (const i of e) i._$AO?.(t, !1), Hl(i, t);
  return !0;
}, "s"), iu = /* @__PURE__ */ n((s) => {
  let t, e;
  do {
    if ((t = s._$AM) === void 0) break;
    e = t._$AN, e.delete(s), s = t;
  } while (e?.size === 0);
}, "o$5"), k1 = /* @__PURE__ */ n((s) => {
  for (let t; t = s._$AM; s = t) {
    let e = t._$AN;
    if (e === void 0) t._$AN = e = /* @__PURE__ */ new Set();
    else if (e.has(s)) break;
    e.add(s), c5(t);
  }
}, "r");
function a5(s) {
  this._$AN !== void 0 ? (iu(this), this._$AM = s, k1(this)) : this._$AM = s;
}
n(a5, "h$1");
function o5(s, t = !1, e = 0) {
  const i = this._$AH, r = this._$AN;
  if (r !== void 0 && r.size !== 0) if (t) if (Array.isArray(i)) for (let a = e; a < i.length; a++) Hl(i[a], !1), iu(i[a]);
  else i != null && (Hl(i, !1), iu(i));
  else Hl(this, s);
}
n(o5, "n$2");
const c5 = /* @__PURE__ */ n((s) => {
  s.type == Xd.CHILD && (s._$AP ??= o5, s._$AQ ??= a5);
}, "c$1"), Wf = class Wf extends wl {
  constructor() {
    super(...arguments), this._$AN = void 0;
  }
  _$AT(t, e, i) {
    super._$AT(t, e, i), k1(this), this.isConnected = t._$AU;
  }
  _$AO(t, e = !0) {
    t !== this.isConnected && (this.isConnected = t, t ? this.reconnected?.() : this.disconnected?.()), e && (Hl(this, t), iu(this));
  }
  setValue(t) {
    if (r5(this._$Ct)) this._$Ct._$AI(t, this);
    else {
      const e = [...this._$Ct._$AH];
      e[this._$Ci] = t, this._$Ct._$AI(e, this, 0);
    }
  }
  disconnected() {
  }
  reconnected() {
  }
};
n(Wf, "f");
let jy = Wf;
const J = /* @__PURE__ */ n(() => new Ky(), "e$1"), jf = class jf {
};
n(jf, "h");
let Ky = jf;
const jp = /* @__PURE__ */ new WeakMap(), K = ln(class extends jy {
  render(s) {
    return g;
  }
  update(s, [t]) {
    const e = t !== this.G;
    return e && this.G !== void 0 && this.rt(void 0), (e || this.lt !== this.ct) && (this.G = t, this.ht = s.options?.host, this.rt(this.ct = s.element)), g;
  }
  rt(s) {
    if (this.isConnected || (s = void 0), typeof this.G == "function") {
      const t = this.ht ?? globalThis;
      let e = jp.get(t);
      e === void 0 && (e = /* @__PURE__ */ new WeakMap(), jp.set(t, e)), e.get(this.G) !== void 0 && this.G.call(this.ht, void 0), e.set(this.G, s), s !== void 0 && this.G.call(this.ht, s);
    } else this.G.value = s;
  }
  get lt() {
    return typeof this.G == "function" ? jp.get(this.ht ?? globalThis)?.get(this.G) : this.G?.value;
  }
  disconnected() {
    this.lt === this.ct && this.rt(void 0);
  }
  reconnected() {
    this.rt(this.ct);
  }
});
function l5(s, t, e) {
  const i = s.queryClient;
  return {
    mutationKey: ["polls", t === "feed" ? "feed" : t],
    mutationFn: /* @__PURE__ */ n(async ({ optionId: o }) => {
      const c = await s.fetch(`/api/options/${o}/vote`, { method: "POST" });
      if (!c.ok) {
        const l = await c.json();
        throw new Error(l.detail || l.title, { cause: l });
      }
      return await c.json();
    }, "mutationFn"),
    onMutate: /* @__PURE__ */ n((o) => (ht(i, { queryKey: e }, o.parentId, (c) => (c.options?.data && (c.options.data = c.options.data?.map((l) => {
      if (l.has_voted) {
        l.has_voted = !1;
        const u = l.votes?.count || 1;
        l.votes ? l.votes.count = u - 1 : l.votes = { count: u - 1 };
      } else if (!l.has_voted && l.id === o.optionId) {
        l.has_voted = !0;
        const u = l.votes?.count || 0;
        l.votes ? l.votes.count = u + 1 : l.votes = { count: u + 1 };
      }
      return l;
    })), c)), { id: o.optionId }), "onMutate"),
    onSuccess: /* @__PURE__ */ n(async (o, c) => {
      const u = await (await s.fetch("/api/" + c.parentType + "/" + c.parentId)).json();
      ht(
        i,
        { queryKey: e, exact: !1 },
        c.parentId,
        (b) => Object.assign(b, u)
      );
    }, "onSuccess")
    /*onError(error: Error, variables: MutatePollVariables) {
      updateCacheItems(queryClient, { queryKey: postsKey, exact: false }, variables.id, (existingPost: PostType) => Object.assign(existingPost, { is_subscribed: variables..is_subscribed }));
    },*/
  };
}
n(l5, "getPollMutationOptions");
function Tw(s, t, e) {
  return new St(s.queryClient, l5(s, t, e));
}
n(Tw, "getPollMutation");
function d5(s, t) {
  return {
    queryKey: ["votes", t],
    enabled: !1,
    queryFn: /* @__PURE__ */ n(async () => await (await s.fetch(`/api/options/${t}`)).json(), "queryFn")
  };
}
n(d5, "getVotesOptions");
const Zc = /* @__PURE__ */ n((s, t) => !!(s && t && t.indexOf(s) !== -1), "hasPermission");
function h5(s) {
  return {
    mutationFn: /* @__PURE__ */ n(async ({ app: e, messageId: i }) => {
      const r = i ? `/api/apps/${e.id}/mark?messageId=${i}` : `/api/apps/${e.id}/mark`;
      await s.fetch(r, { method: i ? "PUT" : "DELETE" });
    }, "mutationFn"),
    onMutate: /* @__PURE__ */ n(async (e) => {
      await s.queryClient.cancelQueries({ queryKey: ["apps", e.app.id] }), await s.queryClient.cancelQueries({ queryKey: ["apps", "list"], exact: !1 }), await s.queryClient.cancelQueries({ queryKey: ["apps", "unread"], exact: !1 }), await s.queryClient.cancelQueries({ queryKey: ["members", e.app.id] }), s.queryClient.setQueryData(
        ["apps", e.app.id],
        (i) => i && { ...i, is_unread: !e.messageId || e.messageId < i.last_message.id }
      ), ht(
        s.queryClient,
        { queryKey: ["apps", "list"], exact: !1 },
        e.app.id,
        (i) => ({
          ...i,
          is_unread: !e.messageId || e.messageId < i.last_message.id
        })
      ), e.userId && ht(
        s.queryClient,
        { queryKey: ["members", e.app.id] },
        e.userId,
        (i) => (e.messageId ? (i.marked_at = (/* @__PURE__ */ new Date()).toISOString(), i.marked_id = e.messageId) : (i.marked_at = void 0, i.marked_id = void 0), i)
      ), jl(
        s.queryClient,
        {
          queryKey: ["apps", "unread"],
          exact: !1,
          predicate: /* @__PURE__ */ n((i) => {
            const a = i.queryKey[2].includes(e.app.type), o = i.queryKey[2], c = !o || !!e.app.members.data?.find((l) => l.uid && l.uid === o);
            return a && c;
          }, "predicate")
        },
        (i) => Math.max(0, i + (e.messageId ? -1 : 1))
      );
    }, "onMutate"),
    onError: /* @__PURE__ */ n((e, i) => {
      console.error(e.message), ht(
        s.queryClient,
        { queryKey: ["apps", "list"], exact: !1 },
        i.app.id,
        (r) => ({ ...r, is_unread: !r.is_unread })
      );
    }, "onError"),
    onSettled: /* @__PURE__ */ n(async (e, i, r) => {
      await s.queryClient.invalidateQueries({ queryKey: ["apps", r.app.id] }), await s.queryClient.invalidateQueries({ queryKey: ["apps", "list"], exact: !1 }), await s.queryClient.invalidateQueries({ queryKey: ["apps", "unread"], exact: !1 }), await s.queryClient.invalidateQueries({ queryKey: ["members", r.app.id] });
    }, "onSettled")
  };
}
n(h5, "getMarkConversationMutationOptions");
function u5(s) {
  return {
    mutationFn: /* @__PURE__ */ n(async ({ appId: e, star: i }) => {
      await s.fetch(`/api/apps/${e}/stars`, { method: i ? "POST" : "DELETE" });
    }, "mutationFn"),
    onMutate: /* @__PURE__ */ n((e) => {
      ht(
        s.queryClient,
        { queryKey: ["apps", "list"], exact: !1 },
        e.appId,
        (i) => ({
          ...i,
          is_starred: e.star
        })
      );
    }, "onMutate"),
    onError: /* @__PURE__ */ n((e, i) => {
      console.error(e.message), ht(
        s.queryClient,
        { queryKey: ["apps", "list"], exact: !1 },
        i.appId,
        (r) => ({
          ...r,
          is_starred: !i.star
        })
      );
    }, "onError")
  };
}
n(u5, "getStarConversationMutationOptions");
function p5(s) {
  return {
    mutationFn: /* @__PURE__ */ n(async ({ appId: e, pin: i }) => {
      await s.fetch(`/api/apps/${e}/pin`, { method: i ? "PUT" : "DELETE" });
    }, "mutationFn"),
    onMutate: /* @__PURE__ */ n((e) => {
      ht(
        s.queryClient,
        { queryKey: ["apps", "list"], exact: !1 },
        e.appId,
        (i) => ({
          ...i,
          is_pinned: e.pin
        })
      );
    }, "onMutate"),
    onSettled: /* @__PURE__ */ n(async () => {
      await s.queryClient.invalidateQueries({ queryKey: ["apps"] });
    }, "onSettled")
  };
}
n(p5, "getPinConversationMutationOptions");
function y5(s) {
  return {
    mutationFn: /* @__PURE__ */ n(async ({ appId: e, members: i }) => {
      await s.fetch(`/api/apps/${e}/members/${i.join(",")}`, { method: "DELETE" });
    }, "mutationFn"),
    onMutate: /* @__PURE__ */ n((e) => {
      Pu(s.queryClient, ["apps", "list"], e.appId);
    }, "onMutate"),
    onSuccess: /* @__PURE__ */ n((e, i) => {
      s.queryClient.removeQueries({ queryKey: ["apps", i.appId] }), s.queryClient.removeQueries({ queryKey: ["members", i.appId] });
    }, "onSuccess"),
    onSettled: /* @__PURE__ */ n(async () => {
      await s.queryClient.invalidateQueries({ queryKey: ["apps"] }), await s.queryClient.invalidateQueries({ queryKey: ["members"] });
    }, "onSettled")
  };
}
n(y5, "getLeaveConversationMutationOptions");
function w5(s) {
  return {
    mutationFn: /* @__PURE__ */ n(async ({ appId: e }) => {
      await s.fetch(`/api/apps/${e}/remove`, { method: "POST" });
    }, "mutationFn"),
    onMutate: /* @__PURE__ */ n((e) => {
      Pu(s.queryClient, ["apps", "list"], e.appId);
    }, "onMutate"),
    onSettled: /* @__PURE__ */ n(async () => {
      await s.queryClient.invalidateQueries({ queryKey: ["apps"] });
    }, "onSettled")
  };
}
n(w5, "getRemoveConversationMutationOptions");
function f5(s) {
  return {
    mutationFn: /* @__PURE__ */ n(async ({ appId: e, userId: i, access: r }) => {
      await s.fetch(`/api/apps/${e}/members/${i}`, { method: "PUT", body: JSON.stringify({ access: r }) });
    }, "mutationFn"),
    onSettled: /* @__PURE__ */ n(async (e, i, r) => {
      await s.queryClient.invalidateQueries({ queryKey: ["apps"] }), await s.queryClient.invalidateQueries({ queryKey: ["members", r.appId] });
    }, "onSettled")
  };
}
n(f5, "getUpdateMemberMutationOptions");
function m5(s) {
  return {
    mutationFn: /* @__PURE__ */ n(async ({ appId: e, members: i }) => {
      await s.fetch(`/api/apps/${e}/members`, {
        method: "PUT",
        body: JSON.stringify(
          i.map((r) => ({ id: r, access: "write" }))
        )
      });
    }, "mutationFn"),
    onSettled: /* @__PURE__ */ n(async (e, i, r) => {
      await s.queryClient.invalidateQueries({ queryKey: ["apps"] }), await s.queryClient.invalidateQueries({ queryKey: ["members", r.appId] });
    }, "onSettled")
  };
}
n(m5, "getAddMembersToConversationMutationOptions");
function v5(s) {
  return {
    mutationFn: /* @__PURE__ */ n(async ({ appId: e, name: i, blobId: r }) => await (await s.fetch(`/api/apps/${e}`, {
      method: "PATCH",
      body: JSON.stringify({
        name: i,
        picture: r
      })
    })).json(), "mutationFn"),
    onMutate: /* @__PURE__ */ n((e) => {
      const i = /* @__PURE__ */ n((r) => (typeof e.name == "string" && (r.name = e.name), typeof e?.thumbnailUrl == "string" && (r.avatar_url = e.thumbnailUrl), r), "modifyAppItem");
      Ee(s.queryClient, ["apps", e.appId], void 0, i), ht(s.queryClient, { queryKey: ["apps", "list"], exact: !1 }, e.appId, i);
    }, "onMutate")
  };
}
n(v5, "getUpdateConversationMutationOptions");
function g5(s) {
  return {
    mutationFn: /* @__PURE__ */ n(async ({ appId: e }) => {
      await s.fetch(`/api/apps/${e}/trash`, { method: "POST" });
    }, "mutationFn"),
    onMutate: /* @__PURE__ */ n((e) => {
      Pu(s.queryClient, ["apps", "list"], e.appId);
    }, "onMutate"),
    onSettled: /* @__PURE__ */ n(async () => {
      await s.queryClient.invalidateQueries({ queryKey: ["apps"] });
    }, "onSettled")
  };
}
n(g5, "getTrashConversationMutationOptions");
function _1(s) {
  return new St(s.queryClient, h5(s));
}
n(_1, "getMarkConversationMutation");
function b5(s) {
  return new St(s.queryClient, u5(s));
}
n(b5, "getStarConversationMutation");
function x5(s) {
  return new St(s.queryClient, p5(s));
}
n(x5, "getPinConversationMutation");
function P1(s) {
  return new St(s.queryClient, y5(s));
}
n(P1, "getLeaveConversationMutation");
function C5(s) {
  return new St(s.queryClient, w5(s));
}
n(C5, "getRemoveConversationMutation");
function $5(s) {
  return new St(s.queryClient, f5(s));
}
n($5, "getUpdateMemberMutation");
function k5(s) {
  return new St(s.queryClient, m5(s));
}
n(k5, "getAddMembersToConversationMutation");
function S1(s) {
  return new St(s.queryClient, v5(s));
}
n(S1, "getUpdateConversationMutation");
function _5(s) {
  return new St(s.queryClient, g5(s));
}
n(_5, "getTrashConversationMutation");
function M1(s, t, e = [rt.ChatRoom, rt.PrivateChat], i) {
  return nr(s, ["apps", t], void 0, {
    initialData: /* @__PURE__ */ n(() => s?.queryClient.getQueryData(["apps", "list", e, i])?.pages.flatMap((r) => r.data).find((r) => r?.id === t), "initialData")
  });
}
n(M1, "getConversationOptions");
function E1(s, t, e) {
  return {
    queryKey: ["members", t],
    queryFn: /* @__PURE__ */ n(async () => await (await s.fetch(`/api/apps/${t}/members`)).json(), "queryFn"),
    ...e
  };
}
n(E1, "getMemberOptions");
function P5(s, t, e, i) {
  return {
    queryKey: ["search__members", e],
    initialPageParam: 0,
    enabled: !0,
    queryFn: /* @__PURE__ */ n(async (r) => {
      const a = t(), o = r.pageParam;
      let c;
      e ? c = await s.fetch(
        `/api/apps/${e}/members?q=${a}${i() !== void 0 ? `&agent=${!!i()}` : ""}&member=false&system=false&skip=${o}`
      ) : c = await s.fetch(
        `/api/users?q=${a}${i() !== void 0 ? `&agent=${!!i()}` : ""}&system=false&skip=${o}`
      );
      const l = await c.json();
      return l.data = l.data || [], l;
    }, "queryFn"),
    getNextPageParam: /* @__PURE__ */ n((r) => {
      if (r.end && r.end < r.count)
        return r.end;
    }, "getNextPageParam")
  };
}
n(P5, "getInfiniteSearchMemberOptions");
const ot = /* @__PURE__ */ n((s) => s ?? g, "o$3");
const Am = /* @__PURE__ */ n((s, t, e) => {
  const i = /* @__PURE__ */ new Map();
  for (let r = t; r <= e; r++) i.set(s[r], r);
  return i;
}, "u"), Be = ln(class extends wl {
  constructor(s) {
    if (super(s), s.type !== Xd.CHILD) throw Error("repeat() can only be used in text expressions");
  }
  dt(s, t, e) {
    let i;
    e === void 0 ? e = t : t !== void 0 && (i = t);
    const r = [], a = [];
    let o = 0;
    for (const c of s) r[o] = i ? i(c, o) : o, a[o] = e(c, o), o++;
    return { values: a, keys: r };
  }
  render(s, t, e) {
    return this.dt(s, t, e).values;
  }
  update(s, [t, e, i]) {
    const r = n5(s), { values: a, keys: o } = this.dt(t, e, i);
    if (!Array.isArray(r)) return this.ut = o, a;
    const c = this.ut ??= [], l = [];
    let u, b, C = 0, v = r.length - 1, f = 0, m = a.length - 1;
    for (; C <= v && f <= m; ) if (r[C] === null) C++;
    else if (r[v] === null) v--;
    else if (c[C] === o[f]) l[f] = fs(r[C], a[f]), C++, f++;
    else if (c[v] === o[m]) l[m] = fs(r[v], a[m]), v--, m--;
    else if (c[C] === o[m]) l[m] = fs(r[C], a[m]), Tl(s, l[m + 1], r[C]), C++, m--;
    else if (c[v] === o[f]) l[f] = fs(r[v], a[f]), Tl(s, r[C], r[v]), v--, f++;
    else if (u === void 0 && (u = Am(o, f, m), b = Am(c, C, v)), u.has(c[C])) if (u.has(c[v])) {
      const $ = b.get(o[f]), E = $ !== void 0 ? r[$] : null;
      if (E === null) {
        const M = Tl(s, r[C]);
        fs(M, a[f]), l[f] = M;
      } else l[f] = fs(E, a[f]), Tl(s, r[C], E), r[$] = null;
      f++;
    } else Wp(r[v]), v--;
    else Wp(r[C]), C++;
    for (; f <= m; ) {
      const $ = Tl(s, l[m + 1]);
      fs($, a[f]), l[f++] = $;
    }
    for (; C <= v; ) {
      const $ = r[C++];
      $ !== null && Wp($);
    }
    return this.ut = o, $1(s, l), gu;
  }
});
const Eh = ln(class extends wl {
  constructor() {
    super(...arguments), this.key = g;
  }
  render(s, t) {
    return this.key = s, t;
  }
  update(s, [t, e]) {
    return t !== this.key && ($1(s), this.key = t), e;
  }
});
function lr(s) {
  s.stopPropagation();
}
n(lr, "inputConsume");
function S5(s) {
  s.key === "Escape" && (s.preventDefault(), s.stopPropagation(), s.target.value = "", s.target.dispatchEvent(new InputEvent("input", { data: "", inputType: "deleteContent" })));
}
n(S5, "inputClearOnEscape");
function Gl(s) {
  s.key === "Escape" && (s.preventDefault(), s.stopPropagation(), s.target.blur());
}
n(Gl, "inputBlurOnEscape");
function M5(s) {
  s.key === "Enter" && s.target.blur();
}
n(M5, "inputBlurOnEnter");
function E5(s) {
  s.target.value ? S5(s) : Gl(s);
}
n(E5, "inputClearAndBlurOnEscape");
function A1(s) {
  s.key === "Enter" && (s.preventDefault(), s.stopPropagation(), s.target.click());
}
n(A1, "clickOnEnter");
function A5(s) {
  s.key === " " && (s.preventDefault(), s.stopPropagation());
}
n(A5, "consumeOnSpace");
function ne(s) {
  A1(s), A5(s);
}
n(ne, "clickOnEnterAndConsumeOnSpace");
function ke(s) {
  s.key === " " && (s.preventDefault(), s.stopPropagation(), s.target.click());
}
n(ke, "clickOnSpace");
function Lw(s) {
  A1(s), ke(s);
}
n(Lw, "clickOnEnterAndSpace");
const Mu = j`@keyframes wy-highlight{0%{--wy-component-background-color: var(--wy-highlight, var(--wy-highlight-light, #e8f2ff));--wy-component-color: var(--wy-on-highlight, var(--wy-on-highlight-light, #001d32))}}[part~=wy-messages]{padding:var(--wy-message-padding, var(--wy-padding-lg, calc(1.5 * var(--wy-padding, calc(.5 * var(--wy-size, 1rem))))));flex:1 1 100%;display:flex;flex-direction:column}[part~=wy-message-placeholder]{opacity:.5}[part~=wy-message-date-separator]{font-size:var(--wy-font-size-xs, calc(.75 * var(--wy-font-size, 1em)));pointer-events:none;position:sticky;display:block;top:calc(var(--wy-padding-lg, calc(1.5 * var(--wy-padding, calc(.5 * var(--wy-size, 1rem))))) + var(--wy-component-offset-top, 0px));z-index:10;align-self:center;justify-self:center;margin:var(--wy-padding, calc(.5 * var(--wy-size, 1rem)));background:var(--wy-surface-container-high, var(--wy-surface-container-high-light, #e6e8ee));color:var(--wy-on-surface, var(--wy-on-surface-light, #181c20));padding:var(--wy-padding-sm, calc(.75 * var(--wy-padding, calc(.5 * var(--wy-size, 1rem)))));border-radius:var(--wy-border-radius-sm, calc(var(--wy-border-radius, calc(.5 * var(--wy-size, 1rem))) - .25 * var(--wy-padding, calc(.5 * var(--wy-size, 1rem)))))}[part~=wy-message]{--wy-component-background-color: var(--wy-surface-container, var(--wy-surface-container-light, #eceef3));--wy-component-color: var(--wy-on-surface, var(--wy-on-surface-light, #181c20));display:flex;flex-direction:row;align-items:flex-start;margin-bottom:calc(var(--wy-padding, calc(.5 * var(--wy-size, 1rem))) + .5 * var(--wy-button-size, calc(var(--wy-button-line-height, 1.5) * var(--wy-size, 1rem) + 2 * var(--wy-button-padding-y, var(--wy-padding, calc(.5 * var(--wy-size, 1rem)))))));scroll-margin-block:6rem}[part~=wy-message][part~=wy-highlight]{animation:wy-highlight 3s 1.5s ease-in-out both}[part~=wy-message-meta]{font-size:var(--wy-font-size-xs, calc(.75 * var(--wy-font-size, 1em)));margin:0 0 calc(.5 * var(--wy-gap, var(--wy-padding, calc(.5 * var(--wy-size, 1rem))))) calc(.5 * var(--wy-gap, var(--wy-padding, calc(.5 * var(--wy-size, 1rem)))));color:var(--wy-on-surface-variant, var(--wy-on-surface-variant-light, #42474e))}[part~=wy-message-bubble]{background-color:var(--wy-component-background-color);color:var(--wy-component-color);border-radius:var(--wy-border-radius, calc(.5 * var(--wy-size, 1rem)));display:flex;flex-direction:column;padding:var(--wy-message-padding, var(--wy-padding-lg, calc(1.5 * var(--wy-padding, calc(.5 * var(--wy-size, 1rem))))));gap:var(--wy-gap, var(--wy-padding, calc(.5 * var(--wy-size, 1rem))));max-width:min(32 * var(--wy-size, 1rem),100% - 2 * var(--wy-message-padding, var(--wy-padding-lg, calc(1.5 * var(--wy-padding, calc(.5 * var(--wy-size, 1rem)))))))}[part~=wy-message-bubble-section]{flex:1 1 100%;display:block;border-radius:var(--wy-border-radius-sm, calc(var(--wy-border-radius, calc(.5 * var(--wy-size, 1rem))) - .25 * var(--wy-padding, calc(.5 * var(--wy-size, 1rem)))));clip-path:border-box}[part~=wy-message-author]{font-size:var(--wy-font-size-xs, calc(.75 * var(--wy-font-size, 1em)));margin:calc(1lh + .5 * var(--wy-gap, var(--wy-padding, calc(.5 * var(--wy-size, 1rem))))) calc(.5 * var(--wy-size, 1rem)) 0 0}[part~=wy-message-content]{display:flex;flex-direction:column;width:100%;align-items:flex-start;flex-wrap:wrap;min-width:0;position:relative}[part~=wy-message-agent]{--wy-component-background-color: var(--wy-tertiary-container, var(--wy-tertiary-container-light, #ebe3bd));--wy-component-color: var(--wy-on-tertiary-container, var(--wy-on-tertiary-container-light, #4c472b))}[part~=wy-message-me]{margin-right:0;margin-left:calc(2.5 * var(--wy-size, 1rem));align-items:flex-end;--wy-component-background-color: var(--wy-secondary-container, var(--wy-secondary-container-light, #d5e4f7));--wy-component-color: var(--wy-on-secondary-container, var(--wy-on-secondary-container-light, #3a4857))}[part~=wy-message-me] [part~=wy-message-meta]{margin:0 calc(.5 * var(--wy-gap, var(--wy-padding, calc(.5 * var(--wy-size, 1rem))))) calc(.5 * var(--wy-gap, var(--wy-padding, calc(.5 * var(--wy-size, 1rem))))) 0}[part~=wy-message-me] [part~=wy-message-content]{align-items:flex-end}[part~=wy-message-bubble-emoji]{background:none;padding:0 0 var(--wy-message-padding, var(--wy-padding-lg, calc(1.5 * var(--wy-padding, calc(.5 * var(--wy-size, 1rem))))))}[part~=wy-message-seenby]{display:flex;gap:var(--wy-gap-sm, calc(.5 * var(--wy-gap, var(--wy-padding, calc(.5 * var(--wy-size, 1rem))))));height:calc(1.125 * var(--wy-size, 1rem));justify-content:flex-end;margin-top:calc(-.125 * var(--wy-size, 1rem))}`, R1 = j`[part~=wy-footerbar]{--wy-component-background-color: var(--wy-surface-container, var(--wy-surface-container-light, #eceef3));--wy-component-color: var(--wy-on-surface, var(--wy-on-surface-light, #181c20));position:absolute;bottom:0;left:0;right:0;z-index:1020}[part~=wy-footerbar]:after{content:\"\";background-color:var(--wy-component-background-color);color:var(--wy-component-color);position:absolute;width:100%;left:0;bottom:0;height:100%;z-index:-1}[part~=wy-footerbar-sticky]{position:sticky}[part~=wy-footerbar-floating]{--wy-component-border-radius: var(--wy-border-radius-outer, var(--wy-border-radius, calc(.5 * var(--wy-size, 1rem))));border-radius:var(--wy-component-border-radius)}[part~=wy-footerbar-floating]:after{border-radius:var(--wy-component-border-radius)}`, dn = j`[part~=wy-pane]{display:flex;flex-direction:column;position:relative;min-height:0;height:100%;flex:1 1 auto}[part~=wy-pane-body]{display:flex;flex-direction:column;flex:1}[part~=wy-pane-group]{padding:var(--wy-padding, calc(.5 * var(--wy-size, 1rem)));display:flex;flex-direction:column;flex:1}[part~=wy-pane-group-fixed-size]{flex:0}[part~=wy-pane-toolbar]{display:flex;flex:1;justify-content:space-between;align-items:center;padding:var(--wy-padding-sm, calc(.75 * var(--wy-padding, calc(.5 * var(--wy-size, 1rem)))))}`, kr = j`[part~=wy-pager]{position:relative;align-self:center;justify-self:center}[part~=wy-pager-bottom]{width:16px;height:256px;max-height:100%;margin-top:-256px;margin-inline:auto;pointer-events:none}[part~=wy-pager-top]{width:16px;height:256px;max-height:100%;margin-bottom:-256px;margin-inline:auto;pointer-events:none}`, T1 = j`[part~=wy-banner]{background-color:var(--wy-surface-container-low, var(--wy-surface-container-low-light, #f1f3f9));color:var(--wy-on-surface, var(--wy-on-surface-light, #181c20));position:relative;-webkit-user-select:none;user-select:none;overflow:hidden;display:flex;align-self:center;text-align:center;width:max-content;min-height:min-content;max-width:calc(100% - 3 * var(--wy-size, 1rem));height:auto;gap:var(--wy-gap, var(--wy-padding, calc(.5 * var(--wy-size, 1rem))));margin-top:0;margin-bottom:var(--wy-gap-lg, calc(1.5 * var(--wy-gap, var(--wy-padding, calc(.5 * var(--wy-size, 1rem))))));padding:calc(.5 * var(--wy-size, 1rem)) calc(1 * var(--wy-size, 1rem));border-radius:var(--wy-border-radius-pill, var(--wy-border-radius, calc(2.5 * var(--wy-size, 1rem))));font-size:var(--wy-font-size-xs, calc(.75 * var(--wy-font-size, 1em)))}[part~=wy-banner][part~=wy-transition]{transition-property:opacity,min-height,padding-top,padding-bottom,margin-top,margin-bottom,height;transition-duration:var(--wy-transition-duration, .2s);transition-timing-function:var(--wy-transition-curve, cubic-bezier(.4, 0, .2, 1))}[part~=wy-banner][part~=wy-transition]:not([part~=wy-show]){opacity:0;min-height:0;height:0;padding-top:0;padding-bottom:0;margin-top:0;margin-bottom:0;pointer-events:none}[part~=wy-banner-action]{position:sticky;top:calc(4.5 * var(--wy-size, 1rem));bottom:calc(4.5 * var(--wy-size, 1rem));z-index:990;margin-left:auto;margin-right:auto;margin-top:0;margin-bottom:calc(.5 * var(--wy-size, 1rem));cursor:pointer;background-color:var(--wy-primary, var(--wy-primary-light, #2f628c));color:var(--wy-on-primary, var(--wy-on-primary-light, #ffffff))}`;
var Ow = /* @__PURE__ */ ((s) => (s.Active = "active", s.Away = "away", s))(Ow || {});
const Kf = class Kf extends wl {
  constructor(t) {
    if (super(t), t.type !== Xd.ATTRIBUTE || t.name !== "part" || t.strings?.length > 2)
      throw new Error(
        "`partMap()` can only be used in the `part` attribute and must be the only section in the attribute."
      );
  }
  render(t) {
    return " " + Object.keys(t).filter((e) => t[e]).join(" ") + " ";
  }
  update(t, [e]) {
    if (this._previousShadowParts === void 0) {
      this._previousShadowParts = /* @__PURE__ */ new Set(), t.strings !== void 0 && (this._staticShadowParts = new Set(
        t.strings.join(" ").split(/\s/).filter((r) => r !== "")
      ));
      for (const r in e)
        e[r] && !this._staticShadowParts?.has(r) && this._previousShadowParts.add(r);
      return this.render(e);
    }
    const i = t.element.part;
    for (const r of this._previousShadowParts)
      r in e || (i.remove(r), this._previousShadowParts.delete(r));
    for (const r in e) {
      const a = !!e[r];
      a !== this._previousShadowParts.has(r) && !this._staticShadowParts?.has(r) && (a ? (i.add(r), this._previousShadowParts.add(r)) : (i.remove(r), this._previousShadowParts.delete(r)));
    }
    return gu;
  }
};
n(Kf, "ShadowPartMapDirective");
let Qy = Kf;
const tt = ln(Qy);
function Eu(s) {
  const t = s;
  t && (t.complete && t.naturalHeight !== 0 ? !t.classList.contains("wy-loading") && !t.part.contains("wy-loading") ? (t.classList.add("wy-loading", "wy-loaded"), t.part.add("wy-loading", "wy-loaded")) : t.decode().then(() => {
    t.classList.add("wy-loaded"), t.part.add("wy-loaded");
  }) : (t.classList.add("wy-loading"), t.part.add("wy-loading")));
}
n(Eu, "checkImageLoad");
function Au(s) {
  const t = s.target;
  t.tagName === "IMG" && (t.classList.contains("wy-loading") || t.part.contains("wy-loading")) && !t.classList.contains("wy-loaded") && !t.part.contains("wy-loaded") && (t.classList.add("wy-loaded"), t.part.add("wy-loaded"));
}
n(Au, "imageLoaded");
const Lt = j`:where(:host,:host>*,[class^=wy-],[class*=\" wy-\"]){box-sizing:border-box;text-align:initial;font-size:var(--wy-font-size, var(--wy-size, 1em))}:where(:host,:host>*,[class^=wy-],[class*=\" wy-\"]) *:before,:where(:host,:host>*,[class^=wy-],[class*=\" wy-\"]) *:after{box-sizing:inherit}:where(:host,:host>*,[class^=wy-],[class*=\" wy-\"]):where(a),:where(:host,:host>*,[class^=wy-],[class*=\" wy-\"]) *:where(a){text-decoration:var(--wy-link-decoration, none);color:var(--wy-link, var(--wy-primary-light, #2f628c))}:where(:host,:host>*,[class^=wy-],[class*=\" wy-\"]):where(a):hover,:where(:host,:host>*,[class^=wy-],[class*=\" wy-\"]) *:where(a):hover{text-decoration:var(--wy-link-hover-decoration, var(--wy-link-decoration, none))}:where(:host,:host>*,[class^=wy-],[class*=\" wy-\"]):where(a):where(:not([href]):not([class])),:where(:host,:host>*,[class^=wy-],[class*=\" wy-\"]):where(a):where(:not([href]):not([class])):hover,:where(:host,:host>*,[class^=wy-],[class*=\" wy-\"]) *:where(a):where(:not([href]):not([class])),:where(:host,:host>*,[class^=wy-],[class*=\" wy-\"]) *:where(a):where(:not([href]):not([class])):hover{text-decoration:none}:where(:host,:host>*,[class^=wy-],[class*=\" wy-\"]):where(p),:where(:host,:host>*,[class^=wy-],[class*=\" wy-\"]) *:where(p){margin-top:0;margin-bottom:calc(1 * var(--wy-size, 1rem))}:where(:host,:host>*,[class^=wy-],[class*=\" wy-\"]):where(label),:where(:host,:host>*,[class^=wy-],[class*=\" wy-\"]) *:where(label){cursor:inherit}:where(:host([hidden])),:where(:host,:host>*,[class^=wy-],[class*=\" wy-\"])[hidden],:where(:host,:host>*,[class^=wy-],[class*=\" wy-\"]) *[hidden]{display:none!important}:where(:host,:host>*,[class^=wy-],[class*=\" wy-\"]):where([role=button]),:where(:host,:host>*,[class^=wy-],[class*=\" wy-\"]) *:where([role=button]){cursor:pointer;-webkit-user-select:none;user-select:none}`, zw = j`:host(wy-avatar){position:relative;display:inline-flex}[part~=wy-avatar-shape]{border-radius:var(--wy-avatar-border-radius, var(--wy-border-radius, 50%))}[part~=wy-avatar-img]{width:var(--wy-component-avatar-size, calc(2 * var(--wy-size, 1rem)));height:var(--wy-component-avatar-size, calc(2 * var(--wy-size, 1rem)));object-fit:cover}[part~=wy-avatar-img][part~=wy-loading]{transition:opacity .15s ease-out;opacity:0}[part~=wy-avatar-img][part~=wy-loading][part~=wy-loaded]{opacity:1;transition:opacity .15s ease-out}[part~=wy-avatar-img][part~=wy-loading][part~=wy-loaded]~[part~=wy-avatar-initials]{transition:opacity .15s .15s ease-out;opacity:0}[part~=wy-avatar-img]~[part~=wy-avatar-initials]{opacity:1;position:absolute}[part~=wy-avatar-initials]{background-color:var(--wy-component-avatar-background-color, var(--wy-primary, var(--wy-primary-light, #2f628c)));color:var(--wy-component-avatar-color, var(--wy-on-primary, var(--wy-on-primary-light, #ffffff)));width:var(--wy-component-avatar-size, calc(2 * var(--wy-size, 1rem)));height:var(--wy-component-avatar-size, calc(2 * var(--wy-size, 1rem)));display:flex;align-items:center;align-content:center;justify-content:center;justify-items:center;font-size:calc(var(--wy-component-avatar-size, calc(2 * var(--wy-size, 1rem))))}[part~=wy-avatar-initials-text]{font-size:max(8px,var(--wy-avatar-font-size, .382em));font-weight:var(--wy-avatar-font-weight, var(--wy-font-weight-bold, unset));line-height:normal}[part~=wy-avatar-type-icon]{display:flex;position:absolute;z-index:1;bottom:0;right:-8.3333333333%;border-radius:50%;color:var(--wy-tertiary, var(--wy-tertiary-light, #645f41));stroke:var(--wy-on-tertiary, var(--wy-on-tertiary-light, #ffffff))}:host(wy-avatar-group){position:relative;display:inline-flex;flex-shrink:0;width:var(--wy-component-avatar-size, calc(2 * var(--wy-size, 1rem)));height:var(--wy-component-avatar-size, calc(2 * var(--wy-size, 1rem)))}[part~=wy-avatar-back]{position:absolute;top:0;left:0}[part~=wy-avatar-front]{position:absolute;bottom:0;right:0}:host(wy-avatar-header){padding:calc(1.5 * var(--wy-size, 1rem));margin-left:auto;margin-right:auto;text-align:center;display:flex;flex-direction:column;justify-content:center;align-items:center}[part~=wy-avatar-description]{padding:var(--wy-padding, calc(.5 * var(--wy-size, 1rem)))}`, L1 = j`[part~=wy-presence]{width:calc(.75 * var(--wy-size, 1rem));height:calc(.75 * var(--wy-size, 1rem));border-radius:50%;position:absolute;bottom:0;right:-5%;box-sizing:border-box}[part~=wy-presence]:not([part~=wy-presence-active]){display:none}[part~=wy-presence]:after{content:\"\";position:absolute;width:100%;height:100%;top:0;left:0;border-radius:inherit;background-color:var(--wy-presence-active, var(--wy-green-light, #00a38d));background-clip:content-box;padding:calc(.125 * var(--wy-size, 1rem));box-sizing:border-box}[part~=wy-presence-mask]{-webkit-mask-image:radial-gradient(circle calc(.75 * var(--wy-size, 1rem)) at bottom calc(.375 * var(--wy-size, 1rem)) right calc(.375 * var(--wy-size, 1rem) - 5%),rgba(0,0,0,0) calc(.375 * var(--wy-size, 1rem)),rgb(0,0,0) calc(.375 * var(--wy-size, 1rem) + 1px));mask-image:radial-gradient(circle calc(.75 * var(--wy-size, 1rem)) at bottom calc(.375 * var(--wy-size, 1rem)) right calc(.375 * var(--wy-size, 1rem) - 5%),#0000 calc(.375 * var(--wy-size, 1rem)),#000 calc(.375 * var(--wy-size, 1rem) + 1px))}[part~=wy-presence-in-text]{position:absolute;bottom:unset;right:unset}[part~=wy-presence-in-text]:after{left:calc(-.5 * var(--wy-size, 1rem))}`;
var R5 = Object.defineProperty, T5 = Object.getOwnPropertyDescriptor, Fw = /* @__PURE__ */ n((s, t, e, i) => {
  for (var r = i > 1 ? void 0 : i ? T5(t, e) : t, a = s.length - 1, o; a >= 0; a--)
    (o = s[a]) && (r = (i ? o(t, e, r) : o(r)) || r);
  return i && r && R5(t, e, r), r;
}, "__decorateClass$1g"), Xa;
let Yc = (Xa = class extends nt {
  constructor() {
    super(...arguments), this.exportParts = new W(this), this.placement = "avatar";
  }
  render() {
    const t = {
      "wy-presence": !0,
      "wy-presence-active": this.status === Ow.Active,
      "wy-presence-in-text": this.placement === "text"
    };
    return h` <span part=${tt(t)} data-presence-id=${this.id}></span> `;
  }
}, n(Xa, "WyPresence"), Xa);
Yc.styles = [
  Lt,
  L1,
  Y
];
Fw([
  y()
], Yc.prototype, "placement", 2);
Fw([
  y()
], Yc.prototype, "status", 2);
Yc = Fw([
  U("wy-presence")
], Yc);
var L5 = "M15,14C12.33,14 7,15.33 7,18V20H23V18C23,15.33 17.67,14 15,14M1,10V12H9V10M15,12A4,4 0 0,0 19,8A4,4 0 0,0 15,4A4,4 0 0,0 11,8A4,4 0 0,0 15,12Z", O5 = "M15,14C12.33,14 7,15.33 7,18V20H23V18C23,15.33 17.67,14 15,14M6,10V7H4V10H1V12H4V15H6V12H9V10M15,12A4,4 0 0,0 19,8A4,4 0 0,0 15,4A4,4 0 0,0 11,8A4,4 0 0,0 15,12Z", z5 = "M13 14H11V9H13M13 18H11V16H13M1 21H23L12 2L1 21Z", F5 = "M13,13H11V7H13M13,17H11V15H13M12,2A10,10 0 0,0 2,12A10,10 0 0,0 12,22A10,10 0 0,0 22,12A10,10 0 0,0 12,2Z", I5 = "M13 13H11V7H13M11 15H13V17H11M15.73 3H8.27L3 8.27V15.73L8.27 21H15.73L21 15.73V8.27L15.73 3Z", D5 = "M19.5,3.09L15,7.59V4H13V11H20V9H16.41L20.91,4.5L19.5,3.09M4,13V15H7.59L3.09,19.5L4.5,20.91L9,16.41V20H11V13H4Z", V5 = "M10,21V19H6.41L10.91,14.5L9.5,13.09L5,17.59V14H3V21H10M14.5,10.91L19,6.41V10H21V3H14V5H17.59L13.09,9.5L14.5,10.91Z", Rm = "M20,11V13H8L13.5,18.5L12.08,19.92L4.16,12L12.08,4.08L13.5,5.5L8,11H20Z", H5 = "M4,11V13H16L10.5,18.5L11.92,19.92L19.84,12L11.92,4.08L10.5,5.5L16,11H4Z", U5 = "M7.5,18A5.5,5.5 0 0,1 2,12.5A5.5,5.5 0 0,1 7.5,7H18A4,4 0 0,1 22,11A4,4 0 0,1 18,15H9.5A2.5,2.5 0 0,1 7,12.5A2.5,2.5 0 0,1 9.5,10H17V11.5H9.5A1,1 0 0,0 8.5,12.5A1,1 0 0,0 9.5,13.5H18A2.5,2.5 0 0,0 20.5,11A2.5,2.5 0 0,0 18,8.5H7.5A4,4 0 0,0 3.5,12.5A4,4 0 0,0 7.5,16.5H17V18H7.5Z", B5 = "M12,3A9,9 0 0,0 3,12H0L4,16L8,12H5A7,7 0 0,1 12,5A7,7 0 0,1 19,12A7,7 0 0,1 12,19C10.5,19 9.09,18.5 7.94,17.7L6.5,19.14C8.04,20.3 9.94,21 12,21A9,9 0 0,0 21,12A9,9 0 0,0 12,3M14,12A2,2 0 0,0 12,10A2,2 0 0,0 10,12A2,2 0 0,0 12,14A2,2 0 0,0 14,12Z", N5 = "M21,19V20H3V19L5,17V11C5,7.9 7.03,5.17 10,4.29C10,4.19 10,4.1 10,4A2,2 0 0,1 12,2A2,2 0 0,1 14,4C14,4.1 14,4.19 14,4.29C16.97,5.17 19,7.9 19,11V17L21,19M14,21A2,2 0 0,1 12,23A2,2 0 0,1 10,21", q5 = "M20.84,22.73L18.11,20H3V19L5,17V11C5,9.86 5.29,8.73 5.83,7.72L1.11,3L2.39,1.73L22.11,21.46L20.84,22.73M19,15.8V11C19,7.9 16.97,5.17 14,4.29C14,4.19 14,4.1 14,4A2,2 0 0,0 12,2A2,2 0 0,0 10,4C10,4.1 10,4.19 10,4.29C9.39,4.47 8.8,4.74 8.26,5.09L19,15.8M12,23A2,2 0 0,0 14,21H10A2,2 0 0,0 12,23Z", W5 = "M19 3H5C3.9 3 3 3.9 3 5V19C3 20.1 3.9 21 5 21H19C20.1 21 21 20.1 21 19V5C21 3.9 20.1 3 19 3M9 17H7V10H9V17M13 17H11V7H13V17M17 17H15V13H17V17Z", Kp = "M21,7L9,19L3.5,13.5L4.91,12.09L9,16.17L19.59,5.59L21,7Z", j5 = "M0.41,13.41L6,19L7.41,17.58L1.83,12M22.24,5.58L11.66,16.17L7.5,12L6.07,13.41L11.66,19L23.66,7M18,7L16.59,5.58L10.24,11.93L11.66,13.34L18,7Z", K5 = "M12 2C6.5 2 2 6.5 2 12S6.5 22 12 22 22 17.5 22 12 17.5 2 12 2M10 17L5 12L6.41 10.59L10 14.17L17.59 6.58L19 8L10 17Z", Q5 = "M12 2C6.5 2 2 6.5 2 12S6.5 22 12 22 22 17.5 22 12 17.5 2 12 2M12 20C7.59 20 4 16.41 4 12S7.59 4 12 4 20 7.59 20 12 16.41 20 12 20M16.59 7.58L10 14.17L7.41 11.59L6 13L10 17L18 9L16.59 7.58Z", G5 = "M19,3H5C3.89,3 3,3.89 3,5V19A2,2 0 0,0 5,21H19A2,2 0 0,0 21,19V5C21,3.89 20.1,3 19,3M19,5V19H5V5H19Z", Z5 = "M19,19H5V5H15V3H5C3.89,3 3,3.89 3,5V19A2,2 0 0,0 5,21H19A2,2 0 0,0 21,19V11H19M7.91,10.08L6.5,11.5L11,16L21,6L19.59,4.58L11,13.17L7.91,10.08Z", Y5 = "M12,20A8,8 0 0,1 4,12A8,8 0 0,1 12,4A8,8 0 0,1 20,12A8,8 0 0,1 12,20M12,2A10,10 0 0,0 2,12A10,10 0 0,0 12,22A10,10 0 0,0 22,12A10,10 0 0,0 12,2Z", X5 = "M19,6.41L17.59,5L12,10.59L6.41,5L5,6.41L10.59,12L5,17.59L6.41,19L12,13.41L17.59,19L19,17.59L13.41,12L19,6.41Z", J5 = "M12,2C17.53,2 22,6.47 22,12C22,17.53 17.53,22 12,22C6.47,22 2,17.53 2,12C2,6.47 6.47,2 12,2M15.59,7L12,10.59L8.41,7L7,8.41L10.59,12L7,15.59L8.41,17L12,13.41L15.59,17L17,15.59L13.41,12L17,8.41L15.59,7Z", tx = "M6.5 20Q4.22 20 2.61 18.43 1 16.85 1 14.58 1 12.63 2.17 11.1 3.35 9.57 5.25 9.15 5.88 6.85 7.75 5.43 9.63 4 12 4 14.93 4 16.96 6.04 19 8.07 19 11 20.73 11.2 21.86 12.5 23 13.78 23 15.5 23 17.38 21.69 18.69 20.38 20 18.5 20Z", ex = "M9,22A1,1 0 0,1 8,21V18H4A2,2 0 0,1 2,16V4C2,2.89 2.9,2 4,2H20A2,2 0 0,1 22,4V16A2,2 0 0,1 20,18H13.9L10.2,21.71C10,21.9 9.75,22 9.5,22V22H9Z", ix = "M9,22A1,1 0 0,1 8,21V18H4A2,2 0 0,1 2,16V4C2,2.89 2.9,2 4,2H20A2,2 0 0,1 22,4V16A2,2 0 0,1 20,18H13.9L10.2,21.71C10,21.9 9.75,22 9.5,22V22H9M10,16V19.08L13.08,16H20V4H4V16H10Z", rx = "M15,9H5V5H15M12,19A3,3 0 0,1 9,16A3,3 0 0,1 12,13A3,3 0 0,1 15,16A3,3 0 0,1 12,19M17,3H5C3.89,3 3,3.9 3,5V19A2,2 0 0,0 5,21H19A2,2 0 0,0 21,19V7L17,3Z", sx = "M19,1L17.74,3.75L15,5L17.74,6.26L19,9L20.25,6.26L23,5L20.25,3.75M9,4L6.5,9.5L1,12L6.5,14.5L9,20L11.5,14.5L17,12L11.5,9.5M19,15L17.74,17.74L15,19L17.74,20.25L19,23L20.25,20.25L23,19L20.25,17.74", nx = "M19,4H15.5L14.5,3H9.5L8.5,4H5V6H19M6,19A2,2 0 0,0 8,21H16A2,2 0 0,0 18,19V7H6V19Z", ax = "M6,19A2,2 0 0,0 8,21H16A2,2 0 0,0 18,19V7H6V19M8.46,11.88L9.87,10.47L12,12.59L14.12,10.47L15.53,11.88L13.41,14L15.53,16.12L14.12,17.53L12,15.41L9.88,17.53L8.47,16.12L10.59,14L8.46,11.88M15.5,4L14.5,3H9.5L8.5,4H5V6H19V4H15.5Z", ox = "M14,14H16L12,10L8,14H10V18H14V14M6,7H18V19C18,19.5 17.8,20 17.39,20.39C17,20.8 16.5,21 16,21H8C7.5,21 7,20.8 6.61,20.39C6.2,20 6,19.5 6,19V7M19,4V6H5V4H8.5L9.5,3H14.5L15.5,4H19Z", cx = "M12,16A2,2 0 0,1 14,18A2,2 0 0,1 12,20A2,2 0 0,1 10,18A2,2 0 0,1 12,16M12,10A2,2 0 0,1 14,12A2,2 0 0,1 12,14A2,2 0 0,1 10,12A2,2 0 0,1 12,10M12,4A2,2 0 0,1 14,6A2,2 0 0,1 12,8A2,2 0 0,1 10,6A2,2 0 0,1 12,4Z", lx = "M5,20H19V18H5M19,9H15V3H9V9H5L12,16L19,9Z", dx = "M17.9,17.39C17.64,16.59 16.89,16 16,16H15V13A1,1 0 0,0 14,12H8V10H10A1,1 0 0,0 11,9V7H13A2,2 0 0,0 15,5V4.59C17.93,5.77 20,8.64 20,12C20,14.08 19.2,15.97 17.9,17.39M11,19.93C7.05,19.44 4,16.08 4,12C4,11.38 4.08,10.78 4.21,10.21L9,15V16A2,2 0 0,0 11,18M12,2A10,10 0 0,0 2,12A10,10 0 0,0 12,22A10,10 0 0,0 22,12A10,10 0 0,0 12,2Z", hx = "M20,8L12,13L4,8V6L12,11L20,6M20,4H4C2.89,4 2,4.89 2,6V18A2,2 0 0,0 4,20H20A2,2 0 0,0 22,18V6C22,4.89 21.1,4 20,4Z", ux = "M12,17.5C14.33,17.5 16.3,16.04 17.11,14H6.89C7.69,16.04 9.67,17.5 12,17.5M8.5,11A1.5,1.5 0 0,0 10,9.5A1.5,1.5 0 0,0 8.5,8A1.5,1.5 0 0,0 7,9.5A1.5,1.5 0 0,0 8.5,11M15.5,11A1.5,1.5 0 0,0 17,9.5A1.5,1.5 0 0,0 15.5,8A1.5,1.5 0 0,0 14,9.5A1.5,1.5 0 0,0 15.5,11M12,20A8,8 0 0,1 4,12A8,8 0 0,1 12,4A8,8 0 0,1 20,12A8,8 0 0,1 12,20M12,2C6.47,2 2,6.5 2,12A10,10 0 0,0 12,22A10,10 0 0,0 22,12A10,10 0 0,0 12,2Z", px = "M15 18H18V15H20V18H23V20H20V23H18V20H15V18M12 17.5C9.67 17.5 7.69 16.04 6.89 14H15.69C14.5 14.78 13.62 15.97 13.23 17.36C12.83 17.45 12.42 17.5 12 17.5M8.5 11C7.67 11 7 10.33 7 9.5C7 8.67 7.67 8 8.5 8C9.33 8 10 8.67 10 9.5C10 10.33 9.33 11 8.5 11M15.5 11C14.67 11 14 10.33 14 9.5C14 8.67 14.67 8 15.5 8C16.33 8 17 8.67 17 9.5C17 10.33 16.33 11 15.5 11M12 20L13.07 19.93C13.18 20.61 13.4 21.26 13.72 21.85C13.16 21.95 12.58 22 12 22C6.47 22 2 17.5 2 12C2 6.5 6.47 2 12 2C17.5 2 22 6.5 22 12C22 12.59 21.95 13.16 21.85 13.72C21.26 13.4 20.62 13.18 19.93 13.07L20 12C20 7.58 16.42 4 12 4C7.58 4 4 7.58 4 12C4 16.42 7.58 20 12 20Z", yx = "M13,9V3.5L18.5,9M6,2C4.89,2 4,2.89 4,4V20A2,2 0 0,0 6,22H18A2,2 0 0,0 20,20V8L14,2H6Z", Tm = "M13,9H18.5L13,3.5V9M6,2H14L20,8V20A2,2 0 0,1 18,22H6C4.89,22 4,21.1 4,20V4C4,2.89 4.89,2 6,2M6.12,15.5L9.86,19.24L11.28,17.83L8.95,15.5L11.28,13.17L9.86,11.76L6.12,15.5M17.28,15.5L13.54,11.76L12.12,13.17L14.45,15.5L12.12,17.83L13.54,19.24L17.28,15.5Z", Lm = "M13,9H18.5L13,3.5V9M6,2H14L20,8V20A2,2 0 0,1 18,22H6C4.89,22 4,21.1 4,20V4C4,2.89 4.89,2 6,2M15,18V16H6V18H15M18,14V12H6V14H18Z", wx = "M14,2H6A2,2 0 0,0 4,4V20A2,2 0 0,0 6,22H18A2,2 0 0,0 20,20V8L14,2M15.8,20H14L12,16.6L10,20H8.2L11.1,15.5L8.2,11H10L12,14.4L14,11H15.8L12.9,15.5L15.8,20M13,9V3.5L18.5,9H13Z", fx = "M13,9H18.5L13,3.5V9M6,2H14L20,8V20A2,2 0 0,1 18,22H6C4.89,22 4,21.1 4,20V4C4,2.89 4.89,2 6,2M6,20H15L18,20V12L14,16L12,14L6,20M8,9A2,2 0 0,0 6,11A2,2 0 0,0 8,13A2,2 0 0,0 10,11A2,2 0 0,0 8,9Z", mx = "M14,2H6A2,2 0 0,0 4,4V20A2,2 0 0,0 6,22H18A2,2 0 0,0 20,20V8L14,2M13,13H11V18A2,2 0 0,1 9,20A2,2 0 0,1 7,18A2,2 0 0,1 9,16C9.4,16 9.7,16.1 10,16.3V11H13V13M13,9V3.5L18.5,9H13Z", vx = "M12.6,12.3H10.6V15.5H12.7C13.3,15.5 13.6,15.3 13.9,15C14.2,14.7 14.3,14.4 14.3,13.9C14.3,13.4 14.2,13.1 13.9,12.8C13.6,12.5 13.2,12.3 12.6,12.3M14,2H6A2,2 0 0,0 4,4V20A2,2 0 0,0 6,22H18A2,2 0 0,0 20,20V8L14,2M15.2,16C14.6,16.5 14.1,16.7 12.8,16.7H10.6V20H9V11H12.8C14.1,11 14.7,11.3 15.2,11.8C15.8,12.4 16,13 16,13.9C16,14.8 15.8,15.5 15.2,16M13,9V3.5L18.5,9H13Z", gx = "M14,2H6A2,2 0 0,0 4,4V20A2,2 0 0,0 6,22H18A2,2 0 0,0 20,20V8L14,2M13.5,16V19H10.5V16H8L12,12L16,16H13.5M13,9V3.5L18.5,9H13Z", bx = "M13,9H18.5L13,3.5V9M6,2H14L20,8V20A2,2 0 0,1 18,22H6C4.89,22 4,21.1 4,20V4C4,2.89 4.89,2 6,2M17,19V13L14,15.2V13H7V19H14V16.8L17,19Z", xx = "M14,2H6A2,2 0 0,0 4,4V20A2,2 0 0,0 6,22H18A2,2 0 0,0 20,20V8L14,2M15.2,20H13.8L12,13.2L10.2,20H8.8L6.6,11H8.1L9.5,17.8L11.3,11H12.6L14.4,17.8L15.8,11H17.3L15.2,20M13,9V3.5L18.5,9H13Z", Cx = "M20,2H4C2.89,2 2,2.89 2,4V20C2,21.11 2.89,22 4,22H20C21.11,22 22,21.11 22,20V4C22,2.89 21.11,2 20,2M12,4L15,7H13V9H11V7H9M7,15L4,12L7,9V11H9V13H7M12,20L9,17H11V15H13V17H15M17,15V13H15V11H17V9L20,12", $x = "M17 4H20C21.1 4 22 4.9 22 6V8H20V6H17V4M4 8V6H7V4H4C2.9 4 2 4.9 2 6V8H4M20 16V18H17V20H20C21.1 20 22 19.1 22 18V16H20M7 18H4V16H2V18C2 19.1 2.9 20 4 20H7V18M18 8H6V16H18V8Z", kx = "M20 6H12L10 4H4C2.9 4 2 4.9 2 6V18C2 19.1 2.9 20 4 20H20C21.1 20 22 19.1 22 18V8C22 6.9 21.1 6 20 6M18 12H16V14H18V16H16V18H14V16H16V14H14V12H16V10H14V8H16V10H18V12Z", _x = "M17,7H22V17H17V19A1,1 0 0,0 18,20H20V22H17.5C16.95,22 16,21.55 16,21C16,21.55 15.05,22 14.5,22H12V20H14A1,1 0 0,0 15,19V5A1,1 0 0,0 14,4H12V2H14.5C15.05,2 16,2.45 16,3C16,2.45 16.95,2 17.5,2H20V4H18A1,1 0 0,0 17,5V7M2,7H13V9H4V15H13V17H2V7M20,15V9H17V15H20Z", Px = "M5,5H10V7H7V10H5V5M14,5H19V10H17V7H14V5M17,14H19V19H14V17H17V14M10,17V19H5V14H7V17H10Z", Sx = "M14,14H19V16H16V19H14V14M5,14H10V19H8V16H5V14M8,5H10V10H5V8H8V5M19,8V10H14V5H16V8H19Z", Mx = "M15.07,11.25L14.17,12.17C13.45,12.89 13,13.5 13,15H11V14.5C11,13.39 11.45,12.39 12.17,11.67L13.41,10.41C13.78,10.05 14,9.55 14,9C14,7.89 13.1,7 12,7A2,2 0 0,0 10,9H8A4,4 0 0,1 12,5A4,4 0 0,1 16,9C16,9.88 15.64,10.67 15.07,11.25M13,19H11V17H13M12,2A10,10 0 0,0 2,12A10,10 0 0,0 12,22A10,10 0 0,0 22,12C22,6.47 17.5,2 12,2Z", Ex = "M11,9H13V7H11M12,20C7.59,20 4,16.41 4,12C4,7.59 7.59,4 12,4C16.41,4 20,7.59 20,12C20,16.41 16.41,20 12,20M12,2A10,10 0 0,0 2,12A10,10 0 0,0 12,22A10,10 0 0,0 22,12A10,10 0 0,0 12,2M11,17H13V11H11V17Z", Ax = "M9.5,3A6.5,6.5 0 0,1 16,9.5C16,11.11 15.41,12.59 14.44,13.73L14.71,14H15.5L20.5,19L19,20.5L14,15.5V14.71L13.73,14.44C12.59,15.41 11.11,16 9.5,16A6.5,6.5 0 0,1 3,9.5A6.5,6.5 0 0,1 9.5,3M9.5,5C7,5 5,7 5,9.5C5,12 7,14 9.5,14C12,14 14,12 14,9.5C14,7 12,5 9.5,5Z", Rx = "M7,10L12,15L17,10H7Z", Tx = "M7,15L12,10L17,15H7Z", Lx = "M20,2H4A2,2 0 0,0 2,4V22L6,18H20A2,2 0 0,0 22,16V4C22,2.89 21.1,2 20,2Z", Ox = "M20,2H4A2,2 0 0,0 2,4V22L6,18H20A2,2 0 0,0 22,16V4A2,2 0 0,0 20,2M6,9H18V11H6M14,14H6V12H14M18,8H6V6H18", zx = "M19,13H5V11H19V13Z", Fx = "M21 2C22.05 2 22.92 2.81 23 3.85L23 4V16C23 17.05 22.18 17.92 21.15 18L21 18H14V20H16V22H8V20H10V18H3C1.95 18 1.08 17.18 1 16.15L1 16V4C1 2.94 1.81 2.08 2.85 2L3 2H21M21 4H3V16H21V4M12 11C14.21 11 16 11.9 16 13V14H8V13C8 11.9 9.79 11 12 11M12 6C13.11 6 14 6.9 14 8S13.11 10 12 10 10 9.11 10 8 10.9 6 12 6Z", Ix = "M14,3V5H17.59L7.76,14.83L9.17,16.24L19,6.41V10H21V3M19,19H5V5H12V3H5C3.89,3 3,3.9 3,5V19A2,2 0 0,0 5,21H19A2,2 0 0,0 21,19V12H19V19Z", Dx = "M20.71,7.04C21.1,6.65 21.1,6 20.71,5.63L18.37,3.29C18,2.9 17.35,2.9 16.96,3.29L15.12,5.12L18.87,8.87M3,17.25V21H6.75L17.81,9.93L14.06,6.18L3,17.25Z", Vx = "M16,12V4H17V2H7V4H8V12L6,14V16H11.2V22H12.8V16H18V14L16,12Z", Hx = "M2,5.27L3.28,4L20,20.72L18.73,22L12.8,16.07V22H11.2V16H6V14L8,12V11.27L2,5.27M16,12L18,14V16H17.82L8,6.18V4H7V2H17V4H16V12Z", Ux = "M19,13H13V19H11V13H5V11H11V5H13V11H19V13Z", Bx = "M12,20C7.59,20 4,16.41 4,12C4,7.59 7.59,4 12,4C16.41,4 20,7.59 20,12C20,16.41 16.41,20 12,20M12,2A10,10 0 0,0 2,12A10,10 0 0,0 12,22A10,10 0 0,0 22,12A10,10 0 0,0 12,2M13,7H11V11H7V13H11V17H13V13H17V11H13V7Z", Nx = "M13,3A9,9 0 0,0 4,12H1L4.89,15.89L4.96,16.03L9,12H6A7,7 0 0,1 13,5A7,7 0 0,1 20,12A7,7 0 0,1 13,19C11.07,19 9.32,18.21 8.06,16.94L6.64,18.36C8.27,20 10.5,21 13,21A9,9 0 0,0 22,12A9,9 0 0,0 13,3Z", qx = "M4,3H5V5H3V4A1,1 0 0,1 4,3M20,3A1,1 0 0,1 21,4V5H19V3H20M15,5V3H17V5H15M11,5V3H13V5H11M7,5V3H9V5H7M21,20A1,1 0 0,1 20,21H19V19H21V20M15,21V19H17V21H15M11,21V19H13V21H11M7,21V19H9V21H7M4,21A1,1 0 0,1 3,20V19H5V21H4M3,15H5V17H3V15M21,15V17H19V15H21M3,11H5V13H3V11M21,11V13H19V11H21M3,7H5V9H3V7M21,7V9H19V7H21Z", Wx = "M2,21L23,12L2,3V10L17,12L2,14V21Z", jx = "M13,19H14A1,1 0 0,1 15,20H15.73L13,17.27V19M22,20V21.18L20.82,20H22M21,22.72L19.73,24L17.73,22H15A1,1 0 0,1 14,23H10A1,1 0 0,1 9,22H2V20H9A1,1 0 0,1 10,19H11V17H4A1,1 0 0,1 3,16V12A1,1 0 0,1 4,11H6.73L4.73,9H4A1,1 0 0,1 3,8V7.27L1,5.27L2.28,4L21,22.72M4,3H20A1,1 0 0,1 21,4V8A1,1 0 0,1 20,9H9.82L7,6.18V5H5.82L3.84,3C3.89,3 3.94,3 4,3M20,11A1,1 0 0,1 21,12V16A1,1 0 0,1 20,17H17.82L11.82,11H20M9,7H10V5H9V7M9,15H10V14.27L9,13.27V15M5,13V15H7V13H5Z", Kx = "M12 1L3 5V11C3 16.55 6.84 21.74 12 23C17.16 21.74 21 16.55 21 11V5L12 1M15.08 16L12 14.15L8.93 16L9.74 12.5L7.03 10.16L10.61 9.85L12 6.55L13.39 9.84L16.97 10.15L14.26 12.5L15.08 16Z", Qx = "M21 11C21 16.55 17.16 21.74 12 23C6.84 21.74 3 16.55 3 11V5L12 1L21 5V11M12 21C15.75 20 19 15.54 19 11.22V6.3L12 3.18L5 6.3V11.22C5 15.54 8.25 20 12 21M15.05 16L11.97 14.15L8.9 16L9.71 12.5L7 10.16L10.58 9.85L11.97 6.55L13.37 9.84L16.95 10.15L14.23 12.5L15.05 16", Gx = "M18 21L14 17H17V7H14L18 3L22 7H19V17H22M2 19V17H12V19M2 13V11H9V13M2 7V5H6V7H2Z", Zx = "M12,17.27L18.18,21L16.54,13.97L22,9.24L14.81,8.62L12,2L9.19,8.62L2,9.24L7.45,13.97L5.82,21L12,17.27Z", Yx = "M12,1L9,9L1,12L9,15L12,23L15,15L23,12L15,9L12,1Z", Xx = "M20.8 22.7L17.9 19.8L18.2 21L12 17.3L5.8 21L7.4 14L2 9.2L6.9 8.8L1.1 3L2.4 1.7L22.1 21.4L20.8 22.7M22 9.2L14.8 8.6L12 2L10 6.8L16.9 13.7L22 9.2Z", Jx = "M21,9L17,5V8H10V10H17V13M7,11L3,15L7,19V16H14V14H7V11Z", t4 = "M23,10C23,8.89 22.1,8 21,8H14.68L15.64,3.43C15.66,3.33 15.67,3.22 15.67,3.11C15.67,2.7 15.5,2.32 15.23,2.05L14.17,1L7.59,7.58C7.22,7.95 7,8.45 7,9V19A2,2 0 0,0 9,21H18C18.83,21 19.54,20.5 19.84,19.78L22.86,12.73C22.95,12.5 23,12.26 23,12V10M1,21H5V9H1V21Z", e4 = "M5,9V21H1V9H5M9,21A2,2 0 0,1 7,19V9C7,8.45 7.22,7.95 7.59,7.59L14.17,1L15.23,2.06C15.5,2.33 15.67,2.7 15.67,3.11L15.64,3.43L14.69,8H21C22.11,8 23,8.9 23,10V12C23,12.26 22.95,12.5 22.86,12.73L19.84,19.78C19.54,20.5 18.83,21 18,21H9M9,19H18.03L21,12V10H12.21L13.34,4.68L9,9.03V19Z", i4 = "M9,3V4H4V6H5V19A2,2 0 0,0 7,21H17A2,2 0 0,0 19,19V6H20V4H15V3H9M9,8H11V17H9V8M13,8H15V17H13V8Z", r4 = "M17,10.5V7A1,1 0 0,0 16,6H4A1,1 0 0,0 3,7V17A1,1 0 0,0 4,18H16A1,1 0 0,0 17,17V13.5L21,17.5V6.5L17,10.5Z", s4 = "M3 5V19H20V5H3M7 7V9H5V7H7M5 13V11H7V13H5M5 15H7V17H5V15M18 17H9V15H18V17M18 13H9V11H18V13M18 9H9V7H18V9Z", n4 = "M4 5V18H21V5H4M14 7V10.5H11V7H14M6 7H9V10.5H6V7M6 16V12.5H9V16H6M11 16V12.5H14V16H11M19 16H16V12.5H19V16M16 10.5V7H19V10.5H16Z", a4 = "M2.28,3L1,4.27L2.47,5.74C2.04,6 1.61,6.29 1.2,6.6L3,9C3.53,8.6 4.08,8.25 4.66,7.93L6.89,10.16C6.15,10.5 5.44,10.91 4.8,11.4L6.6,13.8C7.38,13.22 8.26,12.77 9.2,12.47L11.75,15C10.5,15.07 9.34,15.5 8.4,16.2L12,21L14.46,17.73L17.74,21L19,19.72M12,3C9.85,3 7.8,3.38 5.9,4.07L8.29,6.47C9.5,6.16 10.72,6 12,6C15.38,6 18.5,7.11 21,9L22.8,6.6C19.79,4.34 16.06,3 12,3M12,9C11.62,9 11.25,9 10.88,9.05L14.07,12.25C15.29,12.53 16.43,13.07 17.4,13.8L19.2,11.4C17.2,9.89 14.7,9 12,9Z";
const Om = qx, zm = {
  "zoom-meetings": '<path d="M21.5 12.5c0 .96-.08 1.91-.23 2.83a7.198 7.198 0 01-5.95 5.95 17.527 17.527 0 01-5.66 0 7.198 7.198 0 01-5.95-5.95 17.527 17.527 0 010-5.66c.49-3.05 2.9-5.46 5.95-5.95a17.527 17.527 0 015.66 0c3.05.49 5.46 2.9 5.95 5.95.15.92.23 1.86.23 2.83z" fill="#0b5cff"/><path d="M14.43 14.75c0 .53-.43.96-.96.96H9.29c-1.07 0-1.93-.86-1.93-1.93v-3.54c0-.53.43-.96.96-.96h4.18c1.07 0 1.93.86 1.93 1.93v3.54zM16.87 9.86l-1.41 1.06c-.24.18-.39.47-.39.77v1.61c0 .3.14.59.39.77l1.41 1.06c.32.24.77.01.77-.39v-4.5c0-.4-.45-.62-.77-.39z" fill="#fff"/>',
  "google-meet": '<path fill="#1e88e5" d="M3.5 9.07v5.86l2.09.42 2.1-.42V9.07l-2.1-.42-2.09.42z"/><path d="M17.73 12v5.86c0 .69-.56 1.26-1.26 1.26H7.69l-.42-2.09.42-2.09h5.86v-2.93l2.09-.42 2.09.42z" fill="#4caf50"/><path d="M17.73 6.14V12h-4.19V9.07H7.68l-.42-2.09.42-2.09h8.79c.69 0 1.26.56 1.26 1.26z" fill="#fbc02d"/><path d="M7.69 14.93v4.19H4.76c-.69 0-1.26-.56-1.26-1.26v-2.93h4.19z" fill="#1565c0"/><path fill="#e53935" d="M7.69 4.88v4.19H3.5l4.19-4.19z"/><path fill="#2e7d32" d="M18.15 12l-.42 3.54L13.55 12l4.18-3.54.42 3.54z"/><path d="M21.5 6.19v11.63c0 .35-.41.55-.68.33l-3.09-2.6V8.48l3.09-2.6c.27-.22.68-.03.68.33z" fill="#4caf50"/>',
  "webex-meetings": '<defs><radialGradient id="prefix__b" cx="-951.63" cy="549.04" fx="-951.63" fy="549.04" r=".05" gradientTransform="matrix(-87.16473 -146.04628 -122.52671 73.12756 -15652.44 -179117.85)" gradientUnits="userSpaceOnUse"><stop offset="0" stop-color="#007383"/><stop offset=".41" stop-color="#00717e" stop-opacity=".75"/><stop offset=".87" stop-color="#007281" stop-opacity="0"/></radialGradient><radialGradient id="prefix__c" cx="-952.67" cy="549.07" fx="-952.67" fy="549.07" r=".05" gradientTransform="rotate(-126.7 -53497.341 -58162.752) scale(138.69 -91.09)" gradientUnits="userSpaceOnUse"><stop offset="0" stop-color="#218970"/><stop offset=".46" stop-color="#267e6a"/><stop offset="1" stop-color="#026c51" stop-opacity="0"/></radialGradient><radialGradient id="prefix__d" cx="-952.61" cy="541.2" fx="-952.61" fy="541.2" r=".05" gradientTransform="matrix(-33.8827 -107.79009 -56.09397 17.63256 -1902.72 -112216.84)" gradientUnits="userSpaceOnUse"><stop offset="0" stop-color="#178697"/><stop offset=".41" stop-color="#17838f" stop-opacity=".79"/><stop offset=".87" stop-color="#007281" stop-opacity="0"/></radialGradient><radialGradient id="prefix__e" cx="-949.76" cy="547.24" fx="-949.76" fy="547.24" r=".05" gradientTransform="matrix(-146.98086 -246.36712 -75.94201 45.30646 -98045.43 -258761.67)" gradientUnits="userSpaceOnUse"><stop offset="0" stop-color="#25342f" stop-opacity=".3"/><stop offset="1" stop-color="#25342f" stop-opacity="0"/></radialGradient><radialGradient id="prefix__f" cx="-942.87" cy="563.62" fx="-942.87" fy="563.62" r=".05" gradientTransform="rotate(85.1 -69752.427 38265.017) scale(108.08 -61.22)" gradientUnits="userSpaceOnUse"><stop offset="0" stop-color="#056d4f"/><stop offset=".23" stop-color="#056c4e" stop-opacity=".75"/><stop offset=".6" stop-color="#056c4e" stop-opacity=".3"/><stop offset=".87" stop-color="#056c4f" stop-opacity="0"/></radialGradient><radialGradient id="prefix__g" cx="-940.36" cy="556.77" fx="-940.36" fy="556.77" r=".05" gradientTransform="matrix(68.96997 106.65216 55.9082 -36.1548 33735.1 120422.29)" gradientUnits="userSpaceOnUse"><stop offset="0" stop-color="#22b67b"/><stop offset=".41" stop-color="#24be82"/><stop offset="1" stop-color="#24be82" stop-opacity=".07"/></radialGradient><radialGradient id="prefix__i" cx="-940.77" cy="554.96" fx="-940.77" fy="554.96" r=".05" gradientTransform="rotate(49.69 -177135.555 122596.9) scale(165.85 -165.4)" gradientUnits="userSpaceOnUse"><stop offset="0" stop-color="#09f8ff"/><stop offset="1" stop-color="#0bf7f9" stop-opacity="0"/></radialGradient><radialGradient id="prefix__j" cx="-944.1" cy="553.18" fx="-944.1" fy="553.18" r=".05" gradientTransform="rotate(48.05 -367132.581 263331.898) scale(336.36 -335.78)" gradientUnits="userSpaceOnUse"><stop offset="0" stop-color="#00b8ff"/><stop offset=".75" stop-color="#00b9fc" stop-opacity="0"/></radialGradient><radialGradient id="prefix__k" cx="-951.17" cy="546.23" fx="-951.17" fy="546.23" r=".05" gradientTransform="matrix(-48.64247 -154.74493 -107.10322 33.66679 12252.88 -165563.53)" gradientUnits="userSpaceOnUse"><stop offset="0" stop-color="#00bcff" stop-opacity=".76"/><stop offset=".57" stop-color="#00bafc" stop-opacity="0"/></radialGradient><radialGradient id="prefix__l" cx="-953.78" cy="550.07" fx="-953.78" fy="550.07" r=".05" gradientTransform="matrix(-72.9418 -88.45403 -61.91396 51.05608 -35502.54 -112443.2)" gradientUnits="userSpaceOnUse"><stop offset="0" stop-color="#5fec50"/><stop offset="1" stop-color="#3cc08e" stop-opacity="0"/></radialGradient><radialGradient id="prefix__m" cx="-954.59" cy="523.18" fx="-954.59" fy="523.18" r=".05" gradientTransform="rotate(-79.11 -8961.313 -26349.067) scale(34.73 -23.99)" gradientUnits="userSpaceOnUse"><stop offset="0" stop-color="#28855b"/><stop offset="1" stop-color="#14795c" stop-opacity="0"/></radialGradient><radialGradient id="prefix__n" cx="-955.7" cy="534.72" fx="-955.7" fy="534.72" r=".05" gradientTransform="matrix(6.00138 -27.99393 -39.07222 -8.37636 26647.75 -22260.21)" gradientUnits="userSpaceOnUse"><stop offset="0" stop-color="#28855b"/><stop offset="1" stop-color="#14795c" stop-opacity="0"/></radialGradient><radialGradient id="prefix__o" cx="-954.99" cy="550.19" fx="-954.99" fy="550.19" r=".05" gradientTransform="matrix(-64.43177 -69.7019 -48.95728 45.25565 -34575.21 -91452.74)" gradientUnits="userSpaceOnUse"><stop offset="0" stop-color="#5ff05b"/><stop offset=".81" stop-color="#5de955" stop-opacity="0"/></radialGradient><radialGradient id="prefix__p" cx="-938.28" cy="553.05" fx="-938.28" fy="553.05" r=".05" gradientTransform="matrix(66.96005 51.3617 71.4219 -93.11248 23342.15 99701.75)" gradientUnits="userSpaceOnUse"><stop offset="0" stop-color="#123aa8"/><stop offset=".97" stop-color="#123aa8" stop-opacity="0"/></radialGradient><radialGradient id="prefix__q" cx="-940.1" cy="550.5" fx="-940.1" fy="550.5" r=".05" gradientTransform="matrix(98.43675 14.71147 21.98813 -147.126 80455.97 94829.3)" gradientUnits="userSpaceOnUse"><stop offset="0" stop-color="#123aa8"/><stop offset=".59" stop-color="#123aa8" stop-opacity="0"/></radialGradient><radialGradient id="prefix__r" cx="-938.38" cy="551.97" fx="-938.38" fy="551.97" r=".05" gradientTransform="rotate(24.69 -231174.852 140478.806) scale(85.04 -151.66)" gradientUnits="userSpaceOnUse"><stop offset="0" stop-color="#123aa8"/><stop offset=".76" stop-color="#123aa8" stop-opacity="0"/></radialGradient><radialGradient id="prefix__s" cx="-945.2" cy="560.82" fx="-945.2" fy="560.82" r=".05" gradientTransform="rotate(95.79 -93552.04 41883.384) scale(153.04 -83.75)" gradientUnits="userSpaceOnUse"><stop offset="0" stop-color="#123aa8"/><stop offset=".59" stop-color="#123aa8" stop-opacity="0"/><stop offset="1" stop-color="#123aa8" stop-opacity="0"/></radialGradient><radialGradient id="prefix__t" cx="-945.05" cy="562.66" fx="-945.05" fy="562.66" r=".05" gradientTransform="rotate(93.66 -104350.43 63182.76) scale(184.18 -65.64)" gradientUnits="userSpaceOnUse"><stop offset="0" stop-color="#123aa8"/><stop offset=".88" stop-color="#123aa8" stop-opacity="0"/></radialGradient><radialGradient id="prefix__u" cx="-942.29" cy="567.94" fx="-942.29" fy="567.94" r=".05" gradientTransform="matrix(8.78957 97.33394 51.50044 -4.65066 -20951.98 94373.6)" gradientUnits="userSpaceOnUse"><stop offset="0" stop-color="#123aa8"/><stop offset=".88" stop-color="#123aa8" stop-opacity="0"/></radialGradient><radialGradient id="prefix__v" cx="-942.85" cy="562.23" fx="-942.85" fy="562.23" r=".05" gradientTransform="matrix(4.89377 101.88254 84.00315 -4.03496 -42611.94 98338.06)" gradientUnits="userSpaceOnUse"><stop offset="0" stop-color="#123aa8"/><stop offset=".88" stop-color="#123aa8" stop-opacity="0"/></radialGradient><radialGradient id="prefix__w" cx="-941.74" cy="549.74" fx="-941.74" fy="549.74" r=".05" gradientTransform="rotate(-23.2 244205.91 -321749.854) scale(113.62 -222.29)" gradientUnits="userSpaceOnUse"><stop offset="0" stop-color="#123aa8"/><stop offset=".71" stop-color="#123aa8" stop-opacity="0"/></radialGradient><radialGradient id="prefix__x" cx="-959.91" cy="553.32" fx="-959.91" fy="553.32" r=".05" gradientTransform="matrix(-70.50332 -26.36011 -45.18376 120.84945 -42670.87 -92159.05)" gradientUnits="userSpaceOnUse"><stop offset="0" stop-color="#1a3da0"/><stop offset=".71" stop-color="#123aa8" stop-opacity="0"/></radialGradient><radialGradient id="prefix__y" cx="-940.3" cy="551.63" fx="-940.3" fy="551.63" r=".05" gradientTransform="matrix(137.44492 41.41855 41.86 -138.90985 106154.62 115588.85)" gradientUnits="userSpaceOnUse"><stop offset="0" stop-color="#1e3882"/><stop offset=".76" stop-color="#123aa8" stop-opacity="0"/></radialGradient><radialGradient id="prefix__A" cx="-940.77" cy="554.96" fx="-940.77" fy="554.96" r=".05" gradientTransform="rotate(49.69 -177135.55 122596.884) scale(165.85 -165.4)" gradientUnits="userSpaceOnUse"><stop offset="0" stop-color="#09f8ff"/><stop offset="1" stop-color="#0bf7f9" stop-opacity="0"/></radialGradient><radialGradient id="prefix__D" cx="-951.17" cy="546.23" fx="-951.17" fy="546.23" r=".05" gradientTransform="matrix(-48.64247 -154.74493 -107.10322 33.66679 12252.88 -165563.53)" gradientUnits="userSpaceOnUse"><stop offset="0" stop-color="#00bcff" stop-opacity=".76"/><stop offset=".57" stop-color="#00bafc" stop-opacity="0"/></radialGradient><linearGradient id="prefix__a" x1="76.14" y1="-95.89" x2="64.37" y2="-99.75" gradientTransform="matrix(1 0 0 -1 -58.17 -86.5)" gradientUnits="userSpaceOnUse"><stop offset="0" stop-color="#5cee64"/><stop offset="1" stop-color="#0bf"/></linearGradient><linearGradient id="prefix__h" x1="56.74" y1="-98.36" x2="65.09" y2="-98.43" gradientTransform="matrix(1 0 0 -1 -58.17 -86.5)" gradientUnits="userSpaceOnUse"><stop offset="0" stop-color="#09f8ff"/><stop offset="1" stop-color="#0bf7f9" stop-opacity="0"/></linearGradient><linearGradient id="prefix__z" x1="61.48" y1="-92.95" x2="67.9" y2="-97.04" gradientTransform="matrix(1 0 0 -1 -58.17 -86.5)" gradientUnits="userSpaceOnUse"><stop offset=".23" stop-color="#1dc4ff"/><stop offset="1" stop-color="#1cc1ff" stop-opacity="0"/></linearGradient><linearGradient id="prefix__B" x1="63.13" y1="-92.11" x2="64.93" y2="-93.74" gradientTransform="matrix(1 0 0 -1 -58.17 -86.5)" gradientUnits="userSpaceOnUse"><stop offset="0" stop-color="#26fbff"/><stop offset=".64" stop-color="#0bf7f9" stop-opacity="0"/></linearGradient><linearGradient id="prefix__C" x1="60.36" y1="-97.78" x2="62.85" y2="-97.95" gradientTransform="matrix(1 0 0 -1 -58.17 -86.5)" gradientUnits="userSpaceOnUse"><stop offset="0" stop-color="#26fbff"/><stop offset=".64" stop-color="#0bf7f9" stop-opacity="0"/></linearGradient></defs><path d="M8.59 18.51c-3.33 0-4.47-4.22-4.72-5.04-.64-2.06-.83-3.92-.85-4.11-.12-1.12.78-1.91 1.77-1.91.75 0 1.62.45 1.73 1.54.02.18.17 1.76.71 3.48.56 1.8 1.16 2.49 1.59 2.49.69 0 1.12-1.78 1.49-3.48.28-1.25.57-2.51 1.16-3.63 1.02-1.95 2.57-2.4 3.9-2.4 2.93 0 4.39 3.4 4.39 4.28 0 1.47-1.2 1.85-1.72 1.85-1.14 0-1.52-.76-1.84-1.43-.29-.6-.6-1.1-.99-1.1-.13 0-.25.07-.36.18-.98 1-1.31 6.2-3.26 8.19-1.08 1.1-2.26 1.1-3 1.1z" fill="url(#prefix__a)"/><path d="M8.59 18.51c-3.33 0-4.47-4.22-4.72-5.04-.64-2.06-.83-3.92-.85-4.11-.12-1.12.78-1.91 1.77-1.91.75 0 1.62.45 1.73 1.54.02.18.17 1.76.71 3.48.56 1.8 1.16 2.49 1.59 2.49.69 0 1.12-1.78 1.49-3.48.28-1.25.57-2.51 1.16-3.63 1.02-1.95 2.57-2.4 3.9-2.4 2.93 0 4.39 3.4 4.39 4.28 0 1.47-1.2 1.85-1.72 1.85-1.14 0-1.52-.76-1.84-1.43-.29-.6-.6-1.1-.99-1.1-.13 0-.25.07-.36.18-.98 1-1.31 6.2-3.26 8.19-1.08 1.1-2.26 1.1-3 1.1z" fill="url(#prefix__b)"/><path d="M8.59 18.51c-3.33 0-4.47-4.22-4.72-5.04-.64-2.06-.83-3.92-.85-4.11-.12-1.12.78-1.91 1.77-1.91.75 0 1.62.45 1.73 1.54.02.18.17 1.76.71 3.48.56 1.8 1.16 2.49 1.59 2.49.69 0 1.12-1.78 1.49-3.48.28-1.25.57-2.51 1.16-3.63 1.02-1.95 2.57-2.4 3.9-2.4 2.93 0 4.39 3.4 4.39 4.28 0 1.47-1.2 1.85-1.72 1.85-1.14 0-1.52-.76-1.84-1.43-.29-.6-.6-1.1-.99-1.1-.13 0-.25.07-.36.18-.98 1-1.31 6.2-3.26 8.19-1.08 1.1-2.26 1.1-3 1.1z" fill="url(#prefix__c)"/><path d="M8.59 18.51c-3.33 0-4.47-4.22-4.72-5.04-.64-2.06-.83-3.92-.85-4.11-.12-1.12.78-1.91 1.77-1.91.75 0 1.62.45 1.73 1.54.02.18.17 1.76.71 3.48.56 1.8 1.16 2.49 1.59 2.49.69 0 1.12-1.78 1.49-3.48.28-1.25.57-2.51 1.16-3.63 1.02-1.95 2.57-2.4 3.9-2.4 2.93 0 4.39 3.4 4.39 4.28 0 1.47-1.2 1.85-1.72 1.85-1.14 0-1.52-.76-1.84-1.43-.29-.6-.6-1.1-.99-1.1-.13 0-.25.07-.36.18-.98 1-1.31 6.2-3.26 8.19-1.08 1.1-2.26 1.1-3 1.1z" fill="url(#prefix__d)"/><path d="M8.59 18.51c-3.33 0-4.47-4.22-4.72-5.04-.64-2.06-.83-3.92-.85-4.11-.12-1.12.78-1.91 1.77-1.91.75 0 1.62.45 1.73 1.54.02.18.17 1.76.71 3.48.56 1.8 1.16 2.49 1.59 2.49.69 0 1.12-1.78 1.49-3.48.28-1.25.57-2.51 1.16-3.63 1.02-1.95 2.57-2.4 3.9-2.4 2.93 0 4.39 3.4 4.39 4.28 0 1.47-1.2 1.85-1.72 1.85-1.14 0-1.52-.76-1.84-1.43-.29-.6-.6-1.1-.99-1.1-.13 0-.25.07-.36.18-.98 1-1.31 6.2-3.26 8.19-1.08 1.1-2.26 1.1-3 1.1z" fill="url(#prefix__e)"/><path d="M8.59 18.51c-3.33 0-4.47-4.22-4.72-5.04-.64-2.06-.83-3.92-.85-4.11-.12-1.12.78-1.91 1.77-1.91.75 0 1.62.45 1.73 1.54.02.18.17 1.76.71 3.48.56 1.8 1.16 2.49 1.59 2.49.69 0 1.12-1.78 1.49-3.48.28-1.25.57-2.51 1.16-3.63 1.02-1.95 2.57-2.4 3.9-2.4 2.93 0 4.39 3.4 4.39 4.28 0 1.47-1.2 1.85-1.72 1.85-1.14 0-1.52-.76-1.84-1.43-.29-.6-.6-1.1-.99-1.1-.13 0-.25.07-.36.18-.98 1-1.31 6.2-3.26 8.19-1.08 1.1-2.26 1.1-3 1.1z" fill="url(#prefix__f)"/><path d="M8.59 18.51c-3.33 0-4.47-4.22-4.72-5.04-.64-2.06-.83-3.92-.85-4.11-.12-1.12.78-1.91 1.77-1.91.75 0 1.62.45 1.73 1.54.02.18.17 1.76.71 3.48.56 1.8 1.16 2.49 1.59 2.49.69 0 1.12-1.78 1.49-3.48.28-1.25.57-2.51 1.16-3.63 1.02-1.95 2.57-2.4 3.9-2.4 2.93 0 4.39 3.4 4.39 4.28 0 1.47-1.2 1.85-1.72 1.85-1.14 0-1.52-.76-1.84-1.43-.29-.6-.6-1.1-.99-1.1-.13 0-.25.07-.36.18-.98 1-1.31 6.2-3.26 8.19-1.08 1.1-2.26 1.1-3 1.1z" fill="url(#prefix__g)"/><path d="M8.59 18.51c-3.33 0-4.47-4.22-4.72-5.04-.64-2.06-.83-3.92-.85-4.11-.12-1.12.78-1.91 1.77-1.91.75 0 1.62.45 1.73 1.54.02.18.17 1.76.71 3.48.56 1.8 1.16 2.49 1.59 2.49.69 0 1.12-1.78 1.49-3.48.28-1.25.57-2.51 1.16-3.63 1.02-1.95 2.57-2.4 3.9-2.4 2.93 0 4.39 3.4 4.39 4.28 0 1.47-1.2 1.85-1.72 1.85-1.14 0-1.52-.76-1.84-1.43-.29-.6-.6-1.1-.99-1.1-.13 0-.25.07-.36.18-.98 1-1.31 6.2-3.26 8.19-1.08 1.1-2.26 1.1-3 1.1z" fill="url(#prefix__h)"/><path d="M8.59 18.51c-3.33 0-4.47-4.22-4.72-5.04-.64-2.06-.83-3.92-.85-4.11-.12-1.12.78-1.91 1.77-1.91.75 0 1.62.45 1.73 1.54.02.18.17 1.76.71 3.48.56 1.8 1.16 2.49 1.59 2.49.69 0 1.12-1.78 1.49-3.48.28-1.25.57-2.51 1.16-3.63 1.02-1.95 2.57-2.4 3.9-2.4 2.93 0 4.39 3.4 4.39 4.28 0 1.47-1.2 1.85-1.72 1.85-1.14 0-1.52-.76-1.84-1.43-.29-.6-.6-1.1-.99-1.1-.13 0-.25.07-.36.18-.98 1-1.31 6.2-3.26 8.19-1.08 1.1-2.26 1.1-3 1.1z" fill="url(#prefix__i)"/><path d="M15.37 5.44c-1.34 0-2.88.45-3.9 2.4-.59 1.12-.89 2.38-1.16 3.63-.09.41-.18.83-.28 1.22v5.66c.51-.14 1.05-.42 1.57-.95 1.96-1.99 2.28-7.2 3.26-8.19.11-.11.23-.18.36-.18.39 0 .7.5.99 1.1.32.67.71 1.43 1.84 1.43.52 0 1.72-.38 1.72-1.85 0-.88-1.47-4.28-4.39-4.28z" fill="url(#prefix__j)"/><path d="M8.59 18.51c-3.33 0-4.47-4.22-4.72-5.04-.64-2.06-.83-3.92-.85-4.11-.12-1.12.78-1.91 1.77-1.91.75 0 1.62.45 1.73 1.54.02.18.17 1.76.71 3.48.56 1.8 1.16 2.49 1.59 2.49.69 0 1.12-1.78 1.49-3.48.28-1.25.57-2.51 1.16-3.63 1.02-1.95 2.57-2.4 3.9-2.4 2.93 0 4.39 3.4 4.39 4.28 0 1.47-1.2 1.85-1.72 1.85-1.14 0-1.52-.76-1.84-1.43-.29-.6-.6-1.1-.99-1.1-.13 0-.25.07-.36.18-.98 1-1.31 6.2-3.26 8.19-1.08 1.1-2.26 1.1-3 1.1z" fill="url(#prefix__k)"/><path d="M8.59 18.51c-3.33 0-4.47-4.22-4.72-5.04-.64-2.06-.83-3.92-.85-4.11-.12-1.12.78-1.91 1.77-1.91.75 0 1.62.45 1.73 1.54.02.18.17 1.76.71 3.48.56 1.8 1.16 2.49 1.59 2.49.69 0 1.12-1.78 1.49-3.48.28-1.25.57-2.51 1.16-3.63 1.02-1.95 2.57-2.4 3.9-2.4 2.93 0 4.39 3.4 4.39 4.28 0 1.47-1.2 1.85-1.72 1.85-1.14 0-1.52-.76-1.84-1.43-.29-.6-.6-1.1-.99-1.1-.13 0-.25.07-.36.18-.98 1-1.31 6.2-3.26 8.19-1.08 1.1-2.26 1.1-3 1.1z" fill="url(#prefix__l)"/><path d="M8.59 18.51c-3.33 0-4.47-4.22-4.72-5.04-.64-2.06-.83-3.92-.85-4.11-.12-1.12.78-1.91 1.77-1.91.75 0 1.62.45 1.73 1.54.02.18.17 1.76.71 3.48.56 1.8 1.16 2.49 1.59 2.49.69 0 1.12-1.78 1.49-3.48.28-1.25.57-2.51 1.16-3.63 1.02-1.95 2.57-2.4 3.9-2.4 2.93 0 4.39 3.4 4.39 4.28 0 1.47-1.2 1.85-1.72 1.85-1.14 0-1.52-.76-1.84-1.43-.29-.6-.6-1.1-.99-1.1-.13 0-.25.07-.36.18-.98 1-1.31 6.2-3.26 8.19-1.08 1.1-2.26 1.1-3 1.1z" fill="url(#prefix__m)"/><path d="M8.59 18.51c-3.33 0-4.47-4.22-4.72-5.04-.64-2.06-.83-3.92-.85-4.11-.12-1.12.78-1.91 1.77-1.91.75 0 1.62.45 1.73 1.54.02.18.17 1.76.71 3.48.56 1.8 1.16 2.49 1.59 2.49.69 0 1.12-1.78 1.49-3.48.28-1.25.57-2.51 1.16-3.63 1.02-1.95 2.57-2.4 3.9-2.4 2.93 0 4.39 3.4 4.39 4.28 0 1.47-1.2 1.85-1.72 1.85-1.14 0-1.52-.76-1.84-1.43-.29-.6-.6-1.1-.99-1.1-.13 0-.25.07-.36.18-.98 1-1.31 6.2-3.26 8.19-1.08 1.1-2.26 1.1-3 1.1z" fill="url(#prefix__n)"/><path d="M8.59 18.51c-3.33 0-4.47-4.22-4.72-5.04-.64-2.06-.83-3.92-.85-4.11-.12-1.12.78-1.91 1.77-1.91.75 0 1.62.45 1.73 1.54.02.18.17 1.76.71 3.48.56 1.8 1.16 2.49 1.59 2.49.69 0 1.12-1.78 1.49-3.48.28-1.25.57-2.51 1.16-3.63 1.02-1.95 2.57-2.4 3.9-2.4 2.93 0 4.39 3.4 4.39 4.28 0 1.47-1.2 1.85-1.72 1.85-1.14 0-1.52-.76-1.84-1.43-.29-.6-.6-1.1-.99-1.1-.13 0-.25.07-.36.18-.98 1-1.31 6.2-3.26 8.19-1.08 1.1-2.26 1.1-3 1.1z" fill="url(#prefix__o)"/><path d="M19.22 7.44c-.13 0-.26.01-.39.04.62.89.93 1.83.93 2.23 0 1.47-1.2 1.85-1.72 1.85-.41 0-.71-.1-.96-.25v.03c-.07.29-.13.61-.23.92-.19.6-.45 1.28-.73 1.81-.3.57-.59.9-.94.89-.4-.02-.72-.54-1.13-1.99-.13-.47-.25-.98-.36-1.49-.28-1.25-.62-2.52-1.2-3.63-.95-1.82-2.56-2.4-3.85-2.4-1.2 0-2.09.57-2.79 1.28-.19.2-.41.46-.62.78.61.15 1.19.61 1.28 1.48.02.17.15 1.49.58 3.03.12-.38.3-.95.72-1.88.2-.42.39-.7.56-.87.15-.16.29-.22.41-.22.13 0 .59.12 1.03 1.68.55 1.93.96 5.01 2.52 6.65.69.72 1.65 1.19 2.99 1.19 1.17 0 2.15-.52 2.84-1.24 1.14-1.18 1.77-3.14 2-3.86.64-2.06.82-3.92.84-4.11.12-1.12-.83-1.91-1.77-1.91z" fill="#316aff"/><path d="M19.22 7.44c-.13 0-.26.01-.39.04.62.89.93 1.83.93 2.23 0 1.47-1.2 1.85-1.72 1.85-.41 0-.71-.1-.96-.25v.03c-.07.29-.13.61-.23.92-.19.6-.45 1.28-.73 1.81-.3.57-.59.9-.94.89-.4-.02-.72-.54-1.13-1.99-.13-.47-.25-.98-.36-1.49-.28-1.25-.62-2.52-1.2-3.63-.95-1.82-2.56-2.4-3.85-2.4-1.2 0-2.09.57-2.79 1.28-.19.2-.41.46-.62.78.61.15 1.19.61 1.28 1.48.02.17.15 1.49.58 3.03.12-.38.3-.95.72-1.88.2-.42.39-.7.56-.87.15-.16.29-.22.41-.22.13 0 .59.12 1.03 1.68.55 1.93.96 5.01 2.52 6.65.69.72 1.65 1.19 2.99 1.19 1.17 0 2.15-.52 2.84-1.24 1.14-1.18 1.77-3.14 2-3.86.64-2.06.82-3.92.84-4.11.12-1.12-.83-1.91-1.77-1.91z" fill="url(#prefix__p)"/><path d="M19.22 7.44c-.13 0-.26.01-.39.04.62.89.93 1.83.93 2.23 0 1.47-1.2 1.85-1.72 1.85-.41 0-.71-.1-.96-.25v.03c-.07.29-.13.61-.23.92-.19.6-.45 1.28-.73 1.81-.3.57-.59.9-.94.89-.4-.02-.72-.54-1.13-1.99-.13-.47-.25-.98-.36-1.49-.28-1.25-.62-2.52-1.2-3.63-.95-1.82-2.56-2.4-3.85-2.4-1.2 0-2.09.57-2.79 1.28-.19.2-.41.46-.62.78.61.15 1.19.61 1.28 1.48.02.17.15 1.49.58 3.03.12-.38.3-.95.72-1.88.2-.42.39-.7.56-.87.15-.16.29-.22.41-.22.13 0 .59.12 1.03 1.68.55 1.93.96 5.01 2.52 6.65.69.72 1.65 1.19 2.99 1.19 1.17 0 2.15-.52 2.84-1.24 1.14-1.18 1.77-3.14 2-3.86.64-2.06.82-3.92.84-4.11.12-1.12-.83-1.91-1.77-1.91z" fill="url(#prefix__q)"/><path d="M19.22 7.44c-.13 0-.26.01-.39.04.62.89.93 1.83.93 2.23 0 1.47-1.2 1.85-1.72 1.85-.41 0-.71-.1-.96-.25v.03c-.07.29-.13.61-.23.92-.19.6-.45 1.28-.73 1.81-.3.57-.59.9-.94.89-.4-.02-.72-.54-1.13-1.99-.13-.47-.25-.98-.36-1.49-.28-1.25-.62-2.52-1.2-3.63-.95-1.82-2.56-2.4-3.85-2.4-1.2 0-2.09.57-2.79 1.28-.19.2-.41.46-.62.78.61.15 1.19.61 1.28 1.48.02.17.15 1.49.58 3.03.12-.38.3-.95.72-1.88.2-.42.39-.7.56-.87.15-.16.29-.22.41-.22.13 0 .59.12 1.03 1.68.55 1.93.96 5.01 2.52 6.65.69.72 1.65 1.19 2.99 1.19 1.17 0 2.15-.52 2.84-1.24 1.14-1.18 1.77-3.14 2-3.86.64-2.06.82-3.92.84-4.11.12-1.12-.83-1.91-1.77-1.91z" fill="url(#prefix__r)"/><path d="M19.22 7.44c-.13 0-.26.01-.39.04.62.89.93 1.83.93 2.23 0 1.47-1.2 1.85-1.72 1.85-.41 0-.71-.1-.96-.25v.03c-.07.29-.13.61-.23.92-.19.6-.45 1.28-.73 1.81-.3.57-.59.9-.94.89-.4-.02-.72-.54-1.13-1.99-.13-.47-.25-.98-.36-1.49-.28-1.25-.62-2.52-1.2-3.63-.95-1.82-2.56-2.4-3.85-2.4-1.2 0-2.09.57-2.79 1.28-.19.2-.41.46-.62.78.61.15 1.19.61 1.28 1.48.02.17.15 1.49.58 3.03.12-.38.3-.95.72-1.88.2-.42.39-.7.56-.87.15-.16.29-.22.41-.22.13 0 .59.12 1.03 1.68.55 1.93.96 5.01 2.52 6.65.69.72 1.65 1.19 2.99 1.19 1.17 0 2.15-.52 2.84-1.24 1.14-1.18 1.77-3.14 2-3.86.64-2.06.82-3.92.84-4.11.12-1.12-.83-1.91-1.77-1.91z" fill="url(#prefix__s)"/><path d="M19.22 7.44c-.13 0-.26.01-.39.04.62.89.93 1.83.93 2.23 0 1.47-1.2 1.85-1.72 1.85-.41 0-.71-.1-.96-.25v.03c-.07.29-.13.61-.23.92-.19.6-.45 1.28-.73 1.81-.3.57-.59.9-.94.89-.4-.02-.72-.54-1.13-1.99-.13-.47-.25-.98-.36-1.49-.28-1.25-.62-2.52-1.2-3.63-.95-1.82-2.56-2.4-3.85-2.4-1.2 0-2.09.57-2.79 1.28-.19.2-.41.46-.62.78.61.15 1.19.61 1.28 1.48.02.17.15 1.49.58 3.03.12-.38.3-.95.72-1.88.2-.42.39-.7.56-.87.15-.16.29-.22.41-.22.13 0 .59.12 1.03 1.68.55 1.93.96 5.01 2.52 6.65.69.72 1.65 1.19 2.99 1.19 1.17 0 2.15-.52 2.84-1.24 1.14-1.18 1.77-3.14 2-3.86.64-2.06.82-3.92.84-4.11.12-1.12-.83-1.91-1.77-1.91z" fill="url(#prefix__t)"/><path d="M19.22 7.44c-.13 0-.26.01-.39.04.62.89.93 1.83.93 2.23 0 1.47-1.2 1.85-1.72 1.85-.41 0-.71-.1-.96-.25v.03c-.07.29-.13.61-.23.92-.19.6-.45 1.28-.73 1.81-.3.57-.59.9-.94.89-.4-.02-.72-.54-1.13-1.99-.13-.47-.25-.98-.36-1.49-.28-1.25-.62-2.52-1.2-3.63-.95-1.82-2.56-2.4-3.85-2.4-1.2 0-2.09.57-2.79 1.28-.19.2-.41.46-.62.78.61.15 1.19.61 1.28 1.48.02.17.15 1.49.58 3.03.12-.38.3-.95.72-1.88.2-.42.39-.7.56-.87.15-.16.29-.22.41-.22.13 0 .59.12 1.03 1.68.55 1.93.96 5.01 2.52 6.65.69.72 1.65 1.19 2.99 1.19 1.17 0 2.15-.52 2.84-1.24 1.14-1.18 1.77-3.14 2-3.86.64-2.06.82-3.92.84-4.11.12-1.12-.83-1.91-1.77-1.91z" fill="url(#prefix__u)"/><path d="M19.22 7.44c-.13 0-.26.01-.39.04.62.89.93 1.83.93 2.23 0 1.47-1.2 1.85-1.72 1.85-.41 0-.71-.1-.96-.25v.03c-.07.29-.13.61-.23.92-.19.6-.45 1.28-.73 1.81-.3.57-.59.9-.94.89-.4-.02-.72-.54-1.13-1.99-.13-.47-.25-.98-.36-1.49-.28-1.25-.62-2.52-1.2-3.63-.95-1.82-2.56-2.4-3.85-2.4-1.2 0-2.09.57-2.79 1.28-.19.2-.41.46-.62.78.61.15 1.19.61 1.28 1.48.02.17.15 1.49.58 3.03.12-.38.3-.95.72-1.88.2-.42.39-.7.56-.87.15-.16.29-.22.41-.22.13 0 .59.12 1.03 1.68.55 1.93.96 5.01 2.52 6.65.69.72 1.65 1.19 2.99 1.19 1.17 0 2.15-.52 2.84-1.24 1.14-1.18 1.77-3.14 2-3.86.64-2.06.82-3.92.84-4.11.12-1.12-.83-1.91-1.77-1.91z" fill="url(#prefix__v)"/><path d="M19.22 7.44c-.13 0-.26.01-.39.04.62.89.93 1.83.93 2.23 0 1.47-1.2 1.85-1.72 1.85-.41 0-.71-.1-.96-.25v.03c-.07.29-.13.61-.23.92-.19.6-.45 1.28-.73 1.81-.3.57-.59.9-.94.89-.4-.02-.72-.54-1.13-1.99-.13-.47-.25-.98-.36-1.49-.28-1.25-.62-2.52-1.2-3.63-.95-1.82-2.56-2.4-3.85-2.4-1.2 0-2.09.57-2.79 1.28-.19.2-.41.46-.62.78.61.15 1.19.61 1.28 1.48.02.17.15 1.49.58 3.03.12-.38.3-.95.72-1.88.2-.42.39-.7.56-.87.15-.16.29-.22.41-.22.13 0 .59.12 1.03 1.68.55 1.93.96 5.01 2.52 6.65.69.72 1.65 1.19 2.99 1.19 1.17 0 2.15-.52 2.84-1.24 1.14-1.18 1.77-3.14 2-3.86.64-2.06.82-3.92.84-4.11.12-1.12-.83-1.91-1.77-1.91z" fill="url(#prefix__w)"/><path d="M19.22 7.44c-.13 0-.26.01-.39.04.62.89.93 1.83.93 2.23 0 1.47-1.2 1.85-1.72 1.85-.41 0-.71-.1-.96-.25v.03c-.07.29-.13.61-.23.92-.19.6-.45 1.28-.73 1.81-.3.57-.59.9-.94.89-.4-.02-.72-.54-1.13-1.99-.13-.47-.25-.98-.36-1.49-.28-1.25-.62-2.52-1.2-3.63-.95-1.82-2.56-2.4-3.85-2.4-1.2 0-2.09.57-2.79 1.28-.19.2-.41.46-.62.78.61.15 1.19.61 1.28 1.48.02.17.15 1.49.58 3.03.12-.38.3-.95.72-1.88.2-.42.39-.7.56-.87.15-.16.29-.22.41-.22.13 0 .59.12 1.03 1.68.55 1.93.96 5.01 2.52 6.65.69.72 1.65 1.19 2.99 1.19 1.17 0 2.15-.52 2.84-1.24 1.14-1.18 1.77-3.14 2-3.86.64-2.06.82-3.92.84-4.11.12-1.12-.83-1.91-1.77-1.91z" fill="url(#prefix__x)"/><path d="M19.22 7.44c-.13 0-.26.01-.39.04.62.89.93 1.83.93 2.23 0 1.47-1.2 1.85-1.72 1.85-.41 0-.71-.1-.96-.25v.03c-.07.29-.13.61-.23.92-.19.6-.45 1.28-.73 1.81-.3.57-.59.9-.94.89-.4-.02-.72-.54-1.13-1.99-.13-.47-.25-.98-.36-1.49-.28-1.25-.62-2.52-1.2-3.63-.95-1.82-2.56-2.4-3.85-2.4-1.2 0-2.09.57-2.79 1.28-.19.2-.41.46-.62.78.61.15 1.19.61 1.28 1.48.02.17.15 1.49.58 3.03.12-.38.3-.95.72-1.88.2-.42.39-.7.56-.87.15-.16.29-.22.41-.22.13 0 .59.12 1.03 1.68.55 1.93.96 5.01 2.52 6.65.69.72 1.65 1.19 2.99 1.19 1.17 0 2.15-.52 2.84-1.24 1.14-1.18 1.77-3.14 2-3.86.64-2.06.82-3.92.84-4.11.12-1.12-.83-1.91-1.77-1.91z" fill="url(#prefix__y)"/><path d="M8.59 18.51c-3.33 0-4.47-4.22-4.72-5.04-.64-2.06-.83-3.92-.85-4.11-.12-1.12.78-1.91 1.77-1.91.75 0 1.62.45 1.73 1.54.02.18.17 1.76.71 3.48.56 1.8 1.16 2.49 1.59 2.49.69 0 1.12-1.78 1.49-3.48.28-1.25.57-2.51 1.16-3.63 1.02-1.95 2.57-2.4 3.9-2.4 2.93 0 4.39 3.4 4.39 4.28 0 1.47-1.2 1.85-1.72 1.85-1.14 0-1.52-.76-1.84-1.43-.29-.6-.6-1.1-.99-1.1-.13 0-.25.07-.36.18-.98 1-1.31 6.2-3.26 8.19-1.08 1.1-2.26 1.1-3 1.1z" fill="url(#prefix__z)"/><path d="M8.59 18.51c-3.33 0-4.47-4.22-4.72-5.04-.64-2.06-.83-3.92-.85-4.11-.12-1.12.78-1.91 1.77-1.91.75 0 1.62.45 1.73 1.54.02.18.17 1.76.71 3.48.56 1.8 1.16 2.49 1.59 2.49.69 0 1.12-1.78 1.49-3.48.28-1.25.57-2.51 1.16-3.63 1.02-1.95 2.57-2.4 3.9-2.4 2.93 0 4.39 3.4 4.39 4.28 0 1.47-1.2 1.85-1.72 1.85-1.14 0-1.52-.76-1.84-1.43-.29-.6-.6-1.1-.99-1.1-.13 0-.25.07-.36.18-.98 1-1.31 6.2-3.26 8.19-1.08 1.1-2.26 1.1-3 1.1z" fill="url(#prefix__A)"/><path d="M8.59 18.51c-3.33 0-4.47-4.22-4.72-5.04-.64-2.06-.83-3.92-.85-4.11-.12-1.12.78-1.91 1.77-1.91.75 0 1.62.45 1.73 1.54.02.18.17 1.76.71 3.48.56 1.8 1.16 2.49 1.59 2.49.69 0 1.12-1.78 1.49-3.48.28-1.25.57-2.51 1.16-3.63 1.02-1.95 2.57-2.4 3.9-2.4 2.93 0 4.39 3.4 4.39 4.28 0 1.47-1.2 1.85-1.72 1.85-1.14 0-1.52-.76-1.84-1.43-.29-.6-.6-1.1-.99-1.1-.13 0-.25.07-.36.18-.98 1-1.31 6.2-3.26 8.19-1.08 1.1-2.26 1.1-3 1.1z" fill="url(#prefix__B)"/><path d="M8.59 18.51c-3.33 0-4.47-4.22-4.72-5.04-.64-2.06-.83-3.92-.85-4.11-.12-1.12.78-1.91 1.77-1.91.75 0 1.62.45 1.73 1.54.02.18.17 1.76.71 3.48.56 1.8 1.16 2.49 1.59 2.49.69 0 1.12-1.78 1.49-3.48.28-1.25.57-2.51 1.16-3.63 1.02-1.95 2.57-2.4 3.9-2.4 2.93 0 4.39 3.4 4.39 4.28 0 1.47-1.2 1.85-1.72 1.85-1.14 0-1.52-.76-1.84-1.43-.29-.6-.6-1.1-.99-1.1-.13 0-.25.07-.36.18-.98 1-1.31 6.2-3.26 8.19-1.08 1.1-2.26 1.1-3 1.1z" fill="url(#prefix__C)"/><path d="M8.59 18.51c-3.33 0-4.47-4.22-4.72-5.04-.64-2.06-.83-3.92-.85-4.11-.12-1.12.78-1.91 1.77-1.91.75 0 1.62.45 1.73 1.54.02.18.17 1.76.71 3.48.56 1.8 1.16 2.49 1.59 2.49.69 0 1.12-1.78 1.49-3.48.28-1.25.57-2.51 1.16-3.63 1.02-1.95 2.57-2.4 3.9-2.4 2.93 0 4.39 3.4 4.39 4.28 0 1.47-1.2 1.85-1.72 1.85-1.14 0-1.52-.76-1.84-1.43-.29-.6-.6-1.1-.99-1.1-.13 0-.25.07-.36.18-.98 1-1.31 6.2-3.26 8.19-1.08 1.1-2.26 1.1-3 1.1z" fill="url(#prefix__D)"/>',
  "microsoft-teams": '<path d="M15.56 9.91h4.65c.44 0 .8.36.8.8v4.24c0 1.61-1.31 2.92-2.92 2.92h-.01c-1.61 0-2.92-1.31-2.92-2.92v-4.62c0-.23.19-.42.42-.42z" fill="#5059c9"/><circle cx="18.7" cy="7.19" r="1.88" fill="#5059c9"/><circle cx="12.84" cy="6.35" r="2.72" fill="#7b83eb"/><path d="M16.47 9.91H8.8a.79.79 0 00-.77.8v4.83c-.06 2.6 2 4.77 4.6 4.83 2.6-.06 4.67-2.23 4.6-4.83v-4.83a.783.783 0 00-.77-.8z" fill="#7b83eb"/><path d="M3.77 7.4h7.67c.42 0 .77.34.77.77v7.67c0 .42-.34.77-.77.77H3.77a.77.77 0 01-.77-.77V8.17c0-.42.34-.77.77-.77z" fill="#5a62c3"/><path d="M9.62 10.32H8.09v4.18h-.98v-4.18H5.58v-.81h4.04v.81z" fill="#fff"/>'
}, o4 = {
  box: "M15.39,14.04V14.04C15.39,12.62 14.24,11.47 12.82,11.47C11.41,11.47 10.26,12.62 10.26,14.04V14.04C10.26,15.45 11.41,16.6 12.82,16.6C14.24,16.6 15.39,15.45 15.39,14.04M17.1,14.04C17.1,16.4 15.18,18.31 12.82,18.31C11.19,18.31 9.77,17.39 9.05,16.04C8.33,17.39 6.91,18.31 5.28,18.31C2.94,18.31 1.04,16.43 1,14.11V14.11H1V7H1V7C1,6.56 1.39,6.18 1.86,6.18C2.33,6.18 2.7,6.56 2.71,7V7H2.71V10.62C3.43,10.08 4.32,9.76 5.28,9.76C6.91,9.76 8.33,10.68 9.05,12.03C9.77,10.68 11.19,9.76 12.82,9.76C15.18,9.76 17.1,11.68 17.1,14.04V14.04M7.84,14.04V14.04C7.84,12.62 6.69,11.47 5.28,11.47C3.86,11.47 2.71,12.62 2.71,14.04V14.04C2.71,15.45 3.86,16.6 5.28,16.6C6.69,16.6 7.84,15.45 7.84,14.04M22.84,16.96V16.96C22.95,17.12 23,17.3 23,17.47C23,17.73 22.88,18 22.66,18.15C22.5,18.26 22.33,18.32 22.15,18.32C21.9,18.32 21.65,18.21 21.5,18L19.59,15.47L17.7,18V18C17.53,18.21 17.28,18.32 17.03,18.32C16.85,18.32 16.67,18.26 16.5,18.15C16.29,18 16.17,17.72 16.17,17.46C16.17,17.29 16.23,17.11 16.33,16.96V16.96H16.33V16.96L18.5,14.04L16.33,11.11V11.11H16.33V11.11C16.22,10.96 16.17,10.79 16.17,10.61C16.17,10.35 16.29,10.1 16.5,9.93C16.89,9.65 17.41,9.72 17.7,10.09V10.09L19.59,12.61L21.5,10.09C21.76,9.72 22.29,9.65 22.66,9.93C22.89,10.1 23,10.36 23,10.63C23,10.8 22.95,10.97 22.84,11.11V11.11H22.84V11.11L20.66,14.04L22.84,16.96V16.96H22.84Z",
  dropbox: "M3 6.2L8 9.39L13 6.2L8 3L3 6.2M13 6.2L18 9.39L23 6.2L18 3L13 6.2M3 12.55L8 15.74L13 12.55L8 9.35L3 12.55M18 9.35L13 12.55L18 15.74L23 12.55L18 9.35M8.03 16.8L13.04 20L18.04 16.8L13.04 13.61L8.03 16.8Z",
  "google-drive": "M7.71,3.5L1.15,15L4.58,21L11.13,9.5M9.73,15L6.3,21H19.42L22.85,15M22.28,14L15.42,2H8.58L8.57,2L15.43,14H22.28Z",
  onedrive: "M18.21 10.29Q19 10.34 19.7 10.68 20.39 11 20.9 11.57 21.41 12.12 21.71 12.83 22 13.54 22 14.34 22 15.18 21.68 15.92 21.36 16.66 20.8 17.21 20.25 17.76 19.5 18.08 18.78 18.41 17.94 18.41H7Q5.97 18.41 5.06 18 4.15 17.61 3.47 16.94 2.79 16.26 2.4 15.35 2 14.44 2 13.41 2 12.59 2.26 11.83 2.5 11.08 3 10.45 3.44 9.82 4.08 9.35 4.72 8.88 5.5 8.63 5.87 8.5 6.21 8.5 6.56 8.43 6.93 8.41H6.94Q7.37 7.75 7.95 7.23 8.5 6.71 9.2 6.34 9.87 6 10.62 5.78 11.37 5.59 12.16 5.59 13.22 5.59 14.2 5.94 15.18 6.29 16 6.91 16.8 7.53 17.37 8.39 17.95 9.26 18.21 10.29M12.16 6.84Q11.05 6.84 10.06 7.3 9.06 7.75 8.36 8.6 8.73 8.7 9.07 8.85 9.4 9 9.73 9.2L13.71 11.58L16 10.62Q16.21 10.53 16.44 10.45 16.67 10.38 16.92 10.33 16.68 9.55 16.21 8.91 15.74 8.27 15.11 7.81 14.5 7.35 13.73 7.1 13 6.84 12.16 6.84M4 15.66L12.27 12.18L9.08 10.26Q8.59 9.97 8.06 9.81 7.5 9.66 6.95 9.66 6.19 9.66 5.5 9.96 4.84 10.26 4.34 10.77 3.84 11.29 3.54 11.97 3.25 12.65 3.25 13.41 3.25 14 3.45 14.59 3.64 15.19 4 15.66M17.94 17.16Q18.41 17.16 18.84 17 19.27 16.86 19.64 16.58L13.61 13L5.03 16.59Q5.47 16.86 5.97 17 6.47 17.16 7 17.16M20.45 15.61Q20.75 15 20.75 14.34 20.75 13.7 20.5 13.17 20.26 12.65 19.85 12.28 19.43 11.91 18.88 11.71 18.32 11.5 17.7 11.5 17.35 11.5 17 11.6 16.66 11.68 16.33 11.81 16 11.93 15.67 12.08 15.35 12.23 15.04 12.37Z"
}, c4 = {
  email: hx,
  file: yx,
  "file-upload": gx,
  "file-music": mx,
  "file-image": fx,
  "file-video": bx,
  "file-code": Tm,
  "file-xml": Tm,
  "file-document": Lm,
  "file-word": xx,
  "file-excel": wx,
  "file-pdf": Lm,
  "file-powerpoint": vx,
  "file-compressed": kx
}, Fm = {
  "account-minus": L5,
  "account-plus": O5,
  alert: z5,
  "alert-circle": F5,
  "alert-octagon": I5,
  "arrow-collapse": D5,
  "arrow-expand": V5,
  attachment: U5,
  back: Rm,
  "backup-restore": B5,
  bell: N5,
  "bell-off": q5,
  agent: Yx,
  check: Kp,
  "check-all": j5,
  "check-circle-outline": Q5,
  "checkbox-blank": G5,
  "checkbox-marked": Z5,
  "circle-outline": Y5,
  "check-circle": K5,
  close: X5,
  "close-circle": J5,
  cloud: tx,
  comment: ex,
  "comment-outline": ix,
  "content-save": rx,
  delete: nx,
  "delete-restore": ox,
  "delete-forever": ax,
  "dots-vertical": cx,
  download: lx,
  earth: dx,
  emoticon: ux,
  "emoticon-plus": px,
  "fit-screen": $x,
  "fit-width": Cx,
  fullscreen: Px,
  "fullscreen-exit": Sx,
  "help-circle": Mx,
  information: Ex,
  magnify: Ax,
  meeting: Fx,
  "menu-down": Rx,
  "menu-up": Tx,
  message: Lx,
  "message-text": Ox,
  minus: zx,
  next: H5,
  "open-in-new": Ix,
  pencil: Dx,
  pin: Vx,
  unpin: Hx,
  plus: Ux,
  "plus-circle-outline": Bx,
  poll: W5,
  previous: Rm,
  read: Kp,
  restore: Nx,
  unread: Kp,
  send: Wx,
  "server-network-off": jx,
  "shield-star": Kx,
  "shield-star-outline": Qx,
  sort: Gx,
  star: Zx,
  unstar: Xx,
  stars: sx,
  "swap-horizontal": Jx,
  textbox: _x,
  "thumb-up": t4,
  "thumb-up-outline": e4,
  trashcan: i4,
  video: r4,
  "view-list-outline": s4,
  "view-module-outline": n4,
  "wifi-off": a4,
  ...c4,
  ...o4
}, Im = {
  dropbox: "#0061fe",
  onedrive: "#0078d4",
  box: "#0161d5",
  "google-drive": "#1a73e8",
  zoom: "#4a8cff"
};
function Dm(s) {
  return s && s in Fm ? Fm[s] : "";
}
n(Dm, "getIconMapping");
function l4(s) {
  return s && s in zm ? zm[s] : "";
}
n(l4, "getSvgMapping");
const Qf = class Qf extends wl {
  constructor(t) {
    if (super(t), this.it = g, t.type !== Xd.CHILD) throw Error(this.constructor.directiveName + "() can only be used in child bindings");
  }
  render(t) {
    if (t === g || t == null) return this._t = void 0, this.it = t;
    if (t === gu) return t;
    if (typeof t != "string") throw Error(this.constructor.directiveName + "() called with a non-string value");
    if (t === this.it) return this._t;
    this.it = t;
    const e = [t];
    return e.raw = e, this._t = { _$litType$: this.constructor.resultType, strings: e, values: [] };
  }
};
n(Qf, "e");
let Xc = Qf;
Xc.directiveName = "unsafeHTML", Xc.resultType = 1;
const Bs = ln(Xc);
const Gf = class Gf extends Xc {
};
n(Gf, "t");
let Zl = Gf;
Zl.directiveName = "unsafeSVG", Zl.resultType = 2;
const Vm = ln(Zl), Iw = j`[part~=wy-icon-wrapper]{display:inline-flex;align-items:center;justify-content:center;flex:0 0 auto;position:relative}[part~=wy-icon]{display:inline-flex;align-items:center;justify-content:center;flex:0 0 auto;position:relative;width:var(--wy-component-icon-width);height:var(--wy-component-icon-height)}[part~=wy-icon][width=\"24\"]{width:var(--wy-component-icon-width, calc(1.5 * var(--wy-size, 1rem)))}[part~=wy-icon][height=\"24\"]{height:var(--wy-component-icon-height, calc(1.5 * var(--wy-size, 1rem)))}[part~=wy-icon-first]{margin-left:calc(-.5 * var(--wy-size, 1rem))}[part~=wy-icon-last]{margin-right:calc(-.5 * var(--wy-size, 1rem))}[part~=wy-icon-inline]{vertical-align:-.25em}[part~=wy-icon-overlay]{position:absolute;bottom:0;right:0;border-radius:50%}:host(wy-icon-stack),[part~=wy-icon-stack]{display:inline-flex;position:relative;line-height:1}[part~=wy-icon-stack-layer]{position:absolute}[part~=wy-icon-stack-overlay]{position:absolute;bottom:0;right:0;border-radius:50%;width:calc(var(--wy-component-icon-width) / 2);height:calc(var(--wy-component-icon-height) / 2)}[part~=wy-icon-current-color]{fill:currentColor}[part~=wy-icon-state]{transition:opacity var(--wy-transition, var(--wy-transition-duration, .2s) var(--wy-transition-curve, cubic-bezier(.4, 0, .2, 1)));align-self:center;justify-self:center;margin:auto;opacity:0}[part~=wy-icon-state][part~=wy-active]{opacity:1}[part~=wy-icon-display]{color:var(--wy-on-surface-variant, var(--wy-on-surface-variant-light, #42474e));border-radius:var(--wy-component-border-radius, var(--wy-border-radius, calc(.5 * var(--wy-size, 1rem))));display:flex;align-items:center;justify-content:center;flex-direction:column;text-align:center;padding:calc(1 * var(--wy-size, 1rem)) calc(2 * var(--wy-size, 1rem))}[part~=wy-icon-display-fill]{flex:1 0 auto}[part~=wy-icon-display-icon]{color:var(--wy-component-color);--wy-component-icon-width: calc(4 * var(--wy-size, 1rem));--wy-component-icon-height: calc(4 * var(--wy-size, 1rem));padding:calc(1 * var(--wy-size, 1rem)) calc(2 * var(--wy-size, 1rem))}[part~=wy-icon-display-text],[part~=wy-icon-display-meta]{color:var(--wy-component-color);padding:calc(1 * var(--wy-size, 1rem)) calc(2 * var(--wy-size, 1rem));text-align:center;line-height:var(--wy-line-height, 1.5)}[part~=wy-icon-color-primary]{fill:var(--wy-primary, var(--wy-primary-light, #2f628c))}[part~=wy-icon-color-secondary]{fill:var(--wy-secondary, var(--wy-secondary-light, #51606f))}[part~=wy-icon-color-tertiary]{fill:var(--wy-tertiary, var(--wy-tertiary-light, #645f41))}[part~=wy-icon-color-surface]{fill:var(--wy-surface, var(--wy-surface-light, #f7f9ff))}[part~=wy-icon-color-surface-variant]{fill:var(--wy-surface-variant, var(--wy-surface-variant-light, #dee3eb))}[part~=wy-icon-color-error]{fill:var(--wy-error, var(--wy-error-light, #ba1821))}[part~=wy-icon-color-warning]{fill:var(--wy-warning, var(--wy-warning-light, #adb140))}[part~=wy-icon-color-blue]{fill:var(--wy-blue, var(--wy-blue-light, #8aa9fc))}[part~=wy-icon-color-indigo]{fill:var(--wy-indigo, var(--wy-indigo-light, #9285de))}[part~=wy-icon-color-purple]{fill:var(--wy-purple, var(--wy-purple-light, #c675b6))}[part~=wy-icon-color-pink]{fill:var(--wy-pink, var(--wy-pink-light, #d57193))}[part~=wy-icon-color-red]{fill:var(--wy-red, var(--wy-red-light, #db726b))}[part~=wy-icon-color-orange]{fill:var(--wy-orange, var(--wy-orange-light, #e79b3a))}[part~=wy-icon-color-yellow]{fill:var(--wy-yellow, var(--wy-yellow-light, #adb140))}[part~=wy-icon-color-green]{fill:var(--wy-green, var(--wy-green-light, #00a38d))}[part~=wy-icon-color-teal]{fill:var(--wy-teal, var(--wy-teal-light, #00a0a9))}[part~=wy-icon-color-cyan]{fill:var(--wy-cyan, var(--wy-cyan-light, #0081a2))}[part~=wy-icon-color-gray]{fill:var(--wy-gray, var(--wy-gray-light, #76777a))}[part~=wy-icon-color-black]{fill:var(--wy-black, var(--wy-black, #000000))}[part~=wy-icon-color-white]{fill:var(--wy-white, var(--wy-white, #ffffff))}[part*=wy-kind-]{fill:var(--wy-gray, var(--wy-gray-light, #76777a))}[part~=wy-kind-archive]{fill:var(--wy-orange, var(--wy-orange-light, #e79b3a))}[part~=wy-kind-audio]{fill:var(--wy-teal, var(--wy-teal-light, #00a0a9))}[part~=wy-kind-code]{fill:var(--wy-green, var(--wy-green-light, #00a38d))}[part~=wy-kind-document]{fill:var(--wy-blue, var(--wy-blue-light, #8aa9fc))}[part~=wy-kind-email]{fill:var(--wy-red, var(--wy-red-light, #db726b))}[part~=wy-kind-presentation]{fill:var(--wy-orange, var(--wy-orange-light, #e79b3a))}[part~=wy-kind-spreadsheet]{fill:var(--wy-green, var(--wy-green-light, #00a38d))}[part~=wy-kind-image]{fill:var(--wy-pink, var(--wy-pink-light, #d57193))}[part~=wy-kind-text]{fill:var(--wy-cyan, var(--wy-cyan-light, #0081a2))}[part~=wy-kind-video]{fill:var(--wy-purple, var(--wy-purple-light, #c675b6))}[part~=wy-ext-pdf]{fill:var(--wy-red, var(--wy-red-light, #db726b))}[part~=wy-kind-text-icon]{width:calc(1.25 * var(--wy-size, 1rem));height:calc(1.25 * var(--wy-size, 1rem));vertical-align:calc(-.3125 * var(--wy-size, 1rem))}`;
var d4 = Object.defineProperty, h4 = Object.getOwnPropertyDescriptor, Jt = /* @__PURE__ */ n((s, t, e, i) => {
  for (var r = i > 1 ? void 0 : i ? h4(t, e) : t, a = s.length - 1, o; a >= 0; a--)
    (o = s[a]) && (r = (i ? o(t, e, r) : o(r)) || r);
  return i && r && d4(t, e, r), r;
}, "__decorateClass$1f"), Ja;
let Wt = (Ja = class extends nt {
  constructor() {
    super(...arguments), this.exportParts = new W(this), this.color = "", this.size = 24, this.path = Om, this.inline = !1, this.first = !1, this.last = !1, this.layer = !1, this.state = !1, this.active = !1, this.uniqueId = `wy-icon-${Bl()}`;
  }
  willUpdate(t) {
    super.willUpdate(t), t.has("name") && this.name && (this.path = Dm(this.name) || Om, this.overlayPath = Dm(this.overlayName) || this.overlayPath, this.nativeIconColor = this.color === "native" && Im[this.name] || void 0, this.nativeOverlayColor = this.overlayName && Im[this.overlayName] || void 0);
  }
  render() {
    const t = this.kind ? Mv(this.kind) : this.kind, e = this.ext?.startsWith(".") ? this.ext.substring(1) : this.ext, i = {
      "wy-icon-wrapper": !0,
      "wy-icon-stack-layer": this.layer
    }, r = {
      "wy-icon": !0,
      "wy-icon-current-color": !t && !this.color,
      ["wy-kind-" + t]: !!t,
      ["wy-ext-" + e]: !!e,
      ["wy-icon-color-" + this.color]: !!this.color,
      "wy-icon-first": this.first,
      "wy-icon-last": this.last,
      "wy-icon-inline": this.inline,
      "wy-icon-state": this.state,
      "wy-active": this.active
    }, a = this.size !== 24, o = this.size / 16, c = a ? `width: var(--wy-component-icon-width, calc(${o} * var(--wy-size, 1rem))); height: var(--wy-component-icon-height, calc(${o} * var(--wy-size, 1rem)));` : "", l = this.svg && l4(this.svg);
    return this.overlayPath ? [
      h`
          <style>
            .icon-mask-bg {
              width: var(--wy-component-icon-width, calc(${o} * var(--wy-size, 1rem)));
              height: var(--wy-component-icon-height, calc(${o} * var(--wy-size, 1rem)));
              fill: white;
            }

            .icon-mask {
              width: calc(var(--wy-component-icon-width, calc(${o} * var(--wy-size, 1rem))));
              height: calc(var(--wy-component-icon-height, calc(${o} * var(--wy-size, 1rem))));
              ry: calc(var(--wy-border-radius-pill, var(--wy-border-radius, 50%)));
              x: calc(var(--wy-component-icon-width, calc(${o} * var(--wy-size, 1rem))) / 2);
              y: calc(var(--wy-component-icon-height, calc(${o} * var(--wy-size, 1rem))) / 2);
              stroke: black;
              stroke-width: 4px;
              fill: black;
            }
          </style>
        `,
      h`
          <div part=${tt(i)}>
            <wy-icon-stack style="${c}">
              <svg
                part=${tt(r)}
                viewBox="0 0 24 24"
                width="${this.size}"
                height="${this.size}"
                style="mask-image: url(#${this.uniqueId}-mask); -webkit-mask-image: url(#${this.uniqueId}-mask);"
              >
                <defs>
                  <mask id="${this.uniqueId}-mask">
                    <rect class="icon-mask-bg" />
                    <rect class="icon-mask" />
                  </mask>
                </defs>
                ${l ? Vm(l) : Bh`
                    <path d="${this.path}" style="fill: ${ot(this.nativeIconColor)}" />
                  `}
                <!--rect width="24" height="24" fill="transparent" /-->
              </svg>
              <svg part="wy-icon-stack-overlay" viewBox="0 0 24 24" width="${this.size / 2}" height="${this.size / 2}">
                <path d="${this.overlayPath}" style="fill: ${ot(this.nativeOverlayColor)}" />
                <!--rect width="24" height="24" fill="transparent" /-->
              </svg>
            </wy-icon-stack>
            <slot></slot>
          </div>
        `
    ] : h`
      <div part=${tt(i)}>
        <svg
          part=${tt(r)}
          style="${c}"
          viewBox="0 0 24 24"
          width="${this.size}"
          height="${this.size}"
        >
          ${l ? Vm(l) : Bh`
            <path d="${this.path}" style="fill: ${ot(this.nativeIconColor)}" />
          `}
          <!--rect width="24" height="24" fill="transparent" /-->
        </svg>
        <slot></slot>
      </div>
    `;
  }
}, n(Ja, "WyIcon"), Ja);
Wt.styles = [Lt, Iw, Y];
Jt([
  y()
], Wt.prototype, "name", 2);
Jt([
  y()
], Wt.prototype, "overlayName", 2);
Jt([
  y()
], Wt.prototype, "svg", 2);
Jt([
  y()
], Wt.prototype, "color", 2);
Jt([
  y({ type: Number })
], Wt.prototype, "size", 2);
Jt([
  y()
], Wt.prototype, "kind", 2);
Jt([
  y()
], Wt.prototype, "ext", 2);
Jt([
  y({ attribute: !1 })
], Wt.prototype, "path", 2);
Jt([
  y({ attribute: !1 })
], Wt.prototype, "overlayPath", 2);
Jt([
  y({ type: Boolean })
], Wt.prototype, "inline", 2);
Jt([
  y({ type: Boolean })
], Wt.prototype, "first", 2);
Jt([
  y({ type: Boolean })
], Wt.prototype, "last", 2);
Jt([
  y({ type: Boolean })
], Wt.prototype, "layer", 2);
Jt([
  y({ type: Boolean })
], Wt.prototype, "state", 2);
Jt([
  y({ type: Boolean })
], Wt.prototype, "active", 2);
Jt([
  V()
], Wt.prototype, "nativeIconColor", 2);
Jt([
  V()
], Wt.prototype, "nativeOverlayColor", 2);
Wt = Jt([
  U("wy-icon")
], Wt);
var to;
let ru = (to = class extends nt {
  constructor() {
    super(...arguments), this.exportParts = new W(this);
  }
  render() {
    return h`<slot></slot>`;
  }
}, n(to, "WyIconStack"), to);
ru.styles = [Lt, Iw];
ru = Jt([
  U("wy-icon-stack")
], ru);
var eo;
let Yl = (eo = class extends nt {
  constructor() {
    super(...arguments), this.exportParts = new W(this), this.fill = !1;
  }
  render() {
    return h`
      <div part="wy-icon-display ${tt({ "wy-icon-display-fill": this.fill })}">
        <div part="wy-icon-display-icon">
          <slot></slot>
        </div>
        <div part="wy-icon-display-text">
          <slot name="text"></slot>
        </div>
        <div part="wy-icon-display-meta">
          <slot name="meta"></slot>
        </div>
      </div>
    `;
  }
}, n(eo, "WyIconDisplay"), eo);
Yl.styles = [Lt, Iw, Y];
Jt([
  y({ type: Boolean })
], Yl.prototype, "fill", 2);
Yl = Jt([
  U("wy-icon-display")
], Yl);
var u4 = Object.defineProperty, p4 = Object.getOwnPropertyDescriptor, Je = /* @__PURE__ */ n((s, t, e, i) => {
  for (var r = i > 1 ? void 0 : i ? p4(t, e) : t, a = s.length - 1, o; a >= 0; a--)
    (o = s[a]) && (r = (i ? o(t, e, r) : o(r)) || r);
  return i && r && u4(t, e, r), r;
}, "__decorateClass$1e"), io;
let Oi = (io = class extends nt {
  constructor() {
    super(...arguments), this.exportParts = new W(this), this.size = 32, this.src = "", this.name = "", this.description = "", this.isAgent = !1;
  }
  render() {
    const t = this.size / 16;
    let e;
    this.name && (e = x0(this.name));
    const i = {
      "wy-avatar-shape": !0,
      "wy-presence-mask": this.presence === Ow.Active
    };
    return h`
      ${this.src ? h`
            <img
              alt=""
              title="${this.name}${this.description ? ` • ${this.description}` : ""}"
              part="wy-avatar-img ${tt(i)}"
              style="--wy-component-avatar-size: calc(${t} * var(--wy-size, 1rem));"
              height="${this.size}"
              width="${this.size}"
              src="${this.src}"
              decoding="async"
              loading="lazy"
              ${K(Eu)}
              @load=${(r) => {
      Au(r);
    }}
            />
          ` : h`
          `}
          <div
            part="wy-avatar-initials ${tt(i)}"
            style="--wy-component-avatar-size: calc(${t} * var(--wy-size, 1rem));"
            title="${this.name}${this.description ? ` • ${this.description}` : ""}"
          >
            <span part="wy-avatar-initials-text">${e}</span>
          </div>
      ${this.isAgent ? h`<wy-icon part="wy-avatar-type-icon" name="agent" size="${this.size / 3 * 1.25}"></wy-icon>` : g}
      ${this.presence && !this.isAgent ? h`<wy-presence part="wy-avatar-presence" .status=${this.presence} id=${this.id}></wy-presence>` : g}
    `;
  }
}, n(io, "WyAvatar"), io);
Oi.styles = [Lt, zw, L1];
Je([
  y({ type: Number })
], Oi.prototype, "size", 2);
Je([
  y()
], Oi.prototype, "src", 2);
Je([
  y()
], Oi.prototype, "name", 2);
Je([
  y()
], Oi.prototype, "description", 2);
Je([
  y({ type: Boolean, reflect: !0 })
], Oi.prototype, "isAgent", 2);
Je([
  y()
], Oi.prototype, "presence", 2);
Oi = Je([
  U("wy-avatar")
], Oi);
var ro;
let Ns = (ro = class extends nt {
  constructor() {
    super(...arguments), this.exportParts = new W(this), this.size = 32, this.uniqueId = `wy-avatar-${Bl()}`;
  }
  render() {
    if (!this.user)
      return g;
    const t = this.size / 16, e = (this.members || []).filter((a) => a.id !== this.user?.id).slice(0, 2).reverse(), i = e?.shift() || this.user, r = e?.shift() || (i !== this.user ? this.user : void 0);
    return [
      h`
        <style>
          :host {
            --wy-component-avatar-size: calc(${t} * var(--wy-size, 1rem));
          }

          [part~="wy-avatar-mask-bg"] {
            width: calc(${t * 2 / 3} * var(--wy-size, 1rem));
            height: calc(${t * 2 / 3} * var(--wy-size, 1rem));
            fill: white;
          }

          [part~="wy-avatar-mask"] {
            width: calc(${t * 2 / 3} * var(--wy-size, 1rem));
            height: calc(${t * 2 / 3} * var(--wy-size, 1rem));
            ry: var(--wy-avatar-border-radius, var(--wy-border-radius-pill, var(--wy-border-radius, 50%)));
            x: calc(${t / 3} * var(--wy-size, 1rem));
            y: calc(${t / 3} * var(--wy-size, 1rem));
            stroke: black;
            stroke-width: 4px;
            fill: black;
          }
        </style>
      `,
      h`
        <svg>
          <defs>
            <mask id="${this.uniqueId}-mask">
              <rect part="wy-avatar-mask-bg" />
              <rect part="wy-avatar-mask" />
            </mask>
          </defs>
        </svg>
      `,
      h`
        <wy-avatar
          part="wy-avatar-back"
          style="mask-image: url(#${this.uniqueId}-mask); -webkit-mask-image: url(#${this.uniqueId}-mask);"
          .src=${r?.avatar_url}
          .name=${r?.name}
          size=${this.size * 2 / 3}
        ></wy-avatar>
        <wy-avatar
          part="wy-avatar-front"
          .src=${i.avatar_url}
          .name=${i.name}
          .isAgent=${i.is_agent}
          size=${this.size * 2 / 3}
        ></wy-avatar>
      `
    ];
  }
}, n(ro, "WyAvatarGroup"), ro);
Ns.styles = [Lt, zw, Y];
Je([
  y({ type: Number })
], Ns.prototype, "size", 2);
Je([
  y({
    type: Array,
    attribute: !1
  })
], Ns.prototype, "members", 2);
Je([
  he({ context: Cu, subscribe: !0 }),
  V()
], Ns.prototype, "user", 2);
Ns = Je([
  U("wy-avatar-group")
], Ns);
var so;
let Xl = (so = class extends nt {
  constructor() {
    super(...arguments), this.exportParts = new W(this);
  }
  render() {
    return h`
      <slot></slot>
      ${this.description ? h` <div part="wy-avatar-description">${this.description}</div> ` : g}
    `;
  }
}, n(so, "WyAvatarHeader"), so);
Xl.styles = [Lt, zw];
Je([
  y()
], Xl.prototype, "description", 2);
Xl = Je([
  U("wy-avatar-header")
], Xl);
const y4 = j`[part~=wy-empty]{display:flex;align-items:center;justify-content:center;flex:1;min-width:0;position:relative;flex-direction:column;gap:var(--wy-gap-lg, calc(1.5 * var(--wy-gap, var(--wy-padding, calc(.5 * var(--wy-size, 1rem))))))}`, O1 = j`[part~=wy-progress]{display:inline-flex;align-items:center;justify-content:center;flex:0 0 auto;position:relative}[part~=wy-progress-padded]{margin:var(--wy-padding, calc(.5 * var(--wy-size, 1rem)))}[part~=wy-progress-overlay]{position:absolute!important}[part~=wy-progress-reveal]{overflow:hidden;opacity:1;interpolate-size:allow-keywords;height:auto;transition:width var(--wy-transition, var(--wy-transition-duration, .2s) var(--wy-transition-curve, cubic-bezier(.4, 0, .2, 1))) var(--wy-transition-reveal-delay, 1s),height var(--wy-transition, var(--wy-transition-duration, .2s) var(--wy-transition-curve, cubic-bezier(.4, 0, .2, 1))) var(--wy-transition-reveal-delay, 1s),margin var(--wy-transition, var(--wy-transition-duration, .2s) var(--wy-transition-curve, cubic-bezier(.4, 0, .2, 1))) var(--wy-transition-reveal-delay, 1s),opacity var(--wy-transition, var(--wy-transition-duration, .2s) var(--wy-transition-curve, cubic-bezier(.4, 0, .2, 1))) calc(var(--wy-transition-duration, .2s) + var(--wy-transition-reveal-delay, 1s))}@starting-style{[part~=wy-progress-reveal]{width:0;height:0;opacity:0;margin:0}}`, w4 = j`[part~=wy-progress-circular]{display:inline-flex;align-items:center;justify-content:center;flex:0 0 auto;position:relative;width:var(--wy-component-icon-width);height:var(--wy-component-icon-height)}[part~=wy-progress-circular]:not([width]),[part~=wy-progress-circular][width=\"24\"]{width:var(--wy-component-icon-width, calc(1.5 * var(--wy-size, 1rem)))}[part~=wy-progress-circular]:not([height]),[part~=wy-progress-circular][height=\"24\"]{height:var(--wy-component-icon-height, calc(1.5 * var(--wy-size, 1rem)))}[part~=wy-progress-circular][part~=wy-primary]{color:var(--wy-primary, var(--wy-primary-light, #2f628c))}[part~=wy-progress-remaining]{fill:none;stroke:var(--wy-surface-variant, var(--wy-surface-variant-light, #dee3eb))}[part~=wy-progress-done]{fill:none;stroke:currentColor}[part~=wy-progress-overlay]{position:absolute;inset:0;margin:auto}[part~=wy-progress-indeterminate-circle]{stroke-dasharray:1,100;stroke-dashoffset:0;transform-origin:center;fill:none!important}[part~=wy-progress-indeterminate]{animation:wy-rotate 1.4s linear infinite}[part~=wy-progress-indeterminate] [part~=wy-progress-indeterminate-circle]{animation:wy-dash 1.4s ease-in-out infinite,wy-color 2.8s ease-in-out infinite}@keyframes wy-rotate{to{transform:rotate(360deg)}}@keyframes wy-dash{0%{stroke-dasharray:1,100;stroke-dashoffset:0}50%{stroke-dasharray:44.5,100;stroke-dashoffset:-17.5}to{stroke-dasharray:44.5,100;stroke-dashoffset:-62}}@keyframes wy-color{0%,to{stroke:var(--wy-primary, var(--wy-primary-light, #2f628c))}25%{stroke:var(--wy-primary, var(--wy-primary-light, #2f628c))}50%{stroke:transparent}75%{stroke:var(--wy-primary, var(--wy-primary-light, #2f628c))}}`;
var f4 = Object.defineProperty, m4 = Object.getOwnPropertyDescriptor, ds = /* @__PURE__ */ n((s, t, e, i) => {
  for (var r = i > 1 ? void 0 : i ? m4(t, e) : t, a = s.length - 1, o; a >= 0; a--)
    (o = s[a]) && (r = (i ? o(t, e, r) : o(r)) || r);
  return i && r && f4(t, e, r), r;
}, "__decorateClass$1d"), no;
let yi = (no = class extends nt {
  constructor() {
    super(...arguments), this.shadowParts = new W(this), this.size = 24, this.indeterminate = !1, this.padded = !1, this.overlay = !1, this.value = 0, this.max = 1, this.reveal = !1;
  }
  render() {
    const t = {
      "wy-progress": !0,
      "wy-progress-reveal": this.reveal,
      "wy-progress-overlay": this.overlay,
      "wy-progress-padded": this.padded
    }, e = {
      "wy-progress-circular": !0,
      "wy-primary": !this.indeterminate,
      "wy-progress-indeterminate": this.indeterminate
    }, i = 10, r = Math.PI * (i * 2);
    let a;
    try {
      Number.isFinite(this.value) && Number.isFinite(this.max) && this.max > 0 && (a = this.value / this.max);
    } catch (o) {
      console.error(o);
    }
    return h`
      <div part="${tt(t)}">
        ${this.indeterminate || a === void 0 ? Bh`
              <svg
                part="${tt(e)}"
                viewBox="0 0 24 24"
                width=${this.size}
                height=${this.size}
                role="progressbar"
                aria-label="${g}"
                aria-valuemin="0"
                aria-valuemax=${this.max}
                aria-valuenow=${g}
                xmlns="http://www.w3.org/2000/svg"
              >
                <circle
                  part="wy-progress-circle wy-progress-indeterminate-circle"
                  fill="none"
                  cx="12"
                  cy="12"
                  r=${i}
                  stroke-linecap="butt"
                  stroke-width="2"
                ></circle>
              </svg>
            ` : Bh`
              <svg
                part="${tt(e)}"
                viewBox="0 0 24 24"
                width=${this.size}
                height=${this.size}
                transform="rotate(-90)"
                role="progressbar"
                aria-label="${g}"
                aria-valuemin="0"
                aria-valuemax=${this.max}
                aria-valuenow=${this.value}
                xmlns="http://www.w3.org/2000/svg"
              >
                <circle
                  part="wy-progress-circle wy-progress-remaining"
                  cx="12"
                  cy="12"
                  r=${i}
                  stroke-linecap="butt"
                  stroke-width="2"
                  fill="none"
                  stroke="#eee"
                ></circle>
                <circle
                  part="wy-progress-circle wy-progress-done"
                  cx="12"
                  cy="12"
                  r=${i}
                  stroke-dasharray=${r}
                  stroke-dashoffset=${r * (1 - a)}
                  stroke-linecap="butt"
                  stroke-width="2"
                  fill="none"
                  stroke="currentColor"
                  path-length="200"
                ></circle>
              </svg>
            `}
      </div>
    `;
  }
}, n(no, "WyProgressCircular"), no);
yi.styles = [Lt, O1, w4, Y];
ds([
  y({ type: Number })
], yi.prototype, "size", 2);
ds([
  y({ type: Boolean })
], yi.prototype, "indeterminate", 2);
ds([
  y({ type: Boolean })
], yi.prototype, "padded", 2);
ds([
  y({ type: Boolean })
], yi.prototype, "overlay", 2);
ds([
  y({ type: Number })
], yi.prototype, "value", 2);
ds([
  y({ type: Number })
], yi.prototype, "max", 2);
ds([
  y({ type: Boolean })
], yi.prototype, "reveal", 2);
yi = ds([
  U("wy-progress-circular")
], yi);
var v4 = Object.defineProperty, g4 = Object.getOwnPropertyDescriptor, Dw = /* @__PURE__ */ n((s, t, e, i) => {
  for (var r = i > 1 ? void 0 : i ? g4(t, e) : t, a = s.length - 1, o; a >= 0; a--)
    (o = s[a]) && (r = (i ? o(t, e, r) : o(r)) || r);
  return i && r && v4(t, e, r), r;
}, "__decorateClass$1c"), ao;
let Jc = (ao = class extends nt {
  constructor() {
    super(...arguments), this.exportParts = new W(this), this.noNetwork = !1, this.handleUpdate = () => this.requestUpdate();
  }
  willUpdate(t) {
    if (super.willUpdate(t), t.has("weavy")) {
      const e = t.get("weavy");
      e && e !== this.weavy && e.removeNetworkListener(this.handleUpdate), this.weavy && e !== this.weavy && this.weavy.addNetworkListener(this.handleUpdate);
    }
  }
  render() {
    return h`
      <div part="wy-empty">
        ${this.weavy && !this.noNetwork && this.weavy?.network.state === "unreachable" ? h`
              <wy-icon-display>
                <wy-icon name="server-network-off"></wy-icon>
                <span slot="text">${x("The server is offline, try again in a few minutes...")}</span>
                <wy-progress-circular indeterminate slot="meta" ?hidden=${!this.weavy?.network.isPending}></wy-progress-circular>
              </wy-icon-display>
            ` : !this.noNetwork && this.weavy?.network.state === "offline" ? h`
              <wy-icon-display>
                <wy-icon name="wifi-off"></wy-icon>
                <span slot="text">${x("You are currently offline.")}</span>
                <wy-progress-circular indeterminate slot="meta" ?hidden=${!this.weavy?.network.isPending}></wy-progress-circular>
              </wy-icon-display>
            ` : h`
              <slot>
                <wy-icon-display>
                  <wy-icon name="information"></wy-icon>
                  <slot slot="text" name="title">
                    <span>${x("Nothing to see here yet.")}</span>
                  </slot>
                </wy-icon-display>
              </slot>
            `}
      </div>
    `;
  }
  connectedCallback() {
    super.connectedCallback(), this.weavy?.addNetworkListener(this.handleUpdate);
  }
  disconnectedCallback() {
    super.disconnectedCallback(), this.weavy?.removeNetworkListener(this.handleUpdate);
  }
}, n(ao, "WyEmpty"), ao);
Jc.styles = [
  y4,
  Y
];
Dw([
  he({ context: Xt, subscribe: !0 }),
  V()
], Jc.prototype, "weavy", 2);
Dw([
  y({ type: Boolean })
], Jc.prototype, "noNetwork", 2);
Jc = Dw([
  U("wy-empty"),
  ut()
], Jc);
const b4 = j`@keyframes wy-bouncing{to{transform:translate3d(0,calc(-.25 * var(--wy-size, 1rem)),0)}}@keyframes wy-bouncing-fade{to{opacity:.2}}[part~=wy-bouncer]{width:calc(1.5 * var(--wy-size, 1rem));height:calc(.75 * var(--wy-size, 1rem))}[part~=wy-bouncer-dot]{position:relative;animation:wy-bouncing .6s infinite alternate,wy-bouncing-fade .6s infinite alternate;fill:currentColor}[part~=wy-bouncer-dot-start]{animation-delay:0s,.6s}[part~=wy-bouncer-dot-middle]{animation-delay:.3s,.3s}[part~=wy-bouncer-dot-end]{animation-delay:.6s,0s}`;
var x4 = Object.defineProperty, C4 = Object.getOwnPropertyDescriptor, _r = /* @__PURE__ */ n((s, t, e, i) => {
  for (var r = i > 1 ? void 0 : i ? C4(t, e) : t, a = s.length - 1, o; a >= 0; a--)
    (o = s[a]) && (r = (i ? o(t, e, r) : o(r)) || r);
  return i && r && x4(t, e, r), r;
}, "__decorateClass$1b"), oo;
let si = (oo = class extends nt {
  constructor() {
    super(...arguments), this.exportParts = new W(this), this.typing = new eu(this), this.isPrivateChat = !1, this.members = [], this.typingMembers = [], this.names = [];
  }
  willUpdate(t) {
    super.willUpdate(t), t.has("conversationId") && (this.typing.appId = this.conversationId), t.has("userId") && (this.typing.userId = this.userId);
    const { typingMembers: e, names: i } = this.typing;
    this.typingMembers = e, this.names = i, e.length ? this.typingTime ||= /* @__PURE__ */ new Date() : this.typingTime = void 0;
  }
  render() {
    const t = this.typingMembers.map(
      (o) => this.members.find((c) => c.id === o.id)
    ).filter(cn), e = new Intl.ListFormat(this.weavy?.locale, { style: "long", type: "conjunction" }).format(
      this.names
    ), i = this.typingTime ? new Intl.DateTimeFormat(this.weavy?.locale, { dateStyle: "full", timeStyle: "short" }).format(this.typingTime) : "", r = this.typingTime ? new Intl.DateTimeFormat(this.weavy?.locale, { timeStyle: "short" }).format(this.typingTime) : "", a = h`
      <svg part="wy-bouncer" viewBox="0 0 32 16" xmlns="http://www.w3.org/2000/svg">
        <circle part="wy-bouncer-dot wy-bouncer-dot-start" cx="4" cy="10" r="4" />
        <circle part="wy-bouncer-dot wy-bouncer-dot-middle" cx="16" cy="10" r="4" />
        <circle part="wy-bouncer-dot wy-bouncer-dot-end" cx="28" cy="10" r="4" />
      </svg>
    `;
    return t.length ? h`
          <div part=${tt({ "wy-message": !0, "wy-message-agent": !!t[0]?.is_agent })}>
            <div part="wy-message-author">
              ${t.length > 1 ? h`
                    <wy-avatar-group
                      .size=${32}
                      .members=${t}
                      title=${e}
                    ></wy-avatar-group>
                  ` : h`
                    <wy-avatar
                      .size=${32}
                      .src=${t[0]?.avatar_url}
                      .description=${t[0]?.comment}
                      .name=${e}
                      .isAgent=${t[0]?.is_agent}
                    ></wy-avatar>
                  `}
            </div>

            <div part="wy-message-content">
              <div part="wy-message-meta">
                ${this.isPrivateChat ? h`
                      <time datetime=${ot(this.typingTime?.toISOString())} title=${i}>${r}</time>
                    ` : e}
              </div>
              <div part="wy-message-bubble"> ${a} </div>
            </div>
          </div>
        ` : g;
  }
}, n(oo, "WyMessageTyping"), oo);
si.styles = [Lt, b4, Mu];
_r([
  he({ context: Xt, subscribe: !0 }),
  V()
], si.prototype, "weavy", 2);
_r([
  y({ attribute: !0, type: Number })
], si.prototype, "conversationId", 2);
_r([
  y({ attribute: !0, type: Number })
], si.prototype, "userId", 2);
_r([
  y({ type: Boolean })
], si.prototype, "isPrivateChat", 2);
_r([
  y({ attribute: !1 })
], si.prototype, "members", 2);
_r([
  V()
], si.prototype, "typingMembers", 2);
_r([
  V()
], si.prototype, "names", 2);
_r([
  V()
], si.prototype, "typingTime", 2);
si = _r([
  U("wy-message-typing")
], si);
const Ru = j`:host(wy-item),[part~=wy-item]{--wy-component-color: var(--wy-on-background, var(--wy-on-background-light, #181c20));display:flex;box-sizing:border-box}[part~=wy-item-rounded]{border-radius:var(--wy-border-radius, calc(.5 * var(--wy-size, 1rem)))}[part~=wy-item-rounded-outer]{border-radius:var(--wy-border-radius-outer, var(--wy-border-radius, calc(.5 * var(--wy-size, 1rem))))}[part~=wy-item-inner]{display:flex;position:relative;align-items:center;text-decoration:none;gap:var(--wy-gap, var(--wy-padding, calc(.5 * var(--wy-size, 1rem))));padding:var(--wy-padding, calc(.5 * var(--wy-size, 1rem)));flex:1 1 100%;min-width:0}[part~=wy-item-inner-no-padding]{padding:0}[part~=wy-item-sm]{min-height:calc(2 * var(--wy-size, 1rem));padding:0 var(--wy-padding, calc(.5 * var(--wy-size, 1rem)))}[part~=wy-item-md]{min-height:calc(3 * var(--wy-size, 1rem))}[part~=wy-item-lg]{min-height:calc(4.5 * var(--wy-size, 1rem))}[part~=wy-item-top]{align-items:flex-start}[part~=wy-item-body]{width:100%;min-width:0;min-height:100%;display:flex}[part~=wy-item-rows]{display:flex;flex-direction:column;min-width:0;justify-content:space-evenly;flex:1 1 100%}[part~=wy-item-row]{display:flex;align-items:center;column-gap:var(--wy-gap, var(--wy-padding, calc(.5 * var(--wy-size, 1rem))));min-height:calc(var(--wy-button-line-height, var(--wy-line-height, 1.5)) * var(--wy-size, 1rem))}[part~=wy-item-title] a:after{position:absolute;inset:0;z-index:1;content:\"\"}[part~=wy-item-title] a{color:inherit;text-decoration:none}[part~=wy-item-title]{color:var(--wy-component-color);font-size:var(--wy-item-title-font-size, var(--wy-font-size, 1em));font-weight:var(--wy-item-title-font-weight, var(--wy-font-weight, unset))}[part~=wy-item-title]{overflow:hidden;text-overflow:ellipsis;white-space:nowrap}[part~=wy-item-image]{display:contents}[part~=wy-item-text]{color:var(--wy-outline, var(--wy-outline-light, #72777f));font-size:var(--wy-font-size-sm, calc(.875 * var(--wy-font-size, 1em)))}[part~=wy-item-text]{overflow:hidden;text-overflow:ellipsis;white-space:nowrap}[part~=wy-item-text-sm]{font-size:var(--wy-font-size-xs, calc(.75 * var(--wy-font-size, 1em)))}[part~=wy-item-meta]{white-space:nowrap}[part~=wy-item-actions]{--wy-component-background-color: transparent;display:flex;align-items:center;justify-items:flex-end;flex:0 0 auto;gap:var(--wy-gap-sm, calc(.5 * var(--wy-gap, var(--wy-padding, calc(.5 * var(--wy-size, 1rem))))))}[part~=wy-item-actions-top]{align-self:flex-start;margin:calc(-.25 * var(--wy-size, 1rem))}[part~=wy-item-actions-bottom]{align-self:flex-start;margin:calc(-.125 * var(--wy-size, 1rem)) 0 calc(-.625 * var(--wy-size, 1rem))}[part~=wy-item-left]{margin-right:auto}[part~=wy-item-right]{margin-left:auto}[part~=wy-read]{opacity:var(--wy-opacity-muted, 65%);--wy-component-color: var(--wy-outline, var(--wy-outline-light, #72777f))}[part~=wy-unread]{--wy-component-color: color.token(--wy-on-background)}[part~=wy-unread] [part~=wy-item-title],[part~=wy-unread] [part~=wy-item-text]{font-weight:var(--wy-font-weight-bold, 600);color:var(--wy-component-color)}[part~=wy-item-interactive]{background-color:var(--wy-component-background-color);color:var(--wy-component-color);-webkit-user-select:none;user-select:none;cursor:pointer}[part~=wy-item-interactive]:hover,[part~=wy-item-interactive]:focus{--wy-component-background-color: var(--wy-component-background-color-hover, var(--wy-surface-layer-high, var(--wy-surface-layer-high-light, rgba(0, 0, 0, .0745098039))));text-decoration:none}[part~=wy-item-interactive]:active{--wy-component-background-color: var(--wy-component-background-color-active, var(--wy-surface-layer-highest, var(--wy-surface-layer-highest-light, rgba(0, 0, 0, .1019607843))))}[part~=wy-item-interactive][part~=wy-disabled],[part~=wy-item-interactive]:disabled{opacity:var(--wy-opacity-disabled, 38%);pointer-events:none}[part~=wy-item-interactive][part~=wy-selected]{--wy-component-background-color: var(--wy-component-background-color-selected, var(--wy-primary-container, var(--wy-primary-container-light, #cee5ff)));--wy-component-color: var(--wy-component-color-selected, var(--wy-on-primary-container, var(--wy-on-primary-container-light, #0d4a73)));background-color:var(--wy-component-background-color);color:var(--wy-component-color);border-color:var(--wy-component-background-color)}[part~=wy-item-trashed]{text-decoration:line-through;opacity:var(--wy-opacity-disabled, 38%)}[part~=wy-item-trashed][part~=wy-item-interactive]:hover{opacity:1}[part~=wy-item-list]{display:flex;flex-direction:column;gap:var(--wy-border, 1px);min-width:0}[part~=wy-item-list-filled]{--wy-component-color: var(--wy-on-surface, var(--wy-on-surface-light, #181c20));--wy-component-background-color: var(--wy-surface-layer-low, var(--wy-surface-layer-low-light, rgba(255, 255, 255, .5019607843)));--wy-component-background-color-hover: var(--wy-surface-layer-lowest, var(--wy-surface-layer-lowest-light, rgba(255, 255, 255, .7490196078)));--wy-component-background-color-active: var(--wy-surface-layer-lowest, var(--wy-surface-layer-lowest-light, rgba(255, 255, 255, .7490196078)))}[part~=wy-item-list-rounded]{border-radius:var(--wy-border-radius-sm, calc(var(--wy-border-radius, calc(.5 * var(--wy-size, 1rem))) - .25 * var(--wy-padding, calc(.5 * var(--wy-size, 1rem)))));clip-path:content-box}[part~=wy-item-list-rounded-outer]{border-radius:var(--wy-border-radius-outer, var(--wy-border-radius, calc(.5 * var(--wy-size, 1rem))));clip-path:content-box}[part~=wy-item-list-bordered]{border:var(--wy-border, 1px) solid var(--wy-outline-variant, var(--wy-outline-variant-light, #c2c7cf));gap:0}[part~=wy-item-list-bordered]>:not(slot),[part~=wy-item-list-bordered] ::slotted(*){border-left:none;border-right:none}[part~=wy-item-list-bordered]>:not(slot):not(:first-child),[part~=wy-item-list-bordered] ::slotted(*:not(:first-child)){border-top:var(--wy-border, 1px) solid var(--wy-outline-variant, var(--wy-outline-variant-light, #c2c7cf))}`, fl = j`[part~=wy-title]{font-family:var(--wy-headings-font-family, var(--wy-font-family, unset));font-size:var(--wy-headings-font-size, var(--wy-font-size, 1em));font-style:var(--wy-headings-font-style, unset);font-weight:var(--wy-headings-font-weight, var(--wy-font-weight-bold, 600));line-height:var(--wy-headings-line-height, var(--wy-line-height, 1.5));margin-block:var(--wy-padding, calc(.5 * var(--wy-size, 1rem)))}[part~=wy-meta]{color:var(--wy-outline, var(--wy-outline-light, #72777f));font-size:var(--wy-font-size-sm, calc(.875 * var(--wy-font-size, 1em)))}[part~=wy-meta-sm]{color:var(--wy-outline, var(--wy-outline-light, #72777f));font-size:var(--wy-font-size-xs, calc(.75 * var(--wy-font-size, 1em)))}[part~=wy-quote]{color:var(--wy-outline, var(--wy-outline-light, #72777f))}[part~=wy-quote]:before,[part~=wy-quote]:after{content:'\"'}`;
var $4 = Object.defineProperty, k4 = Object.getOwnPropertyDescriptor, te = /* @__PURE__ */ n((s, t, e, i) => {
  for (var r = i > 1 ? void 0 : i ? k4(t, e) : t, a = s.length - 1, o; a >= 0; a--)
    (o = s[a]) && (r = (i ? o(t, e, r) : o(r)) || r);
  return i && r && $4(t, e, r), r;
}, "__decorateClass$1a"), co;
let le = (co = class extends nt {
  constructor() {
    super(...arguments), this.exportParts = new W(this), this.size = "auto", this.align = "middle", this.actionsPosition = "end", this.rounded = !1, this.noPadding = !1, this.outer = !1, this.interactive = !1, this.selected = !1, this.disabled = !1, this.url = void 0, this.trashed = !1;
  }
  /**
   * If it has an url; handles ctrl click event for the item.
   * @param event MouseEvent
   */
  handleLinkClick(t) {
    if (this.disabled) {
      t.preventDefault(), t.stopPropagation();
      return;
    }
    vu(t) && t.stopPropagation();
  }
  render() {
    const t = !!this._slotActions.length, e = !!this._slotMeta.length, r = !!this._slotText.length || this.actionsPosition === "bottom" && t;
    return h`
      <div
        part=${tt({
      "wy-item-rounded": this.rounded,
      "wy-item-rounded-outer": this.outer,
      "wy-item-inner": !0,
      "wy-item-inner-no-padding": this.noPadding,
      "wy-item-interactive": this.interactive,
      "wy-item-sm": this.size === "sm",
      "wy-item-md": this.size === "md",
      "wy-item-lg": this.size === "lg",
      "wy-item-top": this.align === "top",
      "wy-item-middle": this.align === "middle",
      "wy-item-bottom": this.align === "bottom",
      "wy-read": this.status === "read",
      "wy-unread": this.status === "unread",
      "wy-selected": this.selected,
      "wy-disabled": this.disabled
    })}
      >
        <div part="wy-item-image"><slot name="image"></slot></div>
        <div part="wy-item-body">
          <slot name="content">
            <div part="wy-item-rows">
              <div part="wy-item-row wy-item-row-first">
                <div part="wy-item-title wy-item-left">
                  ${this.url ? h`<a href=${this.url} @click=${(a) => this.handleLinkClick(a)}><slot name="title"></slot></a>` : h`<slot name="title"></slot>`}
                </div>
                <div part="wy-item-meta wy-meta wy-meta-sm" ?hidden=${!e}
                  ><slot name="meta" @slotchange=${() => this.requestUpdate()}></slot
                ></div>
              </div>
              <div part="wy-item-row wy-item-row-second" ?hidden=${!r}>
                <div part="wy-item-text wy-item-left ${tt({ "wy-item-text-sm": this.size === "auto" })}"
                  ><slot name="text" @slotchange=${() => this.requestUpdate()}></slot
                ></div>
                ${this.actionsPosition === "bottom" ? h`
                      <div part="wy-item-actions wy-item-actions-bottom"
                        ><slot name="actions" @slotchange=${() => this.requestUpdate()}></slot
                      ></div>
                    ` : g}
              </div>
            </div>
          </slot>
        </div>
        ${this.actionsPosition !== "bottom" ? h`
              <div
                part="wy-item-actions ${tt({
      "wy-item-actions-top": this.align === "top" || this.actionsPosition === "top"
    })}"
                ?hidden=${!t}
                ><slot name="actions" @slotchange=${() => this.requestUpdate()}></slot
              ></div>
            ` : g}
      </div>
    `;
  }
}, n(co, "WyItem"), co);
le.styles = [Lt, Ru, fl];
te([
  y()
], le.prototype, "size", 2);
te([
  y()
], le.prototype, "align", 2);
te([
  y()
], le.prototype, "actionsPosition", 2);
te([
  y({ type: Boolean, reflect: !0 })
], le.prototype, "rounded", 2);
te([
  y({ type: Boolean, reflect: !0 })
], le.prototype, "noPadding", 2);
te([
  y({ type: Boolean, reflect: !0 })
], le.prototype, "outer", 2);
te([
  y({ type: Boolean, reflect: !0 })
], le.prototype, "interactive", 2);
te([
  y({ type: Boolean, reflect: !0 })
], le.prototype, "selected", 2);
te([
  y({ type: Boolean, reflect: !0 })
], le.prototype, "disabled", 2);
te([
  y({ reflect: !0 })
], le.prototype, "url", 2);
te([
  y({ type: Boolean, reflect: !0 })
], le.prototype, "trashed", 2);
te([
  y()
], le.prototype, "status", 2);
te([
  yl({ slot: "actions" })
], le.prototype, "_slotActions", 2);
te([
  yl({ slot: "meta" })
], le.prototype, "_slotMeta", 2);
te([
  yl({ slot: "text" })
], le.prototype, "_slotText", 2);
le = te([
  U("wy-item")
], le);
var lo;
let Zr = (lo = class extends nt {
  constructor() {
    super(...arguments), this.exportParts = new W(this), this.outer = !1, this.bordered = !1, this.rounded = !1, this.filled = !1;
  }
  render() {
    const t = {
      "wy-item-list": !0,
      "wy-item-list-bordered": this.bordered,
      "wy-item-list-rounded": this.rounded,
      "wy-item-list-outer": this.outer,
      "wy-item-list-filled": this.filled
    };
    return h`<div part=${tt(t)}><slot></slot></div>`;
  }
}, n(lo, "WyItemList"), lo);
Zr.styles = [Ru, Y];
te([
  y({ type: Boolean })
], Zr.prototype, "outer", 2);
te([
  y({ type: Boolean })
], Zr.prototype, "bordered", 2);
te([
  y({ type: Boolean })
], Zr.prototype, "rounded", 2);
te([
  y({ type: Boolean })
], Zr.prototype, "filled", 2);
Zr = te([
  U("wy-item-list")
], Zr);
const z1 = j`[part~=wy-button]{--wy-component-color: var(--wy-primary, var(--wy-primary-light, #2f628c));color:var(--wy-component-color);background:var(--wy-component-background-color, transparent);height:var(--wy-button-size, calc(var(--wy-button-line-height, 1.5) * var(--wy-size, 1rem) + 2 * var(--wy-button-padding-y, var(--wy-padding, calc(.5 * var(--wy-size, 1rem))))));padding:var(--wy-button-padding-y, var(--wy-padding, calc(.5 * var(--wy-size, 1rem)))) var(--wy-button-padding-x, calc(var(--wy-padding, calc(.5 * var(--wy-size, 1rem))) + var(--wy-button-padding-y, var(--wy-padding, calc(.5 * var(--wy-size, 1rem))))));font-family:var(--wy-button-font-family, var(--wy-font-family, unset)),inherit;font-size:var(--wy-button-font-size, var(--wy-font-size, 1em));font-weight:var(--wy-button-font-weight, var(--wy-font-weight, unset));line-height:var(--wy-button-line-height, var(--wy-line-height, 1.5));text-align:center;text-decoration:none;vertical-align:middle;cursor:pointer;-webkit-user-select:none;user-select:none;border:var(--wy-button-border-width, var(--wy-border-outline, 0)) solid var(--wy-outline, var(--wy-outline-light, #72777f));border-radius:var(--wy-button-border-radius, var(--wy-border-radius, calc(1.25 * var(--wy-size, 1rem))));display:inline-flex;align-items:center;justify-content:center;gap:var(--wy-button-gap, var(--wy-gap-sm, calc(.5 * var(--wy-gap, calc(.5 * var(--wy-size, 1rem))))));position:relative}[part~=wy-button][part~=wy-button-sm]{height:calc(var(--wy-button-size, calc(var(--wy-button-line-height, 1.5) * var(--wy-size, 1rem) + 2 * var(--wy-button-padding-y, var(--wy-padding, calc(.5 * var(--wy-size, 1rem)))))) * .8);padding:calc(var(--wy-button-padding-y, var(--wy-padding, calc(.5 * var(--wy-size, 1rem)))) * .25) calc(var(--wy-button-padding-x, calc(var(--wy-padding, calc(.5 * var(--wy-size, 1rem))) + var(--wy-button-padding-y, var(--wy-padding, calc(.5 * var(--wy-size, 1rem)))))) * .5)}[part~=wy-button][part~=wy-disabled],[part~=wy-button]:disabled{opacity:var(--wy-opacity-disabled, 38%);cursor:unset;pointer-events:none}[part~=wy-button-state-layer]>*{z-index:3}[part~=wy-button-state-layer]:before{content:\"\";z-index:2;position:absolute;inset:0;border-radius:inherit;background:var(--wy-component-color);opacity:0%}[part~=wy-button-state-layer]:hover:before{opacity:8%}[part~=wy-button-state-layer]:focus:before,[part~=wy-button-state-layer]:active:before,[part~=wy-button-state-layer][part~=wy-active]:before{opacity:12%}[part~=wy-button-icon]{--wy-component-color: var(--wy-on-surface-variant, var(--wy-on-surface-variant-light, #42474e));width:var(--wy-button-size, calc(var(--wy-button-line-height, 1.5) * var(--wy-size, 1rem) + 2 * var(--wy-button-padding-y, var(--wy-padding, calc(.5 * var(--wy-size, 1rem))))));height:var(--wy-button-size, calc(var(--wy-button-line-height, 1.5) * var(--wy-size, 1rem) + 2 * var(--wy-button-padding-y, var(--wy-padding, calc(.5 * var(--wy-size, 1rem))))));padding:var(--wy-button-padding-y, var(--wy-padding, calc(.5 * var(--wy-size, 1rem))));line-height:1;align-items:center;justify-content:center}[part~=wy-button-icon][part~=wy-button-sm]{height:calc(var(--wy-button-size, calc(var(--wy-button-line-height, 1.5) * var(--wy-size, 1rem) + 2 * var(--wy-button-padding-y, var(--wy-padding, calc(.5 * var(--wy-size, 1rem)))))) * .6);width:calc(var(--wy-button-size, calc(var(--wy-button-line-height, 1.5) * var(--wy-size, 1rem) + 2 * var(--wy-button-padding-y, var(--wy-padding, calc(.5 * var(--wy-size, 1rem)))))) * .6);padding:0;margin:calc(var(--wy-button-padding-y, var(--wy-padding, calc(.5 * var(--wy-size, 1rem)))) * .5)}[part~=wy-button-icon][part~=wy-button-sm][part~=wy-button-state-layer]:before{margin:calc(var(--wy-button-size, calc(var(--wy-button-line-height, 1.5) * var(--wy-size, 1rem) + 2 * var(--wy-button-padding-y, var(--wy-padding, calc(.5 * var(--wy-size, 1rem)))))) * -.1)}[part~=wy-button-icon][part~=wy-active]{--wy-component-color: var(--wy-primary, var(--wy-primary-light, #2f628c))}[part~=wy-button-icon-inline]{height:var(--wy-button-size, calc(var(--wy-button-line-height, 1.5) * var(--wy-size, 1rem) + 2 * var(--wy-button-padding-y, var(--wy-padding, calc(.5 * var(--wy-size, 1rem))))));width:auto;min-width:var(--wy-button-size, calc(var(--wy-button-line-height, 1.5) * var(--wy-size, 1rem) + 2 * var(--wy-button-padding-y, var(--wy-padding, calc(.5 * var(--wy-size, 1rem))))));background:var(--wy-component-background-color);color:currentColor;display:inline-flex;padding:var(--wy-button-padding-y, var(--wy-padding, calc(.5 * var(--wy-size, 1rem))))}[part~=wy-button-icon-inline][part~=wy-button-sm]{height:calc(var(--wy-button-size, calc(var(--wy-button-line-height, 1.5) * var(--wy-size, 1rem) + 2 * var(--wy-button-padding-y, var(--wy-padding, calc(.5 * var(--wy-size, 1rem)))))) * .6);min-width:calc(var(--wy-button-size, calc(var(--wy-button-line-height, 1.5) * var(--wy-size, 1rem) + 2 * var(--wy-button-padding-y, var(--wy-padding, calc(.5 * var(--wy-size, 1rem)))))) * .6);padding:calc(var(--wy-button-padding-y, var(--wy-padding, calc(.5 * var(--wy-size, 1rem)))) * .25);margin:calc(var(--wy-button-padding-y, var(--wy-padding, calc(.5 * var(--wy-size, 1rem)))) * .5)}[part~=wy-button-icon-inline][part~=wy-button-sm][part~=wy-button-state-layer]:before{margin:calc(var(--wy-button-size, calc(var(--wy-button-line-height, 1.5) * var(--wy-size, 1rem) + 2 * var(--wy-button-padding-y, var(--wy-padding, calc(.5 * var(--wy-size, 1rem)))))) * -.1)}[part~=wy-button-inline]{border-radius:var(--wy-border-radius-pill, var(--wy-border-radius, calc(2.5 * var(--wy-size, 1rem))));padding:var(--wy-button-padding-y, var(--wy-padding, calc(.5 * var(--wy-size, 1rem))))}[part~=wy-button-inline][part~=wy-button-sm]{height:calc(var(--wy-button-size, calc(var(--wy-button-line-height, 1.5) * var(--wy-size, 1rem) + 2 * var(--wy-button-padding-y, var(--wy-padding, calc(.5 * var(--wy-size, 1rem)))))) * .8);min-width:calc(var(--wy-button-size, calc(var(--wy-button-line-height, 1.5) * var(--wy-size, 1rem) + 2 * var(--wy-button-padding-y, var(--wy-padding, calc(.5 * var(--wy-size, 1rem)))))) * .8);padding:calc(var(--wy-button-padding-y, var(--wy-padding, calc(.5 * var(--wy-size, 1rem)))) * .75);margin-inline:calc(-.75 * var(--wy-button-padding-y, var(--wy-padding, calc(.5 * var(--wy-size, 1rem)))))}[part~=wy-button-primary]{--wy-component-background-color: var(--wy-primary-container, var(--wy-primary-container-light, #cee5ff));--wy-component-color: var(--wy-on-primary-container, var(--wy-on-primary-container-light, #0d4a73))}[part~=wy-button-variant]{--wy-component-background-color: var(--wy-surface-variant, var(--wy-surface-variant-light, #dee3eb));--wy-component-color: var(--wy-on-surface-variant, var(--wy-on-surface-variant-light, #42474e))}[part~=wy-button-primary-text]{--wy-component-color: var(--wy-primary, var(--wy-primary-light, #2f628c))}[part~=wy-button-inherit-color]{--wy-component-color: currentColor}[part~=wy-button-no-color]{--wy-component-background-color: transparent;--wy-component-color: unset}[part~=wy-button-link]{padding:0;color:var(--wy-link, var(--wy-primary-light, #2f628c));background:transparent;text-decoration:var(--wy-link-decoration, none);height:auto;line-height:inherit;font-size:inherit;vertical-align:baseline}[part~=wy-button-link]:hover,[part~=wy-button-link]:focus{text-decoration:var(--wy-link-hover-decoration, var(--wy-link-decoration, none))}[part~=wy-buttons]{display:flex;align-items:center;flex-wrap:wrap;gap:calc(.5 * var(--wy-size, 1rem))}[part~=wy-buttons-reverse]{flex-direction:row-reverse}[part~=wy-buttons-floating]{position:sticky;padding:var(--wy-padding, calc(.5 * var(--wy-size, 1rem))) var(--wy-padding, calc(.5 * var(--wy-size, 1rem))) 0;top:0;height:0;margin-bottom:calc(-1 * var(--wy-padding, calc(.5 * var(--wy-size, 1rem))));z-index:1030;--wy-component-background-color: var(--wy-surface-variant, var(--wy-surface-variant-light, #dee3eb));--wy-component-color: var(--wy-on-surface-variant, var(--wy-on-surface-variant-light, #42474e))}[part~=wy-buttons-sticky]{position:sticky;padding:var(--wy-padding-sm, calc(.75 * var(--wy-padding, calc(.5 * var(--wy-size, 1rem)))));top:var(--wy-component-offset-top, 0px);z-index:1021;--wy-component-background-color: var(--wy-surface-variant, var(--wy-surface-variant-light, #dee3eb));--wy-component-color: var(--wy-on-surface-variant, var(--wy-on-surface-variant-light, #42474e))}`, F1 = j`[part~=wy-tabs]{display:flex;overflow-y:hidden;padding:var(--wy-padding, calc(.5 * var(--wy-size, 1rem)));flex:1 1 auto}[part~=wy-tabs]{overflow-x:auto;max-width:100%;overflow-y:hidden;overflow-anchor:none;touch-action:pan-x;will-change:scroll-position,opacity;backface-visibility:hidden}[part~=wy-tabs]{scrollbar-width:thin;scrollbar-color:var(--wy-outline-variant, var(--wy-outline-variant-light, #c2c7cf)) transparent}[part~=wy-tabs]::-webkit-scrollbar{height:.375rem;width:.375rem;background:transparent;z-index:999}[part~=wy-tabs]::-webkit-scrollbar-track{background:transparent}[part~=wy-tabs]::-webkit-scrollbar-thumb{background-color:var(--wy-outline-variant, var(--wy-outline-variant-light, #c2c7cf));opacity:75%;border:0 solid transparent;border-radius:0;background-clip:padding-box}[part~=wy-tabs]::-webkit-scrollbar-thumb:hover{background-color:var(--wy-outline, var(--wy-outline-light, #72777f));opacity:75%}[part~=wy-tabs]::-webkit-scrollbar-thumb:window-inactive{background-color:var(--wy-outline, var(--wy-outline-light, #72777f));opacity:50%}[part~=wy-tab]{--wy-component-color: var(--wy-primary, var(--wy-primary-light, #2f628c));--wy-component-background-color: transparent;color:var(--wy-component-color);background:var(--wy-component-background-color);border:none;border-radius:var(--wy-border-radius-pill, var(--wy-border-radius, calc(2.5 * var(--wy-size, 1rem))));padding:var(--wy-button-padding-y, var(--wy-padding, calc(.5 * var(--wy-size, 1rem)))) var(--wy-button-padding-x, calc(var(--wy-padding, calc(.5 * var(--wy-size, 1rem))) + var(--wy-button-padding-y, var(--wy-padding, calc(.5 * var(--wy-size, 1rem))))));display:inline-flex;flex-direction:column;align-items:center;justify-content:center;gap:var(--wy-gap-sm, calc(.5 * var(--wy-gap, var(--wy-padding, calc(.5 * var(--wy-size, 1rem))))));position:relative;text-align:center;text-decoration:none;cursor:pointer;-webkit-user-select:none;user-select:none}[part~=wy-tab] .wy-tab-icon{position:relative;padding:calc(.25 * var(--wy-size, 1rem)) calc(1 * var(--wy-size, 1rem));border-radius:var(--wy-button-border-radius, var(--wy-border-radius, calc(1.25 * var(--wy-size, 1rem))));display:inline-flex}[part~=wy-tab] .wy-tab-icon:before{content:\"\";position:absolute;top:0;left:0;width:100%;height:100%;border-radius:inherit;background:var(--wy-component-color);opacity:0%}[part~=wy-tab]:hover .wy-tab-icon:before{opacity:8%}[part~=wy-tab]:focus .wy-tab-icon:before,[part~=wy-tab]:active .wy-tab-icon:before,[part~=wy-tab].wy-active .wy-tab-icon:before{opacity:12%}[part~=wy-tab] .wy-tab-label{font-family:var(--wy-button-font-family, var(--wy-font-family, unset)),inherit;line-height:var(--wy-button-line-height, var(--wy-line-height, 1.5));font-size:var(--wy-font-size-xs, calc(.75 * var(--wy-font-size, 1em)));font-weight:var(--wy-font-weight, unset)}[part~=wy-button-state-layer][part~=wy-button-sm][part~=wy-tab-sm]{padding:var(--wy-button-padding-y, var(--wy-padding, calc(.5 * var(--wy-size, 1rem)))) calc(var(--wy-button-padding-x, calc(var(--wy-padding, calc(.5 * var(--wy-size, 1rem))) + var(--wy-button-padding-y, var(--wy-padding, calc(.5 * var(--wy-size, 1rem)))))) * .5);margin:var(--wy-padding, calc(.5 * var(--wy-size, 1rem))) auto;font-size:var(--wy-font-size-sm, calc(.875 * var(--wy-font-size, 1em)));height:calc(var(--wy-button-size, calc(var(--wy-button-line-height, 1.5) * var(--wy-size, 1rem) + 2 * var(--wy-button-padding-y, var(--wy-padding, calc(.5 * var(--wy-size, 1rem)))))) * .6)}[part~=wy-button-state-layer][part~=wy-button-sm][part~=wy-tab-sm]:before{margin:calc(var(--wy-button-padding-x, calc(var(--wy-padding, calc(.5 * var(--wy-size, 1rem))) + var(--wy-button-padding-y, var(--wy-padding, calc(.5 * var(--wy-size, 1rem)))))) * -.25) calc(var(--wy-button-padding-x, calc(var(--wy-padding, calc(.5 * var(--wy-size, 1rem))) + var(--wy-button-padding-y, var(--wy-padding, calc(.5 * var(--wy-size, 1rem)))))) * 0)}`;
var _4 = Object.defineProperty, P4 = Object.getOwnPropertyDescriptor, We = /* @__PURE__ */ n((s, t, e, i) => {
  for (var r = i > 1 ? void 0 : i ? P4(t, e) : t, a = s.length - 1, o; a >= 0; a--)
    (o = s[a]) && (r = (i ? o(t, e, r) : o(r)) || r);
  return i && r && _4(t, e, r), r;
}, "__decorateClass$19"), ho;
let Ye = (ho = class extends nt {
  constructor() {
    super(...arguments), this.exportParts = new W(this, !1), this.kind = "filled", this.active = !1, this.small = !1, this.disabled = !1;
  }
  render() {
    const t = {
      "wy-button": !0,
      "wy-active": !!this.active,
      "wy-disabled": !!this.disabled,
      "wy-button-inline": this.kind === "inline",
      "wy-button-icon": this.kind === "icon",
      "wy-button-icon-inline": this.kind === "icon-inline",
      "wy-button-link": this.kind === "link",
      "wy-button-primary": this.color === "primary",
      "wy-button-primary-text": this.color === "primary-text",
      "wy-button-inherit-color": this.color === "inherit",
      "wy-button-no-color": this.color === "none",
      "wy-button-variant": this.color === "variant",
      "wy-button-type-submit": this.type === "submit",
      "wy-button-type-reset": this.type === "reset",
      "wy-button-state-layer": this.kind !== "link" && !this.disabled,
      "wy-button-sm": !!this.small,
      "wy-tab": this.kind === "tab",
      "wy-tab-sm": this.kind === "tab" && !!this.small
    };
    return this.href ? h`
      <a href=${this.href} target=${ot(this.target)} download=${ot(this.download)} part=${tt(t)} aria-disabled=${this.disabled ? "true" : "false"}>
        <slot></slot>
      </a>
    ` : h`
      <button part=${tt(t)} type=${ot(this.type)} ?disabled=${this.disabled} aria-disabled=${this.disabled ? "true" : "false"}>
        <slot></slot>
      </button>
    `;
  }
}, n(ho, "WyButton"), ho);
Ye.styles = [
  Lt,
  z1,
  F1,
  Y,
  j`
      :host {
        position: relative;
      }
    `
];
We([
  y()
], Ye.prototype, "type", 2);
We([
  y()
], Ye.prototype, "kind", 2);
We([
  y({ type: Boolean })
], Ye.prototype, "active", 2);
We([
  y()
], Ye.prototype, "color", 2);
We([
  y({ type: Boolean })
], Ye.prototype, "small", 2);
We([
  y({ type: Boolean, reflect: !0 })
], Ye.prototype, "disabled", 2);
We([
  y()
], Ye.prototype, "href", 2);
We([
  y()
], Ye.prototype, "target", 2);
We([
  y()
], Ye.prototype, "download", 2);
Ye = We([
  U("wy-button")
], Ye);
var uo;
let qs = (uo = class extends nt {
  constructor() {
    super(...arguments), this.exportParts = new W(this), this.tabs = !1, this.position = "normal", this.reverse = !1;
  }
  render() {
    const t = {
      "wy-buttons": !this.tabs,
      "wy-buttons-reverse": this.reverse,
      "wy-tabs": this.tabs,
      "wy-buttons-floating": this.position === "floating",
      "wy-buttons-sticky": this.position === "sticky"
    };
    return h`<div part=${tt(t)}><slot></slot></div>`;
  }
}, n(uo, "WyButtons"), uo);
qs.styles = [
  Lt,
  z1,
  F1,
  Y
];
We([
  y({ type: Boolean })
], qs.prototype, "tabs", 2);
We([
  y({ type: String })
], qs.prototype, "position", 2);
We([
  y({ type: Boolean })
], qs.prototype, "reverse", 2);
qs = We([
  U("wy-buttons")
], qs);
const I1 = "important", S4 = " !" + I1, Jl = ln(class extends wl {
  constructor(s) {
    if (super(s), s.type !== Xd.ATTRIBUTE || s.name !== "style" || s.strings?.length > 2) throw Error("The `styleMap` directive must be used in the `style` attribute and must be the only part in the attribute.");
  }
  render(s) {
    return Object.keys(s).reduce((t, e) => {
      const i = s[e];
      return i == null ? t : t + `${e = e.includes("-") ? e : e.replace(/(?:^(webkit|moz|ms|o)|)(?=[A-Z])/g, "-$&").toLowerCase()}:${i};`;
    }, "");
  }
  update(s, [t]) {
    const { style: e } = s.element;
    if (this.ft === void 0) return this.ft = new Set(Object.keys(t)), this.render(t);
    for (const i of this.ft) t[i] == null && (this.ft.delete(i), i.includes("-") ? e.removeProperty(i) : e[i] = null);
    for (const i in t) {
      const r = t[i];
      if (r != null) {
        this.ft.add(i);
        const a = typeof r == "string" && r.endsWith(S4);
        i.includes("-") || a ? e.setProperty(i, a ? r.slice(0, -11) : r, a ? I1 : "") : e[i] = r;
      }
    }
    return gu;
  }
}), su = Math.min, Is = Math.max, nu = Math.round, Ch = Math.floor, Li = /* @__PURE__ */ n((s) => ({
  x: s,
  y: s
}), "createCoords"), M4 = {
  left: "right",
  right: "left",
  bottom: "top",
  top: "bottom"
};
function Hm(s, t, e) {
  return Is(s, su(t, e));
}
n(Hm, "clamp");
function Tu(s, t) {
  return typeof s == "function" ? s(t) : s;
}
n(Tu, "evaluate");
function Ws(s) {
  return s.split("-")[0];
}
n(Ws, "getSide");
function Lu(s) {
  return s.split("-")[1];
}
n(Lu, "getAlignment");
function D1(s) {
  return s === "x" ? "y" : "x";
}
n(D1, "getOppositeAxis");
function V1(s) {
  return s === "y" ? "height" : "width";
}
n(V1, "getAxisLength");
function Qr(s) {
  const t = s[0];
  return t === "t" || t === "b" ? "y" : "x";
}
n(Qr, "getSideAxis");
function H1(s) {
  return D1(Qr(s));
}
n(H1, "getAlignmentAxis");
function E4(s, t, e) {
  e === void 0 && (e = !1);
  const i = Lu(s), r = H1(s), a = V1(r);
  let o = r === "x" ? i === (e ? "end" : "start") ? "right" : "left" : i === "start" ? "bottom" : "top";
  return t.reference[a] > t.floating[a] && (o = au(o)), [o, au(o)];
}
n(E4, "getAlignmentSides");
function A4(s) {
  const t = au(s);
  return [Gy(s), t, Gy(t)];
}
n(A4, "getExpandedPlacements");
function Gy(s) {
  return s.includes("start") ? s.replace("start", "end") : s.replace("end", "start");
}
n(Gy, "getOppositeAlignmentPlacement");
const Um = ["left", "right"], Bm = ["right", "left"], R4 = ["top", "bottom"], T4 = ["bottom", "top"];
function L4(s, t, e) {
  switch (s) {
    case "top":
    case "bottom":
      return e ? t ? Bm : Um : t ? Um : Bm;
    case "left":
    case "right":
      return t ? R4 : T4;
    default:
      return [];
  }
}
n(L4, "getSideList");
function O4(s, t, e, i) {
  const r = Lu(s);
  let a = L4(Ws(s), e === "start", i);
  return r && (a = a.map((o) => o + "-" + r), t && (a = a.concat(a.map(Gy)))), a;
}
n(O4, "getOppositeAxisPlacements");
function au(s) {
  const t = Ws(s);
  return M4[t] + s.slice(t.length);
}
n(au, "getOppositePlacement");
function z4(s) {
  return {
    top: 0,
    right: 0,
    bottom: 0,
    left: 0,
    ...s
  };
}
n(z4, "expandPaddingObject");
function F4(s) {
  return typeof s != "number" ? z4(s) : {
    top: s,
    right: s,
    bottom: s,
    left: s
  };
}
n(F4, "getPaddingObject");
function ou(s) {
  const {
    x: t,
    y: e,
    width: i,
    height: r
  } = s;
  return {
    width: i,
    height: r,
    top: e,
    left: t,
    right: t + i,
    bottom: e + r,
    x: t,
    y: e
  };
}
n(ou, "rectToClientRect");
function Nm(s, t, e) {
  let {
    reference: i,
    floating: r
  } = s;
  const a = Qr(t), o = H1(t), c = V1(o), l = Ws(t), u = a === "y", b = i.x + i.width / 2 - r.width / 2, C = i.y + i.height / 2 - r.height / 2, v = i[c] / 2 - r[c] / 2;
  let f;
  switch (l) {
    case "top":
      f = {
        x: b,
        y: i.y - r.height
      };
      break;
    case "bottom":
      f = {
        x: b,
        y: i.y + i.height
      };
      break;
    case "right":
      f = {
        x: i.x + i.width,
        y: C
      };
      break;
    case "left":
      f = {
        x: i.x - r.width,
        y: C
      };
      break;
    default:
      f = {
        x: i.x,
        y: i.y
      };
  }
  switch (Lu(t)) {
    case "start":
      f[o] -= v * (e && u ? -1 : 1);
      break;
    case "end":
      f[o] += v * (e && u ? -1 : 1);
      break;
  }
  return f;
}
n(Nm, "computeCoordsFromPlacement");
async function I4(s, t) {
  var e;
  t === void 0 && (t = {});
  const {
    x: i,
    y: r,
    platform: a,
    rects: o,
    elements: c,
    strategy: l
  } = s, {
    boundary: u = "clippingAncestors",
    rootBoundary: b = "viewport",
    elementContext: C = "floating",
    altBoundary: v = !1,
    padding: f = 0
  } = Tu(t, s), m = F4(f), E = c[v ? C === "floating" ? "reference" : "floating" : C], M = ou(await a.getClippingRect({
    element: (e = await (a.isElement == null ? void 0 : a.isElement(E))) == null || e ? E : E.contextElement || await (a.getDocumentElement == null ? void 0 : a.getDocumentElement(c.floating)),
    boundary: u,
    rootBoundary: b,
    strategy: l
  })), A = C === "floating" ? {
    x: i,
    y: r,
    width: o.floating.width,
    height: o.floating.height
  } : o.reference, H = await (a.getOffsetParent == null ? void 0 : a.getOffsetParent(c.floating)), D = await (a.isElement == null ? void 0 : a.isElement(H)) ? await (a.getScale == null ? void 0 : a.getScale(H)) || {
    x: 1,
    y: 1
  } : {
    x: 1,
    y: 1
  }, I = ou(a.convertOffsetParentRelativeRectToViewportRelativeRect ? await a.convertOffsetParentRelativeRectToViewportRelativeRect({
    elements: c,
    rect: A,
    offsetParent: H,
    strategy: l
  }) : A);
  return {
    top: (M.top - I.top + m.top) / D.y,
    bottom: (I.bottom - M.bottom + m.bottom) / D.y,
    left: (M.left - I.left + m.left) / D.x,
    right: (I.right - M.right + m.right) / D.x
  };
}
n(I4, "detectOverflow");
const D4 = 50, V4 = /* @__PURE__ */ n(async (s, t, e) => {
  const {
    placement: i = "bottom",
    strategy: r = "absolute",
    middleware: a = [],
    platform: o
  } = e, c = o.detectOverflow ? o : {
    ...o,
    detectOverflow: I4
  }, l = await (o.isRTL == null ? void 0 : o.isRTL(t));
  let u = await o.getElementRects({
    reference: s,
    floating: t,
    strategy: r
  }), {
    x: b,
    y: C
  } = Nm(u, i, l), v = i, f = 0;
  const m = {};
  for (let $ = 0; $ < a.length; $++) {
    const E = a[$];
    if (!E)
      continue;
    const {
      name: M,
      fn: A
    } = E, {
      x: H,
      y: D,
      data: I,
      reset: B
    } = await A({
      x: b,
      y: C,
      initialPlacement: i,
      placement: v,
      strategy: r,
      middlewareData: m,
      rects: u,
      platform: c,
      elements: {
        reference: s,
        floating: t
      }
    });
    b = H ?? b, C = D ?? C, m[M] = {
      ...m[M],
      ...I
    }, B && f < D4 && (f++, typeof B == "object" && (B.placement && (v = B.placement), B.rects && (u = B.rects === !0 ? await o.getElementRects({
      reference: s,
      floating: t,
      strategy: r
    }) : B.rects), {
      x: b,
      y: C
    } = Nm(u, v, l)), $ = -1);
  }
  return {
    x: b,
    y: C,
    placement: v,
    strategy: r,
    middlewareData: m
  };
}, "computePosition$1"), H4 = /* @__PURE__ */ n(function(s) {
  return s === void 0 && (s = {}), {
    name: "flip",
    options: s,
    async fn(t) {
      var e, i;
      const {
        placement: r,
        middlewareData: a,
        rects: o,
        initialPlacement: c,
        platform: l,
        elements: u
      } = t, {
        mainAxis: b = !0,
        crossAxis: C = !0,
        fallbackPlacements: v,
        fallbackStrategy: f = "bestFit",
        fallbackAxisSideDirection: m = "none",
        flipAlignment: $ = !0,
        ...E
      } = Tu(s, t);
      if ((e = a.arrow) != null && e.alignmentOffset)
        return {};
      const M = Ws(r), A = Qr(c), H = Ws(c) === c, D = await (l.isRTL == null ? void 0 : l.isRTL(u.floating)), I = v || (H || !$ ? [au(c)] : A4(c)), B = m !== "none";
      !v && B && I.push(...O4(c, $, m, D));
      const Q = [c, ...I], it = await l.detectOverflow(t, E), ct = [];
      let st = ((i = a.flip) == null ? void 0 : i.overflows) || [];
      if (b && ct.push(it[M]), C) {
        const vt = E4(r, o, D);
        ct.push(it[vt[0]], it[vt[1]]);
      }
      if (st = [...st, {
        placement: r,
        overflows: ct
      }], !ct.every((vt) => vt <= 0)) {
        var Ot, zt;
        const vt = (((Ot = a.flip) == null ? void 0 : Ot.index) || 0) + 1, Dt = Q[vt];
        if (Dt && (!(C === "alignment" ? A !== Qr(Dt) : !1) || // We leave the current main axis only if every placement on that axis
        // overflows the main axis.
        st.every((xt) => Qr(xt.placement) === A ? xt.overflows[0] > 0 : !0)))
          return {
            data: {
              index: vt,
              overflows: st
            },
            reset: {
              placement: Dt
            }
          };
        let ge = (zt = st.filter((At) => At.overflows[0] <= 0).sort((At, xt) => At.overflows[1] - xt.overflows[1])[0]) == null ? void 0 : zt.placement;
        if (!ge)
          switch (f) {
            case "bestFit": {
              var Mt;
              const At = (Mt = st.filter((xt) => {
                if (B) {
                  const ee = Qr(xt.placement);
                  return ee === A || // Create a bias to the `y` side axis due to horizontal
                  // reading directions favoring greater width.
                  ee === "y";
                }
                return !0;
              }).map((xt) => [xt.placement, xt.overflows.filter((ee) => ee > 0).reduce((ee, lt) => ee + lt, 0)]).sort((xt, ee) => xt[1] - ee[1])[0]) == null ? void 0 : Mt[0];
              At && (ge = At);
              break;
            }
            case "initialPlacement":
              ge = c;
              break;
          }
        if (r !== ge)
          return {
            reset: {
              placement: ge
            }
          };
      }
      return {};
    }
  };
}, "flip$1"), U4 = /* @__PURE__ */ new Set(["left", "top"]);
async function B4(s, t) {
  const {
    placement: e,
    platform: i,
    elements: r
  } = s, a = await (i.isRTL == null ? void 0 : i.isRTL(r.floating)), o = Ws(e), c = Lu(e), l = Qr(e) === "y", u = U4.has(o) ? -1 : 1, b = a && l ? -1 : 1, C = Tu(t, s);
  let {
    mainAxis: v,
    crossAxis: f,
    alignmentAxis: m
  } = typeof C == "number" ? {
    mainAxis: C,
    crossAxis: 0,
    alignmentAxis: null
  } : {
    mainAxis: C.mainAxis || 0,
    crossAxis: C.crossAxis || 0,
    alignmentAxis: C.alignmentAxis
  };
  return c && typeof m == "number" && (f = c === "end" ? m * -1 : m), l ? {
    x: f * b,
    y: v * u
  } : {
    x: v * u,
    y: f * b
  };
}
n(B4, "convertValueToCoords");
const N4 = /* @__PURE__ */ n(function(s) {
  return s === void 0 && (s = 0), {
    name: "offset",
    options: s,
    async fn(t) {
      var e, i;
      const {
        x: r,
        y: a,
        placement: o,
        middlewareData: c
      } = t, l = await B4(t, s);
      return o === ((e = c.offset) == null ? void 0 : e.placement) && (i = c.arrow) != null && i.alignmentOffset ? {} : {
        x: r + l.x,
        y: a + l.y,
        data: {
          ...l,
          placement: o
        }
      };
    }
  };
}, "offset$1"), q4 = /* @__PURE__ */ n(function(s) {
  return s === void 0 && (s = {}), {
    name: "shift",
    options: s,
    async fn(t) {
      const {
        x: e,
        y: i,
        placement: r,
        platform: a
      } = t, {
        mainAxis: o = !0,
        crossAxis: c = !1,
        limiter: l = {
          fn: /* @__PURE__ */ n((M) => {
            let {
              x: A,
              y: H
            } = M;
            return {
              x: A,
              y: H
            };
          }, "fn")
        },
        ...u
      } = Tu(s, t), b = {
        x: e,
        y: i
      }, C = await a.detectOverflow(t, u), v = Qr(Ws(r)), f = D1(v);
      let m = b[f], $ = b[v];
      if (o) {
        const M = f === "y" ? "top" : "left", A = f === "y" ? "bottom" : "right", H = m + C[M], D = m - C[A];
        m = Hm(H, m, D);
      }
      if (c) {
        const M = v === "y" ? "top" : "left", A = v === "y" ? "bottom" : "right", H = $ + C[M], D = $ - C[A];
        $ = Hm(H, $, D);
      }
      const E = l.fn({
        ...t,
        [f]: m,
        [v]: $
      });
      return {
        ...E,
        data: {
          x: E.x - e,
          y: E.y - i,
          enabled: {
            [f]: o,
            [v]: c
          }
        }
      };
    }
  };
}, "shift$1");
function Ou() {
  return typeof window < "u";
}
n(Ou, "hasWindow");
function ml(s) {
  return U1(s) ? (s.nodeName || "").toLowerCase() : "#document";
}
n(ml, "getNodeName");
function Ge(s) {
  var t;
  return (s == null || (t = s.ownerDocument) == null ? void 0 : t.defaultView) || window;
}
n(Ge, "getWindow");
function Bi(s) {
  var t;
  return (t = (U1(s) ? s.ownerDocument : s.document) || window.document) == null ? void 0 : t.documentElement;
}
n(Bi, "getDocumentElement");
function U1(s) {
  return Ou() ? s instanceof Node || s instanceof Ge(s).Node : !1;
}
n(U1, "isNode");
function wi(s) {
  return Ou() ? s instanceof Element || s instanceof Ge(s).Element : !1;
}
n(wi, "isElement");
function Pr(s) {
  return Ou() ? s instanceof HTMLElement || s instanceof Ge(s).HTMLElement : !1;
}
n(Pr, "isHTMLElement");
function qm(s) {
  return !Ou() || typeof ShadowRoot > "u" ? !1 : s instanceof ShadowRoot || s instanceof Ge(s).ShadowRoot;
}
n(qm, "isShadowRoot");
function Jd(s) {
  const {
    overflow: t,
    overflowX: e,
    overflowY: i,
    display: r
  } = fi(s);
  return /auto|scroll|overlay|hidden|clip/.test(t + i + e) && r !== "inline" && r !== "contents";
}
n(Jd, "isOverflowElement");
function W4(s) {
  return /^(table|td|th)$/.test(ml(s));
}
n(W4, "isTableElement");
function zu(s) {
  try {
    if (s.matches(":popover-open"))
      return !0;
  } catch {
  }
  try {
    return s.matches(":modal");
  } catch {
    return !1;
  }
}
n(zu, "isTopLayer");
const j4 = /transform|translate|scale|rotate|perspective|filter/, K4 = /paint|layout|strict|content/, ms = /* @__PURE__ */ n((s) => !!s && s !== "none", "isNotNone");
let Qp;
function Vw(s) {
  const t = wi(s) ? fi(s) : s;
  return ms(t.transform) || ms(t.translate) || ms(t.scale) || ms(t.rotate) || ms(t.perspective) || !Hw() && (ms(t.backdropFilter) || ms(t.filter)) || j4.test(t.willChange || "") || K4.test(t.contain || "");
}
n(Vw, "isContainingBlock");
function Q4(s) {
  let t = Yr(s);
  for (; Pr(t) && !tl(t); ) {
    if (Vw(t))
      return t;
    if (zu(t))
      return null;
    t = Yr(t);
  }
  return null;
}
n(Q4, "getContainingBlock");
function Hw() {
  return Qp == null && (Qp = typeof CSS < "u" && CSS.supports && CSS.supports("-webkit-backdrop-filter", "none")), Qp;
}
n(Hw, "isWebKit");
function tl(s) {
  return /^(html|body|#document)$/.test(ml(s));
}
n(tl, "isLastTraversableNode");
function fi(s) {
  return Ge(s).getComputedStyle(s);
}
n(fi, "getComputedStyle$1");
function Fu(s) {
  return wi(s) ? {
    scrollLeft: s.scrollLeft,
    scrollTop: s.scrollTop
  } : {
    scrollLeft: s.scrollX,
    scrollTop: s.scrollY
  };
}
n(Fu, "getNodeScroll");
function Yr(s) {
  if (ml(s) === "html")
    return s;
  const t = (
    // Step into the shadow DOM of the parent of a slotted node.
    s.assignedSlot || // DOM Element detected.
    s.parentNode || // ShadowRoot detected.
    qm(s) && s.host || // Fallback.
    Bi(s)
  );
  return qm(t) ? t.host : t;
}
n(Yr, "getParentNode");
function B1(s) {
  const t = Yr(s);
  return tl(t) ? s.ownerDocument ? s.ownerDocument.body : s.body : Pr(t) && Jd(t) ? t : B1(t);
}
n(B1, "getNearestOverflowAncestor");
function td(s, t, e) {
  var i;
  t === void 0 && (t = []), e === void 0 && (e = !0);
  const r = B1(s), a = r === ((i = s.ownerDocument) == null ? void 0 : i.body), o = Ge(r);
  if (a) {
    const c = Zy(o);
    return t.concat(o, o.visualViewport || [], Jd(r) ? r : [], c && e ? td(c) : []);
  } else
    return t.concat(r, td(r, [], e));
}
n(td, "getOverflowAncestors");
function Zy(s) {
  return s.parent && Object.getPrototypeOf(s.parent) ? s.frameElement : null;
}
n(Zy, "getFrameElement");
function N1(s) {
  const t = fi(s);
  let e = parseFloat(t.width) || 0, i = parseFloat(t.height) || 0;
  const r = Pr(s), a = r ? s.offsetWidth : e, o = r ? s.offsetHeight : i, c = nu(e) !== a || nu(i) !== o;
  return c && (e = a, i = o), {
    width: e,
    height: i,
    $: c
  };
}
n(N1, "getCssDimensions");
function Uw(s) {
  return wi(s) ? s : s.contextElement;
}
n(Uw, "unwrapElement");
function Un(s) {
  const t = Uw(s);
  if (!Pr(t))
    return Li(1);
  const e = t.getBoundingClientRect(), {
    width: i,
    height: r,
    $: a
  } = N1(t);
  let o = (a ? nu(e.width) : e.width) / i, c = (a ? nu(e.height) : e.height) / r;
  return (!o || !Number.isFinite(o)) && (o = 1), (!c || !Number.isFinite(c)) && (c = 1), {
    x: o,
    y: c
  };
}
n(Un, "getScale");
const G4 = /* @__PURE__ */ Li(0);
function q1(s) {
  const t = Ge(s);
  return !Hw() || !t.visualViewport ? G4 : {
    x: t.visualViewport.offsetLeft,
    y: t.visualViewport.offsetTop
  };
}
n(q1, "getVisualOffsets");
function Z4(s, t, e) {
  return t === void 0 && (t = !1), !e || t && e !== Ge(s) ? !1 : t;
}
n(Z4, "shouldAddVisualOffsets");
function js(s, t, e, i) {
  t === void 0 && (t = !1), e === void 0 && (e = !1);
  const r = s.getBoundingClientRect(), a = Uw(s);
  let o = Li(1);
  t && (i ? wi(i) && (o = Un(i)) : o = Un(s));
  const c = Z4(a, e, i) ? q1(a) : Li(0);
  let l = (r.left + c.x) / o.x, u = (r.top + c.y) / o.y, b = r.width / o.x, C = r.height / o.y;
  if (a) {
    const v = Ge(a), f = i && wi(i) ? Ge(i) : i;
    let m = v, $ = Zy(m);
    for (; $ && i && f !== m; ) {
      const E = Un($), M = $.getBoundingClientRect(), A = fi($), H = M.left + ($.clientLeft + parseFloat(A.paddingLeft)) * E.x, D = M.top + ($.clientTop + parseFloat(A.paddingTop)) * E.y;
      l *= E.x, u *= E.y, b *= E.x, C *= E.y, l += H, u += D, m = Ge($), $ = Zy(m);
    }
  }
  return ou({
    width: b,
    height: C,
    x: l,
    y: u
  });
}
n(js, "getBoundingClientRect");
function Iu(s, t) {
  const e = Fu(s).scrollLeft;
  return t ? t.left + e : js(Bi(s)).left + e;
}
n(Iu, "getWindowScrollBarX");
function W1(s, t) {
  const e = s.getBoundingClientRect(), i = e.left + t.scrollLeft - Iu(s, e), r = e.top + t.scrollTop;
  return {
    x: i,
    y: r
  };
}
n(W1, "getHTMLOffset");
function Y4(s) {
  let {
    elements: t,
    rect: e,
    offsetParent: i,
    strategy: r
  } = s;
  const a = r === "fixed", o = Bi(i), c = t ? zu(t.floating) : !1;
  if (i === o || c && a)
    return e;
  let l = {
    scrollLeft: 0,
    scrollTop: 0
  }, u = Li(1);
  const b = Li(0), C = Pr(i);
  if ((C || !C && !a) && ((ml(i) !== "body" || Jd(o)) && (l = Fu(i)), C)) {
    const f = js(i);
    u = Un(i), b.x = f.x + i.clientLeft, b.y = f.y + i.clientTop;
  }
  const v = o && !C && !a ? W1(o, l) : Li(0);
  return {
    width: e.width * u.x,
    height: e.height * u.y,
    x: e.x * u.x - l.scrollLeft * u.x + b.x + v.x,
    y: e.y * u.y - l.scrollTop * u.y + b.y + v.y
  };
}
n(Y4, "convertOffsetParentRelativeRectToViewportRelativeRect");
function X4(s) {
  return Array.from(s.getClientRects());
}
n(X4, "getClientRects");
function J4(s) {
  const t = Bi(s), e = Fu(s), i = s.ownerDocument.body, r = Is(t.scrollWidth, t.clientWidth, i.scrollWidth, i.clientWidth), a = Is(t.scrollHeight, t.clientHeight, i.scrollHeight, i.clientHeight);
  let o = -e.scrollLeft + Iu(s);
  const c = -e.scrollTop;
  return fi(i).direction === "rtl" && (o += Is(t.clientWidth, i.clientWidth) - r), {
    width: r,
    height: a,
    x: o,
    y: c
  };
}
n(J4, "getDocumentRect");
const Wm = 25;
function t3(s, t) {
  const e = Ge(s), i = Bi(s), r = e.visualViewport;
  let a = i.clientWidth, o = i.clientHeight, c = 0, l = 0;
  if (r) {
    a = r.width, o = r.height;
    const b = Hw();
    (!b || b && t === "fixed") && (c = r.offsetLeft, l = r.offsetTop);
  }
  const u = Iu(i);
  if (u <= 0) {
    const b = i.ownerDocument, C = b.body, v = getComputedStyle(C), f = b.compatMode === "CSS1Compat" && parseFloat(v.marginLeft) + parseFloat(v.marginRight) || 0, m = Math.abs(i.clientWidth - C.clientWidth - f);
    m <= Wm && (a -= m);
  } else u <= Wm && (a += u);
  return {
    width: a,
    height: o,
    x: c,
    y: l
  };
}
n(t3, "getViewportRect");
function e3(s, t) {
  const e = js(s, !0, t === "fixed"), i = e.top + s.clientTop, r = e.left + s.clientLeft, a = Pr(s) ? Un(s) : Li(1), o = s.clientWidth * a.x, c = s.clientHeight * a.y, l = r * a.x, u = i * a.y;
  return {
    width: o,
    height: c,
    x: l,
    y: u
  };
}
n(e3, "getInnerBoundingClientRect");
function jm(s, t, e) {
  let i;
  if (t === "viewport")
    i = t3(s, e);
  else if (t === "document")
    i = J4(Bi(s));
  else if (wi(t))
    i = e3(t, e);
  else {
    const r = q1(s);
    i = {
      x: t.x - r.x,
      y: t.y - r.y,
      width: t.width,
      height: t.height
    };
  }
  return ou(i);
}
n(jm, "getClientRectFromClippingAncestor");
function j1(s, t) {
  const e = Yr(s);
  return e === t || !wi(e) || tl(e) ? !1 : fi(e).position === "fixed" || j1(e, t);
}
n(j1, "hasFixedPositionAncestor");
function i3(s, t) {
  const e = t.get(s);
  if (e)
    return e;
  let i = td(s, [], !1).filter((c) => wi(c) && ml(c) !== "body"), r = null;
  const a = fi(s).position === "fixed";
  let o = a ? Yr(s) : s;
  for (; wi(o) && !tl(o); ) {
    const c = fi(o), l = Vw(o);
    !l && c.position === "fixed" && (r = null), (a ? !l && !r : !l && c.position === "static" && !!r && (r.position === "absolute" || r.position === "fixed") || Jd(o) && !l && j1(s, o)) ? i = i.filter((b) => b !== o) : r = c, o = Yr(o);
  }
  return t.set(s, i), i;
}
n(i3, "getClippingElementAncestors");
function r3(s) {
  let {
    element: t,
    boundary: e,
    rootBoundary: i,
    strategy: r
  } = s;
  const o = [...e === "clippingAncestors" ? zu(t) ? [] : i3(t, this._c) : [].concat(e), i], c = jm(t, o[0], r);
  let l = c.top, u = c.right, b = c.bottom, C = c.left;
  for (let v = 1; v < o.length; v++) {
    const f = jm(t, o[v], r);
    l = Is(f.top, l), u = su(f.right, u), b = su(f.bottom, b), C = Is(f.left, C);
  }
  return {
    width: u - C,
    height: b - l,
    x: C,
    y: l
  };
}
n(r3, "getClippingRect");
function s3(s) {
  const {
    width: t,
    height: e
  } = N1(s);
  return {
    width: t,
    height: e
  };
}
n(s3, "getDimensions");
function n3(s, t, e) {
  const i = Pr(t), r = Bi(t), a = e === "fixed", o = js(s, !0, a, t);
  let c = {
    scrollLeft: 0,
    scrollTop: 0
  };
  const l = Li(0);
  function u() {
    l.x = Iu(r);
  }
  if (n(u, "setLeftRTLScrollbarOffset"), i || !i && !a)
    if ((ml(t) !== "body" || Jd(r)) && (c = Fu(t)), i) {
      const f = js(t, !0, a, t);
      l.x = f.x + t.clientLeft, l.y = f.y + t.clientTop;
    } else r && u();
  a && !i && r && u();
  const b = r && !i && !a ? W1(r, c) : Li(0), C = o.left + c.scrollLeft - l.x - b.x, v = o.top + c.scrollTop - l.y - b.y;
  return {
    x: C,
    y: v,
    width: o.width,
    height: o.height
  };
}
n(n3, "getRectRelativeToOffsetParent");
function Gp(s) {
  return fi(s).position === "static";
}
n(Gp, "isStaticPositioned");
function Km(s, t) {
  if (!Pr(s) || fi(s).position === "fixed")
    return null;
  if (t)
    return t(s);
  let e = s.offsetParent;
  return Bi(s) === e && (e = e.ownerDocument.body), e;
}
n(Km, "getTrueOffsetParent");
function K1(s, t) {
  const e = Ge(s);
  if (zu(s))
    return e;
  if (!Pr(s)) {
    let r = Yr(s);
    for (; r && !tl(r); ) {
      if (wi(r) && !Gp(r))
        return r;
      r = Yr(r);
    }
    return e;
  }
  let i = Km(s, t);
  for (; i && W4(i) && Gp(i); )
    i = Km(i, t);
  return i && tl(i) && Gp(i) && !Vw(i) ? e : i || Q4(s) || e;
}
n(K1, "getOffsetParent");
const a3 = /* @__PURE__ */ n(async function(s) {
  const t = this.getOffsetParent || K1, e = this.getDimensions, i = await e(s.floating);
  return {
    reference: n3(s.reference, await t(s.floating), s.strategy),
    floating: {
      x: 0,
      y: 0,
      width: i.width,
      height: i.height
    }
  };
}, "getElementRects");
function o3(s) {
  return fi(s).direction === "rtl";
}
n(o3, "isRTL");
const c3 = {
  convertOffsetParentRelativeRectToViewportRelativeRect: Y4,
  getDocumentElement: Bi,
  getClippingRect: r3,
  getOffsetParent: K1,
  getElementRects: a3,
  getClientRects: X4,
  getDimensions: s3,
  getScale: Un,
  isElement: wi,
  isRTL: o3
};
function Q1(s, t) {
  return s.x === t.x && s.y === t.y && s.width === t.width && s.height === t.height;
}
n(Q1, "rectsAreEqual");
function l3(s, t) {
  let e = null, i;
  const r = Bi(s);
  function a() {
    var c;
    clearTimeout(i), (c = e) == null || c.disconnect(), e = null;
  }
  n(a, "cleanup");
  function o(c, l) {
    c === void 0 && (c = !1), l === void 0 && (l = 1), a();
    const u = s.getBoundingClientRect(), {
      left: b,
      top: C,
      width: v,
      height: f
    } = u;
    if (c || t(), !v || !f)
      return;
    const m = Ch(C), $ = Ch(r.clientWidth - (b + v)), E = Ch(r.clientHeight - (C + f)), M = Ch(b), H = {
      rootMargin: -m + "px " + -$ + "px " + -E + "px " + -M + "px",
      threshold: Is(0, su(1, l)) || 1
    };
    let D = !0;
    function I(B) {
      const Q = B[0].intersectionRatio;
      if (Q !== l) {
        if (!D)
          return o();
        Q ? o(!1, Q) : i = setTimeout(() => {
          o(!1, 1e-7);
        }, 1e3);
      }
      Q === 1 && !Q1(u, s.getBoundingClientRect()) && o(), D = !1;
    }
    n(I, "handleObserve");
    try {
      e = new IntersectionObserver(I, {
        ...H,
        // Handle <iframe>s
        root: r.ownerDocument
      });
    } catch {
      e = new IntersectionObserver(I, H);
    }
    e.observe(s);
  }
  return n(o, "refresh"), o(!0), a;
}
n(l3, "observeMove");
function G1(s, t, e, i) {
  i === void 0 && (i = {});
  const {
    ancestorScroll: r = !0,
    ancestorResize: a = !0,
    elementResize: o = typeof ResizeObserver == "function",
    layoutShift: c = typeof IntersectionObserver == "function",
    animationFrame: l = !1
  } = i, u = Uw(s), b = r || a ? [...u ? td(u) : [], ...t ? td(t) : []] : [];
  b.forEach((M) => {
    r && M.addEventListener("scroll", e, {
      passive: !0
    }), a && M.addEventListener("resize", e);
  });
  const C = u && c ? l3(u, e) : null;
  let v = -1, f = null;
  o && (f = new ResizeObserver((M) => {
    let [A] = M;
    A && A.target === u && f && t && (f.unobserve(t), cancelAnimationFrame(v), v = requestAnimationFrame(() => {
      var H;
      (H = f) == null || H.observe(t);
    })), e();
  }), u && !l && f.observe(u), t && f.observe(t));
  let m, $ = l ? js(s) : null;
  l && E();
  function E() {
    const M = js(s);
    $ && !Q1($, M) && e(), $ = M, m = requestAnimationFrame(E);
  }
  return n(E, "frameLoop"), e(), () => {
    var M;
    b.forEach((A) => {
      r && A.removeEventListener("scroll", e), a && A.removeEventListener("resize", e);
    }), C?.(), (M = f) == null || M.disconnect(), f = null, l && cancelAnimationFrame(m);
  };
}
n(G1, "autoUpdate");
const Z1 = N4, Y1 = q4, X1 = H4, J1 = /* @__PURE__ */ n((s, t, e) => {
  const i = /* @__PURE__ */ new Map(), r = {
    platform: c3,
    ...e
  }, a = {
    ...r.platform,
    _c: i
  };
  return V4(s, t, {
    ...r,
    platform: a
  });
}, "computePosition"), Du = j`[part~=wy-dropdown-menu]{width:max-content;position:absolute;z-index:1000;min-width:calc(10 * var(--wy-size, 1rem));border:none;padding:0;--wy-component-background-color: var(--wy-surface-container-lowest, var(--wy-surface-container-lowest-light, #ffffff));--wy-component-color: var(--wy-on-surface, var(--wy-on-surface-light, #181c20));background-color:var(--wy-component-background-color);color:var(--wy-component-color);font-size:var(--wy-font-size, var(--wy-size, 1em));text-align:start;background-clip:padding-box;border-radius:var(--wy-dropdown-border-radius, var(--wy-border-radius, calc(.5 * var(--wy-size, 1rem))));box-shadow:var(--wy-dropdown-shadow, var(--wy-shadow-level1, 0 calc(.125 * var(--wy-size, 1rem)) calc(.125 * var(--wy-size, 1rem)) 0 rgba(0, 0, 0, .14), 0 calc(.1875 * var(--wy-size, 1rem)) calc(.0625 * var(--wy-size, 1rem)) calc(-.125 * var(--wy-size, 1rem)) rgba(0, 0, 0, .2), 0 calc(.0625 * var(--wy-size, 1rem)) calc(.3125 * var(--wy-size, 1rem)) 0 rgba(0, 0, 0, .12)));overflow:hidden}[part~=wy-dropdown-menu]:not([popover]){display:block}[part~=wy-dropdown-item]{background-color:transparent;color:var(--wy-component-color, var(--wy-on-surface, var(--wy-on-surface-light, #181c20)));width:100%;min-height:calc(2.5 * var(--wy-size, 1rem));padding:var(--wy-dropdown-padding-y, var(--wy-padding, calc(.5 * var(--wy-size, 1rem)))) var(--wy-dropdown-padding-x, var(--wy-padding-lg, calc(1.5 * var(--wy-padding, calc(.5 * var(--wy-size, 1rem))))));clear:both;font-family:var(--wy-font-family, unset),inherit;font-size:inherit;font-weight:var(--wy-font-weight, unset);line-height:1;text-align:inherit;text-decoration:none;white-space:nowrap;border:0;gap:var(--wy-dropdown-gap, var(--wy-gap, calc(.5 * var(--wy-size, 1rem))));cursor:pointer;display:flex;align-items:center}[part~=wy-dropdown-item]:hover,[part~=wy-dropdown-item]:focus{--wy-component-color: var(--wy-on-surface-variant, var(--wy-on-surface-variant-light, #42474e));--wy-component-background-color: var(--wy-surface-variant, var(--wy-surface-variant-light, #dee3eb));color:var(--wy-component-color);background-color:var(--wy-component-background-color);text-decoration:none}[part~=wy-dropdown-item][part~=wy-active],[part~=wy-dropdown-item]:active{--wy-component-color: var(--wy-on-primary-container, var(--wy-on-primary-container-light, #0d4a73));--wy-component-background-color: var(--wy-primary-container, var(--wy-primary-container-light, #cee5ff));color:var(--wy-component-color);background-color:var(--wy-component-background-color);text-decoration:none}[part~=wy-dropdown-item][part~=wy-disabled],[part~=wy-dropdown-item]:disabled{color:var(--wy-component-color);background-color:var(--wy-component-background-color);pointer-events:none;opacity:var(--wy-opacity-disabled, 38%)}[part~=wy-dropdown-item]>img,[part~=wy-dropdown-item]>wy-icon{margin-right:calc(.5 * var(--wy-size, 1rem));color:inherit}[part~=wy-dropdown-item]:active>wy-icon{color:inherit}[part~=wy-dropdown-item][part~=wy-dropdown-option] wy-icon{visibility:hidden}[part~=wy-dropdown-item][part~=wy-dropdown-option][part~=wy-selected] wy-icon{visibility:visible}[part~=wy-dropdown-divider]{height:0;margin:0;overflow:hidden;border:none;border-top:var(--wy-dropdown-border-width, var(--wy-border, 1px)) solid var(--wy-outline-variant, var(--wy-outline-variant-light, #c2c7cf))}`;
var d3 = Object.defineProperty, h3 = Object.getOwnPropertyDescriptor, Re = /* @__PURE__ */ n((s, t, e, i) => {
  for (var r = i > 1 ? void 0 : i ? h3(t, e) : t, a = s.length - 1, o; a >= 0; a--)
    (o = s[a]) && (r = (i ? o(t, e, r) : o(r)) || r);
  return i && r && d3(t, e, r), r;
}, "__decorateClass$18"), po;
let Xe = (po = class extends nt {
  constructor() {
    super(), this.exportParts = new W(this), this.directionX = "right", this.directionY = "down", this.icon = "dots-vertical", this.small = !1, this.disabled = !1, this._placement = "bottom-start", this.showMenu = !1, this.buttonRef = J(), this.menuRef = J(), this._documentClickHandler = (t) => {
      this.showMenu && (t.preventDefault(), this.menuRef.value?.popover || (this.showMenu = !1));
    }, this.addEventListener("click", (t) => {
      t.preventDefault();
    });
  }
  handleClose(t) {
    (t.type === "toggle" && t.newState === "closed" || t.type === "click") && (this.showMenu = !1, this.dispatchEvent(new CustomEvent("close")));
  }
  handleClickToggle(t) {
    t.stopPropagation(), this.showMenu = this.disabled ? !1 : !this.showMenu;
  }
  willUpdate(t) {
    if (super.willUpdate(t), (t.has("disabled") || t.has("showMenu")) && this.disabled && this.showMenu && (this.showMenu = !1), (t.has("directionX") || t.has("directionY")) && (this._placement = this.directionX === "right" && this.directionY === "down" ? "bottom-start" : this.directionX === "left" && this.directionY === "down" ? "bottom-end" : this.directionX === "right" && this.directionY === "up" ? "top-start" : "top-end"), t.has("showMenu") && (this.showMenu && !this.computePositionCleanup && this.buttonRef.value && this.menuRef.value ? this.computePositionCleanup = G1(this.buttonRef.value, this.menuRef.value, () => {
      if (this.buttonRef.value && this.menuRef.value) {
        const e = !this.menuRef.value.popover, i = e || !o0(this.buttonRef.value);
        J1(this.buttonRef.value, this.menuRef.value, {
          placement: this._placement,
          strategy: e ? "fixed" : "absolute",
          middleware: [
            X1(),
            Z1(({ placement: r }) => r.includes("top") ? 9 : 13),
            Y1({ mainAxis: !0, crossAxis: !0, padding: 4, altBoundary: i })
          ]
        }).then(({ x: r, y: a }) => {
          this.menuRef.value && Object.assign(this.menuRef.value.style, {
            marginLeft: `${r}px`,
            marginTop: `${a}px`,
            top: 0,
            left: 0,
            position: e ? "fixed" : void 0,
            zIndex: e ? 1075 : void 0
          });
        });
      }
    }) : !this.showMenu && this.computePositionCleanup && (this.computePositionCleanup?.(), this.computePositionCleanup = void 0)), this.showMenu) {
      requestAnimationFrame(() => {
        document.addEventListener("click", this._documentClickHandler, { once: !0, capture: !0 });
      });
      try {
        this.menuRef.value?.showPopover();
      } catch {
      }
    } else
      try {
        this.menuRef.value?.hidePopover();
      } catch {
      }
  }
  render() {
    const t = this._slotButton.length === 0 || this._slotButton.length === 1 && this._slotButton[0] instanceof Wt;
    return h`
      <span part="wy-dropdown">
        <span
          ${K(this.buttonRef)}
          part="wy-dropdown-button-container"
          @click=${(e) => this.handleClickToggle(e)}
          @keydown=${ne}
          @keyup=${ke}
        >
          <wy-button
            part="wy-dropdown-button"
            .kind=${t ? "icon" : void 0}
            ?small=${this.small}
            title=${this.title}
            ?active=${this.showMenu}
            ?disabled=${this.disabled}
          >
            <slot name="button-content" @slotchange=${() => this.requestUpdate()}>
              <wy-icon part="wy-dropdown-button-icon" name=${this.icon}></wy-icon>
            </slot>
          </wy-button>
        </span>

        <div
          ${K(this.menuRef)}
          part="wy-dropdown-menu"
          @click=${(e) => this.handleClickToggle(e)}
          @keyup=${Lw}
          ?hidden=${Vn() && !this.showMenu}
          popover=${ot(Vn() ? void 0 : "auto")}
        >
          <slot></slot>
        </div>
      </span>
    `;
  }
  firstUpdated(t) {
    this.menuRef.value?.addEventListener(
      this.menuRef.value.popover ? "toggle" : "click",
      (e) => this.handleClose(e)
    );
  }
  disconnectedCallback() {
    this.computePositionCleanup?.(), super.disconnectedCallback();
  }
}, n(po, "WyDropdown"), po);
Xe.styles = [Lt, Du];
Re([
  y()
], Xe.prototype, "directionX", 2);
Re([
  y()
], Xe.prototype, "directionY", 2);
Re([
  y()
], Xe.prototype, "icon", 2);
Re([
  y({ type: Boolean })
], Xe.prototype, "small", 2);
Re([
  y({ type: Boolean })
], Xe.prototype, "disabled", 2);
Re([
  V()
], Xe.prototype, "_placement", 2);
Re([
  V()
], Xe.prototype, "showMenu", 2);
Re([
  yl({ slot: "button-content" })
], Xe.prototype, "_slotButton", 2);
Re([
  V()
], Xe.prototype, "computePositionCleanup", 2);
Xe = Re([
  U("wy-dropdown")
], Xe);
var yo;
let ed = (yo = class extends nt {
  constructor() {
    super(...arguments), this.exportParts = new W(this), this.active = !1;
  }
  render() {
    return h`<div part="wy-dropdown-item ${tt({ "wy-active": this.active })}" tabindex="0"><slot></slot></div>`;
  }
}, n(yo, "WyDropdownItem"), yo);
ed.styles = [Lt, Du, Y];
Re([
  y({ type: Boolean })
], ed.prototype, "active", 2);
ed = Re([
  U("wy-dropdown-item")
], ed);
var wo;
let el = (wo = class extends nt {
  constructor() {
    super(...arguments), this.exportParts = new W(this), this.active = !1, this.selected = !1;
  }
  render() {
    const t = {
      visibility: this.selected ? null : "hidden"
    };
    return h`
      <div
        part="wy-dropdown-item wy-dropdown-option ${tt({
      "wy-active": this.active,
      "wy-selected": this.selected
    })}"
        tabindex="0"
      >
        <slot name="icon" style=${Jl(t)}
          ><wy-icon part="wy-dropdown-option-icon" name="check"></wy-icon
        ></slot>
        <slot></slot>
      </div>
    `;
  }
}, n(wo, "WyDropdownOption"), wo);
el.styles = [Lt, Du, Y];
Re([
  y({ type: Boolean })
], el.prototype, "active", 2);
Re([
  y({ type: Boolean })
], el.prototype, "selected", 2);
el = Re([
  U("wy-dropdown-option")
], el);
var fo;
let cu = (fo = class extends nt {
  constructor() {
    super(...arguments), this.exportParts = new W(this);
  }
  render() {
    return h`<hr part="wy-dropdown-divider" />`;
  }
}, n(fo, "WyDropdownDivider"), fo);
cu.styles = [Lt, Du, Y];
cu = Re([
  U("wy-dropdown-divider")
], cu);
const tg = j`[part~=wy-placeholder]{cursor:wait;color:var(--wy-outline-variant, var(--wy-outline-variant-light, #c2c7cf));background:var(--wy-outline-variant, var(--wy-outline-variant-light, #c2c7cf));opacity:.5;animation:wy-placeholder-glow 2s ease-in-out infinite;line-height:1.45}@keyframes wy-placeholder-glow{50%{opacity:.2}}`;
var u3 = Object.defineProperty, p3 = Object.getOwnPropertyDescriptor, eg = /* @__PURE__ */ n((s, t, e, i) => {
  for (var r = i > 1 ? void 0 : i ? p3(t, e) : t, a = s.length - 1, o; a >= 0; a--)
    (o = s[a]) && (r = (i ? o(t, e, r) : o(r)) || r);
  return i && r && u3(t, e, r), r;
}, "__decorateClass$17"), mo;
let id = (mo = class extends nt {
  constructor() {
    super(...arguments), this.exportParts = new W(this), this.text = "";
  }
  render() {
    const t = this.text.trim();
    return h`
      <div part="wy-skeleton">
        ${t.length ? t.split(/(\n+)/).map((e) => {
      const i = e.split(/(\s+)/);
      return h`
                <div part="wy-placeholder-line">
                  ${i.map((r) => h` <span part="wy-placeholder">${r}</span> `)}
                </div>
              `;
    }) : g}
      </div>
    `;
  }
}, n(mo, "WySkeleton"), mo);
id.styles = [tg, Y];
eg([
  y()
], id.prototype, "text", 2);
id = eg([
  U("wy-skeleton")
], id);
function ig(s) {
  const t = [s];
  for (; s && s.parent; )
    s = s.parent, t.push(s);
  return t;
}
n(ig, "getEntityChain");
function rg(s, t, e) {
  return s.type === t && (!e || s.id === e.id);
}
n(rg, "isEntityMatch");
function Vu(s, t, e) {
  return ig(s).some((r) => r && rg(r, t, e));
}
n(Vu, "isEntityChainMatch");
function sg(s, t, e) {
  return ig(s).find((r) => r && rg(r, t, e));
}
n(sg, "getEntityChainMatch");
function ng(s, t, e, i) {
  const r = sg(s, i);
  return r && r.parent ? Vu(r.parent, t, e) : !1;
}
n(ng, "hasEntityChildType");
async function ag(s, t, e) {
  let i;
  if (t && e?.link?.app?.id) {
    const a = await t.fetch(`/api/apps/${e.link.app.id}`);
    a.ok && (i = (await a.json()).metadata);
  }
  const r = new CustomEvent("wy-link", {
    bubbles: !0,
    composed: !0,
    cancelable: !0,
    detail: {
      link: e.link ? {
        ...e?.link,
        // HACK: pass along the agent uid so uikit can match the correct agent chat     
        agent: e.actor.is_agent && e.link?.app?.type && Jh.has(e.link.app.type) ? e.actor.uid : void 0
      } : void 0,
      metadata: e.metadata,
      app_type: e?.link?.app?.type && v2.get(e.link.app.type) || Ti,
      source_name: i?.source_name,
      source_url: i?.source_url,
      source_data: i?.source_data
    }
  });
  return s.dispatchEvent(r);
}
n(ag, "dispatchLinkEvent");
function og(s) {
  const t = s.args;
  switch (s.template) {
    case "**{0}** added *{1}* to **{2}**": {
      const [e, i, r] = t;
      return {
        title: x(gt`${e} added ${i} to ${r}`),
        titleHtml: x(h`<strong>${e}</strong> added <em>${i}</em> to <strong>${r}</strong>`)
      };
    }
    case "**{0}** edited *{1}*": {
      const [e, i] = t;
      return {
        title: x(gt`${e} edited ${i}`),
        titleHtml: x(h`<strong>${e}</strong> edited <em>${i}</em>`)
      };
    }
    case '**{0}** commented on *{1}*: "{2}"': {
      const [e, i, r] = t;
      return {
        title: x(gt`${e} commented on ${i}`),
        titleHtml: x(h`<strong>${e}</strong> commented on <em>${i}</em>`),
        detail: r
      };
    }
    case '**{0}** commented on **{1}**: "{2}"': {
      const [e, i, r] = t;
      return {
        title: x(gt`${e} commented on ${i}`),
        titleHtml: x(h`<strong>${e}</strong> commented on <strong>${i}</strong>`),
        detail: r
      };
    }
    case '**{0}** replied to your post: "{1}"': {
      const [e, i] = t;
      return {
        title: x(gt`${e} replied to your post`),
        titleHtml: x(h`<strong>${e}</strong> replied to your post`),
        detail: i
      };
    }
    case '**{0}** replied to a post: "{1}"': {
      const [e, i] = t;
      return {
        title: x(gt`${e} replied to a post`),
        titleHtml: x(h`<strong>${e}</strong> replied to a post`),
        detail: i
      };
    }
    case '**{0}** mentioned you in a comment: "{1}"': {
      const [e, i] = t;
      return {
        title: x(gt`${e} mentioned you in a comment`),
        titleHtml: x(h`<strong>${e}</strong> mentioned you in a comment`),
        detail: i
      };
    }
    case '**{0}** posted in **{1}**: "{2}"': {
      const [e, i, r] = t;
      return {
        title: x(gt`${e} posted in ${i}`),
        titleHtml: x(h`<strong>${e}</strong> posted in <strong>${i}</strong>`),
        detail: r
      };
    }
    case '**{0}** mentioned you in a post: "{1}"': {
      const [e, i] = t;
      return {
        title: x(gt`${e} mentioned you in a post`),
        titleHtml: x(h`<strong>${e}</strong> mentioned you in a post`),
        detail: i
      };
    }
    case 'New message from **{0}**: "{1}"': {
      const [e, i] = t;
      return {
        title: x(gt`New message from ${e}`),
        titleHtml: x(h`New message from <strong>${e}</strong>`),
        detail: i
      };
    }
    case '**{0}** sent a message in **{1}**: "{2}"': {
      const [e, i, r] = t;
      return {
        title: x(gt`${e} sent a message in ${i}`),
        titleHtml: x(h`<strong>${e}</strong> sent a message in <strong>${i}</strong>`),
        detail: r
      };
    }
    case '**{0}** mentioned you in a message: "{1}"': {
      const [e, i] = t;
      return {
        title: x(gt`${e} mentioned you in a message`),
        titleHtml: x(h`<strong>${e}</strong> mentioned you in a message`),
        detail: i
      };
    }
    case '**{0}** edited a post: "{1}"': {
      const [e, i] = t;
      return {
        title: x(gt`${e} edited a post`),
        titleHtml: x(h`<strong>${e}</strong> edited a post`),
        detail: i
      };
    }
    case "**{0}** liked *{1}*": {
      const [e, i] = t;
      return {
        title: x(gt`${e} liked ${i}`),
        titleHtml: x(h`<strong>${e}</strong> liked <em>${i}</em>`)
      };
    }
    case "**{0}** reacted {2} to *{1}*": {
      const [e, i, r] = t;
      return {
        title: x(gt`${e} reacted ${i} to ${r}`),
        titleHtml: x(h`<strong>${e}</strong> reacted ${i} to <em>${r}</em>`)
      };
    }
    case '**{0}** liked your comment: "{1}"': {
      const [e, i] = t;
      return {
        title: x(gt`${e} liked your comment`),
        titleHtml: x(h`<strong>${e}</strong> liked your comment`),
        detail: i
      };
    }
    case '**{0}** reacted {1} to your comment: "{2}"': {
      const [e, i, r] = t;
      return {
        title: x(gt`${e} reacted ${i} to your comment`),
        titleHtml: x(h`<strong>${e}</strong> reacted ${i} to your comment`),
        detail: r
      };
    }
    case '**{0}** liked your message: "{1}"': {
      const [e, i] = t;
      return {
        title: x(gt`${e} liked your message`),
        titleHtml: x(h`<strong>${e}</strong> liked your message`),
        detail: i
      };
    }
    case '**{0}** reacted {1} to your message: "{2}"': {
      const [e, i, r] = t;
      return {
        title: x(gt`${e} reacted ${i} to your message`),
        titleHtml: x(h`<strong>${e}</strong> reacted ${i} to your message`),
        detail: r
      };
    }
    case '**{0}** liked your post: "{1}"': {
      const [e, i] = t;
      return {
        title: x(gt`${e} liked your post`),
        titleHtml: x(h`<strong>${e}</strong> liked your post`),
        detail: i
      };
    }
    case '**{0}** reacted {1} to your post: "{2}"': {
      const [e, i, r] = t;
      return {
        title: x(gt`${e} reacted ${i} to your post`),
        titleHtml: x(h`<strong>${e}</strong> reacted ${i} to your post`),
        detail: r
      };
    }
    case "**{0}** voted on your poll": {
      const [e] = t;
      return {
        title: x(gt`${e} voted on your poll`),
        titleHtml: x(h`<strong>${e}</strong> voted on your poll`)
      };
    }
    case "**{0}** and **{1}** voted on your poll": {
      const [e, i] = t;
      return {
        title: x(gt`${e} and ${i} voted on your poll`),
        titleHtml: x(h`<strong>${e}</strong> and <strong>${i}</strong> voted on your poll`)
      };
    }
    case "**{0}**, **{1}** and **{2}** voted on your poll": {
      const [e, i, r] = t;
      return {
        title: x(gt`${e}, ${i} and ${r} voted on your poll`),
        titleHtml: x(
          h`<strong>${e}</strong>, <strong>${i}</strong> and <strong>${r}</strong> voted on your poll`
        )
      };
    }
    case "**{0}** and {1} others voted on your poll": {
      const [e, i] = t;
      return {
        title: x(gt`${e} and ${i} others voted on your poll`),
        titleHtml: x(h`<strong>${e}</strong> and ${i} others voted on your poll`)
      };
    }
    default:
      return {
        title: s.plain,
        titleHtml: h`${Bs(s.html)}`
      };
  }
}
n(og, "getNotificationText");
function Bw(s, t = me.Preview) {
  const e = new CustomEvent("wy-action", {
    detail: {
      action: t,
      user: s
    },
    composed: !0,
    bubbles: !0,
    cancelable: !0
  });
  this.dispatchEvent(e);
}
n(Bw, "dispatchUserAction");
const Nw = j`[part~=wy-code] pre,.wy-code pre,[part~=wy-content] pre{text-wrap:pretty}[part~=wy-code] .code,[part~=wy-code] code[class*=language-],[part~=wy-code] pre[class*=language-],.wy-code .code,.wy-code code[class*=language-],.wy-code pre[class*=language-],[part~=wy-content] .code,[part~=wy-content] code[class*=language-],[part~=wy-content] pre[class*=language-]{color:var(--wy-code-text, var(--wy-code-text-light, #3b3b3b));font-family:var(--wy-font-monospace, ui-monospace, SFMono-Regular, SF Mono, Menlo, Consolas, Liberation Mono, monospace);text-align:start;white-space:pre-wrap;word-spacing:normal;word-break:normal;word-wrap:break-word;line-height:1.25;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-hyphens:none;-moz-hyphens:none;-ms-hyphens:none;hyphens:none}@media print{[part~=wy-code] code[class*=language-],[part~=wy-code] pre[class*=language-],.wy-code code[class*=language-],.wy-code pre[class*=language-],[part~=wy-content] code[class*=language-],[part~=wy-content] pre[class*=language-]{text-shadow:none}}[part~=wy-code] pre[class*=language-],.wy-code pre[class*=language-],[part~=wy-content] pre[class*=language-]{padding:1em;margin:.5em 0;overflow:auto}[part~=wy-code] :not(pre)>code[class*=language-],[part~=wy-code] pre[class*=language-],.wy-code :not(pre)>code[class*=language-],.wy-code pre[class*=language-],[part~=wy-content] :not(pre)>code[class*=language-],[part~=wy-content] pre[class*=language-]{color:var(--wy-code-text, var(--wy-code-text-light, #3b3b3b));background:var(--wy-background, var(--wy-background-light, #f7f9ff))}[part~=wy-code] :not(pre)>code[class*=language-],.wy-code :not(pre)>code[class*=language-],[part~=wy-content] :not(pre)>code[class*=language-]{padding:.1em;border-radius:.3em;white-space:normal}[part~=wy-code] .token.variable,.wy-code .token.variable,[part~=wy-content] .token.variable{color:var(--wy-code-variable, var(--wy-code-variable-light, #001080))}[part~=wy-code] .token.operator,.wy-code .token.operator,[part~=wy-content] .token.operator{color:var(--wy-code-operator, var(--wy-code-operator-light, #000000))}[part~=wy-code] .token.prolog,.wy-code .token.prolog,[part~=wy-content] .token.prolog{color:var(--wy-code-prolog, var(--wy-code-prolog-light, #000080))}[part~=wy-code] .token.comment,.wy-code .token.comment,[part~=wy-content] .token.comment{color:var(--wy-code-comment, var(--wy-code-comment-light, #008000))}[part~=wy-code] .token.builtin,.wy-code .token.builtin,[part~=wy-content] .token.builtin{color:var(--wy-code-builtin, var(--wy-code-builtin-light, #0070C1))}[part~=wy-code] .token.number,.wy-code .token.number,[part~=wy-content] .token.number{color:var(--wy-code-number, var(--wy-code-number-light, #098658))}[part~=wy-code] .token.inserted,.wy-code .token.inserted,[part~=wy-content] .token.inserted{color:var(--wy-code-inserted, var(--wy-code-inserted-light, #098658))}[part~=wy-code] .token.constant,.wy-code .token.constant,[part~=wy-content] .token.constant{color:var(--wy-code-constant, var(--wy-code-constant-light, #811F3F))}[part~=wy-code] .token.hexcode,.wy-code .token.hexcode,[part~=wy-content] .token.hexcode{color:var(--wy-code-hexcode, var(--wy-code-hexcode-light, #811F3F))}[part~=wy-code] .token.regex,.wy-code .token.regex,[part~=wy-content] .token.regex{color:var(--wy-code-regex, var(--wy-code-regex-light, #811F3F))}[part~=wy-code] .token.char,.wy-code .token.char,[part~=wy-content] .token.char{color:var(--wy-code-char, var(--wy-code-char-light, #811F3F))}[part~=wy-code] .token.tag,.wy-code .token.tag,[part~=wy-content] .token.tag{color:var(--wy-code-tag, var(--wy-code-tag-light, #800000))}[part~=wy-code] .token.attr-name,.wy-code .token.attr-name,[part~=wy-content] .token.attr-name{color:var(--wy-code-attr-name, var(--wy-code-attr-name-light, #E50000))}[part~=wy-code] .token.selector,.wy-code .token.selector,[part~=wy-content] .token.selector{color:var(--wy-code-selector, var(--wy-code-selector-light, #E50000))}[part~=wy-code] .token.property,.wy-code .token.property,[part~=wy-content] .token.property{color:var(--wy-code-property, var(--wy-code-property-light, #E50000))}[part~=wy-code] .token.deleted,.wy-code .token.deleted,[part~=wy-content] .token.deleted{color:var(--wy-code-deleted, var(--wy-code-deleted-light, #A31515))}[part~=wy-code] .token.string,.wy-code .token.string,[part~=wy-content] .token.string{color:var(--wy-code-string, var(--wy-code-string-light, #A31515))}[part~=wy-code] .token.changed,.wy-code .token.changed,[part~=wy-content] .token.changed{color:var(--wy-code-changed, var(--wy-code-changed-light, #0451A5))}[part~=wy-code] .token.punctuation,.wy-code .token.punctuation,[part~=wy-content] .token.punctuation{color:var(--wy-code-punctuation, var(--wy-code-punctuation-light, #000000))}[part~=wy-code] .token.function,.wy-code .token.function,[part~=wy-content] .token.function{color:var(--wy-code-function, var(--wy-code-function-light, #0000FF))}[part~=wy-code] .token.keyword,.wy-code .token.keyword,[part~=wy-content] .token.keyword{color:var(--wy-code-keyword, var(--wy-code-keyword-light, #AF00DB))}[part~=wy-code] .token.class-name,.wy-code .token.class-name,[part~=wy-content] .token.class-name{color:var(--wy-code-class-name, var(--wy-code-class-name-light, #267F99))}[part~=wy-code] .token.important,[part~=wy-code] .token.bold,.wy-code .token.important,.wy-code .token.bold,[part~=wy-content] .token.important,[part~=wy-content] .token.bold{font-weight:700}[part~=wy-code] .token.italic,.wy-code .token.italic,[part~=wy-content] .token.italic{font-style:italic}[part~=wy-content]{text-align:initial;font-size:var(--wy-font-size, var(--wy-size, 1em));overflow-wrap:break-word;min-width:0}[part~=wy-content]:empty{display:none}[part~=wy-content]>:first-child{margin-top:0}[part~=wy-content]>:last-child{margin-bottom:0}[part~=wy-content] a{text-decoration:var(--wy-link-decoration, none);color:var(--wy-link, var(--wy-primary-light, #2f628c))}[part~=wy-content] a:hover{text-decoration:var(--wy-link-hover-decoration, var(--wy-link-decoration, none))}[part~=wy-content] a:where(:not([href]):not([class])),[part~=wy-content] a:where(:not([href]):not([class])):hover{text-decoration:none}[part~=wy-content] p{margin-top:0;margin-bottom:calc(1 * var(--wy-size, 1rem))}[part~=wy-content] b,[part~=wy-content] strong{font-weight:var(--wy-font-weight-bold, 600)}[part~=wy-content] blockquote{border-left:calc(2 * var(--wy-border, 1px)) solid var(--wy-outline-variant, var(--wy-outline-variant-light, #c2c7cf));margin:var(--wy-gap, var(--wy-padding, calc(.5 * var(--wy-size, 1rem)))) 0;padding-left:var(--wy-padding, calc(.5 * var(--wy-size, 1rem)))}[part~=wy-content] blockquote>p{margin:0}[part~=wy-content] hr{border-color:var(--wy-outline-variant, var(--wy-outline-variant-light, #c2c7cf));border-width:var(--wy-border, 1px) 0 0 0;border-style:solid}[part~=wy-content] pre,[part~=wy-content] code{font-family:var(--wy-font-monospace, ui-monospace, SFMono-Regular, SF Mono, Menlo, Consolas, Liberation Mono, monospace);font-size:var(--wy-font-size-sm, calc(.875 * var(--wy-font-size, 1em)))}[part~=wy-content] :not(pre)>code{overflow-wrap:inherit}[part~=wy-content] pre>code{font-size:var(--wy-font-size, var(--wy-size, 1em));white-space:pre-wrap}[part~=wy-content] h1,[part~=wy-content] h2,[part~=wy-content] h3,[part~=wy-content] h4,[part~=wy-content] h5,[part~=wy-content] h6{font-family:var(--wy-headings-font-family, var(--wy-font-family, unset));line-height:var(--wy-headings-line-height, var(--wy-line-height, 1.5))}[part~=wy-content] h1{font-size:var(--wy-h1-font-size, var(--wy-font-size-xxl, 1.5em));font-weight:var(--wy-h1-font-weight, var(--wy-headings-font-weight, var(--wy-font-weight-bold, 600)));margin:var(--wy-h1-margin, 0 0 var(--wy-gap, calc(.5 * var(--wy-size, 1rem))))}[part~=wy-content] h2{font-size:var(--wy-h2-font-size, var(--wy-font-size-xl, 1.25em));font-weight:var(--wy-h2-font-weight, var(--wy-headings-font-weight, var(--wy-font-weight-bold, 600)));margin:var(--wy-h2-margin, var(--wy-gap-xl, calc(2 * var(--wy-gap, calc(.5 * var(--wy-size, 1rem))))) 0 var(--wy-gap-sm, calc(.5 * var(--wy-gap, calc(.5 * var(--wy-size, 1rem))))))}[part~=wy-content] h3{font-size:var(--wy-h3-font-size, var(--wy-font-size-lg, 1.125em));font-weight:var(--wy-h3-font-weight, var(--wy-headings-font-weight, var(--wy-font-weight-bold, 600)));margin:var(--wy-h3-margin, var(--wy-gap-xl, calc(2 * var(--wy-gap, calc(.5 * var(--wy-size, 1rem))))) 0 var(--wy-gap-sm, calc(.5 * var(--wy-gap, calc(.5 * var(--wy-size, 1rem))))))}[part~=wy-content] h4{font-size:var(--wy-h4-font-size, var(--wy-font-size, 1em));font-weight:var(--wy-h4-font-weight, var(--wy-headings-font-weight, var(--wy-font-weight-bold, 600)));margin:var(--wy-h4-margin, var(--wy-gap-xl, calc(2 * var(--wy-gap, calc(.5 * var(--wy-size, 1rem))))) 0 0)}[part~=wy-content] h5{font-size:var(--wy-h5-font-size, var(--wy-font-size, 1em));font-weight:var(--wy-h5-font-weight, var(--wy-headings-font-weight, var(--wy-font-weight-bold, 600)));margin:var(--wy-h5-margin, 0)}[part~=wy-content] h6{font-size:var(--wy-h6-font-size, var(--wy-font-size, 1em));font-weight:var(--wy-h6-font-weight, inherit);margin:var(--wy-h6-margin, 0)}[part~=wy-content] table{display:block;width:100%;max-width:-moz-max-content;max-width:max-content;overflow-x:auto;white-space:nowrap;border-collapse:separate;border-spacing:0 0;margin-bottom:var(--wy-size, 1rem)}[part~=wy-content] table th,[part~=wy-content] table td{padding:var(--wy-padding-sm, calc(.75 * var(--wy-padding, calc(.5 * var(--wy-size, 1rem))))) var(--wy-padding, calc(.5 * var(--wy-size, 1rem)));border-color:inherit;border-style:solid}[part~=wy-content] table th{text-align:inherit;text-align:-webkit-match-parent;border-width:var(--wy-border, 1px) 0 var(--wy-border, 1px) var(--wy-border, 1px);font-weight:var(--wy-headings-font-weight, var(--wy-font-weight-bold, 600))}[part~=wy-content] table th:first-child{border-top-left-radius:var(--wy-border-radius-sm, calc(var(--wy-border-radius, calc(.5 * var(--wy-size, 1rem))) - .25 * var(--wy-padding, calc(.5 * var(--wy-size, 1rem)))))}[part~=wy-content] table th:last-child{border-top-right-radius:var(--wy-border-radius-sm, calc(var(--wy-border-radius, calc(.5 * var(--wy-size, 1rem))) - .25 * var(--wy-padding, calc(.5 * var(--wy-size, 1rem)))));border-right-width:var(--wy-border, 1px)}[part~=wy-content] table td{border-width:0 0 var(--wy-border, 1px) var(--wy-border, 1px)}[part~=wy-content] table td:last-child{border-right-width:var(--wy-border, 1px)}[part~=wy-content] table tr:last-child td:first-child{border-bottom-left-radius:var(--wy-border-radius-sm, calc(var(--wy-border-radius, calc(.5 * var(--wy-size, 1rem))) - .25 * var(--wy-padding, calc(.5 * var(--wy-size, 1rem)))))}[part~=wy-content] table tr:last-child td:last-child{border-bottom-right-radius:var(--wy-border-radius-sm, calc(var(--wy-border-radius, calc(.5 * var(--wy-size, 1rem))) - .25 * var(--wy-padding, calc(.5 * var(--wy-size, 1rem)))))}[part~=wy-content] table>tbody{vertical-align:inherit}[part~=wy-content] table>thead{vertical-align:bottom}[part~=wy-content] img{max-width:100%;border-radius:var(--wy-border-radius-sm, calc(var(--wy-border-radius, calc(.5 * var(--wy-size, 1rem))) - .25 * var(--wy-padding, calc(.5 * var(--wy-size, 1rem)))));margin-block:var(--wy-padding-sm, calc(.75 * var(--wy-padding, calc(.5 * var(--wy-size, 1rem)))))}[part~=wy-content] ol{list-style-type:decimal}[part~=wy-content] ul{list-style-type:disc}[part~=wy-content] li{margin:var(--wy-gap-sm, calc(.5 * var(--wy-gap, var(--wy-padding, calc(.5 * var(--wy-size, 1rem)))))) 0}[part~=wy-content] ol,[part~=wy-content] ul{margin:var(--wy-size, 1rem) 0;padding:0 0 0 1.625em}[part~=wy-content] ol>li,[part~=wy-content] ul>li{padding-left:var(--wy-gap-sm, calc(.5 * var(--wy-gap, var(--wy-padding, calc(.5 * var(--wy-size, 1rem))))))}[part~=wy-content] ol>li>:first-child,[part~=wy-content] ul>li>:first-child{margin-bottom:0;margin-top:0}[part~=wy-content] ol>li>:last-child,[part~=wy-content] ul>li>:last-child{margin-bottom:0}[part~=wy-content] .wy-hashtag{color:var(--wy-link, var(--wy-primary-light, #2f628c))}[part~=wy-content] .wy-mention{font-weight:var(--wy-font-weight-bold, 600);cursor:pointer;color:var(--wy-link, var(--wy-primary-light, #2f628c));text-decoration:var(--wy-link-decoration, none)}[part~=wy-content] .wy-mention:before{content:\"@\"}[part~=wy-content] .wy-mention:hover,[part~=wy-content] .wy-mention:focus{text-decoration:var(--wy-link-hover-decoration, var(--wy-link-decoration, none))}[part~=wy-content-emoji] .wy-emoji{font-size:3em}`, y3 = j`[part~=wy-image]{padding:0;width:50%;position:relative;flex:auto;background-color:inherit;background-clip:content-box;max-height:var(--wy-image-max-size, calc(32 * var(--wy-size, 1rem)))}[part~=wy-image-filled]{position:relative;background-size:100%,100% 100%;background-position:center;background-clip:content-box;clip-path:content-box;align-items:center;justify-content:center}[part~=wy-image-filled]:after{content:\"\";position:absolute;top:0;left:0;width:100%;height:100%;z-index:1;-webkit-backdrop-filter:blur(32px);backdrop-filter:blur(32px);background-color:var(--wy-shade, var(--wy-shade-light, rgba(0, 0, 0, .3)))}[part~=wy-image-full-width]{flex-basis:100%!important}[part~=wy-image-grid]{display:flex;flex-wrap:wrap;justify-content:space-evenly;position:relative;border-radius:var(--wy-border-radius-sm, calc(var(--wy-border-radius, calc(.5 * var(--wy-size, 1rem))) - .25 * var(--wy-padding, calc(.5 * var(--wy-size, 1rem)))));overflow:hidden;gap:var(--wy-gap-sm, calc(.5 * var(--wy-gap, var(--wy-padding, calc(.5 * var(--wy-size, 1rem))))))}[part~=wy-image-grid-outer]{border-radius:0}[part~=wy-image-area]{display:flex;height:100%;cursor:pointer;align-items:center}[part~=wy-image-content]{position:absolute;height:auto;max-height:100%;width:100%;object-fit:contain;z-index:2}[part~=wy-image-content][part~=wy-loading]{transition:opacity .15s ease-out;opacity:0}[part~=wy-image-content][part~=wy-loading][part~=wy-loaded]{opacity:1;transition:opacity .15s ease-out}[part~=wy-image-content][part~=wy-loading][part~=wy-loaded]~[part~=wy-image-preview]{transition-delay:.15s;opacity:0}[part~=wy-image-icon]{object-fit:scale-down;object-position:center center}[part~=wy-image-grid-more]{position:absolute;z-index:3;line-height:100%;color:var(--wy-white, #ffffff);inset:0;display:flex;align-items:center;justify-content:center;font-size:calc(1.5 * var(--wy-size, 1rem));background:rgba(0,0,0,var(--wy-opacity-disabled, 38%))}`;
var w3 = Object.defineProperty, f3 = Object.getOwnPropertyDescriptor, Hu = /* @__PURE__ */ n((s, t, e, i) => {
  for (var r = i > 1 ? void 0 : i ? f3(t, e) : t, a = s.length - 1, o; a >= 0; a--)
    (o = s[a]) && (r = (i ? o(t, e, r) : o(r)) || r);
  return i && r && w3(t, e, r), r;
}, "__decorateClass$16");
const Qm = 128;
var vo;
let Ks = (vo = class extends nt {
  constructor() {
    super(...arguments), this.exportParts = new W(this), this.images = [], this.limit = 3, this.outer = !1;
  }
  /**
   * Dispatch a `file-open` event.
   *
   * @internal
   * @param e - Event that will be prevented.
   * @param file - The file to open.
   */
  dispatchFileOpen(t, e) {
    if (vu(t)) {
      t.stopPropagation();
      return;
    }
    if (t.preventDefault(), !e.is_trashed) {
      const i = new CustomEvent("file-open", { detail: { fileId: e.id } });
      this.dispatchEvent(i);
    }
  }
  render() {
    const t = this.images.length > this.limit ? this.images.length - this.limit : 0, e = this.images.slice(0, this.limit);
    return h`
      <div
        part=${tt({
      "wy-image-grid": !0,
      "wy-image-grid-outer": this.outer
    })}
      >
        ${e.map((i, r) => {
      const a = !i.width || i.width <= Qm || !i.height || i.height <= Qm, o = i.width && i.height ? i.width / i.height : 1, c = 64, l = o.toPrecision(5), u = (o * c).toPrecision(5) + "px", b = a ? c + "px" : (100 / o).toPrecision(5) + "%", C = i.width + "px", v = this.images.length !== 2 && r === 0, f = ai(i.name), m = f === ".gif" || f === ".svg" ? i.download_url : i.preview_url;
      return m ? h`
                <a
                  href="${i.download_url ?? "#"}"
                  @click=${($) => {
        !$.defaultPrevented && this.dispatchFileOpen($, i);
      }}
                  part=${tt({
        "wy-image": !0,
        "wy-image-filled": !0,
        "wy-image-full-width": v
      })}
                  style=${Jl({
        flexBasis: u,
        flexGrow: l,
        flexShrink: l,
        width: C,
        backgroundImage: `linear-gradient(var(--wy-shade-invert, rgba(255,255,255,0.15))), url(${m}), linear-gradient(var(--wy-shade-opaque, white));`
      })}
                >
                  <div part="wy-image-area" style=${Jl({ paddingBottom: b })}>
                    <img
                      part=${tt({ "wy-image-content": !0, "wy-image-icon": a })}
                      src=${m}
                      ${K(Eu)}
                      @load=${Au}
                      alt=""
                      loading="lazy"
                      decoding="async"
                    />
                    ${r === e.length - 1 && t ? h`<span part="wy-image-grid-more">+${t}</span>` : ""}
                  </div>
                </a>
              ` : g;
    })}
      </div>
    `;
  }
}, n(vo, "WyImageGrid"), vo);
Ks.styles = [y3, Y];
Hu([
  y({ attribute: !1 })
], Ks.prototype, "images", 2);
Hu([
  y({ type: Number })
], Ks.prototype, "limit", 2);
Hu([
  y({ type: Boolean, reflect: !0 })
], Ks.prototype, "outer", 2);
Ks = Hu([
  U("wy-image-grid")
], Ks);
var m3 = Object.defineProperty, v3 = Object.getOwnPropertyDescriptor, th = /* @__PURE__ */ n((s, t, e, i) => {
  for (var r = i > 1 ? void 0 : i ? v3(t, e) : t, a = s.length - 1, o; a >= 0; a--)
    (o = s[a]) && (r = (i ? o(t, e, r) : o(r)) || r);
  return i && r && m3(t, e, r), r;
}, "__decorateClass$15"), go;
let rd = (go = class extends nt {
  constructor() {
    super(...arguments), this.exportParts = new W(this);
  }
  /**
   * Triggers file-open event.
   *
   * @param e
   * @param file
   * @returns
   */
  dispatchFileOpen(t, e) {
    t.preventDefault();
    const i = new CustomEvent("file-open", { detail: { fileId: e.id } });
    return this.dispatchEvent(i);
  }
  render() {
    const t = this.file.size && this.file.size > 0 ? ku(this.file.size) : null, e = ai(this.file.name), { icon: i } = cs(this.file.name), r = kw(this.file.name), a = Zd(this.file.provider);
    return h`    
      <wy-item
        @click=${(o) => {
      !o.defaultPrevented && !this.file.is_trashed && this.dispatchFileOpen(o, this.file);
    }}
        size="md"
        title=${this.file.name}
        url=${ot(this.file.download_url ?? this.file.external_url)}
        interactive
      >
        <wy-icon slot="image" name=${i} .overlayName=${a} size="48" kind=${r} ext=${e}></wy-icon>
        <span slot="title">${this.file.name}</span>
        ${t ? h`<span slot="text" title="${t}">${t}</span>` : g}
      </wy-item>
    `;
  }
}, n(go, "WyAttachment"), go);
rd.styles = [Ru, Y];
th([
  y({ attribute: !1 })
], rd.prototype, "file", 2);
rd = th([
  U("wy-attachment")
], rd);
var bo;
let il = (bo = class extends nt {
  constructor() {
    super(...arguments), this.exportParts = new W(this), this.files = [], this.filled = !1;
  }
  dispatchFileOpen(t) {
    const e = new CustomEvent("file-open", { detail: { fileId: t.id } });
    return this.dispatchEvent(e);
  }
  render() {
    return h`
      <wy-item-list rounded ?filled=${this.filled}>
        ${this.files.map((t) => h`<wy-attachment @file-open=${() => this.dispatchFileOpen(t)} .file=${t}></wy-attachment>`)}
      </wy-item-list>
    `;
  }
}, n(bo, "WyAttachmentList"), bo);
il.styles = [Ru, Y];
th([
  y({ attribute: !1 })
], il.prototype, "files", 2);
th([
  y({ type: Boolean })
], il.prototype, "filled", 2);
il = th([
  U("wy-attachment-list")
], il);
var g3 = Object.defineProperty, b3 = Object.getOwnPropertyDescriptor, Uu = /* @__PURE__ */ n((s, t, e, i) => {
  for (var r = i > 1 ? void 0 : i ? b3(t, e) : t, a = s.length - 1, o; a >= 0; a--)
    (o = s[a]) && (r = (i ? o(t, e, r) : o(r)) || r);
  return i && r && g3(t, e, r), r;
}, "__decorateClass$14"), xo;
let sd = (xo = class extends Ae {
  constructor() {
    super(...arguments), this.exportParts = new W(this);
  }
  /**
   * Triggers file-open event.
   * @internal
   */
  dispatchFileOpen(t, e) {
    t.preventDefault();
    const i = new CustomEvent("file-open", { detail: { fileId: e.id } });
    return this.dispatchEvent(i);
  }
  render() {
    const t = this.file.size && this.file.size > 0 ? ku(this.file.size) : null, e = ai(this.file.name), { icon: i } = cs(this.file.name), r = kw(this.file.name), a = Zd(this.file.provider), o = `${this.file.name}${t ? ` • ${t}` : ""}`;
    return h`
      <wy-button
        part="wy-annotation"
        @click=${(c) => {
      !c.defaultPrevented && !this.file.is_trashed && this.dispatchFileOpen(c, this.file);
    }}
        kind="filled"
        small
        title=${o}
      >
        <wy-icon
          part="wy-annotation-icon"
          name=${i}
          .overlayName=${a}
          size="24"
          kind=${r}
          ext=${e}
        ></wy-icon>
        <span part="wy-annotation-text">${this.file.name}</span>
      </wy-button>
    `;
  }
}, n(xo, "WyAnnotation"), xo);
sd.styles = [Y];
Uu([
  y({ type: Object, attribute: !1 })
], sd.prototype, "file", 2);
sd = Uu([
  U("wy-annotation")
], sd);
var Co;
let nd = (Co = class extends qt {
  constructor() {
    super(...arguments), this.exportParts = new W(this), this.files = [];
  }
  render() {
    return this.settings?.annotations === "none" ? g : h`
      <div part="wy-annotations">
        ${this.files.map((t) => h`<wy-annotation .file=${t}></wy-annotation>`)}
      </div>
    `;
  }
}, n(Co, "WyAnnotationList"), Co);
nd.styles = [Y];
Uu([
  y({ attribute: !1 })
], nd.prototype, "files", 2);
nd = Uu([
  U("wy-annotation-list")
], nd);
function rr(s = "", t = "", e = "", i = !1) {
  if ($e(), s) {
    const r = document.createElement("a");
    i && (/^(data:|blob:)/.test(s) ? r.download = e || "download" : s = s.includes("?d=1") || s.includes("&d=1") ? s : s.includes("?") ? s + "&d=1" : s + "?d=1"), t && (r.target = t), r.href = s, document.body.appendChild(r);
    try {
      r.click();
    } catch {
      if (console.warn("Could not open link normally, trying fallback"), /^(?:blob:|data:)/.test(s))
        console.error(`Could not ${i ? "download" : "open"} ${e}`);
      else
        try {
          window.open(s, t);
        } catch {
          console.error(`Could not ${i ? "download" : "open"} ${e}`);
        }
    }
    document.body.removeChild(r);
  }
}
n(rr, "openUrl");
function Gm(s, t) {
  let e = !0;
  const i = globalThis.WEAVY_IMPORT_URL;
  try {
    e = !t || !!(t && i) && !new URL(t).href.startsWith(i);
  } catch (r) {
    console.warn("Could not parse import url", r);
  }
  return new URL(
    s,
    typeof i == "string" && e ? new URL(i, globalThis.location.href) : t
  );
}
n(Gm, "environmentUrl");
const cg = j`[part~=wy-embed]{display:flex;flex-direction:column;word-wrap:break-word;border-radius:var(--wy-border-radius-sm, calc(var(--wy-border-radius, calc(.5 * var(--wy-size, 1rem))) - .25 * var(--wy-padding, calc(.5 * var(--wy-size, 1rem)))));clip-path:border-box;--wy-component-color: var(--wy-on-surface, var(--wy-on-surface-light, #181c20));background-color:var(--wy-surface-layer, var(--wy-surface-layer-light, rgba(0, 0, 0, .0509803922)));color:var(--wy-component-color)}[part~=wy-embed-interactive]{background-color:var(--wy-surface-layer, var(--wy-surface-layer-light, rgba(0, 0, 0, .0509803922)));-webkit-user-select:none;user-select:none;cursor:pointer}[part~=wy-embed-interactive]:active,[part~=wy-embed-interactive]:hover,[part~=wy-embed-interactive]:focus{background-color:var(--wy-surface-layer-high, var(--wy-surface-layer-high-light, rgba(0, 0, 0, .0745098039)))}[part~=wy-embed-interactive][part~=wy-disabled],[part~=wy-embed-interactive]:disabled{pointer-events:none;border-left:none;border-right:none;border-bottom:none;border-radius:0;background-color:transparent}[part~=wy-embed-area]{display:flex;justify-content:center;align-items:center}[part~=wy-embed-icon]{width:calc(3 * var(--wy-size, 1rem));height:calc(3 * var(--wy-size, 1rem));flex:0 0 auto;object-fit:contain}[part~=wy-embed-content]{display:flex;position:relative;padding:0;overflow:hidden;width:100%;flex-direction:column;align-items:center;justify-content:flex-start}[part~=wy-embed-content] iframe,[part~=wy-embed-content] embed,[part~=wy-embed-content] object,[part~=wy-embed-content] video{border:0;width:auto;height:auto;max-height:var(--wy-component-max-size, var(--wy-embed-content-max-size, calc(48 * var(--wy-size, 1rem))))}[part~=wy-embed-photo]{border-top-left-radius:var(--wy-component-border-top-radius, var(--wy-border-radius-sm, calc(var(--wy-border-radius, calc(.5 * var(--wy-size, 1rem))) - .25 * var(--wy-padding, calc(.5 * var(--wy-size, 1rem))))));border-top-right-radius:var(--wy-component-border-top-radius, var(--wy-border-radius-sm, calc(var(--wy-border-radius, calc(.5 * var(--wy-size, 1rem))) - .25 * var(--wy-padding, calc(.5 * var(--wy-size, 1rem))))));border-bottom-left-radius:var(--wy-component-border-bottom-radius, var(--wy-border-radius-sm, calc(var(--wy-border-radius, calc(.5 * var(--wy-size, 1rem))) - .25 * var(--wy-padding, calc(.5 * var(--wy-size, 1rem))))));border-bottom-right-radius:var(--wy-component-border-bottom-radius, var(--wy-border-radius-sm, calc(var(--wy-border-radius, calc(.5 * var(--wy-size, 1rem))) - .25 * var(--wy-padding, calc(.5 * var(--wy-size, 1rem))))));position:relative;background-size:cover;background-position:center;clip-path:border-box}[part~=wy-embed-photo]:after{border-top-left-radius:var(--wy-component-border-top-radius, var(--wy-border-radius-sm, calc(var(--wy-border-radius, calc(.5 * var(--wy-size, 1rem))) - .25 * var(--wy-padding, calc(.5 * var(--wy-size, 1rem))))));border-top-right-radius:var(--wy-component-border-top-radius, var(--wy-border-radius-sm, calc(var(--wy-border-radius, calc(.5 * var(--wy-size, 1rem))) - .25 * var(--wy-padding, calc(.5 * var(--wy-size, 1rem))))));border-bottom-left-radius:var(--wy-component-border-bottom-radius, var(--wy-border-radius-sm, calc(var(--wy-border-radius, calc(.5 * var(--wy-size, 1rem))) - .25 * var(--wy-padding, calc(.5 * var(--wy-size, 1rem))))));border-bottom-right-radius:var(--wy-component-border-bottom-radius, var(--wy-border-radius-sm, calc(var(--wy-border-radius, calc(.5 * var(--wy-size, 1rem))) - .25 * var(--wy-padding, calc(.5 * var(--wy-size, 1rem))))));content:\"\";position:absolute;width:100%;height:100%;z-index:1;-webkit-backdrop-filter:blur(32px);backdrop-filter:blur(32px);background-color:var(--wy-shade, var(--wy-shade-light, rgba(0, 0, 0, .3)))}[part~=wy-embed-photo-with-description]{--wy-component-border-bottom-radius: 0}[part~=wy-embed-image]{width:auto;height:auto;object-fit:contain;flex:0 1 auto;min-width:0;min-height:calc(3 * var(--wy-size, 1rem));max-height:calc(var(--wy-component-max-size, var(--wy-embed-content-max-size, calc(48 * var(--wy-size, 1rem)))) - var(--wy-padding, calc(.5 * var(--wy-size, 1rem))) * 2 - var(--wy-button-line-height, var(--wy-line-height, 1.5)) * var(--wy-size, 1rem));z-index:2}[part~=wy-embed-provider]{color:var(--wy-outline, var(--wy-outline-light, #72777f));text-transform:uppercase;font-size:var(--wy-font-size-sm, calc(.875 * var(--wy-font-size, 1em)));margin-bottom:calc(.25 * var(--wy-size, 1rem))}[part~=wy-embed-provider]{overflow:hidden;text-overflow:ellipsis;white-space:nowrap}[part~=wy-embed-actions]{display:flex;justify-content:space-between}[part~=wy-embed-select]{display:flex;flex-direction:column;min-height:0;--wy-component-max-size: calc(12 * var(--wy-size, 1rem));--wy-component-background-color: transparent}[part~=wy-embed-select] wy-embed{border:var(--wy-border, 1px) solid var(--wy-outline-variant, var(--wy-outline-variant-light, #c2c7cf));border-radius:var(--wy-border-radius, calc(.5 * var(--wy-size, 1rem)));--wy-component-border-top-radius: 0;min-height:0;display:none}[part~=wy-embed-select] wy-embed:first-of-type{display:block}`;
var x3 = Object.defineProperty, C3 = Object.getOwnPropertyDescriptor, eh = /* @__PURE__ */ n((s, t, e, i) => {
  for (var r = i > 1 ? void 0 : i ? C3(t, e) : t, a = s.length - 1, o; a >= 0; a--)
    (o = s[a]) && (r = (i ? o(t, e, r) : o(r)) || r);
  return i && r && x3(t, e, r), r;
}, "__decorateClass$13"), $o;
let rl = ($o = class extends nt {
  constructor() {
    super(...arguments), this.exportParts = new W(this), this.disabled = !1, this.embedContentRef = J();
  }
  /**
   * Dispatch a wy-action event for this embed.
   *
   * @internal
   * @param {ActionType | string} [action=""] - Action to dispatch.
   * @returns {boolean} True if the event was not canceled.
   */
  dispatchActionEvent(t = me.Default) {
    const e = new CustomEvent("wy-action", {
      detail: {
        action: t,
        embed: this.embed
      },
      bubbles: !0,
      composed: !0,
      cancelable: !0
    });
    return this.dispatchEvent(e);
  }
  render() {
    const t = this.embed.actions ? [...this.embed.actions] : [], e = t.shift(), i = t.length === 1 ? t.shift() : void 0, r = !!((this.embed.type === "photo" || this.embed.type === "link") && (this.embed.title || this.embed.description || this.embed.type === "photo" && this.embed.provider_name));
    return h`
      <slot name="before"></slot>
      <div
        part="wy-embed ${tt({
      "wy-embed-interactive": !!this.embed.url,
      "wy-disabled": this.disabled
    })}"
        title=${this.embed.url ? this.embed.url : this.embed.title || ""}
        @click=${() => this.embed.url && this.dispatchActionEvent(me.Default) && rr(this.embed.url, "_blank")}
        @keydown=${ne}
        @keyup=${ke}
      >
        ${(this.embed.type === "video" || this.embed.type === "rich") && this.embed.html ? h`
              <div part="wy-embed-area">
                <div
                  ${K(this.embedContentRef)}
                  part=${tt({
      "wy-embed-content": !0,
      "wy-embed-video": this.embed.type === "video",
      "wy-embed-rich": this.embed.type === "rich"
    })}
                  data-width=${ot(this.embed.width)}
                  data-height=${ot(this.embed.height)}
                >
                  ${Bs(this.embed.html)}
                </div>
              </div>
            ` : g}
        ${this.embed.type === "photo" && this.embed.image && this.embed.thumbnail_url ? h`
              <div
                part="wy-embed-photo wy-embed-area ${tt({
      "wy-embed-photo-with-description": r
    })}"
                style="background-image: linear-gradient(var(--wy-shade-invert, rgba(255,255,255,0.15))), url(${this.embed.thumbnail_url}), linear-gradient(var(--wy-shade-opaque, white));"
              >
                <img
                  part="wy-embed-image"
                  src=${this.embed.thumbnail_url}
                  alt=${this.embed.provider_name || this.embed.title || ""}
                  width=${ot(this.embed.image.width)}
                  height=${ot(this.embed.image.height)}
                />
              </div>
            ` : g}
        ${r ? h`
              <wy-item size="auto">
                ${this.embed.type === "link" && this.embed.image && this.embed.thumbnail_url ? h`
                      <img
                        part="wy-embed-icon"
                        slot="image"
                        src=${this.embed.thumbnail_url}
                        alt=${this.embed.provider_name || this.embed.title || ""}
                        width=${ot(this.embed.image.width)}
                        height=${ot(this.embed.image.height)}
                      />
                    ` : g}
                ${this.embed.provider_name ? h`<span slot="meta" part="wy-embed-provider">${this.embed.provider_name}</span>` : g}
                ${this.embed.title ? h`<span slot="title">${this.embed.title}</span>` : g}
                ${this.embed.description ? h`<span slot="text">${this.embed.description}</span>` : g}
                ${i ? h`
                      <wy-button
                        slot="actions"
                        color="variant"
                        @click=${(a) => {
      a.stopPropagation(), this.dispatchActionEvent(i);
    }}
                      >
                        ${i}
                      </wy-button>
                    ` : g}
                ${e ? h`
                      <wy-button
                        slot="actions"
                        color="primary"
                        @click=${(a) => {
      a.stopPropagation(), this.dispatchActionEvent(e);
    }}
                      >
                        ${e}
                      </wy-button>
                    ` : g}
                ${t.length ? h`
                      <wy-dropdown slot="actions">
                        ${t.map(
      (a) => h`
                              <wy-dropdown-item
                                @click=${(o) => {
        o.preventDefault(), o.stopPropagation(), this.dispatchActionEvent(a);
      }}
                              >
                                ${a}
                              </wy-dropdown-item>
                            `
    )}
                      </wy-dropdown>
                    ` : g}
              </wy-item>
            ` : g}
      </div>
    `;
  }
  updated(t) {
    if (this.embedContentRef.value) {
      const e = this.embedContentRef.value.firstElementChild, { width: i, height: r } = this.embedContentRef.value.dataset, a = Number(r ?? ""), o = a > 128 && a < 256 ? a : 128;
      i && r && (e.style.aspectRatio = `${i} / ${r}`, e.style.minHeight = `${o}px`);
    }
  }
}, n($o, "WyEmbed"), $o);
rl.styles = [Y, cg];
eh([
  y({ attribute: !1 })
], rl.prototype, "embed", 2);
eh([
  y({ type: Boolean, reflect: !0 })
], rl.prototype, "disabled", 2);
rl = eh([
  U("wy-embed")
], rl);
var ko;
let ad = (ko = class extends nt {
  constructor() {
    super(...arguments), this.exportParts = new W(this);
  }
  /**
   * Dispatch a local embed-remove event for the embed with the given id.
  *
   * @internal
   * @param id - Embed id to remove.
   * @returns {boolean} True if the event was not canceled.
   */
  dispatchRemove(t) {
    const e = new CustomEvent("embed-remove", {
      detail: { id: t },
      bubbles: !1,
      composed: !1
    });
    return this.dispatchEvent(e);
  }
  /**
   * Dispatch a local embed-swap event to request rotating/swapping embeds.
  *
   * @internal
   * @returns {boolean} True if the event was not canceled.
   */
  dispatchSwap() {
    const t = new CustomEvent("embed-swap", {
      detail: {},
      bubbles: !1,
      composed: !1
    });
    return this.dispatchEvent(t);
  }
  render() {
    return h`
      <div part="wy-embed-select">
        ${Be(
      this.embeds,
      (t) => t.id,
      (t) => h`
            <wy-embed disabled .embed=${t}>
              <div part="wy-embed-actions" slot="before">
                ${this.embeds.length > 1 ? h`
                      <wy-button kind="icon" @click=${() => this.dispatchSwap()}>
                        <wy-icon name="swap-horizontal"></wy-icon>
                      </wy-button>
                    ` : h`<span></span>`}

                <wy-button kind="icon" @click=${() => this.dispatchRemove(t.id)}
                  ><wy-icon name="close-circle"></wy-icon
                ></wy-button>
              </div>
            </wy-embed>
          `
    )}
      </div>
    `;
  }
}, n(ko, "WyEmbedSelect"), ko);
ad.styles = [Y, cg];
eh([
  y({ attribute: !1 })
], ad.prototype, "embeds", 2);
ad = eh([
  U("wy-embed-select")
], ad);
function Yy(s) {
  switch (s) {
    case "zoom":
      return "zoom-meetings";
    case "microsoft":
      return "microsoft-teams";
    case "google":
      return "google-meet";
  }
}
n(Yy, "getMeetingIconName");
function Xy(s) {
  switch (s) {
    case "zoom":
      return x("Zoom meeting");
    case "microsoft":
      return x("Teams meeting");
    case "google":
      return x("Google Meet");
  }
}
n(Xy, "getMeetingTitle");
var $3 = Object.defineProperty, k3 = Object.getOwnPropertyDescriptor, lg = /* @__PURE__ */ n((s, t, e, i) => {
  for (var r = i > 1 ? void 0 : i ? k3(t, e) : t, a = s.length - 1, o; a >= 0; a--)
    (o = s[a]) && (r = (i ? o(t, e, r) : o(r)) || r);
  return i && r && $3(t, e, r), r;
}, "__decorateClass$12"), _o;
let od = (_o = class extends nt {
  constructor() {
    super(...arguments), this.exportParts = new W(this);
  }
  render() {
    const t = ((/* @__PURE__ */ new Date()).getTime() - new Date(this.meeting.created_at).getTime()) / 36e5 > 2;
    return h`
      <wy-item-list rounded filled>
        ${t ? h`
              <wy-item part="wy-meeting" size="lg" disabled title="${x("Meeting ended")}">
                <wy-icon slot="image" svg="${Yy(this.meeting.provider)}" size="48"></wy-icon>
                <span slot="title">${Xy(this.meeting.provider)}</span>
                <span slot="text">${this.meeting.code}</span>
              </wy-item>
            ` : h`
              <wy-item
                interactive
                size="lg"
                part="wy-meeting"
                @click=${(e) => {
      e.preventDefault(), rr(this.meeting.join_url, "_blank");
    }}
                url=${ot(this.meeting.join_url)}
                title="${x("Join meeting")}"
              >
                <wy-icon
                  slot="image"
                  svg="${Yy(this.meeting.provider)}"
                  size="48"
                  color="native"
                ></wy-icon>
                <span slot="title">${Xy(this.meeting.provider)}</span>
                <span slot="text">${this.meeting.code}</span>
              </wy-item>
            `}
        </wy-item-list>
    `;
  }
}, n(_o, "WyMeetingCard"), _o);
od.styles = [Y];
lg([
  y({ attribute: !1 })
], od.prototype, "meeting", 2);
od = lg([
  U("wy-meeting-card"),
  ut()
], od);
const qw = j`[part~=wy-poll]{min-width:0;display:flex;flex-direction:column;gap:var(--wy-gap-sm, calc(.5 * var(--wy-gap, var(--wy-padding, calc(.5 * var(--wy-size, 1rem))))));box-sizing:border-box;width:100%}[part~=wy-poll-form]{display:flex;flex-direction:column;gap:var(--wy-gap, var(--wy-padding, calc(.5 * var(--wy-size, 1rem))))}[part~=wy-poll-form]:empty{display:none}[part~=wy-poll-option]{--wy-component-color: var(--wy-on-background, var(--wy-on-background-light, #181c20));display:flex;align-items:center;clip-path:border-box;overflow:hidden;position:relative;background-color:var(--wy-surface-layer-lowest, var(--wy-surface-layer-lowest-light, rgba(255, 255, 255, .7490196078)));border:var(--wy-border-outline, 0) solid var(--wy-outline-variant, var(--wy-outline-variant-light, #c2c7cf));border-radius:var(--wy-border-radius-sm, calc(var(--wy-border-radius, calc(.5 * var(--wy-size, 1rem))) - .25 * var(--wy-padding, calc(.5 * var(--wy-size, 1rem)))));padding:var(--wy-padding-sm, calc(.75 * var(--wy-padding, calc(.5 * var(--wy-size, 1rem))))) var(--wy-padding, calc(.5 * var(--wy-size, 1rem)));cursor:pointer;gap:var(--wy-gap-sm, calc(.5 * var(--wy-gap, var(--wy-padding, calc(.5 * var(--wy-size, 1rem))))))}[part~=wy-poll-option]>:not([part~=wy-poll-meter]){z-index:1}[part=wy-poll-image]{display:flex;height:calc(var(--wy-button-size, calc(var(--wy-button-line-height, 1.5) * var(--wy-size, 1rem) + 2 * var(--wy-button-padding-y, var(--wy-padding, calc(.5 * var(--wy-size, 1rem)))))) * .6);width:calc(var(--wy-button-size, calc(var(--wy-button-line-height, 1.5) * var(--wy-size, 1rem) + 2 * var(--wy-button-padding-y, var(--wy-padding, calc(.5 * var(--wy-size, 1rem)))))) * .6);padding:0;line-height:1;align-items:center;justify-content:center}[part~=wy-poll-title]{width:100%;min-width:0;min-height:100%;color:var(--wy-component-color);font-size:var(--wy-item-title-font-size, var(--wy-font-size, 1em));font-weight:var(--wy-item-title-font-weight, var(--wy-font-weight, unset));align-content:center}[part~=wy-poll-title]{overflow:hidden;text-overflow:ellipsis;white-space:nowrap}[part~=wy-poll-amount]{min-width:3em;text-align:end}[part~=wy-poll-meter]{position:absolute;top:0;left:0;bottom:0;background-color:var(--wy-surface-layer-highest, var(--wy-surface-layer-highest-light, rgba(0, 0, 0, .1019607843)))}`, _3 = j`[part~=wy-dialog]{background:none;border:none;padding:0;margin:0;width:100%;height:100%;max-width:100%;max-height:100%;container-type:inline-size;pointer-events:none}[part~=wy-dialog][popover]{position:fixed;inset:0;z-index:1055}[part~=wy-dialog-modal]{align-items:center;justify-content:center;overflow:visible}[part~=wy-dialog-modal][open]{display:flex}[part~=wy-dialog-modal]:popover-open{display:flex}[part~=wy-dialog-modal]:focus-visible{outline:none}[part~=wy-overlay]{--wy-component-background-color: var(--wy-background, var(--wy-background-light, #f7f9ff));--wy-component-color: var(--wy-on-background, var(--wy-on-background-light, #181c20));background-color:var(--wy-component-background-color);color:var(--wy-component-color);display:flex;flex-direction:column;overflow:hidden;container-type:inline-size}[part~=wy-overlay]{interpolate-size:allow-keywords;transition:opacity var(--wy-transition-fast, var(--wy-transition-duration-fast, .1s) var(--wy-transition-curve, cubic-bezier(.4, 0, .2, 1))) .01s,visibility .01s step-end var(--wy-transition-duration-fast, .1s)}[part~=wy-overlay]:not([part~=wy-open]){opacity:0;visibility:hidden}[part~=wy-overlay][part~=wy-open]{transition:opacity var(--wy-transition, var(--wy-transition-duration, .2s) var(--wy-transition-curve, cubic-bezier(.4, 0, .2, 1))),transform var(--wy-transition, var(--wy-transition-duration, .2s) var(--wy-transition-curve, cubic-bezier(.4, 0, .2, 1))),visibility var(--wy-transition-duration, .2s) step-start,top var(--wy-transition, var(--wy-transition-duration, .2s) var(--wy-transition-curve, cubic-bezier(.4, 0, .2, 1))),right var(--wy-transition, var(--wy-transition-duration, .2s) var(--wy-transition-curve, cubic-bezier(.4, 0, .2, 1))),bottom var(--wy-transition, var(--wy-transition-duration, .2s) var(--wy-transition-curve, cubic-bezier(.4, 0, .2, 1))),left var(--wy-transition, var(--wy-transition-duration, .2s) var(--wy-transition-curve, cubic-bezier(.4, 0, .2, 1))),width var(--wy-transition, var(--wy-transition-duration, .2s) var(--wy-transition-curve, cubic-bezier(.4, 0, .2, 1))),height var(--wy-transition, var(--wy-transition-duration, .2s) var(--wy-transition-curve, cubic-bezier(.4, 0, .2, 1)));visibility:visible;opacity:1;transform:none;pointer-events:auto}@starting-style{[part~=wy-overlay][part~=wy-open]{opacity:0;visibility:hidden}}`, P3 = j`[part~=wy-modal]{width:100%;height:100%;border-radius:0}@container (inline-size >= 768px){[part~=wy-modal-centered]{border-radius:var(--wy-border-radius, calc(.5 * var(--wy-size, 1rem)));box-shadow:var(--wy-shadow-level4, 0 calc(1 * var(--wy-size, 1rem)) calc(2.25 * var(--wy-size, 1rem)) calc(.125 * var(--wy-size, 1rem)) rgba(0, 0, 0, .14), 0 calc(.375 * var(--wy-size, 1rem)) calc(1.875 * var(--wy-size, 1rem)) calc(.3125 * var(--wy-size, 1rem)) rgba(0, 0, 0, .12), 0 calc(.5 * var(--wy-size, 1rem)) calc(.625 * var(--wy-size, 1rem)) calc(-.3125 * var(--wy-size, 1rem)) rgba(0, 0, 0, .2));margin-left:calc(4 * var(--wy-size, 1rem));margin-top:auto;margin-right:calc(4 * var(--wy-size, 1rem));margin-bottom:auto;width:calc(32 * var(--wy-size, 1rem));max-height:calc(32 * var(--wy-size, 1rem))}}@container (inline-size >= 768px){[part~=wy-modal-centered][part~=wy-maximized]{margin:0;width:100%;height:100%;max-width:calc(100% - 2 * var(--wy-size, 1rem));max-height:calc(100% - 2 * var(--wy-size, 1rem))}}`, S3 = j`[part~=wy-sheet]{--wy-component-background-color: var(--wy-surface-container-low, var(--wy-surface-container-low-light, #f1f3f9));--wy-component-color: var(--wy-on-surface, var(--wy-on-surface-light, #181c20));position:fixed;z-index:1055;bottom:calc(.5 * var(--wy-size, 1rem));left:calc(.5 * var(--wy-size, 1rem));right:calc(.5 * var(--wy-size, 1rem));max-height:max(50vh,50%);min-height:calc(3 * var(--wy-size, 1rem));margin:calc(.5 * var(--wy-size, 1rem)) auto;max-width:calc(32 * var(--wy-size, 1rem));border-radius:var(--wy-border-radius, calc(.5 * var(--wy-size, 1rem)));box-shadow:var(--wy-shadow-level1, 0 calc(.125 * var(--wy-size, 1rem)) calc(.125 * var(--wy-size, 1rem)) 0 rgba(0, 0, 0, .14), 0 calc(.1875 * var(--wy-size, 1rem)) calc(.0625 * var(--wy-size, 1rem)) calc(-.125 * var(--wy-size, 1rem)) rgba(0, 0, 0, .2), 0 calc(.0625 * var(--wy-size, 1rem)) calc(.3125 * var(--wy-size, 1rem)) 0 rgba(0, 0, 0, .12))}[part~=wy-sheet]:not([part~=wy-open]){transform:translateY(calc(32 * var(--wy-size, 1rem)))}@starting-style{[part~=wy-sheet][part~=wy-open]{transform:translateY(calc(32 * var(--wy-size, 1rem)))}}`, M3 = j`[part~=wy-drawer]{--wy-component-background-color: var(--wy-surface-container-low, var(--wy-surface-container-low-light, #f1f3f9));--wy-component-color: var(--wy-on-surface, var(--wy-on-surface-light, #181c20));position:fixed;z-index:1055;box-shadow:var(--wy-shadow-level1, 0 calc(.125 * var(--wy-size, 1rem)) calc(.125 * var(--wy-size, 1rem)) 0 rgba(0, 0, 0, .14), 0 calc(.1875 * var(--wy-size, 1rem)) calc(.0625 * var(--wy-size, 1rem)) calc(-.125 * var(--wy-size, 1rem)) rgba(0, 0, 0, .2), 0 calc(.0625 * var(--wy-size, 1rem)) calc(.3125 * var(--wy-size, 1rem)) 0 rgba(0, 0, 0, .12));inset:0;border-radius:0}@container (inline-size >= 768px){[part~=wy-drawer]{top:var(--wy-padding-outer, 0px);right:var(--wy-padding-outer, 0px);bottom:var(--wy-padding-outer, 0px);left:auto;width:calc(24 * var(--wy-size, 1rem));min-width:calc(24 * var(--wy-size, 1rem));border-radius:var(--wy-border-radius-outer, 0px)}}@container (inline-size >= 768px){[part~=wy-drawer][part~=wy-maximized]{width:calc(100% - 2 * var(--wy-padding-outer, 0px))}}[part~=wy-drawer]:not([part~=wy-open]){transform:translate(calc(100% + var(--wy-padding-outer, 0px)))}@starting-style{[part~=wy-drawer][part~=wy-open]{transform:translate(calc(100% + var(--wy-padding-outer, 0px)))}}`, Bu = j`[part~=wy-header]{--wy-component-background-color: var(--wy-surface-container, var(--wy-surface-container-light, #eceef3));background-color:var(--wy-component-background-color);box-sizing:border-box}@supports (position: sticky){[part~=wy-header]{position:sticky;top:0;z-index:1020}}[part~=wy-header]{z-index:1022}[part~=wy-header-floating]{--wy-component-border-radius: var(--wy-border-radius, calc(.5 * var(--wy-size, 1rem)));border-radius:var(--wy-component-border-radius)}[part~=wy-header-outer]{--wy-component-border-radius: var(--wy-border-radius-outer, var(--wy-border-radius, calc(.5 * var(--wy-size, 1rem))));border-radius:var(--wy-component-border-radius)}`, dg = j`[part~=wy-titlebar]{--wy-component-color: var(--wy-on-surface, var(--wy-on-surface-light, #181c20));box-sizing:border-box;display:grid;align-items:center;justify-content:space-between;justify-items:center;min-height:var(--wy-titlebar-height, calc(max(1lh, var(--wy-button-size, calc(2.5 * var(--wy-size, 1rem)))) + 2 * var(--wy-padding-sm, calc(.25 * var(--wy-size, 1rem)))));padding:calc(var(--wy-padding-sm, calc(.75 * var(--wy-padding, calc(.5 * var(--wy-size, 1rem))))));flex:0 0 var(--wy-titlebar-height, calc(max(1lh, var(--wy-button-size, calc(2.5 * var(--wy-size, 1rem)))) + 2 * var(--wy-padding-sm, calc(.25 * var(--wy-size, 1rem)))));grid-template-columns:var(--wy-button-size, calc(var(--wy-button-line-height, 1.5) * var(--wy-size, 1rem) + 2 * var(--wy-button-padding-y, var(--wy-padding, calc(.5 * var(--wy-size, 1rem)))))) minmax(0,max-content) var(--wy-button-size, calc(var(--wy-button-line-height, 1.5) * var(--wy-size, 1rem) + 2 * var(--wy-button-padding-y, var(--wy-padding, calc(.5 * var(--wy-size, 1rem))))));column-gap:var(--wy-gap, var(--wy-padding, calc(.5 * var(--wy-size, 1rem))));line-height:var(--wy-line-height, 1.5);color:var(--wy-component-color)}[part~=wy-titlebar-buttons],[part~=wy-titlebar-section]{display:flex;flex:0 0 auto;align-items:center;gap:var(--wy-gap-sm, calc(.5 * var(--wy-gap, var(--wy-padding, calc(.5 * var(--wy-size, 1rem))))));max-width:100%;box-shadow:var(--wy-component-box-shadow);border-radius:var(--wy-button-border-radius, var(--wy-border-radius, calc(1.25 * var(--wy-size, 1rem))));background-color:var(--wy-component-background-color)}[part~=wy-titlebar-lg]{padding:var(--wy-padding, calc(.5 * var(--wy-size, 1rem)));height:auto}[part~=wy-titlebar-buttons-first]{justify-content:flex-start}[part~=wy-titlebar-buttons-last]{justify-content:flex-end}[part~=wy-titlebar-text]{max-width:100%;display:block;color:inherit!important;-webkit-user-select:none;user-select:none;padding:var(--wy-padding, calc(.5 * var(--wy-size, 1rem)))}[part~=wy-titlebar-text]{overflow:hidden;text-overflow:ellipsis;white-space:nowrap}[part~=wy-titlebar-text-trashed]{text-decoration:line-through}`;
var E3 = Object.defineProperty, A3 = Object.getOwnPropertyDescriptor, hs = /* @__PURE__ */ n((s, t, e, i) => {
  for (var r = i > 1 ? void 0 : i ? A3(t, e) : t, a = s.length - 1, o; a >= 0; a--)
    (o = s[a]) && (r = (i ? o(t, e, r) : o(r)) || r);
  return i && r && E3(t, e, r), r;
}, "__decorateClass$11"), Po;
let dr = (Po = class extends nt {
  constructor() {
    super(...arguments), this.exportParts = new W(this), this.header = !1, this.floating = !1, this.outer = !1, this.trashed = !1, this.size = "md";
  }
  /**
   * render function for the inner titlebar.
   * @internal
   * @returns
   */
  renderTitlebar() {
    return h`
      <nav part="wy-titlebar ${tt({ "wy-titlebar-lg": this.size === "lg" })}">
        <slot name="first">
          <div part="wy-titlebar-icon wy-titlebar-buttons wy-titlebar-buttons-first">
            <slot name="icon"></slot>
          </div>
        </slot>
        <slot name="middle">
          <div part="wy-titlebar-title wy-titlebar-section">
            <slot name="title-section">
              <wy-titlebar-text ?trashed=${this.trashed}><slot name="title"></slot></wy-titlebar-text>
            </slot>
          </div>
        </slot>
        <slot name="last">
          <div part="wy-titlebar-actions wy-titlebar-buttons wy-titlebar-buttons-last">
            <slot name="actions"></slot>
          </div>
        </slot>
      </nav>
    `;
  }
  render() {
    return this.header ? h`<header part="wy-header ${tt({ "wy-header-floating": this.floating, "wy-header-outer": this.outer })}"
          >${this.renderTitlebar()}</header
        >` : this.renderTitlebar();
  }
}, n(Po, "WyTitlebar"), Po);
dr.styles = [Bu, dg, Y];
hs([
  y({ type: Boolean })
], dr.prototype, "header", 2);
hs([
  y({ type: Boolean })
], dr.prototype, "floating", 2);
hs([
  y({ type: Boolean })
], dr.prototype, "outer", 2);
hs([
  y({ type: Boolean })
], dr.prototype, "trashed", 2);
hs([
  y()
], dr.prototype, "size", 2);
dr = hs([
  U("wy-titlebar")
], dr);
var So;
let cd = (So = class extends nt {
  constructor() {
    super(...arguments), this.exportParts = new W(this), this.trashed = !1;
  }
  render() {
    const t = {
      "wy-titlebar-text-trashed": this.trashed
    };
    return h` <slot part="wy-titlebar-text ${tt(t)}"></slot> `;
  }
}, n(So, "WyTitlebarText"), So);
cd.styles = [dg, Y];
hs([
  y({ type: Boolean })
], cd.prototype, "trashed", 2);
cd = hs([
  U("wy-titlebar-text")
], cd);
var R3 = Object.defineProperty, T3 = Object.getOwnPropertyDescriptor, ih = /* @__PURE__ */ n((s, t, e, i) => {
  for (var r = i > 1 ? void 0 : i ? T3(t, e) : t, a = s.length - 1, o; a >= 0; a--)
    (o = s[a]) && (r = (i ? o(t, e, r) : o(r)) || r);
  return i && r && R3(t, e, r), r;
}, "__decorateClass$10"), Mo;
let Xr = (Mo = class extends nt {
  constructor() {
    super(...arguments), this.exportParts = new W(this), this.viewportRef = J(), this.show = !0, this.type = "modal", this.maximized = !1, this.noHeader = !1, this.handleClose = (t) => {
      if (this.isModal() || t?.type === "toggle" && t.newState === "closed" || t?.type === "close") {
        this.show = !1;
        const e = new CustomEvent("close");
        this.dispatchEvent(e);
      }
    };
  }
  /**
   * Close the modal.
   */
  close() {
    this.show = !1;
    try {
      this.type !== "modal" && this.viewportRef.value?.popover ? this.viewportRef.value?.hidePopover() : this.viewportRef.value?.close();
    } catch {
    }
  }
  /**
   * Checks whether the overlay is using the modal api.
   *
   * @param [type] - Optional type to check
   * @returns Whether the overlay is using the modal api.
   */
  isModal(t) {
    return t ??= this.type, t === "modal" || t === "full";
  }
  willUpdate(t) {
    if (super.willUpdate(t), t.has("type") && this.viewportRef.value) {
      const e = !this.isModal(t.get("type")) && this.viewportRef.value.popover ? "toggle" : "close";
      this.viewportRef.value.removeEventListener(e, this.handleClose);
    }
  }
  render() {
    if (this.type === "none")
      return g;
    const t = {
      "wy-dialog": !0,
      "wy-dialog-modal": this.isModal()
    }, e = {
      "wy-overlay": !0,
      "wy-modal": this.isModal(),
      // modal, full
      "wy-modal-centered": this.type === "modal",
      "wy-sheet": this.type === "sheet",
      "wy-drawer": this.type === "drawer",
      "wy-maximized": this.maximized,
      "wy-open": this.show
    }, i = this.isModal() ? void 0 : "auto";
    return h`
      <dialog
        part=${tt(t)}
        tabindex="0"
        popover=${ot(i)}
        ${K(this.viewportRef)}
      >
        <div part=${tt(e)}>
          ${this.noHeader ? g : h`
                <slot name="header">
                  <wy-titlebar part="wy-overlay-titlebar" header>
                    <wy-button slot="icon" kind="icon" @click=${() => this.close()}>
                      <wy-icon name="close"></wy-icon>
                    </wy-button>
                    <slot slot="title" name="title"></slot>
                    <slot slot="actions" name="actions"></slot>
                  </wy-titlebar>
                </slot>
              `}
          <slot></slot>
        </div>
      </dialog>
    `;
  }
  updated(t) {
    if (t.has("type")) {
      const e = !this.isModal() && this.viewportRef.value?.popover ? "toggle" : "close";
      this.viewportRef.value?.addEventListener(e, this.handleClose), this.show && (!this.isModal(t.get("type")) && this.viewportRef.value?.popover ? this.viewportRef.value?.hidePopover() : this.viewportRef.value?.close(), this.requestUpdate("show"));
    }
    if (t.has("show"))
      try {
        this.show ? this.isModal() ? this.viewportRef.value?.showModal() : this.viewportRef.value?.popover ? this.viewportRef.value?.showPopover() : this.viewportRef.value?.show() : !this.isModal() && this.viewportRef.value?.popover ? this.viewportRef.value?.hidePopover() : this.viewportRef.value?.close();
      } catch {
      }
    t.has("show") && this.show && this.viewportRef.value?.focus();
  }
  disconnectedCallback() {
    super.disconnectedCallback(), this.close();
  }
}, n(Mo, "WyOverlay"), Mo);
Xr.styles = [Kt, Y, _3, P3, S3, M3];
ih([
  y({ type: Boolean, reflect: !0 })
], Xr.prototype, "show", 2);
ih([
  y({ type: String })
], Xr.prototype, "type", 2);
ih([
  y({ type: Boolean })
], Xr.prototype, "maximized", 2);
ih([
  y({ type: Boolean })
], Xr.prototype, "noHeader", 2);
Xr = ih([
  U("wy-overlay")
], Xr);
const L3 = j`[part~=wy-container]{display:flex;flex-direction:column;position:relative;min-height:min-content;height:fit-content;flex:1 1 auto}[part~=wy-container-padded]{padding:var(--wy-padding, calc(.5 * var(--wy-size, 1rem)));gap:var(--wy-gap, var(--wy-padding, calc(.5 * var(--wy-size, 1rem))))}[part~=wy-container-outer]{padding:var(--wy-padding-outer, 0)}[part~=wy-container-gap-lg]{gap:var(--wy-gap-lg, calc(1.5 * var(--wy-gap, var(--wy-padding, calc(.5 * var(--wy-size, 1rem))))))}[part~=wy-container-gap-xl]{gap:var(--wy-gap-xl, calc(2 * var(--wy-gap, var(--wy-padding, calc(.5 * var(--wy-size, 1rem))))))}`, hn = j`[part~=wy-scroll-y]{overflow-y:auto;max-height:100%;overflow-x:hidden;overflow-anchor:none;touch-action:pan-y;will-change:scroll-position,opacity;backface-visibility:hidden}[part~=wy-scroll-y]:not(body){-webkit-overflow-scrolling:touch}[part~=wy-scroll-x]{overflow-x:auto;max-width:100%;overflow-y:hidden;overflow-anchor:none;touch-action:pan-x;will-change:scroll-position,opacity;backface-visibility:hidden}[part~=wy-scroll-x-y]{overflow-y:auto;max-height:100%;overflow-x:hidden;overflow-anchor:none;touch-action:pan-y;will-change:scroll-position,opacity;backface-visibility:hidden}[part~=wy-scroll-x-y]:not(body){-webkit-overflow-scrolling:touch}[part~=wy-scroll-x-y]{overflow-x:auto;max-width:100%;overflow-y:hidden;overflow-anchor:none;touch-action:pan-x;will-change:scroll-position,opacity;backface-visibility:hidden}[part~=wy-scroll-x-y]{overflow-y:auto;overflow-x:auto;touch-action:pan-y,pan-x}[part~=wy-scroll-x-always],[part~=wy-scroll-y-always]{overflow-y:scroll}`;
var O3 = Object.defineProperty, z3 = Object.getOwnPropertyDescriptor, un = /* @__PURE__ */ n((s, t, e, i) => {
  for (var r = i > 1 ? void 0 : i ? z3(t, e) : t, a = s.length - 1, o; a >= 0; a--)
    (o = s[a]) && (r = (i ? o(t, e, r) : o(r)) || r);
  return i && r && O3(t, e, r), r;
}, "__decorateClass$$"), Eo;
let zi = (Eo = class extends nt {
  constructor() {
    super(...arguments), this.exportParts = new W(this), this.scrollX = !1, this.scrollY = !1, this.padded = !1, this.outer = !1, this.scrollbars = !1, this.gap = "md";
  }
  render() {
    const t = {
      "wy-container": !0,
      "wy-container-padded": this.padded,
      "wy-container-outer": this.outer,
      "wy-container-gap-lg": this.gap === "lg",
      "wy-container-gap-xl": this.gap === "xl",
      "wy-scroll-x": this.scrollX && !this.scrollY,
      "wy-scroll-y": this.scrollY && !this.scrollX,
      "wy-scroll-x-y": this.scrollX && this.scrollY,
      "wy-scroll-x-always": this.scrollbars && this.scrollX,
      "wy-scroll-y-always": this.scrollbars && this.scrollY
    };
    return h`
      <div part=${tt(t)}>
        <slot></slot>
      </div>
    `;
  }
}, n(Eo, "WyContainer"), Eo);
zi.styles = [hn, Y, L3];
un([
  y({ type: Boolean })
], zi.prototype, "scrollX", 2);
un([
  y({ type: Boolean })
], zi.prototype, "scrollY", 2);
un([
  y({ type: Boolean })
], zi.prototype, "padded", 2);
un([
  y({ type: Boolean })
], zi.prototype, "outer", 2);
un([
  y({ type: Boolean })
], zi.prototype, "scrollbars", 2);
un([
  y()
], zi.prototype, "gap", 2);
zi = un([
  U("wy-container")
], zi);
var F3 = Object.defineProperty, I3 = Object.getOwnPropertyDescriptor, vl = /* @__PURE__ */ n((s, t, e, i) => {
  for (var r = i > 1 ? void 0 : i ? I3(t, e) : t, a = s.length - 1, o; a >= 0; a--)
    (o = s[a]) && (r = (i ? o(t, e, r) : o(r)) || r);
  return i && r && F3(t, e, r), r;
}, "__decorateClass$_"), Ao;
let ld = (Ao = class extends nt {
  constructor() {
    super(...arguments), this.exportParts = new W(this), this.pollOptions = [];
  }
  /**
   * Dispatch a `vote` event for the specified option.
   *
   * @param optionId - Identifier of the option to vote for.
   * @returns {boolean} True if the event was not canceled.
   */
  dispatchVote(t) {
    const e = new CustomEvent("vote", { detail: { optionId: t } });
    return this.dispatchEvent(e);
  }
  render() {
    const t = this.pollOptions.reduce((e, i) => e + (i.votes?.count || 0), 0);
    return h`
      <div part="wy-poll">
        ${this.pollOptions.map(
      (e) => h`<wy-poll-option
              @vote=${(i) => this.dispatchVote(i.detail.optionId)}
              .option=${e}
              .totalVotes=${t}
            ></wy-poll-option>`
    )}
      </div>
    `;
  }
}, n(Ao, "WyPoll"), Ao);
ld.styles = [
  qw,
  Y
];
vl([
  y({ type: Array, attribute: !1 })
], ld.prototype, "pollOptions", 2);
ld = vl([
  U("wy-poll")
], ld);
var Ro;
let Qs = (Ro = class extends Ae {
  constructor() {
    super(...arguments), this.exportParts = new W(this), this.totalVotes = 0, this.showSheet = !1, this.getVotesQuery = new ve(this);
  }
  async updated(t) {
    t.has("weavy") && this.weavy && this.option && this.option.id && await this.getVotesQuery.trackQuery(d5(this.weavy, this.option.id));
  }
  /**
   * Dispatch a `vote` event for the provided option id.
   *
   * @internal
   * @param optionId - Identifier of the option to vote for.
   * @returns {boolean} True if the event was not canceled.
   */
  dispatchVote(t) {
    if (!t)
      return;
    const e = new CustomEvent("vote", { detail: { optionId: t } });
    return this.dispatchEvent(e);
  }
  /**
   * Toggle the voter sheet and refresh vote data.
   *
   * @internal
   * @param e - Triggering user event.
   */
  openSheet(t) {
    t.preventDefault(), t.stopPropagation(), this.getVotesQuery.observer?.refetch(), this.showSheet = !this.showSheet;
  }
  render() {
    if (!this.option || !this.option.id)
      return g;
    const { data: t, isLoading: e } = this.getVotesQuery.result ?? {}, i = this.totalVotes > 0 ? Math.round((this.option.votes?.count || 0) / this.totalVotes * 100) : 0;
    return h`
      <div
        part="wy-poll-option"
        tabindex="0"
        @click=${() => this.dispatchVote(this.option?.id)}
        @keydown=${ne}
        @keyup=${ke}
      >
        <div part="wy-poll-meter" style="width: ${i + "%"}"></div>
        <div part="wy-poll-image">
          ${this.option.has_voted ? h`<wy-icon name="check-circle"></wy-icon>` : h`<wy-icon name="circle-outline"></wy-icon>`}
        </div>
        <div part="wy-poll-title">${this.option.text}</div>
        ${i > 0 ? h`<span
              part="wy-poll-amount"
              tabindex="0"
              @click=${(r) => this.openSheet(r)}
              @keydown=${ne}
              @keyup=${ke}
            >
              ${i + "%"}
            </span>` : h`<span part="wy-poll-amount"></span>`}
      </div>

      ${this.weavy ? h`
            <wy-overlay type="sheet" .show=${this.showSheet} @close=${() => this.showSheet = !1}>
              <span slot="title">${x(gt`Votes on ${this.option.text}`)}</span>
              <wy-container scrollY padded>
                ${this.showSheet && t && !e ? h`
                      ${t.votes?.data ? t.votes.data.map(
      (r) => h`
                              <wy-item>
                                <wy-avatar
                                  slot="image"
                                  .size=${32}
                                  .src=${r.avatar_url}
                                  .name=${r.name}
                                ></wy-avatar>
                                <span slot="title">${r.name}</span>
                              </wy-item>
                            `
    ) : g}
                    ` : g}
              </wy-container>
            </wy-overlay>
          ` : g}
    `;
  }
}, n(Ro, "WyPollOption"), Ro);
Qs.styles = [qw, Y];
vl([
  y({ type: Number, attribute: !1 })
], Qs.prototype, "totalVotes", 2);
vl([
  y({ attribute: !1 })
], Qs.prototype, "option", 2);
vl([
  V()
], Qs.prototype, "showSheet", 2);
Qs = vl([
  U("wy-poll-option"),
  ut()
], Qs);
function D3(s, t, e, i, r, a, o) {
  return new St(s.queryClient, {
    mutationFn: /* @__PURE__ */ n(async () => await s.fetch(`/api/${r}/${i}/reactions`, {
      method: a ? "POST" : "DELETE",
      body: JSON.stringify({ content: a })
    }), "mutationFn"),
    onMutate: /* @__PURE__ */ n(() => {
      const c = e === "apps" && r === "posts" ? [r, t] : [e, t, r];
      Ee(
        s.queryClient,
        c,
        i,
        (l) => sl(l, a, o)
      ), e === "apps" && r === "posts" && Ee(
        s.queryClient,
        ["posts", "feed"],
        i,
        (l) => sl(l, a, o)
      );
    }, "onMutate")
  });
}
n(D3, "reactionMutation");
function V3(s, t, e) {
  return {
    queryKey: [t, e, "reactions"],
    enabled: !1,
    queryFn: /* @__PURE__ */ n(async () => await (await s.fetch("/api/" + t + "/" + e + "/reactions")).json(), "queryFn")
  };
}
n(V3, "getReactionListOptions");
function sl(s, t, e) {
  return !t && !s.reactions?.data || (s.reactions || (s.reactions = { count: 0, data: [] }), s.reactions.data || (s.reactions.data = []), t ? s.reactions.data = [
    ...s.reactions.data.filter((i) => i.created_by?.id !== e.id),
    { content: t, created_by: e }
  ] : s.reactions.data && (s.reactions.data = [...s.reactions.data.filter((i) => i.created_by?.id !== e.id)])), s;
}
n(sl, "updateReaction");
const H3 = j`[part~=wy-reaction]{font-size:calc(1.125 * var(--wy-size, 1rem))}[part~=wy-reactions]{display:inline-flex;align-items:center;column-gap:calc(.25 * var(--wy-size, 1rem))}[part~=wy-reactions] :first-child:not(:only-child){margin-left:calc(.25 * var(--wy-size, 1rem))}[part~=wy-reactions] :last-child:not(:only-child){margin-right:calc(.25 * var(--wy-size, 1rem))}[part~=wy-reaction-count]{margin:0 calc(.25 * var(--wy-size, 1rem)) 0;line-height:1;font-size:var(--wy-font-size-sm, calc(.875 * var(--wy-font-size, 1em)))}[part~=wy-reaction-menu]{width:max-content;position:absolute;z-index:1000;border:none;--wy-component-background-color: var(--wy-surface-container-lowest, var(--wy-surface-container-lowest-light, #ffffff));--wy-component-color: var(--wy-on-surface, var(--wy-on-surface-light, #181c20));background-color:var(--wy-component-background-color);color:var(--wy-component-color);display:flex;margin:0;text-align:start;background-clip:padding-box;box-shadow:var(--wy-shadow-level1, 0 calc(.125 * var(--wy-size, 1rem)) calc(.125 * var(--wy-size, 1rem)) 0 rgba(0, 0, 0, .14), 0 calc(.1875 * var(--wy-size, 1rem)) calc(.0625 * var(--wy-size, 1rem)) calc(-.125 * var(--wy-size, 1rem)) rgba(0, 0, 0, .2), 0 calc(.0625 * var(--wy-size, 1rem)) calc(.3125 * var(--wy-size, 1rem)) 0 rgba(0, 0, 0, .12));overflow:hidden;padding:var(--wy-padding-sm, calc(.75 * var(--wy-padding, calc(.5 * var(--wy-size, 1rem)))));font-size:calc(1.25 * var(--wy-size, 1rem));border-radius:var(--wy-border-radius-pill, var(--wy-border-radius, calc(2.5 * var(--wy-size, 1rem))))}[part~=wy-reaction-picker]{display:flex;flex-direction:row;gap:var(--wy-gap-sm, calc(.5 * var(--wy-gap, var(--wy-padding, calc(.5 * var(--wy-size, 1rem))))))}[part~=wy-reactions-line]{display:flex;align-items:center;gap:calc(.1875 * var(--wy-size, 1rem));justify-content:end}[part~=wy-reactions-line-reverse]{flex-direction:row-reverse;justify-content:start}[part~=wy-reactions-line-bottom]{position:absolute;bottom:calc(-1 * var(--wy-size, 1rem));right:calc(.25 * var(--wy-size, 1rem));z-index:1}[part~=wy-reactions-line-below]{position:relative;height:0;min-height:0;max-height:0;margin:calc(var(--wy-message-padding, var(--wy-padding-lg, calc(1.5 * var(--wy-padding, calc(.5 * var(--wy-size, 1rem)))))) - var(--wy-gap, var(--wy-padding, calc(.5 * var(--wy-size, 1rem))))) calc(-1 * var(--wy-message-padding, var(--wy-padding-lg, calc(1.5 * var(--wy-padding, calc(.5 * var(--wy-size, 1rem))))))) calc(-1 * var(--wy-message-padding, var(--wy-padding-lg, calc(1.5 * var(--wy-padding, calc(.5 * var(--wy-size, 1rem)))))));padding-left:calc(.1875 * var(--wy-size, 1rem));padding-right:calc(.1875 * var(--wy-size, 1rem));gap:calc(.125 * var(--wy-size, 1rem));flex:1 0 auto;justify-content:flex-end}`, hg = j`[part~=wy-emoji-icon]{display:inline-flex;justify-content:center;align-items:center;line-height:1;width:1em;height:1em;font-size:calc(1.25 * var(--wy-size, 1rem))}[part~=wy-emoji-icon-sm]{width:calc(1.125 * var(--wy-size, 1rem));height:calc(1.125 * var(--wy-size, 1rem));font-size:calc(1.125 * var(--wy-size, 1rem))}`;
var U3 = Object.defineProperty, B3 = Object.getOwnPropertyDescriptor, oe = /* @__PURE__ */ n((s, t, e, i) => {
  for (var r = i > 1 ? void 0 : i ? B3(t, e) : t, a = s.length - 1, o; a >= 0; a--)
    (o = s[a]) && (r = (i ? o(t, e, r) : o(r)) || r);
  return i && r && U3(t, e, r), r;
}, "__decorateClass$Z"), To;
let Yt = (To = class extends qt {
  constructor() {
    super(...arguments), this.exportParts = new W(this), this.directionX = "left", this.directionY = "up", this.small = !1, this.reactions = [], this.emojis = "", this.parentType = "apps", this.entityType = "messages", this.line = !1, this.lineReverse = !1, this.lineBottom = !1, this.lineBelow = !1, this._placement = "bottom-start", this.show = !1, this.showSheet = !1, this.buttonRef = J(), this.menuRef = J(), this.reactionListQuery = new ve(this), this._documentClickHandler = (t) => {
      this.show && (t.preventDefault(), this.menuRef.value?.popover || (this.show = !1));
    }, this.handleReaction = async (t) => {
      if (!this.weavy || !this.parentId || !this.user)
        return;
      const e = D3(
        this.weavy,
        this.parentId,
        this.parentType,
        this.entityId,
        this.entityType,
        this.reactedEmoji === t ? void 0 : t,
        this.user
      );
      this.reactedEmoji = t, await e.mutate(), this.reactionListQuery.observer?.refetch();
    };
  }
  /**
   * Closes the picker when the popover hides.
   *
   * @internal
   */
  handleClose(t) {
    if (t.type === "toggle" && t.newState === "closed" || t.type === "click") {
      this.show = !1;
      const e = new CustomEvent("close");
      this.dispatchEvent(e);
    }
  }
  /**
   * Toggles the emoji picker visibility.
   *
   * @internal
   */
  handleClickToggle(t) {
    t.stopPropagation(), this.show = !this.show;
  }
  /**
   * Opens the detailed reaction sheet view.
   *
   * @internal
   */
  handleReactionsClick() {
    this.reactionListQuery.observer?.refetch(), this.showSheet = !this.showSheet, this.show = !1;
  }
  async willUpdate(t) {
    if (super.willUpdate(t), (t.has("weavy") || t.has("entityId") || t.has("showSheet")) && this.weavy && this.entityId && this.showSheet && await this.reactionListQuery.trackQuery(V3(this.weavy, this.entityType, this.entityId)), (t.has("reactions") || t.has("user")) && this.user && (this.reactedEmoji = this.reactions?.find((e) => e.created_by?.id === this.user?.id)?.content), (t.has("directionX") || t.has("directionY")) && (this._placement = this.directionX === "right" && this.directionY === "down" ? "bottom-start" : this.directionX === "left" && this.directionY === "down" ? "bottom-end" : this.directionX === "right" && this.directionY === "up" ? "top-start" : "top-end"), t.has("show") && (this.show && !this._computePositionCleanup && this.buttonRef.value && this.menuRef.value ? this._computePositionCleanup = G1(this.buttonRef.value, this.menuRef.value, () => {
      requestAnimationFrame(() => {
        this.buttonRef.value && this.menuRef.value && J1(this.buttonRef.value, this.menuRef.value, {
          placement: this._placement,
          strategy: this.menuRef.value.popover ? "absolute" : "fixed",
          middleware: [
            X1(),
            Z1({ mainAxis: 0, alignmentAxis: -8 }),
            Y1({ mainAxis: !0, crossAxis: !0, padding: 4, altBoundary: !0 })
          ]
        }).then(({ x: e, y: i }) => {
          this.menuRef.value && Object.assign(this.menuRef.value.style, {
            marginLeft: `${e}px`,
            marginTop: `${i}px`,
            top: 0,
            left: 0,
            position: this.menuRef.value.popover ? void 0 : "fixed",
            zIndex: this.menuRef.value.popover ? void 0 : 1075
          });
        });
      });
    }) : !this.show && this._computePositionCleanup && (this._computePositionCleanup(), this._computePositionCleanup = void 0)), this.show) {
      requestAnimationFrame(() => {
        document.addEventListener("click", this._documentClickHandler, { once: !0, capture: !0 });
      });
      try {
        this.menuRef.value?.showPopover();
      } catch {
      }
    } else
      try {
        this.menuRef.value?.hidePopover();
      } catch {
      }
    t.has("settings") && this.settings?.reactions && this.emojis != this.settings.reactions && (this.emojis = this.settings.reactions);
  }
  render() {
    const { data: t, isPending: e } = this.reactionListQuery.result ?? {};
    if (!this.emojis?.length)
      return g;
    const i = this.emojis?.length === 1 ? this.emojis[0] : "", r = [
      ...new Map(this.reactions?.map((u) => [u.content, u])).values()
    ], a = {
      "wy-emoji-icon": !0,
      "wy-emoji-icon-sm": this.small
    }, o = i ? h`
          ${this.reactions && this.reactions?.length > 1 ? h`<wy-button
                kind="icon-inline"
                ?active=${this.showSheet}
                ?small=${this.small}
                @click=${() => this.handleReactionsClick()}
              >
                <div part="wy-reactions">
                  <span part=${tt(a)}>
                    <small part="wy-reaction-count">${this.reactions.length}</small>
                  </span>
                </div>
              </wy-button>` : g}

          <div>
            <wy-button
              part="wy-reaction-menu-button"
              color="inherit"
              kind="icon"
              ?small=${this.small}
              ?active=${this.reactedEmoji === i}
              @click=${() => {
      this.handleReaction(i);
    }}
              @keydown=${ne}
              @keyup=${ke}
              title=${x("React", { desc: "Button action to react" })}
            >
              <span part=${tt(a)} title=${i}>${i}</span>
            </wy-button>
          </div>
        ` : h`
          ${r.length ? h`
                <wy-button
                  kind="icon-inline"
                  ?active=${this.showSheet}
                  ?small=${this.small}
                  @click=${() => this.handleReactionsClick()}
                >
                  <div part="wy-reactions">
                    ${r.map((u) => h`<span part=${tt(a)} title="">${u.content}</span>`)}
                    ${this.reactions && this.reactions?.length > 1 ? h`<small part="wy-reaction-count">${this.reactions.length}</small>` : g}
                  </div>
                </wy-button>
              ` : g}

          <div ${K(this.buttonRef)}>
            <wy-button
              part="wy-reaction-menu-button"
              color="inherit"
              kind="icon"
              ?active=${this.show}
              ?small=${this.small}
              @click=${(u) => this.handleClickToggle(u)}
              @keydown=${ne}
              @keyup=${ke}
              title=${x("React", { desc: "Button action to react" })}
            >
              <wy-icon name="emoticon" size=${this.small ? 20 : 24}></wy-icon>
            </wy-button>
          </div>

          <div
            ${K(this.menuRef)}
            part="wy-reaction-menu"
            @click=${(u) => this.handleClickToggle(u)}
            @keyup=${Lw}
            ?hidden=${!this.show}
            popover=${ot(Vn() ? void 0 : "auto")}
          >
            <div part="wy-reaction-picker">
              ${this.emojis.split(" ").map(
      (u) => h`
                    <wy-button
                      kind="icon"
                      color="none"
                      ?active=${this.reactedEmoji === u}
                      @click=${() => {
        this.handleReaction(u);
      }}
                    >
                      <span part="wy-emoji-icon">${u}</span>
                    </wy-button>
                  `
    )}
            </div>
          </div>
        `, c = h`
      ${this.weavy && this.showSheet ? h`
            <wy-overlay type="sheet" .show=${this.showSheet} @close=${() => this.showSheet = !1}>
              <span slot="title">${x("Reactions")}</span>
              <wy-container scrollY padded>
                ${this.showSheet && t && !e ? h`
                      ${t.data?.map(
      (u) => h` <wy-reaction-item .reaction=${u}></wy-reaction-item> `
    )}
                    ` : g}
              </wy-container>
            </wy-overlay>
          ` : g}
    `, l = {
      "wy-reactions-line": !0,
      "wy-reactions-line-reverse": this.lineReverse,
      "wy-reactions-line-bottom": this.lineBottom,
      "wy-reactions-line-below": this.lineBelow
    };
    return this.line || this.lineReverse || this.lineBottom || this.lineBelow ? h`
          <div part=${tt(l)}>${o}</div>
          ${c}
        ` : [o, c];
  }
  firstUpdated(t) {
    this.menuRef.value?.addEventListener(
      this.menuRef.value.popover ? "toggle" : "click",
      (e) => this.handleClose(e)
    );
  }
  disconnectedCallback() {
    this._computePositionCleanup?.(), super.disconnectedCallback();
  }
}, n(To, "WyReactions"), To);
Yt.styles = [Lt, H3, hg, Y];
oe([
  y()
], Yt.prototype, "directionX", 2);
oe([
  y()
], Yt.prototype, "directionY", 2);
oe([
  y({ type: Boolean })
], Yt.prototype, "small", 2);
oe([
  y({ attribute: !1 })
], Yt.prototype, "reactions", 2);
oe([
  y({ attribute: !1 })
], Yt.prototype, "emojis", 2);
oe([
  y({ type: String })
], Yt.prototype, "parentType", 2);
oe([
  y({ attribute: !0, type: Number })
], Yt.prototype, "parentId", 2);
oe([
  y({ attribute: !0, type: String })
], Yt.prototype, "entityType", 2);
oe([
  y({ attribute: !0, type: Number })
], Yt.prototype, "entityId", 2);
oe([
  y({ type: Boolean })
], Yt.prototype, "line", 2);
oe([
  y({ type: Boolean })
], Yt.prototype, "lineReverse", 2);
oe([
  y({ type: Boolean })
], Yt.prototype, "lineBottom", 2);
oe([
  y({ type: Boolean })
], Yt.prototype, "lineBelow", 2);
oe([
  V()
], Yt.prototype, "_placement", 2);
oe([
  V()
], Yt.prototype, "reactedEmoji", 2);
oe([
  V()
], Yt.prototype, "show", 2);
oe([
  V()
], Yt.prototype, "showSheet", 2);
Yt = oe([
  U("wy-reactions"),
  ut()
], Yt);
var Lo;
let dd = (Lo = class extends nt {
  constructor() {
    super(...arguments), this.exportParts = new W(this);
  }
  render() {
    return h`
      <wy-item>
        <wy-avatar
          slot="image"
          .src=${this.reaction.created_by?.avatar_url}
          .name=${this.reaction.created_by?.name}
        ></wy-avatar>
        <span slot="title">${this.reaction.created_by?.name}</span>
        <span slot="actions" part="wy-emoji-icon">${this.reaction.content}</span>
      </wy-item>
    `;
  }
}, n(Lo, "WyReactionItem"), Lo);
dd.styles = [Lt, hg];
oe([
  y({ attribute: !1 })
], dd.prototype, "reaction", 2);
dd = oe([
  U("wy-reaction-item")
], dd);
const N3 = j`[part~=wy-preview]{--wy-component-background-color: var(--wy-background, var(--wy-background-light, #f7f9ff));--wy-component-color: var(--wy-on-background, var(--wy-on-background-light, #181c20));background-color:var(--wy-component-background-color);color:var(--wy-component-color);display:flex;flex-direction:column;box-sizing:border-box;align-items:stretch;justify-content:stretch;position:relative;flex:1 1 100%;min-width:calc(16 * var(--wy-size, 1rem));min-height:calc(16 * var(--wy-size, 1rem));max-height:100%;max-width:100%}[part~=wy-preview-layout]{width:100%;height:100%}[part~=wy-preview-main]{display:flex;height:calc(100% - var(--wy-titlebar-height, calc(max(1lh, var(--wy-button-size, calc(2.5 * var(--wy-size, 1rem)))) + 2 * var(--wy-padding-sm, calc(.25 * var(--wy-size, 1rem))))));min-height:0}@container (inline-size < 768px){[part~=wy-preview-main]{flex-direction:column}}[part~=wy-preview-swiper]{display:flex;flex-direction:row;flex:1 1 100%;align-items:center;justify-content:start;min-width:calc(16 * var(--wy-size, 1rem));min-height:calc(16 * var(--wy-size, 1rem));max-height:100%;max-width:100%;overflow-x:scroll;scroll-snap-type:x mandatory;scroll-behavior:auto;overscroll-behavior-x:none;scrollbar-width:none}[part~=wy-preview-swiper]::-webkit-scrollbar{display:none}[part~=wy-preview-swiper-disabled]{overflow-x:hidden}[part~=wy-preview-area]{position:relative;min-width:100%;min-height:100%;width:100%;height:100%;flex:1 1 auto;display:flex;flex-direction:column;scroll-snap-stop:always;scroll-snap-align:center}[part~=wy-nav-prev],[part~=wy-nav-next]{--wy-component-background-color: var(--wy-surface-variant, var(--wy-surface-variant-light, #dee3eb));--wy-component-color: var(--wy-on-surface-variant, var(--wy-on-surface-variant-light, #42474e));background-color:var(--wy-component-background-color);position:absolute;top:50%;z-index:1000;border-radius:50%}[part~=wy-nav-prev]{left:calc(.5 * var(--wy-size, 1rem))}[part~=wy-nav-next]{right:calc(.5 * var(--wy-size, 1rem))}`, q3 = j`[part~=wy-sidebar]{--wy-component-background-color: var(--wy-surface-container-low, var(--wy-surface-container-low-light, #f1f3f9));--wy-component-color: var(--wy-on-surface, var(--wy-on-surface-light, #181c20));background:var(--wy-component-background-color);color:var(--wy-component-color);min-width:0;min-height:0;flex-grow:0;flex-shrink:0;flex-basis:calc(22 * var(--wy-size, 1rem));z-index:10;display:flex;flex-direction:column;transition:flex-basis var(--wy-transition, var(--wy-transition-duration, .2s) var(--wy-transition-curve, cubic-bezier(.4, 0, .2, 1)));position:relative;order:9999;overflow:clip}[part~=wy-sidebar]>*{transition:opacity var(--wy-transition-fast, var(--wy-transition-duration-fast, .1s) var(--wy-transition-curve, cubic-bezier(.4, 0, .2, 1))) calc(var(--wy-transition-duration, .2s) - var(--wy-transition-duration-fast, .1s));min-width:calc(22 * var(--wy-size, 1rem))}[part~=wy-sidebar][hidden]{display:flex!important;flex-basis:0}[part~=wy-sidebar][hidden]>*{transition:opacity var(--wy-transition-fast, var(--wy-transition-duration-fast, .1s) var(--wy-transition-curve, cubic-bezier(.4, 0, .2, 1)));opacity:0}@container (inline-size >= 768px){[part~=wy-sidebar]>*{min-width:calc(22 * var(--wy-size, 1rem))}[part~=wy-sidebar] [part~=wy-sidebar-handle]{display:none}}@container (inline-size < 768px){[part~=wy-sidebar]{transition:flex-basis var(--wy-transition, var(--wy-transition-duration, .2s) var(--wy-transition-curve, cubic-bezier(.4, 0, .2, 1))),margin-top var(--wy-transition, var(--wy-transition-duration, .2s) var(--wy-transition-curve, cubic-bezier(.4, 0, .2, 1)));border-top-left-radius:var(--wy-border-radius, calc(.5 * var(--wy-size, 1rem)));border-top-right-radius:var(--wy-border-radius, calc(.5 * var(--wy-size, 1rem)));flex-basis:50vh}[part~=wy-sidebar]:not([hidden]){margin-top:calc(-1 * var(--wy-border-radius, calc(.5 * var(--wy-size, 1rem))))}[part~=wy-sidebar]:not([hidden])[part~=wy-maximized]{flex-basis:calc(100% - var(--wy-titlebar-height, calc(max(1lh, var(--wy-button-size, calc(2.5 * var(--wy-size, 1rem)))) + 2 * var(--wy-padding-sm, calc(.25 * var(--wy-size, 1rem))))) * .5);margin-top:calc(-50% + var(--wy-titlebar-height, calc(max(1lh, var(--wy-button-size, calc(2.5 * var(--wy-size, 1rem)))) + 2 * var(--wy-padding-sm, calc(.25 * var(--wy-size, 1rem))))) * 2.5 - var(--wy-border-radius, calc(.5 * var(--wy-size, 1rem))))}[part~=wy-sidebar]:not([hidden])[part~=wy-maximized]~* [part~=wy-nav-prev],[part~=wy-sidebar]:not([hidden])[part~=wy-maximized]~* [part~=wy-nav-next]{display:none}[part~=wy-sidebar]>*{min-height:var(--wy-titlebar-height, calc(max(1lh, var(--wy-button-size, calc(2.5 * var(--wy-size, 1rem)))) + 2 * var(--wy-padding-sm, calc(.25 * var(--wy-size, 1rem)))))}}[part~=wy-sidebar-handle]{width:calc(4 * var(--wy-size, 1rem));background-clip:content-box;background-color:var(--wy-on-surface-variant, var(--wy-on-surface-variant-light, #42474e));height:calc(1 * var(--wy-size, 1rem));border:calc(.375 * var(--wy-size, 1rem)) solid transparent;position:absolute;top:0;z-index:2;left:50%;margin-left:calc(-2 * var(--wy-size, 1rem));border-radius:var(--wy-border-radius-pill, var(--wy-border-radius, calc(2.5 * var(--wy-size, 1rem))));cursor:pointer}`;
var W3 = Object.defineProperty, j3 = Object.getOwnPropertyDescriptor, Nu = /* @__PURE__ */ n((s, t, e, i) => {
  for (var r = i > 1 ? void 0 : i ? j3(t, e) : t, a = s.length - 1, o; a >= 0; a--)
    (o = s[a]) && (r = (i ? o(t, e, r) : o(r)) || r);
  return i && r && W3(t, e, r), r;
}, "__decorateClass$Y"), Oo;
let Gs = (Oo = class extends nt {
  constructor() {
    super(...arguments), this.exportParts = new W(this);
  }
  /**
   * Dispatch `file-preview-loaded` when the fallback content is ready.
   *
   * @returns {boolean} True if the event was not canceled.
   */
  dispatchLoaded() {
    const t = new CustomEvent("file-preview-loaded");
    return this.dispatchEvent(t);
  }
  render() {
    const t = this.icon, e = Zd(this.provider);
    return h`
      <wy-icon-display fill>
        <wy-icon name=${t} .overlayName=${e}></wy-icon>
        <span slot="text">
          ${this.provider ? h`
                <p>${x("No preview available :(")} </p>
                <wy-button kind="filled" color="variant" href=${this.src} target="_blank">${x(gt`Open in ${this.provider}`)}</wy-button>
              ` : h`<div>${x("No preview available :(")}</div>`}
        </span>
      </wy-icon-display>
    `;
  }
  updated(t) {
    (t.has("icon") || t.has("src")) && (this.icon || this.src) && this.dispatchLoaded();
  }
}, n(Oo, "WyPreviewIcon"), Oo);
Gs.styles = [
  Y
];
Nu([
  y()
], Gs.prototype, "src", 2);
Nu([
  y()
], Gs.prototype, "icon", 2);
Nu([
  y()
], Gs.prototype, "provider", 2);
Gs = Nu([
  U("wy-preview-icon"),
  ut()
], Gs);
const K3 = j`[part~=wy-content-image]{min-width:0;display:flex;align-items:center;justify-content:center;position:relative;margin:auto;box-sizing:border-box;max-height:100%;max-width:100%;transition:transform var(--wy-transition, var(--wy-transition-duration, .2s) var(--wy-transition-curve, cubic-bezier(.4, 0, .2, 1))),transform-origin var(--wy-transition-duration, .2s) step-end}[part~=wy-content-image][part~=wy-zoom]{cursor:zoom-out;transition:transform var(--wy-transition, var(--wy-transition-duration, .2s) var(--wy-transition-curve, cubic-bezier(.4, 0, .2, 1))),transform-origin var(--wy-transition-duration, .2s) step-start}[part~=wy-content-image-img]{min-width:0;min-height:0;width:100%;height:100%}[part~=wy-content-image-img][part~=wy-loading]{transition:opacity .15s ease-out;opacity:0}[part~=wy-content-image-img][part~=wy-loading][part~=wy-loaded]{opacity:1;transition:opacity .15s ease-out}[part~=wy-content-image-img][part~=wy-loading][part~=wy-loaded]~[part~=wy-content-progress]{display:none}[part~=wy-content-image-img]~[part~=wy-content-progress]{position:absolute}`;
var Q3 = Object.defineProperty, G3 = Object.getOwnPropertyDescriptor, qu = /* @__PURE__ */ n((s, t, e, i) => {
  for (var r = i > 1 ? void 0 : i ? G3(t, e) : t, a = s.length - 1, o; a >= 0; a--)
    (o = s[a]) && (r = (i ? o(t, e, r) : o(r)) || r);
  return i && r && Q3(t, e, r), r;
}, "__decorateClass$X"), zo;
let Zs = (zo = class extends nt {
  constructor() {
    super(...arguments), this.exportParts = new W(this);
  }
  /**
   * Dispatches the `file-preview-loaded` event once the image has loaded.
   *
   * @internal
   */
  dispatchLoaded() {
    const t = new CustomEvent("file-preview-loaded");
    return this.dispatchEvent(t);
  }
  render() {
    const t = !!(this.width && this.height), e = t ? { "--width": this.width, "--height": this.height } : {};
    return h`
      <div part="wy-content-image ${tt({ "wy-intrinsic-image": !t })}" style=${Jl(e)}>
        <img
          part="wy-content-image-img"
          src=${this.src}
          ${K(Eu)}
          @load=${(i) => {
      Au(i), this.dispatchLoaded();
    }}
          width=${ot(this.width)}
          height=${ot(this.height)}
          decoding="async"
          alt=${x("Preview")}
        />
        ${t ? h`<wy-progress-circular part="wy-content-progress" indeterminate overlay></wy-progress-circular>` : g}
      </div>
    `;
  }
}, n(zo, "WyPreviewImage"), zo);
Zs.styles = [K3, Y];
qu([
  y()
], Zs.prototype, "src", 2);
qu([
  y({ type: Number })
], Zs.prototype, "width", 2);
qu([
  y({ type: Number })
], Zs.prototype, "height", 2);
Zs = qu([
  U("wy-preview-image"),
  ut()
], Zs);
function ug(s) {
  (s.classList.contains("wy-loading") || s.part.contains("wy-loading")) && (s.classList.add("wy-loaded"), s.part.add("wy-loaded")), s.classList.add("wy-error"), s.part.add("wy-error"), s.outerHTML = s.outerHTML.replace(/<(video|audio)/, "<div").replace(/(video|audio)>/, "div>");
}
n(ug, "mediaFallback");
function Z3(s) {
  const t = s.target;
  (t.tagName === "VIDEO" || t.tagName === "AUDIO") && (t.classList.contains("wy-loading") || t.part.contains("wy-loading")) && (t.classList.add("wy-loaded"), t.part.add("wy-loaded"));
}
n(Z3, "mediaLoaded");
function Zm(s) {
  const t = s.target;
  if (t.tagName === "SOURCE" && t.parentNode) {
    const e = t.parentNode, i = parseInt(e.dataset.errors || "0") + 1;
    e.dataset.errors = i.toString(), e.querySelectorAll("source").length >= i && (console.warn(e.tagName.toLowerCase() + " source error, switching to fallback"), ug(e));
  }
}
n(Zm, "mediaError");
function Ym(s) {
  const t = s.target;
  (t.tagName === "VIDEO" || t.tagName === "AUDIO") && (t.tagName === "VIDEO" && !t.videoWidth || t.tagName === "AUDIO" && !t.duration) && (console.warn(t.tagName.toLowerCase() + " track not available, switching to fallback"), ug(t));
}
n(Ym, "codecError");
const Y3 = j`[part~=wy-content-video]{box-sizing:border-box}[part~=wy-content-video]:not([part~=wy-error]){min-height:0;max-width:100%;max-height:100%;flex:1 1 auto}[part~=wy-content-video],[part~=wy-content-audio]{margin:auto}[part~=wy-content-video][part~=wy-loading]:not([part~=wy-loaded]),[part~=wy-content-audio][part~=wy-loading]:not([part~=wy-loaded]){visibility:hidden}[part~=wy-content-video][part~=wy-loading][part~=wy-loaded]~[part~=wy-content-progress],[part~=wy-content-audio][part~=wy-loading][part~=wy-loaded]~[part~=wy-content-progress]{display:none}[part~=wy-content-video]:focus,[part~=wy-content-audio]:focus{outline:none}`;
var X3 = Object.defineProperty, J3 = Object.getOwnPropertyDescriptor, gl = /* @__PURE__ */ n((s, t, e, i) => {
  for (var r = i > 1 ? void 0 : i ? J3(t, e) : t, a = s.length - 1, o; a >= 0; a--)
    (o = s[a]) && (r = (i ? o(t, e, r) : o(r)) || r);
  return i && r && X3(t, e, r), r;
}, "__decorateClass$W"), Fo;
let hr = (Fo = class extends nt {
  constructor() {
    super(...arguments), this.exportParts = new W(this), this.format = "", this.play = !1, this.name = "", this.handleLoaded = (t) => {
      Z3(t), this.dispatchLoaded();
    };
  }
  /**
   * Dispatches the `file-preview-loaded` event after the media metadata becomes available.
   *
   * @internal
   * @returns {boolean} `true` if the event was not canceled.
   */
  dispatchLoaded() {
    const t = new CustomEvent("file-preview-loaded");
    return this.dispatchEvent(t);
  }
  /**
   * Registers load and error listeners on the provided media element.
   *
   * @internal
   * @param mediaElement - Media element to observe.
   */
  registerLoading(t) {
    this.unregisterLoading(), t && (this.mediaElement = t, t.part.add("wy-loading"), t.addEventListener("error", Zm, !0), t.addEventListener("loadedmetadata", this.handleLoaded, !0), t.addEventListener("loadedmetadata", Ym, !0));
  }
  /**
   * Cleans up listeners and state from the previously registered media element.
   *
   * @internal
   */
  unregisterLoading() {
    this.mediaElement && (this.mediaElement.pause(), this.mediaElement.removeAttribute("autoplay"), this.mediaElement.setAttribute("preload", "none"), this.mediaElement.removeEventListener("error", Zm, !0), this.mediaElement.removeEventListener("loadedmetadata", this.handleLoaded, !0), this.mediaElement.removeEventListener("loadedmetadata", Ym, !0), this.mediaElement = void 0);
  }
  render() {
    return this.format === "video" ? h`
          <video ${K((t) => this.registerLoading(t))} part="wy-content-video" controls crossorigin="use-credentials">
            <source src=${this.src} type=${ot(this.mediaType)} />
            <wy-preview-icon src=${this.src} icon="file-video"></wy-preview-icon>
          </video>
          <wy-progress-circular part="wy-content-progress" indeterminate overlay></wy-progress-circular>
        ` : h`
          <audio ${K((t) => this.registerLoading(t))} part="wy-content-audio" controls crossorigin="use-credentials">
            <source src=${this.src} type=${ot(this.mediaType)} />
          </audio>
        `;
  }
  updated(t) {
    super.updated(t), t.has("play") && this.mediaElement && (this.play ? this.mediaElement.play() : this.mediaElement.pause());
  }
  disconnectedCallback() {
    this.unregisterLoading(), super.disconnectedCallback();
  }
}, n(Fo, "WyPreviewMedia"), Fo);
hr.styles = [
  Y3,
  Y
];
gl([
  y()
], hr.prototype, "format", 2);
gl([
  y()
], hr.prototype, "src", 2);
gl([
  y({ type: Boolean })
], hr.prototype, "play", 2);
gl([
  y()
], hr.prototype, "name", 2);
gl([
  y()
], hr.prototype, "mediaType", 2);
hr = gl([
  U("wy-preview-media")
], hr);
const tC = j`[part~=wy-content-code]{align-self:flex-start;box-sizing:border-box;width:100%;padding:calc(2 * var(--wy-size, 1rem)) calc(3.5 * var(--wy-size, 1rem));font-size:var(--wy-font-size-sm, calc(.875 * var(--wy-font-size, 1em)))}`, eC = j`[part~=wy-content-text],[part~=wy-content-html] pre,[part~=wy-content-html] code{word-break:break-word;white-space:pre-wrap}[part~=wy-content-html] hr{border-color:var(--wy-outline, var(--wy-outline-light, #72777f));border-width:var(--wy-border, 1px) 0 0 0;border-style:solid}[part~=wy-content-html] h1,[part~=wy-content-html] h2,[part~=wy-content-html] h3,[part~=wy-content-html] h4,[part~=wy-content-html] h5,[part~=wy-content-html] h6{font-family:var(--wy-document-headings-font-family, var(--wy-headings-font-family, var(--wy-font-family, unset)));line-height:var(--wy-document-headings-line-height, var(--wy-headings-line-height, var(--wy-line-height, 1.5)))}[part~=wy-content-html] h1{font-size:var(--wy-document-h1-font-size, calc(2 * var(--wy-font-size, 1em)));font-weight:var(--wy-document-h1-font-weight, var(--wy-font-weight-bold, 600));margin:var(--wy-document-h1-margin, 0 0 var(--wy-gap, calc(.5 * var(--wy-size, 1rem))))}[part~=wy-content-html] h2{font-size:var(--wy-document-h2-font-size, calc(1.5 * var(--wy-font-size, 1em)));font-weight:var(--wy-document-h2-font-weight, var(--wy-font-weight-bold, 600));margin:var(--wy-document-h2-margin, var(--wy-gap-xl, calc(2 * var(--wy-gap, calc(.5 * var(--wy-size, 1rem))))) 0 var(--wy-gap-sm, calc(.5 * var(--wy-gap, calc(.5 * var(--wy-size, 1rem))))))}[part~=wy-content-html] h3{font-size:var(--wy-document-h3-font-size, calc(1.25 * var(--wy-font-size, 1em)));font-weight:var(--wy-document-h3-font-weight, var(--wy-font-weight-bold, 600));margin:var(--wy-document-h3-margin, var(--wy-gap-xl, calc(2 * var(--wy-gap, calc(.5 * var(--wy-size, 1rem))))) 0 var(--wy-gap-sm, calc(.5 * var(--wy-gap, calc(.5 * var(--wy-size, 1rem))))))}[part~=wy-content-html] h4{font-size:var(--wy-document-h4-font-size, calc(1.125 * var(--wy-font-size, 1em)));font-weight:var(--wy-document-h4-font-weight, var(--wy-font-weight-bold, 600));margin:var(--wy-document-h4-margin, var(--wy-gap-xl, calc(2 * var(--wy-gap, calc(.5 * var(--wy-size, 1rem))))) 0 0)}[part~=wy-content-html] h5{font-size:var(--wy-document-h5-font-size, var(--wy-font-size, 1em));font-weight:var(--wy-document-h5-font-weight, var(--wy-font-weight-bold, 600));margin:var(--wy-document-h5-margin, var(--wy-gap-xl, calc(2 * var(--wy-gap, calc(.5 * var(--wy-size, 1rem))))) 0 0)}[part~=wy-content-html] h6{font-size:var(--wy-document-h6-font-size, var(--wy-font-size, 1em));font-weight:var(--wy-document-h6-font-weight, var(--wy-font-weight-bold, 600));margin:var(--wy-document-h6-margin, 0)}`, iC = j`[part~=wy-document]{--wy-component-background-color: var(--wy-background, var(--wy-background-light, #f7f9ff));--wy-component-color: var(--wy-on-background, var(--wy-on-background-light, #181c20));background-color:var(--wy-component-background-color);color:var(--wy-component-color);align-self:flex-start;padding:calc(2 * var(--wy-size, 1rem)) calc(3.5 * var(--wy-size, 1rem));box-sizing:border-box;width:100%;min-height:100%;flex:1 0 auto;text-align:start}@container (inline-size >= 768px){[part~=wy-document]{padding:calc(3 * var(--wy-size, 1rem)) calc(4 * var(--wy-size, 1rem));margin:calc(2 * var(--wy-size, 1rem)) auto;border-radius:calc(.125 * var(--wy-size, 1rem));box-shadow:var(--wy-shadow-level2, 0 calc(.25 * var(--wy-size, 1rem)) calc(.3125 * var(--wy-size, 1rem)) 0 rgba(0, 0, 0, .14), 0 calc(.0625 * var(--wy-size, 1rem)) calc(.625 * var(--wy-size, 1rem)) 0 rgba(0, 0, 0, .12), 0 calc(.125 * var(--wy-size, 1rem)) calc(.25 * var(--wy-size, 1rem)) calc(-.0625 * var(--wy-size, 1rem)) rgba(0, 0, 0, .2));width:120ch;min-height:170ch;max-width:calc(100% - 3.5 * var(--wy-size, 1rem))}}`, rC = j`[part~=wy-code] pre,.wy-code pre{text-wrap:pretty}[part~=wy-code] .code,[part~=wy-code] code[class*=language-],[part~=wy-code] pre[class*=language-],.wy-code .code,.wy-code code[class*=language-],.wy-code pre[class*=language-]{color:var(--wy-code-text, var(--wy-code-text-light, #3b3b3b));font-family:var(--wy-font-monospace, ui-monospace, SFMono-Regular, SF Mono, Menlo, Consolas, Liberation Mono, monospace);text-align:start;white-space:pre-wrap;word-spacing:normal;word-break:normal;word-wrap:break-word;line-height:1.25;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-hyphens:none;-moz-hyphens:none;-ms-hyphens:none;hyphens:none}@media print{[part~=wy-code] code[class*=language-],[part~=wy-code] pre[class*=language-],.wy-code code[class*=language-],.wy-code pre[class*=language-]{text-shadow:none}}[part~=wy-code] pre[class*=language-],.wy-code pre[class*=language-]{padding:1em;margin:.5em 0;overflow:auto}[part~=wy-code] :not(pre)>code[class*=language-],[part~=wy-code] pre[class*=language-],.wy-code :not(pre)>code[class*=language-],.wy-code pre[class*=language-]{color:var(--wy-code-text, var(--wy-code-text-light, #3b3b3b));background:var(--wy-background, var(--wy-background-light, #f7f9ff))}[part~=wy-code] :not(pre)>code[class*=language-],.wy-code :not(pre)>code[class*=language-]{padding:.1em;border-radius:.3em;white-space:normal}[part~=wy-code] .token.variable,.wy-code .token.variable{color:var(--wy-code-variable, var(--wy-code-variable-light, #001080))}[part~=wy-code] .token.operator,.wy-code .token.operator{color:var(--wy-code-operator, var(--wy-code-operator-light, #000000))}[part~=wy-code] .token.prolog,.wy-code .token.prolog{color:var(--wy-code-prolog, var(--wy-code-prolog-light, #000080))}[part~=wy-code] .token.comment,.wy-code .token.comment{color:var(--wy-code-comment, var(--wy-code-comment-light, #008000))}[part~=wy-code] .token.builtin,.wy-code .token.builtin{color:var(--wy-code-builtin, var(--wy-code-builtin-light, #0070C1))}[part~=wy-code] .token.number,.wy-code .token.number{color:var(--wy-code-number, var(--wy-code-number-light, #098658))}[part~=wy-code] .token.inserted,.wy-code .token.inserted{color:var(--wy-code-inserted, var(--wy-code-inserted-light, #098658))}[part~=wy-code] .token.constant,.wy-code .token.constant{color:var(--wy-code-constant, var(--wy-code-constant-light, #811F3F))}[part~=wy-code] .token.hexcode,.wy-code .token.hexcode{color:var(--wy-code-hexcode, var(--wy-code-hexcode-light, #811F3F))}[part~=wy-code] .token.regex,.wy-code .token.regex{color:var(--wy-code-regex, var(--wy-code-regex-light, #811F3F))}[part~=wy-code] .token.char,.wy-code .token.char{color:var(--wy-code-char, var(--wy-code-char-light, #811F3F))}[part~=wy-code] .token.tag,.wy-code .token.tag{color:var(--wy-code-tag, var(--wy-code-tag-light, #800000))}[part~=wy-code] .token.attr-name,.wy-code .token.attr-name{color:var(--wy-code-attr-name, var(--wy-code-attr-name-light, #E50000))}[part~=wy-code] .token.selector,.wy-code .token.selector{color:var(--wy-code-selector, var(--wy-code-selector-light, #E50000))}[part~=wy-code] .token.property,.wy-code .token.property{color:var(--wy-code-property, var(--wy-code-property-light, #E50000))}[part~=wy-code] .token.deleted,.wy-code .token.deleted{color:var(--wy-code-deleted, var(--wy-code-deleted-light, #A31515))}[part~=wy-code] .token.string,.wy-code .token.string{color:var(--wy-code-string, var(--wy-code-string-light, #A31515))}[part~=wy-code] .token.changed,.wy-code .token.changed{color:var(--wy-code-changed, var(--wy-code-changed-light, #0451A5))}[part~=wy-code] .token.punctuation,.wy-code .token.punctuation{color:var(--wy-code-punctuation, var(--wy-code-punctuation-light, #000000))}[part~=wy-code] .token.function,.wy-code .token.function{color:var(--wy-code-function, var(--wy-code-function-light, #0000FF))}[part~=wy-code] .token.keyword,.wy-code .token.keyword{color:var(--wy-code-keyword, var(--wy-code-keyword-light, #AF00DB))}[part~=wy-code] .token.class-name,.wy-code .token.class-name{color:var(--wy-code-class-name, var(--wy-code-class-name-light, #267F99))}[part~=wy-code] .token.important,[part~=wy-code] .token.bold,.wy-code .token.important,.wy-code .token.bold{font-weight:700}[part~=wy-code] .token.italic,.wy-code .token.italic{font-style:italic}`;
var sC = Object.defineProperty, nC = Object.getOwnPropertyDescriptor, pn = /* @__PURE__ */ n((s, t, e, i) => {
  for (var r = i > 1 ? void 0 : i ? nC(t, e) : t, a = s.length - 1, o; a >= 0; a--)
    (o = s[a]) && (r = (i ? o(t, e, r) : o(r)) || r);
  return i && r && sC(t, e, r), r;
}, "__decorateClass$V"), Io;
let Fi = (Io = class extends nt {
  constructor() {
    super(...arguments), this.exportParts = new W(this), this.html = !1, this.code = !1, this.textOrHtmlContent = "", this.loading = !0;
  }
  /**
   * Dispatches the `file-preview-loaded` event when content becomes available.
   *
   * @internal
   * @returns {boolean} `true` if the event was not canceled.
   */
  dispatchLoaded() {
    const t = new CustomEvent("file-preview-loaded");
    return this.dispatchEvent(t);
  }
  updated(t) {
    super.updated(t), (t.has("weavy") || t.has("src")) && this.weavy && (this.loading = !0, this.weavy.fetchOptions().then((e) => fetch(this.src, e)).then(S0).then((e) => new Response(e)).then((e) => e.text()).then((e) => {
      this.loading = !1, this.textOrHtmlContent = e, this.dispatchLoaded();
    }));
  }
  render() {
    return this.loading ? h` <wy-empty><wy-progress-circular part="wy-content-progress" indeterminate></wy-progress-circular></wy-empty> ` : this.html ? this.code ? h` <div part="wy-content-code wy-code">${Bs(this.textOrHtmlContent)}</div> ` : h`
            <div part="wy-document wy-light">
              <div part="wy-content-html">${Bs(this.textOrHtmlContent)}</div>
            </div>
          ` : this.code ? h` <div part="wy-content-code">${this.textOrHtmlContent}</div> ` : h`
          <div part="wy-document wy-light">
            <pre part="wy-content-text">${this.textOrHtmlContent}</pre>
          </div>
        `;
  }
}, n(Io, "WyPreviewText"), Io);
Fi.styles = [Kt, eC, tC, iC, rC, Y];
pn([
  he({ context: Xt, subscribe: !0 }),
  V()
], Fi.prototype, "weavy", 2);
pn([
  y()
], Fi.prototype, "src", 2);
pn([
  y({ type: Boolean })
], Fi.prototype, "html", 2);
pn([
  y({ type: Boolean })
], Fi.prototype, "code", 2);
pn([
  V()
], Fi.prototype, "textOrHtmlContent", 2);
pn([
  V()
], Fi.prototype, "loading", 2);
Fi = pn([
  U("wy-preview-text")
], Fi);
const aC = j`[part~=wy-content-embed]{border:0;display:block;box-sizing:border-box;width:100%;height:100%;top:0;left:0;flex:1 1 100%}[part~=wy-content-embed][part~=wy-loaded]{--wy-component-background-color: var(--wy-surface-layer, var(--wy-surface-layer-light, rgba(0, 0, 0, .0509803922)));--wy-component-color: var(--wy-on-surface, var(--wy-on-surface-light, #181c20));background-color:var(--wy-component-background-color);color:var(--wy-component-color)}[part~=wy-content-embed][part~=wy-loaded]~[part~=wy-content-embed-fallback],[part~=wy-content-embed][part~=wy-loaded]~[part~=wy-content-progress]{display:none}[part~=wy-content-embed][part~=wy-loading]:not([part~=wy-loaded]){position:absolute;visibility:hidden;z-index:-1;pointer-events:none}[part~=wy-content-embed][part~=wy-loading]:not([part~=wy-loaded]):not([part~=wy-fallback])~[part~=wy-content-embed-fallback]{display:none}[part~=wy-content-embed][part~=wy-loading]:not([part~=wy-loaded])[part~=wy-fallback]~[part~=wy-content-progress]{display:none}`;
var oC = Object.defineProperty, cC = Object.getOwnPropertyDescriptor, rh = /* @__PURE__ */ n((s, t, e, i) => {
  for (var r = i > 1 ? void 0 : i ? cC(t, e) : t, a = s.length - 1, o; a >= 0; a--)
    (o = s[a]) && (r = (i ? o(t, e, r) : o(r)) || r);
  return i && r && oC(t, e, r), r;
}, "__decorateClass$U"), Do;
let Jr = (Do = class extends nt {
  constructor() {
    super(...arguments), this.exportParts = new W(this);
  }
  /**
   * Emit `file-preview-loaded` once the preview has finished loading.
   *
   * @returns {boolean} True if the event was not canceled.
   */
  dispatchLoaded() {
    const t = new CustomEvent("file-preview-loaded");
    return this.dispatchEvent(t);
  }
  /**
   * Attach load listeners and register the provided embed element.
   *
   * @internal
   * @param embedElement - Newly rendered embed element.
   */
  registerLoading(t) {
    if (this.unregisterLoading?.(), t) {
      this.embedElement = t, t.part.add("wy-loading");
      const e = window.setTimeout(() => {
        t.part.add("wy-fallback"), this.dispatchLoaded();
      }, 2500), i = /* @__PURE__ */ n((r) => {
        const a = r.target;
        a.tagName === "OBJECT" && a.part.contains("wy-loading") && !a.part.contains("wy-loaded") && (a.part.add("wy-loaded"), window.clearTimeout(e), this.dispatchLoaded());
      }, "embedLoaded");
      t.addEventListener("load", i, !0), this.unregisterLoading = () => {
        this.embedElement && (t.removeEventListener("load", i, !0), window.clearTimeout(e), this.embedElement = void 0);
      };
    }
  }
  render() {
    return h`
      <object title=${x("Preview")} ${K((t) => this.registerLoading(t))} part="wy-content-embed" data=${this.src}></object>
      <wy-progress-circular part="wy-content-progress" indeterminate overlay></wy-progress-circular>
      <wy-preview-icon
        src=${this.src}
        icon=${this.icon}
        provider=${ot(this.provider)}
        part="wy-content-embed-fallback"></wy-preview-icon>
    `;
  }
  disconnectedCallback() {
    this.unregisterLoading?.(), super.disconnectedCallback();
  }
}, n(Do, "WyPreviewEmbed"), Do);
Jr.styles = [
  aC,
  Y
];
rh([
  y()
], Jr.prototype, "src", 2);
rh([
  y()
], Jr.prototype, "name", 2);
rh([
  y()
], Jr.prototype, "icon", 2);
rh([
  y()
], Jr.prototype, "provider", 2);
Jr = rh([
  U("wy-preview-embed"),
  ut()
], Jr);
const lC = j`.wy-pdf-container{position:absolute;width:100%;height:100%;bottom:0;left:0}.wy-pdf-container{overflow-y:auto;max-height:100%;overflow-x:hidden;overflow-anchor:none;touch-action:pan-y;will-change:scroll-position,opacity;backface-visibility:hidden}.wy-pdf-container:not(body){-webkit-overflow-scrolling:touch}.wy-pdf-container{overflow-x:auto;max-width:100%;overflow-y:hidden;overflow-anchor:none;touch-action:pan-x;will-change:scroll-position,opacity;backface-visibility:hidden}.wy-pdf-container{overflow-y:auto;overflow-x:auto;touch-action:pan-y,pan-x}.wy-content-pdf .textLayer{position:absolute;text-align:initial;inset:0;overflow:clip;opacity:1;line-height:1;text-size-adjust:none;forced-color-adjust:none;transform-origin:0 0;caret-color:CanvasText;z-index:0}.wy-content-pdf .textLayer.highlighting{touch-action:none}.wy-content-pdf .textLayer :is(span,br){color:transparent;position:absolute;white-space:pre;cursor:text;transform-origin:0% 0%}.wy-content-pdf .textLayer>:not(.markedContent),.wy-content-pdf .textLayer .markedContent span:not(.markedContent){z-index:1}.wy-content-pdf .textLayer span.markedContent{top:0;height:0}.wy-content-pdf .textLayer span[role=img]{-webkit-user-select:none;user-select:none;cursor:default}.wy-content-pdf .textLayer .highlight{--highlight-bg-color: rgb(180 0 170 / .25);--highlight-selected-bg-color: rgb(0 100 0 / .25);--highlight-backdrop-filter: none;--highlight-selected-backdrop-filter: none}@media screen and (forced-colors:active){.wy-content-pdf .textLayer .highlight{--highlight-bg-color: transparent;--highlight-selected-bg-color: transparent;--highlight-backdrop-filter: var(--hcm-highlight-filter);--highlight-selected-backdrop-filter: var(--hcm-highlight-selected-filter)}}.wy-content-pdf .textLayer .highlight{margin:-1px;padding:1px;background-color:var(--highlight-bg-color);-webkit-backdrop-filter:var(--highlight-backdrop-filter);backdrop-filter:var(--highlight-backdrop-filter);border-radius:4px}.wy-content-pdf .textLayer .highlight.appended{position:initial}.wy-content-pdf .textLayer .highlight.begin{border-radius:4px 0 0 4px}.wy-content-pdf .textLayer .highlight.end{border-radius:0 4px 4px 0}.wy-content-pdf .textLayer .highlight.middle{border-radius:0}.wy-content-pdf .textLayer .highlight.selected{background-color:var(--highlight-selected-bg-color);-webkit-backdrop-filter:var(--highlight-selected-backdrop-filter);backdrop-filter:var(--highlight-selected-backdrop-filter)}.wy-content-pdf .textLayer ::selection{background:#0000ff40;background:color-mix(in srgb,AccentColor,transparent 75%)}.wy-content-pdf .textLayer br::selection{background:transparent}.wy-content-pdf .textLayer .endOfContent{display:block;position:absolute;inset:100% 0 0;z-index:0;cursor:default;-webkit-user-select:none;user-select:none}.wy-content-pdf .textLayer.selecting .endOfContent{top:0}.wy-content-pdf .annotationLayer{--annotation-unfocused-field-background: url(\"data:image/svg+xml;charset=UTF-8,<svg width='1px' height='1px' xmlns='http://www.w3.org/2000/svg'><rect width='100%' height='100%' style='fill:rgba(0, 54, 255, 0.13);'/></svg>\");--input-focus-border-color: Highlight;--input-focus-outline: 1px solid Canvas;--input-unfocused-border-color: transparent;--input-disabled-border-color: transparent;--input-hover-border-color: black;--link-outline: none}@media screen and (forced-colors:active){.wy-content-pdf .annotationLayer{--input-focus-border-color: CanvasText;--input-unfocused-border-color: ActiveText;--input-disabled-border-color: GrayText;--input-hover-border-color: Highlight;--link-outline: 1.5px solid LinkText}.wy-content-pdf .annotationLayer .textWidgetAnnotation :is(input,textarea):required,.wy-content-pdf .annotationLayer .choiceWidgetAnnotation select:required,.wy-content-pdf .annotationLayer .buttonWidgetAnnotation:is(.checkBox,.radioButton) input:required{outline:1.5px solid selectedItem}.wy-content-pdf .annotationLayer .linkAnnotation{outline:var(--link-outline)}.wy-content-pdf .annotationLayer .linkAnnotation:hover{-webkit-backdrop-filter:var(--hcm-highlight-filter);backdrop-filter:var(--hcm-highlight-filter)}.wy-content-pdf .annotationLayer .linkAnnotation>a:hover{opacity:0!important;background:none!important;box-shadow:none}.wy-content-pdf .annotationLayer .popupAnnotation .popup{outline:calc(1.5px * var(--scale-factor)) solid CanvasText!important;background-color:ButtonFace!important;color:ButtonText!important}.wy-content-pdf .annotationLayer .highlightArea:hover:after{position:absolute;top:0;left:0;width:100%;height:100%;-webkit-backdrop-filter:var(--hcm-highlight-filter);backdrop-filter:var(--hcm-highlight-filter);content:\"\";pointer-events:none}.wy-content-pdf .annotationLayer .popupAnnotation.focused .popup{outline:calc(3px * var(--scale-factor)) solid Highlight!important}}.wy-content-pdf .annotationLayer{position:absolute;top:0;left:0;pointer-events:none;transform-origin:0 0}.wy-content-pdf .annotationLayer[data-main-rotation=\"90\"] .norotate{transform:rotate(270deg) translate(-100%)}.wy-content-pdf .annotationLayer[data-main-rotation=\"180\"] .norotate{transform:rotate(180deg) translate(-100%,-100%)}.wy-content-pdf .annotationLayer[data-main-rotation=\"270\"] .norotate{transform:rotate(90deg) translateY(-100%)}.wy-content-pdf .annotationLayer.disabled section,.wy-content-pdf .annotationLayer.disabled .popup{pointer-events:none}.wy-content-pdf .annotationLayer .annotationContent{position:absolute;width:100%;height:100%;pointer-events:none}.wy-content-pdf .annotationLayer .annotationContent.freetext{background:transparent;border:none;inset:0;overflow:visible;white-space:nowrap;font:10px sans-serif;line-height:1.35;-webkit-user-select:none;user-select:none}.wy-content-pdf .annotationLayer section{position:absolute;text-align:initial;pointer-events:auto;box-sizing:border-box;transform-origin:0 0}.wy-content-pdf .annotationLayer section:has(div.annotationContent) canvas.annotationContent{display:none}.textLayer.selecting~.wy-content-pdf .annotationLayer section{pointer-events:none}.wy-content-pdf .annotationLayer :is(.linkAnnotation,.buttonWidgetAnnotation.pushButton)>a{position:absolute;font-size:1em;top:0;left:0;width:100%;height:100%}.wy-content-pdf .annotationLayer :is(.linkAnnotation,.buttonWidgetAnnotation.pushButton):not(.hasBorder)>a:hover{opacity:.2;background-color:#ff0;box-shadow:0 2px 10px #ff0}.wy-content-pdf .annotationLayer .linkAnnotation.hasBorder:hover{background-color:#ff03}.wy-content-pdf .annotationLayer .hasBorder{background-size:100% 100%}.wy-content-pdf .annotationLayer .textAnnotation img{position:absolute;cursor:pointer;width:100%;height:100%;top:0;left:0}.wy-content-pdf .annotationLayer .textWidgetAnnotation :is(input,textarea),.wy-content-pdf .annotationLayer .choiceWidgetAnnotation select,.wy-content-pdf .annotationLayer .buttonWidgetAnnotation:is(.checkBox,.radioButton) input{background-image:var(--annotation-unfocused-field-background);border:2px solid var(--input-unfocused-border-color);box-sizing:border-box;font:calc(9px * var(--scale-factor)) sans-serif;height:100%;margin:0;vertical-align:top;width:100%}.wy-content-pdf .annotationLayer .textWidgetAnnotation :is(input,textarea):required,.wy-content-pdf .annotationLayer .choiceWidgetAnnotation select:required,.wy-content-pdf .annotationLayer .buttonWidgetAnnotation:is(.checkBox,.radioButton) input:required{outline:1.5px solid red}.wy-content-pdf .annotationLayer .choiceWidgetAnnotation select option{padding:0}.wy-content-pdf .annotationLayer .buttonWidgetAnnotation.radioButton input{border-radius:50%}.wy-content-pdf .annotationLayer .textWidgetAnnotation textarea{resize:none}.wy-content-pdf .annotationLayer .textWidgetAnnotation :is(input,textarea)[disabled],.wy-content-pdf .annotationLayer .choiceWidgetAnnotation select[disabled],.wy-content-pdf .annotationLayer .buttonWidgetAnnotation:is(.checkBox,.radioButton) input[disabled]{background:none;border:2px solid var(--input-disabled-border-color);cursor:not-allowed}.wy-content-pdf .annotationLayer .textWidgetAnnotation :is(input,textarea):hover,.wy-content-pdf .annotationLayer .choiceWidgetAnnotation select:hover,.wy-content-pdf .annotationLayer .buttonWidgetAnnotation:is(.checkBox,.radioButton) input:hover{border:2px solid var(--input-hover-border-color)}.wy-content-pdf .annotationLayer .textWidgetAnnotation :is(input,textarea):hover,.wy-content-pdf .annotationLayer .choiceWidgetAnnotation select:hover,.wy-content-pdf .annotationLayer .buttonWidgetAnnotation.checkBox input:hover{border-radius:2px}.wy-content-pdf .annotationLayer .textWidgetAnnotation :is(input,textarea):focus,.wy-content-pdf .annotationLayer .choiceWidgetAnnotation select:focus{background:none;border:2px solid var(--input-focus-border-color);border-radius:2px;outline:var(--input-focus-outline)}.wy-content-pdf .annotationLayer .buttonWidgetAnnotation:is(.checkBox,.radioButton) :focus{background-image:none;background-color:transparent}.wy-content-pdf .annotationLayer .buttonWidgetAnnotation.checkBox :focus{border:2px solid var(--input-focus-border-color);border-radius:2px;outline:var(--input-focus-outline)}.wy-content-pdf .annotationLayer .buttonWidgetAnnotation.radioButton :focus{border:2px solid var(--input-focus-border-color);outline:var(--input-focus-outline)}.wy-content-pdf .annotationLayer .buttonWidgetAnnotation.checkBox input:checked:before,.wy-content-pdf .annotationLayer .buttonWidgetAnnotation.checkBox input:checked:after,.wy-content-pdf .annotationLayer .buttonWidgetAnnotation.radioButton input:checked:before{background-color:CanvasText;content:\"\";display:block;position:absolute}.wy-content-pdf .annotationLayer .buttonWidgetAnnotation.checkBox input:checked:before,.wy-content-pdf .annotationLayer .buttonWidgetAnnotation.checkBox input:checked:after{height:80%;left:45%;width:1px}.wy-content-pdf .annotationLayer .buttonWidgetAnnotation.checkBox input:checked:before{transform:rotate(45deg)}.wy-content-pdf .annotationLayer .buttonWidgetAnnotation.checkBox input:checked:after{transform:rotate(-45deg)}.wy-content-pdf .annotationLayer .buttonWidgetAnnotation.radioButton input:checked:before{border-radius:50%;height:50%;left:25%;top:25%;width:50%}.wy-content-pdf .annotationLayer .textWidgetAnnotation input.comb{font-family:monospace;padding-left:2px;padding-right:0}.wy-content-pdf .annotationLayer .textWidgetAnnotation input.comb:focus{width:103%}.wy-content-pdf .annotationLayer .buttonWidgetAnnotation:is(.checkBox,.radioButton) input{appearance:none}.wy-content-pdf .annotationLayer .fileAttachmentAnnotation .popupTriggerArea{height:100%;width:100%}.wy-content-pdf .annotationLayer .popupAnnotation{position:absolute;font-size:calc(9px * var(--scale-factor));pointer-events:none;width:max-content;max-width:45%;height:auto}.wy-content-pdf .annotationLayer .popup{background-color:#ff9;box-shadow:0 calc(2px * var(--scale-factor)) calc(5px * var(--scale-factor)) #888;border-radius:calc(2px * var(--scale-factor));outline:1.5px solid rgb(255,255,74);padding:calc(6px * var(--scale-factor));cursor:pointer;font:message-box;white-space:normal;word-wrap:break-word;pointer-events:auto}.wy-content-pdf .annotationLayer .popupAnnotation.focused .popup{outline-width:3px}.wy-content-pdf .annotationLayer .popup *{font-size:calc(9px * var(--scale-factor))}.wy-content-pdf .annotationLayer .popup>.header{display:inline-block}.wy-content-pdf .annotationLayer .popup>.header h1{display:inline}.wy-content-pdf .annotationLayer .popup>.header .popupDate{display:inline-block;margin-left:calc(5px * var(--scale-factor));width:fit-content}.wy-content-pdf .annotationLayer .popupContent{border-top:1px solid rgb(51,51,51);margin-top:calc(2px * var(--scale-factor));padding-top:calc(2px * var(--scale-factor))}.wy-content-pdf .annotationLayer .richText>*{white-space:pre-wrap;font-size:calc(9px * var(--scale-factor))}.wy-content-pdf .annotationLayer .popupTriggerArea{cursor:pointer}.wy-content-pdf .annotationLayer section svg{position:absolute;width:100%;height:100%;top:0;left:0}.wy-content-pdf .annotationLayer .annotationTextContent{position:absolute;width:100%;height:100%;opacity:0;color:transparent;-webkit-user-select:none;user-select:none;pointer-events:none}.wy-content-pdf .annotationLayer .annotationTextContent span{width:100%;display:inline-block}.wy-content-pdf .annotationLayer svg.quadrilateralsContainer{contain:strict;width:0;height:0;position:absolute;top:0;left:0;z-index:-1}:host{--xfa-unfocused-field-background: url(\"data:image/svg+xml;charset=UTF-8,<svg width='1px' height='1px' xmlns='http://www.w3.org/2000/svg'><rect width='100%' height='100%' style='fill:rgba(0, 54, 255, 0.13);'/></svg>\");--xfa-focus-outline: auto}@media screen and (forced-colors:active){:host{--xfa-focus-outline: 2px solid CanvasText}.wy-content-pdf .xfaLayer *:required{outline:1.5px solid selectedItem}}.wy-content-pdf .xfaLayer{background-color:transparent}.wy-content-pdf .xfaLayer .highlight{margin:-1px;padding:1px;background-color:#efcbed;border-radius:4px}.wy-content-pdf .xfaLayer .highlight.appended{position:initial}.wy-content-pdf .xfaLayer .highlight.begin{border-radius:4px 0 0 4px}.wy-content-pdf .xfaLayer .highlight.end{border-radius:0 4px 4px 0}.wy-content-pdf .xfaLayer .highlight.middle{border-radius:0}.wy-content-pdf .xfaLayer .highlight.selected{background-color:#cbdfcb}.wy-content-pdf .xfaPage{overflow:hidden;position:relative}.wy-content-pdf .xfaContentarea{position:absolute}.wy-content-pdf .xfaPrintOnly{display:none}.wy-content-pdf .xfaLayer{position:absolute;text-align:initial;top:0;left:0;transform-origin:0 0;line-height:1.2}.wy-content-pdf .xfaLayer *{color:inherit;font:inherit;font-style:inherit;font-weight:inherit;font-kerning:inherit;letter-spacing:-.01px;text-align:inherit;text-decoration:inherit;box-sizing:border-box;background-color:transparent;padding:0;margin:0;pointer-events:auto;line-height:inherit}.wy-content-pdf .xfaLayer *:required{outline:1.5px solid red}.wy-content-pdf .xfaLayer div,.wy-content-pdf .xfaLayer svg,.wy-content-pdf .xfaLayer svg *{pointer-events:none}.wy-content-pdf .xfaLayer a{color:#00f}.wy-content-pdf .xfaRich li{margin-left:3em}.wy-content-pdf .xfaFont{color:#000;font-weight:400;font-kerning:none;font-size:10px;font-style:normal;letter-spacing:0;text-decoration:none;vertical-align:0}.wy-content-pdf .xfaCaption{overflow:hidden;flex:0 0 auto}.wy-content-pdf .xfaCaptionForCheckButton{overflow:hidden;flex:1 1 auto}.wy-content-pdf .xfaLabel{height:100%;width:100%}.wy-content-pdf .xfaLeft{display:flex;flex-direction:row;align-items:center}.wy-content-pdf .xfaRight{display:flex;flex-direction:row-reverse;align-items:center}.wy-content-pdf :is(.xfaLeft,.xfaRight)>:is(.xfaCaption,.xfaCaptionForCheckButton){max-height:100%}.wy-content-pdf .xfaTop{display:flex;flex-direction:column;align-items:flex-start}.wy-content-pdf .xfaBottom{display:flex;flex-direction:column-reverse;align-items:flex-start}.wy-content-pdf :is(.xfaTop,.xfaBottom)>:is(.xfaCaption,.xfaCaptionForCheckButton){width:100%}.wy-content-pdf .xfaBorder{background-color:transparent;position:absolute;pointer-events:none}.wy-content-pdf .xfaWrapped{width:100%;height:100%}.wy-content-pdf :is(.xfaTextfield,.xfaSelect):focus{background-image:none;background-color:transparent;outline:var(--xfa-focus-outline);outline-offset:-1px}.wy-content-pdf :is(.xfaCheckbox,.xfaRadio):focus{outline:var(--xfa-focus-outline)}.wy-content-pdf .xfaTextfield,.wy-content-pdf .xfaSelect{height:100%;width:100%;flex:1 1 auto;border:none;resize:none;background-image:var(--xfa-unfocused-field-background)}.wy-content-pdf .xfaSelect{padding-inline:2px}.wy-content-pdf :is(.xfaTop,.xfaBottom)>:is(.xfaTextfield,.xfaSelect){flex:0 1 auto}.wy-content-pdf .xfaButton{cursor:pointer;width:100%;height:100%;border:none;text-align:center}.wy-content-pdf .xfaLink{width:100%;height:100%;position:absolute;top:0;left:0}.wy-content-pdf .xfaCheckbox,.wy-content-pdf .xfaRadio{width:100%;height:100%;flex:0 0 auto;border:none}.wy-content-pdf .xfaRich{white-space:pre-wrap;width:100%;height:100%}.wy-content-pdf .xfaImage{object-position:left top;object-fit:contain;width:100%;height:100%}.wy-content-pdf .xfaLrTb,.wy-content-pdf .xfaRlTb,.wy-content-pdf .xfaTb{display:flex;flex-direction:column;align-items:stretch}.wy-content-pdf .xfaLr{display:flex;flex-direction:row;align-items:stretch}.wy-content-pdf .xfaRl{display:flex;flex-direction:row-reverse;align-items:stretch}.wy-content-pdf .xfaTb>div{justify-content:left}.wy-content-pdf .xfaPosition,.wy-content-pdf .xfaArea{position:relative}.wy-content-pdf .xfaValignMiddle{display:flex;align-items:center}.wy-content-pdf .xfaTable{display:flex;flex-direction:column;align-items:stretch}.wy-content-pdf .xfaTable .xfaRow{display:flex;flex-direction:row;align-items:stretch}.wy-content-pdf .xfaTable .xfaRlRow{display:flex;flex-direction:row-reverse;align-items:stretch;flex:1}.wy-content-pdf .xfaTable .xfaRlRow>div{flex:1}.wy-content-pdf :is(.xfaNonInteractive,.xfaDisabled,.xfaReadOnly) :is(input,textarea){background:initial}@media print{.wy-content-pdf .xfaTextfield,.wy-content-pdf .xfaSelect{background:transparent}.wy-content-pdf .xfaSelect{appearance:none;text-indent:1px;text-overflow:\"\"}}:host{--viewer-container-height: 0;--pdfViewer-padding-bottom: 0;--page-margin: 1px auto -8px;--page-border: 9px solid transparent;--spreadHorizontalWrapped-margin-LR: -3.5px;--loading-icon-delay: .4s}@media screen and (forced-colors:active){:host{--pdfViewer-padding-bottom: 9px;--page-margin: 8px auto -1px;--page-border: 1px solid CanvasText;--spreadHorizontalWrapped-margin-LR: 3.5px}}.wy-content-pdf [data-main-rotation=\"90\"]{transform:rotate(90deg) translateY(-100%)}.wy-content-pdf [data-main-rotation=\"180\"]{transform:rotate(180deg) translate(-100%,-100%)}.wy-content-pdf [data-main-rotation=\"270\"]{transform:rotate(270deg) translate(-100%)}.wy-content-pdf #hiddenCopyElement,.wy-content-pdf .hiddenCanvasElement{position:absolute;top:0;left:0;width:0;height:0;display:none}.wy-content-pdf .pdfViewer{--scale-factor: 1;--page-bg-color: unset;padding-bottom:var(--pdfViewer-padding-bottom);--hcm-highlight-filter: none;--hcm-highlight-selected-filter: none}@media screen and (forced-colors:active){.wy-content-pdf .pdfViewer{--hcm-highlight-filter: invert(100%)}}.wy-content-pdf .pdfViewer.copyAll{cursor:wait}.wy-content-pdf .pdfViewer .canvasWrapper{overflow:hidden;width:100%;height:100%}.wy-content-pdf .pdfViewer .canvasWrapper canvas{position:absolute;top:0;left:0;margin:0;display:block;width:100%;height:100%;contain:content}.wy-content-pdf .pdfViewer .canvasWrapper canvas .structTree{contain:strict}.wy-content-pdf .pdfViewer .page{--scale-round-x: 1px;--scale-round-y: 1px;direction:ltr;width:816px;height:1056px;margin:var(--page-margin);position:relative;overflow:visible;border:var(--page-border);background-clip:content-box;background-color:var(--page-bg-color, rgb(255, 255, 255))}.wy-content-pdf .pdfViewer .dummyPage{position:relative;width:0;height:var(--viewer-container-height)}.wy-content-pdf .pdfViewer.noUserSelect{-webkit-user-select:none;user-select:none}.wy-content-pdf .pdfViewer.removePageBorders .page{margin:0 auto 10px;border:none}.wy-content-pdf .pdfViewer.singlePageView{display:inline-block}.wy-content-pdf .pdfViewer.singlePageView .page{margin:0;border:none}.wy-content-pdf .pdfViewer:is(.scrollHorizontal,.scrollWrapped),.wy-content-pdf .spread{margin-inline:3.5px;text-align:center}.wy-content-pdf .pdfViewer.scrollHorizontal,.wy-content-pdf .spread{white-space:nowrap}.wy-content-pdf .pdfViewer.removePageBorders,.wy-content-pdf .pdfViewer:is(.scrollHorizontal,.scrollWrapped) .spread{margin-inline:0}.wy-content-pdf .spread :is(.page,.dummyPage),.wy-content-pdf .pdfViewer:is(.scrollHorizontal,.scrollWrapped) :is(.page,.spread){display:inline-block;vertical-align:middle}.wy-content-pdf .spread .page,.wy-content-pdf .pdfViewer:is(.scrollHorizontal,.scrollWrapped) .page{margin-inline:var(--spreadHorizontalWrapped-margin-LR)}.wy-content-pdf .pdfViewer.removePageBorders .spread .page,.wy-content-pdf .pdfViewer.removePageBorders:is(.scrollHorizontal,.scrollWrapped) .page{margin-inline:5px}.wy-content-pdf .pdfViewer .page.loadingIcon:after{position:absolute;top:0;left:0;content:\"\";width:100%;height:100%;display:none;transition-property:display;transition-delay:var(--loading-icon-delay);z-index:5;contain:strict}.wy-content-pdf .pdfViewer .page.loading:after{display:block}.wy-content-pdf .pdfViewer .page:not(.loading):after{transition-property:none;display:none}.wy-content-pdf .pdfPresentationMode .pdfViewer{padding-bottom:0}.wy-content-pdf .pdfPresentationMode .spread{margin:0}.wy-content-pdf .pdfPresentationMode .pdfViewer .page{margin:0 auto;border:2px solid transparent}.wy-content-pdf .pdfViewer .page{box-shadow:var(--wy-shadow-level2, 0 calc(.25 * var(--wy-size, 1rem)) calc(.3125 * var(--wy-size, 1rem)) 0 rgba(0, 0, 0, .14), 0 calc(.0625 * var(--wy-size, 1rem)) calc(.625 * var(--wy-size, 1rem)) 0 rgba(0, 0, 0, .12), 0 calc(.125 * var(--wy-size, 1rem)) calc(.25 * var(--wy-size, 1rem)) calc(-.0625 * var(--wy-size, 1rem)) rgba(0, 0, 0, .2));background-color:var(--wy-white, var(--wy-white, #ffffff))}.wy-content-pdf .pdfViewer .page.loadingIcon:after{background:url(\"data:image/svg+xml,%3Csvg viewBox='0 0 24 24' width='24' height='24' xmlns='http://www.w3.org/2000/svg' xmlns:xlink='http://www.w3.org/1999/xlink'%3E%3Cstyle type='text/css'%3Eg%7Banimation:rotate 2s linear infinite;transform-origin:center center%7Dcircle%7Bstroke-dasharray:1,100;stroke-dashoffset:0;animation:dash 1.5s ease-in-out infinite,color 6s ease-in-out infinite%7D@keyframes rotate%7B100%25%7Btransform:rotate(360deg)%7D%7D@keyframes dash%7B0%25%7Bstroke-dasharray:1,100;stroke-dashoffset:0%7D50%25%7Bstroke-dasharray:44.5,100;stroke-dashoffset:-17.5px%7D100%25%7Bstroke-dasharray:44.5,100;stroke-dashoffset:-62px%7D%7D@keyframes color%7B0%25,100%25%7Bstroke:%23216eb4%7D40%25%7Bstroke:%2328b7cf%7D66%25%7Bstroke:%23216eb4%7D80%25,90%25%7Bstroke:%2328b7cf%7D%7D%3C/style%3E%3Cg%3E%3Ccircle fill='none' cx='12' cy='12' r='11' stroke-linecap='butt' stroke-width='2' /%3E%3C/g%3E%3C/svg%3E\") center no-repeat}.wy-pdf-page-number{width:calc(3 * var(--wy-size, 1rem))}.wy-pdf-zoom-level{width:calc(5 * var(--wy-size, 1rem))}`, pg = j`[part~=wy-toolbar]{--wy-component-color: var(--wy-on-surface, var(--wy-on-surface-light, #181c20));--wy-component-background-color: var(--wy-surface-container-low, var(--wy-surface-container-low-light, #f1f3f9));box-sizing:border-box;display:flex;flex-wrap:wrap;align-items:center;justify-content:flex-start;justify-items:center;min-height:var(--wy-toolbar-height, calc(max(1lh, var(--wy-button-size, calc(2.5 * var(--wy-size, 1rem)))) + 2 * var(--wy-padding-sm, calc(.25 * var(--wy-size, 1rem)))));padding:calc(.5 * var(--wy-padding, calc(.5 * var(--wy-size, 1rem))));flex:1 0 var(--wy-toolbar-height, calc(max(1lh, var(--wy-button-size, calc(2.5 * var(--wy-size, 1rem)))) + 2 * var(--wy-padding-sm, calc(.25 * var(--wy-size, 1rem)))));column-gap:var(--wy-gap, var(--wy-padding, calc(.5 * var(--wy-size, 1rem))));line-height:var(--wy-line-height, 1.5);background-color:var(--wy-component-background-color);color:var(--wy-component-color);border-radius:var(--wy-component-border-radius)}[part~=wy-toolbar-center]{margin:0 auto;border-radius:var(--wy-border-radius, calc(.5 * var(--wy-size, 1rem)))}[part~=wy-toolbar-buttons]{display:flex;flex:0 0 auto;align-items:center;gap:var(--wy-gap-sm, calc(.5 * var(--wy-gap, var(--wy-padding, calc(.5 * var(--wy-size, 1rem))))));max-width:100%}[part~=wy-toolbar-buttons-last]{margin-inline-start:auto}[part~=wy-toolbar-text]{margin-left:var(--wy-gap, var(--wy-padding, calc(.5 * var(--wy-size, 1rem))))}[part~=wy-toolbar-center-text]{text-align:center}[part~=wy-toolbars-bottom]{position:absolute;z-index:1030;bottom:calc(var(--wy-footerbar-height, calc(4 * var(--wy-size, 1rem))) / 2);left:0;right:0;min-height:var(--wy-toolbar-height, calc(max(1lh, var(--wy-button-size, calc(2.5 * var(--wy-size, 1rem)))) + 2 * var(--wy-padding-sm, calc(.25 * var(--wy-size, 1rem)))));display:flex;flex-direction:column;justify-content:center;justify-items:center;box-sizing:border-box}`, us = j`[part~=wy-input]{--wy-component-background-color: var(--wy-surface-container-lowest, var(--wy-surface-container-lowest-light, #ffffff));--wy-component-color: var(--wy-on-surface, var(--wy-on-surface-light, #181c20));background-color:var(--wy-component-background-color);color:var(--wy-component-color);border:var(--wy-input-border-width, var(--wy-border, 1px)) solid var(--wy-outline-variant, var(--wy-outline-variant-light, #c2c7cf));margin:calc(-1 * var(--wy-input-border-width, var(--wy-border, 1px)));display:block;max-width:100%;min-width:0;flex:1 1 auto;min-height:calc(var(--wy-input-font-size, var(--wy-font-size, 1em)) * 1.5 * var(--wy-size, 1rem) + var(--wy-input-padding-y, var(--wy-padding, calc(.5 * var(--wy-size, 1rem)))) * 2 + var(--wy-padding-sm, calc(.75 * var(--wy-padding, calc(.5 * var(--wy-size, 1rem))))) * 2);padding:var(--wy-input-padding-y, var(--wy-padding, calc(.5 * var(--wy-size, 1rem)))) var(--wy-input-padding-x, var(--wy-padding-lg, calc(1.5 * var(--wy-padding, calc(.5 * var(--wy-size, 1rem))))));font-family:var(--wy-input-font-family, var(--wy-font-family, inherit)),inherit;font-size:var(--wy-input-font-size, var(--wy-font-size, 1em));font-weight:var(--wy-input-font-weight, var(--wy-font-weight, unset));line-height:var(--wy-input-line-height, var(--wy-line-height, calc(1.5 * var(--wy-size, 1rem))));background-clip:padding-box;appearance:none;border-radius:var(--wy-input-border-radius, var(--wy-border-radius, calc(.5 * var(--wy-size, 1rem))))}[part~=wy-input]:focus{--wy-component-color: var(--wy-on-surface, var(--wy-on-surface-light, #181c20));color:var(--wy-component-color);border-color:var(--wy-primary, var(--wy-primary-light, #2f628c));outline:0}[part~=wy-input]::placeholder{color:var(--wy-outline, var(--wy-outline-light, #72777f));opacity:1}[part~=wy-input]:disabled{opacity:38%}[part~=wy-input]::file-selector-button,[part~=wy-input]::-webkit-file-upload-button{color:var(--wy-component-color);background-color:var(--wy-component-background-color);padding:var(--wy-input-padding-y, var(--wy-padding, calc(.5 * var(--wy-size, 1rem)))) var(--wy-input-padding-x, var(--wy-padding-lg, calc(1.5 * var(--wy-padding, calc(.5 * var(--wy-size, 1rem))))));margin:calc(-1 * var(--wy-input-padding-y, var(--wy-padding, calc(.5 * var(--wy-size, 1rem))))) calc(-1 * var(--wy-input-padding-x, var(--wy-padding-lg, calc(1.5 * var(--wy-padding, calc(.5 * var(--wy-size, 1rem)))))));margin-inline-end:var(--wy-input-padding-x, var(--wy-padding-lg, calc(1.5 * var(--wy-padding, calc(.5 * var(--wy-size, 1rem))))));pointer-events:none;border-color:inherit;border-style:solid;border-width:0;border-inline-end-width:var(--wy-input-border-width, var(--wy-border, 1px));border-radius:0}[part~=wy-input]:hover:not(:disabled):not([readonly])::file-selector-button,[part~=wy-input]:hover:not(:disabled):not([readonly]) ::-webkit-file-upload-button{background-color:var(--wy-component-background-color)}[part~=wy-input][type=file]{overflow:hidden}[part~=wy-input][type=file]:not(:disabled):not([readonly]){cursor:pointer}[part~=wy-input]::-webkit-date-and-time-value{height:calc(var(--wy-input-line-height, var(--wy-line-height, calc(1.5 * var(--wy-size, 1rem)))) * 1em)}[part~=wy-input][type=search]::-webkit-search-cancel-button{-webkit-appearance:none}[part~=wy-input-filled]{--wy-component-background-color: var(--wy-surface-container, var(--wy-surface-container-light, #eceef3));--wy-component-color: var(--wy-on-surface, var(--wy-on-surface-light, #181c20));background:var(--wy-component-background-color);color:var(--wy-component-color);border-radius:var(--wy-input-filled-border-radius, var(--wy-border-radius-lg, var(--wy-border-radius, calc(1.25 * var(--wy-size, 1rem)))));border-color:var(--wy-component-background-color)}[part~=wy-input-filled]:focus{border-color:var(--wy-outline-variant, var(--wy-outline-variant-light, #c2c7cf))}[part~=wy-label]{display:inline-block;font-family:var(--wy-headings-font-family, var(--wy-font-family, unset));font-size:var(--wy-headings-font-size, var(--wy-font-size, 1em));font-style:var(--wy-headings-font-style, unset);font-weight:var(--wy-headings-font-weight, var(--wy-font-weight-bold, 600));line-height:var(--wy-headings-line-height, var(--wy-line-height, 1.5));margin-block:var(--wy-padding, calc(.5 * var(--wy-size, 1rem)))}[part~=wy-description]{color:var(--wy-outline, var(--wy-outline-light, #72777f));font-size:var(--wy-font-size-sm, calc(.875 * var(--wy-font-size, 1em)));margin-top:calc(.25 * var(--wy-size, 1rem))}[part~=wy-is-invalid],[part~=wy-is-invalid]:focus{border-color:var(--wy-error, var(--wy-error-light, #ba1821))}[part~=wy-invalid-feedback]{margin-top:calc(.25 * var(--wy-size, 1rem));font-size:var(--wy-font-size-sm, calc(.875 * var(--wy-font-size, 1em)));color:var(--wy-error, var(--wy-error-light, #ba1821))}[part~=wy-input-group]{position:relative;display:flex;gap:var(--wy-gap, var(--wy-padding, calc(.5 * var(--wy-size, 1rem))));align-items:center;flex:1 0 auto}[part~=wy-input-group-button-icon]{--wy-component-background-color: transparent}[part~=wy-input-group-input-with-overlay]{padding-right:calc(var(--wy-input-padding-y, var(--wy-padding, calc(.5 * var(--wy-size, 1rem)))) + var(--wy-button-size, calc(var(--wy-button-line-height, 1.5) * var(--wy-size, 1rem) + 2 * var(--wy-button-padding-y, var(--wy-padding, calc(.5 * var(--wy-size, 1rem)))))))}[part~=wy-input-group-button-icon-overlay]{position:absolute;display:inline-flex;top:0;right:0;z-index:4;border-radius:var(--wy-input-border-radius, var(--wy-border-radius, calc(.5 * var(--wy-size, 1rem))));--wy-component-background-color: transparent}[part~=wy-input-group-input]:placeholder-shown~[part~=wy-input-group-button-icon][type=reset]{display:none}[part~=wy-input-group-input]:not(:placeholder-shown)~[part~=wy-input-group-button-icon][type=reset]+[part~=wy-input-group-button-icon]{display:none}[part~=wy-no-result]{text-align:center;padding:calc(1 * var(--wy-size, 1rem))}`;
var dC = Object.defineProperty, hC = Object.getOwnPropertyDescriptor, Wu = /* @__PURE__ */ n((s, t, e, i) => {
  for (var r = i > 1 ? void 0 : i ? hC(t, e) : t, a = s.length - 1, o; a >= 0; a--)
    (o = s[a]) && (r = (i ? o(t, e, r) : o(r)) || r);
  return i && r && dC(t, e, r), r;
}, "__decorateClass$T"), Vo;
let Ys = (Vo = class extends nt {
  constructor() {
    super(...arguments), this.exportParts = new W(this), this.whenPdfjs = new Promise((t) => {
      this.whenPdfjsResolve = t;
    }), this.MAX_CANVAS_PIXELS = 0, this.TEXT_LAYER_MODE = 0, this.MAX_IMAGE_SIZE = -1, this.CMAP_PACKED = !0, this.DEFAULT_SCALE_DELTA = 1.1, this.MIN_SCALE = 0.25, this.MAX_SCALE = 10, this.DEFAULT_SCALE_VALUE = "auto", this.ENABLE_XFA = !0, this.DEFAULT_WORKER_URL = "/pdfjs/pdf.worker.min.mjs", this.DEFAULT_CMAPS_URL = "/pdfjs/cmaps/", this.pageNumberRef = J(), this.totalPagesRef = J(), this.zoomLevelRef = J(), this.viewerContainerRef = J(), this.resizer = new ResizeObserver(() => {
      this.pdfViewer && (this.delayedResize && (clearTimeout(this.delayedResize), this.delayedResize = void 0), this.delayedResize = window.setTimeout(() => {
        this.pdfViewer && (this.pdfViewer.currentScaleValue = this.pdfViewer._currentScaleValue);
      }, 100));
    });
  }
  /**
   * Emit `file-preview-loaded` once the viewer has initialized.
   *
   * @returns {boolean} True if the event was not canceled.
   */
  dispatchLoaded() {
    const t = new CustomEvent("file-preview-loaded");
    return this.dispatchEvent(t);
  }
  ////////
  /**
   * Open the configured PDF source in the viewer.
   *
   * @internal
   */
  async open() {
    const { pdfjsLib: t } = await this.whenPdfjs;
    if (!this.pdfViewer || !this.pdfHistory || !this.l10n || !this.pdfLinkService)
      return;
    this.pdfLoadingTask && await this.close();
    const e = t.getDocument({
      url: this.src,
      maxImageSize: this.MAX_IMAGE_SIZE,
      enableXfa: this.ENABLE_XFA,
      cMapUrl: this.CMAP_URL?.toString() || "",
      cMapPacked: this.CMAP_PACKED
    });
    this.pdfLoadingTask = e, e.onProgress = (i) => {
    };
    try {
      const i = await e.promise;
      this.pdfDocument = i, this.pdfViewer.setDocument(i), this.pdfLinkService.setDocument(i), this.pdfHistory.initialize({
        // @ts-expect-error Type 'string | null' is not assignable to type 'string'.
        fingerprint: i.fingerprints[0]
      });
    } catch (i) {
      let r = "pdfjs-loading-error";
      i instanceof t.InvalidPDFException ? r = "pdfjs-invalid-file-error" : i instanceof t.MissingPDFException ? r = "pdfjs-missing-file-error" : i instanceof t.UnexpectedResponseException && (r = "pdfjs-unexpected-response-error"), await this.l10n.get(r, void 0, void 0).then((a) => {
        this.pdfViewError(t, a, { message: i?.message });
      });
    }
    this.dispatchLoaded();
  }
  /**
   * Close any currently loaded PDF and release resources.
   *
   * @internal
   */
  async close() {
    if (!this.pdfLoadingTask)
      return Promise.resolve();
    const t = this.pdfLoadingTask.destroy();
    return this.pdfLoadingTask = void 0, this.pdfDocument && (this.pdfDocument = void 0, this.pdfViewer?.setDocument(null), this.pdfLinkService?.setDocument(null, null), this.pdfHistory && this.pdfHistory.reset()), await t;
  }
  /**
   * Log a pdf.js related error with contextual metadata.
   *
   * @internal
   * @param pdfjsLib - pdf.js core library.
   * @param message - Human readable error.
   * @param moreInfo - Additional error metadata.
   */
  pdfViewError(t, e, i) {
    const r = [`PDF.js v${t?.version || "?"} (build: ${t?.build || "?"})`];
    i && (r.push(`Message: ${i.message}`), i.stack ? r.push(`Stack: ${i.stack}`) : (i.filename && r.push(`File: ${i.filename}`), i.lineNumber && r.push(`Line: ${i.lineNumber}`))), console.error(`${e}

${r.join(`
`)}`);
  }
  ///////
  /**
   * Navigate to the provided page number.
   *
   * @param pageNumber - One-based page index.
   */
  setPage(t) {
    this.pdfViewer && (this.pdfViewer.currentPageNumber = t);
  }
  /**
   * Increase zoom level by the configured delta.
   *
   * @param ticks - Number of zoom steps to apply.
   */
  zoomIn(t = 0) {
    if (this.pdfViewer) {
      let e = this.pdfViewer.currentScale;
      do
        e = parseFloat((e * this.DEFAULT_SCALE_DELTA).toFixed(2)), e = Math.ceil(e * 10) / 10, e = Math.min(this.MAX_SCALE, e);
      while (--t && e < this.MAX_SCALE);
      this.pdfViewer.currentScaleValue = e.toFixed(2);
    }
  }
  /**
   * Decrease zoom level by the configured delta.
   *
   * @param ticks - Number of zoom steps to apply.
   */
  zoomOut(t = 0) {
    if (this.pdfViewer) {
      let e = this.pdfViewer.currentScale;
      do
        e = parseFloat((e / this.DEFAULT_SCALE_DELTA).toFixed(2)), e = Math.floor(e * 10) / 10, e = Math.max(this.MIN_SCALE, e);
      while (--t && e > this.MIN_SCALE);
      this.pdfViewer.currentScaleValue = e.toFixed(2);
    }
  }
  /**
   * Apply an absolute zoom level or named scale preset.
   *
   * @param scale - Numeric zoom or preset name.
   */
  setScale(t) {
    this.pdfViewer && (this.pdfViewer.currentScaleValue = typeof t == "number" ? t.toFixed(2) : t);
  }
  /**
   * Validate and update the current page based on the input value.
   */
  updatePage() {
    if (this.pdfViewer && this.pageNumberRef.value) {
      const t = parseInt(this.pageNumberRef.value.value);
      isNaN(t) ? this.setPage(this.pdfViewer.currentPageNumber) : t > this.pdfViewer.pagesCount ? this.setPage(this.pdfViewer.pagesCount) : t <= 0 ? this.setPage(1) : this.setPage(t);
    }
  }
  /**
   * Select all text inside an input, aiding quick replacement.
   *
   * @param e - Input focus event.
   */
  select(t) {
    const e = t.target;
    e && e.setSelectionRange(0, e.value.length);
  }
  /**
   * Fit the PDF to the current viewport height.
   */
  fitToPage() {
    this.setScale("page-fit");
  }
  /**
   * Fit the PDF to the current viewport width.
   */
  fitToWidth() {
    this.setScale("page-width");
  }
  /**
   * Validate and persist zoom level from the input field.
   */
  updateZoom() {
    if (this.pdfViewer && this.zoomLevelRef.value) {
      const t = parseFloat(this.zoomLevelRef.value.value.replace("%", ""));
      isNaN(t) ? this.setScale(this.pdfViewer.currentScale + 1e-4) : this.setScale(t / 100);
    }
  }
  // clearDocument() {
  //   try {
  //     this.loadingTask?.destroy();
  //     //console.debug("loadingTask cleanup", loadingTask)
  //   } catch (e) {
  //     /* No worries */
  //   }
  //   try {
  //     // eslint-disable-next-line @typescript-eslint/ban-ts-comment
  //     // @ts-ignore due to incorrect param type def?
  //     this.pdfViewer?.setDocument(null);
  //   } catch (e) {
  //     /* No worries */
  //   }
  //   try {
  //     this.pdfLinkService.setDocument(null, null);
  //   } catch (e) {
  //     /* No worries */
  //   }
  // }
  async willUpdate(t) {
    if (super.willUpdate(t), t.has("weavy") && this.weavy) {
      if (this.pdfjsLib || (await this.weavy.whenUrl(), this.pdfjsLib = await import(
        /* webpackIgnore: true */
        /* @vite-ignore */
        new URL("./es/pdfjs-dist/build/pdf.mjs-C9mQiY1P.js", typeof WEAVY_IMPORT_URL == "string" && (!import.meta.url || !new URL(import.meta.url).href.startsWith(WEAVY_IMPORT_URL)) ? new URL(WEAVY_IMPORT_URL, globalThis.location.href) : import.meta.url).href
      ), globalThis.pdfjsLib = this.pdfjsLib, this.pdfjsViewer = await import(
        /* webpackIgnore: true */
        /* @vite-ignore */
        new URL("./es/pdfjs-dist/web/pdf_viewer.mjs-CR_ho08l.js", typeof WEAVY_IMPORT_URL == "string" && (!import.meta.url || !new URL(import.meta.url).href.startsWith(WEAVY_IMPORT_URL)) ? new URL(WEAVY_IMPORT_URL, globalThis.location.href) : import.meta.url).href
      ), this.whenPdfjsResolve?.({
        pdfjsLib: this.pdfjsLib,
        pdfjsViewer: this.pdfjsViewer
      })), this.pdfjsLib && !this.WORKER_URL)
        try {
          this.WORKER_URL = Gm(this.DEFAULT_WORKER_URL, import.meta.url), this.WORKER_URL.searchParams.append("v", this.weavy.version), this.pdfjsLib.GlobalWorkerOptions.workerSrc = this.WORKER_URL.toString();
        } catch {
          console.warn("Invalid PDF worker source, using fake worker instead.");
        }
      if (!this.CMAP_URL)
        try {
          this.CMAP_URL = Gm(this.DEFAULT_CMAPS_URL, import.meta.url);
        } catch {
          console.warn("Invalid PDF CMAPS source, skipping CMAPS.");
        }
    }
  }
  update(t) {
    super.update(t), (t.has("weavy") || t.has("src") || t.has("pdfViewer")) && this.weavy && this.src && this.pdfViewer && this.open();
  }
  async updated() {
    if (this.weavy && this.viewerContainerRef.value && !this.pdfViewer) {
      const { pdfjsLib: t, pdfjsViewer: e } = await this.whenPdfjs;
      this.viewerContainerRef.value && !this.pdfViewer && (this.pdfEventBus = new e.EventBus(), this.pdfLinkService = new e.PDFLinkService({
        eventBus: this.pdfEventBus
      }), this.l10n = new e.GenericL10n(this.weavy?.locale), this.pdfViewer = new e.PDFViewer({
        container: this.viewerContainerRef.value,
        eventBus: this.pdfEventBus,
        linkService: this.pdfLinkService,
        //findController: this.pdfFindController,
        annotationEditorMode: t.AnnotationEditorType.DISABLE,
        l10n: this.l10n,
        maxCanvasPixels: this.MAX_CANVAS_PIXELS,
        textLayerMode: this.TEXT_LAYER_MODE
      }), this.pdfLinkService?.setViewer(this.pdfViewer), this.pdfHistory = new e.PDFHistory({
        eventBus: this.pdfEventBus,
        linkService: this.pdfLinkService
      }), this.pdfLinkService?.setHistory(this.pdfHistory), this.pdfEventBus?.on("scalechanging", () => {
        this.zoomLevelRef.value && this.pdfViewer ? this.zoomLevelRef.value.value = Math.round(this.pdfViewer.currentScale * 100).toFixed(0) + "%" : console.warn("Could not set zoom level");
      }), this.pdfEventBus?.on("pagechanging", () => {
        this.pageNumberRef.value && this.pdfViewer ? this.pageNumberRef.value.value = this.pdfViewer.currentPageNumber.toFixed(0) : console.warn("Could not set page number");
      }), this.pdfEventBus?.on("pagesinit", () => {
        this.isConnected && this.pdfViewer && this.pageNumberRef.value && this.totalPagesRef.value ? (this.pdfViewer.currentScaleValue = this.DEFAULT_SCALE_VALUE, this.pageNumberRef.value.value = "1", this.totalPagesRef.value.innerText = this.pdfViewer.pagesCount.toFixed(0), this.resizer.observe(this.pdfViewer.container)) : console.warn("Could not init pdf page");
      }));
    }
  }
  render() {
    return h`
      <div class="wy-content-pdf">
        <div part="wy-toolbars-bottom">
          <nav part="wy-toolbar wy-toolbar-center">
            <div part="wy-toolbar-buttons">
              <input
                type="text"
                part="wy-input wy-toolbar-center-text"
                class="wy-pdf-page-number"
                ${K(this.pageNumberRef)}
                @keydown=${Gl}
                @keyup=${lr}
                @change=${() => this.updatePage()}
                @click=${(t) => this.select(t)}
              />
              <span part="wy-toolbar-text">/</span>
              <span part="wy-toolbar-text" ${K(this.totalPagesRef)}>1</span>
            </div>
            <div part="wy-toolbar-buttons">
              <wy-button kind="icon" class="btn-zoom-out" @click=${() => this.zoomOut()} title=${x("Zoom out")}>
                <wy-icon name="minus"></wy-icon>
              </wy-button>
              <input
                type="text"
                part="wy-input"
                class="wy-pdf-zoom-level"
                ${K(this.zoomLevelRef)}
                @keydown=${Gl}
                @keyup=${lr}
                @change=${() => this.updateZoom()}
                @click=${(t) => this.select(t)}
                value="100%"
              />
              <wy-button kind="icon" class="btn-zoom-in" @click=${() => this.zoomIn()} title=${x("Zoom in")}>
                <wy-icon name="plus"></wy-icon>
              </wy-button>
            </div>
            <div part="wy-toolbar-buttons">
              <wy-button kind="icon" class="btn-fit-page" @click=${() => this.fitToWidth()} title=${x("Fit to width")}>
                <wy-icon name="fit-width"></wy-icon>
              </wy-button>
              <wy-button kind="icon" @click=${() => this.fitToPage()} title=${x("Fit to screen")}>
                <wy-icon name="fit-screen"></wy-icon>
              </wy-button>
            </div>
          </nav>
        </div>
        <div ${K(this.viewerContainerRef)} class="wy-pdf-container">
          <div class="pdfViewer"></div>
        </div>
      </div>
    `;
  }
  connectedCallback() {
    super.connectedCallback(), this.pdfViewer && this.resizer.observe(this.pdfViewer.container);
  }
  disconnectedCallback() {
    this.resizer.unobserve(this);
    try {
      this.close(), this.pdfViewer?.cleanup();
    } catch {
    }
    this.pdfViewer = void 0, super.disconnectedCallback();
  }
}, n(Vo, "WyPdfViewer"), Vo);
Ys.styles = [
  lC,
  pg,
  us,
  Y
];
Wu([
  he({ context: Xt, subscribe: !0 }),
  V()
], Ys.prototype, "weavy", 2);
Wu([
  y()
], Ys.prototype, "src", 2);
Wu([
  V()
], Ys.prototype, "pdfViewer", 2);
Ys = Wu([
  U("wy-pdf-viewer"),
  ut()
], Ys);
var uC = Object.defineProperty, pC = Object.getOwnPropertyDescriptor, Ww = /* @__PURE__ */ n((s, t, e, i) => {
  for (var r = i > 1 ? void 0 : i ? pC(t, e) : t, a = s.length - 1, o; a >= 0; a--)
    (o = s[a]) && (r = (i ? o(t, e, r) : o(r)) || r);
  return i && r && uC(t, e, r), r;
}, "__decorateClass$S"), Ho;
let nl = (Ho = class extends nt {
  constructor() {
    super(...arguments), this.exportParts = new W(this), this.current = !1;
  }
  /**
   * Emits the `file-preview-loaded` event when the preview has finished loading.
   *
   * @internal
   * @returns {boolean} `true` if the event was not canceled.
   */
  dispatchLoaded() {
    const t = new CustomEvent("file-preview-loaded", {
      detail: { file: this.file }
    });
    return this.dispatchEvent(t);
  }
  render() {
    const t = this.file;
    if (!t) return g;
    const { icon: e } = cs(t.name), i = ai(t.name);
    let r = t.preview_url || t.download_url || "";
    t.preview_format === "image" && (i === ".gif" || i === ".svg") && (r = t.download_url || "");
    const o = r, c = t.external_url, l = t.is_trashed ? "none" : t.preview_format, u = t.name, b = t.width, C = t.height, v = t.media_type, f = t.provider;
    return l === "image" ? h`<wy-preview-image
        src=${o}
        width=${ot(b)}
        height=${ot(C)}
        @file-preview-loaded=${() => this.dispatchLoaded()}
      ></wy-preview-image>` : l === "pdf" ? h`<wy-pdf-viewer src=${o} @file-preview-loaded=${() => this.dispatchLoaded()}></wy-pdf-viewer>` : l === "video" || l === "audio" ? h`<wy-preview-media
        format=${l}
        src=${o}
        name=${u}
        mediaType=${v}
        ?play=${this.current}
        @file-preview-loaded=${() => this.dispatchLoaded()}
      ></wy-preview-media>` : l === "text" ? h`<wy-preview-text src=${o} @file-preview-loaded=${() => this.dispatchLoaded()}></wy-preview-text>` : l === "code" ? h`<wy-preview-text
        src=${o}
        ?html=${!/^(?:blob:|data:)/.test(o)}
        code
        @file-preview-loaded=${() => this.dispatchLoaded()}
      ></wy-preview-text>` : l === "html" ? h`<wy-preview-text
        src=${o}
        html
        @file-preview-loaded=${() => this.dispatchLoaded()}
      ></wy-preview-text>` : l === "embed" ? h`<wy-preview-embed
        src=${o}
        name=${u}
        icon=${e}
        provider=${ot(f)}
        @file-preview-loaded=${() => this.dispatchLoaded()}
      ></wy-preview-embed>` : l === "none" ? c ? h`<wy-preview-icon
            src=${c}
            icon=${e}
            provider=${ot(f)}
            @file-preview-loaded=${() => this.dispatchLoaded()}
          ></wy-preview-icon>` : h`<wy-preview-icon
            src=${o}
            icon=${e}
            @file-preview-loaded=${() => this.dispatchLoaded()}
          ></wy-preview-icon>` : g;
  }
}, n(Ho, "WyPreviewItem"), Ho);
nl.styles = [Y];
Ww([
  y({ type: Object })
], nl.prototype, "file", 2);
Ww([
  y({ type: Boolean })
], nl.prototype, "current", 2);
nl = Ww([
  U("wy-preview-item")
], nl);
var yC = Object.defineProperty, wC = Object.getOwnPropertyDescriptor, sh = /* @__PURE__ */ n((s, t, e, i) => {
  for (var r = i > 1 ? void 0 : i ? wC(t, e) : t, a = s.length - 1, o; a >= 0; a--)
    (o = s[a]) && (r = (i ? o(t, e, r) : o(r)) || r);
  return i && r && yC(t, e, r), r;
}, "__decorateClass$R"), Uo;
let Xs = (Uo = class extends nt {
  constructor() {
    super(...arguments), this.exportParts = new W(this), this.small = !1, this.hasEventListener = {
      "edit-name": !1,
      subscribe: !1,
      trash: !1,
      restore: !1,
      "delete-forever": !1
    };
  }
  addEventListener(t, e, i) {
    if (this.hasEventListener) {
      const r = t;
      Object.prototype.hasOwnProperty.call(this.hasEventListener, r) && Object.assign(this.hasEventListener, {
        [r]: !0
      });
    }
    super.addEventListener(t, e, i);
  }
  /**
   * Open download URL for the current file.
   * @internal
   */
  triggerDownload() {
    this.file && rr(this.file.download_url, "_top", this.file.name, !0);
  }
  /**
   * Open external provider URL for the current file.
   * @internal
   */
  triggerExternal() {
    this.file && rr(this.file.external_url, "_blank", this.file.name);
  }
  /**
   * Open application-specific URL for the current file.
   * @internal
   */
  triggerApplication() {
    this.file && rr(this.file.application_url, "_top", this.file.name);
  }
  /**
   * Emit an `edit-name` event for the current file.
   * @internal
   */
  dispatchEditName() {
    const t = new CustomEvent("edit-name", {
      detail: { file: this.file }
    });
    return this.dispatchEvent(t);
  }
  /**
   * Emit a `subscribe` event toggling subscription state.
   *
   * @internal
   * @param subscribe - Desired subscription setting.
   */
  dispatchSubscribe(t) {
    const e = new CustomEvent("subscribe", {
      detail: {
        file: this.file,
        subscribe: t
      }
    });
    return this.dispatchEvent(e);
  }
  /**
   * Emit a `trash` event for the current file.
   * @internal
   */
  dispatchTrash() {
    const t = new CustomEvent("trash", {
      detail: { file: this.file }
    });
    return this.dispatchEvent(t);
  }
  /**
   * Emit a `restore` event for the current file.
   * @internal
   */
  dispatchRestore() {
    const t = new CustomEvent("restore", {
      detail: { file: this.file }
    });
    return this.dispatchEvent(t);
  }
  /**
   * Emit a `delete-forever` event for the current file.
   * @internal
   */
  dispatchDeleteForever() {
    const t = new CustomEvent("delete-forever", {
      detail: { file: this.file }
    });
    return this.dispatchEvent(t);
  }
  render() {
    if (!this.file)
      return g;
    const { icon: t } = cs(this.file.name), e = this.file.id >= 1, i = this.file.provider, r = this.file.provider || "app";
    return h`
      <wy-dropdown directionX="left" ?small=${this.small}>
        ${e && this.file.is_trashed ? h`
              ${this.hasEventListener.restore ? h`
                    <wy-dropdown-item @click=${() => this.dispatchRestore()}>
                      <wy-icon name="delete-restore"></wy-icon>
                      ${x("Restore")}
                    </wy-dropdown-item>
                  ` : g}
              ${this.hasEventListener.restore && this.hasEventListener["delete-forever"] ? h` <wy-dropdown-divider></wy-dropdown-divider> ` : g}
              ${this.hasEventListener["delete-forever"] ? h`
                    <wy-dropdown-item @click=${() => this.dispatchDeleteForever()}>
                      <wy-icon name="delete-forever"></wy-icon>
                      ${x("Delete")}
                    </wy-dropdown-item>
                  ` : g}
            ` : g}
        ${this.file.is_trashed ? g : h`
              ${this.file.external_url ? h`
                    <wy-dropdown-item @click=${() => this.triggerExternal()}>
                      <wy-icon name=${t}></wy-icon>
                      ${x(gt`Open in ${i}`)}
                    </wy-dropdown-item>
                  ` : h`
                    ${this.componentFeatures?.allowsFeature(_.WebDAV) && this.file.application_url ? h`
                          <wy-dropdown-item @click=${() => this.triggerApplication()}>
                            <wy-icon
                              name=${this.file.provider ? Mv(this.file.provider) : t}
                            ></wy-icon>
                            ${x(gt`Open in ${r}`)}
                          </wy-dropdown-item>
                        ` : g}
                    <wy-dropdown-item @click=${() => this.triggerDownload()}>
                      <wy-icon name="download"></wy-icon>
                      ${x("Download")}
                    </wy-dropdown-item>
                  `}
              ${e ? h`
                    ${this.hasEventListener["edit-name"] ? h`
                          <wy-dropdown-item @click=${() => this.dispatchEditName()}>
                            <wy-icon name="textbox"></wy-icon>
                            ${x("Rename")}
                          </wy-dropdown-item>
                        ` : g}
                    ${this.hasEventListener.subscribe ? this.file.is_subscribed ? h`
                            <wy-dropdown-item @click=${() => this.dispatchSubscribe(!1)}>
                              <wy-icon name="bell-off"></wy-icon>
                              ${x("Unsubscribe")}
                            </wy-dropdown-item>
                          ` : h`
                            <wy-dropdown-item @click=${() => this.dispatchSubscribe(!0)}>
                              <wy-icon name="bell"></wy-icon>
                              ${x("Subscribe")}
                            </wy-dropdown-item>
                          ` : g}
                    ${this.hasEventListener.trash ? h`
                          <wy-dropdown-divider></wy-dropdown-divider>
                          <wy-dropdown-item @click=${() => this.dispatchTrash()}>
                            <wy-icon name="delete"></wy-icon>
                            ${x("Trash")}
                          </wy-dropdown-item>
                        ` : g}
                  ` : g}
            `}
        <slot></slot>
      </wy-dropdown>
    `;
  }
}, n(Uo, "WyFileMenu"), Uo);
sh([
  he({ context: xu, subscribe: !0 }),
  V()
], Xs.prototype, "componentFeatures", 2);
sh([
  y({ type: Object })
], Xs.prototype, "file", 2);
sh([
  y({ type: Boolean })
], Xs.prototype, "small", 2);
sh([
  y({ type: Object })
], Xs.prototype, "hasEventListener", 2);
Xs = sh([
  U("wy-file-menu"),
  ut()
], Xs);
function fC(s, t, e, i = {}) {
  return {
    ...i,
    initialPageParam: 0,
    queryKey: [t, "comments", e],
    queryFn: /* @__PURE__ */ n(async (r) => {
      const a = r.pageParam, o = "/api/" + t + "/" + e + "/comments?order_by=id&skip=" + a, l = await (await s.fetch(o)).json();
      return l.data = l.data || [], l;
    }, "queryFn"),
    getNextPageParam: /* @__PURE__ */ n((r) => {
      if (r.end && r.end < r.count)
        return r.end;
    }, "getNextPageParam")
  };
}
n(fC, "getCommentsOptions");
function mC(s, t) {
  return {
    mutationFn: /* @__PURE__ */ n(async (i) => await (await s.fetch("/api/comments/" + i.id, {
      method: "PATCH",
      body: JSON.stringify({
        text: i.text,
        blobs: i.blobs,
        attachments: i.attachments,
        meeting_id: i.meeting_id,
        options: i.poll_options.filter((a) => a.text.trim() !== "").map((a) => ({ id: a.id, text: a.text })),
        embed_id: i.embed_id || null
      })
    })).json(), "mutationFn"),
    mutationKey: t,
    onSuccess: /* @__PURE__ */ n((i, r) => {
      r.id && Ee(s.queryClient, [r.type, r.parent_id, "comments"], r.id, () => i);
    }, "onSuccess")
  };
}
n(mC, "getUpdateCommentMutationOptions");
function vC(s) {
  const t = s.queryClient;
  return {
    mutationFn: /* @__PURE__ */ n(async (i) => await (await s.fetch("/api/" + i.type + "/" + i.parent_id + "/comments", {
      method: "POST",
      body: JSON.stringify({
        text: i.text,
        blobs: i.blobs,
        meeting_id: i.meeting_id,
        options: i.poll_options.filter((a) => a.text.trim() !== "").map((a) => ({ text: a.text })),
        embed_id: i.embed_id,
        context: i.context
      })
    })).json(), "mutationFn"),
    onMutate: /* @__PURE__ */ n(async (i) => {
      const r = [i.type, "comments", i.parent_id];
      await t.cancelQueries({ queryKey: r });
      const a = Kl(s.queryClient, r, !1);
      if (i.user) {
        const o = {
          id: a ? a.id - 1 : -1,
          app: i.type === "apps" ? { id: i.parent_id } : { id: -1 },
          is_trashed: !1,
          text: i.text,
          html: i.text,
          plain: i.text,
          created_by: i.user,
          created_at: (/* @__PURE__ */ new Date()).toUTCString(),
          attachments: { count: 0 },
          reactions: { count: 0 },
          is_subscribed: !1,
          is_starred: !1
        };
        i.type === "files" ? o.parent = { type: It.File, id: i.parent_id } : i.type === "posts" && (o.parent = { type: It.Post, id: i.parent_id }), Wl(t, r, o, { descending: !1 });
      }
    }, "onMutate"),
    onSuccess: /* @__PURE__ */ n((i, r) => {
      const a = [r.type, "comments", i.parent?.id ?? i.app?.id];
      if (!Yd(s.queryClient, a, i.id)) {
        const c = Kl(s.queryClient, a, !0);
        c ? Ee(s.queryClient, a, c.id, () => i) : Wl(s.queryClient, a, i), i.parent?.type === It.Post && i.app && (ht(t, { queryKey: ["posts", i.app.id], exact: !1 }, i.parent.id, (l) => (l.comments ? l.comments.count += 1 : l.comments = { count: 1 }, l)), ht(t, { queryKey: ["posts", "feed"], exact: !1 }, i.parent.id, (l) => (l.comments ? l.comments.count += 1 : l.comments = { count: 1 }, l)));
      }
    }, "onSuccess")
  };
}
n(vC, "getAddCommentMutationOptions");
function gC(s, t, e) {
  const i = s.queryClient, a = {
    mutationKey: [t, e, "comments"],
    mutationFn: /* @__PURE__ */ n(async ({ id: o }) => {
      if (!(await s.fetch("/api/comments/" + o + "/trash", { method: "POST" })).ok)
        throw new Error();
    }, "mutationFn"),
    onMutate: /* @__PURE__ */ n((o) => {
      ht(
        i,
        { queryKey: a.mutationKey, exact: !1 },
        o.id,
        (c) => ({ ...c, is_trashed: !0 })
      );
    }, "onMutate"),
    onSuccess: /* @__PURE__ */ n((o, c) => {
      Ee(i, [c.type, c.appId], c.parentId, (l) => (l.comments.count -= 1, l)), c.type === "posts" && Ee(i, ["posts", "feed"], c.parentId, (l) => (l.comments.count -= 1, l));
    }, "onSuccess")
  };
  return a;
}
n(gC, "getTrashCommentMutationOptions");
function bC(s, t, e) {
  return new St(s.queryClient, gC(s, t, e));
}
n(bC, "getTrashCommentMutation");
function xC(s, t, e) {
  const i = s.queryClient, a = {
    mutationKey: [t, e, "comments"],
    mutationFn: /* @__PURE__ */ n(async ({ id: o }) => {
      const c = await s.fetch("/api/comments/" + o + "/restore", { method: "POST" });
      if (!c.ok) {
        const l = await c.json();
        throw new Error(l.detail || l.title, { cause: l });
      }
    }, "mutationFn"),
    onMutate: /* @__PURE__ */ n((o) => {
      ht(
        i,
        { queryKey: a.mutationKey, exact: !1 },
        o.id,
        (c) => ({ ...c, is_trashed: !1 })
      );
    }, "onMutate"),
    onSuccess: /* @__PURE__ */ n((o, c) => {
      Ee(i, [c.type, c.appId], c.parentId, (l) => (l.comments.count += 1, l));
    }, "onSuccess")
  };
  return a;
}
n(xC, "getRestoreCommentMutationOptions");
function CC(s, t, e) {
  return new St(s.queryClient, xC(s, t, e));
}
n(CC, "getRestoreCommentMutation");
const jw = j`@keyframes wy-highlight{0%{--wy-component-background-color: var(--wy-highlight, var(--wy-highlight-light, #e8f2ff));--wy-component-color: var(--wy-on-highlight, var(--wy-on-highlight-light, #001d32))}}:host(wy-comments){--wy-component-background-color: var(--wy-surface-container, var(--wy-surface-container-light, #eceef3));--wy-component-border-radius: var(--wy-border-radius-outer, var(--wy-border-radius, calc(.5 * var(--wy-size, 1rem))))}[part~=wy-comment-list-padded]{padding:var(--wy-padding, calc(.5 * var(--wy-size, 1rem)))}[part~=wy-comments]{display:flex;flex-direction:column}[part~=wy-comment]{display:flex;flex-direction:column;gap:var(--wy-gap-sm, calc(.5 * var(--wy-gap, var(--wy-padding, calc(.5 * var(--wy-size, 1rem))))));margin-bottom:var(--wy-gap-sm, calc(.5 * var(--wy-gap, var(--wy-padding, calc(.5 * var(--wy-size, 1rem))))))}[part~=wy-comment][part~=wy-highlight]{animation:wy-highlight 3s 1.5s ease-in-out both}[part~=wy-comment-reveal]{interpolate-size:allow-keywords;overflow:hidden;transition:height var(--wy-transition-fast, var(--wy-transition-duration-fast, .1s) var(--wy-transition-curve, cubic-bezier(.4, 0, .2, 1))),margin-bottom var(--wy-transition-fast, var(--wy-transition-duration-fast, .1s) var(--wy-transition-curve, cubic-bezier(.4, 0, .2, 1))),opacity var(--wy-transition-fast, var(--wy-transition-duration-fast, .1s) var(--wy-transition-curve, cubic-bezier(.4, 0, .2, 1))) var(--wy-transition-duration-fast, .1s);height:auto;opacity:1}@starting-style{[part~=wy-comment-reveal]{height:0;margin-bottom:0;opacity:0}}[part~=wy-comment-skeleton]{transition-delay:var(--wy-transition-reveal-delay, 1s)}[part~=wy-comment-body]{display:flex;flex-direction:column;gap:var(--wy-gap-lg, calc(1.5 * var(--wy-gap, var(--wy-padding, calc(.5 * var(--wy-size, 1rem))))));min-width:0;flex:1 1 100%}[part~=wy-comment-footer]{margin-left:calc(-1 * var(--wy-padding, calc(.5 * var(--wy-size, 1rem))));margin-top:calc(-1 * var(--wy-padding, calc(.5 * var(--wy-size, 1rem))))}::part(wy-trashed){text-decoration:line-through;color:var(--wy-outline, var(--wy-outline-light, #72777f))}wy-comment::part(wy-editor){padding-left:0;padding-right:0;margin-right:calc(-1 * var(--wy-size, 1rem))}`, $C = 1e3 * 60 * 60, Zp = $C * 24;
function bl(s, t, e, i = 7) {
  e ??= /* @__PURE__ */ new Date();
  const r = new Date(e.getFullYear(), e.getMonth(), e.getDate()), a = t.valueOf() - e.valueOf(), o = t.toDateString() === e.toDateString(), c = t.valueOf() > r.valueOf() - Zp * i && t.valueOf() < r.valueOf() + Zp * i;
  if (!o && c) {
    const l = Math.round(a / Zp);
    return new Intl.RelativeTimeFormat(s, { numeric: "auto" }).format(l, "days");
  } else return o ? new Intl.DateTimeFormat(s, { timeStyle: "short" }).format(t) : new Intl.DateTimeFormat(s, { dateStyle: "short" }).format(t);
}
n(bl, "relativeTime");
const yg = j`@keyframes wy-highlight{0%{--wy-component-background-color: var(--wy-highlight, var(--wy-highlight-light, #e8f2ff));--wy-component-color: var(--wy-on-highlight, var(--wy-on-highlight-light, #001d32))}}`;
var kC = Object.defineProperty, _C = Object.getOwnPropertyDescriptor, yn = /* @__PURE__ */ n((s, t, e, i) => {
  for (var r = i > 1 ? void 0 : i ? _C(t, e) : t, a = s.length - 1, o; a >= 0; a--)
    (o = s[a]) && (r = (i ? o(t, e, r) : o(r)) || r);
  return i && r && kC(t, e, r), r;
}, "__decorateClass$Q"), Bo;
let Ii = (Bo = class extends qt {
  constructor() {
    super(...arguments), this.exportParts = new W(this), this.location = "apps", this.highlight = !1, this.reveal = !1, this.editing = !1, this.previewAnnotationsRef = J(), this.previewAttachmentsRef = J(), this.highlightRef = J(), this.updateCommentMutation = new Ue(this), this.dispatchUserAction = Bw.bind(this);
  }
  /**
   * Dispatch a poll vote event for this comment.
   *
   * @internal
   */
  dispatchVote(t) {
    const e = new CustomEvent("vote", {
      detail: { optionId: t, parentId: this.comment.id, parentType: "comments" }
    });
    return this.dispatchEvent(e);
  }
  /**
   * Dispatch a trash event for this comment.
   *
   * @internal
   */
  dispatchTrash() {
    const t = new CustomEvent("trash", { detail: { id: this.comment.id } });
    return this.dispatchEvent(t);
  }
  /**
   * Dispatch a restore event for this comment.
   *
   * @internal
   */
  dispatchRestore() {
    const t = new CustomEvent("restore", {
      detail: { id: this.comment.id }
    });
    return this.dispatchEvent(t);
  }
  /**
   * Submit updated comment content via mutation and exit edit mode.
   *
   * @internal
   */
  updateComment(t) {
    this.updateCommentMutation.mutate({
      id: this.comment.id,
      type: this.location,
      parent_id: this.parentId,
      text: t.detail.text,
      meeting_id: t.detail.meetingId,
      blobs: t.detail.blobs,
      attachments: t.detail.attachments,
      poll_options: t.detail.pollOptions,
      embed_id: t.detail.embedId
    }), this.editing = !1;
  }
  async willUpdate(t) {
    super.willUpdate(t), (t.has("parentId") || t.has("weavy")) && this.parentId && this.weavy && await this.updateCommentMutation.trackMutation(
      mC(this.weavy, [this.location, this.parentId, "comments"])
    ), t.has("link") && (this.highlight = !!(this.link && Vu(this.link, It.Comment, { id: this.comment.id }))), t.has("highlight") && (this.highlight ? this.part.add("wy-highlight") : this.part.remove("wy-highlight"));
  }
  render() {
    const t = this.comment.attachments?.data?.filter((o) => o.kind === "image" && o.thumbnail_url) || [], e = this.comment.attachments?.data?.filter((o) => o.kind !== "image" || !o.thumbnail_url) || [], i = new Intl.DateTimeFormat(this.weavy?.locale, {
      dateStyle: "full",
      timeStyle: "short"
    }).format(new Date(this.comment.created_at)), r = bl(this.weavy?.locale, new Date(this.comment.created_at)), a = this.comment.id < 0;
    return h`
      <div part="wy-comment ${tt({ "wy-comment-reveal": this.reveal, "wy-comment-skeleton": a })}">
        ${a ? h`
              <wy-item align="top" part="wy-comment-header">
                <wy-avatar
                  slot="image"
                  .src="${this.comment.created_by.avatar_url}"
                  .size=${32}
                  .name=${this.comment.created_by.name}
                  .isAgent=${this.comment.created_by.is_agent}
                ></wy-avatar>
                <div slot="content" part="wy-comment-body">
                  <div part="wy-comment-title">
                    ${this.comment.created_by.name}
                    <small part="wy-meta">
                      ·
                      <time datetime=${this.comment.created_at} title=${i}>${r}</time>
                      ${this.comment.updated_at ? h`<time datetime=${this.comment.updated_at}> · ${x("edited")}</time>` : g}
                    </small>
                  </div>
                  ${this.comment.html ? h`<div part="wy-content"><wy-skeleton .text=${this.comment.text}></wy-skeleton></div>` : ""}
                </div>
              </wy-item>
            ` : this.comment.is_trashed ? h`
                <wy-item part="wy-comment-header">
                  <wy-avatar
                    slot="image"
                    .src="${this.comment.created_by.avatar_url}"
                    .size=${32}
                    .name=${this.comment.created_by.name}
                    .isAgent=${this.comment.created_by.is_agent}
                  ></wy-avatar>
                  <span part="wy-trashed" slot="title">${x("Comment was trashed.")}</span>
                  <wy-button small slot="actions" @click=${() => this.dispatchRestore()} color="variant"
                    >${x("Undo")}</wy-button
                  >
                </wy-item>
              ` : this.editing ? h`
                  <wy-item align="top" part="wy-comment-header">
                    <wy-avatar
                      slot="image"
                      .src="${this.comment.created_by.avatar_url}"
                      .size=${32}
                      .name=${this.comment.created_by.name}
                      .isAgent=${this.comment.created_by.is_agent}
                    ></wy-avatar>
                    <wy-button small slot="actions" @click=${() => this.editing = !1} kind="icon">
                      <wy-icon name="close"></wy-icon>
                    </wy-button>
                    <div slot="content" part="wy-comment-body">
                      <div part="wy-comment-title">
                        ${this.comment.created_by.name}

                        <small part="wy-meta">
                          ·
                          <time datetime=${this.comment.created_at} title=${i}>${r}</time>
                        </small>
                      </div>
                      <wy-editor-msg
                        editorLocation=${this.location}
                        .text=${this.comment.text}
                        .embed=${this.comment.embed}
                        .pollOptions=${this.comment.options?.data ?? []}
                        .attachments=${this.comment.attachments?.data ?? []}
                        .parentId=${this.comment.id}
                        .typing=${!1}
                        .draft=${!1}
                        placeholder=${x("Edit comment...")}
                        buttonText=${x("Update", { desc: "Button action to update" })}
                        @submit=${(o) => this.updateComment(o)}
                        @ready=${async (o) => {
      const c = o.target;
      await c.focusInput(), await c.selectAllContent();
    }}
                      ></wy-editor-msg>
                    </div>
                  </wy-item>
                ` : h`
                  <wy-item align="top" part="wy-comment-header">
                    <wy-avatar
                      slot="image"
                      .src=${this.comment.created_by.avatar_url}
                      .size=${32}
                      .name=${this.comment.created_by.name}
                      .isAgent=${this.comment.created_by.is_agent}
                      @click=${() => this.dispatchUserAction(this.comment.created_by)}
                      role="button"
                      title=${this.comment.created_by.name}
                    ></wy-avatar>

                    ${this.user && this.user.id === this.comment.created_by.id ? h`
                          <wy-dropdown small slot="actions">
                            ${this.user.id === this.comment.created_by.id ? h`
                                  <wy-dropdown-item @click=${() => this.editing = !0}>
                                    <wy-icon name="pencil"></wy-icon>
                                    ${x("Edit")}
                                  </wy-dropdown-item>
                                ` : g}
                            ${this.user.id === this.comment.created_by.id ? h`
                                  <wy-dropdown-item @click=${() => this.dispatchTrash()}>
                                    <wy-icon name="trashcan"></wy-icon>
                                    ${x("Trash")}
                                  </wy-dropdown-item>
                                ` : g}
                          </wy-dropdown>
                        ` : g}

                    <div slot="content" part="wy-comment-body">
                      <div part="wy-comment-title">
                        <wy-button kind="link" @click=${() => this.dispatchUserAction(this.comment.created_by)}
                          >${this.comment.created_by.name}</wy-button
                        >
                        <small part="wy-meta">
                          ·
                          <time datetime=${this.comment.created_at} title=${i}>${r}</time>
                          ${this.comment.updated_at ? h`<time datetime=${this.comment.updated_at}> · ${x("edited")}</time>` : g}
                        </small>
                      </div>

                      <!-- image grid -->
                      ${t && t.length ? h`<wy-image-grid
                            part="wy-comment-images"
                            .images=${t}
                            @file-open=${(o) => {
      this.previewAttachmentsRef.value?.open(o.detail.fileId);
    }}
                          ></wy-image-grid>` : ""}

                      <!-- text content -->
                      ${this.comment.html ? h`<div
                            part="wy-content"
                            @click=${(o) => {
      if (o.target instanceof HTMLElement && o.target.matches('.wy-mention, [part~="wy-mention"]')) {
        const c = o.target.dataset.eid?.startsWith("u") && parseInt(o.target.dataset.eid.substring(1));
        c && this.dispatchUserAction({ id: c, name: o.target.innerText });
      }
    }}
                            >${// eslint-disable-next-line lit-a11y/click-events-have-key-events
    Bs(this.comment.html)}</div
                          >` : ""}

                      <!-- annotations -->
                      ${this.comment.annotations?.data?.length ? h`<wy-annotation-list
                            .files=${this.comment.annotations.data}
                            @file-open=${(o) => {
      this.previewAnnotationsRef.value?.open(o.detail.fileId);
    }}
                          ></wy-annotation-list>` : g}

                      <!-- poll -->
                      ${this.comment.options?.data?.length ? h`
                            <wy-poll
                              .pollOptions=${this.comment.options.data}
                              @vote=${(o) => this.dispatchVote(o.detail.optionId)}
                            ></wy-poll>
                          ` : g}

                      <!-- embeds -->
                      ${this.comment.embed && this.componentFeatures?.allowsFeature(_.Embeds) ? h` <wy-embed .embed=${this.comment.embed}></wy-embed> ` : g}

                      <!-- files -->
                      ${e.length ? h`<wy-attachment-list
                            filled
                            .files=${e ?? []}
                            @file-open=${(o) => {
      this.previewAttachmentsRef.value?.open(o.detail.fileId);
    }}
                          ></wy-attachment-list>` : ""}

                      <!-- meeting -->
                      ${this.comment.meeting ? h`<wy-meeting-card .meeting=${this.comment.meeting}></wy-meeting-card>` : ""}

                      <div part="wy-comment-footer">
                        ${this.componentFeatures?.allowsFeature(_.Reactions) ? h` <wy-reactions
                              lineReverse
                              small
                              .reactions=${this.comment.reactions?.data}
                              parentType=${this.location}
                              parentId=${this.parentId}
                              entityId=${this.comment.id}
                              entityType="comments"
                            ></wy-reactions>` : g}
                        ${this.comment.annotations?.data?.length ? h`<wy-preview
                              ${K(this.previewAnnotationsRef)}
                              .files=${this.comment.annotations.data}
                              .isAttachment=${!0}
                            ></wy-preview> ` : g}
                        ${this.comment.attachments?.data?.length ? h`<wy-preview
                              ${K(this.previewAttachmentsRef)}
                              .files=${[...t, ...e]}
                              .isAttachment=${!0}
                            ></wy-preview> ` : g}
                      </div>
                    </div>
                  </wy-item>
                `}
      </div>
    `;
  }
  updated(t) {
    t.has("highlight") && this.highlight && this.highlightRef.value?.scrollIntoView({ block: "nearest" });
  }
}, n(Bo, "WyComment"), Bo);
Ii.styles = [Lt, jw, fl, yg, Nw, Y];
yn([
  y({ type: Object, attribute: !1 })
], Ii.prototype, "comment", 2);
yn([
  y({ type: Number })
], Ii.prototype, "parentId", 2);
yn([
  y({ attribute: !1 })
], Ii.prototype, "location", 2);
yn([
  V()
], Ii.prototype, "highlight", 2);
yn([
  y({ type: Boolean, reflect: !0 })
], Ii.prototype, "reveal", 2);
yn([
  V()
], Ii.prototype, "editing", 2);
Ii = yn([
  U("wy-comment"),
  ut()
], Ii);
const PC = j`[part~=wy-editor-comment]{position:relative;border-radius:var(--wy-padding-outer, 0)}[part~=wy-editor-comment-bottom]{position:sticky;bottom:0;z-index:999;background-color:var(--wy-component-background-color);padding:var(--wy-padding, calc(.5 * var(--wy-size, 1rem))) var(--wy-padding-sm, calc(.75 * var(--wy-padding, calc(.5 * var(--wy-size, 1rem)))));border-radius:var(--wy-component-border-radius);color:var(--wy-component-color)}[part~=wy-editor-comment-inputs]{display:flex;flex-direction:row;align-items:flex-end;gap:var(--wy-gap-sm, calc(.5 * var(--wy-gap, var(--wy-padding, calc(.5 * var(--wy-size, 1rem))))))}[part~=wy-editor-comment-text]{flex:1 1 100%;display:flex;flex-direction:column}[part~=wy-editor-parts]{padding-top:var(--wy-padding, calc(.5 * var(--wy-size, 1rem)));min-height:0}`;
var Yp, Xm;
function SC() {
  if (Xm) return Yp;
  Xm = 1;
  var s = "Expected a function", t = NaN, e = "[object Symbol]", i = /^\s+|\s+$/g, r = /^[-+]0x[0-9a-f]+$/i, a = /^0b[01]+$/i, o = /^0o[0-7]+$/i, c = parseInt, l = typeof bh == "object" && bh && bh.Object === Object && bh, u = typeof self == "object" && self && self.Object === Object && self, b = l || u || Function("return this")(), C = Object.prototype, v = C.toString, f = Math.max, m = Math.min, $ = /* @__PURE__ */ n(function() {
    return b.Date.now();
  }, "now");
  function E(B, Q, it) {
    var ct, st, Ot, zt, Mt, vt, Dt = 0, ge = !1, At = !1, xt = !0;
    if (typeof B != "function")
      throw new TypeError(s);
    Q = I(Q) || 0, A(it) && (ge = !!it.leading, At = "maxWait" in it, Ot = At ? f(I(it.maxWait) || 0, Q) : Ot, xt = "trailing" in it ? !!it.trailing : xt);
    function ee(Ct) {
      var ze = ct, Et = st;
      return ct = st = void 0, Dt = Ct, zt = B.apply(Et, ze), zt;
    }
    n(ee, "invokeFunc");
    function lt(Ct) {
      return Dt = Ct, Mt = setTimeout(Ke, Q), ge ? ee(Ct) : zt;
    }
    n(lt, "leadingEdge");
    function $t(Ct) {
      var ze = Ct - vt, Et = Ct - Dt, ie = Q - ze;
      return At ? m(ie, Ot - Et) : ie;
    }
    n($t, "remainingWait");
    function ci(Ct) {
      var ze = Ct - vt, Et = Ct - Dt;
      return vt === void 0 || ze >= Q || ze < 0 || At && Et >= Ot;
    }
    n(ci, "shouldInvoke");
    function Ke() {
      var Ct = $();
      if (ci(Ct))
        return Ni(Ct);
      Mt = setTimeout(Ke, $t(Ct));
    }
    n(Ke, "timerExpired");
    function Ni(Ct) {
      return Mt = void 0, xt && ct ? ee(Ct) : (ct = st = void 0, zt);
    }
    n(Ni, "trailingEdge");
    function qi() {
      Mt !== void 0 && clearTimeout(Mt), Dt = 0, ct = vt = st = Mt = void 0;
    }
    n(qi, "cancel");
    function El() {
      return Mt === void 0 ? zt : Ni($());
    }
    n(El, "flush");
    function Ci() {
      var Ct = $(), ze = ci(Ct);
      if (ct = arguments, st = this, vt = Ct, ze) {
        if (Mt === void 0)
          return lt(vt);
        if (At)
          return Mt = setTimeout(Ke, Q), ee(vt);
      }
      return Mt === void 0 && (Mt = setTimeout(Ke, Q)), zt;
    }
    return n(Ci, "debounced"), Ci.cancel = qi, Ci.flush = El, Ci;
  }
  n(E, "debounce");
  function M(B, Q, it) {
    var ct = !0, st = !0;
    if (typeof B != "function")
      throw new TypeError(s);
    return A(it) && (ct = "leading" in it ? !!it.leading : ct, st = "trailing" in it ? !!it.trailing : st), E(B, Q, {
      leading: ct,
      maxWait: Q,
      trailing: st
    });
  }
  n(M, "throttle");
  function A(B) {
    var Q = typeof B;
    return !!B && (Q == "object" || Q == "function");
  }
  n(A, "isObject");
  function H(B) {
    return !!B && typeof B == "object";
  }
  n(H, "isObjectLike");
  function D(B) {
    return typeof B == "symbol" || H(B) && v.call(B) == e;
  }
  n(D, "isSymbol");
  function I(B) {
    if (typeof B == "number")
      return B;
    if (D(B))
      return t;
    if (A(B)) {
      var Q = typeof B.valueOf == "function" ? B.valueOf() : B;
      B = A(Q) ? Q + "" : Q;
    }
    if (typeof B != "string")
      return B === 0 ? B : +B;
    B = B.replace(i, "");
    var it = a.test(B);
    return it || o.test(B) ? c(B.slice(2), it ? 2 : 8) : r.test(B) ? t : +B;
  }
  return n(I, "toNumber"), Yp = M, Yp;
}
n(SC, "requireLodash_throttle");
var MC = SC();
const Kw = /* @__PURE__ */ E0(MC);
function EC(s, t) {
  return new St(s.queryClient, {
    mutationFn: /* @__PURE__ */ n(async () => await s.fetch(`/api/apps/${t}/typing`, {
      method: "PUT",
      body: JSON.stringify({})
    }), "mutationFn")
  });
}
n(EC, "typingMutation");
function Jm(s, t) {
  return new St(s.queryClient, {
    mutationFn: /* @__PURE__ */ n(async () => {
      const e = await s.fetch(`/x/${t}/meetings`, { method: "POST" });
      if (!e.ok)
        throw new Error("Failed to create meeting");
      return await e.json();
    }, "mutationFn")
  });
}
n(Jm, "addMeetingMutation");
async function AC(s, t) {
  const e = await s.fetch("/api/blobs/external", { method: "POST", body: JSON.stringify(t) });
  if (!e.ok) {
    const r = await e.json();
    throw new Error(r.detail || r.title, { cause: r });
  }
  return await e.json();
}
n(AC, "externalBlob");
function RC(s, t, e, i) {
  const r = s.queryClient, a = ["apps", e, "blobs", i];
  return {
    mutationFn: /* @__PURE__ */ n(async (c) => await AC(s, c.externalBlob), "mutationFn"),
    mutationKey: a,
    onMutate: /* @__PURE__ */ n(async (c) => (await r.cancelQueries({ queryKey: a, exact: !0 }), { type: "attach", file: Pw(void 0, c.externalBlob.name, c.externalBlob.size, void 0, t), status: { state: "pending" } }), "onMutate"),
    onSuccess: /* @__PURE__ */ n((c, l, u) => {
      Bt(r, a, l, (b) => {
        b.status.state = "ok", b.status.progress = void 0, b.status.text = void 0;
      });
    }, "onSuccess"),
    onError(c, l, u) {
      const b = c.cause;
      b && b.status === 409 ? Bt(r, a, l, (C) => {
        C.status.state = "conflict", C.status.progress = void 0, C.status.text = b.detail || b.title;
      }) : Bt(r, a, l, (C) => {
        C && (C.status.state = "error", C.status.progress = void 0, C.status.text = b.detail || b.title);
      });
    }
  };
}
n(RC, "getExternalBlobMutationOptions");
function wg(s, t, e, i) {
  return new St(s.queryClient, RC(s, t, e, i));
}
n(wg, "getExternalBlobMutation");
const TC = /(((https?|ftp):\/\/|(www|ftp)\.)[\w]+(.[\w]+)([\w\-.,@?^=%&amp;:/~+#]*[\w\-@?^=%&amp;/~+#]))/gim;
let _n = [], hd = [], lu = [], Ah = [], er = {};
const LC = /* @__PURE__ */ n((s, t) => s.length === t.length && s.every((e, i) => e === t[i]), "arrayEquals");
function OC() {
  return !!Object.keys(er).length;
}
n(OC, "isFetchingEmbeds");
async function zC(s, t) {
  let e;
  try {
    const i = await t.fetch(`/api/embeds?url=${encodeURIComponent(s)}`);
    if (!i.ok)
      throw new Error();
    e = await i.json(), delete er[s], hd = [...hd, s];
  } catch {
    lu = [...lu, s], delete er[s];
  }
  return e;
}
n(zC, "fetchEmbed");
const FC = /* @__PURE__ */ n(() => {
  _n = [], hd = [], lu = [], Ah = [], er = {};
}, "clearEmbeds"), tv = /* @__PURE__ */ n((s) => {
  hd = s;
}, "initEmbeds"), IC = /* @__PURE__ */ n((s, t, e) => {
  let i = s.match(TC)?.map((r) => r) || null;
  if (i !== null && (i = i.map((r) => r.startsWith("//") ? "http:" + r : !r.startsWith("http://") && !r.startsWith("https://") ? "http://" + r : r)), !(i === null || i.length === 0)) {
    if (i.length !== _n.length || !LC(i, _n)) {
      _n = i, i.forEach((r) => {
        !hd.includes(r) && !lu.includes(r) && !Ah.includes(r) && typeof er[r] > "u" && ($e(), er[r] = window.setTimeout(async () => {
          const a = await zC(r, e);
          a && t(a);
        }, 500));
      }), Ah = Ah.filter((r) => _n.includes(r));
      for (const r in er)
        _n.includes(r) || ($e(), window.clearTimeout(er[r]), delete er[r]);
    }
  }
}, "getEmbeds"), DC = j`[part~=wy-editor-msg]{padding:var(--wy-padding-lg, calc(1.5 * var(--wy-padding, calc(.5 * var(--wy-size, 1rem)))));display:flex;flex-direction:column;gap:var(--wy-gap-lg, calc(1.5 * var(--wy-gap, var(--wy-padding, calc(.5 * var(--wy-size, 1rem))))))}[part~=wy-editor-msg-inputs]{display:flex;flex-direction:row;align-items:flex-end;justify-content:space-between;flex-wrap:wrap;gap:var(--wy-gap-sm, calc(.5 * var(--wy-gap, var(--wy-padding, calc(.5 * var(--wy-size, 1rem))))))}[part~=wy-editor-msg-buttons]{display:flex;flex-direction:row;align-items:flex-end;gap:var(--wy-gap-sm, calc(.5 * var(--wy-gap, var(--wy-padding, calc(.5 * var(--wy-size, 1rem))))));flex:1 0 auto}[part~=wy-editor-msg-text]{flex:1 1 100%;display:flex;flex-direction:column}[part~=wy-editor-parts]{display:flex;flex-direction:column;gap:var(--wy-gap, var(--wy-padding, calc(.5 * var(--wy-size, 1rem))));min-height:0}`, fg = j`[part~=wy-dragging]:after{content:attr(data-drag-title);background-color:#ffffff80;display:flex;justify-content:center;align-items:center;border:calc(.125 * var(--wy-size, 1rem)) dashed var(--wy-outline, var(--wy-outline-light, #72777f));color:var(--wy-on-surface, var(--wy-on-surface-light, #181c20));font-size:calc(1.5 * var(--wy-size, 1rem));font-weight:var(--wy-font-weight-bold, 600);z-index:10000;position:absolute;inset:0}`;
var VC = Object.defineProperty, HC = Object.getOwnPropertyDescriptor, wn = /* @__PURE__ */ n((s, t, e, i) => {
  for (var r = i > 1 ? void 0 : i ? HC(t, e) : t, a = s.length - 1, o; a >= 0; a--)
    (o = s[a]) && (r = (i ? o(t, e, r) : o(r)) || r);
  return i && r && VC(t, e, r), r;
}, "__decorateClass$P"), No;
let Di = (No = class extends nt {
  constructor() {
    super(...arguments), this.exportParts = new W(this), this.status = {
      state: "ok"
    }, this.interactive = !1, this.rounded = !1, this.isRenaming = !1;
  }
  /**
   * Emit an `edit-name` event requesting rename mode for the provided file.
   *
   * @param file - File that should enter rename mode.
   * @returns Whether the event was not canceled.
   */
  dispatchEditName(t) {
    const e = new CustomEvent("edit-name", { detail: { file: t } });
    return this.dispatchEvent(e);
  }
  /**
   * Emit a `rename` event with a new filename.
   *
   * @param file - File being renamed.
   * @param name - New filename to apply.
   * @returns Whether the event was not canceled.
   */
  dispatchRename(t, e) {
    const i = new CustomEvent("rename", { detail: { file: t, name: e } });
    return this.dispatchEvent(i);
  }
  /**
   * Emit a `trash` event to move the file to the recycle bin.
   *
   * @param file - File slated for trashing.
   * @returns Whether the event was not canceled.
   */
  dispatchTrash(t) {
    const e = new CustomEvent("trash", { detail: { file: t } });
    return this.dispatchEvent(e);
  }
  /**
   * Emit a `restore` event to recover the provided file.
   *
   * @param file - File to restore from trash.
   * @returns Whether the event was not canceled.
   */
  dispatchRestore(t) {
    const e = new CustomEvent("restore", { detail: { file: t } });
    return this.dispatchEvent(e);
  }
  /**
   * Emit a `delete-forever` event to permanently remove the file.
   *
   * @param file - File targeted for permanent deletion.
   * @returns Whether the event was not canceled.
   */
  dispatchDeleteForever(t) {
    const e = new CustomEvent("delete-forever", { detail: { file: t } });
    return this.dispatchEvent(e);
  }
  /**
   * Emit a `subscribe` event toggling subscription for the file.
   *
   * @param file - File whose subscription changes.
   * @param subscribe - Desired subscription state.
   * @returns Whether the event was not canceled.
   */
  dispatchSubscribe(t, e) {
    const i = new CustomEvent("subscribe", { detail: { file: t, subscribe: e } });
    return this.dispatchEvent(i);
  }
  render() {
    const t = this.file;
    if (!t) return g;
    const { icon: e } = cs(t.name), i = ai(t.name), r = /* @__PURE__ */ n((o) => {
      o.stopImmediatePropagation();
      const c = o.target;
      c.value && c.value !== t.name ? this.dispatchRename(t, c.value) : this.dispatchEditName({ id: NaN });
    }, "handleRename"), a = /* @__PURE__ */ n((o) => {
      const c = o.target;
      o.key === "Escape" ? (o.preventDefault(), c.value = t.name, c.blur()) : o.key === "Enter" && (o.preventDefault(), c.blur());
    }, "handleRenameKey");
    return h`
      <wy-item
        ?rounded=${this.rounded}
        ?interactive=${this.interactive && !t.is_trashed && !this.isRenaming}
        ?trashed=${t.is_trashed}
        title=${t.name}
        size="sm"
      >
        ${this.status.state === "error" ? h`<wy-icon
              slot="image"
              name="alert-octagon"
              color="error"
              title=${ot(this.status.text)}
            ></wy-icon>` : this.status.state === "conflict" ? h`<wy-icon slot="image" name="alert" color="yellow" title=${ot(this.status.text)}></wy-icon>` : h`<wy-icon slot="image" .name=${e} .size=${24} .kind=${t.kind} ext=${i}></wy-icon>`}
        ${this.isRenaming ? h`
              <input
                slot="title"
                type="text"
                maxlength="256"
                part="wy-input"
                .defaultValue=${t.name}
                @blur=${r}
                @keyup=${(o) => {
      lr(o), a(o);
    }}
                @click=${(o) => o.preventDefault()}
                @focus=${_w}
                ${K(hw)}
              />
            ` : h`
              <span slot="title" title=${t.name + (this.status.text ? `: ${this.status.text}` : "")}>
                <slot name="title"
                  >${this.title || t.name}${this.status.text ? h`: <em>${this.status.text}</em>` : g}</slot
                >
              </span>
            `}

        <slot slot="actions" name="actions">
          <wy-file-menu
            .file=${t}
            @edit-name=${(o) => this.dispatchEditName(o.detail.file)}
            @trash=${(o) => this.dispatchTrash(o.detail.file)}
            @restore=${(o) => this.dispatchRestore(o.detail.file)}
            @delete-forever=${(o) => this.dispatchDeleteForever(o.detail.file)}
            @subscribe=${(o) => this.dispatchSubscribe(o.detail.file, o.detail.subscribe)}
          >
          </wy-file-menu>
        </slot>
      </wy-item>
    `;
  }
}, n(No, "WyFileItem"), No);
Di.styles = [us];
wn([
  y({ type: Object })
], Di.prototype, "file", 2);
wn([
  y({ type: Object })
], Di.prototype, "status", 2);
wn([
  y({ type: Boolean })
], Di.prototype, "interactive", 2);
wn([
  y({ type: Boolean })
], Di.prototype, "rounded", 2);
wn([
  y()
], Di.prototype, "actionType", 2);
wn([
  V()
], Di.prototype, "isRenaming", 2);
Di = wn([
  U("wy-file-item")
], Di);
const UC = /* @__PURE__ */ n((s) => {
  var t, e, i, r, Jy, mg, tw, Pn, ew, b;
  return b = class extends s {
    /**
     * @constructor
     * @hideconstructor
     */
    constructor(...f) {
      super(...f);
      T(this, r);
      T(this, t, []);
      T(this, e);
      T(this, i, /* @__PURE__ */ new Set());
    }
    get eventParent() {
      return p(this, e);
    }
    set eventParent(f) {
      f?.eventChildren && f.eventChildren.add(this), P(this, e, f);
    }
    get eventChildren() {
      return p(this, i);
    }
    /**
     * Registers one or several event listeners. All event listeners are managed and automatically unregistered on destroy.
     *
     * When listening to weavy events, you may also listen to `before:` and `after:` events by simply adding the prefix to a weavy event.
     * Event handlers listening to weavy events may return modified data that is returned to the trigger. The data is passed on to the next event in the trigger event chain. If an event handler calls `event.stopPropagation()` or `return false`, the event chain will be stopped and the value is returned.
     *
     * @example <caption>Instance event</caption>
     * myInstance.on("before:options", function(options) { ... })
     * myInstance.on("options", function(options) { ... })
     * myInstance.on("after:options", function(options) { ... })
     *
     * @category eventhandling
     * @function
     * @name WeavyEvents#on
     * @param {string} events - One or several event names separated by spaces. You may provide any namespaces in the names or use the general namespace parameter instead.
     * @param {string|Object} [selector] - Only applicable if the context supports selectors, for instance jQuery.on().
     * @param {function} handler - The listener. The first argument is always the event, followed by any data arguments provided by the trigger.
     */
    on(...f) {
      const m = dt(this, r, ew).call(this, f);
      if (f[3]) {
        const E = /* @__PURE__ */ n((...M) => {
          try {
            m.handler.apply(this, M);
          } catch {
            try {
              m.handler();
            } catch (A) {
              console.warn("Could not invoke one handler:", A);
            }
          }
          dt(this, r, tw).call(this, m.events, m.selector, null, m.handler);
        }, "attachedHandler");
        dt(this, r, Jy).call(this, m.events, m.selector, E, m.handler);
      } else
        dt(this, r, Jy).call(this, m.events, m.selector, m.handler, m.handler);
    }
    /**
     * Registers one or several event listeners that are executed once. All event listeners are managed and automatically unregistered on destroy.
     *
     * Similar to {@link WeavyEvents#on}.
     *
     * @category eventhandling
     * @function
     * @name WeavyEvents#one
     * @param {string} events - One or several event names separated by spaces. You may provide any namespaces in the names or use the general namespace parameter instead.
     * @param {string|Object} [selector] - Only applicable if the context supports selectors, for instance jQuery.on().
     * @param {Function} handler - The listener. The first argument is always the event, followed by any data arguments provided by the trigger.
     */
    one(f, m, $) {
      this.on(f, m, $, !0);
    }
    /**
     * Unregisters event listeners. The arguments must match the arguments provided on registration using .on() or .one().
     *
     * @category eventhandling
     * @function
     * @name WeavyEvents#off
     * @param {string} events - One or several event names separated by spaces. You may provide any namespaces in the names or use the general namespace parameter instead.
     * @param {string} [selector] - Only applicable if the context supports selectors, for instance jQuery.on().
     * @param {function} handler - The listener. The first argument is always the event, followed by any data arguments provided by the trigger.
     * @returns {boolean} Was the handler removed?
     */
    off(...f) {
      const m = dt(this, r, ew).call(this, f), $ = dt(this, r, mg).call(this, m.events, m.selector, m.handler);
      return dt(this, r, tw).call(this, m.events, m.selector, $, m.handler);
    }
    /**
     * Clears all registered eventhandlers
     *
     * @category eventhandling
     * @function
     * @name WeavyEvents#clear
     */
    clearEventHandlers() {
      p(this, t).length = 0;
    }
    /**
     * Trigger a custom event. Events are per default triggered on the weavy instance using the weavy namespace.
     *
     * The trigger has an event chain that adds `before:` and `after:` events automatically for all events except when any custom `prefix:` is specified. This way you may customize the eventchain by specifying `before:`, `on:` and `after:` in your event name to fire them one at the time. The `on:` prefix will then be removed from the name when the event is fired.
     *
     * Eventhandlers listening to the event may return modified data that is returned by the trigger event. The data is passed on to the next event in the trigger event chain. If an event handler calls `event.stopPropagation()` or `return false`, the event chain will be stopped and the value is returned.
     *
     * @example
     * // Normal triggering
     * weavyEvents.triggerEvent("myevent");
     *
     * // Will trigger the following events on the root instance
     * // 1. before:myevent.event.weavy
     * // 2. myevent.event.weavy
     * // 3. after:myevent.event.weavy
     *
     * @example
     * // Custom triggering, one at the time
     * weavyEvents.triggerEvent("before:myevent");
     * weavyEvents.triggerEvent("on:myevent");
     * weavyEvents.triggerEvent("after:myevent");
     *
     * @example
     * // Advanced triggering with data handling
     *
     * function doSomething() {
     *     // Will trigger the events sequentially and check the response data in between
     *
     *     var myTriggerData = { counter: 123, label: "my label" };
     *
     *     // Custom triggering, one at the time
     *
     *     // 1. Trigger before: and save the response data back to myTriggerData
     *     myTriggerData = weavyEvents.triggerEvent("before:myevent", myTriggerData);
     *
     *     if (myTriggerData === false) {
     *         console.warn("before:myevent was cancelled by event.stopPropagation() or return false");
     *         return;
     *     }
     *
     *     // ...
     *
     *     // 2. Continue with on: and save the response data back to myTriggerData
     *     myTriggerData = weavyEvents.triggerEvent("on:myevent", myTriggerData);
     *
     *     if (myTriggerData === false) {
     *         console.warn("on:myevent was cancelled by event.stopPropagation() or return false");
     *         return;
     *     }
     *
     *     // ...
     *
     *     // 3. At last trigger after: and save the response data back to myTriggerData
     *     myTriggerData = weavyEvents.triggerEvent("after:myevent", myTriggerData);
     *
     *     if (myTriggerData === false) {
     *         console.warn("after:myevent was cancelled by event.stopPropagation() or return false");
     *         return;
     *     }
     *
     *     console.log("myevent was fully executed", myTriggerData);
     *     return myTriggerData;
     * }
     *
     * @category eventhandling
     * @function
     * @name WeavyEvents#triggerEvent
     * @param {string} name - The name of the event.
     * @param {(Array/Object/JSON)} [data] - Data may be an array or plain object with data or a JSON encoded string.
     * @param {any} ...additionalData - Any raw extra data
     * @returns {data} The data passed to the event trigger including any modifications by event handlers. Returns false if the event is cancelled.
     */
    triggerEvent(f, m, ...$) {
      const E = /^(before|on|after):/.test(f), M = E ? f.split(":")[0] : "";
      f = f.replace(/^(before|on|after):/, "");
      const A = "before:" + f, H = f, D = "after:" + f;
      if (m && typeof m == "string")
        try {
          m = JSON.parse(m);
        } catch {
          console.warn("Could not parse event data", f, m);
        }
      let I;
      if (E) {
        if (M === "before" || M === "after") {
          if (p(this, e) && (I = p(this, e).triggerEvent(
            M === "before" ? A : D,
            m,
            ...$
          ), m = I || I === !1 ? I : m, m === !1) || (I = dt(this, r, Pn).call(this, M === "before" ? A : D, m, ...$), m = I || I === !1 ? I : m, m === !1))
            return m;
        } else if (M === "on" && (I = dt(this, r, Pn).call(this, H, m, ...$), m = I || I === !1 ? I : m, m === !1 || p(this, e) && (I = p(this, e).triggerEvent("on:" + H, m, ...$), m = I || I === !1 ? I : m, m === !1)))
          return m;
      } else {
        if (p(this, e) && (I = p(this, e).triggerEvent(A, m, ...$), m = I || I === !1 ? I : m, m === !1) || (I = dt(this, r, Pn).call(this, A, m, ...$), m = I || I === !1 ? I : m, m === !1) || (I = dt(this, r, Pn).call(this, H, m, ...$), m = I || I === !1 ? I : m, m === !1) || p(this, e) && (I = p(this, e).triggerEvent("on:" + H, m, ...$), m = I || I === !1 ? I : m, m === !1) || p(this, e) && (I = p(this, e).triggerEvent(D, m, ...$), m = I || I === !1 ? I : m, m === !1))
          return m;
        I = dt(this, r, Pn).call(this, D, m, ...$), m = I || I === !1 ? I : m;
      }
      return m;
    }
  }, t = new WeakMap(), e = new WeakMap(), i = new WeakMap(), r = new WeakSet(), /**
   * Saves a single event handler.
   *
   * @internal
   * @function
   * @param {string} event - One or more events. Multiple events are currently not registered individually.
   * @param {string|Object} [selector] - Optional refinement selector
   * @param {function} handler - The handler function. may be wrapped for once-handlers
   * @param {function} originalHandler - The original non-wrapped event handler.
   */
  Jy = /* @__PURE__ */ n(function(f, m, $, E) {
    p(this, t).push({
      events: f,
      selector: m,
      handler: $,
      originalHandler: E
    });
  }, "#registerEventHandler"), /**
   * Returns the event handler or wrapped event handler. The arguments must match the registered event handler.
   *
   * @internal
   * @function
   * @param {string} events - The events registered
   * @param {string|Object} [selector] - The optional selector for the handler.
   * @param {function} handler - The registered handler
   * @param {function} [originalHandler] - The original registered handler
   */
  mg = /* @__PURE__ */ n(function(f, m, $, E) {
    const M = {
      events: f,
      selector: m,
      handler: $,
      originalHandler: E || $
    }, A = p(this, t).filter((H) => zs(M, H, !0)).pop();
    return A && A.handler;
  }, "#getEventHandler"), /**
   * Unregister an event handler. Arguments must match the registered event handler.
   *
   * @internal
   * @function
   * @param {string} event - The events registered
   * @param {function} handler - The registered handler
   * @param {string|Object} [selector] - The optional selector for the handler.
   * @returns {boolean} - True if any handler was removed
   */
  tw = /* @__PURE__ */ n(function(f, m, $, E) {
    const M = {
      events: f,
      selector: m,
      handler: $,
      originalHandler: E
    };
    let A = !1;
    return p(this, t).forEach((H, D) => {
      zs(M, H, !0) && (A = !0, p(this, t).splice(D, 1));
    }), A;
  }, "#unregisterEventHandler"), /**
   * Triggers any local event handlers registered. Each handler may modify the data and return it or return false to cancel the event chain. .stopPropagation() and .preventDefault() may also be used.
   *
   * @example
   * weavyEvents.on("myevent", function(e, data) { ... })
   *
   * triggerHandler(this, "myevent", { key: 1 })
   *
   * @internal
   * @function
   * @param {any} eventName - The name of the event. Event names without prefix will also trigger handlers with the "on:" prefix.
   * @param {any} data - Any data to pass to the handler
   * @param {any} ...additionalData - Any extra data
   */
  Pn = /* @__PURE__ */ n(function(f, m, ...$) {
    let E = !1;
    return p(this, t).forEach((M) => {
      M.events.split(" ").forEach((A) => {
        if (A = A.indexOf("on:") === 0 ? A.split("on:")[1] : A, f === A && (!M.selector || zs(M.selector, m, !0))) {
          const H = M.handler(m, ...$);
          H ? m = H : H === !1 && (E = !0);
        }
      });
    }), E ? !1 : m;
  }, "#triggerHandler"), /**
   * Extracts and normalizes all parts of the events arguments.
   *
   * @internal
   * @function
   * @param {Array.<Object>} eventArguments - The function argument list: `[context], events, [selector], handler`
   * @returns {Object}
   * @property {string} events - Event names with added namespace for local events.
   * @property {string|Object} selector - The optional selector.
   * @property {function} handler - The handler function
   * @
   */
  ew = /* @__PURE__ */ n(function(f) {
    const m = typeof f[1] == "function" ? f[1] : f[2], $ = typeof f[1] == "function" ? null : f[1];
    return { events: f[0], selector: $, handler: m };
  }, "#getEventArguments"), n(b, "WeavyEvents"), b;
}, "MixinWeavyEvents"), BC = UC(class {
});
function ev(s) {
  let t = "";
  try {
    t = /^((?:https?:\/\/[^/]+)|(?:file:\/\/))\/?/.exec(s)?.[1] || "";
  } catch {
    console.error(
      "Unable to resolve location origin. Make sure you are using http, https or file protocol and have a valid location URL."
    );
  }
  return t;
}
n(ev, "extractOrigin");
const Zf = class Zf extends BC {
  constructor(t = {}) {
    super(), this.contentWindows = /* @__PURE__ */ new Set(), this.contentWindowsMapByWeavyId = /* @__PURE__ */ new Map(), this.contentWindowOrigins = /* @__PURE__ */ new WeakMap(), this.contentWindowNames = /* @__PURE__ */ new WeakMap(), this.contentWindowWeavyIds = /* @__PURE__ */ new WeakMap(), this.contentWindowDomain = /* @__PURE__ */ new WeakMap(), this.origin = dw() ? ev(window.location.href) : "", this.timeout = 2e3, t?.timeout && (this.timeout = t.timeout), window.addEventListener("message", (e) => {
      if (e.data.name && e.data.weavyId !== void 0) {
        if (e.data.weavyMessageId && e.data.name !== "message-receipt" && e.data.name !== "unready")
          try {
            e.source?.postMessage(
              { name: "message-receipt", weavyId: e.data.weavyId, weavyMessageId: e.data.weavyMessageId },
              { targetOrigin: e.origin }
            );
          } catch {
            console.error("could not post back message-receipt to source");
          }
        switch (e.data.name) {
          case "register-child": {
            if ((!e.source || !this.contentWindowWeavyIds.has(e.source)) && console.warn("register-child: contentWindow not pre-registered", e.source), e.source && this.contentWindowOrigins.get(e.source) !== e.origin) {
              console.error(
                "register-child: " + this.contentWindowNames.get(e.source) + " has invalid origin",
                e.origin
              );
              return;
            }
            try {
              const i = e.source ? this.contentWindowWeavyIds.get(e.source) : void 0, r = e.source ? this.contentWindowNames.get(e.source) : void 0;
              r && e.source && e.source.postMessage(
                {
                  name: "register-window",
                  windowName: r,
                  weavyId: i || !0
                },
                { targetOrigin: e.origin }
              );
            } catch (i) {
              console.error("could not register frame window", i);
            }
            break;
          }
          case "ready": {
            e.source && this.contentWindowsMapByWeavyId.has(e.data.weavyId) && this.contentWindowNames.has(e.source) && this.contentWindowsMapByWeavyId.get(e.data.weavyId)?.get(this.contentWindowNames.get(e.source)) && (this.contentWindowDomain.set(e.source, e.origin), this.distributeMessage(e));
            break;
          }
          case "unready": {
            this.contentWindowsMapByWeavyId.has(e.data.weavyId) && this.distributeMessage(e, !0);
            break;
          }
          default: {
            (e.source === window || this.contentWindowsMapByWeavyId.size) && this.distributeMessage(e);
            break;
          }
        }
      }
    });
  }
  /**
   * The weavy console logging.
   */
  get console() {
    return console;
  }
  distributeMessage(t, e = !1) {
    const i = t.source === window && t.origin === this.origin;
    if (e ||= t.source && this.contentWindowOrigins.has(t.source) && t.origin === this.contentWindowOrigins.get(t.source) || !1, i || e) {
      e && !t.data.windowName && t.source && (t.data.windowName = this.contentWindowNames.get(t.source));
      const r = t.data.name;
      r && this.triggerEvent(r, t.data, t), this.triggerEvent("message", t.data, t);
    }
  }
  /**
   * Sends the id of a frame to the frame content scripts, so that the frame gets aware of which id it has.
   * The frame needs to have a unique name attribute.
   *
   * @category panels
   * @param {string} weavyId - The id of the group or entity which the contentWindow belongs to.
   * @param {Window} contentWindow - The frame window to send the data to.
   */
  registerContentWindow(t, e, i, r) {
    try {
      if (!e) {
        console.error("registerContentWindow() No valid contentWindow to register, must be a window and have a name.");
        return;
      }
    } catch {
      console.error("registerContentWindow() cannot access contentWindowName");
    }
    t.self && (t = t.self), this.console.log("registerContentWindow", t), (!i || i === "true") && (i = !0), this.contentWindowsMapByWeavyId.has(i) || this.contentWindowsMapByWeavyId.set(i, /* @__PURE__ */ new Map()), this.contentWindowsMapByWeavyId.get(i)?.set(e, t), this.contentWindows.add(t), this.contentWindowNames.set(t, e), this.contentWindowWeavyIds.set(t, i), this.contentWindowOrigins.set(t, r);
  }
  unregisterAll(t) {
    this.contentWindowsMapByWeavyId.has(t) && (this.contentWindowsMapByWeavyId.get(t)?.forEach((e, i) => {
      this.unregisterContentWindow(i, t);
    }), this.contentWindowsMapByWeavyId.get(t), this.contentWindowsMapByWeavyId.delete(t));
  }
  unregisterContentWindow(t, e) {
    if (this.contentWindowsMapByWeavyId.has(e)) {
      if (this.contentWindowsMapByWeavyId.get(e)?.has(t)) {
        const i = this.contentWindowsMapByWeavyId.get(e)?.get(t);
        if (i)
          try {
            this.contentWindows.delete(i), this.contentWindowNames.delete(i), this.contentWindowWeavyIds.delete(i), this.contentWindowOrigins.delete(i);
          } catch {
          }
      }
      if (this.contentWindowsMapByWeavyId.get(e)?.delete(t), this.contentWindowsMapByWeavyId.get(e)?.size === 0)
        try {
          this.contentWindowsMapByWeavyId.delete(e);
        } catch {
        }
    }
  }
  async whenPostMessage(t, e, i) {
    $e(), i === null && (i = void 0);
    const r = t === window.self, a = r ? ev(window.location.href) : this.contentWindowOrigins.get(t);
    if (r || t && a === this.contentWindowDomain.get(t)) {
      e.weavyMessageId || (e.weavyMessageId = Bl() + Bl());
      let c;
      await Promise.race([
        new Promise((l, u) => {
          c = window.setTimeout(() => {
            u(new Error("postMessage() receipt timed out: " + e.weavyMessageId + ", " + e.name));
          }, this.timeout || 2e3);
        }),
        new Promise((l) => {
          this.on("message-receipt", { weavyId: e.weavyId, weavyMessageId: e.weavyMessageId }, () => {
            clearTimeout(c), l(void 0);
          }), t.postMessage(e, { targetOrigin: a, transfer: i });
        })
      ]);
    } else
      throw new Error("postMessage() Invalid window origin: " + a + ", " + e.name);
  }
  postToChildren(t, e) {
    if (typeof t != "object" || !t.name) {
      console.error("postToChildren() Invalid message format", t);
      return;
    }
    e === null && (e = void 0), t.distributeName = t.name, t.name = "distribute", t.weavyId = t.weavyId || !0, this.contentWindows.forEach((i) => {
      const r = this.contentWindowOrigins.get(i);
      if (r === this.contentWindowDomain.get(i))
        try {
          i.postMessage(t, { targetOrigin: r, transfer: e });
        } catch {
          console.warn(
            "postToChildren() could not distribute message to " + this.contentWindowNames.get(i)
          );
        }
    });
  }
  async postToFrame(t, e, i, r) {
    if (typeof i != "object" || !i.name) {
      console.error("postToFrame() Invalid message format", i);
      return;
    }
    const a = this.contentWindowsMapByWeavyId.get(e)?.get(t);
    if (!a)
      throw new Error(`postToFrame() Window not registered: ${e}, ${t}`);
    return i.weavyId = e, await this.whenPostMessage(a, i, r);
  }
  async postToSelf(t, e) {
    if (typeof t != "object" || !t.name) {
      console.error("postToSelf() Invalid message format", t);
      return;
    }
    return t.weavyId = t.weavyId || !0, await this.whenPostMessage(window.self, t, e);
  }
  postToSource(t, e, i) {
    if (t.source && t.data.weavyId !== void 0) {
      const r = t.source === window.self && t.origin === this.origin, a = this.contentWindowOrigins.has(t.source) && t.origin === this.contentWindowOrigins.get(t.source);
      if (i === null && (i = void 0), r || a) {
        e.weavyId = t.data.weavyId;
        try {
          t.source.postMessage(e, { targetOrigin: t.origin, transfer: i });
        } catch (o) {
          console.error("postToSource() Could not post message back to source", o);
        }
      }
    }
  }
};
n(Zf, "WeavyPostalParent");
let iw = Zf;
var NC = Object.defineProperty, qC = Object.getOwnPropertyDescriptor, ju = /* @__PURE__ */ n((s, t, e, i) => {
  for (var r = i > 1 ? void 0 : i ? qC(t, e) : t, a = s.length - 1, o; a >= 0; a--)
    (o = s[a]) && (r = (i ? o(t, e, r) : o(r)) || r);
  return i && r && NC(t, e, r), r;
}, "__decorateClass$O"), qo;
let Js = (qo = class extends Ae {
  constructor() {
    super(...arguments), this.exportParts = new W(this), this.iframeVisible = !1, this.showOverlay = !1, this.isRegistered = !1, this.iframeElementRef = J(), this.overlayRef = J();
  }
  /**
   * Open the file picker overlay.
   */
  open() {
    this.showOverlay = !0;
  }
  /**
   * Close the file picker overlay.
   */
  close() {
    this.showOverlay = !1, this.iframeVisible = !1;
  }
  /**
   * Returns the topmost available window/frame origin used when building the iframe src.
   *
   * @internal
   */
  get origin() {
    let t = "";
    try {
      window.location.ancestorOrigins && 0 < window.location.ancestorOrigins.length ? t = window.location.ancestorOrigins[window.location.ancestorOrigins.length - 1] : window.top && (t = window.top.document.location.origin);
    } catch {
    }
    if (!t)
      try {
        t = window.self.document.location.origin;
      } catch {
        console.error("Filebrowser: Could not read current origin.");
      }
    return t;
  }
  /**
   * Trigger `external-blobs` event with any selected blobs.
   *
   * @internal
   * @param {ExternalBlobType[] | null} externalBlobs - The externally selected blobs.
   * @returns {boolean} Whether the event was successful.
   */
  dispatchExternalBlobs(t) {
    const e = new CustomEvent("external-blobs", {
      detail: { externalBlobs: t }
    });
    return this.dispatchEvent(e);
  }
  connectedCallback() {
    super.connectedCallback(), this.handleFiles = (t, e) => {
      e.source === this.iframeElementRef.value?.contentWindow?.self && (this.dispatchExternalBlobs(t.blobs), this.close());
    }, this.handleClose = (t, e) => {
      e.source === this.iframeElementRef.value?.contentWindow?.self && this.close();
    }, this.handleGoogleSelected = (t, e) => {
      e.source === this.iframeElementRef.value?.contentWindow?.self && this.overlayRef.value && (this.overlayRef.value.maximized = !0);
    }, this.weavyPostal || (this.weavyPostal = new iw()), this.weavyPostal.on("add-external-blobs", this.handleFiles), this.weavyPostal.on("request:file-browser-close", this.handleClose), this.weavyPostal.on("google-selected", this.handleGoogleSelected);
  }
  disconnectedCallback() {
    super.disconnectedCallback(), this.weavyPostal?.off("add-external-blobs", this.handleFiles), this.weavyPostal?.off("request:file-browser-close", this.handleClose), this.weavyPostal?.off("google-selected", this.handleGoogleSelected);
  }
  updated() {
    this.weavy && (this.showOverlay && !this.src && (this.src = new URL(
      "?origin=" + this.origin + "&v=X&t=" + Date.now().toString() + "&weavyId=wy-filebrowser",
      this.weavy.cloudFilePickerUrl
    )), this.weavyPostal && !this.isRegistered && this.showOverlay && this.src ? this.iframeElementRef.value?.contentWindow && (this.weavyPostal.registerContentWindow(
      this.iframeElementRef.value?.contentWindow.self,
      "weavy-filebrowser",
      "wy-filebrowser",
      this.src.origin
    ), this.isRegistered = !0) : this.weavyPostal && this.isRegistered && !this.showOverlay && this.src && (this.weavyPostal.unregisterContentWindow("weavy-filebrowser", "wy-filebrowser"), this.isRegistered = !1, this.src = void 0));
  }
  render() {
    return this.weavy ? h`
      <wy-overlay
        ?noHeader=${this.iframeVisible}
        part="wy-cloud-files-overlay"
        .show=${this.showOverlay}
        ${K(this.overlayRef)}
        @close=${() => this.close()}
      >
        ${this.showOverlay ? h`
              <wy-progress-circular
                part="wy-cloud-files-progress"
                indeterminate
                overlay
                ?hidden=${this.iframeVisible}
              ></wy-progress-circular>
              <iframe
                part="wy-cloud-picker-frame"
                ${K(this.iframeElementRef)}
                @load=${() => this.iframeVisible = !0}
                src=${ot(this.src?.toString())}
                id="weavy-filebrowser"
                name="weavy-filebrowser"
                title=${x("Cloud File Browser")}
              ></iframe>
            ` : g}
      </wy-overlay>
    ` : g;
  }
}, n(qo, "WyCloudFiles"), qo);
Js.styles = [
  Y,
  j`
      [part~="wy-cloud-picker-frame"] {
        flex: 1 1 100%;
        border: 0;
      }
    `
];
ju([
  V()
], Js.prototype, "src", 2);
ju([
  V()
], Js.prototype, "iframeVisible", 2);
ju([
  V()
], Js.prototype, "showOverlay", 2);
Js = ju([
  U("wy-cloud-files"),
  ut()
], Js);
const ri = dw() ? window.navigator.userAgent : "", vg = ri.includes("iPad") || ri.includes("Android") && !ri.includes("Mobi") || !1, WC = ri.includes("Mobi") || vg || !1, jC = !WC && !vg, iv = ri.includes("Windows") ? "Windows" : ri.includes("Macintosh") ? "Mac" : ri.includes("iPad") || ri.includes("iPhone") || ri.includes("iPod") ? "iOS" : ri.includes("Android") ? "Android" : void 0;
iv === "Android" && ri.includes("; wv") || iv === "iOS" && ri.includes("Safari");
const KC = j`@charset \"UTF-8\";[part~=wy-scroll-y]{overflow-y:auto;max-height:100%;overflow-x:hidden;overflow-anchor:none;touch-action:pan-y;will-change:scroll-position,opacity;backface-visibility:hidden}[part~=wy-scroll-y]:not(body){-webkit-overflow-scrolling:touch}[part~=wy-scroll-x]{overflow-x:auto;max-width:100%;overflow-y:hidden;overflow-anchor:none;touch-action:pan-x;will-change:scroll-position,opacity;backface-visibility:hidden}[part~=wy-scroll-x-y]{overflow-y:auto;max-height:100%;overflow-x:hidden;overflow-anchor:none;touch-action:pan-y;will-change:scroll-position,opacity;backface-visibility:hidden}[part~=wy-scroll-x-y]:not(body){-webkit-overflow-scrolling:touch}[part~=wy-scroll-x-y]{overflow-x:auto;max-width:100%;overflow-y:hidden;overflow-anchor:none;touch-action:pan-x;will-change:scroll-position,opacity;backface-visibility:hidden}[part~=wy-scroll-x-y]{overflow-y:auto;overflow-x:auto;touch-action:pan-y,pan-x}[part~=wy-scroll-x-always],[part~=wy-scroll-y-always]{overflow-y:scroll}[part~=wy-code] pre,.wy-code pre,[part~=wy-core-editor] .cm-content pre{text-wrap:pretty}[part~=wy-code] .code,[part~=wy-code] code[class*=language-],[part~=wy-code] pre[class*=language-],.wy-code .code,.wy-code code[class*=language-],.wy-code pre[class*=language-],[part~=wy-core-editor] .cm-content .code,[part~=wy-core-editor] .cm-content code[class*=language-],[part~=wy-core-editor] .cm-content pre[class*=language-]{color:var(--wy-code-text, var(--wy-code-text-light, #3b3b3b));font-family:var(--wy-font-monospace, ui-monospace, SFMono-Regular, SF Mono, Menlo, Consolas, Liberation Mono, monospace);text-align:start;white-space:pre-wrap;word-spacing:normal;word-break:normal;word-wrap:break-word;line-height:1.25;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-hyphens:none;-moz-hyphens:none;-ms-hyphens:none;hyphens:none}@media print{[part~=wy-code] code[class*=language-],[part~=wy-code] pre[class*=language-],.wy-code code[class*=language-],.wy-code pre[class*=language-],[part~=wy-core-editor] .cm-content code[class*=language-],[part~=wy-core-editor] .cm-content pre[class*=language-]{text-shadow:none}}[part~=wy-code] pre[class*=language-],.wy-code pre[class*=language-],[part~=wy-core-editor] .cm-content pre[class*=language-]{padding:1em;margin:.5em 0;overflow:auto}[part~=wy-code] :not(pre)>code[class*=language-],[part~=wy-code] pre[class*=language-],.wy-code :not(pre)>code[class*=language-],.wy-code pre[class*=language-],[part~=wy-core-editor] .cm-content :not(pre)>code[class*=language-],[part~=wy-core-editor] .cm-content pre[class*=language-]{color:var(--wy-code-text, var(--wy-code-text-light, #3b3b3b));background:var(--wy-background, var(--wy-background-light, #f7f9ff))}[part~=wy-code] :not(pre)>code[class*=language-],.wy-code :not(pre)>code[class*=language-],[part~=wy-core-editor] .cm-content :not(pre)>code[class*=language-]{padding:.1em;border-radius:.3em;white-space:normal}[part~=wy-code] .token.variable,.wy-code .token.variable,[part~=wy-core-editor] .cm-content .token.variable{color:var(--wy-code-variable, var(--wy-code-variable-light, #001080))}[part~=wy-code] .token.operator,.wy-code .token.operator,[part~=wy-core-editor] .cm-content .token.operator{color:var(--wy-code-operator, var(--wy-code-operator-light, #000000))}[part~=wy-code] .token.prolog,.wy-code .token.prolog,[part~=wy-core-editor] .cm-content .token.prolog{color:var(--wy-code-prolog, var(--wy-code-prolog-light, #000080))}[part~=wy-code] .token.comment,.wy-code .token.comment,[part~=wy-core-editor] .cm-content .token.comment{color:var(--wy-code-comment, var(--wy-code-comment-light, #008000))}[part~=wy-code] .token.builtin,.wy-code .token.builtin,[part~=wy-core-editor] .cm-content .token.builtin{color:var(--wy-code-builtin, var(--wy-code-builtin-light, #0070C1))}[part~=wy-code] .token.number,.wy-code .token.number,[part~=wy-core-editor] .cm-content .token.number{color:var(--wy-code-number, var(--wy-code-number-light, #098658))}[part~=wy-code] .token.inserted,.wy-code .token.inserted,[part~=wy-core-editor] .cm-content .token.inserted{color:var(--wy-code-inserted, var(--wy-code-inserted-light, #098658))}[part~=wy-code] .token.constant,.wy-code .token.constant,[part~=wy-core-editor] .cm-content .token.constant{color:var(--wy-code-constant, var(--wy-code-constant-light, #811F3F))}[part~=wy-code] .token.hexcode,.wy-code .token.hexcode,[part~=wy-core-editor] .cm-content .token.hexcode{color:var(--wy-code-hexcode, var(--wy-code-hexcode-light, #811F3F))}[part~=wy-code] .token.regex,.wy-code .token.regex,[part~=wy-core-editor] .cm-content .token.regex{color:var(--wy-code-regex, var(--wy-code-regex-light, #811F3F))}[part~=wy-code] .token.char,.wy-code .token.char,[part~=wy-core-editor] .cm-content .token.char{color:var(--wy-code-char, var(--wy-code-char-light, #811F3F))}[part~=wy-code] .token.tag,.wy-code .token.tag,[part~=wy-core-editor] .cm-content .token.tag{color:var(--wy-code-tag, var(--wy-code-tag-light, #800000))}[part~=wy-code] .token.attr-name,.wy-code .token.attr-name,[part~=wy-core-editor] .cm-content .token.attr-name{color:var(--wy-code-attr-name, var(--wy-code-attr-name-light, #E50000))}[part~=wy-code] .token.selector,.wy-code .token.selector,[part~=wy-core-editor] .cm-content .token.selector{color:var(--wy-code-selector, var(--wy-code-selector-light, #E50000))}[part~=wy-code] .token.property,.wy-code .token.property,[part~=wy-core-editor] .cm-content .token.property{color:var(--wy-code-property, var(--wy-code-property-light, #E50000))}[part~=wy-code] .token.deleted,.wy-code .token.deleted,[part~=wy-core-editor] .cm-content .token.deleted{color:var(--wy-code-deleted, var(--wy-code-deleted-light, #A31515))}[part~=wy-code] .token.string,.wy-code .token.string,[part~=wy-core-editor] .cm-content .token.string{color:var(--wy-code-string, var(--wy-code-string-light, #A31515))}[part~=wy-code] .token.changed,.wy-code .token.changed,[part~=wy-core-editor] .cm-content .token.changed{color:var(--wy-code-changed, var(--wy-code-changed-light, #0451A5))}[part~=wy-code] .token.punctuation,.wy-code .token.punctuation,[part~=wy-core-editor] .cm-content .token.punctuation{color:var(--wy-code-punctuation, var(--wy-code-punctuation-light, #000000))}[part~=wy-code] .token.function,.wy-code .token.function,[part~=wy-core-editor] .cm-content .token.function{color:var(--wy-code-function, var(--wy-code-function-light, #0000FF))}[part~=wy-code] .token.keyword,.wy-code .token.keyword,[part~=wy-core-editor] .cm-content .token.keyword{color:var(--wy-code-keyword, var(--wy-code-keyword-light, #AF00DB))}[part~=wy-code] .token.class-name,.wy-code .token.class-name,[part~=wy-core-editor] .cm-content .token.class-name{color:var(--wy-code-class-name, var(--wy-code-class-name-light, #267F99))}[part~=wy-code] .token.important,[part~=wy-code] .token.bold,.wy-code .token.important,.wy-code .token.bold,[part~=wy-core-editor] .cm-content .token.important,[part~=wy-core-editor] .cm-content .token.bold{font-weight:700}[part~=wy-code] .token.italic,.wy-code .token.italic,[part~=wy-core-editor] .cm-content .token.italic{font-style:italic}[part~=wy-core-editor]{box-sizing:border-box}[part~=wy-core-editor] .cm-scroller{font-family:unset;line-height:1.5}[part~=wy-core-editor] .cm-widgetBuffer{vertical-align:unset}[part~=wy-core-editor] .cm-placeholder{pointer-events:none;color:var(--wy-outline, var(--wy-outline-light, #72777f))}[part~=wy-core-editor] .cm-content{caret-color:var(--wy-on-background, var(--wy-on-background-light, #181c20))}[part~=wy-core-editor] .cm-content ::part(wy-link){text-decoration:var(--wy-link-decoration, none);color:var(--wy-link, var(--wy-primary-light, #2f628c))}[part~=wy-core-editor] .wy-mention{font-weight:700}[part~=wy-core-editor] .cm-editor{box-sizing:border-box;flex-direction:column;position:relative!important;display:flex!important}[part~=wy-core-editor] .cm-editor.cm-focused{--wy-component-color: var(--wy-on-background, var(--wy-on-background-light, #181c20));color:var(--wy-component-color);border-color:var(--wy-primary, var(--wy-primary-light, #2f628c));outline:0}[part~=wy-core-editor] .cm-editor .cm-scroller{overflow:auto}[part~=wy-core-editor] .cm-editor .cm-line{display:block;padding:0px var(--wy-padding-sm, calc(.75 * var(--wy-padding, calc(.5 * var(--wy-size, 1rem))))) 0px var(--wy-padding-sm, calc(.75 * var(--wy-padding, calc(.5 * var(--wy-size, 1rem)))))}[part~=wy-core-editor] .cm-tooltip.cm-tooltip-autocomplete>ul{font-family:unset;max-height:calc(15 * var(--wy-size, 1rem))}[part~=wy-core-editor] .cm-tooltip.cm-tooltip-autocomplete>ul>li{padding:0}[part~=wy-core-editor] .cm-tooltip{border:none;background-clip:padding-box;background-color:var(--wy-surface-container-low, var(--wy-surface-container-low-light, #f1f3f9));color:var(--wy-on-surface, var(--wy-on-surface-light, #181c20));border-radius:var(--wy-border-radius, calc(.5 * var(--wy-size, 1rem)));box-shadow:var(--wy-shadow-level1, 0 calc(.125 * var(--wy-size, 1rem)) calc(.125 * var(--wy-size, 1rem)) 0 rgba(0, 0, 0, .14), 0 calc(.1875 * var(--wy-size, 1rem)) calc(.0625 * var(--wy-size, 1rem)) calc(-.125 * var(--wy-size, 1rem)) rgba(0, 0, 0, .2), 0 calc(.0625 * var(--wy-size, 1rem)) calc(.3125 * var(--wy-size, 1rem)) 0 rgba(0, 0, 0, .12));overflow:hidden;z-index:1000}[part~=wy-core-editor] .cm-tooltip-autocomplete ul li[aria-selected] wy-item{background:var(--wy-primary-container, var(--wy-primary-container-light, #cee5ff));color:unset}[part~=wy-core-editor] .cm-tooltip-autocomplete .cm-completionLabel{display:none}[part~=wy-core-editor] .cm-tooltip-autocomplete ::part(wy-item-interactive):hover,[part~=wy-core-editor] .cm-tooltip-autocomplete ::part(wy-item-interactive):focus{background:var(--wy-surface-variant, var(--wy-surface-variant-light, #dee3eb))}`, QC = j`[part~=wy-core-editor]{flex:1 1 100%;display:flex;flex-direction:column}[part~=wy-core-editor-message]{margin-inline:var(--wy-padding-sm, calc(.75 * var(--wy-padding, calc(.5 * var(--wy-size, 1rem)))))}[part~=wy-core-editor] .cm-editor{flex:1 1 100%;max-height:11.25em;background-color:var(--wy-surface-container-lowest, var(--wy-surface-container-lowest-light, #ffffff));color:var(--wy-on-surface, var(--wy-on-surface-light, #181c20));border:var(--wy-input-border-width, var(--wy-border, 1px)) solid transparent;caret-color:var(--wy-primary, var(--wy-primary-light, #2f628c))}[part~=wy-core-editor] .cm-editor:focus{border-color:var(--wy-primary, var(--wy-primary-light, #2f628c))}[part~=wy-core-editor] .cm-editor .cm-content{font-family:var(--wy-input-font-family, var(--wy-font-family, inherit)),inherit;font-size:var(--wy-input-font-size, var(--wy-font-size, 1em));padding:var(--wy-input-padding-y, var(--wy-padding, calc(.5 * var(--wy-size, 1rem)))) var(--wy-input-padding-x, var(--wy-padding-lg, calc(1.5 * var(--wy-padding, calc(.5 * var(--wy-size, 1rem))))))}[part~=wy-core-editor-tooltips]{width:0;height:0;position:fixed;z-index:1000;border:none;padding:0;overflow:visible}[part~=wy-core-editor-tooltips]:not([popover]){display:block}[part~=wy-core-post-editor] .cm-editor{min-height:4em;border-radius:var(--wy-border-radius, calc(.5 * var(--wy-size, 1rem)))}[part~=wy-core-post-editor] .cm-editor .cm-line{padding:0}[part~=wy-core-editor-message] .cm-editor{border-radius:var(--wy-input-filled-border-radius, var(--wy-border-radius-lg, var(--wy-border-radius, calc(1.25 * var(--wy-size, 1rem)))));margin:calc(-1 * var(--wy-input-border-width, var(--wy-border, 1px)))}[part~=wy-core-editor-message] .cm-editor.cm-focused{border-color:var(--wy-outline-variant, var(--wy-outline-variant-light, #c2c7cf))}[part~=wy-core-editor-message] .cm-editor .cm-content{font-size:var(--wy-input-font-size, var(--wy-font-size, 1em));min-height:calc(var(--wy-input-font-size, var(--wy-font-size, 1em)) * 1.5 * var(--wy-size, 1rem) + var(--wy-input-padding-y, var(--wy-padding, calc(.5 * var(--wy-size, 1rem)))) * 2 - var(--wy-input-border-width, var(--wy-border, 1px)));line-height:calc(1.5 * var(--wy-size, 1rem))}[part~=wy-core-editor-comment] .cm-editor{border-radius:calc(1.25 * var(--wy-size, 1rem));margin:calc(-1 * var(--wy-input-border-width, var(--wy-border, 1px)))}`;
var GC = Object.defineProperty, ZC = Object.getOwnPropertyDescriptor, gg = /* @__PURE__ */ n((s) => {
  throw TypeError(s);
}, "__typeError$f"), ps = /* @__PURE__ */ n((s, t, e, i) => {
  for (var r = i > 1 ? void 0 : i ? ZC(t, e) : t, a = s.length - 1, o; a >= 0; a--)
    (o = s[a]) && (r = (i ? o(t, e, r) : o(r)) || r);
  return i && r && GC(t, e, r), r;
}, "__decorateClass$N"), bg = /* @__PURE__ */ n((s, t, e) => t.has(s) || gg("Cannot " + e), "__accessCheck$f"), rv = /* @__PURE__ */ n((s, t, e) => (bg(s, t, "read from private field"), e ? e.call(s) : t.get(s)), "__privateGet$f"), sv = /* @__PURE__ */ n((s, t, e) => t.has(s) ? gg("Cannot add the same private member more than once") : t instanceof WeakSet ? t.add(s) : t.set(s, e), "__privateAdd$f"), YC = /* @__PURE__ */ n((s, t, e, i) => (bg(s, t, "write to private field"), t.set(s, e), e), "__privateSet$b"), Rh, rw, Wo;
let Vi = (Wo = class extends qt {
  constructor() {
    super(), this.exportParts = new W(this), this.disabled = !1, this.placeholder = "", this.text = "", this.typing = !0, this.editorType = "posts", this.showTooltips = !1, sv(this, Rh), sv(this, rw, new Promise((t) => {
      YC(this, Rh, t);
    })), this.editorRef = J(), this.editorInitialized = !1, this.tooltipsRef = J(), this.throttledTyping = Kw(
      () => {
        const t = new CustomEvent("typing", {
          bubbles: !0,
          composed: !0
        });
        this.dispatchEvent(t);
      },
      2e3,
      { leading: !0, trailing: !1 }
    ), this.addEventListener("keydown", Gl), this.addEventListener("keyup", lr);
  }
  /**
   * Select all content in the editor.
   *
   * @internal
   */
  async selectAllContent() {
    await this.whenEditor(), this.editor?.dispatch({
      selection: {
        anchor: 0,
        head: this.editor.state.doc.length
      }
    });
  }
  /**
   * Place cursor at the end of the editor content.
   *
   * @internal
   */
  async setCursorLast() {
    await this.whenEditor(), this.editor?.dispatch({
      selection: {
        anchor: this.editor.state.doc.length,
        head: this.editor.state.doc.length
      }
    });
  }
  /**
   * Focus the editor input.
   *
   * @internal
   */
  async focusInput() {
    await this.whenEditor(), this.editor?.focus();
  }
  /**
   * Resolves when the editor is available.
   *
   * @returns {Promise<EditorView>}
   */
  async whenEditor() {
    return await rv(this, rw);
  }
  willUpdate(t) {
    super.willUpdate(t), (t.has("weavy") || t.has("app") || t.has("user")) && (t.get("weavy") || t.get("app") || t.get("user")) && this.weavy && this.app && this.user && this.clear(), t.has("text") && this.editor && this.editor.state.doc.toString() !== this.text && this.editor.dispatch({ changes: { from: 0, to: this.editor.state.doc.length, insert: this.text } });
  }
  updated(t) {
    if ((t.has("weavy") || t.has("user")) && this.weavy && this.user && this.editorRef.value && this.tooltipsRef.value && this.weavy.whenUrl().then(() => {
      import(
        /* webpackIgnore: true */
        /* @vite-ignore */
        new URL("./es/editor-CI6f7aKN.js", typeof WEAVY_IMPORT_URL == "string" && (!import.meta.url || !new URL(import.meta.url).href.startsWith(WEAVY_IMPORT_URL)) ? new URL(WEAVY_IMPORT_URL, globalThis.location.href) : import.meta.url).href
      ).then((e) => e.editor).then(
        ({
          weavyHighlighter: e,
          syntaxHighlighting: i,
          history: r,
          dropCursor: a,
          mentions: o,
          tooltips: c,
          autocompletion: l,
          placeholder: u,
          keymap: b,
          weavyKeymap: C,
          defaultKeymap: v,
          historyKeymap: f,
          markdown: m,
          languages: $,
          EditorView: E,
          EditorState: M,
          Compartment: A
        }) => {
          var H;
          this.editorInitialized || (this.editorInitialized = !0, this.editorEditable = new A(), this.editorPlaceholder = new A(), this.editorKeymap = new A(), this.EditorView = E, this.placeholderExtension = u, this.keymapFacet = b, this.keymaps = {
            weavyModifierEnterSendKeymap: [{ key: "Mod-Enter", run: /* @__PURE__ */ n(() => this.submit(), "run") }],
            weavyEnterSendKeymap: [{ key: "Enter", run: /* @__PURE__ */ n(() => this.submit(), "run") }],
            weavyKeymap: [
              ...C,
              {
                key: "ArrowUp",
                run: /* @__PURE__ */ n((D) => D.state.doc.toString().trim() ? !1 : this.editLast(), "run")
              }
            ],
            defaultKeymap: [...v],
            historyKeymap: [...f]
          }, this.editorExtensions = [
            E.contentAttributes.of({
              spellcheck: "true",
              autocorrect: "on",
              autocapitalize: "on",
              enterkeyhint: this.settings?.enterToSend === "always" ? "send" : "enter"
            }),
            c({
              parent: this.tooltipsRef.value,
              position: Vn() ? "fixed" : "absolute",
              tooltipSpace: /* @__PURE__ */ n((D) => {
                this.showTooltips = !0;
                try {
                  this.tooltipsRef.value?.showPopover();
                } catch {
                }
                const I = D.dom.ownerDocument.documentElement;
                return { top: 0, left: 0, bottom: I.clientHeight, right: I.clientWidth };
              }, "tooltipSpace")
            }),
            r(),
            a(),
            o,
            l({
              override: this.componentFeatures?.allowsFeature(_.Mentions) ? [(D) => this.autocomplete(D)] : [],
              //showMention
              closeOnBlur: !1,
              aboveCursor: this.editorType !== "posts",
              icons: !1,
              addToOptions: [
                {
                  render: /* @__PURE__ */ n(function(D, I) {
                    const B = document.createElement("wy-item");
                    B.interactive = !1, (!D.item?.access || D.item.access === Rr.None) && (B.disabled = !0);
                    const Q = document.createElement("wy-avatar");
                    Q.slot = "image", Q.src = D.item?.avatar_url || "", Q.name = D.item?.name || "";
                    const it = document.createElement("span");
                    return it.slot = "title", it.innerText = D.item?.name || "", B.appendChild(Q), B.appendChild(it), B;
                  }, "render"),
                  position: 10
                }
              ]
            }),
            i(e, { fallback: !0 }),
            E.lineWrapping,
            m({ codeLanguages: $ }),
            E.domEventHandlers({
              paste: /* @__PURE__ */ n((D, I) => {
                let B = [];
                const Q = D.clipboardData?.items || [];
                for (const it of Q)
                  if (it.kind === "file") {
                    const ct = it.getAsFile();
                    ct && (B = [...B, ct]);
                  }
                if (this.componentFeatures?.allowsFeature(_.Attachments) && B.length > 0)
                  return this.dispatchFiles(B);
              }, "paste"),
              keyup: /* @__PURE__ */ n((D, I) => {
                this.componentFeatures?.allowsFeature(_.Typing) && this.typing && I.state.doc.toString() !== "" && this.throttledTyping();
              }, "keyup")
            }),
            E.updateListener.of((D) => {
              D.docChanged && (this.text = D.state.doc.toString().trim(), this.dispatchChange(this.text));
            }),
            // Compartments
            this.editorEditable.of(E.editable.of(!this.disabled)),
            this.editorPlaceholder.of(this.placeholderExtension(this.placeholder)),
            this.editorKeymap.of(this.keymapFacet.of(this.getKeymaps())),
            E.updateListener.of((D) => {
              this.updatePlaceHolderText();
              const I = Array.from(
                (this.editor?.state).config.compartments.keys()
              );
              this.editorEditable = I[0], this.editorPlaceholder = I[1], this.editorKeymap = I[2];
            })
          ], this.editor || (this.editor = new E({
            state: M.create({
              doc: this.text,
              extensions: this.editorExtensions
            }),
            parent: this.editorRef.value
          }), (H = rv(this, Rh)) == null || H.call(this, this.editor)));
        }
      );
    }), t.has("disabled") && this.editor && this.editorEditable && this.EditorView && this.editor.dispatch({
      // Update readonly state
      effects: this.editorEditable.reconfigure(this.EditorView.editable.of(!this.disabled))
    }), t.has("placeholder") && this.editor && this.editorPlaceholder && this.placeholderExtension) {
      const e = this.placeholderExtension(this.placeholder);
      this.editor.dispatch({
        // Update placeholder state
        effects: this.editorPlaceholder.reconfigure(e)
      }), this.updatePlaceHolderText();
    }
    if (t.has("settings") && this.editor && this.editorKeymap && this.keymapFacet && (this.editor.dispatch({
      // Update readonly state
      effects: this.editorKeymap.reconfigure(this.keymapFacet.of(this.getKeymaps()))
    }), this.updateEnterKeyHint()), t.has("showTooltips"))
      try {
        this.showTooltips ? this.tooltipsRef.value?.showPopover() : this.tooltipsRef.value?.hidePopover();
      } catch {
      }
  }
  /**
   * Update the visible placeholder text within the fallback DOM copy.
   *
   * @internal
   */
  updatePlaceHolderText() {
    const t = this.renderRoot.querySelector(".cm-placeholder");
    t && this.editor && (t.setAttribute("aria-label", `placeholder ${this.placeholder}`), t.innerText = this.placeholder);
  }
  /**
   * Update enter key hint on the content element.
   *
   * @internal
   */
  updateEnterKeyHint() {
    const t = this.renderRoot.querySelector(".cm-content");
    t && this.editor && (t.enterKeyHint = this.settings?.enterToSend === "always" ? "send" : "enter");
  }
  /**
   * Compute and return keymaps for the current settings/editor type.
   *
   * @internal
   */
  getKeymaps() {
    if (!this.keymaps)
      return [];
    const { weavyKeymap: t, defaultKeymap: e, historyKeymap: i, weavyEnterSendKeymap: r, weavyModifierEnterSendKeymap: a } = this.keymaps;
    let o = this.settings?.enterToSend === "never" ? [] : [...a];
    return ((!this.settings?.enterToSend || this.settings?.enterToSend === "auto") && this.editorType === "messages" && jC || this.settings?.enterToSend === "always") && (o = [...r, ...o]), [...o, ...t, ...e, ...i];
  }
  /**
   * Autocomplete handler for mentions. Returns completion results or null.
   *
   * Called by the editor to resolve mention suggestions.
   *
   * @internal
   * @param context - Completion context from the editor
   * @returns Promise<CompletionResult | null>
   */
  async autocomplete(t) {
    if (!this.weavy || !this.app)
      return null;
    let e = t.matchBefore(/(?:^|\s)(?!\]\()@(?=\S)([^@]+)/);
    if (!t.explicit && !e) return null;
    e = t.matchBefore(/@[^@]+/);
    const i = e?.text.substring(1), a = await (await this.weavy.fetch(`/api/apps/${this.app.id}/members?member=null&q=${i}`)).json();
    let o = [];
    return a.data && (o = a.data.filter((c) => typeof c.name < "u").map((c) => ({
      item: c,
      label: c.name,
      apply: /* @__PURE__ */ n((l, u, b, C) => {
        const v = "[" + c.name + "](@u" + c.id.toString() + ")";
        let f = l.state.update({ changes: { from: b - 1, to: b } });
        l.dispatch(f), f = l.state.update({
          changes: { from: b - 1, to: C - 1, insert: v }
        }), l.dispatch(f);
      }, "apply")
    }))), {
      from: e ? e.from + 1 : t.pos,
      options: o,
      filter: !1
    };
  }
  /**
   * Submit the editor content by dispatching a `submit` event.
   *
   * Validates that there is content to submit and that no uploads/embeds are still processing.
   */
  submit() {
    const t = this.editor?.state.doc.toString().trim() ?? "", e = new CustomEvent("submit", {
      detail: { text: t },
      bubbles: !1,
      composed: !1
    }), i = this.dispatchEvent(e);
    return i && this.clear(), i;
  }
  editLast() {
    const t = new CustomEvent("edit-last", {
      bubbles: !0,
      composed: !0
    });
    return this.dispatchEvent(t);
  }
  /**
   * Clear the editor UI and internal state (text, embeds, attachments, metadata).
   */
  clear() {
    const t = new CustomEvent("clear", {
      bubbles: !0,
      composed: !0
    });
    this.dispatchEvent(t) && (this.editor?.dispatch({ changes: { from: 0, to: this.editor.state.doc.length, insert: "" } }), this.text = "");
  }
  dispatchChange(t) {
    const e = new CustomEvent("change", {
      detail: { text: t },
      bubbles: !0,
      composed: !0
    });
    return this.dispatchEvent(e);
  }
  dispatchFiles(t) {
    const e = new CustomEvent("drop-files", {
      detail: { files: t },
      bubbles: !0,
      composed: !0
    });
    return this.dispatchEvent(e);
  }
  /**
   * Editor loading fallback dummy.
   * Hard copy of the rendered nodes when the editor is empty. Cleaned up to not be editable.
   */
  renderEditorDummy() {
    return this.editorInitialized ? g : h`
          <div class="cm-editor">
            <div class="cm-announced"></div>
            <div tabindex="-1" class="cm-scroller">
              <div class="cm-content cm-lineWrapping">
                <div class="cm-line"
                  >${this.text ? this.text : h`<img class="cm-widgetBuffer" aria-hidden="true" /><span class="cm-placeholder"
                          >${this.placeholder}</span
                        ><br />`}</div
                >
              </div>
            </div>
          </div>
        `;
  }
  render() {
    return h`
      <div
        part=${tt({
      "wy-core-editor": !0,
      "wy-core-post-editor": this.editorType === "posts",
      "wy-core-editor-comment": this.editorType === "comments",
      "wy-core-editor-message": this.editorType === "messages"
    })}
        ${K(this.editorRef)}
      >
        ${this.renderEditorDummy()}
        <div
          ${K(this.tooltipsRef)}
          part="wy-core-editor-tooltips"
          ?hidden=${Vn() && !this.showTooltips}
          popover=${ot(Vn() ? void 0 : "auto")}
        ></div>
      </div>
    `;
  }
}, n(Wo, "WyEditorCore"), Wo);
Rh = /* @__PURE__ */ new WeakMap();
rw = /* @__PURE__ */ new WeakMap();
Vi.styles = [Lt, KC, QC, Y];
ps([
  y({ type: Boolean })
], Vi.prototype, "disabled", 2);
ps([
  y()
], Vi.prototype, "placeholder", 2);
ps([
  y()
], Vi.prototype, "text", 2);
ps([
  y({ type: Boolean })
], Vi.prototype, "typing", 2);
ps([
  y()
], Vi.prototype, "editorType", 2);
ps([
  V()
], Vi.prototype, "showTooltips", 2);
ps([
  V()
], Vi.prototype, "editor", 2);
Vi = ps([
  U("wy-editor-core")
], Vi);
var XC = Object.defineProperty, JC = Object.getOwnPropertyDescriptor, xg = /* @__PURE__ */ n((s) => {
  throw TypeError(s);
}, "__typeError$e"), pe = /* @__PURE__ */ n((s, t, e, i) => {
  for (var r = i > 1 ? void 0 : i ? JC(t, e) : t, a = s.length - 1, o; a >= 0; a--)
    (o = s[a]) && (r = (i ? o(t, e, r) : o(r)) || r);
  return i && r && XC(t, e, r), r;
}, "__decorateClass$M"), Cg = /* @__PURE__ */ n((s, t, e) => t.has(s) || xg("Cannot " + e), "__accessCheck$e"), Xp = /* @__PURE__ */ n((s, t, e) => (Cg(s, t, "read from private field"), e ? e.call(s) : t.get(s)), "__privateGet$e"), Jp = /* @__PURE__ */ n((s, t, e) => t.has(s) ? xg("Cannot add the same private member more than once") : t instanceof WeakSet ? t.add(s) : t.set(s, e), "__privateAdd$e"), nv = /* @__PURE__ */ n((s, t, e, i) => (Cg(s, t, "write to private field"), t.set(s, e), e), "__privateSet$a"), Th, Lh, sw, jo;
let Tt = (jo = class extends qt {
  constructor() {
    super(), this.exportParts = new W(this), this.storage = Rv("localStorage"), this.disabled = !1, this.placeholder = "", this.text = "", this.metadata = {}, this.pollOptions = [], this.attachments = [], this.buttonText = "", this.typing = !0, this.draft = !1, this.editorType = "posts", this.editorClass = "wy-editor-msg", this.editorLocation = "apps", Jp(this, Th, []), Jp(this, Lh), Jp(this, sw, new Promise((t) => {
      nv(this, Lh, t);
    })), this.draftKey = "", this.uploadBlobMutation = new Ue(
      this
    ), this.mutatingFiles = new Wc(this), this.fileInputRef = J(), this.cloudFilesRef = J(), this.dropZone = new tu(this), this.editorRef = J(), this.throttledDrafting = Kw(
      () => {
        this.saveDraft();
      },
      500,
      { leading: !0, trailing: !0 }
    ), this.createMeeting = async (t) => {
      if (this.weavy && t.source === this.authWindow && this.weavy.url.origin === t.origin && t.data && t.data.name && t.data.name.endsWith("-authorized")) {
        const e = t.data.name.slice(0, -11), r = await Jm(this.weavy, e).mutate();
        r.auth_url || (this.meeting = r);
      }
    }, this.addEventListener("drop-files", (t) => this.handleDropFiles(t)), this.addEventListener("keydown", Gl), this.addEventListener("keyup", lr);
  }
  /**
   * Select all content in the editor.
   */
  async selectAllContent() {
    await this.whenEditor(), await this.editorRef.value?.selectAllContent();
  }
  /**
   * Place cursor at the end of the editor content.
   */
  async setCursorLast() {
    await this.whenEditor(), await this.editorRef.value?.setCursorLast();
  }
  /**
   * Focus the editor input.
   */
  async focusInput() {
    await this.whenEditor(), await this.editorRef.value?.focusInput();
  }
  set embeds(t) {
    const e = t.filter((i) => i.type !== "link" || i.title || i.description);
    nv(this, Th, [...e]);
  }
  get embeds() {
    return Xp(this, Th);
  }
  /**
   * Resolves when the editor is available.
   *
   * @returns {Promise<WyEditorCore>}
   */
  async whenEditor() {
    return await Xp(this, sw);
  }
  /**
   * Throttled typing indicator sender to reduce network traffic.
   *
   * @internal
   */
  async handleTyping() {
    this.weavy && this.app && !Jh.has(this.app.type) && await EC(this.weavy, this.app.id).mutate();
  }
  willUpdate(t) {
    if (super.willUpdate(t), t.has("app") && (this.mutationAppId = void 0), (t.has("weavy") || t.has("app") || t.has("user") || t.has("parentId")) && this.weavy && this.app && this.user) {
      if (this.mutationAppId = this.mutationAppId ?? this.app?.id ?? Date.now() * -1, this.draftKey = `draft-${this.editorType}-${this.parentId || this.mutationAppId}`, this.uploadBlobMutation.trackMutation(
        Sw(
          this.weavy,
          this.user,
          this.mutationAppId,
          `${this.editorLocation}-${this.parentId || this.mutationAppId}`
        )
      ), this.mutatingFiles.trackMutationState(
        {
          filters: {
            mutationKey: [
              "apps",
              this.mutationAppId,
              "blobs",
              `${this.editorLocation}-${this.parentId || this.mutationAppId}`
            ],
            exact: !0
          }
        },
        this.weavy.queryClient
      ), this.externalBlobMutation = wg(
        this.weavy,
        this.user,
        this.mutationAppId,
        `${this.editorLocation}-${this.parentId || this.mutationAppId}`
      ), this.draft && this.storage && (!this.hasChanged() || t.get("weavy") || t.get("app") || t.get("user") || t.get("parentId"))) {
        const e = this.storage.getItem(this.draftKey);
        if (e) {
          const i = JSON.parse(e);
          this.text = i.text ?? "", this.embeds = i.embeds, this.meeting = i.meeting, i.pollOptions?.length > 0 && (this.pollOptions = i.pollOptions), tv(this.embeds.map((r) => r.url).filter((r) => typeof r == "string"));
        } else
          this.clearEditor();
      }
      this.embed && (this.embeds = [this.embed], tv(this.embeds.map((e) => e.url).filter((e) => typeof e == "string")));
    }
  }
  updated(t) {
    var e;
    this.editorRef.value && ((e = Xp(this, Lh)) == null || e.call(this, this.editorRef.value)), t.has("text") && (this.throttledDrafting(), this.componentFeatures?.allowsFeature(_.Embeds) && this.text !== "" && this.handleEmbeds(this.text));
  }
  connectedCallback() {
    super.connectedCallback(), window.addEventListener("message", this.createMeeting);
  }
  disconnectedCallback() {
    super.disconnectedCallback(), window.removeEventListener("message", this.createMeeting);
  }
  handleRemoveMeeting() {
    this.meeting = void 0;
  }
  /**
   * Open native file picker.
   *
   * @internal
   */
  async openFileInput() {
    await this.whenEditor(), this.fileInputRef.value?.click();
  }
  /**
   * Open cloud files picker overlay.
   *
   * @internal
   */
  async openCloudFiles() {
    await this.whenEditor(), this.cloudFilesRef.value?.open();
  }
  /**
   * Handle files dropped on the editor.
   *
   * @internal
   * @param e - Drop files event
   */
  handleDropFiles(t) {
    const e = t.detail;
    e.files && this.handleUploadFiles(e.files);
  }
  /**
   * Upload array of File objects via the upload blob mutation controller.
   *
   * Preserves input value if provided and saves draft after each upload.
   *
   * @internal
   * @param files - FileList or array of File objects to upload
   * @param input - Optional input element to reset after upload
   */
  async handleUploadFiles(t, e) {
    if (t) {
      for (let i = 0; i < t.length; i++) {
        const a = { file: t[i] };
        await this.uploadBlobMutation.mutate(a), this.saveDraft();
      }
      e && (e.value = "");
    }
  }
  /**
   * Handle external blob imports (cloud/external sources).
   *
   * @internal
   * @param externalBlobs - Array of external blob descriptors
   */
  handleExternalBlobs(t) {
    if (t)
      for (let e = 0; e < t.length; e++) {
        const i = t[e];
        this.externalBlobMutation?.mutate({ externalBlob: i });
      }
  }
  /**
   * Handle removal/abortion of an upload mutation and clean cache state.
   *
   * @internal
   * @param mutation - Mutation state object to remove
   */
  handleRemoveUpload(t) {
    !this.weavy || !this.mutationAppId || (t.status === "pending" && Fy(t.variables) && t.variables.abort?.(), l1(
      this.weavy.queryClient,
      ["apps", this.mutationAppId, "blobs", `${this.editorLocation}-${this.parentId || this.mutationAppId}`],
      (e) => e.state.data?.id === t.data?.id
    ));
  }
  /**
   * Persist draft to storage if draft mode is enabled.
   *
   * Saves editor text, embeds, poll options and ongoing file uploads to localStorage.
   *
   * @internal
   */
  saveDraft() {
    if (!this.draft || !this.storage) return;
    const t = this.mutatingFiles.result;
    if ((!t || !t.length) && !this.meeting && !this.embeds.length && (!this.pollOptions.length || this.pollOptions.filter((e) => e.text.trim() !== "").length === 0) && this.text === "")
      this.storage.removeItem(this.draftKey);
    else {
      const e = {
        meeting: this.meeting,
        text: this.text,
        pollOptions: this.pollOptions.filter((i) => i.text.trim() !== ""),
        embeds: this.embeds
      };
      this.storage.setItem(this.draftKey, JSON.stringify(e));
    }
  }
  /**
   * Handle removal of an attachment reference from the editor state.
   *
   * @internal
   * @param attachment - Attachment to remove
   */
  handleRemoveAttachment(t) {
    this.attachments = this.attachments.filter((e) => e.id !== t.id);
  }
  /**
   * Submit the editor content by dispatching a `submit` event.
   *
   * Validates that there is content to submit and that no uploads/embeds are still processing.
   *
   * @internal
   */
  submit() {
    const t = this.mutatingFiles.result, e = t?.some((b) => b.status === "pending"), i = this.text.trim() ?? "", r = this.meeting?.id, a = t?.map((b) => b.data?.id).filter(cn), o = this.attachments?.map((b) => b.id) || [], c = this.pollOptions.filter((b) => b.text.trim() !== "");
    if (OC() || e || !this.contextDataBlobs || !i && !r && a?.length == 0 && c.length == 0 && o.length == 0 && this.embeds.length == 0)
      return;
    const l = this.contextDataBlobs.length ? this.contextDataBlobs : void 0, u = new CustomEvent("submit", {
      detail: { text: i, meetingId: r, blobs: a, attachments: o, pollOptions: c, embedId: this.embeds[0]?.id, contextData: l },
      bubbles: !0,
      composed: !0
    });
    this.dispatchEvent(u), this.resetEditor();
  }
  editLast() {
    const t = new CustomEvent("edit-last", {
      bubbles: !0,
      composed: !0
    });
    this.dispatchEvent(t);
  }
  /**
   * Reset the editor state after a successful submit.
   *
   * Clears editor content, removes pending uploads from the query cache and deletes the stored draft.
   *
   * @internal
   */
  resetEditor() {
    this.clearEditor(), this.weavy && this.mutationAppId && d2(this.weavy.queryClient, [
      "apps",
      this.mutationAppId,
      "blobs",
      `${this.editorLocation}-${this.parentId || this.mutationAppId}`
    ]), this.storage?.removeItem(this.draftKey);
  }
  /**
   * Clear the editor UI and internal state (text, embeds, attachments, metadata).
   *
   * @internal
   */
  clearEditor() {
    this.text = "", this.meeting = void 0, this.pollOptions = [], this.embeds = [], this.metadata = {}, FC();
  }
  /**
   * Checks if the editor has a dirty internal state (text, embeds, attachments, metadata).
   *
   * @internal
   */
  hasChanged() {
    return this.text !== "" || this.meeting !== void 0 || this.pollOptions.length || this.embeds.length || this.metadata && this.metadata.length;
  }
  /**
   * Create or initiate a meeting for the current user.
   *
   * If the meeting requires third-party auth, opens an auth popup; otherwise stores the created meeting.
   *
   * @internal
   * @param name - Provider key for the meeting (e.g. "zoom", "google", "microsoft")
   */
  async handleMeetingClick(t) {
    if (!this.weavy || !this.user)
      return;
    const i = await Jm(this.weavy, t).mutate();
    i.auth_url ? this.authWindow = window.open(i.auth_url, "oauthwin", "height=640,width=480") : this.meeting = i;
  }
  /**
   * Add a discovered embed to the editor and persist draft.
   *
   * @internal
   * @param embed - Embed object to add.
   */
  setEmbeds(t) {
    this.embeds = [t, ...this.embeds], this.saveDraft();
  }
  /**
   * Scan content for embeds and add them via setEmbeds callback.
   *
   * @internal
   * @param content - Text content to scan for embed links.
   */
  handleEmbeds(t) {
    this.weavy && IC(t, this.setEmbeds.bind(this), this.weavy);
  }
  /**
   * Remove an embed by id from the editor embeds list and persist draft.
   *
   * @internal
   * @param e - Embed remove event containing embed id.
   */
  removeEmbed(t) {
    this.embeds = this.embeds.filter((e) => e.id !== t.detail.id), this.saveDraft();
  }
  /**
   * Cycle the primary embed (move first to the end) and persist draft.
   *
   * @internal
   */
  swapEmbed() {
    const t = this.embeds.shift();
    t && (this.embeds = [...this.embeds, t]), this.saveDraft();
  }
  /**
   * Toggle the poll UI in the editor and initialize default options when opened.
   *
   * @param [open=true] - Whether to open the polls - set `false` to close.
   * @internal
   */
  togglePolls(t) {
    this.pollOptions.length === 0 ? this.pollOptions = Array.from({ length: 3 }, () => ({ id: null, text: "" })) : t !== !0 && (this.pollOptions = []);
  }
  /**
   * Handle change to a poll option input and persist draft.
   *
   * @internal
   * @param e - Input event
   * @param index - Index of the poll option changed
   */
  handlePollOptionChange(t, e) {
    const i = [...this.pollOptions];
    i[e].text = t.target.value, this.pollOptions = i, this.saveDraft();
  }
  /**
   * Add a new poll option when focus reaches the last option (limit 5).
   *
   * @internal
   * @param e - Focus/keyboard event
   * @param index - Index of the poll option that triggered the add
   */
  handlePollOptionAdd(t, e) {
    if (e === this.pollOptions.length - 1 && this.pollOptions.length < 5) {
      const i = { id: null, text: "" };
      this.pollOptions = [...this.pollOptions, i], this.saveDraft();
    }
  }
  renderEditor() {
    return h`
      <wy-editor-core
        ${K(this.editorRef)}
        .editorType=${this.editorType}
        .typing=${this.typing}
        .text=${this.text}
        @typing=${() => this.handleTyping()}
        @change=${(t) => {
      this.text = t.detail.text;
    }}
        @edit=${() => this.editLast()}
        @drop-files=${(t) => this.handleDropFiles(t)}
        @submit=${() => this.submit()}
      ></wy-editor-core>
    `;
  }
  renderTopSlot() {
    return g;
  }
  renderMiddleSlot() {
    return h`
      <!-- Input -->
      ${this.renderEditor()}

      <div part="wy-editor-msg-inputs">
        <div part="wy-editor-msg-buttons">
          ${this.componentFeatures?.allowsFeature(_.Attachments) ? h`<wy-button
                  kind="icon"
                  @click=${() => this.openFileInput()}
                  title=${x("From device")}
                  ?disabled=${this.disabled}
                >
                  <wy-icon name="attachment"></wy-icon>
                </wy-button>
                <input
                  type="file"
                  ${K(this.fileInputRef)}
                  @click=${(t) => t.stopPropagation()}
                  @change=${(t) => this.handleUploadFiles(
      Array.from(t.target.files || []),
      t.target
    )}
                  multiple
                  hidden
                  tabindex="-1"
                  ?disabled=${this.disabled}
                />` : g}
          ${this.componentFeatures?.allowsFeature(_.CloudFiles) ? h`<wy-button
                kind="icon"
                @click=${() => this.openCloudFiles()}
                title=${x("From cloud")}
                ?disabled=${this.disabled}
              >
                <wy-icon name="cloud"></wy-icon>
              </wy-button>` : g}
          ${this.componentFeatures?.allowsAnyFeature(_.Meetings, _.ZoomMeetings) ? h`
                <wy-button
                  kind="icon"
                  @click=${() => this.handleMeetingClick("zoom")}
                  title=${x("Zoom meeting")}
                  ?disabled=${this.disabled}
                >
                  <wy-icon svg="zoom-meetings"></wy-icon>
                </wy-button>
              ` : g}
          ${this.componentFeatures?.allowsAnyFeature(_.Meetings, _.GoogleMeet) ? h`
                <wy-button
                  kind="icon"
                  @click=${() => this.handleMeetingClick("google")}
                  title=${x("Google Meet")}
                  ?disabled=${this.disabled}
                >
                  <wy-icon svg="google-meet"></wy-icon>
                </wy-button>
              ` : g}
          ${this.componentFeatures?.allowsAnyFeature(_.Meetings, _.MicrosoftTeams) ? h`
                <wy-button
                  kind="icon"
                  @click=${() => this.handleMeetingClick("microsoft")}
                  title=${x("Microsoft Teams")}
                  ?disabled=${this.disabled}
                >
                  <wy-icon svg="microsoft-teams"></wy-icon>
                </wy-button>
              ` : g}
          ${this.componentFeatures?.allowsFeature(_.Polls) ? h`<wy-button
                kind="icon"
                @click=${() => this.togglePolls()}
                title=${x("Poll")}
                ?disabled=${this.disabled}
              >
                <wy-icon name="poll"></wy-icon>
              </wy-button>` : g}
            <slot name="actions"></slot>
        </div>

        <slot name="inputs"></slot>
        
        <!-- Button -->
        <wy-button @click="${() => this.submit()}" color="primary" title=${this.buttonText} ?disabled=${this.disabled}>
          ${this.buttonText}
        </wy-button>
      </div>
    `;
  }
  /**
   * Render the bottom slot which aggregates lists (polls, meetings, file uploads, attachments, embeds).
   *
   * @internal
   */
  renderBottomSlot() {
    return [this.renderLists()];
  }
  /**
   * Render the lists section (poll options, meetings, file uploads, attachments, embeds).
   *
   * @internal
   */
  renderLists() {
    const t = this.mutatingFiles.result, e = this.componentFeatures?.allowsFeature(_.Polls) && this.pollOptions.length > 0 || !1, i = this.meeting?.provider === "zoom" && this.componentFeatures?.allowsAnyFeature(_.Meetings, _.ZoomMeetings) || this.meeting?.provider === "google" && this.componentFeatures?.allowsAnyFeature(_.Meetings, _.GoogleMeet) || this.meeting?.provider === "microsoft" && this.componentFeatures?.allowsAnyFeature(_.Meetings, _.MicrosoftTeams) || !1, r = t && t.length > 0 || !1, a = this.attachments && this.attachments.length > 0, o = this.componentFeatures?.allowsFeature(_.Embeds) && this.embeds.length > 0 || !1;
    return this.disabled || !(e || i || r || a || o) ? g : h` <div part="wy-editor-parts">
      <!-- polls -->
      ${e ? h`
            <div part="wy-poll-form">
              ${this.pollOptions.map((c, l) => {
      const u = l + 1;
      return h`<input
                  value="${c.text}"
                  part="wy-input"
                  type="text"
                  placeholder=${x(gt`Option ${u}`)}
                  @change=${(b) => this.handlePollOptionChange(b, l)}
                  @keyup=${lr}
                  @focus=${(b) => this.handlePollOptionAdd(b, l)}
                />`;
    })}
            </div>
          ` : g}

      <!-- meetings -->
      ${i && this.meeting ? h`
            <wy-item size="sm">
              <wy-icon slot="image" svg="${Yy(this.meeting.provider)}"></wy-icon>
              <span slot="title">${Xy(this.meeting.provider)}</span>
              <wy-button slot="actions" kind="icon" @click=${() => this.handleRemoveMeeting()}>
                <wy-icon name="close"></wy-icon>
              </wy-button>
            </wy-item>
          ` : g}

      <!-- blobs -->
      ${r && t ? h`<div>
            ${Be(
      t,
      (c) => "mutation" + c.submittedAt,
      (c) => {
        if (c.context?.file) {
          const l = c.context.file, u = {
            ...c.context.status
          };
          return h`
                    <wy-file-item
                      .file=${c.context.file}
                      .status=${u}
                      title="${Sv(c.context.type)}: ${l.name + (u.text ? `: ${u.text}` : "")}"
                    >
                      <span slot="title"
                        ><strong></strong> ${l.name}
                        ${u.text ? h`: <em>${u.text}</em>` : g}</span
                      >
                      ${u.state === "pending" ? h`
                            <wy-progress-circular
                              slot="actions"
                              padded
                              ?indeterminate=${!u.progress}
                              .max=${100}
                              .value=${u.progress || 0}
                            ></wy-progress-circular>
                          ` : g}
                      <wy-button
                        slot="actions"
                        kind="icon"
                        @click=${() => {
            this.handleRemoveUpload(c);
          }}
                        title=${x("Discard", { desc: "Button action to discard" })}
                      >
                        <wy-icon name="close"></wy-icon>
                      </wy-button>
                    </wy-file-item>
                  `;
        }
        return g;
      }
    )}
          </div>` : g}

      <!-- attachments -->
      ${a ? this.attachments.map(
      (c) => h`<wy-file-item .file=${c} title="${c.name}">
                <span slot="title">${c.name}</span>
                <wy-button
                  slot="actions"
                  kind="icon"
                  @click=${() => this.handleRemoveAttachment(c)}
                  title=${x("Remove", { desc: "Button action to remove" })}
                >
                  <wy-icon name="close"></wy-icon>
                </wy-button>
              </wy-file-item>`
    ) : g}

      <!-- embeds -->
      ${o ? h`
            <wy-embed-select
              .embeds=${this.embeds}
              @embed-remove=${(c) => this.removeEmbed(c)}
              @embed-swap=${() => this.swapEmbed()}
            ></wy-embed-select>
          ` : g}
    </div>`;
  }
  /**
   * Render the cloud files picker if cloud files feature is enabled.
   *
   * @internal
   */
  renderCloudFiles() {
    return this.disabled ? g : h`
      <wy-cloud-files
        ${K(this.cloudFilesRef)}
        @external-blobs=${(t) => this.handleExternalBlobs(t.detail.externalBlobs)}
      ></wy-cloud-files>
    `;
  }
  render() {
    const t = this.dropZone.isDragActive;
    return h`
      <div
        part=${tt({
      "wy-editor": !0,
      [this.editorClass]: !0,
      "wy-dragging": t
    })}
        data-drag-title=${x("Drop files here")}
      >
        ${this.renderTopSlot()} ${this.renderMiddleSlot()} ${this.renderBottomSlot()}
      </div>
      ${this.renderCloudFiles()}
    `;
  }
}, n(jo, "WyEditorMsg"), jo);
Th = /* @__PURE__ */ new WeakMap();
Lh = /* @__PURE__ */ new WeakMap();
sw = /* @__PURE__ */ new WeakMap();
Tt.styles = [Lt, DC, qw, fg, us];
pe([
  y({ type: Boolean })
], Tt.prototype, "disabled", 2);
pe([
  y({ attribute: !1 })
], Tt.prototype, "parentId", 2);
pe([
  y()
], Tt.prototype, "placeholder", 2);
pe([
  y()
], Tt.prototype, "text", 2);
pe([
  y({ type: Object })
], Tt.prototype, "metadata", 2);
pe([
  y({ attribute: !1 })
], Tt.prototype, "embed", 2);
pe([
  y({ attribute: !1 })
], Tt.prototype, "pollOptions", 2);
pe([
  y({ attribute: !1 })
], Tt.prototype, "attachments", 2);
pe([
  y({ attribute: !1 })
], Tt.prototype, "meeting", 2);
pe([
  y()
], Tt.prototype, "buttonText", 2);
pe([
  y({ type: Boolean })
], Tt.prototype, "typing", 2);
pe([
  y({ type: Boolean })
], Tt.prototype, "draft", 2);
pe([
  y()
], Tt.prototype, "editorType", 2);
pe([
  y()
], Tt.prototype, "editorClass", 2);
pe([
  y()
], Tt.prototype, "editorLocation", 2);
pe([
  V()
], Tt.prototype, "embeds", 1);
pe([
  V()
], Tt.prototype, "draftKey", 2);
pe([
  V()
], Tt.prototype, "mutationAppId", 2);
Tt = pe([
  U("wy-editor-msg"),
  ut()
], Tt);
var t$ = Object.getOwnPropertyDescriptor, e$ = /* @__PURE__ */ n((s, t, e, i) => {
  for (var r = i > 1 ? void 0 : i ? t$(t, e) : t, a = s.length - 1, o; a >= 0; a--)
    (o = s[a]) && (r = o(r) || r);
  return r;
}, "__decorateClass$L"), Ko;
let du = (Ko = class extends Tt {
  constructor() {
    super(), this.editorType = "comments", this.editorClass = "wy-editor-comment";
  }
  willUpdate(t) {
    super.willUpdate(t), t.has("editorLocation") && (this.editorLocation === "files" ? this.editorClass = "wy-editor-comment wy-editor-comment-bottom" : this.editorLocation === "apps" && (this.editorClass = "wy-editor-comment wy-editor-comment-bottom"));
  }
  /**
   * Render the top slot for comment editor with no content.
   *
   * @internal
   */
  renderTopSlot() {
    return g;
  }
  /**
   * Render the primary middle slot containing the add-menu, editor and send button.
   *
   * Overrides the base implementation to provide message-specific controls and layout.
   *
   * @internal
   */
  renderMiddleSlot() {
    return h`<div part="wy-editor-comment-inputs">
      <!-- Add -->
      ${this.componentFeatures?.allowsAnyFeature(
      _.Attachments,
      _.CloudFiles,
      _.Meetings,
      _.ZoomMeetings,
      _.GoogleMeet,
      _.MicrosoftTeams,
      _.Polls
    ) ? h`<wy-dropdown icon="plus" directionY="up" ?disabled=${this.disabled}>
            ${this.componentFeatures?.allowsFeature(_.Attachments) ? h`<wy-dropdown-item @click=${() => this.openFileInput()} title=${x("From device")}>
                    <wy-icon name="attachment"></wy-icon>
                    <span>${x("From device")}</span>
                  </wy-dropdown-item>
                  <input
                    type="file"
                    ${K(this.fileInputRef)}
                    @click=${(t) => t.stopPropagation()}
                    @change=${(t) => this.handleUploadFiles(
      Array.from(t.target.files || []),
      t.target
    )}
                    multiple
                    hidden
                    tabindex="-1"
                  />` : g}
            ${this.componentFeatures?.allowsFeature(_.CloudFiles) ? h`<wy-dropdown-item @click=${() => this.openCloudFiles()} title=${x("From cloud")}>
                  <wy-icon name="cloud"></wy-icon>
                  <span>${x("From cloud")}</span>
                </wy-dropdown-item>` : g}
            ${this.componentFeatures?.allowsAnyFeature(_.Meetings, _.ZoomMeetings) ? h`
                  <wy-dropdown-item @click=${() => this.handleMeetingClick("zoom")} title=${x("Zoom meeting")}>
                    <wy-icon svg="zoom-meetings"></wy-icon>
                    <span>${x("Zoom meeting")}</span>
                  </wy-dropdown-item>
                ` : g}
            ${this.componentFeatures?.allowsAnyFeature(_.Meetings, _.GoogleMeet) ? h`
                  <wy-dropdown-item @click=${() => this.handleMeetingClick("google")} title=${x("Google Meet")}>
                    <wy-icon svg="google-meet"></wy-icon>
                    <span>${x("Google Meet")}</span>
                  </wy-dropdown-item>
                ` : g}
            ${this.componentFeatures?.allowsAnyFeature(_.Meetings, _.MicrosoftTeams) ? h`
                  <wy-dropdown-item
                    @click=${() => this.handleMeetingClick("microsoft")}
                    title=${x("Microsoft Teams")}
                  >
                    <wy-icon svg="microsoft-teams"></wy-icon>
                    <span>${x("Microsoft Teams")}</span>
                  </wy-dropdown-item>
                ` : g}
            ${this.componentFeatures?.allowsFeature(_.Polls) ? h`<wy-dropdown-item @click=${() => this.togglePolls()} title=${x("Poll")}>
                  <wy-icon name="poll"></wy-icon>
                  <span>${x("Poll")}</span>
                </wy-dropdown-item>` : g}
          </wy-dropdown>` : g}

      <!-- Input -->

      ${this.renderEditor()}

      <!-- Button -->
      <wy-button kind="icon" @click="${() => this.submit()}" title=${this.buttonText} ?disabled=${this.disabled}>
        <wy-icon name="send"></wy-icon>
      </wy-button>
    </div>`;
  }
  /**
   * Render content that appears below the message editor.
   *
   * By default returns the same lists section used by the base editor.
   *
   * @internal
   */
  renderBottomSlot() {
    return [this.renderLists()];
  }
}, n(Ko, "WyEditorComment"), Ko);
du.styles = [...Tt.styles, PC];
du = e$([
  U("wy-editor-comment"),
  ut()
], du);
var i$ = Object.defineProperty, r$ = Object.getOwnPropertyDescriptor, $g = /* @__PURE__ */ n((s) => {
  throw TypeError(s);
}, "__typeError$d"), xl = /* @__PURE__ */ n((s, t, e, i) => {
  for (var r = i > 1 ? void 0 : i ? r$(t, e) : t, a = s.length - 1, o; a >= 0; a--)
    (o = s[a]) && (r = (i ? o(t, e, r) : o(r)) || r);
  return i && r && i$(t, e, r), r;
}, "__decorateClass$K"), kg = /* @__PURE__ */ n((s, t, e) => t.has(s) || $g("Cannot " + e), "__accessCheck$d"), $h = /* @__PURE__ */ n((s, t, e) => (kg(s, t, "read from private field"), e ? e.call(s) : t.get(s)), "__privateGet$d"), ty = /* @__PURE__ */ n((s, t, e) => t.has(s) ? $g("Cannot add the same private member more than once") : t instanceof WeakSet ? t.add(s) : t.set(s, e), "__privateAdd$d"), ey = /* @__PURE__ */ n((s, t, e, i) => (kg(s, t, "write to private field"), t.set(s, e), e), "__privateSet$9"), Oh, nw, Sn, Qo;
let ur = (Qo = class extends qt {
  constructor() {
    super(...arguments), this.exportParts = new W(this), this.location = "apps", this.reveal = !1, this.padded = !1, ty(this, Oh), ty(this, nw, new Promise((t) => {
      ey(this, Oh, t);
    })), this.commentsQuery = new ar(this), this.addCommentMutation = new Ue(this), this.infiniteScroll = new or(this), this.pagerRef = J(), ty(this, Sn), this.handleRealtimeCommentCreated = () => {
      this.weavy?.queryClient.invalidateQueries({ queryKey: [this.location, "comments", this.parentId] });
    }, this.handleRealtimeReactionAdded = (t) => {
      !this.weavy || t.entity.type !== It.Comment || Ee(
        this.weavy.queryClient,
        [this.location, "comments", this.parentId],
        t.entity.id,
        (e) => sl(e, t.reaction, t.actor)
      );
    }, this.handleRealtimeReactionDeleted = (t) => {
      !this.weavy || t.entity.type !== It.Comment || Ee(
        this.weavy.queryClient,
        [this.location, "comments", this.parentId],
        t.entity.id,
        (e) => sl(e, void 0, t.actor)
      );
    };
  }
  /**
   * Resolves when `parentId` is available.
   *
   * @returns Promise<number>
   */
  async whenParentId() {
    return await $h(this, nw);
  }
  async willUpdate(t) {
    var e, i;
    if (super.willUpdate(t), t.has("parentId") && this.parentId && ((e = $h(this, Oh)) == null || e.call(this, this.parentId)), (t.has("parentId") || t.has("weavy") || t.has("componentFeatures")) && this.parentId && this.weavy && (await this.commentsQuery.trackInfiniteQuery(fC(this.weavy, this.location, this.parentId)), await this.addCommentMutation.trackMutation(vC(this.weavy)), this.removeCommentMutation = bC(this.weavy, this.location, this.parentId), this.restoreCommentMutation = CC(this.weavy, this.location, this.parentId)), (t.has("weavy") || t.has("app") || t.has("componentFeatures") || t.has("parentId")) && this.weavy && this.app && this.parentId) {
      this.pollMutation = Tw(this.weavy, this.app.id, [this.location, "comments", this.parentId]), (i = $h(this, Sn)) == null || i.call(this);
      const r = `a${this.app.id}`;
      this.weavy.subscribe(r, "comment_created", this.handleRealtimeCommentCreated), this.componentFeatures?.allowsFeature(_.Reactions) && (this.weavy.subscribe(r, "reaction_added", this.handleRealtimeReactionAdded), this.weavy.subscribe(r, "reaction_removed", this.handleRealtimeReactionDeleted)), ey(this, Sn, () => {
        this.weavy?.unsubscribe(r, "comment_created", this.handleRealtimeCommentCreated), this.weavy?.unsubscribe(r, "reaction_added", this.handleRealtimeReactionAdded), this.weavy?.unsubscribe(r, "reaction_removed", this.handleRealtimeReactionDeleted), ey(this, Sn, void 0);
      });
    }
  }
  update(t) {
    super.update(t), this.infiniteScroll.observe(this.commentsQuery.result, this.pagerRef.value);
  }
  /**
   * Handle submit from the comment editor and trigger add comment mutation.
   *
   * @internal
   */
  async handleSubmit(t) {
    this.app && this.parentId && this.user && await this.addCommentMutation.mutate({
      app_id: this.app.id,
      parent_id: this.parentId,
      type: this.location,
      text: t.detail.text,
      meeting_id: t.detail.meetingId,
      blobs: t.detail.blobs,
      poll_options: t.detail.pollOptions,
      embed_id: t.detail.embedId,
      context: t.detail.contextData,
      user: this.user
    });
  }
  /**
   * Render comment items from a flattened page array.
   *
   * @internal
   */
  renderComments(t) {
    return t ? Be(
      t,
      (e) => e.id,
      (e) => this.parentId ? h`<wy-comment
                id="comment-${e.id}"
                ?reveal=${this.reveal}
                .parentId=${this.parentId}
                .location=${this.location}
                .comment=${e}
                @trash=${async (i) => {
        const r = await this.whenApp(), a = await this.whenParentId();
        this.removeCommentMutation?.mutate({
          id: i.detail.id,
          appId: r.id,
          parentId: a,
          type: this.location
        });
      }}
                @restore=${async (i) => {
        const r = await this.whenApp(), a = await this.whenParentId();
        this.restoreCommentMutation?.mutate({
          id: i.detail.id,
          appId: r.id,
          parentId: a,
          type: this.location
        });
      }}
                @vote=${(i) => {
        i.detail.parentId && i.detail.parentType && (console.log("Voting on option", i.detail.optionId, "for comment", i.detail.parentId), this.pollMutation?.mutate({
          optionId: i.detail.optionId,
          parentType: i.detail.parentType,
          parentId: i.detail.parentId
        }));
      }}
              ></wy-comment>` : g
    ) : g;
  }
  render() {
    const { data: t, hasNextPage: e, isPending: i } = this.commentsQuery.result ?? {}, r = pi(t);
    return h`
      ${r && r.length ? h`
            <div part="wy-comments">
              ${this.renderComments(r)}
              ${e ? h`<div ${K(this.pagerRef)} part="wy-pager wy-pager-bottom"></div>` : g}
            </div>
          ` : h`
            <wy-empty noNetwork
              ><wy-progress-circular indeterminate padded reveal ?hidden=${!i}></wy-progress-circular
            ></wy-empty>
          `}

      <wy-editor-comment
        editorLocation=${this.location}
        .parentId=${this.parentId}
        .typing=${!1}
        .draft=${!0}
        ?disabled=${!Zc(Vs.Create, this.app?.permissions)}
        placeholder=${this.placeholder ?? x("Create a comment...")}
        buttonText=${x("Comment", { desc: "Button action to comment" })}
        @submit=${(a) => this.handleSubmit(a)}
      ></wy-editor-comment>
    `;
  }
  disconnectedCallback() {
    var t;
    (t = $h(this, Sn)) == null || t.call(this), super.disconnectedCallback();
  }
}, n(Qo, "WyCommentList"), Qo);
Oh = /* @__PURE__ */ new WeakMap();
nw = /* @__PURE__ */ new WeakMap();
Sn = /* @__PURE__ */ new WeakMap();
ur.styles = [jw, kr];
xl([
  y({ type: Number })
], ur.prototype, "parentId", 2);
xl([
  y({ attribute: !1 })
], ur.prototype, "location", 2);
xl([
  y()
], ur.prototype, "placeholder", 2);
xl([
  y({ type: Boolean, reflect: !0 })
], ur.prototype, "reveal", 2);
xl([
  y({ type: Boolean, reflect: !0 })
], ur.prototype, "padded", 2);
ur = xl([
  U("wy-comment-list"),
  ut()
], ur);
function Qw(s, t) {
  return ["apps", s.id, "file", t.id, "versions"];
}
n(Qw, "getFileVersionsKey");
function s$(s, t, e) {
  const i = s.queryClient, r = ["apps", t.id, "files"], a = Qw(t, e), o = {
    mutationKey: r,
    mutationFn: /* @__PURE__ */ n(async ({ versionFile: c }) => {
      if (c.id >= 1 && c.rev) {
        if (!(await s.fetch(`/api/files/${c.id}/versions/${c.rev}/restore`, {
          method: "POST"
        })).ok)
          throw new Error();
      } else
        throw new Error(`Could not restore ${c.name} to version ${c.rev}.`);
    }, "mutationFn"),
    onMutate: /* @__PURE__ */ n((c) => (ht(
      i,
      { queryKey: o.mutationKey, exact: !1 },
      c.versionFile.id,
      (l) => Object.assign(l, c.versionFile, { status: "pending" })
    ), { type: "version", file: e, status: { state: "pending" } }), "onMutate"),
    onSuccess: /* @__PURE__ */ n((c, l) => {
      ht(
        i,
        { queryKey: o.mutationKey, exact: !1 },
        l.versionFile.id,
        (u) => Object.assign(u, c, { status: "ok" })
      ), Bt(i, o.mutationKey, l, (u) => {
        u.status.state = "ok";
      });
    }, "onSuccess"),
    onError: /* @__PURE__ */ n((c, l, u) => {
      u?.file && ht(
        i,
        { queryKey: o.mutationKey, exact: !1 },
        l.versionFile.id,
        (b) => Object.assign(b, u.file, { status: "error" })
      ), Bt(i, o.mutationKey, l, (b) => {
        b.status.state = "error", b.status.text = c.message;
      });
    }, "onError"),
    onSettled: /* @__PURE__ */ n(async () => {
      await i.invalidateQueries({ queryKey: a });
    }, "onSettled")
  };
  return o;
}
n(s$, "getFileVersionRestoreMutationOptions");
function n$(s, t, e) {
  return new St(s.queryClient, s$(s, t, e));
}
n(n$, "getFileVersionRestoreMutation");
function a$(s, t, e) {
  const i = s.queryClient, r = Qw(t, e);
  return {
    mutationKey: r,
    mutationFn: /* @__PURE__ */ n(async ({ versionFile: o }) => {
      if (o.id >= 1 && o.rev) {
        const c = await s.fetch(`/api/files/${o.id}/versions/${o.rev}`, {
          method: "DELETE"
        });
        if (!c.ok) {
          const l = await c.json();
          throw new Error(l.detail || l.title, { cause: l });
        }
      } else {
        const c = {
          status: 400,
          title: `Could not remove ${o.name} version ${o.rev}.`
        };
        throw new Error(c.detail || c.title, { cause: c });
      }
    }, "mutationFn"),
    onMutate: /* @__PURE__ */ n((o) => {
      Ee(
        i,
        r,
        /* @__PURE__ */ n((l) => l.id === o.versionFile.id && l.rev === o.versionFile.rev, "versionPredicate"),
        (l) => Object.assign(l, { status: "pending" })
      );
    }, "onMutate"),
    onSuccess: /* @__PURE__ */ n((o, c) => {
      Pu(i, r, /* @__PURE__ */ n((u) => u.id === c.versionFile.id && u.rev === c.versionFile.rev, "versionPredicate"));
    }, "onSuccess"),
    onError: /* @__PURE__ */ n((o, c) => {
      Ee(
        i,
        r,
        /* @__PURE__ */ n((u) => u.id === c.versionFile.id && u.rev === c.versionFile.rev, "versionPredicate"),
        (u) => Object.assign(u, { status: void 0 })
      );
    }, "onError")
  };
}
n(a$, "getFileVersionDeleteMutationOptions");
function o$(s, t, e) {
  return new St(s.queryClient, a$(s, t, e));
}
n(o$, "getFileVersionDeleteMutation");
var c$ = Object.defineProperty, l$ = Object.getOwnPropertyDescriptor, Gw = /* @__PURE__ */ n((s, t, e, i) => {
  for (var r = i > 1 ? void 0 : i ? l$(t, e) : t, a = s.length - 1, o; a >= 0; a--)
    (o = s[a]) && (r = (i ? o(t, e, r) : o(r)) || r);
  return i && r && c$(t, e, r), r;
}, "__decorateClass$J"), Go;
let ud = (Go = class extends qt {
  constructor() {
    super(...arguments), this.exportParts = new W(this), this.fileVersionsQuery = new ve(this);
  }
  /**
   * Selects the provided version and emits `file-version-select`.
   *
   * @param versionFile - Version to select.
   */
  selectVersion(t) {
    this.activeVersion = t, this.dispatchFileVersionSelect(t);
  }
  /**
   * Emit a `file-version-select` event with the chosen version.
   *
   * @internal
   * @param versionFile - Version to announce.
   * @returns Whether the event was not canceled.
   */
  dispatchFileVersionSelect(t) {
    const e = new CustomEvent("file-version-select", {
      detail: { versionFile: t }
    });
    return this.dispatchEvent(e);
  }
  /**
   * Revert the file to the supplied version.
   *
   * @internal
   * @param versionFile - Version to restore.
   */
  handleRevert(t) {
    this.fileVersionRestoreMutation?.mutate({ versionFile: t }), this.selectVersion(t);
  }
  /**
   * Remove the supplied version and adjust the active selection if needed.
   *
   * @internal
   * @param versionFile - Version to delete.
   */
  handleRemove(t) {
    this.fileVersionDeleteMutation?.mutate({ versionFile: t }), this.activeVersion === t && (this.activeVersion = this.file);
  }
  /**
   * Download the provided version via the browser.
   *
   * @internal
   * @param file - Version file to download.
   */
  triggerDownload(t) {
    rr(t.download_url, "_top", t.name, !0);
  }
  async willUpdate(t) {
    super.willUpdate(t), (t.has("weavy") || t.has("file") || t.has("app")) && this.weavy && this.file && this.app && (await this.fileVersionsQuery.trackQuery(
      nr(
        this.weavy,
        Qw(this.app, this.file),
        `/api/files/${this.file.id}/versions`
      )
    ), this.fileVersionRestoreMutation = n$(this.weavy, this.app, this.file), this.fileVersionDeleteMutation = o$(this.weavy, this.app, this.file));
  }
  render() {
    const { data: t, isPending: e } = this.fileVersionsQuery.result ?? { isPending: !0 };
    return e ? h`<wy-progress-circular indeterminate overlay></wy-progress-circular>` : t?.data ? h`
          <wy-item-list part="wy-versions">
            ${Be(
      t.data,
      (i) => i.id,
      (i, r) => {
        const a = cs(i.name || "").icon, o = t.data ? t.data.length - r : NaN, c = ai(i.name), l = new Date(i.updated_at || i.created_at), u = !!this.file.external_url, b = new Intl.DateTimeFormat(this.weavy?.locale, {
          dateStyle: "full",
          timeStyle: "short"
        }).format(l), C = bl(this.weavy?.locale, new Date(l));
        return u ? h`
                      <wy-empty noNetwork>
                        <span slot="title">${x("Versions are not available for external cloud files.")}</span>
                      </wy-empty>
                    ` : h`
                      <wy-item
                        size="lg"
                        interactive
                        ?selected=${i.rev == this.activeVersion?.rev}
                        tabindex="0"
                        @click=${() => this.selectVersion(i)}
                        @keydown=${ne}
                        @keyup=${ke}
                      >
                        <wy-icon
                          slot="image"
                          name=${a}
                          size="48"
                          kind=${i.kind}
                          ext=${c}
                        ></wy-icon>
                        <span slot="title">${o}. ${i.name}</span>
                        <span slot="text">
                          <time datetime=${i.updated_at || i.created_at} title=${b}
                            >${C}</time
                          >
                          ${i.updated_by ? h`· ${i.updated_by?.name}` : g}
                        </span>

                        <wy-dropdown slot="actions" directionX="left">
                          <wy-dropdown-item @click=${() => this.triggerDownload(i)}>
                            <wy-icon name="download"></wy-icon>
                            ${x("Download")}
                          </wy-dropdown-item>

                          ${r !== 0 ? h`
                                <wy-dropdown-divider></wy-dropdown-divider>
                                <wy-dropdown-item @click=${() => this.handleRevert(i)}>
                                  <wy-icon name="restore"></wy-icon>
                                  ${"Revert"}
                                </wy-dropdown-item>
                                <wy-dropdown-item @click=${() => this.handleRemove(i)}>
                                  <wy-icon name="delete"></wy-icon>
                                  ${x("Remove", { desc: "Button action to remove" })}
                                </wy-dropdown-item>
                              ` : g}
                        </wy-dropdown>
                      </wy-item>
                    `;
      }
    )}
          </wy-item-list>
        ` : g;
  }
}, n(Go, "WyFileVersions"), Go);
Gw([
  y({ attribute: !1 })
], ud.prototype, "file", 2);
Gw([
  y({ attribute: !1 })
], ud.prototype, "activeVersion", 2);
ud = Gw([
  U("wy-file-versions"),
  ut()
], ud);
function d$(s, t) {
  const e = s.queryClient;
  return {
    mutationKey: ["users", t, "follow"],
    mutationFn: /* @__PURE__ */ n(async ({ follow: r }) => {
      if (t) {
        const a = await s.fetch(`/api/users/${t}/${r ? "follow" : "unfollow"}`, {
          method: "POST"
        });
        if (!a.ok)
          throw await a.json();
      } else
        throw new Error(`Could not ${r ? "follow" : "unfollow"} member ${t}.`);
    }, "mutationFn"),
    onMutate: /* @__PURE__ */ n(({ follow: r }) => {
      let a;
      return e.setQueryData(["users", t], (c) => ({
        ...c,
        is_followed: r
      })), {
        previousFollow: a,
        follow: r
      };
    }, "onMutate"),
    onError: /* @__PURE__ */ n(async () => {
      await e.invalidateQueries({ queryKey: ["users"], exact: !1 });
    }, "onError")
  };
}
n(d$, "getUserFollowMutationOptions");
function h$(s) {
  return s instanceof CustomEvent && typeof s.detail.action == "string";
}
n(h$, "isActionEvent");
const u$ = j`:host{--_track-height: 4px;--_track-shape: 2px;--_active-indicator-height: 2px;--_active-indicator-color: var(--wy-primary, var(--wy-primary-light, #2f628c));--_warning-color: var(--wy-warning, var(--wy-warning-light, #adb140));--_error-color: var(--wy-error, var(--wy-error-light, #ba1821));--_track-color: var(--wy-surface-variant, var(--wy-surface-variant-light, #dee3eb));display:contents;position:relative}[part~=wy-progress-linear]{border-radius:var(--_track-shape);position:relative;min-width:80px;height:var(--_track-height);content-visibility:auto;contain:strict;flex:1 0 auto;direction:ltr;inset:0;border-radius:inherit;overflow:hidden;display:flex;align-items:center}[part~=wy-progress-linear][part~=wy-progress-padded]{margin:calc(1 * var(--wy-size, 1rem)) calc(2 * var(--wy-size, 1rem))}[part~=wy-inactive-track],[part~=wy-bar],[part~=wy-bar-inner]{position:absolute}[part~=wy-bar]{animation:none;width:100%;height:var(--_active-indicator-height);transform-origin:left center;transition:transform .25s cubic-bezier(.4,0,.6,1)}[part~=wy-secondary-bar]{display:none}[part~=wy-bar-inner]{inset:0;animation:none;background:var(--_active-indicator-color)}[part~=wy-inactive-track]{background:var(--_track-color);inset:0;transition:transform .25s cubic-bezier(.4,0,.6,1);transform-origin:left center}[part~=wy-progress-indeterminate] [part~=wy-bar]{transition:none}[part~=wy-progress-indeterminate] [part~=wy-primary-bar]{inset-inline-start:-145.167%}[part~=wy-progress-indeterminate] [part~=wy-secondary-bar]{inset-inline-start:-54.8889%;display:block}[part~=wy-progress-indeterminate] [part~=wy-primary-bar]{animation:linear infinite 2s;animation-name:primary-indeterminate-translate}[part~=wy-progress-indeterminate] [part~=wy-primary-bar]>[part~=wy-bar-inner]{animation:linear infinite 2s primary-indeterminate-scale}[part~=wy-progress-indeterminate] [part~=wy-secondary-bar]{animation:linear infinite 2s;animation-name:secondary-indeterminate-translate}[part~=wy-progress-indeterminate] [part~=wy-secondary-bar]>[part~=wy-bar-inner]{animation:linear infinite 2s secondary-indeterminate-scale}@keyframes primary-indeterminate-scale{0%{transform:scaleX(.08)}36.65%{animation-timing-function:cubic-bezier(.334731,.12482,.785844,1);transform:scaleX(.08)}69.15%{animation-timing-function:cubic-bezier(.06,.11,.6,1);transform:scaleX(.661479)}to{transform:scaleX(.08)}}@keyframes secondary-indeterminate-scale{0%{animation-timing-function:cubic-bezier(.205028,.057051,.57661,.453971);transform:scaleX(.08)}19.15%{animation-timing-function:cubic-bezier(.152313,.196432,.648374,1.00432);transform:scaleX(.457104)}44.15%{animation-timing-function:cubic-bezier(.257759,-.003163,.211762,1.38179);transform:scaleX(.72796)}to{transform:scaleX(.08)}}@keyframes primary-indeterminate-translate{0%{transform:translate(0)}20%{animation-timing-function:cubic-bezier(.5,0,.701732,.495819);transform:translate(0)}59.15%{animation-timing-function:cubic-bezier(.302435,.381352,.55,.956352);transform:translate(83.6714%)}to{transform:translate(200.611%)}}@keyframes secondary-indeterminate-translate{0%{animation-timing-function:cubic-bezier(.15,0,.515058,.409685);transform:translate(0)}25%{animation-timing-function:cubic-bezier(.31033,.284058,.8,.733712);transform:translate(37.6519%)}48.35%{animation-timing-function:cubic-bezier(.4,.627035,.6,.902026);transform:translate(84.3862%)}to{transform:translate(160.278%)}}[part~=wy-progress-warning] [part~=wy-primary-bar],[part~=wy-progress-warning] [part~=wy-secondary-bar],[part~=wy-progress-warning] [part~=wy-bar-inner],[part~=wy-progress-error] [part~=wy-primary-bar],[part~=wy-progress-error] [part~=wy-secondary-bar],[part~=wy-progress-error] [part~=wy-bar-inner]{animation:none!important}[part~=wy-progress-warning] [part~=wy-secondary-bar],[part~=wy-progress-error] [part~=wy-secondary-bar]{display:none!important}[part~=wy-progress-warning] [part~=wy-primary-bar],[part~=wy-progress-error] [part~=wy-primary-bar]{transform:scaleX(1)!important}[part~=wy-progress-warning] [part~=wy-bar-inner]{background:var(--_warning-color)}[part~=wy-progress-error] [part~=wy-bar-inner]{background:var(--_error-color)}`;
var p$ = Object.defineProperty, y$ = Object.getOwnPropertyDescriptor, Sr = /* @__PURE__ */ n((s, t, e, i) => {
  for (var r = i > 1 ? void 0 : i ? y$(t, e) : t, a = s.length - 1, o; a >= 0; a--)
    (o = s[a]) && (r = (i ? o(t, e, r) : o(r)) || r);
  return i && r && p$(t, e, r), r;
}, "__decorateClass$I"), Zo;
let ni = (Zo = class extends nt {
  constructor() {
    super(...arguments), this.shadowParts = new W(this), this.padded = !1, this.overlay = !1, this.reveal = !1, this.value = 0, this.max = 1, this.indeterminate = !1, this.warning = !1, this.error = !1;
  }
  render() {
    let t;
    try {
      Number.isFinite(this.value) && Number.isFinite(this.max) && this.max > 0 && (t = this.value / this.max);
    } catch (a) {
      console.error(a);
    }
    const e = this.indeterminate || t === void 0, i = {
      transform: `scaleX(${(e ? 1 : this.value / this.max) * 100}%)`
    }, r = {
      "wy-progress": !0,
      "wy-progress-linear": !0,
      "wy-progress-indeterminate": e,
      "wy-progress-overlay": this.overlay,
      "wy-progress-padded": this.padded,
      "wy-progress-reveal": this.reveal,
      "wy-progress-warning": this.warning,
      "wy-progress-error": this.error
    };
    return h`
      <div
        part=${tt(r)}
        role="progressbar"
        aria-label="${g}"
        aria-valuemin="0"
        aria-valuemax=${this.max}
        aria-valuenow=${e ? g : this.value}
      >
        <div part="wy-inactive-track"></div>
        <div part="wy-bar wy-primary-bar" style=${Jl(i)}>
          <div part="wy-bar-inner"></div>
        </div>
        <div part="wy-bar wy-secondary-bar">
          <div part="wy-bar-inner"></div>
        </div>
      </div>
    `;
  }
}, n(Zo, "WyProgressLinear"), Zo);
ni.styles = [Lt, O1, u$];
Sr([
  y({ type: Boolean })
], ni.prototype, "padded", 2);
Sr([
  y({ type: Boolean })
], ni.prototype, "overlay", 2);
Sr([
  y({ type: Boolean })
], ni.prototype, "reveal", 2);
Sr([
  y({ type: Number })
], ni.prototype, "value", 2);
Sr([
  y({ type: Number })
], ni.prototype, "max", 2);
Sr([
  y({ type: Boolean })
], ni.prototype, "indeterminate", 2);
Sr([
  y({ type: Boolean })
], ni.prototype, "warning", 2);
Sr([
  y({ type: Boolean })
], ni.prototype, "error", 2);
ni = Sr([
  U("wy-progress-linear")
], ni);
var w$ = Object.defineProperty, f$ = Object.getOwnPropertyDescriptor, _g = /* @__PURE__ */ n((s) => {
  throw TypeError(s);
}, "__typeError$c"), nh = /* @__PURE__ */ n((s, t, e, i) => {
  for (var r = i > 1 ? void 0 : i ? f$(t, e) : t, a = s.length - 1, o; a >= 0; a--)
    (o = s[a]) && (r = (i ? o(t, e, r) : o(r)) || r);
  return i && r && w$(t, e, r), r;
}, "__decorateClass$H"), m$ = /* @__PURE__ */ n((s, t, e) => t.has(s) || _g("Cannot " + e), "__accessCheck$c"), kh = /* @__PURE__ */ n((s, t, e) => (m$(s, t, "read from private field"), e ? e.call(s) : t.get(s)), "__privateGet$c"), av = /* @__PURE__ */ n((s, t, e) => t.has(s) ? _g("Cannot add the same private member more than once") : t instanceof WeakSet ? t.add(s) : t.set(s, e), "__privateAdd$c"), zh, Fh, Yo;
let ts = (Yo = class extends cr {
  constructor() {
    super(...arguments), this.exportParts = new W(this), av(this, zh, new Ue(this)), av(this, Fh, new ve(this)), this.show = !1, this.handleUserAction = (t) => {
      h$(t) && !t.defaultPrevented && t.detail.user && t.detail.action === me.Preview && (t.preventDefault(), this.uid = t.detail.user.id, this.profile = t.detail.user, this.show = !0);
    };
  }
  /**
   * Follows the provided user/profile.
   * @param [follow=true] - Whether the user should be followed or not.
   */
  follow(t = !0) {
    this.user && this.uid !== this.user.id && this.uid !== this.user.uid && kh(this, zh).mutate({ follow: t });
  }
  async willUpdate(t) {
    if (super.willUpdate(t), t.has("listenTo") && this.listenTo) {
      try {
        t.get("listenTo")?.removeEventListener("wy-action", this.handleUserAction);
      } catch {
      }
      this.listenTo.addEventListener("wy-action", this.handleUserAction);
    }
    t.has("profile") && this.profile && (this.uid ??= this.profile.id), (t.has("weavy") || t.has("uid") || t.has("show")) && this.weavy && this.uid && this.show && await kh(this, Fh).trackQuery(
      nr(this.weavy, ["users", this.uid], void 0, {
        initialData: this.profile
      })
    ), (t.has("weavy") || t.has("uid")) && this.weavy && this.uid && await kh(this, zh).trackMutation(d$(this.weavy, this.uid));
  }
  render() {
    const { data: t, isPending: e } = kh(this, Fh).result;
    return h`
      <slot
        name="toggle"
        @click=${(i) => {
      this.show = !this.show;
    }}
        @keydown=${ne}
        @keyup=${ke}
      ></slot>
      ${t ? h`
            <wy-overlay type="sheet" .show=${this.show} @close=${() => this.show = !1}>
              <span slot="title">${t?.name}</span>
              ${this.componentFeatures?.allowsFeature(_.Follow) && this.user && this.user.id !== t.id && t.id > 0 ? h`
                    <wy-button
                      slot="actions"
                      kind="filled"
                      color="primary"
                      @click=${() => this.follow(!t?.is_followed)}
                      >${t?.is_followed ? x("Unfollow") : x("Follow")}</wy-button
                    >
                  ` : g}
              <wy-container scrollY padded>
                <wy-avatar-header
                  description=${t.id == 0 ? "(System user)" : t.id == -1 ? "(Deleted user)" : `${t?.nickname || t?.name}${this.profile?.uid ? ` (@${t.uid})` : ""}`}
                >
                  <wy-avatar
                    src=${ot(t?.avatar_url)}
                    name=${t.name}
                    description=${ot(t?.comment)}
                    presence=${t.presence}
                    ?isAgent=${t?.is_agent}
                    size=${96}
                  ></wy-avatar>
                </wy-avatar-header>

                ${t.email || t.phone_number ? h`
                      <table>
                        ${t.email ? h`<tr
                              ><td><strong>${x("E-mail")}</strong></td
                              ><td>${t.email}</td></tr
                            >` : g}
                        ${t.phone_number ? h`<tr
                              ><td><strong>${x("Phone")}</strong></td
                              ><td>${t.phone_number}</td></tr
                            >` : g}
                      </table>
                    ` : g}
                ${e ? h`<wy-progress-linear indeterminate reveal></wy-progress-linear>` : g}
              </wy-container>
            </wy-overlay>
          ` : g}
    `;
  }
  disconnectedCallback() {
    super.disconnectedCallback(), this.listenTo?.removeEventListener("wy-action", this.handleUserAction);
  }
}, n(Yo, "WyUserCard"), Yo);
zh = /* @__PURE__ */ new WeakMap();
Fh = /* @__PURE__ */ new WeakMap();
ts.styles = [Y];
nh([
  y({
    converter: $u
  })
], ts.prototype, "uid", 2);
nh([
  y({ attribute: !1 })
], ts.prototype, "profile", 2);
nh([
  y({ type: Boolean })
], ts.prototype, "show", 2);
nh([
  y({ attribute: !1 })
], ts.prototype, "listenTo", 2);
ts = nh([
  U("wy-user-card"),
  ut()
], ts);
var v$ = Object.defineProperty, g$ = Object.getOwnPropertyDescriptor, ye = /* @__PURE__ */ n((s, t, e, i) => {
  for (var r = i > 1 ? void 0 : i ? g$(t, e) : t, a = s.length - 1, o; a >= 0; a--)
    (o = s[a]) && (r = (i ? o(t, e, r) : o(r)) || r);
  return i && r && v$(t, e, r), r;
}, "__decorateClass$G"), Xo;
let jt = (Xo = class extends qt {
  constructor() {
    super(...arguments), this.exportParts = new W(this), this.currentId = NaN, this.isAttachment = !1, this.filled = !1, this.showOverlay = !1, this.commentsOpen = !1, this.versionsOpen = !1, this.sidePanelMaximized = !1, this.persistState = new Hs(this), this.swipeScroller = new Wy(this), this.previewFileRef = J(), this.nextRef = J(), this.prevRef = J(), this.loadingQueue = [], this.disableSwipeScroll = !1, this.handleKeys = (t) => {
      this.showOverlay && (t.key === "ArrowLeft" ? (t.stopPropagation(), this.setPrev()) : t.key === "ArrowRight" && (t.stopPropagation(), this.setNext()));
    };
  }
  /**
   * Moves a file to the front of the loading queue.
   *
   * @internal
   * @param file - File to prioritize.
   * @param state - Optional state override.
   */
  moveFirstInQueue(t, e) {
    let i = { file: t };
    const r = this.loadingQueue.filter((a) => a.file === t ? (i = a, !1) : !0);
    this.loadingQueue = [{ ...i, ...e }, ...r];
  }
  /**
   * Updates loading metadata for the supplied file.
   *
   * @internal
   * @param file - File whose state should change.
   * @param state - State patch to merge.
   */
  updateLoadingState(t, e) {
    t && (this.loadingQueue = this.loadingQueue.map((i) => t === i.file ? { ...i, ...e } : i));
  }
  /**
   * Marks the next unloaded item as loading.
   *
   * @internal
   */
  loadNextInQueue() {
    const t = this.loadingQueue.find((e) => !e.loaded);
    t && !t?.loading && this.updateLoadingState(t.file, { loading: !0 });
  }
  /**
   * Emits `wy-preview-open` with the current preview context.
   *
   * @internal
   */
  async dispatchOpen() {
    const t = await this.whenApp(), e = await this.whenComponentFeatures(), i = this.currentId, r = this.commentsOpen ? "comments" : this.versionsOpen ? "versions" : void 0, a = this.currentFile ? [this.currentFile] : [], o = this.isAttachment, c = this.contextDataBlobs, l = new CustomEvent("wy-preview-open", {
      detail: {
        fileId: i,
        tab: r,
        files: a,
        app: t,
        features: e.allowedFeatures().join(" "),
        isAttachment: o,
        contextDataBlobs: c
      },
      cancelable: !0,
      bubbles: !1,
      composed: !0
    });
    this.dispatchEvent(l) || (this.showOverlay = !1, this.commentsOpen = !1, this.versionsOpen = !1);
  }
  /**
   * Emits the `wy-preview-close` event.
   *
   * @internal
   */
  dispatchClose() {
    const t = new CustomEvent("wy-preview-close", {
      cancelable: !1,
      bubbles: !1,
      composed: !0
    });
    this.dispatchEvent(t);
  }
  /**
   * Opens the overlay for the provided file identifier.
   *
   * @param fileId - File to display.
   * @param showTab - Optional sidebar tab to activate.
   */
  async open(t, e) {
    await this.whenApp(), await this.updateComplete, t && (this.currentId = t), e && this.toggleSidebarTab(e, !0), this.showOverlay = !0;
  }
  /**
   * Closes the preview overlay.
   */
  close() {
    this.showOverlay = !1;
  }
  /**
   * Toggles sidebar visibility for comments or versions.
   *
   * @internal
   * @param tab - Sidebar to affect.
   * @param state - Forced open state; toggles when omitted.
   */
  toggleSidebarTab(t, e) {
    t === "comments" ? (this.versionsOpen = !1, this.commentsOpen = e !== void 0 ? e : !this.commentsOpen) : t === "versions" ? (this.commentsOpen = !1, this.versionsOpen = e !== void 0 ? e : !this.versionsOpen) : (this.commentsOpen = !1, this.versionsOpen = !1);
  }
  /**
   * Handles file version selection events.
   *
   * @internal
   * @param e - Version selection event.
   */
  handleVersionFile(t) {
    this.currentVersionFile = t.detail.versionFile;
  }
  /**
   * Scrolls to the previous preview area.
   *
   * @internal
   */
  scrollToPrev() {
    this.prevRef.value && (this.unblockSwipeScroll(), this.prevRef.value.scrollIntoView({ behavior: this.weavy?.scrollBehavior }));
  }
  /**
   * Scrolls to the next preview area.
   *
   * @internal
   */
  scrollToNext() {
    this.nextRef.value && (this.unblockSwipeScroll(), this.nextRef.value.scrollIntoView({ behavior: this.weavy?.scrollBehavior }));
  }
  /**
   * Disables swipe scrolling for a short duration.
   *
   * @internal
   * @param duration - Milliseconds before re-enabling.
   */
  blockSwipeScroll(t = 20) {
    window.clearTimeout(this.disableSwipeScrollTimeout), this.disableSwipeScroll = !0, this.disableSwipeScrollTimeout = window.setTimeout(() => this.disableSwipeScroll = !1, t);
  }
  /**
   * Re-enables swipe scrolling immediately.
   *
   * @internal
   */
  unblockSwipeScroll() {
    window.clearTimeout(this.disableSwipeScrollTimeout), this.disableSwipeScroll = !1;
  }
  /**
   * Selects the previous file when available.
   *
   * @internal
   */
  setPrev() {
    this.previousFile && (this.currentId = this.previousFile.id, this.blockSwipeScroll());
  }
  /**
   * Selects the next file when available.
   *
   * @internal
   */
  setNext() {
    this.nextFile && (this.currentId = this.nextFile.id, this.blockSwipeScroll());
  }
  /**
   * Registers swipe observers on the active scroll element.
   *
   * @internal
   */
  registerSwipeScroller() {
    this.swipeScrollElement && (this.swipeScroller.whenPrev ??= () => this.setPrev(), this.swipeScroller.whenNext ??= () => this.setNext(), this.swipeScroller.createObserver(this.swipeScrollElement));
  }
  async willUpdate(t) {
    if (super.willUpdate(t), (t.has("app") || t.has("user")) && this.app && this.user && !this.isAttachment && (this.persistState.observe(
      [
        { name: "commentsOpen", override: !0 },
        { name: "versionsOpen", override: !0 }
      ],
      `a${this.app.id}-preview`,
      `u${this.user.id}`
    ), this.commentsOpen && this.versionsOpen && (this.versionsOpen = !1)), t.has("queryResult") && this.queryResult) {
      const { data: e } = this.queryResult ?? {};
      this.files = e?.filter((i) => i && !i.is_trashed);
    }
    if (t.has("infiniteQueryResult") && this.infiniteQueryResult) {
      const { data: e } = this.infiniteQueryResult ?? {};
      this.files = pi(e).filter((i) => i && !i.is_trashed);
    }
    t.has("currentFile") && this.currentFile && this.currentFile.id !== this.currentId && (this.currentId = this.currentFile.id), (t.has("files") || t.has("currentId") || t.has("showOverlay")) && this.files && this.showOverlay && (this.currentFile = void 0, this.previousFile = void 0, this.nextFile = void 0, this.currentVersionFile = void 0, this.currentVersionFile = this.currentFile = this.files.find((e, i) => this.files && e.id === this.currentId ? (i >= 1 && (this.previousFile = this.files[i - 1]), i < this.files.length - 1 && (this.nextFile = this.files[i + 1]), this.infiniteQueryResult && i >= this.files.length - 2 && this.infiniteQueryResult.hasNextPage && !this.infiniteQueryResult.isFetchingNextPage && this.infiniteQueryResult.fetchNextPage(), this.infiniteQueryResult && i <= 1 && this.infiniteQueryResult.hasPreviousPage && !this.infiniteQueryResult.isFetchingPreviousPage && this.infiniteQueryResult.fetchPreviousPage(), !0) : !1)), t.has("currentFile") && this.currentFile && !this.files && (this.files = [this.currentFile]), t.has("previousFile") && this.previousFile && this.moveFirstInQueue(this.previousFile), t.has("nextFile") && this.nextFile && this.moveFirstInQueue(this.nextFile), t.has("currentVersionFile") && this.currentVersionFile && this.moveFirstInQueue(this.currentVersionFile, { loading: !0 }), t.has("loadingQueue") && this.loadNextInQueue(), t.has("showOverlay") && (this.showOverlay ? await this.dispatchOpen() : t.get("showOverlay") && this.dispatchClose());
  }
  /**
   * Renders the preview header for the active file.
   *
   * @internal
   * @param activeFile - File displayed in the main pane.
   */
  renderHeader(t) {
    return h`
      <wy-titlebar header ?trashed=${!!t?.is_trashed}>
        <wy-button slot="icon" kind="icon" @click=${() => this.close()}><wy-icon name="close"></wy-icon></wy-button>
        ${t ? h` <span slot="title">${t.name}</span> ` : g}
        ${t ? h`
              ${this.componentFeatures?.allowsFeature(_.Comments) && t.id >= 1 && !this.isAttachment ? h`
                    <wy-button
                      slot="actions"
                      kind="icon"
                      ?active=${this.commentsOpen}
                      @click=${() => this.toggleSidebarTab("comments")}
                      title=${x("Comments")}
                    >
                      <wy-icon-stack>
                        ${t.comments?.count && t.comments?.count > 0 ? h`<wy-icon name="comment" state ?active=${!this.commentsOpen}></wy-icon>
                              <wy-icon name="comment" layer state ?active=${this.commentsOpen}></wy-icon>` : h`<wy-icon name="comment-outline" state ?active=${!this.commentsOpen}></wy-icon>
                              <wy-icon name="comment" layer state ?active=${this.commentsOpen}></wy-icon>`}
                      </wy-icon-stack>
                    </wy-button>
                  ` : g}
              <wy-file-menu slot="actions" .file=${t}>
                ${this.componentFeatures?.allowsFeature(_.Versions) && t.id >= 1 && !this.isAttachment ? h`
                      <wy-dropdown-item ?active=${this.versionsOpen} @click=${() => this.toggleSidebarTab("versions")}>
                        <wy-icon name="backup-restore"></wy-icon>
                        ${x("Versions")}
                      </wy-dropdown-item>
                    ` : g}
              </wy-file-menu>
            ` : g}
      </wy-titlebar>
    `;
  }
  render() {
    let t = this.files === void 0;
    this.queryResult ? t = this.queryResult.isPending : this.infiniteQueryResult && (t = this.infiniteQueryResult.isPending);
    const e = [this.previousFile, this.currentVersionFile, this.nextFile].filter(cn);
    this.commentsOpen && this.versionsOpen && (this.versionsOpen = !1);
    const i = {
      "wy-preview-swiper-disabled": this.disableSwipeScroll
    };
    return this.weavy ? h`
      <wy-overlay
        part="wy-dark"
        maximized
        noHeader
        type=${this.filled ? "full" : "modal"}
        .show=${this.showOverlay}
        @keyup=${this.handleKeys}
        @close=${() => {
      this.showOverlay = !1;
    }}
      >
        ${this.showOverlay ? h`<div part="wy-preview-layout">
              ${this.renderHeader(this.currentFile)}

              <div part="wy-preview-main">
                ${this.isAttachment ? g : h` <aside
                        id="tab-comments"
                        part="wy-sidebar ${tt({
      "wy-active": this.commentsOpen,
      "wy-maximized": this.sidePanelMaximized
    })}"
                        ?hidden=${!this.commentsOpen}
                      >
                        <nav>
                          <wy-item size="md">
                            <span slot="title" part="wy-title">${x("Comments")}</span>
                            <wy-button
                              slot="actions"
                              kind="icon"
                              @click=${() => this.toggleSidebarTab("comments", !1)}
                            >
                              <wy-icon name="close"></wy-icon>
                            </wy-button>
                          </wy-item>
                          <button
                            @click=${() => this.sidePanelMaximized = !this.sidePanelMaximized}
                            part="wy-sidebar-handle"
                            title=${this.sidePanelMaximized ? x("Restore side panel") : x("Maximize side panel")}
                          ></button>
                        </nav>
                        <div part="wy-pane wy-scroll-y">
                          ${this.commentsOpen && this.currentFile && this.currentFile.id >= 1 && this.app && this.user ? h`
                                <wy-comment-list
                                  reveal
                                  .parentId=${this.currentFile.id}
                                  .location=${"files"}
                                ></wy-comment-list>
                              ` : g}
                        </div>
                      </aside>
                      <aside
                        id="tab-versions"
                        part="wy-sidebar ${tt({
      "wy-active": this.versionsOpen,
      "wy-maximized": this.sidePanelMaximized
    })}"
                        ?hidden=${!this.versionsOpen}
                      >
                        <nav>
                          <wy-item size="md">
                            <span slot="title" part="wy-title">${x("Versions")}</span>
                            <wy-button
                              slot="actions"
                              kind="icon"
                              @click=${() => this.toggleSidebarTab("versions", !1)}
                            >
                              <wy-icon name="close"></wy-icon>
                            </wy-button>
                          </wy-item>
                          <button
                            @click=${() => this.sidePanelMaximized = !this.sidePanelMaximized}
                            part="wy-sidebar-handle"
                            title=${this.sidePanelMaximized ? x("Restore side panel") : x("Maximize side panel")}
                          ></button>
                        </nav>
                        <div part="wy-pane wy-scroll-y">
                          <div part="wy-pane-body">
                            ${this.versionsOpen && this.currentFile && this.app ? h`
                                  <wy-file-versions
                                    .file=${this.currentFile}
                                    .activeVersion=${this.currentVersionFile}
                                    @file-version-select=${(r) => this.handleVersionFile(r)}
                                  ></wy-file-versions>
                                ` : g}
                          </div>
                        </div>
                      </aside>`}

                <div part="wy-preview">
                  <div
                    ${K((r) => {
      r && (this.swipeScrollElement = r);
    })}
                    part="wy-preview-swiper ${tt(i)}"
                  >
                    ${Be(
      e,
      (r) => "preview-area-" + r?.id,
      (r) => {
        const a = /* @__PURE__ */ n((l) => {
          l?.scrollIntoView(), requestAnimationFrame(() => l?.scrollIntoView());
        }, "currentPreviewFileCallback"), o = r === this.currentVersionFile ? a : r === this.nextFile ? this.nextRef : r === this.previousFile ? this.prevRef : void 0, c = this.loadingQueue.find((l) => l.file === r);
        return r ? h`
                              <div
                                id="preview-${r.id}"
                                ${K(o)}
                                part="wy-preview-area wy-scroll-x-y"
                              >
                                ${!t && (c?.loading || c?.loaded) ? h`
                                      <wy-preview-item
                                        .file=${r}
                                        ?current=${r === this.currentVersionFile}
                                        @file-preview-loaded=${(l) => this.updateLoadingState(l.detail.file, { loaded: !0 })}
                                      ></wy-preview-item>
                                    ` : h` <wy-progress-circular indeterminate overlay></wy-progress-circular> `}
                              </div>
                            ` : g;
      }
    )}
                  </div>
                  ${this.currentFile ? h`
                        ${this.previousFile ? h`
                              <nav part="wy-nav-prev">
                                <wy-button kind="icon" @click=${() => this.scrollToPrev()}>
                                  <wy-icon name="previous"></wy-icon>
                                </wy-button>
                              </nav>
                            ` : g}
                        ${this.nextFile ? h`
                              <nav part="wy-nav-next">
                                <wy-button kind="icon" @click=${() => this.scrollToNext()}>
                                  <wy-icon name="next"></wy-icon>
                                </wy-button>
                              </nav>
                            ` : g}
                      ` : g}
                </div>
              </div>
            </div> ` : g}
      <wy-user-card .listenTo=${this}></wy-user-card>
      </wy-overlay>
    ` : g;
  }
  updated(t) {
    t.has("swipeScrollElement") && t.get("swipeScrollElement") !== this.swipeScrollElement && this.registerSwipeScroller(), !this.disableSwipeScroll && this.swipeScroller.swipeElement === this.swipeScrollElement ? this.swipeScroller.observe(this.prevRef.value, this.nextRef.value) : this.swipeScroller.clearObserver();
  }
}, n(Xo, "WyPreview"), Xo);
jt.styles = [N3, q3, dn, hn, fl, Kt];
ye([
  y({ attribute: !1 })
], jt.prototype, "files", 2);
ye([
  y({ attribute: !1 })
], jt.prototype, "queryResult", 2);
ye([
  y({ attribute: !1 })
], jt.prototype, "infiniteQueryResult", 2);
ye([
  y({ type: Object })
], jt.prototype, "user", 2);
ye([
  y({ type: Number })
], jt.prototype, "currentId", 2);
ye([
  y({ type: Boolean })
], jt.prototype, "isAttachment", 2);
ye([
  y({ type: Boolean })
], jt.prototype, "filled", 2);
ye([
  V()
], jt.prototype, "currentFile", 2);
ye([
  V()
], jt.prototype, "currentVersionFile", 2);
ye([
  V()
], jt.prototype, "previousFile", 2);
ye([
  V()
], jt.prototype, "nextFile", 2);
ye([
  V()
], jt.prototype, "showOverlay", 2);
ye([
  V()
], jt.prototype, "commentsOpen", 2);
ye([
  V()
], jt.prototype, "versionsOpen", 2);
ye([
  V()
], jt.prototype, "sidePanelMaximized", 2);
ye([
  V()
], jt.prototype, "swipeScrollElement", 2);
ye([
  V()
], jt.prototype, "loadingQueue", 2);
ye([
  V()
], jt.prototype, "disableSwipeScroll", 2);
jt = ye([
  U("wy-preview"),
  ut()
], jt);
var b$ = Object.defineProperty, x$ = Object.getOwnPropertyDescriptor, Te = /* @__PURE__ */ n((s, t, e, i) => {
  for (var r = i > 1 ? void 0 : i ? x$(t, e) : t, a = s.length - 1, o; a >= 0; a--)
    (o = s[a]) && (r = (i ? o(t, e, r) : o(r)) || r);
  return i && r && b$(t, e, r), r;
}, "__decorateClass$F"), Jo;
let de = (Jo = class extends qt {
  constructor() {
    super(...arguments), this.exportParts = new W(this), this.isPrivateChat = !1, this.createdAt = "", this.html = "", this.text = "", this.annotations = [], this.attachments = [], this.pollOptions = [], this.reactions = [], this.seenBy = [], this.highlight = !1, this.previewAnnotationsRef = J(), this.previewAttachmentsRef = J(), this.highlightRef = J(), this.dispatchUserAction = Bw.bind(this);
  }
  /**
   * True when the message was authored by the current user.
   */
  get me() {
    return this.author.id === this.user?.id;
  }
  /**
   * Emit a `vote` event for the specified poll option.
   *
   * @internal
   * @param optionId - Identifier of the selected poll option.
   * @returns {boolean} True if the event was not canceled.
   */
  dispatchVote(t) {
    const e = new CustomEvent("vote", {
      detail: { optionId: t, parentId: this.messageId, parentType: "messages" }
    });
    return this.dispatchEvent(e);
  }
  willUpdate(t) {
    super.willUpdate(t), t.has("link") && (this.highlight = !!(this.link && Vu(this.link, It.Message, { id: this.messageId })));
  }
  render() {
    const t = this.attachments?.filter((o) => o.kind === "image" && o.thumbnail_url) || [], e = this.attachments?.filter((o) => o.kind !== "image" || !o.thumbnail_url) || [], i = this.createdAt ? new Intl.DateTimeFormat(this.weavy?.locale, { dateStyle: "full", timeStyle: "short" }).format(
      new Date(this.createdAt)
    ) : "", r = this.createdAt ? new Intl.DateTimeFormat(this.weavy?.locale, { timeStyle: "short" }).format(new Date(this.createdAt)) : "", a = !this.annotations?.length && !this.attachments?.length && !this.embed && !this.meeting && !this.pollOptions?.length && $0(this.text);
    return h`
      <div
        part=${tt({
      "wy-message": !0,
      "wy-message-me": this.me,
      "wy-message-agent": !!this.author.is_agent,
      "wy-highlight": this.highlight
    })}
        ${K(this.highlightRef)}
      >
        ${this.me ? "" : h`
              <div part="wy-message-author">
                <wy-avatar
                  .src=${this.author.avatar_url}
                  .size=${32}
                  .name=${this.author.name}
                  .description=${this.author.comment}
                  .isAgent=${this.author.is_agent}
                  @click=${() => this.dispatchUserAction(this.author)}
                  role="button"
                  title="${this.author.name} ${this.author.comment}"
                ></wy-avatar>
              </div>
            `}

        <div part="wy-message-content">
          <div part="wy-message-meta">
            ${!this.isPrivateChat && !this.me ? h`<span
                  ><wy-button kind="link" @click=${() => this.dispatchUserAction(this.author)}
                    >${this.author.name}</wy-button
                  >
                  ·
                </span> ` : ""}
            <time datetime=${this.createdAt} title=${i}>${r}</time>
          </div>

          <div part=${tt({ "wy-message-bubble": !0, "wy-message-bubble-emoji": a })}>
            ${this.messageId < 0 ? h`<wy-skeleton .text=${this.text}></wy-skeleton>` : h`
                  ${t && t.length ? h`<wy-image-grid
                        part="wy-message-bubble-section"
                        .images=${t}
                        @file-open=${(o) => {
      this.previewAttachmentsRef.value?.open(o.detail.fileId);
    }}
                      ></wy-image-grid>` : g}

                  <!-- text -->
                  ${this.html ? h`<div
                        part=${tt({
      "wy-content": !0,
      "wy-message-bubble-section": !0,
      "wy-content-emoji": a
    })}
                        @click=${(o) => {
      if (o.target instanceof HTMLElement && o.target.matches('.wy-mention, [part~="wy-mention"]')) {
        const c = o.target.dataset.eid?.startsWith("u") && parseInt(o.target.dataset.eid.substring(1));
        c && this.dispatchUserAction({ id: c, name: o.target.innerText });
      }
    }}
                        >${// eslint-disable-next-line lit-a11y/click-events-have-key-events
    Bs(this.html)}</div
                      >` : g}
                  ${this.annotations && this.annotations.length ? h`<wy-annotation-list
                        part="wy-message-bubble-section"
                        .files=${this.annotations}
                        @file-open=${(o) => {
      this.previewAnnotationsRef.value?.open(o.detail.fileId);
    }}
                      ></wy-annotation-list>` : g}
                  ${this.pollOptions && this.pollOptions.length ? h`<wy-poll
                        .pollOptions=${this.pollOptions}
                        @vote=${(o) => this.dispatchVote(o.detail.optionId)}
                      ></wy-poll>` : g}
                  ${this.componentFeatures?.allowsFeature(_.Embeds) && this.embed ? h` <wy-embed .embed=${this.embed}></wy-embed> ` : g}
                  ${e && e.length ? h`<wy-attachment-list
                        filled
                        part="wy-message-bubble-section"
                        .files=${e}
                        @file-open=${(o) => {
      this.previewAttachmentsRef.value?.open(o.detail.fileId);
    }}
                      ></wy-attachment-list>` : g}
                  ${this.meeting ? h`<wy-meeting-card .meeting=${this.meeting}></wy-meeting-card>` : g}
                  ${this.componentFeatures?.allowsFeature(_.Reactions) && this.conversation ? h`
                        ${Eh(
      `reactions-${this.conversation.id}-${this.messageId}`,
      h`<wy-reactions
                            lineBelow
                            ?lineReverse=${!this.me}
                            small
                            directionX=${this.me ? "right" : "left"}
                            .reactions=${this.reactions}
                            parentId=${this.conversation.id}
                            parentType="apps"
                            entityId=${this.messageId}
                            entityType="messages"
                          ></wy-reactions>`
    )}
                      ` : g}
                `}
          </div>
        </div>
      </div>
      ${this.componentFeatures?.allowsFeature(_.Receipts) ? h`<div part="wy-message-seenby">
            ${this.seenBy && this.seenBy.length ? h`
                  ${this.seenBy.map((o) => {
      const c = o.marked_at ? new Intl.DateTimeFormat(this.weavy?.locale, {
        dateStyle: "full",
        timeStyle: "short"
      }).format(new Date(o.marked_at)) : "";
      return h`<wy-avatar
                      title=${x(gt`Seen by ${o.name} at ${c}`)}
                      .name=${o.name}
                      .src=${o.avatar_url}
                      size=${18}
                    ></wy-avatar>`;
    })}
                ` : g}
          </div>` : g}
      ${this.annotations ? Eh(
      `annotation-preview-message-${this.messageId}`,
      h`
              <wy-preview
                ${K(this.previewAnnotationsRef)}
                .files=${this.annotations}
                .isAttachment=${!0}
              ></wy-preview>
            `
    ) : g}
      ${this.attachments ? Eh(
      `preview-message-${this.messageId}`,
      h`
              <wy-preview
                ${K(this.previewAttachmentsRef)}
                .files=${[...t, ...e]}
                .isAttachment=${!0}
              ></wy-preview>
            `
    ) : g}
    `;
  }
  updated(t) {
    t.has("highlight") && this.highlight && this.highlightRef.value?.scrollIntoView({ block: "nearest" });
  }
}, n(Jo, "WyMessage"), Jo);
de.styles = [Lt, Mu, Nw, Y];
Te([
  y({ attribute: !1, type: Object })
], de.prototype, "conversation", 2);
Te([
  y({ type: Number })
], de.prototype, "messageId", 2);
Te([
  y({ type: Object })
], de.prototype, "author", 2);
Te([
  y({ type: Boolean })
], de.prototype, "isPrivateChat", 2);
Te([
  y()
], de.prototype, "createdAt", 2);
Te([
  y()
], de.prototype, "html", 2);
Te([
  y()
], de.prototype, "text", 2);
Te([
  y({ type: Array })
], de.prototype, "annotations", 2);
Te([
  y({ type: Array })
], de.prototype, "attachments", 2);
Te([
  y({ attribute: !1 })
], de.prototype, "meeting", 2);
Te([
  y({ type: Array })
], de.prototype, "pollOptions", 2);
Te([
  y({ attribute: !1 })
], de.prototype, "embed", 2);
Te([
  y({ type: Array })
], de.prototype, "reactions", 2);
Te([
  y({ type: Array })
], de.prototype, "seenBy", 2);
Te([
  y({ type: Boolean })
], de.prototype, "highlight", 2);
de = Te([
  U("wy-message"),
  ut()
], de);
var C$ = Object.defineProperty, $$ = Object.getOwnPropertyDescriptor, Pg = /* @__PURE__ */ n((s) => {
  throw TypeError(s);
}, "__typeError$b"), oi = /* @__PURE__ */ n((s, t, e, i) => {
  for (var r = i > 1 ? void 0 : i ? $$(t, e) : t, a = s.length - 1, o; a >= 0; a--)
    (o = s[a]) && (r = (i ? o(t, e, r) : o(r)) || r);
  return i && r && C$(t, e, r), r;
}, "__decorateClass$E"), Sg = /* @__PURE__ */ n((s, t, e) => t.has(s) || Pg("Cannot " + e), "__accessCheck$b"), ov = /* @__PURE__ */ n((s, t, e) => (Sg(s, t, "read from private field"), e ? e.call(s) : t.get(s)), "__privateGet$b"), k$ = /* @__PURE__ */ n((s, t, e) => t.has(s) ? Pg("Cannot add the same private member more than once") : t instanceof WeakSet ? t.add(s) : t.set(s, e), "__privateAdd$b"), cv = /* @__PURE__ */ n((s, t, e, i) => (Sg(s, t, "write to private field"), t.set(s, e), e), "__privateSet$8"), Mn, tc;
let Oe = (tc = class extends qt {
  constructor() {
    super(...arguments), this.exportParts = new W(this), this.header = !1, this.lastReadMessagePosition = "below", this.showNewMessages = !1, this.isCreatingConversation = !1, this.showReadReceipts = !1, this.messagesQuery = new ar(this), this.membersQuery = new ve(this), this.addMessageMutation = new Ue(this), this.infiniteScroll = new Ny(this), this.pagerRef = J(), this.bottomRef = J(), this.editorRef = J(), this.shouldBeAtBottom = !0, this.isTyping = !1, this.handleRealtimeMessage = async (t) => {
      if (!this.weavy || !this.conversation || !(this.conversationId && this.conversationId > 0) || !this.user)
        return;
      await this.messagesQuery.observer?.getCurrentQuery().promise;
      const e = t.message.app.uid ?? t.message.app.id, i = ["messages", t.message.app.id];
      let r = Yd(this.weavy.queryClient, i, t.message.id);
      r || (t.message.created_by.id === this.user.id && (r = Kl(this.weavy.queryClient, i, !0), r && Ee(this.weavy.queryClient, i, r.id, () => t.message)), r || Wl(this.weavy.queryClient, i, t.message)), this.weavy.queryClient.setQueryData(
        ["apps", e],
        (a) => a && { ...a, last_message: t.message }
      ), t.actor.id !== this.user.id && (this.isAtBottom ? (this.markAsRead(t.message.id), requestAnimationFrame(() => {
        this.scrollToBottom();
      })) : (this.weavy.queryClient.setQueryData(
        ["apps", e],
        (a) => a && { ...a, is_unread: !0 }
      ), this.lastReadMessagePosition = "above", this.lastReadMessageId = t.message.id, this.showNewMessages = !0), ht(
        this.weavy.queryClient,
        { queryKey: ["members", t.message.app.id], exact: !1 },
        t.actor.id,
        (a) => ({ ...a, marked_id: t.message.id, marked_at: t.message.created_at })
      ));
    }, this.handleRealtimeReactionAdded = (t) => {
      !this.weavy || !this.user || !this.conversation || ht(
        this.weavy.queryClient,
        { queryKey: ["messages"], exact: !1 },
        t.entity.id,
        (e) => (e.reactions || (e.reactions = { count: 0 }), e.reactions.data = [
          ...(e.reactions.data || []).filter((i) => i.created_by?.id !== t.actor.id),
          { content: t.reaction, created_by: t.actor }
        ], e)
      );
    }, this.handleRealtimeReactionDeleted = (t) => {
      !this.weavy || !this.conversation || !this.user || ht(
        this.weavy.queryClient,
        { queryKey: ["messages"], exact: !1 },
        t.entity.id,
        (e) => (e.reactions && e.reactions.data && (e.reactions.data = e.reactions.data.filter((i) => i.created_by?.id !== t.actor.id)), e)
      );
    }, this.handleRealtimeMarked = (t) => {
      !this.weavy || !this.conversation || ht(
        this.weavy.queryClient,
        { queryKey: ["members", this.conversation.id] },
        t.actor.id,
        (e) => ({
          ...e,
          marked_id: t.marked_id,
          marked_at: t.marked_at
        })
      );
    }, this.markAsReadHandler = () => {
      !document.hidden && this.isAtBottom && this.markAsRead();
    }, k$(this, Mn);
  }
  isPrivateChat(t) {
    return (t ?? this.conversation)?.type === rt.PrivateChat;
  }
  isChatRoom(t) {
    return (t ?? this.conversation)?.type === rt.ChatRoom;
  }
  /**
   * Read-only helper returning whether the viewport is currently scrolled to the bottom.
   *
   * @internal
   */
  get isAtBottom() {
    return this.bottomRef.value ? R2(this.bottomRef.value) : !0;
  }
  /**
   * Scroll the conversation to the bottom.
   *
   * @param smooth - Whether to perform a smooth scroll.
   * @returns Promise<void>
   *
   * @internal
   */
  async scrollToBottom(t = !1) {
    this.bottomRef.value && await ny(this.bottomRef.value), A2(this.bottomRef.value) && this.conversationId && this.conversationId > 0 && (requestAnimationFrame(() => {
      qp(this.weavy?.queryClient, ["messages", this.conversationId], void 0, 1);
    }), await w1("bottom", this.bottomRef.value, t));
  }
  /**
   * Handle typing indicator events from child components.
   *
   * @internal
   * @param e - Typing event
   */
  handleTyping(t) {
    this.isTyping = !!t.detail.count, this.isTyping && this.isAtBottom && requestAnimationFrame(() => {
      this.scrollToBottom(!0);
    });
  }
  /**
   * Submit handler for the message editor. Adds a message and optionally creates a conversation first.
   *
   * @internal
   * @param e - Editor submit event
   * @returns Promise<MessageType>
   */
  async handleSubmit(t) {
    if (!this.user)
      throw new Error("Error submitting message. Missing user.");
    const e = {
      text: t.detail.text,
      meeting_id: t.detail.meetingId,
      poll_options: t.detail.pollOptions,
      embed_id: t.detail.embedId,
      blobs: t.detail.blobs,
      user: this.user,
      context: t.detail.contextData
    };
    if (this.agentInstructions && (e.metadata = {
      instructions: this.agentInstructions
    }), !this.conversation && this.weavy && this.createConversation && (this.isCreatingConversation = !0, await this.createConversation(e), await this.updateComplete), !this.conversation)
      throw new Error("Error submitting message. Missing conversation.");
    const i = await this.addMessageMutation.mutate({
      ...e,
      app_id: this.conversation.id
    });
    return this.showNewMessages = !1, requestAnimationFrame(() => {
      this.scrollToBottom();
    }), this.isCreatingConversation = !1, i;
  }
  async handleEditLast(t) {
    if (!this.user)
      return;
    const i = pi(this.messagesQuery.result.data).findLast((r) => this.user && r.created_by.id === this.user.id && r.text);
    if (i) {
      const r = Date.now() - new Date(i.created_at).getTime();
      r < 1e3 * 60 * 60 * 24 ? (this.editMessage = i, await this.setEditorText(i.text), await this.selectAllInEditor()) : console.warn("Last message is too old to edit.", r);
    }
  }
  /**
   * Set the editor text programmatically.
   *
   * @param text - Text to set in the editor.
   * @returns Promise<void>
   *
   * @internal
   */
  async setEditorText(t) {
    this.editorRef.value && (this.editorRef.value.text = t, await this.editorRef.value.updateComplete, await new Promise((e) => requestAnimationFrame(e)));
  }
  /**
   * Set metadata on the editor instance.
   *
   * @param metadata - Optional metadata object.
   * @returns Promise<void>
   *
   * @internal
   */
  async setEditorMetadata(t = {}) {
    await this.updateComplete, this.editorRef.value && (this.editorRef.value.metadata = t);
  }
  /**
   * Select all content in the editor.
   *
   * @internal
   */
  async selectAllInEditor() {
    this.editorRef.value && (await this.updateComplete, await this.editorRef.value.updateComplete, await this.editorRef.value?.selectAllContent());
  }
  /**
   * Move the editor cursor to the end of the content.
   *
   * @internal
   */
  async setCursorLastInEditor() {
    this.editorRef.value && (await this.updateComplete, await this.editorRef.value.updateComplete, await this.editorRef.value?.setCursorLast());
  }
  /**
   * Focus the editor input.
   *
   * @internal
   */
  async focusEditor() {
    this.editorRef.value && await this.editorRef.value?.focusInput();
  }
  /**
   * Sets the conversation title when the conversation is empty, based on message text.
   *
   * @internal
   * @param name - Title string to set.
   * @returns Promise<void>
   */
  async setEmptyConversationTitle(t) {
    !this.conversation || this.conversation.name || (t = Av(t), await this.updateConversationMutation?.mutate({ appId: this.conversation.id, name: t }));
  }
  /**
   * Mark the conversation as read. Respects visibility and component lifecycle.
   *
   * @param messageId - Optional message id to mark as read.
   * @returns Promise<void>
   *
   * @internal
   */
  async markAsRead(t) {
    await c0(), await Promise.race([ny(this), _v(this, !1)]), !(!this.componentFeatures?.allowsFeature(_.Receipts) || !this.isConnected) && this.conversation && this.conversation.last_message && await this.markConversationMutation?.mutate({
      app: this.conversation,
      messageId: t ?? this.conversation.last_message.id,
      userId: this.user?.id
    });
  }
  async willUpdate(t) {
    var e;
    if (super.willUpdate(t), t.has("weavy") && this.weavy && (this.updateConversationMutation = S1(this.weavy), this.markConversationMutation = _1(this.weavy)), (t.has("weavy") || t.has("conversationId") || t.has("componentFeatures")) && this.weavy) {
      (e = ov(this, Mn)) == null || e.call(this);
      const i = t.get("conversationId");
      if (i && i > 0 && requestAnimationFrame(() => {
        qp(this.weavy?.queryClient, ["messages", i], void 0, 1);
      }), this.conversationId && this.conversationId > 0) {
        await this.messagesQuery.trackInfiniteQuery(t5(this.weavy, this.conversationId)), await this.addMessageMutation.trackMutation(
          e5(this.weavy, ["messages", this.conversationId])
        ), await this.membersQuery.trackQuery(E1(this.weavy, this.conversationId, {})), this.pollMutation = Tw(this.weavy, this.conversationId, ["messages", this.conversationId]), this.lastReadMessageId = void 0, this.showNewMessages = !1;
        const r = `a${this.conversationId}`;
        this.weavy.subscribe(r, "message_created", this.handleRealtimeMessage), this.componentFeatures?.allowsFeature(_.Reactions) && (this.weavy.subscribe(r, "reaction_added", this.handleRealtimeReactionAdded), this.weavy.subscribe(r, "reaction_removed", this.handleRealtimeReactionDeleted)), this.componentFeatures?.allowsFeature(_.Receipts) && this.weavy.subscribe(r, "app_marked", this.handleRealtimeMarked).then((a) => {
          this.showReadReceipts = a;
        }), cv(this, Mn, () => {
          this.weavy?.unsubscribe(r, "message_created", this.handleRealtimeMessage), this.weavy?.unsubscribe(r, "reaction_added", this.handleRealtimeReactionAdded), this.weavy?.unsubscribe(r, "reaction_removed", this.handleRealtimeReactionDeleted), this.weavy?.unsubscribe(r, "app_marked", this.handleRealtimeMarked), this.showReadReceipts = !1, cv(this, Mn, void 0);
        });
      } else
        this.messagesQuery.untrackInfiniteQuery(), this.addMessageMutation.untrackMutation(), this.membersQuery.untrackQuery();
    }
    if (t.has("showNewMessages") && this.showNewMessages && (this.shouldBeAtBottom = this.isAtBottom), t.has("conversationId") && t.get("conversationId") !== this.conversationId ? this.shouldBeAtBottom = !!this.conversationId : this.shouldBeAtBottom = this.isAtBottom, t.has("conversation")) {
      const i = t.get("conversation");
      if (i?.id !== this.conversation?.id || i?.is_unread !== this.conversation?.is_unread)
        if (this.componentFeatures?.allowsFeature(_.Receipts) && this.conversation?.is_unread) {
          const r = this.membersQuery.result.data?.data?.find(
            (a) => a.id === this.user?.id
          )?.marked_id;
          r && r < this.conversation.last_message.id && (this.lastReadMessagePosition = "below", this.lastReadMessageId = r, this.showNewMessages = !0), (i?.id !== this.conversation?.id || i?.last_message.id !== this.conversation?.last_message.id && (this.shouldBeAtBottom || this.isAtBottom)) && this.markAsRead();
        } else i?.id !== this.conversation?.id && (this.showNewMessages = !1);
    }
    if (this.conversation && !this.conversation?.name && !Pm(this.messagesQuery.result.data)) {
      const r = pi(this.messagesQuery.result.data).find((a) => a.plain);
      r && this.setEmptyConversationTitle(Ev(r.plain));
    }
  }
  renderConversationHeader() {
    if (!this.header)
      return h` <!-- Top of the conversation --> `;
    const { isPending: t, hasNextPage: e } = this.messagesQuery.result ?? {};
    if (!this.conversation || t || e)
      return g;
    const { data: i } = this.membersQuery.result ?? {}, r = this.user && this.isPrivateChat() ? (this.conversation?.members?.data || []).filter((a) => a.id !== this.user?.id)?.[0] ?? this.user : null;
    return h`
      <wy-avatar-header description=${ot(r?.comment)}>
        ${this.conversation.avatar_url ? h`<wy-avatar .size=${96} src=${this.conversation.avatar_url}></wy-avatar>` : this.isChatRoom() ? h` <wy-avatar-group
                .members=${i?.data}
                title=${this.conversation.name}
                .size=${96}
              ></wy-avatar-group>` : r?.avatar_url ? h`
                  <wy-avatar
                    src=${ot(r?.avatar_url)}
                    name=${this.conversation.name}
                    description=${ot(r?.comment)}
                    ?isAgent=${r?.is_agent}
                    size=${96}
                  ></wy-avatar>
                ` : g}
      </wy-avatar-header>
    `;
  }
  renderMessages() {
    const { isPending: t } = this.weavy?.network ?? { isPending: !0 }, {
      data: e,
      isPending: i
      //hasNextPage,
    } = this.messagesQuery.result ?? { isPending: t }, { data: r } = this.membersQuery.result ?? {}, a = pi(e);
    let o;
    return this.conversation && e && !Pm(e) ? h`
          <div part="wy-messages">
            <div ${K(this.pagerRef)} part="wy-pager wy-pager-top"></div>

            ${a && this.conversation && this.user ? Be(
      a,
      (c) => c.id,
      (c, l) => {
        const u = new Date(c.created_at);
        let b = h``;
        if (o?.toDateString() !== u.toDateString()) {
          const f = new Intl.DateTimeFormat(this.weavy?.locale, {
            dateStyle: "short"
          }).format(u);
          o = u, b = h`<time part="wy-message-date-separator">${f}</time>`;
        }
        let C = h``;
        this.lastReadMessageId && this.lastReadMessageId === c.id && (C = h`<div
                        id="unread-marker"
                        part="wy-banner wy-banner-action wy-transition ${this.showNewMessages ? "wy-show" : ""}"
                        tabindex=${this.showNewMessages ? 0 : -1}
                        @click=${() => {
          let f = `#message-${this.lastReadMessageId}`;
          this.lastReadMessagePosition === "below" && (f += "~ wy-message"), this.renderRoot.querySelector(f)?.scrollIntoView({
            block: "start",
            inline: "nearest",
            behavior: "smooth"
          });
        }}
                        @keydown=${ne}
                        @keyup=${ke}
                      >
                        ${x("New messages")}
                      </div>`);
        const v = r?.data?.find((f) => f.id === c.created_by.id) || c.created_by;
        return h`${[
          h`${b}`,
          h`${this.lastReadMessagePosition === "above" ? C : g}`,
          Eh(
            `message-${c.id}`,
            h`<wy-message
                          id="message-${c.id}"
                          .conversation=${this.conversation}
                          .messageId=${c.id}
                          .author=${v}
                          .isPrivateChat=${this.conversation?.type === rt.PrivateChat || this.conversation?.type === rt.AgentChat}
                          .createdAt=${c.created_at}
                          .text=${c.plain}
                          .html=${c.html}
                          .annotations=${c.annotations?.data}
                          .attachments=${c.attachments?.data}
                          .meeting=${c.meeting}
                          .pollOptions=${c.options?.data}
                          .embed=${c.embed}
                          .reactions=${c.reactions?.data}
                          .seenBy=${this.showReadReceipts && r && r.data && r.data.length > 0 ? r.data.filter((f) => f.marked_id === c.id && f.id !== this.user?.id) : []}
                          @vote=${(f) => {
              f.detail.parentId && f.detail.parentType && f.detail.parentId && this.pollMutation?.mutate({
                optionId: f.detail.optionId,
                parentType: f.detail.parentType,
                parentId: f.detail.parentId
              });
            }}
                        ></wy-message>`
          ),
          h`${this.lastReadMessagePosition === "below" ? C : g}`
        ]}`;
      }
    ) : g}
            ${this.componentFeatures?.allowsFeature(_.Typing) ? h`
                  <wy-message-typing
                    .conversationId=${this.conversation.id}
                    .userId=${this.user?.id}
                    .isPrivateChat=${this.isPrivateChat()}
                    .members=${r?.data ?? []}
                    @typing=${(c) => this.handleTyping(c)}
                  ></wy-message-typing>
                ` : g}
          </div>
        ` : h`
          <div part="wy-messages">
            <wy-empty part="wy-pane">
              ${i && this.conversationId || this.isCreatingConversation ? h`<wy-progress-circular indeterminate overlay></wy-progress-circular>` : h` <slot name="empty">${this.conversationId ? x("Start the conversation!") : g}</slot> `}
            </wy-empty>
          </div>
        `;
  }
  render() {
    return h`
      ${this.renderConversationHeader()} ${this.renderMessages()}
      <div ${K(this.bottomRef)}></div>
      <div part="wy-footerbar wy-footerbar-sticky wy-footerbar-floating">
        <slot name="footerbar"></slot>
        <wy-editor-message
          ${K(this.editorRef)}
          .draft=${!0}
          placeholder=${this.placeholder ?? x("Type a message...")}
          ?disabled=${this.conversation && !Zc(Vs.Create, this.conversation?.permissions)}
          @submit=${(t) => this.handleSubmit(t)}
          @edit-last=${(t) => {
    }}
        ></wy-editor-message>
      </div>
    `;
  }
  updated() {
    this.shouldBeAtBottom && (qp(this.weavy?.queryClient, ["messages", this.conversationId], void 0, 1), requestAnimationFrame(() => {
      this.scrollToBottom();
    })), this.bottomObserver || (this.bottomObserver = new IntersectionObserver((t) => {
      t.forEach((e) => {
        e.isIntersecting && !this.isTyping && this.conversation?.is_unread && this.markAsRead();
      });
    })), this.bottomRef && this.bottomRef.value && this.bottomObserver.observe(this.bottomRef.value), this.infiniteScroll.observe(this.messagesQuery.result, this.pagerRef.value);
  }
  // hook up observer
  connectedCallback() {
    super.connectedCallback(), document.addEventListener("visibilitychange", this.markAsReadHandler), this.conversationId && this.requestUpdate("conversationId");
  }
  disconnectedCallback() {
    var t;
    (t = ov(this, Mn)) == null || t.call(this), this.bottomObserver && this.bottomObserver.disconnect(), document.removeEventListener("visibilitychange", this.markAsReadHandler), this.conversation = void 0, this.shouldBeAtBottom = this.isAtBottom, super.disconnectedCallback();
  }
}, n(tc, "WyConversation"), tc);
Mn = /* @__PURE__ */ new WeakMap();
Oe.styles = [
  Mu,
  kr,
  dn,
  R1,
  T1,
  j`
      :host {
        position: relative;
        display: flex;
        flex-direction: column;
        flex: 1 1 auto;
      }
    `
];
oi([
  Ui({ context: bu }),
  y({ attribute: !1, type: Object })
], Oe.prototype, "conversation", 2);
oi([
  y({ type: Number })
], Oe.prototype, "conversationId", 2);
oi([
  y({ attribute: !1 })
], Oe.prototype, "editMessage", 2);
oi([
  y({ type: Boolean })
], Oe.prototype, "header", 2);
oi([
  y()
], Oe.prototype, "agentInstructions", 2);
oi([
  y()
], Oe.prototype, "placeholder", 2);
oi([
  V()
], Oe.prototype, "lastReadMessagePosition", 2);
oi([
  V()
], Oe.prototype, "lastReadMessageId", 2);
oi([
  V()
], Oe.prototype, "showNewMessages", 2);
oi([
  V()
], Oe.prototype, "isCreatingConversation", 2);
oi([
  V()
], Oe.prototype, "showReadReceipts", 2);
Oe = oi([
  U("wy-conversation"),
  ut()
], Oe);
var _$ = Object.getOwnPropertyDescriptor, Mg = /* @__PURE__ */ n((s) => {
  throw TypeError(s);
}, "__typeError$a"), P$ = /* @__PURE__ */ n((s, t, e, i) => {
  for (var r = i > 1 ? void 0 : i ? _$(t, e) : t, a = s.length - 1, o; a >= 0; a--)
    (o = s[a]) && (r = o(r) || r);
  return r;
}, "__decorateClass$D"), S$ = /* @__PURE__ */ n((s, t, e) => t.has(s) || Mg("Cannot " + e), "__accessCheck$a"), lv = /* @__PURE__ */ n((s, t, e) => (S$(s, t, "read from private field"), e ? e.call(s) : t.get(s)), "__privateGet$a"), M$ = /* @__PURE__ */ n((s, t, e) => t.has(s) ? Mg("Cannot add the same private member more than once") : t instanceof WeakSet ? t.add(s) : t.set(s, e), "__privateAdd$a"), Ih, ec;
let hu = (ec = class extends qt {
  constructor() {
    super(...arguments), this.exportParts = new W(this), M$(this, Ih, new Wc(this));
  }
  async willUpdate(t) {
    super.willUpdate(t), (t.has("weavy") || t.has("contextId") || t.has("componentFeatures")) && this.weavy && this.contextId && this.componentFeatures?.allowsFeature(_.ContextData) && await lv(this, Ih).trackMutationState(
      {
        filters: {
          mutationKey: ["apps", this.contextId, "data"],
          exact: !0
        }
      },
      this.weavy.queryClient
    );
  }
  render() {
    const { result: t, isMutating: e } = lv(this, Ih), i = t?.filter((o) => o.variables?.file), r = d1(i), a = Dy(i);
    return this.componentFeatures?.allowsFeature(_.ContextData) && t && t.length ? h`
          ${e || r.percent !== null && r.percent < 100 ? h`
                <wy-progress-linear
                  ?indeterminate=${r.percent === null}
                  overlay
                  reveal
                  ?warning=${a === "error"}
                  value=${r.percent ?? 0}
                  max=${100}
                ></wy-progress-linear>
              ` : g}
        ` : g;
  }
}, n(ec, "WyContextDataProgress"), ec);
Ih = /* @__PURE__ */ new WeakMap();
hu.styles = [Y];
hu = P$([
  U("wy-context-data-progress")
], hu);
var E$ = Object.defineProperty, A$ = Object.getOwnPropertyDescriptor, Zw = /* @__PURE__ */ n((s, t, e, i) => {
  for (var r = i > 1 ? void 0 : i ? A$(t, e) : t, a = s.length - 1, o; a >= 0; a--)
    (o = s[a]) && (r = (i ? o(t, e, r) : o(r)) || r);
  return i && r && E$(t, e, r), r;
}, "__decorateClass$C"), ic;
let al = (ic = class extends ae {
  constructor() {
    super(...arguments), this.appType = rt.Chat, this.componentFeatures = new Gt({
      // All available features as enabled/disabled by default
      [_.Attachments]: !0,
      [_.ContextData]: !0,
      [_.CloudFiles]: !0,
      [_.Embeds]: !0,
      [_.Follow]: !0,
      [_.GoogleMeet]: !0,
      [_.Meetings]: !0,
      [_.Mentions]: !0,
      [_.MicrosoftTeams]: !0,
      [_.Polls]: !0,
      [_.Previews]: !0,
      [_.Reactions]: !0,
      [_.Receipts]: !1,
      [_.Typing]: !1,
      [_.ZoomMeetings]: !0
    }), this.theme = new ce(this, al.styles);
  }
  render() {
    return h`
      <wy-buttons position="floating" reverse><slot name="actions"></slot></wy-buttons>
      <wy-conversation
        .agentInstructions=${this.instructions}
        .conversation=${this.app}
        .conversationId=${this.app?.id}
        .placeholder=${this.placeholder}
      >
        <wy-context-data-progress slot="footerbar"></wy-context-data-progress>
      </wy-conversation>
      <wy-user-card .listenTo=${this.shadowRoot}></wy-user-card>
    `;
  }
}, n(ic, "WyChat"), ic);
al.styles = [bi, Cr, $r, ls, Kt, Pe];
Zw([
  y()
], al.prototype, "instructions", 2);
Zw([
  y()
], al.prototype, "placeholder", 2);
al = Zw([
  U("wy-chat"),
  ut()
], al);
var R$ = Object.defineProperty, T$ = Object.getOwnPropertyDescriptor, Eg = /* @__PURE__ */ n((s, t, e, i) => {
  for (var r = i > 1 ? void 0 : i ? T$(t, e) : t, a = s.length - 1, o; a >= 0; a--)
    (o = s[a]) && (r = (i ? o(t, e, r) : o(r)) || r);
  return i && r && R$(t, e, r), r;
}, "__decorateClass$B"), rc;
let pd = (rc = class extends ae {
  constructor() {
    super(...arguments), this.appType = rt.Comments, this.componentFeatures = new Gt({
      // All available features as enabled/disabled by default
      [_.Attachments]: !0,
      [_.CloudFiles]: !0,
      [_.ContextData]: !0,
      [_.Embeds]: !0,
      [_.Follow]: !0,
      [_.GoogleMeet]: !1,
      [_.Meetings]: !1,
      [_.Mentions]: !0,
      [_.MicrosoftTeams]: !1,
      [_.Polls]: !0,
      [_.Previews]: !0,
      [_.Reactions]: !0,
      [_.Typing]: !1,
      // Has no effect currently
      [_.ZoomMeetings]: !1
    }), this.theme = new ce(this, pd.styles);
  }
  render() {
    return h`
      <wy-buttons position="floating" reverse><slot name="actions"></slot></wy-buttons>
      <wy-comment-list .parentId=${this.app?.id} .location=${"apps"} .placeholder=${this.placeholder}></wy-comment-list>
      <wy-user-card .listenTo=${this.shadowRoot}></wy-user-card>
      <wy-context-data-progress></wy-context-data-progress>
    `;
  }
}, n(rc, "WyComments"), rc);
pd.styles = [bi, Cr, $r, ls, Kt, Pe, jw];
Eg([
  y()
], pd.prototype, "placeholder", 2);
pd = Eg([
  U("wy-comments"),
  ut()
], pd);
function L$(s) {
  if (!this.weavy || !this.app)
    return;
  const t = {
    message: s.message,
    direction: s.message.created_by.id === this.user?.id ? "outbound" : "inbound"
  };
  s.message.created_by.is_agent && (t.agent = s.message.created_by.uid);
  const e = new CustomEvent("wy-message", {
    bubbles: !1,
    cancelable: !1,
    composed: !0,
    detail: t
  });
  this.dispatchEvent(e);
}
n(L$, "triggerMessageEvent");
var O$ = Object.defineProperty, z$ = Object.getOwnPropertyDescriptor, Yw = /* @__PURE__ */ n((s, t, e, i) => {
  for (var r = i > 1 ? void 0 : i ? z$(t, e) : t, a = s.length - 1, o; a >= 0; a--)
    (o = s[a]) && (r = (i ? o(t, e, r) : o(r)) || r);
  return i && r && O$(t, e, r), r;
}, "__decorateClass$A"), sc;
let ol = (sc = class extends ae {
  constructor() {
    super(...arguments), this.appType = rt.AgentChat, this.componentFeatures = new Gt({
      // All available features as enabled/disabled by default
      [_.Attachments]: !1,
      [_.ContextData]: !0,
      [_.Embeds]: !0,
      [_.Mentions]: !1,
      [_.Previews]: !0,
      [_.Reactions]: !1,
      [_.Typing]: !0
    }), this.theme = new ce(this, ol.styles), this.conversationRef = J(), this.handleRealtimeMessage = L$.bind(this);
  }
  /**
   * Sets the editor input to a suggested text. This replaces the text content of the editor. This can be used to create any custom suggestions.
   *
   * @param {string} text - The text suggestion to place in the editor.
   */
  async setSuggestion(t) {
    await this.conversationRef.value?.setEditorText(t);
  }
  async willUpdate(t) {
    if (await super.willUpdate(t), t.has("weavy") && this.weavy && (this.addConversationMutation = p1(this.weavy)), (t.has("app") || t.has("weavy")) && this.weavy && (this.unsubscribeToRealtime?.(), this.app)) {
      const e = `a${this.app.id}`;
      this.weavy.subscribe(e, "message_created", this.handleRealtimeMessage), this.unsubscribeToRealtime = () => {
        this.weavy?.unsubscribe(e, "message_created", this.handleRealtimeMessage), this.unsubscribeToRealtime = void 0;
      };
    }
    t.has("agent") && t.get("agent") && this.agent && this.reset();
  }
  render() {
    return this.agent ? h`
          <wy-buttons position="floating" reverse>
            <slot name="actions"></slot>
          </wy-buttons>
          <wy-conversation
            ${K(this.conversationRef)}
            .conversation=${this.app}
            .conversationId=${this.app?.id}
            .placeholder=${this.placeholder ?? x("Ask anything...")}
            .agentInstructions=${this.instructions}
            .createConversation=${this.agent && this.addConversationMutation ? async (t) => {
      if (!this.agent || !this.addConversationMutation)
        throw new Error("Agent or addConversationMutation not defined");
      const e = {
        uid: `wy-copilot-${Vl()}`,
        name: Av(Ev(this.name ?? t.text)),
        members: [this.agent],
        type: ui.AgentChat
      }, i = await this.addConversationMutation.mutate(e);
      return this.app = i, await this.updateComplete, i;
    } : void 0}
          >
            <slot
              slot="empty"
              name="empty"
              @click=${async (t) => {
      t.target.matches(".suggestion") && (t.stopPropagation(), await this.setSuggestion(t.target.innerText), await this.conversationRef.value?.setCursorLastInEditor(), await this.conversationRef.value?.focusEditor());
    }}
              @keydown=${ne}
              @keyup=${ke}
            >
              <slot name="header">
                <wy-icon-display>
                  <slot name="icon">
                    <wy-icon name="stars"></wy-icon>
                  </slot>
                </wy-icon-display>
              </slot>
              <slot name="suggestions">
                <wy-item-list>
                  <slot name="suggestion-list">
                    <!--wy-button class="suggestion">Summarize this page</wy-button-->
                  </slot>
                </wy-item-list>
              </slot>
              <slot name="footer"></slot>
            </slot>
            <wy-context-data-progress slot="footerbar"></wy-context-data-progress>
          </wy-conversation>
          <wy-user-card .listenTo=${this.shadowRoot}></wy-user-card>
        ` : h`
          <wy-empty>
            <wy-icon-display>
              <slot name="icon">
                <wy-icon name="stars"></wy-icon>
              </slot>
            </wy-icon-display>
          </wy-empty>
        `;
  }
}, n(sc, "WyCopilot"), sc);
ol.styles = [bi, Cr, $r, ls, Kt, Pe];
Yw([
  y()
], ol.prototype, "instructions", 2);
Yw([
  y()
], ol.prototype, "placeholder", 2);
ol = Yw([
  U("wy-copilot"),
  ut()
], ol);
var F$ = Object.defineProperty, I$ = Object.getOwnPropertyDescriptor, Ag = /* @__PURE__ */ n((s) => {
  throw TypeError(s);
}, "__typeError$9"), ah = /* @__PURE__ */ n((s, t, e, i) => {
  for (var r = i > 1 ? void 0 : i ? I$(t, e) : t, a = s.length - 1, o; a >= 0; a--)
    (o = s[a]) && (r = (i ? o(t, e, r) : o(r)) || r);
  return i && r && F$(t, e, r), r;
}, "__decorateClass$z"), Rg = /* @__PURE__ */ n((s, t, e) => t.has(s) || Ag("Cannot " + e), "__accessCheck$9"), Ll = /* @__PURE__ */ n((s, t, e) => (Rg(s, t, "read from private field"), e ? e.call(s) : t.get(s)), "__privateGet$9"), dv = /* @__PURE__ */ n((s, t, e) => t.has(s) ? Ag("Cannot add the same private member more than once") : t instanceof WeakSet ? t.add(s) : t.set(s, e), "__privateAdd$9"), iy = /* @__PURE__ */ n((s, t, e, i) => (Rg(s, t, "write to private field"), t.set(s, e), e), "__privateSet$7"), Dh, Ar, nc;
let es = (nc = class extends nt {
  constructor() {
    super(...arguments), this.exportParts = new W(this), this.inputRef = J(), this.compact = !1, dv(this, Dh, /* @__PURE__ */ new Set()), dv(this, Ar, ""), this.throttledSearch = Kw(
      () => {
        this.requestUpdate("query");
      },
      250,
      { leading: !1, trailing: !0 }
    );
  }
  set tags(t) {
    iy(this, Dh, new Set(t ? t.split(" ").filter(cn) : void 0));
  }
  get tags() {
    return Array.from(Ll(this, Dh)).join(" ");
  }
  set query(t) {
    iy(this, Ar, t || "");
  }
  get query() {
    return Ll(this, Ar);
  }
  /**
   * Put focus on the input field.
   */
  focusInput() {
    this.inputRef.value?.focus();
  }
  /**
   * Clear the input field.
   */
  clear() {
    this.query = "";
  }
  willUpdate(t) {
    if (t.has("query")) {
      const e = new CustomEvent("search", {
        bubbles: !0,
        composed: !1,
        detail: { query: Ll(this, Ar) }
      });
      this.dispatchEvent(e);
    }
  }
  render() {
    const t = {
      "wy-input": !0,
      "wy-input-group-input": !0,
      "wy-input-filled": !0,
      "wy-input-group-input-with-overlay": this.compact
    }, e = {
      "wy-input-group-button-icon": !0,
      "wy-input-group-button-icon-overlay": this.compact
    };
    return h`
      <div part="wy-input-group">
        <input
          part=${tt(t)}
          name="text"
          .value=${Ll(this, Ar) || ""}
          ${K(this.inputRef)}
          @input=${() => {
      iy(this, Ar, this.inputRef.value?.value || ""), Ll(this, Ar) ? this.throttledSearch() : this.requestUpdate("query");
    }}
          @keydown=${E5}
          @keyup=${lr}
          placeholder=${this.placeholder || x("Search...")}
          size="4"
        />
        <wy-button type="reset" @click=${() => this.clear()} kind="icon" part=${tt(e)}>
          <wy-icon name="close-circle"></wy-icon>
        </wy-button>
        <wy-button kind="icon" part=${tt(e)}>
          <wy-icon name="magnify"></wy-icon>
        </wy-button>
      </div>
    `;
  }
}, n(nc, "WySearch"), nc);
Dh = /* @__PURE__ */ new WeakMap();
Ar = /* @__PURE__ */ new WeakMap();
es.styles = [us, Y];
ah([
  y()
], es.prototype, "placeholder", 2);
ah([
  y({ type: Boolean })
], es.prototype, "compact", 2);
ah([
  y()
], es.prototype, "tags", 1);
ah([
  y()
], es.prototype, "query", 1);
es = ah([
  U("wy-search"),
  ut()
], es);
function D$(s, t) {
  const e = s.queryClient, r = {
    mutationKey: ["posts", t === "feed" ? "feed" : t.id],
    mutationFn: /* @__PURE__ */ n(async ({ id: a }) => {
      if (!(await s.fetch("/api/posts/" + a + "/trash", { method: "POST" })).ok)
        throw new Error();
    }, "mutationFn"),
    onMutate: /* @__PURE__ */ n((a) => {
      ht(
        e,
        { queryKey: r.mutationKey, exact: !1 },
        a.id,
        (o) => Object.assign(o, { is_trashed: !0 })
      );
    }, "onMutate"),
    onSuccess: /* @__PURE__ */ n((a, o) => {
      ht(
        e,
        { queryKey: r.mutationKey, exact: !1 },
        o.id,
        (c) => Object.assign(c, a)
      );
    }, "onSuccess")
  };
  return r;
}
n(D$, "getTrashPostMutationOptions");
function V$(s, t) {
  return new St(s.queryClient, D$(s, t));
}
n(V$, "getTrashPostMutation");
function H$(s, t) {
  const e = s.queryClient, r = {
    mutationKey: ["posts", t === "feed" ? "feed" : t.id],
    mutationFn: /* @__PURE__ */ n(async ({ id: a }) => {
      const o = await s.fetch("/api/posts/" + a + "/restore", { method: "POST" });
      if (!o.ok) {
        const c = await o.json();
        throw new Error(c.detail || c.title, { cause: c });
      }
    }, "mutationFn"),
    onMutate: /* @__PURE__ */ n((a) => {
      ht(
        e,
        { queryKey: r.mutationKey, exact: !1 },
        a.id,
        (o) => Object.assign(o, { is_trashed: !1 })
      );
    }, "onMutate"),
    onSuccess: /* @__PURE__ */ n((a, o) => {
      ht(
        e,
        { queryKey: r.mutationKey, exact: !1 },
        o.id,
        (c) => Object.assign(c, { is_trashed: !1 })
      );
    }, "onSuccess")
  };
  return r;
}
n(H$, "getRestorePostMutationOptions");
function U$(s, t) {
  return new St(s.queryClient, H$(s, t));
}
n(U$, "getRestorePostMutation");
function B$(s, t) {
  const e = s.queryClient, i = ["posts", t === "feed" ? "feed" : t.id];
  return {
    mutationKey: i,
    mutationFn: /* @__PURE__ */ n(async ({ id: a, subscribe: o }) => {
      const c = await s.fetch(`/api/posts/${a}/${o ? "subscribe" : "unsubscribe"}`, {
        method: "POST"
      });
      if (!c.ok) {
        const l = await c.json();
        throw new Error(l.detail || l.title, { cause: l });
      }
    }, "mutationFn"),
    onMutate: /* @__PURE__ */ n((a) => {
      ht(
        e,
        { queryKey: i, exact: !1 },
        a.id,
        (o) => Object.assign(o, { is_subscribed: a.subscribe })
      );
    }, "onMutate"),
    onError: /* @__PURE__ */ n(async () => {
      await e.invalidateQueries({ queryKey: i, exact: !1 });
    }, "onError")
  };
}
n(B$, "getSubscribePostMutationOptions");
function N$(s, t) {
  return new St(s.queryClient, B$(s, t));
}
n(N$, "getSubscribePostMutation");
function q$(s, t, e = {
  app: void 0,
  q: "",
  tag: "",
  trashed: !1,
  following: !1,
  order_by: "id desc"
}, i = !1) {
  const r = Qd({ ...e, count_only: i }), a = new URLSearchParams(pw(r));
  return {
    initialPageParam: 0,
    queryKey: ["posts", t, r],
    queryFn: /* @__PURE__ */ n(async (o) => {
      const c = o.pageParam, l = new URLSearchParams(a);
      l.append("out", "details"), l.append("skip", c.toString());
      const u = t === "feed" ? `/api/posts?${l.toString()}` : `/api/apps/${t}/posts?${l.toString()}`, C = await (await s.fetch(u)).json();
      return C.data = C.data || [], C;
    }, "queryFn"),
    getNextPageParam: /* @__PURE__ */ n((o) => {
      if (o.end && o.end < o.count)
        return o.end;
    }, "getNextPageParam")
  };
}
n(q$, "getPostsOptions");
function W$(s, t) {
  const e = s.queryClient;
  return {
    mutationFn: /* @__PURE__ */ n(async (r) => await (await s.fetch("/api/posts/" + r.id, {
      method: "PATCH",
      body: JSON.stringify({
        text: r.text,
        blobs: r.blobs,
        attachments: r.attachments,
        meeting_id: r.meeting_id,
        options: r.poll_options.filter((o) => o.text.trim() !== "").map((o) => ({ id: o.id, text: o.text })),
        embed_id: r.embed_id || null
      })
    })).json(), "mutationFn"),
    mutationKey: t,
    onMutate: /* @__PURE__ */ n((r) => {
      ht(
        e,
        [
          { queryKey: ["posts", r.app_id], exact: !1 },
          { queryKey: ["posts", "feed"], exact: !1 }
        ],
        r.id,
        (a) => ({
          ...a,
          text: r.text,
          html: r.text
        })
      );
    }, "onMutate"),
    onSuccess: /* @__PURE__ */ n((r, a) => {
      a.id && ht(
        e,
        [
          { queryKey: ["posts", a.app_id], exact: !1 },
          { queryKey: ["posts", "feed"], exact: !1 }
        ],
        a.id,
        () => r
      );
    }, "onSuccess"),
    onError: /* @__PURE__ */ n(() => {
      e.invalidateQueries({ queryKey: ["posts"], exact: !1 });
    }, "onError")
  };
}
n(W$, "getUpdatePostMutationOptions");
function j$(s, t) {
  const e = s.queryClient;
  return {
    mutationFn: /* @__PURE__ */ n(async (r) => await (await s.fetch("/api/apps/" + r.app_id + "/posts", {
      method: "POST",
      body: JSON.stringify({
        text: r.text,
        blobs: r.blobs,
        meeting_id: r.meeting_id,
        options: r.poll_options.filter((o) => o.text.trim() !== "").map((o) => ({ text: o.text })),
        embed_id: r.embed_id,
        context: r.context
      })
    })).json(), "mutationFn"),
    mutationKey: t,
    onMutate: /* @__PURE__ */ n(async (r) => {
      const a = ["posts", r.app_id];
      await e.cancelQueries({ queryKey: a });
      const c = {
        id: -1 * Date.now(),
        app: { id: r.app_id },
        is_subscribed: !0,
        is_trashed: !1,
        text: r.text,
        html: r.text,
        plain: r.text,
        created_by: r.user,
        created_at: (/* @__PURE__ */ new Date()).toUTCString(),
        attachments: { count: 0 },
        reactions: { count: 0 },
        is_starred: !1,
        comments: { count: 0 }
      };
      return _m(
        e,
        [
          { queryKey: ["posts", r.app_id], exact: !1, predicate: /* @__PURE__ */ n((l) => _h(l), "predicate") },
          { queryKey: ["posts", "feed"], exact: !1, predicate: /* @__PURE__ */ n((l) => _h(l), "predicate") }
        ],
        c
      ), { tempPost: c };
    }, "onMutate"),
    onSuccess: /* @__PURE__ */ n((r, a, o) => {
      _m(
        e,
        [
          { queryKey: ["posts", a.app_id], exact: !1, predicate: /* @__PURE__ */ n((c) => _h(c), "predicate") },
          { queryKey: ["posts", "feed"], exact: !1, predicate: /* @__PURE__ */ n((c) => _h(c), "predicate") }
        ],
        r,
        o.tempPost.id
      ), e.invalidateQueries({ queryKey: ["posts", a.app_id], exact: !1 }), e.invalidateQueries({ queryKey: ["posts", "feed"], exact: !1 });
    }, "onSuccess"),
    onError: /* @__PURE__ */ n(() => {
      e.invalidateQueries({ queryKey: ["posts"], exact: !1 });
    }, "onError")
  };
}
n(j$, "getAddPostMutationOptions");
function _h(s) {
  const t = s.queryKey.at(-1);
  return !(f1(t) && (t.q || t.tag || t.trashed));
}
n(_h, "permitsOptimisticPost");
function K$(s, t, e, i = {
  app: void 0,
  q: "",
  tag: "",
  trashed: !1,
  following: !1,
  order_by: void 0
}, r = {}) {
  const a = Qd({ ...i, count_only: !0, after: e }), o = ["posts", t === "feed" ? "feed" : t.id, "count", a], c = new URLSearchParams(pw(a)), l = t === "feed" ? `/api/posts?${c.toString()}` : `/api/apps/${t.id}/posts?${c.toString()}`;
  return nr(s, o, l, r);
}
n(K$, "getPostsCountOptions");
function Tg(s, t, e, i, r) {
  const a = `a${e.id}`, o = r ? "feed" : e.id;
  return [
    {
      group: a,
      event: "comment_created",
      onMessage: /* @__PURE__ */ n((c) => {
        c.actor.id === i.id || c.comment.parent?.type !== It.Post || r && r.id !== c.comment.parent.id || (ht(
          s.queryClient,
          [{ queryKey: ["posts", o], exact: !1 }],
          c.comment.parent.id,
          (l) => (l.comments ? l.comments.count += 1 : l.comments = { count: 1 }, l)
        ), s.queryClient.invalidateQueries({
          queryKey: ["posts", "comments", c.comment.parent.id]
        }));
      }, "onMessage")
    },
    t.allowsFeature(_.Reactions) ? {
      group: a,
      event: "reaction_added",
      onMessage: /* @__PURE__ */ n((c) => {
        c.entity.type === It.Post && ht(
          s.queryClient,
          [{ queryKey: ["posts", o], exact: !1 }],
          c.entity.id,
          (l) => sl(l, c.reaction, c.actor)
        );
      }, "onMessage")
    } : void 0,
    t.allowsFeature(_.Reactions) ? {
      group: a,
      event: "reaction_removed",
      onMessage: /* @__PURE__ */ n((c) => {
        c.entity.type === It.Post && ht(
          s.queryClient,
          [{ queryKey: ["posts", o], exact: !1 }],
          c.entity.id,
          (l) => sl(l, void 0, c.actor)
        );
      }, "onMessage")
    } : void 0
  ];
}
n(Tg, "getRealtimePostOptions");
const Yf = class Yf {
  constructor(t) {
    this.registrationRequested = !1, this.realtimeOptions = [], t.addController(this), this.host = t, this.whenWeavyContext = new Promise((e) => this.resolveWeavyContext = e), this.setContexts(), this.registerRealtime();
  }
  get weavy() {
    return this.weavyContext?.value;
  }
  /**
   * Initiates context consumers
   */
  async setContexts() {
    await gi(this.host), this.weavyContext = new He(this.host, { context: Xt, subscribe: !0 });
  }
  /**
   * Register realtime handlers.
   */
  async registerRealtime() {
    if (!this.registrationRequested) {
      this.registrationRequested = !0;
      const t = await this.whenWeavyContext;
      for (const e of this.realtimeOptions)
        if (e) {
          const { group: i, event: r, onMessage: a } = e;
          await t.subscribe(i, r, a);
        }
      this.registrationRequested = !1;
    }
  }
  /**
   * Unregister realtime handlers.
   *
   * @param skipAwait - Skip waiting for any context.
   */
  async unregisterRealtime(t = !1) {
    if (!this.registrationRequested) {
      !t && await this.whenWeavyContext;
      for (const e of this.realtimeOptions)
        if (e) {
          const { group: i, event: r, onMessage: a } = e;
          await this.weavy?.unsubscribe(i, r, a);
        }
    }
  }
  /**
   * Tracks unread data. Initiates the query data with the given filtering scope.
   *
   * @param typeFilter - The notification types to track.
   * @param appId - Optional app id for the filtering scope.
   */
  async track(t) {
    await this.unregisterRealtime(), this.realtimeOptions = t, await this.registerRealtime();
  }
  hostUpdate() {
    this.weavyContext?.value && this.resolveWeavyContext?.(this.weavyContext?.value);
  }
  hostDisconnected() {
    this.weavy && this.unregisterRealtime(!0);
  }
};
n(Yf, "RealtimeController");
let yd = Yf;
var Os, ac, Wr;
const Xf = class Xf {
  constructor(t) {
    T(this, Os);
    T(this, ac);
    T(this, Wr);
    P(this, ac, 0), this.filter = {
      q: "",
      tag: "",
      trashed: !1,
      following: !1,
      order_by: "id desc"
    }, this.updateInterval = 1e4, this.registrationRequested = !1, this.refresh = () => {
      this.currentQuery.result.isPending || this.currentQuery.result.refetch();
    }, t.addController(this), this.host = t, this.currentQuery = new ve(t), this.postsRealtime = new yd(t), this.whenWeavyContext = new Promise((e) => this.resolveWeavyContext = e), this.setContexts();
  }
  /**
   * The number of unread items for the current scope
   */
  get unread() {
    return p(this, ac);
  }
  /**
   * Is the unread count pending?
   */
  get isUnreadPending() {
    return this.currentQuery.result.isPending;
  }
  /**
   * The underlying query result for the unread count.
   */
  get unreadResult() {
    return this.currentQuery.result;
  }
  get weavy() {
    return this.weavyContext?.value;
  }
  /**
   * Initiates context consumers
   */
  async setContexts() {
    await gi(this.host), this.weavyContext = new He(this.host, { context: Xt, subscribe: !0 });
  }
  /**
   * Dispatch a `wy-unread` event on the host.
   *
   * @fires {WyUnreadEventType} wy-unread - Emitted when the number of unread items change.
   */
  dispatchUnreadEvent() {
    const t = new CustomEvent("wy-unread", {
      detail: { unread: this.unread },
      bubbles: !1,
      composed: !0
    });
    this.host.dispatchEvent(t);
  }
  async track(t, e, i) {
    this.filter = i;
    const r = await this.whenWeavyContext, a = 10080 * 60 * 1e3;
    P(this, Os, void 0), await this.currentQuery.trackQuery(K$(r, t, e, this.filter, { staleTime: a }), !1), t !== "feed" && await this.postsRealtime.track([
      {
        group: `a${t.id}`,
        event: "post_created",
        onMessage: /* @__PURE__ */ n((o) => {
          !o.post.app || o.post.app.id !== t.id || this.refresh();
        }, "onMessage")
      }
    ]);
  }
  hostConnected() {
    p(this, Wr) && window.clearInterval(p(this, Wr)), P(this, Wr, window.setInterval(this.refresh, this.updateInterval));
  }
  hostDisconnected() {
    p(this, Wr) && window.clearInterval(p(this, Wr));
  }
  hostUpdate() {
    this.weavyContext?.value && this.resolveWeavyContext?.(this.weavyContext?.value);
    let t = 0;
    if (this.currentQuery.result.isPending || this.currentQuery.result.isStale)
      P(this, Os, void 0);
    else {
      const e = this.currentQuery.result.data?.count ?? 0;
      p(this, Os) ?? P(this, Os, e), t = e - p(this, Os);
    }
    t !== this.unread && (P(this, ac, t), this.dispatchUnreadEvent(), this.host.requestUpdate());
  }
};
Os = new WeakMap(), ac = new WeakMap(), Wr = new WeakMap(), n(Xf, "UnreadPostsController");
let aw = Xf;
const oh = j`@keyframes wy-highlight{0%{--wy-component-background-color: var(--wy-highlight, var(--wy-highlight-light, #e8f2ff));--wy-component-color: var(--wy-on-highlight, var(--wy-on-highlight-light, #001d32))}}[part~=wy-posts-header]{--wy-component-color: var(--wy-on-surface, var(--wy-on-surface-light, #181c20));background-color:var(--wy-surface-container-low, var(--wy-surface-container-low-light, #f1f3f9));color:var(--wy-component-color);display:flex;flex-direction:column;position:relative;--wy-component-border-radius: var(--wy-border-radius, calc(.5 * var(--wy-size, 1rem)));border-radius:var(--wy-border-radius-outer, var(--wy-border-radius, calc(.5 * var(--wy-size, 1rem))))}[part~=wy-post]{--wy-component-color: var(--wy-on-surface, var(--wy-on-surface-light, #181c20));background-color:var(--wy-background, var(--wy-background-light, #f7f9ff));color:var(--wy-component-color);display:flex;flex-direction:column;position:relative;clip-path:border-box;--wy-component-border-radius: var(--wy-border-radius, calc(.5 * var(--wy-size, 1rem)));border-radius:var(--wy-component-border-radius)}[part~=wy-post][part~=wy-highlight]{animation:wy-highlight 3s 1.5s ease-in-out both}[part~=wy-post-images]{display:flex;flex-direction:column;margin-top:var(--wy-gap-lg, calc(1.5 * var(--wy-gap, var(--wy-padding, calc(.5 * var(--wy-size, 1rem))))))}[part~=wy-post-header]{display:flex;flex-direction:column;padding:var(--wy-gap-lg, calc(1.5 * var(--wy-gap, var(--wy-padding, calc(.5 * var(--wy-size, 1rem)))))) var(--wy-padding-lg, calc(1.5 * var(--wy-padding, calc(.5 * var(--wy-size, 1rem))))) 0;gap:var(--wy-gap-lg, calc(1.5 * var(--wy-gap, var(--wy-padding, calc(.5 * var(--wy-size, 1rem))))))}[part~=wy-post-body]{display:flex;flex-direction:column;padding:var(--wy-padding-lg, calc(1.5 * var(--wy-padding, calc(.5 * var(--wy-size, 1rem))))) var(--wy-padding, calc(.5 * var(--wy-size, 1rem))) 0;gap:var(--wy-gap-xl, calc(2 * var(--wy-gap, var(--wy-padding, calc(.5 * var(--wy-size, 1rem))))))}[part~=wy-post-footer]{display:flex;align-items:center;justify-content:space-between;padding:var(--wy-padding, calc(.5 * var(--wy-size, 1rem))) var(--wy-padding-lg, calc(1.5 * var(--wy-padding, calc(.5 * var(--wy-size, 1rem)))))}[part~=wy-post-content]{padding:0 var(--wy-padding-sm, calc(.75 * var(--wy-padding, calc(.5 * var(--wy-size, 1rem)))))}[part~=wy-post-comments]{background-color:var(--wy-surface-container-low, var(--wy-surface-container-low-light, #f1f3f9));interpolate-size:allow-keywords;padding:0 var(--wy-padding, calc(.5 * var(--wy-size, 1rem)));height:0;visibility:hidden;transition:height var(--wy-transition, var(--wy-transition-duration, .2s) var(--wy-transition-curve, cubic-bezier(.4, 0, .2, 1))),padding var(--wy-transition-fast, var(--wy-transition-duration-fast, .1s) var(--wy-transition-curve, cubic-bezier(.4, 0, .2, 1)));overflow:hidden}[part~=wy-post-comments][part~=wy-show]{height:auto;visibility:visible;padding:var(--wy-padding, calc(.5 * var(--wy-size, 1rem)))}[part~=wy-post-trashed]{padding-bottom:var(--wy-padding-lg, calc(1.5 * var(--wy-padding, calc(.5 * var(--wy-size, 1rem)))))}::part(wy-trashed){text-decoration:line-through;color:var(--wy-outline, var(--wy-outline-light, #72777f))}`;
var Q$ = Object.defineProperty, G$ = Object.getOwnPropertyDescriptor, Lg = /* @__PURE__ */ n((s, t, e, i) => {
  for (var r = i > 1 ? void 0 : i ? G$(t, e) : t, a = s.length - 1, o; a >= 0; a--)
    (o = s[a]) && (r = (i ? o(t, e, r) : o(r)) || r);
  return i && r && Q$(t, e, r), r;
}, "__decorateClass$y"), oc;
let wd = (oc = class extends nt {
  constructor() {
    super(...arguments), this.exportParts = new W(this);
  }
  /**
   * Emit a `restore` event requesting restoration of the trashed post.
   *
   * @returns {boolean} True if the event was not canceled.
   */
  dispatchRestore() {
    const t = new CustomEvent("restore", { detail: { id: this.post.id } });
    return this.dispatchEvent(t);
  }
  render() {
    return h`
      <div part="wy-post wy-post-trashed">
        <wy-item part="wy-post-header" size="md" noPadding>
          <wy-avatar
            slot="image"
            .src="${this.post.created_by.avatar_url}"
            .isAgent=${this.post.created_by.is_agent}
            .size=${48}
            .name=${this.post.created_by.name}
          ></wy-avatar>
          <span part="wy-trashed" slot="title">${x("Post was trashed.")}</span>
          <wy-button small slot="actions" @click=${() => this.dispatchRestore()} color="variant"
            >${x("Undo")}</wy-button
          >
        </wy-item>
      </div>
    `;
  }
}, n(oc, "WyPostTrashed"), oc);
wd.styles = [oh, Y];
Lg([
  y({ attribute: !1 })
], wd.prototype, "post", 2);
wd = Lg([
  U("wy-post-trashed"),
  ut()
], wd);
var Z$ = Object.defineProperty, Y$ = Object.getOwnPropertyDescriptor, Cl = /* @__PURE__ */ n((s, t, e, i) => {
  for (var r = i > 1 ? void 0 : i ? Y$(t, e) : t, a = s.length - 1, o; a >= 0; a--)
    (o = s[a]) && (r = (i ? o(t, e, r) : o(r)) || r);
  return i && r && Z$(t, e, r), r;
}, "__decorateClass$x"), cc;
let pr = (cc = class extends qt {
  constructor() {
    super(...arguments), this.exportParts = new W(this), this.highlight = !1, this.showComments = !1, this.loadComments = !1, this.isCommentLinked = !1, this.previewAnnotationsRef = J(), this.previewAttachmentsRef = J(), this.highlightRef = J(), this.dispatchUserAction = Bw.bind(this);
  }
  /**
   * Emit a `vote` event for the specified poll option.
   *
   * @internal
   * @param optionId - Identifier of the selected poll option.
   * @returns {boolean} True if the event was not canceled.
   */
  dispatchVote(t) {
    const e = new CustomEvent("vote", { detail: { optionId: t, parentType: "posts" } });
    return this.dispatchEvent(e);
  }
  /**
   * Emit a `subscribe` event toggling post subscription state.
   *
   * @internal
   * @param subscribe - Desired subscription state.
   * @returns {boolean} True if the event was not canceled.
   */
  dispatchSubscribe(t) {
    const e = new CustomEvent("subscribe", {
      detail: { id: this.post.id, subscribe: t }
    });
    return this.dispatchEvent(e);
  }
  /**
   * Emit a `trash` event requesting the post to move into trash.
   *
   * @internal
   * @returns {boolean} True if the event was not canceled.
   */
  dispatchTrash() {
    const t = new CustomEvent("trash", { detail: { id: this.post.id } });
    return this.dispatchEvent(t);
  }
  /**
   * Emit an `edit` event toggling edit mode for this post.
   *
   * @internal
   * @param edit - Desired edit state.
   * @returns {boolean} True if the event was not canceled.
   */
  dispatchEdit(t) {
    const e = new CustomEvent("edit", { detail: { edit: t } });
    return this.dispatchEvent(e);
  }
  /**
   * Toggle comment visibility and ensure comment data is loaded.
   *
   * @internal
   * @param e - Click event originating from the comment button.
   */
  handleCommentsClick(t) {
    t.preventDefault(), this.showComments = !this.showComments, this.loadComments = !0;
  }
  willUpdate(t) {
    super.willUpdate(t), t.has("link") && (this.highlight = !!(this.link && Vu(this.link, It.Post, { id: this.post.id })), this.isCommentLinked = !!(this.link && ng(this.link, It.Post, { id: this.post.id }, It.Comment))), t.has("isCommentLinked") && this.isCommentLinked && (this.loadComments = !0, this.showComments = !0);
  }
  render() {
    const t = this.post.attachments?.data?.filter((D) => D.kind === "image" && D.thumbnail_url) ?? [], e = this.post.attachments?.data?.filter((D) => D.kind !== "image" || !D.thumbnail_url) ?? [], i = this.post.annotations?.data, r = this.post.options?.data, a = this.post.comments?.count || 0, o = this.post.reactions?.data, { embed: c, meeting: l } = this.post, u = new Intl.DateTimeFormat(this.weavy?.locale, {
      dateStyle: "full",
      timeStyle: "short"
    }).format(new Date(this.post.created_at)), b = bl(this.weavy?.locale, new Date(this.post.created_at)), C = this.post.updated_at && this.weavy ? new Intl.DateTimeFormat(this.weavy.locale, { dateStyle: "full", timeStyle: "short" }).format(
      new Date(this.post.updated_at)
    ) : "", v = t && !!t.length, f = this.post.html && !!this.post.html, m = i && !!i.length, $ = r && !!r.length, E = c && this.componentFeatures?.allowsFeature(_.Embeds), M = e && !!e.length, A = l && !!l, H = f || m || $ || E || M || A;
    return this.post.id < 0 ? h`
          <div part="wy-post">
            <wy-item slot="button" part="wy-post-header" align="top" size="md" noPadding>
              <wy-avatar
                slot="image"
                .src="${this.post.created_by.avatar_url}"
                .isAgent=${this.post.created_by.is_agent}
                .size=${48}
                .name=${this.post.created_by.name}
              ></wy-avatar>
              <span slot="title" part="wy-placeholder">${this.post.created_by.name}</span>
              <time slot="text" part="wy-placeholder">${b}</time>
            </wy-item>
            <div part="wy-post-body">
              <div part="wy-content wy-post-content"><wy-skeleton .text=${this.post.text}></wy-skeleton></div>
            </div>
            <div part="wy-post-footer"></div>
          </div>
        ` : h`
          <div
            part=${tt({ "wy-post": !0, "wy-highlight": this.highlight && !this.isCommentLinked })}
            ${K(this.highlightRef)}
          >
            <wy-item slot="toggle" part="wy-post-header" align="top" size="md" noPadding>
              <wy-avatar
                slot="image"
                .src="${this.post.created_by.avatar_url}"
                .isAgent=${this.post.created_by.is_agent}
                .size=${48}
                .name=${this.post.created_by.name}
                title=${this.post.created_by.name}
                role="button"
                @click=${() => this.dispatchUserAction(this.post.created_by)}
              ></wy-avatar>
              <wy-button 
                kind="link"
                slot="title"
                @click=${() => this.dispatchUserAction(this.post.created_by)}
                >${this.post.created_by.name}</wy-button>
              <span slot="text">
                <time datetime=${this.post.created_at} title=${u}>${b}</time>
                ${this.post.updated_at ? h`<time datetime="${this.post.updated_at}" title=${C}> · ${x("edited")}</time>` : g}
              </span>
              <wy-dropdown slot="actions">
                ${this.post.is_subscribed ? h`<wy-dropdown-item @click=${() => this.dispatchSubscribe(!1)}>
                        <wy-icon name="bell-off"></wy-icon>
                        ${x("Unsubscribe")}
                      </wy-dropdown-item>` : h`<wy-dropdown-item @click=${() => this.dispatchSubscribe(!0)}>
                        <wy-icon name="bell"></wy-icon>
                        ${x("Subscribe")}
                      </wy-dropdown-item>`}
                ${this.user && this.user.id === this.post.created_by.id ? h`<wy-dropdown-item @click=${() => this.dispatchEdit(!0)}>
                        <wy-icon name="pencil"></wy-icon>
                        ${x("Edit")}
                      </wy-dropdown-item>` : g}
                ${this.user && this.user.id === this.post.created_by.id ? h`<wy-dropdown-item @click=${() => this.dispatchTrash()}>
                        <wy-icon name="trashcan"></wy-icon>
                        ${x("Trash")}
                      </wy-dropdown-item>` : g}
              </wy-dropdown>
            </wy-item>

            <!-- image grid -->
            ${v ? h`<wy-image-grid
                    part="wy-post-images"
                    outer
                    .images=${t}
                    @file-open=${(D) => {
      this.previewAttachmentsRef.value?.open(D.detail.fileId);
    }}
                  ></wy-image-grid>` : ""}

            ${H ? h`
                    <div part="wy-post-body">
                      <!-- text content -->
                      ${f ? h`<div
                            part="wy-content wy-post-content"
                            @click=${(D) => {
      if (D.target instanceof HTMLElement && D.target.matches('.wy-mention, [part~="wy-mention"]')) {
        const I = D.target.dataset.eid?.startsWith("u") && parseInt(D.target.dataset.eid.substring(1));
        I && this.dispatchUserAction({ id: I, name: D.target.innerText });
      }
    }}
                            >${// eslint-disable-next-line lit-a11y/click-events-have-key-events
    Bs(this.post.html)}</div
                          >` : ""}

                      <!-- annotations -->
                      ${m ? h`<wy-annotation-list
                            .files=${i}
                            @file-open=${(D) => {
      this.previewAnnotationsRef.value?.open(D.detail.fileId);
    }}
                          ></wy-annotation-list>` : g}

                      <!-- poll -->
                      ${$ ? h`
                            <wy-poll
                              .pollOptions=${r}
                              @vote=${(D) => this.dispatchVote(D.detail.optionId)}
                            ></wy-poll>
                          ` : g}

                      <!-- embeds -->
                      ${E ? h` <wy-embed .embed=${c}></wy-embed> ` : g}

                      <!-- files -->
                      ${M ? h`<wy-attachment-list
                            filled
                            part="wy-post-attachments"
                            .files=${e}
                            @file-open=${(D) => {
      this.previewAttachmentsRef.value?.open(D.detail.fileId);
    }}
                          ></wy-attachment-list>` : g}
                      <!-- meeting -->
                      ${A ? h`<wy-meeting-card .meeting=${l}></wy-meeting-card>` : g}
                    </div>
                  ` : g}
          <div part="wy-post-footer">

            <!-- comment count -->
            ${this.componentFeatures?.allowsFeature(_.Comments) ? h` <wy-button
                    small
                    kind="inline"
                    ?active=${this.showComments}
                    part="wy-meta"
                    color="inherit"
                    @click=${(D) => this.handleCommentsClick(D)}
                  >
                    ${a !== 1 ? x(gt`${a} comments`) : x("1 comment")}
                  </wy-button>` : g}

            <!-- reactions -->
            ${this.componentFeatures?.allowsFeature(_.Reactions) && this.app ? h`
                    <wy-reactions
                      line
                      small
                      .reactions=${o}
                      parentId=${this.app.id}
                      parentType="apps"
                      entityId=${this.post.id}
                      entityType="posts"
                    ></wy-reactions>
                  ` : g}

          </div>

          <!-- comments -->
          <div part="wy-post-comments ${tt({ "wy-show": this.showComments })}">
            ${this.loadComments ? h`
                    <wy-comment-list
                      reveal
                      part="wy-post-comment-list"
                      .parentId=${this.post.id}
                      .location=${"posts"}
                    ></wy-comment-list>
                  ` : g}
          </div>
        </div>

            ${m ? h`<wy-preview
                    ${K(this.previewAnnotationsRef)}
                    .files=${i}
                    .isAttachment=${!0}
                  ></wy-preview> ` : g}
            ${v || M ? h`<wy-preview
                    ${K(this.previewAttachmentsRef)}
                    .files=${[...t, ...e]}
                    .isAttachment=${!0}
                  ></wy-preview> ` : g}
          </div>
        `;
  }
  updated(t) {
    t.has("highlight") && this.highlight && this.highlightRef.value?.scrollIntoView({ block: "nearest" });
  }
}, n(cc, "WyPostView"), cc);
pr.styles = [
  Lt,
  oh,
  yg,
  Nw,
  tg,
  Mu,
  fl,
  Y
];
Cl([
  y({ attribute: !1 })
], pr.prototype, "post", 2);
Cl([
  y({ type: Boolean })
], pr.prototype, "highlight", 2);
Cl([
  V()
], pr.prototype, "showComments", 2);
Cl([
  V()
], pr.prototype, "loadComments", 2);
Cl([
  V()
], pr.prototype, "isCommentLinked", 2);
pr = Cl([
  U("wy-post-view"),
  ut()
], pr);
var X$ = Object.defineProperty, J$ = Object.getOwnPropertyDescriptor, Og = /* @__PURE__ */ n((s, t, e, i) => {
  for (var r = i > 1 ? void 0 : i ? J$(t, e) : t, a = s.length - 1, o; a >= 0; a--)
    (o = s[a]) && (r = (i ? o(t, e, r) : o(r)) || r);
  return i && r && X$(t, e, r), r;
}, "__decorateClass$w"), lc;
let fd = (lc = class extends qt {
  constructor() {
    super(...arguments), this.exportParts = new W(this), this.updatePostMutation = new Ue(this);
  }
  /**
   * Emit an `edit` event toggling edit mode for the post.
   *
   * @param edit - Desired edit state.
   * @returns {boolean} True if the event was not canceled.
   */
  dispatchEdit(t) {
    const e = new CustomEvent("edit", { detail: { edit: t } });
    return this.dispatchEvent(e);
  }
  /**
   * Handle editor submit events and trigger the post update mutation.
   *
   * @param e - Editor submit detail containing updated content.
   */
  async handleSubmit(t) {
    const e = await this.whenApp(), i = await this.whenUser();
    this.updatePostMutation.mutate({
      id: this.post.id,
      app_id: e.id,
      user: i,
      text: t.detail.text,
      meeting_id: t.detail.meetingId,
      blobs: t.detail.blobs,
      attachments: t.detail.attachments,
      poll_options: t.detail.pollOptions,
      embed_id: t.detail.embedId
    }), this.dispatchEdit(!1);
  }
  async willUpdate(t) {
    super.willUpdate(t), (t.has("weavy") || t.has("app")) && this.weavy && this.app && await this.updatePostMutation.trackMutation(W$(this.weavy, ["posts", this.app.id]));
  }
  render() {
    const t = bl(this.weavy?.locale, new Date(this.post.created_at));
    return h`
      <div part="wy-post">
        <wy-item part="wy-post-header" align="top" size="md" noPadding>
          <wy-avatar
            slot="image"
            .src="${this.post.created_by.avatar_url}"
            .isAgent=${this.post.created_by.is_agent}
            .size=${48}
            .name=${this.post.created_by.name}
          ></wy-avatar>
          <span slot="title" part="wy-placeholder">${this.post.created_by.name}</span>
          <time slot="text" part="wy-placeholder">${t}</time>
          <wy-button slot="actions" kind="icon" @click=${() => this.dispatchEdit(!1)}>
            <wy-icon name="close"></wy-icon>
          </wy-button>
        </wy-item>

        <wy-editor-msg
          editorLocation="apps"
          .text=${this.post.text}
          .embed=${this.post.embed}
          .pollOptions=${this.post.options?.data ?? []}
          .attachments=${this.post.attachments?.data ?? []}
          .parentId=${this.post.id}
          .typing=${!1}
          .draft=${!1}
          placeholder=${x("Edit post...")}
          buttonText=${x("Update", { desc: "Button action to update" })}
          @submit=${(e) => this.handleSubmit(e)}
        ></wy-editor-msg>
      </div>
    `;
  }
}, n(lc, "WyPostEdit"), lc);
fd.styles = [oh, fl, Y];
Og([
  y({ attribute: !1 })
], fd.prototype, "post", 2);
fd = Og([
  U("wy-post-edit"),
  ut()
], fd);
var t9 = Object.defineProperty, e9 = Object.getOwnPropertyDescriptor, zg = /* @__PURE__ */ n((s) => {
  throw TypeError(s);
}, "__typeError$8"), ch = /* @__PURE__ */ n((s, t, e, i) => {
  for (var r = i > 1 ? void 0 : i ? e9(t, e) : t, a = s.length - 1, o; a >= 0; a--)
    (o = s[a]) && (r = (i ? o(t, e, r) : o(r)) || r);
  return i && r && t9(t, e, r), r;
}, "__decorateClass$v"), Fg = /* @__PURE__ */ n((s, t, e) => t.has(s) || zg("Cannot " + e), "__accessCheck$8"), kn = /* @__PURE__ */ n((s, t, e) => (Fg(s, t, "read from private field"), e ? e.call(s) : t.get(s)), "__privateGet$8"), ry = /* @__PURE__ */ n((s, t, e) => t.has(s) ? zg("Cannot add the same private member more than once") : t instanceof WeakSet ? t.add(s) : t.set(s, e), "__privateAdd$8"), sy = /* @__PURE__ */ n((s, t, e, i) => (Fg(s, t, "write to private field"), t.set(s, e), e), "__privateSet$6"), Fl, Vh, En, dc;
let is = (dc = class extends cr {
  constructor() {
    super(...arguments), this.exportParts = new W(this), this.postRealtime = new yd(this), ry(this, Fl), ry(this, Vh, new Promise((t) => {
      sy(this, Fl, t);
    })), ry(this, En, new ve(this)), this.editing = !1;
  }
  /**
   * Resolves when app data is available.
   *
   * @returns {Promise<AppType>}
   */
  async whenApp() {
    return await kn(this, Vh);
  }
  /**
   * Emit a `vote` event scoped to the post.
   *
   * @internal
   * @param optionId - Identifier of the selected poll option.
   * @returns {boolean} True if the event was not canceled.
   */
  dispatchVote(t) {
    const e = new CustomEvent("vote", {
      detail: { optionId: t, parentId: this.post.id, parentType: "posts" }
    });
    return this.dispatchEvent(e);
  }
  /**
   * Emit a `subscribe` event toggling post subscription.
   *
   * @internal
   * @param subscribe - Desired subscription state.
   * @returns {boolean} True if the event was not canceled.
   */
  dispatchSubscribe(t) {
    const e = new CustomEvent("subscribe", {
      detail: { id: this.post.id, subscribe: t }
    });
    return this.dispatchEvent(e);
  }
  /**
   * Emit a `trash` event requesting the post to be trashed.
   *
   * @internal
   * @returns {boolean} True if the event was not canceled.
   */
  dispatchTrash() {
    const t = new CustomEvent("trash", { detail: { id: this.post.id } });
    return this.dispatchEvent(t);
  }
  /**
   * Emit a `restore` event requesting the post to be restored.
   *
   * @internal
   * @returns {boolean} True if the event was not canceled.
   */
  dispatchRestore() {
    const t = new CustomEvent("restore", { detail: { id: this.post.id } });
    return this.dispatchEvent(t);
  }
  willUpdate(t) {
    var e;
    (t.has("uid") || t.has("weavy")) && (this.uid && this.weavy ? kn(this, En).trackQuery(u1(this.weavy, this.uid)) : kn(this, En).untrackQuery()), kn(this, En).result?.isPending || (this.app = kn(this, En).result?.data), t.has("app") && this.app && (t.get("app") && sy(this, Vh, new Promise((i) => {
      sy(this, Fl, i);
    })), (e = kn(this, Fl)) == null || e.call(this, this.app));
  }
  render() {
    return h`
      ${this.post.is_trashed ? h`<wy-post-trashed
            id=${`post-trashed-${this.post.id}`}
            .post=${this.post}
            @restore=${() => {
      this.dispatchRestore();
    }}
          ></wy-post-trashed> ` : g}
      ${!this.post.is_trashed && this.editing ? h`<wy-post-edit
            id=${`post-edit-${this.post.id}`}
            .post=${this.post}
            @edit=${(t) => {
      this.editing = t.detail.edit;
    }}
          ></wy-post-edit> ` : g}
      ${!this.post.is_trashed && !this.editing ? h`<wy-post-view
            id=${`post-view-${this.post.id}`}
            .post=${this.post}
            @edit=${(t) => {
      this.editing = t.detail.edit;
    }}
            @subscribe=${(t) => {
      this.dispatchSubscribe(t.detail.subscribe);
    }}
            @trash=${() => {
      this.dispatchTrash();
    }}
            @vote=${(t) => {
      this.dispatchVote(t.detail.optionId);
    }}
          ></wy-post-view> ` : g}
    `;
  }
  updated(t) {
    (t.has("weavy") || t.has("app") || t.has("uid") || t.has("componentFeatures") || t.has("user")) && this.weavy && this.uid && this.app && this.app?.id !== t.get("app")?.id && this.componentFeatures && this.user && this.postRealtime.track(
      Tg(this.weavy, this.componentFeatures, this.app, this.user, this.post)
    );
  }
}, n(dc, "WyPost"), dc);
Fl = /* @__PURE__ */ new WeakMap();
Vh = /* @__PURE__ */ new WeakMap();
En = /* @__PURE__ */ new WeakMap();
is.styles = [Lt];
ch([
  y({ attribute: !1 })
], is.prototype, "post", 2);
ch([
  Ui({ context: bu }),
  V()
], is.prototype, "app", 2);
ch([
  y({ converter: $u })
], is.prototype, "uid", 2);
ch([
  V()
], is.prototype, "editing", 2);
is = ch([
  U("wy-post")
], is);
var i9 = Object.defineProperty, r9 = Object.getOwnPropertyDescriptor, Ku = /* @__PURE__ */ n((s, t, e, i) => {
  for (var r = i > 1 ? void 0 : i ? r9(t, e) : t, a = s.length - 1, o; a >= 0; a--)
    (o = s[a]) && (r = (i ? o(t, e, r) : o(r)) || r);
  return i && r && i9(t, e, r), r;
}, "__decorateClass$u"), hc;
let tn = (hc = class extends qt {
  constructor() {
    super(...arguments), this.exportParts = new W(this), this.postsQuery = new ar(this), this.postsRealtime = new yd(this), this.infiniteScroll = new or(this), this.pagerRef = J(), this.unreadPostsController = new aw(this), this.feed = !1, this.filter = {
      app: void 0,
      q: "",
      tag: "",
      trashed: !1,
      following: !1,
      order_by: void 0
    };
  }
  /** Current unread conversation count. */
  get unread() {
    return this.unreadPostsController.unread;
  }
  async willUpdate(t) {
    if (super.willUpdate(t), (t.has("weavy") || t.has("app") || t.has("feed") || t.has("filter")) && this.weavy && (this.app || this.feed) && this.filter) {
      const e = this.app && !this.feed ? this.app.id : "feed";
      await this.postsQuery.trackInfiniteQuery(q$(this.weavy, e, this.filter));
    }
    if (this.postsQuery.result.data) {
      let e = this.lastPostId;
      pi(this.postsQuery.result.data).forEach((i) => {
        (!e || i.id > e) && (e = i.id);
      }), this.lastPostId = e;
    }
    if ((t.has("weavy") || t.has("app") || t.has("feed")) && this.weavy && (this.feed || this.app)) {
      const e = !this.feed && this.app ? this.app : "feed", i = !this.feed && this.app?.id || "feed";
      this.subscribePostMutation = N$(this.weavy, e), this.removePostMutation = V$(this.weavy, e), this.restorePostMutation = U$(this.weavy, e), this.pollMutation = Tw(this.weavy, i, ["posts", i]);
    }
    if ((t.has("filter") || t.has("lastPostId") || t.has("app") || t.has("feed")) && (this.feed || this.app)) {
      const e = this.app && !this.feed ? this.app : "feed";
      this.unreadPostsController.track(e, this.lastPostId, this.filter);
    }
  }
  render() {
    const { data: t, isPending: e, hasNextPage: i } = this.postsQuery.result ?? {}, r = pi(t);
    return e ? h`<wy-empty><wy-progress-circular indeterminate padded></wy-progress-circular></wy-empty> ` : h`
          <div
            part="wy-banner wy-banner-action wy-transition ${this.unread ? "wy-show" : ""}"
            tabindex=${this.unread ? 0 : -1}
            @click=${async () => {
      this.shadowRoot?.firstElementChild && await w1("top", this.shadowRoot?.firstElementChild, !0), this.weavy?.queryClient.invalidateQueries({ queryKey: ["posts", this.feed ? "feed" : this.app?.id], exact: !1 });
    }}
            @keydown=${ne}
            @keyup=${ke}
          >
            ${x("New posts")}
          </div>

          ${r ? Be(
      r,
      (a) => a.id,
      (a) => {
        const o = this.feed && a.app ? a.app.id : void 0, c = this.feed ? void 0 : this.app;
        return h`<wy-post
                    id="post-${a.id}"
                    .uid=${o}
                    .app=${c}
                    .post=${a}
                    @subscribe=${(l) => {
          this.subscribePostMutation?.mutate({
            id: l.detail.id,
            subscribe: l.detail.subscribe
          });
        }}
                    @trash=${(l) => {
          this.removePostMutation?.mutate({ id: l.detail.id });
        }}
                    @restore=${(l) => {
          this.restorePostMutation?.mutate({ id: l.detail.id });
        }}
                    @vote=${(l) => {
          l.detail.parentType && l.detail.parentId && this.pollMutation?.mutate({
            optionId: l.detail.optionId,
            parentType: l.detail.parentType,
            parentId: l.detail.parentId
          });
        }}
                  ></wy-post>`;
      }
    ) : h`<wy-empty></wy-empty>`}
          ${i ? h`<div ${K(this.pagerRef)} part="wy-pager wy-pager-bottom"></div>` : g}
        `;
  }
  updated(t) {
    super.update(t), (t.has("weavy") || t.has("app") || t.has("componentFeatures") || t.has("user") || t.has("feed")) && this.weavy && this.app && this.app?.id !== t.get("app")?.id && this.componentFeatures && this.user && !this.feed && this.postsRealtime.track(Tg(this.weavy, this.componentFeatures, this.app, this.user)), this.infiniteScroll.observe(this.postsQuery.result, this.pagerRef.value);
  }
}, n(hc, "WyPostList"), hc);
tn.styles = [oh, Bu, kr, Y, T1];
Ku([
  V()
], tn.prototype, "lastPostId", 2);
Ku([
  y({ type: Boolean })
], tn.prototype, "feed", 2);
Ku([
  y({
    type: Object,
    hasChanged: /* @__PURE__ */ n((s, t) => Ze(s) && Ze(t) && !zs(s, t), "hasChanged")
  })
], tn.prototype, "filter", 2);
tn = Ku([
  U("wy-post-list"),
  ut()
], tn);
var s9 = Object.defineProperty, n9 = Object.getOwnPropertyDescriptor, Ig = /* @__PURE__ */ n((s, t, e, i) => {
  for (var r = i > 1 ? void 0 : i ? n9(t, e) : t, a = s.length - 1, o; a >= 0; a--)
    (o = s[a]) && (r = (i ? o(t, e, r) : o(r)) || r);
  return i && r && s9(t, e, r), r;
}, "__decorateClass$t"), uc;
let md = (uc = class extends qt {
  constructor() {
    super(...arguments), this.exportParts = new W(this), this.addPostMutation = new Ue(this);
  }
  /**
   * Handle editor submissions by dispatching the create-post mutation.
   *
   * @param e - Submitted editor data.
   */
  async handleSubmit(t) {
    const e = await this.whenApp(), i = await this.whenUser();
    this.addPostMutation.mutate({
      app_id: e.id,
      text: t.detail.text,
      meeting_id: t.detail.meetingId,
      blobs: t.detail.blobs,
      poll_options: t.detail.pollOptions,
      embed_id: t.detail.embedId,
      user: i,
      context: t.detail.contextData
    });
  }
  async willUpdate(t) {
    super.willUpdate(t), (t.has("weavy") || t.has("app") || t.has("componentFeatures")) && this.weavy && this.app && this.app?.id !== t.get("app")?.id && await this.addPostMutation.trackMutation(j$(this.weavy, ["posts", this.app.id]));
  }
  render() {
    return h`
      <div part="wy-posts-header">
        <wy-editor-msg
          editorLocation="apps"
          ?disabled=${!Zc(Vs.Create, this.app?.permissions)}
          .typing=${!1}
          .draft=${!0}
          placeholder=${this.placeholder ?? x("Create a post...")}
          buttonText=${x("Post")}
          @submit=${(t) => this.handleSubmit(t)}
        >
          <slot name="actions" slot="actions"></slot>

          ${this.apps && this.apps.length > 1 ? h`
                <wy-dropdown slot="inputs">
                  ${this.apps.map(
      (t) => h`
                      ${t.id === this.apps?.current?.id ? h`<label slot="button-content">${t.name}</label>` : g}
                      <wy-dropdown-option
                        ?selected=${t.id === this.apps?.current?.id}
                        @click="${() => {
        this.apps && (this.apps.current = t);
      }}"
                        >${t.name}</wy-dropdown-option
                      >
                    `
    )}
                  <wy-icon last slot="button-content" name="menu-down"></wy-icon>
                </wy-dropdown>
              ` : g}
        </wy-editor-msg>
        <wy-context-data-progress></wy-context-data-progress>
      </div>
    `;
  }
}, n(uc, "WyPostForm"), uc);
md.styles = [oh, Bu, kr, Y];
Ig([
  y()
], md.prototype, "placeholder", 2);
md = Ig([
  U("wy-post-form"),
  ut()
], md);
var a9 = Object.defineProperty, o9 = Object.getOwnPropertyDescriptor, Dg = /* @__PURE__ */ n((s) => {
  throw TypeError(s);
}, "__typeError$7"), $l = /* @__PURE__ */ n((s, t, e, i) => {
  for (var r = i > 1 ? void 0 : i ? o9(t, e) : t, a = s.length - 1, o; a >= 0; a--)
    (o = s[a]) && (r = (i ? o(t, e, r) : o(r)) || r);
  return i && r && a9(t, e, r), r;
}, "__decorateClass$s"), c9 = /* @__PURE__ */ n((s, t, e) => t.has(s) || Dg("Cannot " + e), "__accessCheck$7"), hv = /* @__PURE__ */ n((s, t, e) => (c9(s, t, "read from private field"), e ? e.call(s) : t.get(s)), "__privateGet$7"), l9 = /* @__PURE__ */ n((s, t, e) => t.has(s) ? Dg("Cannot add the same private member more than once") : t instanceof WeakSet ? t.add(s) : t.set(s, e), "__privateAdd$7"), Hh, ow, pc;
let yr = (pc = class extends ae {
  constructor() {
    super(...arguments), l9(this, Hh), this.appType = rt.Posts, this.componentFeatures = new Gt({
      // All available features as enabled/disabled by default
      [_.Attachments]: !0,
      [_.CloudFiles]: !0,
      [_.Comments]: !0,
      [_.ContextData]: !0,
      [_.Embeds]: !0,
      [_.Follow]: !0,
      [_.GoogleMeet]: !1,
      [_.Meetings]: !1,
      [_.Mentions]: !0,
      [_.MicrosoftTeams]: !1,
      [_.Polls]: !0,
      [_.Previews]: !0,
      [_.Reactions]: !0,
      [_.Typing]: !1,
      // Has no effect currently
      [_.ZoomMeetings]: !1
    }), this.theme = new ce(this, yr.styles), this.postListRef = J(), this.search = !1, this.following = !1;
  }
  /**
   * Matches apps and checks if the cache has the post.
   * @internal
   */
  matchesLink(t) {
    const e = (
      // Normal contextual app
      t && t.app && (this.uids.some(
        (r) => typeof r == "string" && t.app?.uid === r || // Normal app with app uid
        typeof r == "number" && t.app?.id === r
        // Normal app with app id
      ) || this.apps?.some((r) => r.id === t.app?.id))
    ), i = t && !this.uids.length && !this.apps?.length;
    if (e)
      return !0;
    if (this.weavy && i) {
      const r = t.type === It.Post ? t.id : t.type === It.Comment && t.parent?.type === It.Post ? t.parent.id : void 0, a = Qd({ ...hv(this, Hh, ow) });
      return !!(r && Yd(this.weavy.queryClient, ["posts", "feed", a], r));
    }
    return !1;
  }
  /** Current unread posts count. */
  get unread() {
    return this.postListRef.value?.unread || 0;
  }
  render() {
    return h`
      <wy-buttons position="floating" reverse><slot name="actions"></slot></wy-buttons>
      <wy-container part="wy-feed" padded outer gap="xl">
        <wy-post-form .placeholder=${this.placeholder}></wy-post-form>
        ${this.search ? h`
              <wy-buttons position="sticky">
                <wy-search
                  compact
                  query=${this.query || void 0}
                  @search=${(t) => this.query = t.detail.query}
                ></wy-search>
                <slot name="actions"></slot>
              </wy-buttons>
            ` : g}
        ${this.uids.length ? h`
              <wy-post-list
                ${K(this.postListRef)}
                ?feed=${this.uids.length > 1 || this.apps && this.apps.length > 1}
                .filter=${hv(this, Hh, ow)}
              ></wy-post-list>
            ` : g}
      </wy-container>
      <wy-user-card .listenTo=${this}></wy-user-card>
    `;
  }
}, n(pc, "WyFeed"), pc);
Hh = /* @__PURE__ */ new WeakSet();
ow = /* @__PURE__ */ n(function() {
  return {
    app: this.uids,
    q: this.query || void 0,
    tag: this.tag || void 0,
    trashed: !1,
    following: this.following,
    order_by: "id desc"
  };
}, "filter_get$1");
yr.styles = [Kt, bi, Cr, $r, ls, Pe];
$l([
  y({ type: Boolean })
], yr.prototype, "search", 2);
$l([
  y({ type: Boolean })
], yr.prototype, "following", 2);
$l([
  y()
], yr.prototype, "query", 2);
$l([
  y()
], yr.prototype, "tag", 2);
$l([
  y()
], yr.prototype, "placeholder", 2);
yr = $l([
  U("wy-feed"),
  ut()
], yr);
var d9 = Object.defineProperty, h9 = Object.getOwnPropertyDescriptor, Vg = /* @__PURE__ */ n((s) => {
  throw TypeError(s);
}, "__typeError$6"), kl = /* @__PURE__ */ n((s, t, e, i) => {
  for (var r = i > 1 ? void 0 : i ? h9(t, e) : t, a = s.length - 1, o; a >= 0; a--)
    (o = s[a]) && (r = (i ? o(t, e, r) : o(r)) || r);
  return i && r && d9(t, e, r), r;
}, "__decorateClass$r"), u9 = /* @__PURE__ */ n((s, t, e) => t.has(s) || Vg("Cannot " + e), "__accessCheck$6"), uv = /* @__PURE__ */ n((s, t, e) => (u9(s, t, "read from private field"), e ? e.call(s) : t.get(s)), "__privateGet$6"), p9 = /* @__PURE__ */ n((s, t, e) => t.has(s) ? Vg("Cannot add the same private member more than once") : t instanceof WeakSet ? t.add(s) : t.set(s, e), "__privateAdd$6"), Uh, cw, yc;
let wr = (yc = class extends ae {
  constructor() {
    super(...arguments), p9(this, Uh), this.appType = rt.Posts, this.componentFeatures = new Gt({
      // All available features as enabled/disabled by default
      [_.Attachments]: !0,
      [_.CloudFiles]: !0,
      [_.Comments]: !0,
      [_.ContextData]: !0,
      [_.Embeds]: !0,
      [_.Follow]: !0,
      [_.GoogleMeet]: !1,
      [_.Meetings]: !1,
      [_.Mentions]: !0,
      [_.MicrosoftTeams]: !1,
      [_.Polls]: !0,
      [_.Previews]: !0,
      [_.Reactions]: !0,
      [_.Typing]: !1,
      // Has no effect currently
      [_.ZoomMeetings]: !1
    }), this.theme = new ce(this, wr.styles), this.postListRef = J(), this.search = !1, this.following = !1;
  }
  /**
   * Matches apps and checks if the cache has the post.
   * @internal
   */
  matchesLink(t) {
    const e = (
      // Normal contextual app
      t && t.app && (this.uids.some(
        (r) => typeof r == "string" && t.app?.uid === r || // Normal app with app uid
        typeof r == "number" && t.app?.id === r
        // Normal app with app id
      ) || this.apps?.some((r) => r.id === t.app?.id))
    ), i = t && !this.uids.length && !this.apps?.length;
    if (e)
      return !0;
    if (this.weavy && i) {
      const r = t.type === It.Post ? t.id : t.type === It.Comment && t.parent?.type === It.Post ? t.parent.id : void 0, a = Qd({ ...uv(this, Uh, cw) });
      return !!(r && Yd(this.weavy.queryClient, ["posts", "feed", a], r));
    }
    return !1;
  }
  /** Current unread posts count. */
  get unread() {
    return this.postListRef.value?.unread || 0;
  }
  render() {
    return h`
      ${this.search ? g : h`<wy-buttons position="floating" reverse><slot name="actions"></slot></wy-buttons>`}
      <wy-container part="wy-posts" padded outer gap="xl">
        ${this.search ? h`
              <wy-buttons position="sticky">
                <wy-search
                  compact
                  query=${this.query || void 0}
                  @search=${(t) => this.query = t.detail.query}
                ></wy-search>
                <slot name="actions"></slot>
              </wy-buttons>
            ` : g}
        <wy-post-list
          ?feed=${this.uids.length !== 1 || this.apps && this.apps.length > 1}
          ${K(this.postListRef)}
          .filter=${uv(this, Uh, cw)}
        ></wy-post-list>
      </wy-container>
      <wy-user-card .listenTo=${this}></wy-user-card>
    `;
  }
}, n(yc, "WyPosts"), yc);
Uh = /* @__PURE__ */ new WeakSet();
cw = /* @__PURE__ */ n(function() {
  return {
    app: this.uids,
    q: this.query || void 0,
    tag: this.tag || void 0,
    trashed: !1,
    following: this.following,
    order_by: "id desc"
  };
}, "filter_get");
wr.styles = [Kt, Cr, bi, $r, ls, Pe];
kl([
  y({ type: Boolean })
], wr.prototype, "search", 2);
kl([
  y({ type: Boolean })
], wr.prototype, "following", 2);
kl([
  y()
], wr.prototype, "query", 2);
kl([
  y()
], wr.prototype, "tag", 2);
kl([
  y()
], wr.prototype, "placeholder", 2);
wr = kl([
  U("wy-posts")
], wr);
var y9 = Object.defineProperty, w9 = Object.getOwnPropertyDescriptor, Hg = /* @__PURE__ */ n((s, t, e, i) => {
  for (var r = i > 1 ? void 0 : i ? w9(t, e) : t, a = s.length - 1, o; a >= 0; a--)
    (o = s[a]) && (r = (i ? o(t, e, r) : o(r)) || r);
  return i && r && y9(t, e, r), r;
}, "__decorateClass$q"), wc;
let vd = (wc = class extends ae {
  constructor() {
    super(...arguments), this.appType = rt.Posts, this.componentFeatures = new Gt({
      // All available features as enabled/disabled by default
      [_.Attachments]: !0,
      [_.CloudFiles]: !0,
      [_.ContextData]: !0,
      [_.Embeds]: !0,
      [_.GoogleMeet]: !1,
      [_.Meetings]: !1,
      [_.Mentions]: !0,
      [_.MicrosoftTeams]: !1,
      [_.Polls]: !0,
      [_.ZoomMeetings]: !1
    }), this.theme = new ce(this, vd.styles);
  }
  /**
   * Disabled link matching/consuming.
   * @internal
   */
  matchesLink() {
    return !1;
  }
  render() {
    return h`
      <wy-post-form .placeholder=${this.placeholder}>
        <slot name="actions" slot="actions"></slot>
      </wy-post-form>
    `;
  }
}, n(wc, "WyPostEditor"), wc);
vd.styles = [Kt, bi, $r, Pe];
Hg([
  y()
], vd.prototype, "placeholder", 2);
vd = Hg([
  U("wy-post-editor")
], vd);
var f9 = Object.defineProperty, m9 = Object.getOwnPropertyDescriptor, lh = /* @__PURE__ */ n((s, t, e, i) => {
  for (var r = i > 1 ? void 0 : i ? m9(t, e) : t, a = s.length - 1, o; a >= 0; a--)
    (o = s[a]) && (r = (i ? o(t, e, r) : o(r)) || r);
  return i && r && f9(t, e, r), r;
}, "__decorateClass$p"), fc;
let rs = (fc = class extends qt {
  constructor() {
    super(...arguments), this.exportParts = new W(this), this.order = { by: "name", descending: !1 }, this.view = "list", this.showTrashed = !1, this.showUploadSheet = !1, this.mutatingFiles = new Wc(
      this
    ), this.previousFailedFileMutations = [], this.fileInputRef = J(), this.cloudFilesRef = J(), this.openFileInput = () => {
      this.fileInputRef.value?.click();
    }, this.openCloudFiles = () => {
      this.cloudFilesRef.value?.open();
    };
  }
  // remove file attachment
  /**
   * Remove (or abort) an ongoing upload mutation.
   *
   * @internal
   * @param mutationState - Mutation state to remove.
   */
  async handleRemoveMutation(t) {
    const e = await this.whenWeavy(), i = await this.whenApp();
    t.status === "pending" && Fy(t.variables) && t.variables.abort?.();
    const r = t.status === "error" && !t.variables?.blob || t.status === "pending" ? ["apps", i.id, "blobs"] : ["apps", i.id, "files"];
    l1(
      e.queryClient,
      r,
      (a) => a.state.submittedAt === t.submittedAt
    );
  }
  /**
   * Retry an upload by replacing the existing file.
   *
   * @internal
   * @param mutation - Failed mutation carrying the blob data.
   */
  handleOverwriteUpload(t) {
    const e = t.variables?.blob;
    e && this.dispatchCreateFiles([e], !0);
  }
  /**
   * Dispatch an `upload-files` event with the selected files.
   *
   * @param files - Files chosen from the native picker.
   * @returns Whether the event was not canceled.
   */
  dispatchUploadFiles(t) {
    this.weavy && this.app && (h2(this.weavy, this.app), Dy(this.mutatingFiles.result) === "ok" && Vy(this.weavy, this.app));
    const e = new CustomEvent("upload-files", {
      detail: { files: t }
    });
    return this.dispatchEvent(e);
  }
  /**
   * Emit an `external-blobs` event with selected cloud blobs.
   *
   * @internal
   * @param externalBlobs - Blobs returned from the cloud picker.
   * @returns {boolean} True if the event was not canceled.
   */
  dispatchExternalBlobs(t) {
    const e = new CustomEvent("external-blobs", {
      detail: { externalBlobs: t }
    });
    return this.dispatchEvent(e);
  }
  /**
   * Emit a `create-files` event for programmatic uploads.
   *
   * @internal
   * @param blobs - Blobs to create files from.
   * @param replace - When true, replace the existing file.
   * @returns {boolean} True if the event was not canceled.
   */
  dispatchCreateFiles(t, e = !1) {
    const i = new CustomEvent("create-files", {
      detail: { blobs: t, replace: e }
    });
    return this.dispatchEvent(i);
  }
  /**
   * Emit an `order` event reflecting the chosen sort order.
   *
   * @internal
   * @param order - Order to apply.
   * @returns {boolean} True if the event was not canceled.
   */
  dispatchOrder(t) {
    const e = new CustomEvent("order", { detail: { order: t } });
    return this.dispatchEvent(e);
  }
  /**
   * Emit a `view` event for changing layout mode.
   *
   * @internal
   * @param view - Layout to activate.
   * @returns {boolean} True if the event was not canceled.
   */
  dispatchView(t) {
    const e = new CustomEvent("view", { detail: { view: t } });
    return this.dispatchEvent(e);
  }
  /**
   * Emit a `show-trashed` event to toggle trashed visibility.
   *
   * @internal
   * @param showTrashed - Whether to show trashed files.
   * @returns {boolean} True if the event was not canceled.
   */
  dispatchShowTrashed(t) {
    const e = new CustomEvent("show-trashed", {
      detail: { showTrashed: t }
    });
    return this.dispatchEvent(e);
  }
  /**
   * Emit a `subscribe` event to toggle app subscription.
   *
   * @internal
   * @param subscribe - Desired subscription state.
   * @returns {boolean} True if the event was not canceled.
   */
  dispatchSubscribe(t) {
    const e = new CustomEvent("subscribe", { detail: { subscribe: t } });
    return this.dispatchEvent(e);
  }
  async willUpdate(t) {
    super.willUpdate(t), t.has("app") && this.app && this.app.id !== t.get("app")?.id && this.weavy && Vy(this.weavy, this.app), (t.has("weavy") || t.has("app")) && this.weavy && this.app && await this.mutatingFiles.trackMutationState(
      {
        filters: {
          mutationKey: ["apps", this.app.id],
          exact: !1
        }
      },
      this.weavy.queryClient
    );
  }
  renderFileMutation(t) {
    if (t.context?.file) {
      const e = t.context.file, i = {
        ...t.context.status
      };
      return t.context.status.state === "conflict" && (i.text = x("Replace existing file?")), h`
        <wy-file-item
          rounded
          .file=${t.context?.file}
          .status=${i}
          .actionType=${t.context.type}
          title="${Sv(t.context.type)}: ${e.name}"
        >
          <span slot="title">${e.name}</span>
          ${t.context.status.state === "conflict" && i.text ? h`: <span slot="actions" title=${i.text}><em>${i.text}</em></span>` : g}
          ${i.state === "conflict" && t.variables?.blob ? h`
                <wy-button
                  slot="actions"
                  kind="icon"
                  @click=${() => this.handleOverwriteUpload(
        t
      )}
                  title=${x("Replace")}
                >
                  <wy-icon name="check"></wy-icon>
                </wy-button>
              ` : g}
          ${i.state === "pending" ? h`
                <wy-progress-circular
                  slot="actions"
                  padded
                  ?indeterminate=${!i.progress}
                  .max=${100}
                  .value=${i.progress || 0}
                ></wy-progress-circular>

                ${Fy(t.variables) ? h`<wy-button slot="actions" kind="icon" @click=${() => this.handleRemoveMutation(t)}>
                      <wy-icon name="close"></wy-icon>
                    </wy-button>` : g}
              ` : h`<wy-button slot="actions" kind="icon" @click=${() => this.handleRemoveMutation(t)}>
                <wy-icon name="close"></wy-icon>
              </wy-button> `}
        </wy-file-item>
      `;
    }
    return g;
  }
  render() {
    const t = this.mutatingFiles.result, e = Cm(t), i = xm(t), r = d1(t), a = Dy(t);
    return h`
      <header part="wy-files-header wy-header wy-header-outer">
        <nav part="wy-files-header-toolbar wy-toolbar">
          <div part="wy-toolbar-buttons">
            ${Zc(Vs.Create, this.app?.permissions) ? h`
                  <wy-dropdown title=${x("Add files")}>
                    <wy-icon slot="button-content" name="plus" first></wy-icon>
                    <span slot="button-content">${x("Add files")}</span>
                    <wy-dropdown-item @click=${this.openFileInput} title=${x("From device")}>
                      <wy-icon name="attachment"></wy-icon>
                      <span>${x("From device")}</span>
                    </wy-dropdown-item>
                    <input
                      type="file"
                      data-testid="uploadFile"
                      ${K(this.fileInputRef)}
                      @click=${(o) => o.stopPropagation()}
                      @change=${(o) => {
      this.dispatchUploadFiles(o.target.files) && (o.target.value = "");
    }}
                      multiple
                      hidden
                      tabindex="-1"
                    />
                    ${this.componentFeatures?.allowsFeature(_.CloudFiles) ? h`
                          <wy-dropdown-item @click=${this.openCloudFiles} title=${x("From cloud")}>
                            <wy-icon name="cloud"></wy-icon>
                            <span>${x("From cloud")}</span>
                          </wy-dropdown-item>
                        ` : g}
                  </wy-dropdown>
                ` : g}
            ${t?.length ? h`
                  <wy-button
                    kind="icon"
                    @click=${() => {
      this.showUploadSheet = !this.showUploadSheet;
    }}
                    title=${a === "conflict" ? x("File conflict") : a === "error" ? x("Upload error") : a === "pending" ? x("Pending") : x("All uploads finished")}
                  >
                    ${a === "conflict" ? h`<wy-icon name="alert" color="yellow"></wy-icon>` : a === "error" ? h`<wy-icon name="alert-octagon" color="error"></wy-icon>` : a === "pending" ? h`<wy-progress-circular
                          ?indeterminate=${r.percent === null}
                          .value=${r.loaded}
                          .max=${r.total}
                        ></wy-progress-circular>` : h`<wy-icon name="check"></wy-icon>`}
                  </wy-button>
                ` : g}
          </div>
          <div part="wy-toolbar-buttons wy-toolbar-buttons-last">
            <slot name="actions"></slot>

            <wy-dropdown icon="sort" title="Sort items by" directionX="left">
              <wy-dropdown-option
                ?selected=${this.order.by === "name"}
                @click=${() => this.dispatchOrder({ ...this.order, by: "name" })}
              >
                ${x("Name")}
              </wy-dropdown-option>
              <wy-dropdown-option
                ?selected=${this.order.by === "updated_at"}
                @click=${() => this.dispatchOrder({ ...this.order, by: "updated_at" })}
              >
                ${x("Modified")}
              </wy-dropdown-option>
              <wy-dropdown-option
                ?selected=${this.order.by === "size"}
                @click=${() => this.dispatchOrder({ ...this.order, by: "size" })}
              >
                ${x("Size")}
              </wy-dropdown-option>
              <wy-dropdown-divider></wy-dropdown-divider>
              <wy-dropdown-option
                ?selected=${!this.order.descending}
                @click=${() => this.dispatchOrder({ ...this.order, descending: !1 })}
              >
                ${x("Ascending")}
              </wy-dropdown-option>
              <wy-dropdown-option
                ?selected=${this.order.descending}
                @click=${() => this.dispatchOrder({ ...this.order, descending: !0 })}
              >
                ${x("Descending")}
              </wy-dropdown-option>
            </wy-dropdown>

            <wy-dropdown
              icon=${this.view === "grid" ? "view-module-outline" : "view-list-outline"}
              title="View options"
              directionX="left"
            >
              <wy-dropdown-option ?selected=${this.view === "list"} @click=${() => this.dispatchView("list")}>
                ${x("List view")}
              </wy-dropdown-option>
              <wy-dropdown-option ?selected=${this.view === "grid"} @click=${() => this.dispatchView("grid")}>
                ${x("Grid view")}
              </wy-dropdown-option>
              <wy-dropdown-divider></wy-dropdown-divider>
              <wy-dropdown-option ?selected=${!this.showTrashed} @click=${() => this.dispatchShowTrashed(!1)}>
                ${x("Hide trashed")}
              </wy-dropdown-option>
              <wy-dropdown-option ?selected=${this.showTrashed} @click=${() => this.dispatchShowTrashed(!0)}>
                ${x("Show trashed")}
              </wy-dropdown-option>
            </wy-dropdown>

            <wy-dropdown directionX="left" ?disabled=${!this.app}>
              ${this.app?.is_subscribed ? h`<wy-dropdown-item @click=${() => this.dispatchSubscribe(!1)}>
                    <wy-icon name="bell-off"></wy-icon>
                    ${x("Unsubscribe")}
                  </wy-dropdown-item>` : h`<wy-dropdown-item @click=${() => this.dispatchSubscribe(!0)}>
                    <wy-icon name="bell"></wy-icon>
                    ${x("Subscribe")}
                  </wy-dropdown-item>`}
              ${this.app?.archive_url ? h`<wy-dropdown-item
                    @click=${() => rr(
      this.app?.archive_url,
      "_top",
      `${this.app?.uid ? this.app.uid : `${this.app?.type}-${this.app?.id}`}.zip`,
      !0
    )}
                  >
                    <wy-icon name="download"></wy-icon>
                    ${x("Download files")}
                  </wy-dropdown-item>` : g}
            </wy-dropdown>
          </div>
        </nav>

        <wy-context-data-progress></wy-context-data-progress>
      </header>

      ${this.weavy ? h`
            <wy-overlay
              type="sheet"
              .show=${this.showUploadSheet}
              @close=${() => {
      this.showUploadSheet = !1;
    }}
            >
              <span slot="title">${x("File actions")}</span>
              <wy-container scrollY padded>
                ${!e.length && !i.length ? h`
                      <wy-empty noNetwork><wy-container padded>${x("No pending uploads")}</wy-container></wy-empty>
                    ` : g}
                ${e.length ? h`
                      ${Be(
      e,
      (o) => "mutation" + o.submittedAt,
      (o) => this.renderFileMutation(o)
    )}
                    ` : g}
                ${i.length ? h`
                      ${Be(
      i,
      (o) => "mutation" + o.submittedAt,
      (o) => this.renderFileMutation(o)
    )}
                    ` : g}
              </wy-container>
            </wy-overlay>
          ` : g}

      <wy-cloud-files
        ${K(this.cloudFilesRef)}
        @external-blobs=${(o) => this.dispatchExternalBlobs(o.detail.externalBlobs)}
      ></wy-cloud-files>
    `;
  }
  updated(t) {
    super.updated(t);
    const e = Cm(this.mutatingFiles.result), i = xm(this.mutatingFiles.result);
    e.some(
      (a) => !this.previousFailedFileMutations.includes(a)
    ) ? this.showUploadSheet = !0 : this.showUploadSheet && !e.length && !i.length && setTimeout(() => {
      !e.length && !i.length && (this.showUploadSheet = !1);
    }, 1500), this.previousFailedFileMutations = e ?? [];
  }
}, n(fc, "WyFilesHeader"), fc);
rs.styles = [Bu, pg, Y];
lh([
  y({ type: Object })
], rs.prototype, "order", 2);
lh([
  y()
], rs.prototype, "view", 2);
lh([
  y({ type: Boolean })
], rs.prototype, "showTrashed", 2);
lh([
  V()
], rs.prototype, "showUploadSheet", 2);
rs = lh([
  U("wy-files-header"),
  ut()
], rs);
function v9(s, t, e = {}, i = {}) {
  if (!s)
    throw new Error("useFileList must be used within a WeavyContext");
  return {
    ...i,
    queryKey: ["apps", t, "files", e],
    initialPageParam: 0,
    queryFn: /* @__PURE__ */ n(async (a) => {
      const o = a.pageParam, c = !!e?.trashed, l = e.order ? e.order.by + (e.order.descending ? "+desc" : "") : "";
      let u = "/api/apps/" + t + "/files?skip=" + o + "&order_by=" + l;
      return c && (u += "&trashed=null"), await (await s.fetch(u)).json();
    }, "queryFn"),
    getNextPageParam: /* @__PURE__ */ n((a) => a.end && a.end < a.count ? a.end : null, "getNextPageParam")
  };
}
n(v9, "getInfiniteFileListOptions");
function g9(s, t) {
  const e = s.queryClient, r = {
    mutationKey: ["apps", t.id, "files"],
    mutationFn: /* @__PURE__ */ n(async ({ file: a }) => {
      if (a.id >= 1) {
        if (!(await s.fetch("/api/files/" + a.id + "/trash", { method: "POST" })).ok)
          throw new Error();
      } else
        throw new Error(`Could not trash ${a.name}.`);
    }, "mutationFn"),
    onMutate: /* @__PURE__ */ n((a) => (ht(
      e,
      { queryKey: r.mutationKey, exact: !1 },
      a.file.id,
      (o) => Object.assign(o, { is_trashed: !0 })
    ), { type: "trash", file: a.file, status: { state: "pending" } }), "onMutate"),
    onSuccess: /* @__PURE__ */ n((a, o) => {
      ht(
        e,
        { queryKey: r.mutationKey, exact: !1 },
        o.file.id,
        (c) => Object.assign(c, a)
      ), Bt(e, r.mutationKey, o, (c) => {
        c.status.state = "ok";
      });
    }, "onSuccess"),
    onError(a, o) {
      ht(
        e,
        { queryKey: r.mutationKey, exact: !1 },
        o.file.id,
        (c) => Object.assign(c, { is_trashed: !1 })
      ), Bt(e, r.mutationKey, o, (c) => {
        c.status.state = "error", c.status.text = a.message;
      });
    }
  };
  return r;
}
n(g9, "getTrashFileMutationOptions");
function b9(s, t) {
  return new St(s.queryClient, g9(s, t));
}
n(b9, "getTrashFileMutation");
function x9(s, t) {
  const e = s.queryClient, r = {
    mutationKey: ["apps", t.id, "files"],
    mutationFn: /* @__PURE__ */ n(async ({ file: a }) => {
      if (a.id >= 1) {
        const o = await s.fetch("/api/files/" + a.id + "/restore", { method: "POST" });
        if (!o.ok) {
          const c = await o.json();
          throw new Error(c.detail || c.title, { cause: c });
        }
      } else {
        const o = { status: 400, title: `Could not restore ${a.name}.` };
        throw new Error(o.detail || o.title, { cause: o });
      }
    }, "mutationFn"),
    onMutate: /* @__PURE__ */ n((a) => (ht(
      e,
      { queryKey: r.mutationKey, exact: !1 },
      a.file.id,
      (o) => Object.assign(o, { status: "pending" })
    ), { type: "restore", file: a.file, status: { state: "pending" } }), "onMutate"),
    onSuccess: /* @__PURE__ */ n((a, o) => {
      ht(
        e,
        { queryKey: r.mutationKey, exact: !1 },
        o.file.id,
        (c) => Object.assign(c, { is_trashed: !1, status: "ok" })
      ), Bt(e, r.mutationKey, o, (c) => {
        c.status.state = "ok";
      });
    }, "onSuccess"),
    onError(a, o) {
      ht(
        e,
        { queryKey: r.mutationKey, exact: !1 },
        o.file.id,
        (c) => Object.assign(c, { is_trashed: !0 })
      ), Bt(e, r.mutationKey, o, (c) => {
        c.status.state = "error", c.status.text = a.message;
      });
    }
  };
  return r;
}
n(x9, "getRestoreFileMutationOptions");
function C9(s, t) {
  return new St(s.queryClient, x9(s, t));
}
n(C9, "getRestoreFileMutation");
function $9(s, t) {
  const e = s.queryClient, r = {
    mutationKey: ["apps", t.id, "files"],
    mutationFn: /* @__PURE__ */ n(async ({ file: a }) => {
      if (a.id >= 1 && a.is_trashed) {
        const o = await s.fetch("/api/files/" + a.id, { method: "DELETE" });
        if (!o.ok) {
          const c = await o.json();
          throw new Error(c.detail || c.title, { cause: c });
        }
      } else {
        const o = { status: 400, title: `Could not delete ${a.name} forever.` };
        throw new Error(o.detail || o.title, { cause: o });
      }
    }, "mutationFn"),
    onMutate: /* @__PURE__ */ n((a) => (ht(
      e,
      { queryKey: r.mutationKey, exact: !1 },
      a.file.id,
      (o) => Object.assign(o, { status: "pending" })
    ), { type: "delete-forever", file: a.file, status: { state: "pending" } }), "onMutate"),
    onSuccess: /* @__PURE__ */ n((a, o) => {
      F2(e, { queryKey: r.mutationKey, exact: !1 }, o.file.id), Bt(e, r.mutationKey, o, (c) => {
        c.status.state = "ok";
      });
    }, "onSuccess"),
    onError(a, o) {
      ht(
        e,
        { queryKey: r.mutationKey, exact: !1 },
        o.file.id,
        (c) => Object.assign(c, { status: void 0 })
      ), Bt(e, r.mutationKey, o, (c) => {
        c.status.state = "error", c.status.text = a.message;
      });
    }
  };
  return r;
}
n($9, "getDeleteForeverFileMutationOptions");
function k9(s, t) {
  return new St(s.queryClient, $9(s, t));
}
n(k9, "getDeleteForeverFileMutation");
function _9(s, t) {
  const e = s.queryClient, i = ["apps", t.id, "files"];
  return {
    mutationKey: i,
    mutationFn: /* @__PURE__ */ n(async ({ file: a, subscribe: o }) => {
      if (a.id >= 1) {
        const c = await s.fetch(`/api/files/${a.id}/${o ? "subscribe" : "unsubscribe"}`, {
          method: "POST"
        });
        if (!c.ok) {
          const l = await c.json();
          throw new Error(l.detail || l.title, { cause: l });
        }
      } else
        throw new Error(`Could not ${o ? "subscribe" : "unsubscribe"} to ${a.name}.`);
    }, "mutationFn"),
    onMutate: /* @__PURE__ */ n((a) => (ht(
      e,
      { queryKey: i, exact: !1 },
      a.file.id,
      (o) => ({ ...o, is_subscribed: a.subscribe, status: "pending" })
    ), {
      type: a.subscribe ? "subscribe" : "unsubscribe",
      file: a.file,
      status: { state: "pending" }
    }), "onMutate"),
    onSuccess: /* @__PURE__ */ n((a, o) => {
      ht(
        e,
        { queryKey: i, exact: !1 },
        o.file.id,
        (c) => ({ ...c, status: "ok" })
      ), Bt(e, i, o, (c) => {
        c.status.state = "ok";
      });
    }, "onSuccess"),
    onError: /* @__PURE__ */ n((a, o) => {
      ht(
        e,
        { queryKey: i, exact: !1 },
        o.file.id,
        (c) => ({ ...c, is_subscribed: o.file.is_subscribed, status: "error" })
      ), Bt(e, i, o, (c) => {
        c.status.state = "error", c.status.text = a.message;
      });
    }, "onError")
  };
}
n(_9, "getSubscribeFileMutationOptions");
function P9(s, t) {
  return new St(s.queryClient, _9(s, t));
}
n(P9, "getSubscribeFileMutation");
function S9(s, t) {
  const e = s.queryClient, r = {
    mutationKey: ["apps", t.id, "files"],
    mutationFn: /* @__PURE__ */ n(async ({ file: a, name: o }) => {
      if (a.id >= 1) {
        const c = await s.fetch("/api/files/" + a.id, {
          method: "PATCH",
          body: JSON.stringify({
            name: o
          })
        });
        if (!c.ok) {
          const l = await c.json();
          throw new Error(l.detail || l.title, { cause: l });
        }
        return await c.json();
      } else
        throw new Error(`Could not rename ${a.name}`);
    }, "mutationFn"),
    onMutate: /* @__PURE__ */ n((a) => (ht(
      e,
      { queryKey: r.mutationKey, exact: !1 },
      a.file.id,
      (o) => Object.assign(o, { name: a.name })
    ), { type: "rename", file: a.file, status: { state: "pending" } }), "onMutate"),
    onSuccess: /* @__PURE__ */ n((a, o) => {
      ht(
        e,
        { queryKey: r.mutationKey, exact: !1 },
        o.file.id,
        (c) => Object.assign(c, a)
      ), Bt(e, r.mutationKey, o, (c) => {
        c.status.state = "ok";
      });
    }, "onSuccess"),
    onError: /* @__PURE__ */ n((a, o) => {
      ht(
        e,
        { queryKey: r.mutationKey, exact: !1 },
        o.file.id,
        (c) => Object.assign(c, { name: o.file.name })
      ), Bt(e, r.mutationKey, o, (c) => {
        c.status.state = "error", c.status.text = a.message;
      });
    }, "onError")
  };
  return r;
}
n(S9, "getRenameFileMutationOptions");
function M9(s, t) {
  return new St(s.queryClient, S9(s, t));
}
n(M9, "getRenameFileMutation");
const E9 = j`[part~=wy-grid]{display:grid;grid-auto-flow:dense;grid-template-columns:repeat(auto-fill,minmax(calc(10 * var(--wy-size, 1rem)),1fr));grid-gap:var(--wy-gap, var(--wy-padding, calc(.5 * var(--wy-size, 1rem))));padding:var(--wy-padding, calc(.5 * var(--wy-size, 1rem))) 0}`, A9 = j`@keyframes wy-highlight{0%{--wy-component-background-color: var(--wy-highlight, var(--wy-highlight-light, #e8f2ff));--wy-component-color: var(--wy-on-highlight, var(--wy-on-highlight-light, #001d32))}}[part~=wy-card]{--wy-component-background-color: var(--wy-surface-layer-high, var(--wy-surface-layer-high-light, rgba(0, 0, 0, .0745098039)));--wy-component-color: var(--wy-on-surface, var(--wy-on-surface-light, #181c20));--wy-component-border-radius: var(--wy-border-radius-sm, calc(var(--wy-border-radius, calc(.5 * var(--wy-size, 1rem))) - .25 * var(--wy-padding, calc(.5 * var(--wy-size, 1rem)))));background-color:var(--wy-component-background-color);color:var(--wy-component-color);border-radius:var(--wy-border-radius, calc(.5 * var(--wy-size, 1rem)));position:relative;display:flex;flex-direction:column;min-width:0;word-wrap:break-word;background-clip:border-box;padding:var(--wy-padding-sm, calc(.75 * var(--wy-padding, calc(.5 * var(--wy-size, 1rem)))))}[part~=wy-card][part~=wy-highlight]{animation:wy-highlight 3s 1.5s ease-in-out both}[part~=wy-card-primary]{--wy-component-background-color: var(--wy-primary-container, var(--wy-primary-container-light, #cee5ff));--wy-component-color: var(--wy-on-primary-container, var(--wy-on-primary-container-light, #0d4a73))}[part~=wy-trashed]{text-decoration:line-through;opacity:var(--wy-opacity-disabled, 38%)}[part~=wy-card-hover]{cursor:pointer;-webkit-user-select:none;user-select:none}[part~=wy-card-hover]:before{content:\"\";position:absolute;top:0;left:0;width:100%;height:100%;border-radius:inherit;background:var(--wy-component-color);opacity:0%}[part~=wy-card-hover]:hover:before{opacity:var(--wy-opacity-state-hover, 8%)}[part~=wy-card-hover]:focus:before{opacity:var(--wy-opacity-state-focus, 12%)}[part~=wy-card-hover]:active:before,[part~=wy-card-hover][part~=wy-active]:before{opacity:var(--wy-opacity-state-active, 12%)}[part~=wy-card-actions]{position:absolute;top:var(--wy-padding, calc(.5 * var(--wy-size, 1rem)));right:var(--wy-padding, calc(.5 * var(--wy-size, 1rem)));align-items:center;display:flex}[part~=wy-card-icon]{--wy-component-icon-width: calc(6 * var(--wy-size, 1rem));--wy-component-icon-height: calc(6 * var(--wy-size, 1rem));display:flex;align-items:center;justify-content:center;flex-direction:column;text-align:center;margin:auto}[part~=wy-card-image]{width:auto;height:auto;border-radius:var(--wy-component-border-radius);object-fit:cover;object-position:center;flex:1 1 100%;max-height:calc(10 * var(--wy-size, 1rem))}[part~=wy-card-image][part~=wy-loading]{transition:opacity .15s ease-out;opacity:0}[part~=wy-card-image][part~=wy-loading][part~=wy-loaded]{opacity:1;transition:opacity .15s ease-out}[part~=wy-card-image-top]{object-position:top center}[part~=wy-card-title]{padding:0 var(--wy-padding-sm, calc(.75 * var(--wy-padding, calc(.5 * var(--wy-size, 1rem)))));display:flex;align-items:center;justify-content:space-between;gap:var(--wy-gap-sm, calc(.5 * var(--wy-gap, var(--wy-padding, calc(.5 * var(--wy-size, 1rem))))));height:2.25rem;box-sizing:border-box}[part~=wy-input]{height:2.25rem;box-sizing:border-box;flex:0 1 auto}[part~=wy-card-text]{line-height:var(--wy-line-height, 1.5);flex:0 1 auto;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}[part~=wy-card-action]{flex:0 0 auto;white-space:nowrap}`, R9 = j`[part~=wy-badge]{display:inline-block;text-align:center;vertical-align:middle;align-self:center;font-weight:var(--wy-font-weight-bold, 600);font-size:var(--wy-font-size-xs, calc(.75 * var(--wy-font-size, 1em)));line-height:1.333333;color:var(--wy-on-primary-container, var(--wy-on-primary-container-light, #0d4a73));border-radius:var(--wy-border-radius-pill, var(--wy-border-radius, calc(2.5 * var(--wy-size, 1rem))));background-color:var(--wy-primary-container, var(--wy-primary-container-light, #cee5ff));padding:.333333em;min-width:1lh}[part~=wy-badge]:empty{display:none!important}[part~=wy-badge-positioned]{margin:calc(-.5 * (var(--wy-font-size-xs, calc(.75 * var(--wy-font-size, 1em))) + .333333em * 2));position:absolute}[part~=wy-badge-positioned]:not(:empty){display:block}[part~=wy-badge-top-right]{top:0;right:0}[part~=wy-badge-bottom-right]{bottom:0;right:0}[part~=wy-badge-bottom-left]{bottom:0;left:0}[part~=wy-badge-top-left]{top:0;left:0}[part~=wy-badge-compact]{padding:.25em;max-width:calc(var(--wy-font-size-xs, calc(.75 * var(--wy-font-size, 1em))) * 3 + .25em);height:calc(var(--wy-font-size-xs, calc(.75 * var(--wy-font-size, 1em))) + .25em);line-height:calc(var(--wy-font-size-xs, calc(.75 * var(--wy-font-size, 1em))) + .25em)}[part~=wy-badge-compact]{overflow:hidden;text-overflow:ellipsis;white-space:nowrap}[part~=wy-badge-compact][part~=wy-badge-positioned]{margin:calc(-.5 * (var(--wy-font-size-xs, calc(.75 * var(--wy-font-size, 1em))) + .25em * 2))}[part~=wy-badge-dot]{content-visibility:hidden;line-height:0;min-width:0;height:0;width:0;padding:var(--wy-padding-sm, calc(.75 * var(--wy-padding, calc(.5 * var(--wy-size, 1rem)))))}[part~=wy-badge-dot][part~=wy-badge-positioned]{margin:calc(-1 * var(--wy-padding-sm, calc(.75 * var(--wy-padding, calc(.5 * var(--wy-size, 1rem))))))}[part~=wy-badge-reveal]{opacity:1;transition:opacity var(--wy-transition, var(--wy-transition-duration, .2s) var(--wy-transition-curve, cubic-bezier(.4, 0, .2, 1))) calc(var(--wy-transition-duration, .2s) + var(--wy-transition-reveal-delay, 1s))}@starting-style{[part~=wy-badge-reveal]{opacity:0}}`;
var T9 = Object.defineProperty, L9 = Object.getOwnPropertyDescriptor, _l = /* @__PURE__ */ n((s, t, e, i) => {
  for (var r = i > 1 ? void 0 : i ? L9(t, e) : t, a = s.length - 1, o; a >= 0; a--)
    (o = s[a]) && (r = (i ? o(t, e, r) : o(r)) || r);
  return i && r && T9(t, e, r), r;
}, "__decorateClass$o"), mc;
let fr = (mc = class extends nt {
  constructor() {
    super(...arguments), this.exportParts = new W(this), this.appearance = "count", this.position = "inline", this.reveal = !1, this.count = NaN, this.limit = 99;
  }
  render() {
    const t = Number.isInteger(this.count) ? this.count > this.limit ? `${this.limit}+` : this.count : "", e = /^(top|bottom)-(right|left)$/.test(this.position), i = {
      "wy-badge": !0,
      "wy-badge-reveal": this.reveal,
      "wy-badge-compact": this.appearance === "compact",
      "wy-badge-dot": this.appearance === "dot",
      "wy-badge-positioned": e,
      [`wy-badge-${this.position}`]: e
    };
    return this.appearance !== "none" && t ? h`<span part=${tt(i)} title=${this.count}>${t}</span>` : g;
  }
}, n(mc, "WyBadge"), mc);
fr.styles = [R9, Y];
_l([
  y({ type: String })
], fr.prototype, "appearance", 2);
_l([
  y({ type: String })
], fr.prototype, "position", 2);
_l([
  y({ type: Boolean })
], fr.prototype, "reveal", 2);
_l([
  y({ type: Number })
], fr.prototype, "count", 2);
_l([
  y({ type: Number })
], fr.prototype, "limit", 2);
fr = _l([
  U("wy-badge")
], fr);
var O9 = Object.defineProperty, z9 = Object.getOwnPropertyDescriptor, Qu = /* @__PURE__ */ n((s, t, e, i) => {
  for (var r = i > 1 ? void 0 : i ? z9(t, e) : t, a = s.length - 1, o; a >= 0; a--)
    (o = s[a]) && (r = (i ? o(t, e, r) : o(r)) || r);
  return i && r && O9(t, e, r), r;
}, "__decorateClass$n"), vc;
let en = (vc = class extends Ae {
  constructor() {
    super(), this.exportParts = new W(this), this.isRenamingId = NaN, this.highlightId = NaN, this.highlightRef = J(), this.addEventListener("edit-name", (t) => {
      this.isRenamingId = t.detail.file.id;
    }), this.addEventListener("rename", () => {
      this.isRenamingId = NaN;
    });
  }
  /**
   * Emit a `file-open` event requesting preview for the given file.
   *
   * @param fileId - Identifier of the file to open.
   * @param tab - Optional preview tab to activate.
   * @returns Whether the event was not canceled.
   */
  dispatchFileOpen(t, e) {
    const i = new CustomEvent("file-open", { detail: { fileId: t, tab: e } });
    return this.dispatchEvent(i);
  }
  /**
   * Emit an `order` event with the selected sort order.
   *
   * @param order - Order to apply.
   * @returns Whether the event was not canceled.
   */
  dispatchOrder(t) {
    const e = new CustomEvent("order", { detail: { order: t } });
    return this.dispatchEvent(e);
  }
  /**
   * Emit an `edit-name` event to enter rename mode for the given file.
   *
   * @param file - File that should be renamed.
   * @returns Whether the event was not canceled.
   */
  dispatchEditName(t) {
    const e = new CustomEvent("edit-name", { detail: { file: t } });
    return this.dispatchEvent(e);
  }
  /**
   * Emit a `rename` event containing the updated file name.
   *
   * @param file - File being renamed.
   * @param name - New filename.
   * @returns Whether the event was not canceled.
   */
  dispatchRename(t, e) {
    const i = new CustomEvent("rename", { detail: { file: t, name: e } });
    return this.dispatchEvent(i);
  }
  /**
   * Emit a `trash` event to move the file to the recycle bin.
   *
   * @param file - File to trash.
   * @returns Whether the event was not canceled.
   */
  dispatchTrash(t) {
    const e = new CustomEvent("trash", { detail: { file: t } });
    return this.dispatchEvent(e);
  }
  /**
   * Emit a `restore` event to recover a trashed file.
   *
   * @param file - File to restore.
   * @returns Whether the event was not canceled.
   */
  dispatchRestore(t) {
    const e = new CustomEvent("restore", { detail: { file: t } });
    return this.dispatchEvent(e);
  }
  /**
   * Emit a `delete-forever` event to permanently remove a file.
   *
   * @param file - File to delete.
   * @returns Whether the event was not canceled.
   */
  dispatchDeleteForever(t) {
    const e = new CustomEvent("delete-forever", { detail: { file: t } });
    return this.dispatchEvent(e);
  }
  /**
   * Emit a `subscribe` event toggling file subscription.
   *
   * @param file - File to subscribe or unsubscribe.
   * @param subscribe - Desired subscription state.
   * @returns Whether the event was not canceled.
   */
  dispatchSubscribe(t, e) {
    const i = new CustomEvent("subscribe", { detail: { file: t, subscribe: e } });
    return this.dispatchEvent(i);
  }
  render() {
    return this.files && this.files.length ? h`
        <div part="wy-grid">
          ${Be(
      this.files,
      (t) => t.id,
      (t) => {
        const e = t.size && t.size > 0 ? ku(t.size) : g, i = t.updated_at || t.created_at, r = new Intl.DateTimeFormat(this.weavy?.locale, {
          dateStyle: "full",
          timeStyle: "short"
        }).format(new Date(i)), a = !!(this.isRenamingId && this.isRenamingId === t.id), { icon: o } = cs(t.name), c = ai(t.name), l = Zd(t.provider), u = /* @__PURE__ */ n((v) => {
          v.stopImmediatePropagation();
          const f = v.target;
          f.value && f.value !== t.name ? this.dispatchRename(t, f.value) : this.dispatchEditName({ id: NaN });
        }, "handleRename"), b = /* @__PURE__ */ n((v) => {
          const f = v.target;
          v.key === "Escape" ? (v.preventDefault(), f.value = t.name, f.blur()) : v.key === "Enter" && (v.preventDefault(), f.blur());
        }, "handleRenameKey"), C = !!(this.highlightId && this.highlightId === t.id);
        return h`
                <div
                  part=${tt({
          "wy-card": !0,
          "wy-trashed": t.is_trashed,
          "wy-card-hover": !t.is_trashed && !a,
          "wy-highlight": C
        })}
                  title="${t.name} • ${e} • ${r}"
                  tabindex="0"
                  @click=${(v) => {
          vu(v) && (v.stopPropagation(), v.preventDefault(), rr(t.download_url ?? t.external_url, "_blank")), !v.defaultPrevented && !t.is_trashed && this.dispatchFileOpen(t.id);
        }}
                  @keydown=${ne}
                  @keyup=${ke}
                  ${C && this.highlightRef ? K(this.highlightRef) : g}
                >
                  <div part="wy-card-actions">
                    <wy-file-menu
                      small
                      .file=${t}
                      @edit-name=${(v) => this.dispatchEditName(v.detail.file)}
                      @trash=${(v) => this.dispatchTrash(v.detail.file)}
                      @restore=${(v) => this.dispatchRestore(v.detail.file)}
                      @delete-forever=${(v) => this.dispatchDeleteForever(v.detail.file)}
                      @subscribe=${(v) => this.dispatchSubscribe(v.detail.file, v.detail.subscribe)}
                    ></wy-file-menu>
                  </div>
                  ${!t.is_trashed && t.thumbnail_url ? h`
                        <img
                          part="wy-card-image ${tt({ "wy-card-image-top": t.kind !== "image" })}"
                          width=${ot(t.width)}
                          height=${ot(t.height)}
                          src=${t.thumbnail_url}
                          alt=${t.name}
                          ${K(Eu)}
                          @load=${Au}
                          loading="lazy"
                          decoding="async"
                        />
                      ` : h`
                        <div part="wy-card-icon ">
                          <wy-icon
                            name=${o}
                            .overlayName=${l}
                            size="96"
                            kind=${t.kind}
                            ext=${c}
                          ></wy-icon>
                        </div>
                      `}
                  ${a ? h`
                        <input
                          type="text"
                          name="filename"
                          maxlength="256"
                          part="wy-input"
                          .defaultValue=${t.name}
                          @blur=${u}
                          @keyup=${(v) => {
          lr(v), b(v);
        }}
                          @click=${(v) => v.preventDefault()}
                          @focus=${_w}
                          ${K(hw)}
                        />
                      ` : h`<div part="wy-card-title">
                        <div part="wy-card-text">${t.name}</div> ${t.comments?.count ? h`<wy-button
                              part="wy-card-button-icon"
                              small
                              color="none"
                              kind="inline"
                              @click=${(v) => {
          !v.defaultPrevented && !t.is_trashed && (v.target.blur(), this.dispatchFileOpen(t.id, "comments"), v.stopPropagation());
        }}
                              title=${x(gt`${t.comments.count} comments`)}
                            >
                              <wy-badge count=${t.comments.count}></wy-badge>
                            </wy-button>` : g}
                      </div>`}
                </div>
              `;
      }
    )}
        </div>
      ` : g;
  }
  updated(t) {
    t.has("highlightId") && this.highlightId && this.highlightRef.value?.scrollIntoView({ block: "nearest" });
  }
}, n(vc, "WyFileGrid"), vc);
en.styles = [us, E9, A9, Y];
Qu([
  y({ attribute: !1 })
], en.prototype, "files", 2);
Qu([
  V()
], en.prototype, "isRenamingId", 2);
Qu([
  V()
], en.prototype, "highlightId", 2);
en = Qu([
  s1("wy-file-grid"),
  ut()
], en);
const F9 = j`@keyframes wy-highlight{0%{--wy-component-background-color: var(--wy-highlight, var(--wy-highlight-light, #e8f2ff));--wy-component-color: var(--wy-on-highlight, var(--wy-on-highlight-light, #001d32))}}[part~=wy-table]{table-layout:fixed;width:100%;vertical-align:top;border-collapse:collapse}[part~=wy-table] tr[part~=wy-highlight]{animation:wy-highlight 3s 1.5s ease-in-out both}[part~=wy-table] th{font-weight:var(--wy-headings-font-weight, var(--wy-font-weight-bold, 600))}[part~=wy-table] th,[part~=wy-table] td{background-color:var(--wy-component-background-color);padding:var(--wy-table-cell-padding-y, var(--wy-gap-sm, calc(.5 * var(--wy-gap, calc(.5 * var(--wy-size, 1rem)))))) var(--wy-table-cell-padding-x, var(--wy-gap-sm, calc(.5 * var(--wy-gap, calc(.5 * var(--wy-size, 1rem))))));box-shadow:var(--wy-outline-variant, var(--wy-outline-variant-light, #c2c7cf)) 0 -1px 0 0 inset;line-height:1;vertical-align:middle;height:calc(calc(2 * var(--wy-size, 1rem)) + var(--wy-table-cell-padding-y, var(--wy-gap-sm, calc(.5 * var(--wy-gap, calc(.5 * var(--wy-size, 1rem)))))) * 2)}[part~=wy-table]>tbody{vertical-align:inherit}[part~=wy-table]>thead{vertical-align:bottom}[part~=wy-table][part~=wy-table-interactive]>tbody>tr:hover>*{--wy-component-color: var(--wy-on-surface, var(--wy-on-surface-light, #181c20));background-color:var(--wy-surface-layer, var(--wy-surface-layer-light, rgba(0, 0, 0, .0509803922)));color:var(--wy-component-color);cursor:pointer}[part~=wy-col-icon]{vertical-align:middle;text-align:center;padding:calc(var(--wy-table-cell-padding-y, var(--wy-gap-sm, calc(.5 * var(--wy-gap, calc(.5 * var(--wy-size, 1rem)))))) * .5) calc(var(--wy-table-cell-padding-x, var(--wy-gap-sm, calc(.5 * var(--wy-gap, calc(.5 * var(--wy-size, 1rem)))))) * .5);width:calc(calc(2 * var(--wy-size, 1rem)) + var(--wy-table-cell-padding-x, var(--wy-gap-sm, calc(.5 * var(--wy-gap, calc(.5 * var(--wy-size, 1rem)))))) * 2);font-size:var(--wy-font-size, var(--wy-size, 1em));font-weight:var(--wy-font-weight, unset);text-transform:none;letter-spacing:normal}[part~=wy-col-icon-content]{display:flex;justify-content:center;align-items:center}[part~=wy-trashed]{text-decoration:line-through}[part~=wy-sort-link]{display:inline-flex;align-items:center;color:var(--wy-primary, var(--wy-primary-light, #2f628c));cursor:pointer}`, I9 = j`@keyframes wy-highlight{0%{--wy-component-background-color: var(--wy-highlight, var(--wy-highlight-light, #e8f2ff));--wy-component-color: var(--wy-on-highlight, var(--wy-on-highlight-light, #001d32))}}table thead{text-align:start;display:none}@container (inline-size >= 576px){table thead{display:table-header-group}}[part~=wy-col-name]{width:100%}[part~=wy-col-name]{overflow:hidden;text-overflow:ellipsis;white-space:nowrap}[part~=wy-col-name] [part~=wy-input]{width:100%;box-sizing:border-box}[part~=wy-col-time]{width:calc(8 * var(--wy-size, 1rem));display:none}@container (inline-size >= 768px){[part~=wy-col-time]{display:table-cell}}[part~=wy-col-time]{overflow:hidden;text-overflow:ellipsis;white-space:nowrap}[part~=wy-col-kind]{width:calc(8 * var(--wy-size, 1rem));display:none}@container (inline-size >= 768px){[part~=wy-col-kind]{display:table-cell}}[part~=wy-col-kind]{overflow:hidden;text-overflow:ellipsis;white-space:nowrap}[part~=wy-col-size]{width:calc(6 * var(--wy-size, 1rem));text-align:end;display:none}@container (inline-size >= 576px){[part~=wy-col-size]{display:table-cell}}[part~=wy-col-size]{overflow:hidden;text-overflow:ellipsis;white-space:nowrap}`;
var D9 = Object.defineProperty, V9 = Object.getOwnPropertyDescriptor, dh = /* @__PURE__ */ n((s, t, e, i) => {
  for (var r = i > 1 ? void 0 : i ? V9(t, e) : t, a = s.length - 1, o; a >= 0; a--)
    (o = s[a]) && (r = (i ? o(t, e, r) : o(r)) || r);
  return i && r && D9(t, e, r), r;
}, "__decorateClass$m"), gc;
let ss = (gc = class extends Ae {
  constructor() {
    super(), this.exportParts = new W(this), this.order = { by: "name", descending: !1 }, this.isRenamingId = NaN, this.highlightId = NaN, this.highlightRef = J(), this.addEventListener("edit-name", (t) => {
      this.isRenamingId = t.detail.file.id;
    }), this.addEventListener("rename", () => {
      this.isRenamingId = NaN;
    });
  }
  /**
   * Emit a `file-open` event requesting preview for the supplied file.
   *
   * @param fileId - Identifier of the file to open.
   * @param tab - Optional preview tab to activate.
   * @returns Whether the event was not canceled.
   */
  dispatchFileOpen(t, e) {
    const i = new CustomEvent("file-open", { detail: { fileId: t, tab: e } });
    return this.dispatchEvent(i);
  }
  /**
   * Emit an `order` event with the provided sort order.
   *
   * @param order - Order to apply.
   * @returns Whether the event was not canceled.
   */
  dispatchOrder(t) {
    const e = new CustomEvent("order", { detail: { order: t } });
    return this.dispatchEvent(e);
  }
  /**
   * Emit an `edit-name` event to enter rename mode for the specified file.
   *
   * @param file - File that should be renamed.
   * @returns Whether the event was not canceled.
   */
  dispatchEditName(t) {
    const e = new CustomEvent("edit-name", { detail: { file: t } });
    return this.dispatchEvent(e);
  }
  /**
   * Emit a `rename` event carrying the updated filename.
   *
   * @param file - File being renamed.
   * @param name - New filename value.
   * @returns Whether the event was not canceled.
   */
  dispatchRename(t, e) {
    const i = new CustomEvent("rename", { detail: { file: t, name: e } });
    return this.dispatchEvent(i);
  }
  /**
   * Emit a `trash` event to move the file to the recycle bin.
   *
   * @param file - File to trash.
   * @returns Whether the event was not canceled.
   */
  dispatchTrash(t) {
    const e = new CustomEvent("trash", { detail: { file: t } });
    return this.dispatchEvent(e);
  }
  /**
   * Emit a `restore` event to recover a trashed file.
   *
   * @param file - File to restore.
   * @returns Whether the event was not canceled.
   */
  dispatchRestore(t) {
    const e = new CustomEvent("restore", { detail: { file: t } });
    return this.dispatchEvent(e);
  }
  /**
   * Emit a `delete-forever` event to permanently remove the file.
   *
   * @param file - File targeted for permanent deletion.
   * @returns Whether the event was not canceled.
   */
  dispatchDeleteForever(t) {
    const e = new CustomEvent("delete-forever", { detail: { file: t } });
    return this.dispatchEvent(e);
  }
  /**
   * Emit a `subscribe` event toggling subscription state for the file.
   *
   * @param file - File whose subscription changes.
   * @param subscribe - Desired subscription state.
   * @returns Whether the event was not canceled.
   */
  dispatchSubscribe(t, e) {
    const i = new CustomEvent("subscribe", { detail: { file: t, subscribe: e } });
    return this.dispatchEvent(i);
  }
  /**
   * Render the complete file table layout.
   *
   * @param files - Files to render.
   * @param order - Current table order.
   * @param isRenamingId - File id in rename mode.
   * @param highlightId - File id to highlight.
   * @param highlightRef - Reference for scrolling highlighted rows.
   */
  renderFileTable(t, e, i, r, a) {
    return t && t.length ? h`
          <table part="wy-table wy-table-interactive">
            <thead>${this.renderFileTableHeaders.call(this, e)}</thead>
            <tbody>
              ${Be(
      t,
      (o) => o.id,
      (o) => this.renderFileTableRow.call(this, this.weavy, { file: o }, i, r, a)
    )}
            </tbody>
          </table>
        ` : g;
  }
  /**
   * Render table header cells with sorting affordances.
   *
   * @param order - Current sort order.
   */
  renderFileTableHeaders(t) {
    const e = [
      { col: "icon", by: void 0, title: "" },
      // File icon
      { col: "name", by: "name", title: x("Name") },
      { col: "icon", by: void 0, title: "" },
      // Comments icon
      { col: "time", by: "updated_at", title: x("Modified") },
      { col: "kind", by: void 0, title: x("Kind") },
      { col: "size", by: "size", title: x("Size") },
      { col: "icon", by: void 0, title: "" }
      // Menu
    ];
    return h`
      <tr>
        ${e.map((i) => {
      if (i.title) {
        const r = i.by === t?.by, a = /* @__PURE__ */ n((o) => {
          o.preventDefault(), i.by && this.dispatchOrder({ by: i.by, descending: r && !t?.descending });
        }, "onHeaderClick");
        return h` <th part="wy-th ${`wy-col-${i.col}`}">
              ${i.by ? h`<div
                    part="wy-sort-link"
                    tabindex="0"
                    @click=${a}
                    @keydown=${ne}
                    @keyup=${ke}
                    >${i.title}
                    ${r && h`<wy-icon name=${t?.descending ? "menu-down" : "menu-up"}></wy-icon>` || g}</div
                  >` : i.title}
            </th>`;
      } else
        return h`<th part="wy-th wy-col-icon"></th>`;
    })}
      </tr>
    `;
  }
  /**
   * Render a single file table row.
   *
   * @param weavy - Current Weavy context.
   * @param row - Row data containing the file.
   * @param isRenamingId - File id in rename mode.
   * @param highlightId - File id to highlight.
   * @param highlightRef - Reference for scrolling highlighted rows.
   */
  renderFileTableRow(t, { file: e }, i, r, a) {
    const o = e.size && e.size > 0 ? ku(e.size) : g, c = e.updated_at || e.created_at, l = new Intl.DateTimeFormat(t?.locale, { dateStyle: "full", timeStyle: "short" }).format(
      new Date(c)
    ), u = new Intl.DateTimeFormat(t?.locale, { dateStyle: "short" }).format(
      new Date(c)
    ), b = !!(i && i === e.id), { icon: C } = cs(e.name), v = ai(e.name), f = Zd(e.provider), m = /* @__PURE__ */ n((M) => {
      M.stopImmediatePropagation();
      const A = M.target;
      A.value && A.value !== e.name ? this.dispatchRename(e, A.value) : this.dispatchEditName({ id: NaN });
    }, "handleRename"), $ = /* @__PURE__ */ n((M) => {
      const A = M.target;
      M.key === "Escape" ? (M.preventDefault(), A.value = e.name, A.blur()) : M.key === "Enter" && (M.preventDefault(), A.blur());
    }, "handleRenameKey"), E = !!(r && r === e.id);
    return h`
      <tr
        part=${tt({ "wy-tr": !0, "wy-highlight": E, "wy-trashed": e.is_trashed })}
        @click=${(M) => {
      vu(M) && (M.stopPropagation(), M.preventDefault(), rr(e.download_url ?? e.external_url, "_blank")), !M.defaultPrevented && !e.is_trashed && this.dispatchFileOpen(e.id);
    }}
        ${E && a ? K(a) : g}
      >
        <td part="wy-td wy-col-icon"
          ><wy-icon part="wy-col-icon-content" name=${C} .overlayName=${f} size="24" kind=${e.kind} ext=${v}></wy-icon
        ></td>
        <td part="wy-td wy-col-name">
          ${b ? h`
                <input
                  type="text"
                  name="filename"
                  maxlength="256"
                  part="wy-input"
                  .defaultValue=${e.name}
                  @blur=${m}
                  @keyup=${(M) => {
      lr(M), $(M);
    }}
                  @click=${(M) => M.preventDefault()}
                  @focus=${_w}
                  ${K(hw)}
                />
              ` : h`${e.name}`}
        </td>
        <td part="wy-td wy-col-icon"
          >${e.comments?.count ? h`<wy-button
                part="wy-col-icon-content"
                kind="inline"
                @click=${(M) => {
      !M.defaultPrevented && !e.is_trashed && (M.target.blur(), this.dispatchFileOpen(e.id, "comments"), M.stopPropagation());
    }}
                title=${x(gt`${e.comments.count} comments`)}
              >
                <wy-badge count=${e.comments.count}></wy-badge>
              </wy-button>` : g}</td
        >
        <td part="wy-td wy-col-time"
          ><time datetime="${c}" title=${l}>${u}</time></td
        >
        <td part="wy-td wy-col-kind">${e.kind}</td>
        <td part="wy-td wy-col-size">${o}</td>
        <td part="wy-td wy-col-icon">
          <wy-file-menu
            part="wy-col-icon-content"
            .file=${e}
            @edit-name=${(M) => this.dispatchEditName(M.detail.file)}
            @trash=${(M) => this.dispatchTrash(M.detail.file)}
            @restore=${(M) => this.dispatchRestore(M.detail.file)}
            @delete-forever=${(M) => this.dispatchDeleteForever(M.detail.file)}
            @subscribe=${(M) => this.dispatchSubscribe(M.detail.file, M.detail.subscribe)}
          >
          </wy-file-menu>
        </td>
      </tr>
    `;
  }
  render() {
    return this.files && this.files.length ? this.renderFileTable.call(
      this,
      this.files,
      this.order,
      this.isRenamingId,
      this.highlightId,
      this.highlightRef
    ) : g;
  }
  updated(t) {
    t.has("highlightId") && this.highlightId && this.highlightRef.value?.scrollIntoView({ block: "nearest" });
  }
}, n(gc, "WyFileTable"), gc);
ss.styles = [us, F9, I9, Y];
dh([
  y({ attribute: !1 })
], ss.prototype, "files", 2);
dh([
  y({ attribute: !1 })
], ss.prototype, "order", 2);
dh([
  V()
], ss.prototype, "isRenamingId", 2);
dh([
  V()
], ss.prototype, "highlightId", 2);
ss = dh([
  s1("wy-file-table"),
  ut()
], ss);
var H9 = Object.defineProperty, U9 = Object.getOwnPropertyDescriptor, Ug = /* @__PURE__ */ n((s) => {
  throw TypeError(s);
}, "__typeError$5"), Pl = /* @__PURE__ */ n((s, t, e, i) => {
  for (var r = i > 1 ? void 0 : i ? U9(t, e) : t, a = s.length - 1, o; a >= 0; a--)
    (o = s[a]) && (r = (i ? o(t, e, r) : o(r)) || r);
  return i && r && H9(t, e, r), r;
}, "__decorateClass$l"), Bg = /* @__PURE__ */ n((s, t, e) => t.has(s) || Ug("Cannot " + e), "__accessCheck$5"), pv = /* @__PURE__ */ n((s, t, e) => (Bg(s, t, "read from private field"), e ? e.call(s) : t.get(s)), "__privateGet$5"), B9 = /* @__PURE__ */ n((s, t, e) => t.has(s) ? Ug("Cannot add the same private member more than once") : t instanceof WeakSet ? t.add(s) : t.set(s, e), "__privateAdd$5"), yv = /* @__PURE__ */ n((s, t, e, i) => (Bg(s, t, "write to private field"), t.set(s, e), e), "__privateSet$5"), An, bc;
let mr = (bc = class extends qt {
  constructor() {
    super(...arguments), this.exportParts = new W(this), this.order = { by: "name", descending: !1 }, this.view = "list", this.showTrashed = !1, this.highlightId = NaN, this.highlightComment = !1, this.filesQuery = new ar(this), this.previewRef = J(), this.infiniteScroll = new or(this), this.pagerRef = J(), this.handleRealtimeFileCreated = (t) => {
      !this.weavy || !this.app || (t.file.created_by = t.actor, this.weavy.queryClient.invalidateQueries({ queryKey: this.getFilesQueryKey(this.app) }));
    }, this.handleRealtimeFileUpdated = (t) => {
      !this.weavy || !this.app || this.weavy.queryClient.invalidateQueries({ queryKey: this.getFilesQueryKey(this.app) });
    }, this.handleRealtimeFileTrashed = (t) => {
      !this.weavy || !this.app || this.weavy.queryClient.invalidateQueries({ queryKey: this.getFilesQueryKey(this.app) });
    }, this.handleRealtimeFileRestored = (t) => {
      !this.weavy || !this.app || this.weavy.queryClient.invalidateQueries({ queryKey: this.getFilesQueryKey(this.app) });
    }, this.handleRealtimeFileDeleted = (t) => {
      !this.weavy || !this.app || this.weavy.queryClient.invalidateQueries({ queryKey: this.getFilesQueryKey(this.app) });
    }, this.handleRealtimeCommentCreated = () => {
      !this.weavy || !this.app || this.weavy.queryClient.invalidateQueries({ queryKey: this.getFilesQueryKey(this.app) });
    }, B9(this, An);
  }
  /**
   * Resolve the query key used for the app file listing.
   *
   * @internal
   * @param app - App owning the files.
   */
  getFilesQueryKey(t) {
    return ["apps", t.id, "files"];
  }
  /**
   * Emit a `file-open` event requesting preview for the supplied file.
   *
   * @param fileId - Identifier of the file to open.
   * @param tab - Optional preview tab to activate.
   * @returns {boolean} True if the event was not canceled.
   */
  dispatchFileOpen(t, e) {
    const i = new CustomEvent("file-open", { detail: { fileId: t, tab: e } });
    return this.dispatchEvent(i);
  }
  /**
   * Emit an `order` event with the provided sort order.
   *
   * @param order - Order to apply.
   * @returns {boolean} True if the event was not canceled.
   */
  dispatchOrder(t) {
    const e = new CustomEvent("order", { detail: { order: t } });
    return this.dispatchEvent(e);
  }
  async willUpdate(t) {
    var e;
    if (super.willUpdate(t), t.has("link") && (this.highlightId = this.link && sg(this.link, It.File)?.id, this.highlightComment = this.link && this.highlightId ? ng(this.link, It.File, { id: this.highlightId }, It.Comment) : !1), t.has("highlightComment") && this.highlightComment && this.highlightId && this.dispatchFileOpen(this.highlightId, "comments"), (t.has("weavy") || t.has("app") || t.has("order") || t.has("showTrashed")) && this.weavy && this.app && await this.filesQuery.trackInfiniteQuery(
      v9(this.weavy, this.app.id, {
        order: this.order,
        trashed: this.showTrashed
      })
    ), (t.has("weavy") || t.has("app") || t.has("user")) && this.weavy && this.app && this.user) {
      this.renameFileMutation = M9(this.weavy, this.app), this.subscribeFileMutation = P9(this.weavy, this.app), this.trashFileMutation = b9(this.weavy, this.app), this.restoreFileMutation = C9(this.weavy, this.app), this.deleteForeverFileMutation = k9(this.weavy, this.app), (e = pv(this, An)) == null || e.call(this);
      const i = `a${this.app.id}`;
      this.weavy.subscribe(i, "file_created", this.handleRealtimeFileCreated), this.weavy.subscribe(i, "file_updated", this.handleRealtimeFileUpdated), this.weavy.subscribe(i, "file_trashed", this.handleRealtimeFileTrashed), this.weavy.subscribe(i, "file_restored", this.handleRealtimeFileRestored), this.weavy.subscribe(i, "file_deleted", this.handleRealtimeFileDeleted), this.weavy.subscribe(i, "comment_created", this.handleRealtimeCommentCreated), yv(this, An, () => {
        this.weavy?.unsubscribe(i, "file_created", this.handleRealtimeFileCreated), this.weavy?.unsubscribe(i, "file_updated", this.handleRealtimeFileUpdated), this.weavy?.unsubscribe(i, "file_trashed", this.handleRealtimeFileTrashed), this.weavy?.unsubscribe(i, "file_restored", this.handleRealtimeFileRestored), this.weavy?.unsubscribe(i, "file_deleted", this.handleRealtimeFileDeleted), this.weavy?.unsubscribe(i, "comment_created", this.handleRealtimeCommentCreated), yv(this, An, void 0);
      });
    }
  }
  render() {
    const { data: t, hasNextPage: e, isPending: i } = this.filesQuery.result, r = pi(t);
    return r && r.length ? h`
        ${this.view === "grid" ? h`
              <wy-file-grid
                .files=${r}
                .highlightId=${this.highlightId}
                @file-open=${(a) => {
      this.previewRef.value?.open(a.detail.fileId, a.detail.tab);
    }}
                @order=${(a) => this.dispatchOrder(a.detail.order)}
                @rename=${(a) => {
      this.renameFileMutation?.mutate({ file: a.detail.file, name: a.detail.name });
    }}
                @subscribe=${(a) => {
      this.subscribeFileMutation?.mutate({
        file: a.detail.file,
        subscribe: a.detail.subscribe
      });
    }}
                @trash=${(a) => {
      this.trashFileMutation?.mutate({ file: a.detail.file });
    }}
                @restore=${(a) => {
      this.restoreFileMutation?.mutate({ file: a.detail.file });
    }}
                @delete-forever=${(a) => {
      this.deleteForeverFileMutation?.mutate({ file: a.detail.file });
    }}
              ></wy-file-grid>
            ` : h`
              <wy-file-table
                .files=${r}
                .order=${this.order}
                .highlightId=${this.highlightId}
                @file-open=${(a) => {
      this.previewRef.value?.open(a.detail.fileId, a.detail.tab);
    }}
                @order=${(a) => this.dispatchOrder(a.detail.order)}
                @rename=${(a) => {
      this.renameFileMutation?.mutate({ file: a.detail.file, name: a.detail.name });
    }}
                @subscribe=${(a) => {
      this.subscribeFileMutation?.mutate({
        file: a.detail.file,
        subscribe: a.detail.subscribe
      });
    }}
                @trash=${(a) => {
      this.trashFileMutation?.mutate({ file: a.detail.file });
    }}
                @restore=${(a) => {
      this.restoreFileMutation?.mutate({ file: a.detail.file });
    }}
                @delete-forever=${(a) => {
      this.deleteForeverFileMutation?.mutate({ file: a.detail.file });
    }}
              ></wy-file-table>
            `}
        ${e ? h`<div ${K(this.pagerRef)} part="wy-pager wy-pager-bottom"></div>` : g}
        ${t ? h` <wy-preview ${K(this.previewRef)} .infiniteQueryResult=${this.filesQuery.result}></wy-preview> ` : g}
      ` : h`
        <wy-empty>
          ${i ? h`<wy-progress-circular indeterminate padded reveal></wy-progress-circular>` : h` <wy-icon-display>
                <wy-icon name="file-upload"></wy-icon>
                <span slot="text">${x("Add some files to get started!")}</span>
              </wy-icon-display>`}
        </wy-empty>
      `;
  }
  updated() {
    this.infiniteScroll.observe(this.filesQuery.result, this.pagerRef.value);
  }
  disconnectedCallback() {
    var t;
    (t = pv(this, An)) == null || t.call(this), super.disconnectedCallback();
  }
}, n(bc, "WyFilesList"), bc);
An = /* @__PURE__ */ new WeakMap();
mr.styles = [kr];
Pl([
  y({ type: Object })
], mr.prototype, "order", 2);
Pl([
  y()
], mr.prototype, "view", 2);
Pl([
  y({ type: Boolean })
], mr.prototype, "showTrashed", 2);
Pl([
  V()
], mr.prototype, "highlightId", 2);
Pl([
  V()
], mr.prototype, "highlightComment", 2);
mr = Pl([
  U("wy-files-list"),
  ut()
], mr);
var N9 = Object.defineProperty, q9 = Object.getOwnPropertyDescriptor, Gu = /* @__PURE__ */ n((s, t, e, i) => {
  for (var r = i > 1 ? void 0 : i ? q9(t, e) : t, a = s.length - 1, o; a >= 0; a--)
    (o = s[a]) && (r = (i ? o(t, e, r) : o(r)) || r);
  return i && r && N9(t, e, r), r;
}, "__decorateClass$k"), xc;
let rn = (xc = class extends ae {
  constructor() {
    super(), this.appType = rt.Files, this.componentFeatures = new Gt({
      // All available features as enabled/disabled by default
      [_.Attachments]: !0,
      [_.CloudFiles]: !0,
      [_.Comments]: !0,
      [_.ContextData]: !0,
      [_.Embeds]: !0,
      [_.Follow]: !0,
      [_.GoogleMeet]: !1,
      [_.Meetings]: !1,
      [_.Mentions]: !0,
      [_.MicrosoftTeams]: !1,
      [_.Polls]: !0,
      [_.Previews]: !0,
      [_.Reactions]: !0,
      [_.Typing]: !1,
      // Has no effect currently
      [_.Versions]: !0,
      [_.WebDAV]: !0,
      [_.ZoomMeetings]: !1
    }), this.theme = new ce(this, rn.styles), this.view = "list", this.order = { by: "name", descending: !1 }, this.showTrashed = !1, this.persistState = new Hs(this), this.dropZone = new tu(this), this.uploadBlobMutation = new Ue(this), this.createFileMutation = new Ue(this), this.addEventListener("drop-files", (t) => this.handleBlobUpload(t));
  }
  /** @internal */
  handleBlobUpload(t) {
    const e = t.detail;
    if (e.files)
      for (let i = 0; i < e.files.length; i++) {
        const a = { file: e.files[i] };
        this.uploadBlobMutation.mutate(a).then((o) => this.handleCreateFile(o));
      }
  }
  /** @internal */
  handleExternalBlobs(t) {
    if (t.detail.externalBlobs)
      for (let e = 0; e < t.detail.externalBlobs.length; e++) {
        const i = t.detail.externalBlobs[e];
        this.externalBlobMutation?.mutate({ externalBlob: i }).then((r) => this.handleCreateFile(r));
      }
  }
  /** @internal */
  async handleCreateFile(t, e) {
    return await this.createFileMutation.mutate({ blob: t, replace: e });
  }
  async willUpdate(t) {
    await super.willUpdate(t), (t.has("uid") || t.has("weavy") || t.has("user")) && this.uid && this.weavy && this.user && this.persistState.observe(
      [
        { name: "view", override: !0 },
        { name: "order", override: !0 },
        { name: "showTrashed", override: !0 }
      ],
      this.uid.toString(),
      `u${this.user.id}`
    ), (t.has("weavy") || t.has("app") || t.has("user")) && this.weavy && this.app && this.user && (await this.uploadBlobMutation.trackMutation(Sw(this.weavy, this.user, this.app.id)), await this.createFileMutation.trackMutation(u2(this.weavy, this.user, this.app)), this.externalBlobMutation = wg(this.weavy, this.user, this.app.id));
  }
  render() {
    const t = this.dropZone.isDragActive;
    return h`
      <div part="wy-files ${tt({ "wy-dragging": t })}" data-drag-title=${x("Drop files here")}>
        <wy-files-header
          .order=${this.order}
          .showTrashed=${this.showTrashed}
          .view=${this.view}
          @upload-files=${(e) => this.handleBlobUpload(e)}
          @external-blobs=${(e) => this.handleExternalBlobs(e)}
          @create-files=${(e) => e.detail.blobs.forEach((i) => this.handleCreateFile(i, e.detail.replace))}
          @order=${(e) => {
      this.order = e.detail.order;
    }}
          @show-trashed=${(e) => {
      this.showTrashed = e.detail.showTrashed;
    }}
          @view=${(e) => {
      this.view = e.detail.view;
    }}
          @subscribe=${(e) => this.subscribe(e.detail.subscribe)}
        >
          <slot name="actions" slot="actions"></slot>
        </wy-files-header>

        <wy-files-list
          .view=${this.view}
          .order=${this.order}
          .showTrashed=${this.showTrashed}
          @order=${(e) => {
      this.order = e.detail.order;
    }}
        ></wy-files-list>
      </div>

      <wy-user-card .listenTo=${this.shadowRoot}></wy-user-card>
    `;
  }
}, n(xc, "WyFiles"), xc);
rn.styles = [
  fg,
  Kt,
  bi,
  Cr,
  $r,
  Pe,
  ls
];
Gu([
  y()
], rn.prototype, "view", 2);
Gu([
  y({ type: Object })
], rn.prototype, "order", 2);
Gu([
  y({ type: Boolean })
], rn.prototype, "showTrashed", 2);
rn = Gu([
  U("wy-files"),
  ut()
], rn);
const W9 = j`[part~=wy-notification] strong{font-weight:var(--wy-font-weight-bold, 600)}[part~=wy-notification] p{display:inline}[part~=wy-notification-text]{color:var(--wy-component-color)}[part~=wy-notification-text]{display:-webkit-box;-webkit-line-clamp:3;-webkit-box-orient:vertical;overflow:hidden}[part~=wy-notification-text]{white-space:normal}`;
var j9 = Object.defineProperty, K9 = Object.getOwnPropertyDescriptor, hh = /* @__PURE__ */ n((s, t, e, i) => {
  for (var r = i > 1 ? void 0 : i ? K9(t, e) : t, a = s.length - 1, o; a >= 0; a--)
    (o = s[a]) && (r = (i ? o(t, e, r) : o(r)) || r);
  return i && r && j9(t, e, r), r;
}, "__decorateClass$j"), Cc;
let ns = (Cc = class extends Ae {
  constructor() {
    super(...arguments), this.exportParts = new W(this), this.selected = !1, this.standalone = !1;
  }
  /**
   * Emit a `select` event for the current notification.
   *
   * @internal
   * @returns {boolean} True if the event was not canceled.
   */
  dispatchSelect(t) {
    const e = new CustomEvent("select", {
      detail: { notificationId: this.notificationId }
    });
    return this.dispatchEvent(e);
  }
  /**
   * Emit a `mark` event toggling read status.
   *
   * @internal
   * @param e - Source event triggering the action.
   * @param markAsRead - Target read state.
   * @returns {boolean} True if the event was not canceled.
   */
  dispatchMark(t, e) {
    if (t.stopPropagation(), e === !!this.notification.is_unread) {
      const i = new CustomEvent("mark", {
        detail: { notificationId: this.notificationId, markAsRead: e }
      });
      return this.dispatchEvent(i);
    }
    return !0;
  }
  /**
   * Emit a `hide` event when the toast should disappear.
   *
   * @internal
   * @returns {boolean} True if the event was not canceled.
   */
  dispatchHide() {
    if (this.standalone) {
      const t = new CustomEvent("hide", {
        bubbles: !0
      });
      return this.dispatchEvent(t);
    }
    return !0;
  }
  /**
   * Emit a `close` event when the toast should close.
   *
   * @internal
   * @returns {boolean} True if the event was not canceled.
   */
  dispatchClose() {
    if (this.standalone) {
      const t = new CustomEvent("close", {
        bubbles: !0
      });
      return this.dispatchEvent(t);
    }
    return !0;
  }
  /**
   * Handle click interactions and trigger navigation plus dismissal.
   *
   * @internal
   */
  async handleClick(t) {
    this.dispatchSelect(t), this.dispatchMark(t, !0), this.dispatchHide(), await ag(this, this.weavy, this.notification), this.dispatchClose();
  }
  render() {
    const t = this.notification.created_at ? new Intl.DateTimeFormat(this.weavy?.locale, { dateStyle: "full", timeStyle: "short" }).format(
      new Date(this.notification.created_at)
    ) : "", e = this.notification.created_at ? bl(this.weavy?.locale, new Date(this.notification.created_at)) : "", i = this.notification.actor, { title: r, titleHtml: a, detail: o } = og(this.notification);
    return h`
      <wy-item
        part="wy-notification"
        size="md"
        interactive
        outer
        status=${!this.standalone && !this.notification.is_unread ? "read" : void 0}
        ?selected=${!this.standalone && this.selected}
        align="top"
        actionsPosition=${this.standalone ? "end" : "bottom"}
        tabindex="0"
        @click=${(c) => this.handleClick(c)}
        @keydown=${ne}
        @keyup=${ke}
      >
        <wy-avatar
          slot="image"
          src=${ot(i?.avatar_url)}
          name=${ot(i?.name)}
          ?isAgent=${i?.is_agent}
          id=${ot(i?.id)}
          size=${48}
        ></wy-avatar>

        <div slot="title" part="wy-notification-text" title=${r + (o ? `: "${o}"` : "")}>
          ${a}${o ? h`: <q part="wy-quote">${o}</q> ` : g}
      </div>

        ${this.standalone ? g : h`
              <time slot="text" part="wy-meta" datetime=${this.notification.created_at.toString()} title=${t}>
                ${e}
              </time>
            `}
        ${this.standalone ? h`
              <wy-button
                slot="actions"
                kind="icon"
                @click=${(c) => this.dispatchMark(c, !!this.notification.is_unread) && this.dispatchClose()}
              >
                <wy-icon name="close"></wy-icon>
              </wy-button>
            ` : h`
              <wy-button
                small
                slot="actions"
                kind="icon"
                @click=${(c) => this.dispatchMark(c, !!this.notification.is_unread) && this.dispatchClose()}
                title=${this.notification.is_unread ? x("Mark as read") : x("Mark as unread")}
              >
                <wy-icon
                  name=${this.notification.is_unread ? "read" : "unread"}
                  color=${this.notification.is_unread ? "" : "secondary"}
                ></wy-icon>
              </wy-button>
            `}
      </wy-item>
    `;
  }
}, n(Cc, "WyNotificationListItem"), Cc);
ns.styles = [Lt, fl, W9, Y];
hh([
  y({ type: Number })
], ns.prototype, "notificationId", 2);
hh([
  y({ type: Boolean, reflect: !0 })
], ns.prototype, "selected", 2);
hh([
  y({ type: Boolean })
], ns.prototype, "standalone", 2);
hh([
  y({ attribute: !1 })
], ns.prototype, "notification", 2);
ns = hh([
  U("wy-notification-list-item"),
  ut()
], ns);
var Q9 = Object.defineProperty, G9 = Object.getOwnPropertyDescriptor, Ng = /* @__PURE__ */ n((s) => {
  throw TypeError(s);
}, "__typeError$4"), fn = /* @__PURE__ */ n((s, t, e, i) => {
  for (var r = i > 1 ? void 0 : i ? G9(t, e) : t, a = s.length - 1, o; a >= 0; a--)
    (o = s[a]) && (r = (i ? o(t, e, r) : o(r)) || r);
  return i && r && Q9(t, e, r), r;
}, "__decorateClass$i"), qg = /* @__PURE__ */ n((s, t, e) => t.has(s) || Ng("Cannot " + e), "__accessCheck$4"), wv = /* @__PURE__ */ n((s, t, e) => (qg(s, t, "read from private field"), e ? e.call(s) : t.get(s)), "__privateGet$4"), Z9 = /* @__PURE__ */ n((s, t, e) => t.has(s) ? Ng("Cannot add the same private member more than once") : t instanceof WeakSet ? t.add(s) : t.set(s, e), "__privateAdd$4"), fv = /* @__PURE__ */ n((s, t, e, i) => (qg(s, t, "write to private field"), t.set(s, e), e), "__privateSet$4"), Rn, $c;
let cl = ($c = class extends qt {
  constructor() {
    super(...arguments), this.exportParts = new W(this), this.typeFilter = _t.All, this.notificationsQuery = new ar(this), this.infiniteScroll = new or(this), this.pagerRef = J(), this.handleRefresh = (t) => {
      this.notificationsQuery.result.refetch();
    }, Z9(this, Rn);
  }
  /**
   * Mark or unmark a notification as read.
   *
   * @param notificationId - Target notification id.
   * @param markAsRead - Desired read state.
   */
  async markAsRead(t, e = !0) {
    await this.markNotificationMutation?.mutate({ notificationId: t, markAsRead: e });
  }
  /**
   * Select the provided notification and emit a `wy-action` event.
   *
   * @internal
   * @param notification - Notification to select.
   */
  selectNotification(t) {
    this.notificationId = t.id, this.dispatchAction(me.Select, t);
  }
  /**
   * Triggers `wy-action` event.
   *
   * @internal
   * @param action - The performed action.
   * @param notification - Selected notification payload.
   * @returns Whether the event was successful.
   */
  dispatchAction(t, e) {
    const i = new CustomEvent("wy-action", {
      detail: { action: t, notification: e },
      bubbles: !0,
      composed: !0
    });
    return this.dispatchEvent(i);
  }
  async willUpdate(t) {
    var e;
    super.willUpdate(t), (t.has("weavy") || t.has("typeFilter") || t.has("app")) && this.weavy && await this.notificationsQuery.trackInfiniteQuery(
      I2(this.weavy, this.typeFilter, this.app?.id)
    ), t.has("weavy") && this.weavy && (this.markNotificationMutation = x1(this.weavy), (e = wv(this, Rn)) == null || e.call(this), this.weavy.subscribe(null, "notification_created", this.handleRefresh), this.weavy.subscribe(null, "notification_updated", this.handleRefresh), this.weavy.subscribe(null, "notifications_marked", this.handleRefresh), fv(this, Rn, () => {
      this.weavy?.unsubscribe(null, "notification_created", this.handleRefresh), this.weavy?.unsubscribe(null, "notification_updated", this.handleRefresh), this.weavy?.unsubscribe(null, "notifications_marked", this.handleRefresh), fv(this, Rn, void 0);
    }));
  }
  update(t) {
    super.update(t), this.infiniteScroll.observe(this.notificationsQuery.result, this.pagerRef.value);
  }
  /**
   * Render the notifications collection.
   *
   * @internal
   * @param user - Current user.
   * @param infiniteData - Notification pages.
   */
  renderNotifications(t, e) {
    if (e) {
      const i = pi(e);
      return Be(
        i,
        (r) => r?.id,
        (r) => [
          h`<wy-notification-list-item
              notificationId=${r.id}
              .notification=${r}
              .selected=${this.notificationId == r.id}
              @select=${(a) => this.selectNotification(r)}
              @mark=${(a) => this.markAsRead(a.detail.notificationId, a.detail.markAsRead)}
            ></wy-notification-list-item>`
        ]
      );
    }
    return g;
  }
  render() {
    const { data: t, hasNextPage: e, isPending: i } = this.notificationsQuery.result ?? {};
    return h`
      ${this.user ? h`
            <div class="wy-notifications">
              ${!i && this.user && t ? t.pages[0]?.count ? this.renderNotifications(this.user, t) : h`
                      <div part="wy-pane-body">
                        <div part="wy-pane-group">
                          <slot name="empty">
                            <wy-empty>${x("No updates yet.")}</wy-empty>
                          </slot>
                        </div>
                      </div>
                    ` : h`<wy-empty><wy-progress-circular indeterminate padded></wy-progress-circular></wy-empty>`}
              ${e ? h`<div ${K(this.pagerRef)} part="wy-pager wy-pager-bottom"></div>` : g}
            </div>
          ` : h`<wy-empty part="wy-pane"><wy-progress-circular indeterminate overlay></wy-progress-circular></wy-empty>`}
    `;
  }
  disconnectedCallback() {
    var t;
    (t = wv(this, Rn)) == null || t.call(this), super.disconnectedCallback();
  }
}, n($c, "WyNotificationList"), $c);
Rn = /* @__PURE__ */ new WeakMap();
cl.styles = [
  dn,
  kr,
  j`
      :host {
        position: relative;
      }
    `
];
fn([
  y({ type: Number })
], cl.prototype, "notificationId", 2);
fn([
  y()
], cl.prototype, "typeFilter", 2);
cl = fn([
  U("wy-notification-list"),
  ut()
], cl);
var kc;
let sn = (kc = class extends nt {
  constructor() {
    super(...arguments), this.exportParts = new W(this), this.typeFilter = _t.All, this.activeFilter = _t.All;
  }
  /**
   * Trigger `filter` event.
   *
   * @param typeFilter - Filter to emit.
   * @returns Whether the event was dispatched successfully.
   */
  dispatchFilter(t) {
    const e = new CustomEvent("filter", {
      detail: { typeFilter: t }
    });
    return this.dispatchEvent(e);
  }
  render() {
    const t = !!this._slotDefault.length;
    return h`
      <div part="wy-pane-toolbar">
        <slot @slotchange=${() => this.requestUpdate()}></slot>
        ${t ? g : h`
              <wy-buttons tabs>
                <wy-button
                  ?active=${this.typeFilter === _t.All}
                  @click=${() => {
      this.dispatchFilter(_t.All) && (this.typeFilter = _t.All);
    }}
                  kind="tab"
                  small
                >
                  ${x("All")}
                </wy-button>
                <wy-button
                  ?active=${this.typeFilter === _t.Activity}
                  @click=${() => {
      this.dispatchFilter(_t.Activity) && (this.typeFilter = _t.Activity);
    }}
                  kind="tab"
                  small
                >
                  ${x("Activities")}
                </wy-button>
                <wy-button
                  ?active=${this.typeFilter === _t.Mention}
                  @click=${() => {
      this.dispatchFilter(_t.Mention) && (this.typeFilter = _t.Mention);
    }}
                  kind="tab"
                  small
                >
                  ${x("Mentions")}
                </wy-button>
                <wy-button
                  ?active=${this.typeFilter === _t.Reaction}
                  @click=${() => {
      this.dispatchFilter(_t.Reaction) && (this.typeFilter = _t.Reaction);
    }}
                  kind="tab"
                  small
                >
                  ${x("Reactions")}
                </wy-button>
              </wy-buttons>
            `}

        <slot name="actions" @slotchange=${() => this.requestUpdate()}></slot>
      </div>
    `;
  }
}, n(kc, "WyNotificationHeader"), kc);
sn.styles = [
  dn,
  kr,
  j`
      :host {
        position: relative;
      }
    `
];
fn([
  y()
], sn.prototype, "typeFilter", 2);
fn([
  V()
], sn.prototype, "activeFilter", 2);
fn([
  yl({ flatten: !0, selector: ":not(slot)" })
], sn.prototype, "_slotDefault", 2);
sn = fn([
  U("wy-notification-header"),
  ut()
], sn);
var Y9 = Object.defineProperty, X9 = Object.getOwnPropertyDescriptor, Xw = /* @__PURE__ */ n((s, t, e, i) => {
  for (var r = i > 1 ? void 0 : i ? X9(t, e) : t, a = s.length - 1, o; a >= 0; a--)
    (o = s[a]) && (r = (i ? o(t, e, r) : o(r)) || r);
  return i && r && Y9(t, e, r), r;
}, "__decorateClass$h"), _c;
let ll = (_c = class extends Us {
  constructor() {
    super(...arguments), this.unreadNotifications = new Ql(this), this.theme = new ce(this, ll.styles), this.currentTypeFilter = _t.All, this.typeFilter = _t.All;
  }
  /** 
   * Disabled link matching/consuming.
   * @internal 
   */
  matchesLink() {
    return !1;
  }
  /** Current unread notification count. */
  get unread() {
    return this.unreadNotifications.unread;
  }
  /** Marks all tracked notifications as read. */
  async markAllAsRead() {
    await this.unreadNotifications.markAllAsRead();
  }
  async willUpdate(t) {
    await super.willUpdate(t), t.has("typeFilter") && (this.currentTypeFilter = this.typeFilter), (t.has("typeFilter") || t.has("app")) && await this.unreadNotifications.track(this.typeFilter, this.app?.id);
  }
  render() {
    const t = this.typeFilter === _t.All;
    return h`
      ${t ? h`
            <wy-notification-header
              @filter=${(e) => {
      this.currentTypeFilter = e.detail.typeFilter;
    }}
            >
              <slot name="actions" slot="actions">
                <wy-button kind="icon" @click=${() => this.markAllAsRead()} title=${x("Mark all as read")}>
                  <wy-icon name="check-all"></wy-icon>
                </wy-button>
              </slot>
            </wy-notification-header>
          ` : g}
      ${this.user ? h` <wy-notification-list typeFilter=${this.currentTypeFilter}></wy-notification-list> ` : h`
            <wy-empty>
              <wy-progress-circular indeterminate padded reveal></wy-progress-circular>
            </wy-empty>
          `}
    `;
  }
}, n(_c, "WyNotifications"), _c);
ll.styles = [Kt, bi, Cr, $r, ls, Pe];
Xw([
  V()
], ll.prototype, "currentTypeFilter", 2);
Xw([
  y()
], ll.prototype, "typeFilter", 2);
ll = Xw([
  U("wy-notifications"),
  ut()
], ll);
const Wg = j`:host{display:inline-flex}`;
var J9 = Object.defineProperty, tk = Object.getOwnPropertyDescriptor, Zu = /* @__PURE__ */ n((s, t, e, i) => {
  for (var r = i > 1 ? void 0 : i ? tk(t, e) : t, a = s.length - 1, o; a >= 0; a--)
    (o = s[a]) && (r = (i ? o(t, e, r) : o(r)) || r);
  return i && r && J9(t, e, r), r;
}, "__decorateClass$g"), Pc;
let nn = (Pc = class extends Us {
  constructor() {
    super(...arguments), this.theme = new ce(this, nn.styles), this.unreadNotifications = new Ql(this), this.badge = "count", this.badgePosition = "inline", this.typeFilter = _t.All;
  }
  /** 
   * Disabled link matching/consuming.
   * @internal 
   */
  matchesLink() {
    return !1;
  }
  /** Current unread notification count. */
  get unread() {
    return this.unreadNotifications.unread;
  }
  /** Marks all tracked notifications as read. */
  async markAllAsRead() {
    await this.unreadNotifications.markAllAsRead();
  }
  async willUpdate(t) {
    await super.willUpdate(t), (t.has("typeFilter") || t.has("app")) && await this.unreadNotifications.track(this.typeFilter, this.app?.id);
  }
  render() {
    return this.user && this.badge !== "none" ? h`
          <wy-badge
            appearance=${this.badge}
            position=${this.badgePosition}
            .count=${this.unreadNotifications.isUnreadPending ? NaN : this.unreadNotifications.unread}
          ></wy-badge>
        ` : g;
  }
}, n(Pc, "WyNotificationBadge"), Pc);
nn.styles = [Wg, Kt, Pe];
Zu([
  y({ type: String })
], nn.prototype, "badge", 2);
Zu([
  y({ type: String })
], nn.prototype, "badgePosition", 2);
Zu([
  y()
], nn.prototype, "typeFilter", 2);
nn = Zu([
  U("wy-notification-badge")
], nn);
var ek = Object.defineProperty, ik = Object.getOwnPropertyDescriptor, ys = /* @__PURE__ */ n((s, t, e, i) => {
  for (var r = i > 1 ? void 0 : i ? ik(t, e) : t, a = s.length - 1, o; a >= 0; a--)
    (o = s[a]) && (r = (i ? o(t, e, r) : o(r)) || r);
  return i && r && ek(t, e, r), r;
}, "__decorateClass$f"), Sc;
let mi = (Sc = class extends Us {
  constructor() {
    super(...arguments), this.theme = new ce(this, mi.styles), this.unreadNotifications = new Ql(this), this.overlay = "sheet", this.badge = "compact", this.badgePosition = "top-right", this.typeFilter = _t.All, this.currentTypeFilter = _t.All, this.showNotificationList = !1;
  }
  /** 
   * Disabled link matching/consuming.
   * @internal 
   */
  matchesLink() {
    return !1;
  }
  set list(t) {
    console.warn(`.list is deprecated. Use .overlay = "${t}"; instead`), this.overlay = t;
  }
  get list() {
    return this.overlay;
  }
  /** Current unread notification count. */
  get unread() {
    return this.unreadNotifications.unread;
  }
  /** Marks all tracked notifications as read. */
  async markAllAsRead() {
    await this.unreadNotifications.markAllAsRead();
  }
  async willUpdate(t) {
    await super.willUpdate(t), t.has("typeFilter") && (this.currentTypeFilter = this.typeFilter), (t.has("typeFilter") || t.has("app")) && await this.unreadNotifications.track(this.typeFilter, this.app?.id);
  }
  render() {
    const t = this.typeFilter === _t.All;
    return h`
      <wy-button
        kind="icon"
        ?active=${this.showNotificationList}
        @click=${() => this.showNotificationList = !this.showNotificationList}
      >
        <wy-icon name="bell">
          ${this.user && this.badge !== "none" ? h`
                <wy-badge
                  appearance=${this.badge}
                  position=${this.badgePosition}
                  .count=${this.unreadNotifications.isUnreadPending ? NaN : this.unreadNotifications.unread}
                ></wy-badge>
              ` : g}
        </wy-icon>
      </wy-button>

      ${this.overlay !== "none" ? h`<wy-overlay
            type=${this.overlay}
            .show=${this.showNotificationList}
            @close=${() => this.showNotificationList = !1}
          >
            <slot name="actions" slot="actions">
              <wy-button kind="icon" @click=${() => this.markAllAsRead()} title=${x("Mark all as read")}>
                <wy-icon name="check-all"></wy-icon>
              </wy-button>
              ${this.app ? h`
                    <wy-dropdown>
                      ${this.app?.is_subscribed ? h`
                            <wy-dropdown-item @click=${() => this.subscribe(!1)}>
                              <wy-icon name="bell-off"></wy-icon>
                              ${x("Unsubscribe")}
                            </wy-dropdown-item>
                          ` : h`
                            <wy-dropdown-item @click=${() => this.subscribe(!0)}>
                              <wy-icon name="bell"></wy-icon>
                              ${x("Subscribe")}
                            </wy-dropdown-item>
                          `}
                    </wy-dropdown>
                  ` : g}
            </slot>
            <slot slot="title">${x("Notifications")}</slot>
            <wy-container scrollY>
              ${this.showNotificationList ? h`
                    ${t ? h`
                          <wy-notification-header
                            @filter=${(e) => {
      this.currentTypeFilter = e.detail.typeFilter;
    }}
                          ></wy-notification-header>
                        ` : g}
                    <wy-notification-list typeFilter=${this.currentTypeFilter}>
                      ${this.app && !this.app?.is_subscribed ? h`
                            <wy-empty slot="empty">
                              <div>${x("You are not subscribed to updates yet.")}</div>
                              <wy-button color="primary" @click=${() => this.subscribe(!0)}
                                >${x("Subscribe")}</wy-button
                              >
                            </wy-empty>
                          ` : g}
                    </wy-notification-list>
                  ` : g}
            </wy-container>
          </wy-overlay>` : g}
    `;
  }
}, n(Sc, "WyNotificationButton"), Sc);
mi.styles = [Y, Kt, Pe];
ys([
  y({ type: String })
], mi.prototype, "overlay", 2);
ys([
  y({ type: String })
], mi.prototype, "list", 1);
ys([
  y({ type: String })
], mi.prototype, "badge", 2);
ys([
  y({ type: String })
], mi.prototype, "badgePosition", 2);
ys([
  y()
], mi.prototype, "typeFilter", 2);
ys([
  V()
], mi.prototype, "currentTypeFilter", 2);
ys([
  V()
], mi.prototype, "showNotificationList", 2);
mi = ys([
  U("wy-notification-button"),
  ut()
], mi);
const jg = j`[part~=wy-toasts]{background:none;border:none;padding:var(--wy-padding-lg, calc(1.5 * var(--wy-padding, calc(.5 * var(--wy-size, 1rem)))));margin:0;box-sizing:border-box;width:100%;height:100%;display:flex;flex-direction:column;flex-wrap:wrap;align-content:flex-end;align-items:flex-end;justify-content:flex-end;pointer-events:none}[part~=wy-toasts][popover]{position:fixed;inset:0;z-index:1070}[part~=wy-toasts]>::slotted(*){pointer-events:auto}[part~=wy-toast]{--wy-component-background-color: var(--wy-surface-container-low, var(--wy-surface-container-low-light, #f1f3f9));--wy-color: var(--wy-on-surface, var(--wy-on-surface-light, #181c20));background:var(--wy-component-background-color);color:var(--wy-component-color);gap:var(--wy-gap, var(--wy-padding, calc(.5 * var(--wy-size, 1rem))));margin-top:0;margin-bottom:var(--wy-gap-lg, calc(1.5 * var(--wy-gap, var(--wy-padding, calc(.5 * var(--wy-size, 1rem))))));display:flex;cursor:pointer;border-radius:var(--wy-border-radius, calc(.5 * var(--wy-size, 1rem)));padding:0;-webkit-user-select:none;user-select:none;box-shadow:var(--wy-shadow-level2, 0 calc(.25 * var(--wy-size, 1rem)) calc(.3125 * var(--wy-size, 1rem)) 0 rgba(0, 0, 0, .14), 0 calc(.0625 * var(--wy-size, 1rem)) calc(.625 * var(--wy-size, 1rem)) 0 rgba(0, 0, 0, .12), 0 calc(.125 * var(--wy-size, 1rem)) calc(.25 * var(--wy-size, 1rem)) calc(-.0625 * var(--wy-size, 1rem)) rgba(0, 0, 0, .2));position:relative;overflow:hidden;min-height:calc(3 * var(--wy-size, 1rem));max-width:calc(32 * var(--wy-size, 1rem))}[part~=wy-toast][part~=wy-fade]{transition-property:opacity,min-height,padding-top,padding-bottom,margin-top,margin-bottom,height;transition-duration:var(--wy-transition-duration, .2s);transition-timing-function:var(--wy-transition-curve, cubic-bezier(.4, 0, .2, 1))}[part~=wy-toast][part~=wy-fade]:not([part~=wy-show]){opacity:0;min-height:0;height:0;padding-top:0;padding-bottom:0;margin-top:0;margin-bottom:0;pointer-events:none}`;
var rk = Object.defineProperty, sk = Object.getOwnPropertyDescriptor, uh = /* @__PURE__ */ n((s, t, e, i) => {
  for (var r = i > 1 ? void 0 : i ? sk(t, e) : t, a = s.length - 1, o; a >= 0; a--)
    (o = s[a]) && (r = (i ? o(t, e, r) : o(r)) || r);
  return i && r && rk(t, e, r), r;
}, "__decorateClass$e"), Mc;
let gd = (Mc = class extends nt {
  constructor() {
    super(...arguments), this.exportParts = new W(this), this.show = !1, this.viewportRef = J();
  }
  /**
   * Close the popover layer.
   */
  close() {
    this.show = !1;
    try {
      this.viewportRef.value?.hidePopover();
    } catch {
    }
  }
  /**
   * Handler when popover is toggled.
   * @internal
   */
  handleClose(t) {
    if (t.type === "toggle" && t.newState === "closed" || t.type === "click") {
      this.show = !1;
      const e = new CustomEvent("hide");
      this.dispatchEvent(e);
    }
  }
  render() {
    return h`
      <div part="wy-toasts" tabindex="0" ${K(this.viewportRef)} ?hidden=${!this.show} popover="auto">
        <slot></slot>
      </div>
    `;
  }
  willUpdate(t) {
    if (super.willUpdate(t), t.has("show"))
      try {
        this.show ? this.viewportRef.value?.showPopover() : this.viewportRef.value?.hidePopover();
      } catch {
      }
    t.has("show") && this.show && this.viewportRef.value?.focus();
  }
  firstUpdated(t) {
    this.viewportRef.value?.addEventListener(this.viewportRef.value.popover ? "toggle" : "click", (e) => {
      this.handleClose(e);
    }), this.show && this.viewportRef.value?.showPopover();
  }
  disconnectedCallback() {
    super.disconnectedCallback();
  }
}, n(Mc, "WyToasts"), Mc);
gd.styles = [
  jg,
  Y
];
uh([
  y({ type: Boolean })
], gd.prototype, "show", 2);
gd = uh([
  U("wy-toasts")
], gd);
var Ec;
let vr = (Ec = class extends nt {
  constructor() {
    super(...arguments), this.exportParts = new W(this), this.toastRef = J(), this.show = !1, this.duration = vr.defaultDuration;
  }
  /**
   * Hide the toast.
   */
  hide() {
    this.show = !1;
  }
  /**
   * Hide and close the toast.
   * 
   * @param silent - Indicates that the toast should close silently.
   * @fires {ClosedEventType} closed - When the toast has been hidden and closed.
   */
  async close(t = !1) {
    this.show = !1, await new Promise((i) => requestAnimationFrame(i)), this.toastRef.value && await ny(this.toastRef.value, !1);
    const e = new CustomEvent("closed", { detail: { silent: t } });
    this.dispatchEvent(e);
  }
  willUpdate(t) {
    super.willUpdate(t), t.has("show") && (this.timeout && (window.clearTimeout(this.timeout), this.timeout = void 0), this.show && this.duration > 0 && this.duration < 1 / 0 && (this.timeout = window.setTimeout(() => this.close(!0), this.duration)));
  }
  render() {
    return h`
      <div
        ${K(this.toastRef)}
        part="wy-toast wy-fade ${this.show ? "wy-show" : ""}"
        @hide=${(t) => {
      t.stopPropagation(), this.hide();
    }}
        @close=${() => this.close()}
        @keyup=${Lw}
      >
        <slot></slot>
      </div>
    `;
  }
  firstUpdated() {
    requestAnimationFrame(() => {
      this.show = !0;
    });
  }
  disconnectedCallback() {
    super.disconnectedCallback(), this.timeout && (window.clearTimeout(this.timeout), this.timeout = void 0);
  }
}, n(Ec, "WyToast"), Ec);
vr.styles = [
  jg,
  Y
];
vr.defaultDuration = 5e3;
uh([
  V()
], vr.prototype, "show", 2);
uh([
  y({ type: Number })
], vr.prototype, "duration", 2);
vr = uh([
  U("wy-toast")
], vr);
var nk = Object.defineProperty, ak = Object.getOwnPropertyDescriptor, Kg = /* @__PURE__ */ n((s) => {
  throw TypeError(s);
}, "__typeError$3"), Sl = /* @__PURE__ */ n((s, t, e, i) => {
  for (var r = i > 1 ? void 0 : i ? ak(t, e) : t, a = s.length - 1, o; a >= 0; a--)
    (o = s[a]) && (r = (i ? o(t, e, r) : o(r)) || r);
  return i && r && nk(t, e, r), r;
}, "__decorateClass$d"), Qg = /* @__PURE__ */ n((s, t, e) => t.has(s) || Kg("Cannot " + e), "__accessCheck$3"), mv = /* @__PURE__ */ n((s, t, e) => (Qg(s, t, "read from private field"), e ? e.call(s) : t.get(s)), "__privateGet$3"), ok = /* @__PURE__ */ n((s, t, e) => t.has(s) ? Kg("Cannot add the same private member more than once") : t instanceof WeakSet ? t.add(s) : t.set(s, e), "__privateAdd$3"), vv = /* @__PURE__ */ n((s, t, e, i) => (Qg(s, t, "write to private field"), t.set(s, e), e), "__privateSet$3"), Tn, Ac;
let gr = (Ac = class extends Us {
  constructor() {
    super(...arguments), this.theme = new ce(this, gr.styles), this.typeFilter = _t.All, this.appearance = "internal", this.requestUserPermission = !1, this.duration = vr.defaultDuration, this._notifications = [], this._nativeNotifications = [], this.handleEvent = async (t) => {
      if (this.typeFilter === _t.All || this.typeFilter === t.notification.type)
        if (t.action === "notification_deleted")
          this.removeNotification(t.notification.id), this.closeNativeNotification(t.notification.id);
        else {
          const { title: e, detail: i } = og(t.notification), r = {
            ...t.notification,
            title: e,
            detail: i,
            lang: this.weavy?.locale
          }, a = new CustomEvent("wy-notification", {
            bubbles: !0,
            composed: !0,
            cancelable: !0,
            detail: r
          });
          (!this.requestUserPermission && this.appearance !== "native" || await this.hasUserPermission() ? this.dispatchEvent(a) : !1) && (t.action === "notification_created" && t.notification.is_unread ? await this.addOrUpdateNotification(t.notification) : await this.updateNotification(t.notification), await this.addOrUpdateNativeNotification(r));
        }
    }, ok(this, Tn);
  }
  /** 
   * Disabled link matching/consuming.
   * @internal 
   */
  matchesLink() {
    return !1;
  }
  /** @internal */
  async addOrUpdateNotification(t) {
    if (!this.requestUserPermission || await this.hasUserPermission()) {
      const e = [...this._notifications], i = e.findIndex((r) => r.id === t.id);
      i !== -1 ? (e.splice(i, 1, t), this._notifications = e) : (e.push(t), this._notifications = e);
    }
  }
  /** @internal */
  async updateNotification(t) {
    if (!this.requestUserPermission || await this.hasUserPermission()) {
      const e = [...this._notifications], i = e.findIndex((r) => r.id === t.id);
      i !== -1 && (e.splice(i, 1, t), this._notifications = e);
    }
  }
  /** @internal */
  removeNotification(t) {
    const e = [...this._notifications], i = e.findIndex((r) => r.id === t);
    i !== -1 && (e.splice(i, 1), this._notifications = e);
  }
  /** @internal */
  async addOrUpdateNativeNotification(t) {
    if (this.appearance === "native" && await this.hasUserPermission()) {
      const e = this.removeNativeNotification(t.id), i = t.actor, r = new Notification(t.title, {
        tag: `wy-${t.id}`,
        lang: t.lang,
        body: t.detail,
        icon: i.avatar_url,
        // @ts-expect-error Property `renotify` not available in ts types yet
        renotify: e && t.is_unread
      });
      r.onclick = async () => {
        await this.markAsRead(t.id, !0), await ag(this, this.weavy, t);
      }, r.onclose = () => {
        this.removeNativeNotification(t.id);
      }, this._nativeNotifications = [...this._nativeNotifications, r];
    }
  }
  /** @internal */
  removeNativeNotification(t) {
    const e = [...this._nativeNotifications], i = e.findIndex(
      (r) => r.tag === `wy-${t}`
    );
    return i ? (e.splice(i, 1), this._nativeNotifications = e, !0) : !1;
  }
  /** @internal */
  closeNativeNotification(t) {
    const e = this._nativeNotifications.findIndex(
      (i) => i.tag === `wy-${t}`
    );
    return e ? (this._nativeNotifications[e].close(), !0) : !1;
  }
  /** Checks if the user has granted permission for desktop notifications. A request will be made if permission has not yet been granted. */
  async hasUserPermission() {
    if (!("Notification" in window))
      console.error("This browser does not support desktop notifications");
    else if (!window.isSecureContext)
      console.error(
        `Desktop notifications can only be used in secure contexts. 
        See https://developer.mozilla.org/en-US/docs/Web/Security/Secure_Contexts`
      );
    else {
      if (Notification.permission === "granted")
        return !0;
      if (Notification.permission !== "denied")
        return await Notification.requestPermission() === "granted";
    }
    return !1;
  }
  /** Clears queued internal toast notifications. */
  clearNotifications() {
    this._notifications = [];
  }
  /**
   * Marks a notification as read or unread.
   * @param notificationId - The ID of the notification to mark.
   * @param [markAsRead=true] - Whether to mark the notification as read (true) or unread (false).
   */
  async markAsRead(t, e = !0) {
    await this.markNotificationMutation?.mutate({ notificationId: t, markAsRead: e });
  }
  async willUpdate(t) {
    var e;
    await super.willUpdate(t), t.has("weavy") && this.weavy && (this.markNotificationMutation = x1(this.weavy), (e = mv(this, Tn)) == null || e.call(this), this.weavy.subscribe(null, "notification_created", this.handleEvent), this.weavy.subscribe(null, "notification_updated", this.handleEvent), vv(this, Tn, () => {
      this.weavy?.unsubscribe(null, "notification_created", this.handleEvent), this.weavy?.unsubscribe(null, "notification_updated", this.handleEvent), vv(this, Tn, void 0);
    })), (t.has("requestUserPermission") && this.requestUserPermission || t.has("appearance") && this.appearance === "native") && this.hasUserPermission();
  }
  render() {
    return h`
      ${this.user && this.appearance === "internal" ? h`
            <wy-toasts ?show=${!!this._notifications.length} @hide=${() => this.clearNotifications()}>
              ${Be(
      this._notifications,
      (t) => t.id,
      (t) => h`
                    <wy-toast
                      duration=${this.duration}
                      @closed=${(e) => {
        e.detail.silent || this.markAsRead(t.id, !0), this.removeNotification(t.id);
      }}
                    >
                      <wy-notification-list-item standalone .notification=${t}></wy-notification-list-item>
                    </wy-toast>
                  `
    )}
            </wy-toasts>
          ` : g}
    `;
  }
  disconnectedCallback() {
    var t;
    (t = mv(this, Tn)) == null || t.call(this), super.disconnectedCallback();
  }
}, n(Ac, "WyNotificationToasts"), Ac);
Tn = /* @__PURE__ */ new WeakMap();
gr.styles = [Kt, Y, Pe];
Sl([
  y()
], gr.prototype, "typeFilter", 2);
Sl([
  y()
], gr.prototype, "appearance", 2);
Sl([
  y({ type: Boolean })
], gr.prototype, "requestUserPermission", 2);
Sl([
  y({ type: Number })
], gr.prototype, "duration", 2);
Sl([
  V()
], gr.prototype, "_notifications", 2);
gr = Sl([
  U("wy-notification-toasts"),
  ut()
], gr);
const Bk = new Map(
  Object.entries({
    [ui.ChatRoom]: rt.ChatRoom,
    [ui.PrivateChat]: rt.PrivateChat,
    [rt.ChatRoom]: ui.ChatRoom,
    [rt.PrivateChat]: ui.PrivateChat
  })
), Nk = new Map(
  Object.entries({
    [ui.AgentChat]: rt.AgentChat,
    [rt.AgentChat]: ui.AgentChat
  })
), dl = {
  // All available features as enabled/disabled by default
  [_.Attachments]: !0,
  [_.CloudFiles]: !0,
  [_.ContextData]: !0,
  [_.Embeds]: !0,
  [_.Follow]: !0,
  [_.GoogleMeet]: !0,
  [_.Meetings]: !0,
  [_.Mentions]: !0,
  [_.MicrosoftTeams]: !0,
  [_.Polls]: !0,
  [_.Previews]: !0,
  [_.Reactions]: !0,
  [_.Receipts]: !0,
  [_.Typing]: !0,
  [_.ZoomMeetings]: !0
}, Jw = {
  // All available features as enabled/disabled by default
  [_.Attachments]: !0,
  [_.ContextData]: !0,
  [_.Embeds]: !0,
  [_.Previews]: !0,
  [_.Reactions]: !1,
  [_.Receipts]: !0,
  [_.Typing]: !0
}, tf = j`[part~=wy-messenger-layout]{display:flex;flex:1;align-items:stretch;position:relative;container-type:inline-size;align-self:stretch;min-height:0;min-width:0;box-sizing:border-box}[part~=wy-messenger-overlay-container]{--wy-padding-outer: var(--wy-padding, calc(.5 * var(--wy-size, 1rem)));--wy-component-offset-top: calc(var(--wy-padding, calc(.5 * var(--wy-size, 1rem))) + var(--wy-titlebar-height, calc(max(1lh, var(--wy-button-size, calc(2.5 * var(--wy-size, 1rem)))) + 2 * var(--wy-padding-sm, calc(.25 * var(--wy-size, 1rem))))))}[part~=wy-messenger-conversation-list]{flex:0 1 100%;min-width:0;max-width:none;display:flex;flex-direction:column;padding:var(--wy-padding-outer, 0);gap:var(--wy-padding, calc(.5 * var(--wy-size, 1rem)))}[part~=wy-messenger-conversation]{display:flex;flex-direction:column;overflow-anchor:none;height:100%;width:100%;flex:0 1 100%;box-sizing:border-box;padding:var(--wy-padding-outer, 0);--wy-component-offset-top: var(--wy-titlebar-height, calc(max(1lh, var(--wy-button-size, calc(2.5 * var(--wy-size, 1rem)))) + 2 * var(--wy-padding-sm, calc(.25 * var(--wy-size, 1rem)))))}[part~=wy-close-conversation]{display:contents}@container (inline-size < 768px){[part~=wy-messenger-conversation-list]:not([data-conversation-id=\"\"]){display:none}[part~=wy-messenger-conversation][data-conversation-id=\"\"]{display:none}}@container (inline-size >= 768px){[part~=wy-messenger-conversation-list]{flex:0 1 50%;max-width:calc(24 * var(--wy-size, 1rem));padding-right:var(--wy-padding, calc(.5 * var(--wy-size, 1rem)));border-right:var(--wy-border, 1px) solid var(--wy-outline-variant, var(--wy-outline-variant-light, #c2c7cf))}[part~=wy-messenger-conversation]{min-width:max(50%,calc(16 * var(--wy-size, 1rem)));padding-left:var(--wy-padding, calc(.5 * var(--wy-size, 1rem)))}[part~=wy-close-conversation]{display:none}}`;
var ck = Object.defineProperty, lk = Object.getOwnPropertyDescriptor, ef = /* @__PURE__ */ n((s, t, e, i) => {
  for (var r = i > 1 ? void 0 : i ? lk(t, e) : t, a = s.length - 1, o; a >= 0; a--)
    (o = s[a]) && (r = (i ? o(t, e, r) : o(r)) || r);
  return i && r && ck(t, e, r), r;
}, "__decorateClass$c"), Rc;
let hl = (Rc = class extends Ae {
  constructor() {
    super(...arguments), this.exportParts = new W(this), this.uploadBlobMutation = new Ue(this), this.fileInputRef = J(), this.openFileInput = () => {
      this.fileInputRef.value?.click();
    };
  }
  /**
   * Uploads the first selected file and resets the input.
   *
   * @internal
   */
  async handleBlobChange(t, e) {
    if (t && this.weavy) {
      const r = { file: t[0] };
      await this.uploadBlobMutation.trackMutation(y2(this.weavy));
      const a = await this.uploadBlobMutation.mutate(r);
      e && (e.value = "");
      const o = new CustomEvent("blob-uploaded", { detail: { blob: a } });
      this.dispatchEvent(o);
    }
  }
  render() {
    return h`
      <div part="wy-upload">
        <slot
          part="wy-upload-placeholder"
          name="placeholder"
          @click=${this.openFileInput}
          @keydown=${ne}
          @keyup=${ke}
        ></slot>
        <input
          accept=${ot(this.accept)}
          type="file"
          ${K(this.fileInputRef)}
          @click=${(t) => t.stopPropagation()}
          @change=${(t) => this.handleBlobChange(t.target.files, t.target)}
          hidden
        />
        <slot part="wy-upload-action" name="action">
          ${this.label ? h`<wy-button part="wy-upload-button" @click=${this.openFileInput}>${this.label}</wy-button>` : g}
        </slot>
      </div>
    `;
  }
}, n(Rc, "WyUpload"), Rc);
hl.styles = [
  j`
      :host {
        display: contents;
      }

      [part~="wy-upload"] {
        text-align: center;
      }

      [part~="wy-upload-placeholder"] {
        cursor: pointer;
      }
    `
];
ef([
  y({ attribute: !1 })
], hl.prototype, "label", 2);
ef([
  y({ attribute: !1 })
], hl.prototype, "accept", 2);
hl = ef([
  U("wy-upload")
], hl);
var dk = Object.defineProperty, hk = Object.getOwnPropertyDescriptor, ws = /* @__PURE__ */ n((s, t, e, i) => {
  for (var r = i > 1 ? void 0 : i ? hk(t, e) : t, a = s.length - 1, o; a >= 0; a--)
    (o = s[a]) && (r = (i ? o(t, e, r) : o(r)) || r);
  return i && r && dk(t, e, r), r;
}, "__decorateClass$b"), Tc;
let vi = (Tc = class extends nt {
  constructor() {
    super(...arguments), this.exportParts = new W(this), this.agentFilter = void 0, this.selected = [], this.select = [], this.text = "", this.peopleQuery = new ar(this), this.searchRef = J(), this.infiniteScroll = new or(this), this.pagerRef = J();
  }
  /**
   * Emits the selected members via the `submit` event.
   *
   * @internal
   * @returns {boolean} `true` if the event was not canceled.
   */
  dispatchSubmit() {
    this.selected = [...this.selected, ...this.select];
    const t = new CustomEvent("submit", {
      detail: { members: this.selected }
    });
    return this.dispatchEvent(t);
  }
  /**
   * Checks whether a member is currently selected.
   *
   * @internal
   */
  isChecked(t) {
    return this.select.find((e) => e.id === t) != null;
  }
  /**
   * Toggles selection state for the provided member.
   *
   * @internal
   */
  handleSelected(t, e) {
    e ? this.select = [...this.select, t] : (this.select = this.select.filter((i) => i.id !== t.id), this.selected = this.selected.filter((i) => i.id !== t.id));
  }
  /**
   * Renders the list of currently selected members.
   *
   * @internal
   */
  getSelected() {
    return this.selected.length > 0 ? h`
        ${this.selected.map((t) => h`
            <wy-item
              interactive
              @click=${() => this.handleSelected(t, !1)}
              @keydown=${ne}
              @keyup=${ne}
            >
              <wy-avatar
                slot="image"
                id=${t.id}
                .src=${t.avatar_url}
                .name=${t.name}
                .description=${t.comment}
                .presence=${t.presence}
                .isAgent=${t.is_agent}
                size=${32}
              ></wy-avatar>
              <span slot="title"> ${t.name} </span>
              <wy-button slot="actions" kind="icon" .active=${!1}
                ><wy-icon name="checkbox-marked"></wy-icon
              ></wy-button>
            </wy-item>
          `)}
      ` : g;
  }
  /**
   * Renders search results for the current query and filter.
   *
   * @internal
   */
  getSearchResult() {
    const { data: t, hasNextPage: e, isPending: i } = this.peopleQuery.result ?? { data: [], isPending: !0 }, r = pi(t), a = !!r.length, o = [];
    return i ? o.push(h`<wy-progress-circular indeterminate overlay></wy-progress-circular>`) : a || o.push(h`<div part="wy-pane-group">
        <div part="wy-no-result">
          ${this.text ? x("Your search did not match any people.") : x("No more users found.")}
        </div>
      </div>`), a && o.push(
      h` ${r.filter((c) => this.selected.find((l) => l.id === c.id) === void 0).map((c) => h` <wy-item
              interactive
              @click=${() => this.handleSelected(c, !this.isChecked(c.id))}
              @keydown=${ne}
              @keyup=${ne}
            >
              <wy-avatar
                slot="image"
                id=${c.id}
                .src=${c.avatar_url}
                .name=${c.name}
                .description=${c.comment}
                .presence=${c.presence}
                .isAgent=${c.is_agent}
                size=${32}
              ></wy-avatar>
              <span slot="title"> ${c.name} </span>
              <wy-button slot="actions" kind="icon" .active=${!1}
                ><wy-icon name="${this.isChecked(c.id) ? "checkbox-marked" : "checkbox-blank"}"></wy-icon
              ></wy-button>
            </wy-item>`) ?? g}`
    ), e && o.push(h`<div ${K(this.pagerRef)} part="wy-pager wy-pager-bottom"></div>`), o;
  }
  render() {
    return h`<div part="wy-pane wy-scroll-y wy-scroll-y-always">
      <div part="wy-pane-body">
        <div part="wy-pane-group wy-pane-group-fixed-size">
          <wy-search ${K(this.searchRef)} @search=${(t) => this.text = t.detail.query}></wy-search>
        </div>
        <div>
          <wy-buttons tabs>
            <wy-button
              ?active=${this.agentFilter === void 0}
              @click=${() => this.agentFilter = void 0}
              kind="tab"
              small
              >${x("All")}</wy-button
            >
            <wy-button ?active=${this.agentFilter === !1} @click=${() => this.agentFilter = !1} kind="tab" small
              >${x("People")}</wy-button
            >
            <wy-button ?active=${this.agentFilter === !0} @click=${() => this.agentFilter = !0} kind="tab" small
              >${x("Agents")}</wy-button
            >
          </wy-buttons>
        </div>
        <div part="wy-pane-body"> ${this.getSelected()} ${this.getSearchResult()} </div>
      </div>
      <div part="wy-footerbar wy-footerbar-sticky">
        <div part="wy-pane-group">
          <wy-buttons reverse>
            <wy-button
              color="primary"
              @click=${() => this.dispatchSubmit()}
              ?disabled=${this.selected.length === 0 && this.select.length === 0 ? !0 : void 0}
              >${this.buttonTitle ?? x("Create")}</wy-button
            >
          </wy-buttons>
        </div>
      </div>
    </div>`;
  }
  async willUpdate(t) {
    super.willUpdate(t), t.has("weavy") && this.weavy && await this.peopleQuery.trackInfiniteQuery(
      P5(
        this.weavy,
        () => this.text,
        this.appId,
        () => this.agentFilter
      )
    );
  }
  async updated(t) {
    this.infiniteScroll.observe(this.peopleQuery.result, this.pagerRef.value), (t.has("text") || t.has("agentFilter")) && (this.select.length > 0 && (this.selected = [...this.selected, ...this.select], this.select = []), await this.peopleQuery.result?.refetch?.(), this.searchRef.value?.focusInput());
  }
}, n(Tc, "WyUsersSearch"), Tc);
vi.styles = [hn, R1, us, kr, Y, dn];
ws([
  he({ context: Xt, subscribe: !0 }),
  V()
], vi.prototype, "weavy", 2);
ws([
  y({ attribute: !1 })
], vi.prototype, "appId", 2);
ws([
  y({ attribute: !1 })
], vi.prototype, "buttonTitle", 2);
ws([
  V()
], vi.prototype, "agentFilter", 2);
ws([
  V()
], vi.prototype, "selected", 2);
ws([
  V()
], vi.prototype, "select", 2);
ws([
  V()
], vi.prototype, "text", 2);
vi = ws([
  U("wy-users-search"),
  ut()
], vi);
var uk = Object.defineProperty, pk = Object.getOwnPropertyDescriptor, Gg = /* @__PURE__ */ n((s) => {
  throw TypeError(s);
}, "__typeError$2"), mn = /* @__PURE__ */ n((s, t, e, i) => {
  for (var r = i > 1 ? void 0 : i ? pk(t, e) : t, a = s.length - 1, o; a >= 0; a--)
    (o = s[a]) && (r = (i ? o(t, e, r) : o(r)) || r);
  return i && r && uk(t, e, r), r;
}, "__decorateClass$a"), Zg = /* @__PURE__ */ n((s, t, e) => t.has(s) || Gg("Cannot " + e), "__accessCheck$2"), gv = /* @__PURE__ */ n((s, t, e) => (Zg(s, t, "read from private field"), e ? e.call(s) : t.get(s)), "__privateGet$2"), yk = /* @__PURE__ */ n((s, t, e) => t.has(s) ? Gg("Cannot add the same private member more than once") : t instanceof WeakSet ? t.add(s) : t.set(s, e), "__privateAdd$2"), bv = /* @__PURE__ */ n((s, t, e, i) => (Zg(s, t, "write to private field"), t.set(s, e), e), "__privateSet$2"), Ln, Lc;
let Hi = (Lc = class extends Ae {
  constructor() {
    super(...arguments), this.exportParts = new W(this), this.showDetails = !1, this.showAddMembers = !1, this.conversationTitle = "", this.conversationTitleInput = "", this.membersQuery = new ve(this), this.handleRealtimeAppUpdated = (t) => {
      !this.conversationId || t.app.id !== this.conversationId || (this.conversationTitle = this.conversationTitleInput = t.app.name);
    }, this.handlePresenceChange = (t) => {
      if (!this.weavy)
        return;
      Array.isArray(t) || (t = [parseInt(t)]);
      const e = /* @__PURE__ */ n((i = []) => (i.forEach((r) => {
        r.presence = t.indexOf(r.id) != -1 ? "active" : "away";
      }), i), "updateMembersInApps");
      this.weavy.queryClient.setQueryData(["apps", this.conversationId], (i) => (i.members.data = e(i.members.data), i)), this.weavy.queryClient.setQueryData(["members", this.conversationId], (i) => e(i));
    }, yk(this, Ln);
  }
  /** Checks whether the current or provided conversation is an agent chat. */
  isAgentChat(t) {
    return (t ?? this.conversation)?.type === rt.AgentChat;
  }
  /** Checks whether the current or provided conversation is a chat room. */
  isChatRoom(t) {
    return (t ?? this.conversation)?.type === rt.ChatRoom;
  }
  /** Checks whether the current or provided conversation is a private chat. */
  isPrivateChat(t) {
    return (t ?? this.conversation)?.type === rt.PrivateChat;
  }
  /**
   * Adds a list of members to the conversation.
   *
   * @internal
   */
  async addMembers(t) {
    this.showAddMembers = !1, this.showDetails = !0, !(!this.weavy || !this.conversationId) && (await this.addMembersMutation?.mutate({ appId: this.conversationId, members: t.map((e) => e.id) }), await this.membersQuery.result.refetch(), await this.weavy.queryClient.invalidateQueries({ queryKey: ["apps"] }));
  }
  /**
   * Handles saving the conversation name from the conversation name input.
   *
   * @internal
   */
  async handleSaveConversationName() {
    if (!this.weavy || !this.conversationId)
      return;
    const t = this.conversationTitleInput.trim() === "" ? null : this.conversationTitleInput.trim();
    await this.updateConversationMutation?.mutate({ appId: this.conversationId, name: t });
  }
  /**
   * Updates the avatar with an uploaded blob picture.
   *
   * @internal
   */
  async handleAvatarUploaded(t) {
    !this.weavy || !this.conversationId || await this.updateConversationMutation?.mutate({
      appId: this.conversationId,
      blobId: t.id,
      thumbnailUrl: t.thumbnail_url
    });
  }
  /**
   * Clears the set avatar for the conversation.
   *
   * @internal
   */
  async clearAvatar() {
    !this.weavy || !this.conversationId || await this.updateConversationMutation?.mutate({ appId: this.conversationId, blobId: null, thumbnailUrl: null });
  }
  /**
   * Updates the access for a member in the conversation.
   *
   * @internal
   */
  async updateMember(t, e) {
    !this.weavy || !this.conversationId || (await this.updateMemberMutation?.mutate({
      appId: this.conversationId,
      userId: t,
      access: e
    }), await this.membersQuery.result.refetch());
  }
  /**
   * Removes the current or provided member from the conversation.
   *
   * If no memberId is provided, the current user leaves the conversation and the component state is cleared.
   *
   * @internal
   */
  async leaveConversation(t) {
    !this.weavy || !this.conversationId || !this.user || (t && await this.leaveConversationMutation?.mutate({
      appId: this.conversationId,
      members: [t]
    }), !t || t === this.user.id ? (this.showDetails = !1, this.conversation = void 0, this.conversationId = void 0, this.dispatchAction(me.Select, null)) : await this.membersQuery.result.refetch(), await this.weavy.queryClient.invalidateQueries({ queryKey: ["apps"] }));
  }
  /**
   * Triggers `wy-action` event.
   *
   * @param action - The performed action.
   * @param conversation - The conversation to select or `null` to clear.
   * @returns Whether the event was successful.
   *
   * @internal
   */
  dispatchAction(t, e) {
    this.conversationId = e?.id;
    const i = new CustomEvent("wy-action", { detail: { action: t, app: e }, bubbles: !0, composed: !0 });
    return this.dispatchEvent(i);
  }
  async willUpdate(t) {
    var e;
    if (super.willUpdate(t), t.has("weavy") && this.weavy && (this.leaveConversationMutation = P1(this.weavy), this.addMembersMutation = k5(this.weavy), this.updateConversationMutation = S1(this.weavy), this.updateMemberMutation = $5(this.weavy)), t.has("conversationId") && (this.showDetails = !1), (t.has("weavy") || t.has("conversationId")) && this.weavy)
      if ((e = gv(this, Ln)) == null || e.call(this), this.conversationId) {
        await this.membersQuery.trackQuery(
          E1(this.weavy, this.conversationId, {
            initialData: /* @__PURE__ */ n(() => {
              if (this.conversationId)
                return this.weavy?.queryClient.getQueryData(["apps", this.conversationId])?.members;
            }, "initialData")
          })
        );
        const i = `a${this.conversationId}`;
        this.weavy.subscribe(i, "app_updated", this.handleRealtimeAppUpdated), this.weavy.subscribe(null, "online", this.handlePresenceChange), bv(this, Ln, () => {
          this.weavy?.unsubscribe(i, "app_updated", this.handleRealtimeAppUpdated), this.weavy?.unsubscribe(null, "online", this.handlePresenceChange), bv(this, Ln, void 0);
        });
      } else
        this.membersQuery.untrackQuery();
    t.has("conversation") && this.conversation && (this.conversationTitleInput = this.conversationTitle = this.conversation.name);
  }
  render() {
    const { data: t } = this.membersQuery.result ?? {}, e = (t?.data || []).filter((r) => r.access === Rr.Admin).length, i = this.user && this.isPrivateChat() ? (this.conversation?.members?.data || []).filter((r) => r.id !== this.user?.id)?.[0] ?? this.user : null;
    return this.conversationId ? h`
          <wy-titlebar outer header floating part="wy-conversation-titlebar">
            <slot slot="icon" name="icon"><span></span></slot>
            ${this.conversation && this.user ? h`
                  ${this.conversation.type === rt.PrivateChat ? h`<wy-presence
                        slot="title-section"
                        placement="text"
                        .status=${i?.presence}
                        id=${ot(i?.id)}
                      ></wy-presence>` : g}
                  <wy-titlebar-text slot="title-section">${this.conversationTitle}</wy-titlebar-text>
                ` : g}
            ${this.isChatRoom() ? h`<wy-button
                  slot="actions"
                  kind="icon"
                  @click=${() => this.showDetails = !0}
                  title="${x("Details")}"
                >
                  <wy-icon name="information"></wy-icon>
                </wy-button>` : g}
          </wy-titlebar>

          <!-- details modal -->
          ${this.weavy ? h`
                <wy-overlay
                  part="wy-conversation-details"
                  .show=${this.showDetails}
                  @close=${() => {
      this.showDetails = !1;
    }}
                >
                  <wy-titlebar header slot="header">
                    <wy-button
                      slot="icon"
                      kind="icon"
                      @click=${() => {
      this.showDetails = !1;
    }}
                    >
                      <wy-icon name="close"></wy-icon>
                    </wy-button>
                    <span slot="title">${this.conversationTitle}</span>
                  </wy-titlebar>
                  <div part="wy-scroll-y">
                    ${this.showDetails && this.conversation && this.user ? h`
                          <wy-avatar-header>
                            ${this.isChatRoom() ? h`
                                  <wy-upload
                                    @blob-uploaded=${(r) => this.handleAvatarUploaded(r.detail.blob)}
                                    .accept=${"image/*"}
                                    .label=${x("Select picture")}
                                  >
                                    <div slot="placeholder">
                                      ${this.conversation.avatar_url ? h`<wy-avatar .size=${96} src=${this.conversation.avatar_url}></wy-avatar>` : h`<wy-avatar-group
                                            .members=${t?.data}
                                            title=${this.conversation.name}
                                            .size=${96}
                                          ></wy-avatar-group>`}
                                    </div>
                                    ${this.conversation.avatar_url ? h`<div slot="action"
                                          ><wy-button @click=${() => this.clearAvatar()}
                                            >${x("Remove picture")}</wy-button
                                          ></div
                                        >` : g}
                                  </wy-upload>
                                ` : h`
                                  <wy-avatar
                                    src=${ot(i?.avatar_url)}
                                    name=${ot(i?.name)}
                                    presence=${i?.presence || "away"}
                                    ?isAgent=${i?.is_agent}
                                    id=${ot(i?.id)}
                                    size=${96}
                                  ></wy-avatar>
                                `}
                          </wy-avatar-header>
                          ${this.isChatRoom() ? h`
                                <div part="wy-pane-group">
                                  <label part="wy-label" for="roomName">${x("Room name")}</label>

                                  <input
                                    id="roomName"
                                    part="wy-input"
                                    .value=${this.conversationTitleInput}
                                    @input=${(r) => {
      this.conversationTitleInput = r.target.value;
    }}
                                    @keyup=${M5}
                                    @blur=${() => this.handleSaveConversationName()}
                                  />

                                  <div part="wy-description">
                                    ${x("Changing the name of a group chat changes it for everyone.")}
                                  </div>
                                  <br />
                                  <label part="wy-label">${x("Members")}</label>
                                  ${t ? h`
                                        <wy-item-list>
                                          ${t.data?.map(
      (r) => h`
                                              <wy-item>
                                                <wy-avatar
                                                  slot="image"
                                                  .src=${r.avatar_url}
                                                  .name=${r.name}
                                                  .description=${r.comment}
                                                  .isAgent=${r.is_agent}
                                                  size=${32}
                                                ></wy-avatar>
                                                <span slot="title">
                                                  ${r.name}
                                                  ${r.access === Rr.Admin ? h` <wy-icon
                                                        size="20"
                                                        inline
                                                        name="shield-star"
                                                        title=${x("Admin")}
                                                      ></wy-icon>` : g}
                                                </span>
                                                ${this.user && this.user.id === r.id && !Zc(Vs.Admin, this.conversation?.permissions) ? h` <wy-button
                                                      slot="actions"
                                                      @click=${() => this.leaveConversation(r.id)}
                                                      title=${x("Leave conversation")}
                                                      kind="icon"
                                                    >
                                                      <wy-icon name="close"></wy-icon>
                                                    </wy-button>` : Zc(Vs.Admin, this.conversation?.permissions) ? h`<wy-dropdown slot="actions">
                                                      <wy-dropdown-item
                                                        @click=${() => this.leaveConversation(r.id)}
                                                      >
                                                        <wy-icon name="account-minus"></wy-icon>
                                                        ${this.user && this.user.id === r.id ? x("Leave conversation") : x("Remove member")}
                                                      </wy-dropdown-item>
                                                      ${e > 1 && r.access === Rr.Admin ? h`<wy-dropdown-item
                                                            @click=${() => this.updateMember(r.id, Rr.Write)}
                                                          >
                                                            <wy-icon name="shield-star-outline"></wy-icon>
                                                            ${x("Remove as admin")}
                                                          </wy-dropdown-item>` : r.access !== Rr.Admin ? h`<wy-dropdown-item
                                                            @click=${() => this.updateMember(r.id, Rr.Admin)}
                                                          >
                                                            <wy-icon name="shield-star"></wy-icon>
                                                            ${x("Make admin")}
                                                          </wy-dropdown-item>` : g}
                                                    </wy-dropdown>` : g}
                                              </wy-item>
                                            `
    ) ?? g}
                                        </wy-item-list>
                                      ` : g}
                                  <div>
                                    <wy-button
                                      part="wy-conversation-details-add-members-button"
                                      kind="filled"
                                      color="primary"
                                      @click=${() => {
      this.showDetails = !1, this.showAddMembers = !0;
    }}
                                      title=${x("Add members")}
                                    >
                                      ${x("Add members")}
                                    </wy-button>
                                  </div>
                                </div>
                              ` : g}
                        ` : g}
                  </div>
                </wy-overlay>
              ` : g}

          <!-- add members modal -->
          ${this.weavy ? h`
                <wy-overlay
                  part="wy-conversation-add-members"
                  .show=${this.showAddMembers}
                  @close=${() => {
      this.showAddMembers = !1;
    }}
                >
                  <wy-titlebar header>
                    <wy-button
                      slot="icon"
                      kind="icon"
                      @click=${() => {
      this.showAddMembers = !1;
    }}
                    >
                      <wy-icon name="close"></wy-icon>
                    </wy-button>
                    <span slot="title">${x("Add members")}</span>
                  </wy-titlebar>
                  ${this.showAddMembers ? h`
                        <wy-users-search
                          .buttonTitle=${x("Add members")}
                          .appId=${this.conversationId}
                          @submit=${(r) => this.addMembers(r.detail.members)}
                        ></wy-users-search>
                      ` : g}
                </wy-overlay>
              ` : g}
        ` : g;
  }
  disconnectedCallback() {
    var t;
    (t = gv(this, Ln)) == null || t.call(this), super.disconnectedCallback();
  }
}, n(Lc, "WyConversationHeader"), Lc);
Ln = /* @__PURE__ */ new WeakMap();
Hi.styles = [us, dn, hn, Y];
mn([
  y({ attribute: !1 })
], Hi.prototype, "conversationId", 2);
mn([
  y({ attribute: !1 })
], Hi.prototype, "conversation", 2);
mn([
  V()
], Hi.prototype, "showDetails", 2);
mn([
  V()
], Hi.prototype, "showAddMembers", 2);
mn([
  V()
], Hi.prototype, "conversationTitle", 2);
mn([
  V()
], Hi.prototype, "conversationTitleInput", 2);
Hi = mn([
  U("wy-conversation-header"),
  ut()
], Hi);
var wk = Object.defineProperty, fk = Object.getOwnPropertyDescriptor, Ml = /* @__PURE__ */ n((s, t, e, i) => {
  for (var r = i > 1 ? void 0 : i ? fk(t, e) : t, a = s.length - 1, o; a >= 0; a--)
    (o = s[a]) && (r = (i ? o(t, e, r) : o(r)) || r);
  return i && r && wk(t, e, r), r;
}, "__decorateClass$9"), Oc;
let br = (Oc = class extends Nt {
  constructor() {
    super(...arguments), this.componentFeatures = new Gt(dl), this.theme = new ce(this, br.styles), this.unreadConversationsController = new Qc(this), this.createConversationController = new Kc(this), this.componentTypes = [rt.ChatRoom, rt.PrivateChat], this.conversationId = null, this.conversationQuery = new ve(this), this.persistState = new Hs(this), this.conversationListRef = J(), this.conversationNewRef = J();
  }
  set agent(t) {
    super.agent = t, this._agentUid ? (this.componentTypes = [rt.AgentChat], this.componentFeatures = new Gt(
      Jw,
      this.componentFeatures.allowedFeatures()
    )) : (this.componentTypes = [rt.ChatRoom, rt.PrivateChat], this.componentFeatures = new Gt(
      dl,
      this.componentFeatures.allowedFeatures()
    )), this.conversationId = null;
  }
  get agent() {
    return super.agent;
  }
  /** Current unread conversation count. */
  get unread() {
    return this.unreadConversationsController.unread;
  }
  /**
   * Creates a new conversation.
   *
   * When in agent mode, a conversation is created instantly.
   *
   * @param members - Optional array of member ids or member uids.
   */
  async createConversation(t) {
    return await this.createConversationController.create(t);
  }
  /**
   * Opens the select member dialog to let the user select members for creating a conversation.
   *
   * @returns Promise resolving to any selected member ids or uids.
   */
  async selectMembers() {
    return this.conversationId = null, await this.conversationNewRef.value?.selectMembers();
  }
  async willUpdate(t) {
    await super.willUpdate(t), (t.has("weavy") || t.has("agent") || t.has("user")) && this.weavy && this.user && this.persistState.observe(
      [{ name: "conversationId", override: !1 }],
      this.agent || "messenger",
      `u${this.user?.id}`,
      (e) => {
        if (e.has("conversationId") && this.conversationId) {
          const i = new CustomEvent("wy-action", {
            detail: {
              action: me.Select,
              app: { id: this.conversationId }
            },
            bubbles: !0,
            composed: !0
          });
          this.dispatchEvent(i);
        }
      }
    ), t.has("link") && this.link?.app && (this.conversationId = this.link.app.id), (t.has("conversationId") || t.has("weavy")) && this.weavy && (this.conversationId ? this.conversationQuery.trackQuery(
      M1(this.weavy, this.conversationId, this.componentTypes)
    ) : this.conversationQuery.untrackQuery()), t.has("agent") && (this.createConversationController.agent = this.agent), (t.has("componentTypes") || t.has("agent")) && await this.unreadConversationsController.track(this.componentTypes, this.agent);
  }
  render() {
    const { isPending: t } = this.weavy?.network ?? { isPending: !0 }, { data: e } = this.conversationQuery.result ?? {}, i = this.conversationListRef.value?.conversationsQuery.result.data?.pages[0].count || 0;
    return h`
      <div part="wy-messenger-layout">
        <div
          part="wy-messenger-conversation-list wy-scroll-y"
          data-conversation-id=${this.conversationId !== null && this.conversationId !== void 0 ? this.conversationId : ""}
        >
          <slot name="header"></slot>
          <wy-conversation-list
            ${K(this.conversationListRef)}
            .conversationTypes=${this.componentTypes}
            .agent=${this.agent}
            conversationId=${ot(this.conversationId !== null ? this.conversationId : void 0)}
            @wy-action=${(r) => {
      !r.defaultPrevented && r.detail.action === me.Select && r.detail.app !== void 0 && (this.conversationId = r.detail.app?.id);
    }}
          >
            <wy-conversation-new
              slot="actions"
              .agent=${this.agent}
              @create=${async (r) => {
      const a = await this.createConversationController.create(r.detail.members);
      a && (this.conversationId = a.id);
    }}
              ${K(this.conversationNewRef)}
            >
              <slot name="conversation-new"></slot>
            </wy-conversation-new>
            <slot name="actions" slot="actions"></slot>
          </wy-conversation-list>
        </div>

        <div
          part="wy-messenger-conversation wy-scroll-y"
          data-conversation-id=${this.conversationId !== null && this.conversationId !== void 0 ? this.conversationId : ""}
        >
          <wy-conversation-header
            .conversationId=${this.conversationId || void 0}
            .conversation=${e}
            @wy-action=${(r) => {
      !r.defaultPrevented && r.detail.action === me.Select && r.detail.app !== void 0 && (this.conversationId = r.detail.app?.id);
    }}
            ?hidden=${!this.conversationId}
          >
            <span slot="icon" part="wy-close-conversation">
              <wy-button kind="inline" @click=${() => this.conversationId = null}>
                <wy-icon name="back">
                  <wy-badge
                    reveal
                    .count=${this.unreadConversationsController.isUnreadPending ? NaN : this.unreadConversationsController.unread}
                  ></wy-badge>
                </wy-icon>
              </wy-button>
            </span>
          </wy-conversation-header>

          ${this.conversationId ? h`<wy-conversation
                .conversationId=${this.conversationId}
                .conversation=${e}
                .agentInstructions=${this.instructions}
                .placeholder=${this.placeholder ?? (this.agent ? x("Ask anything...") : void 0)}
                .header=${!this.agent}
              ></wy-conversation>` : i ? h`<wy-empty noNetwork>${x("Select a conversation")}</wy-empty>` : g}
        </div>
        <wy-user-card .listenTo=${this}></wy-user-card>
        <wy-context-data-progress></wy-context-data-progress>
      </div>
    `;
  }
}, n(Oc, "WyMessenger"), Oc);
br.styles = [Kt, hn, tf, bi, Cr, Pe];
Ml([
  y({ attribute: !1 })
], br.prototype, "componentTypes", 2);
Ml([
  y({ type: String })
], br.prototype, "agent", 1);
Ml([
  y()
], br.prototype, "instructions", 2);
Ml([
  y()
], br.prototype, "placeholder", 2);
Ml([
  y({ type: Number })
], br.prototype, "conversationId", 2);
br = Ml([
  U("wy-messenger"),
  ut()
], br);
var mk = Object.defineProperty, vk = Object.getOwnPropertyDescriptor, xi = /* @__PURE__ */ n((s, t, e, i) => {
  for (var r = i > 1 ? void 0 : i ? vk(t, e) : t, a = s.length - 1, o; a >= 0; a--)
    (o = s[a]) && (r = (i ? o(t, e, r) : o(r)) || r);
  return i && r && mk(t, e, r), r;
}, "__decorateClass$8"), zc;
let Ne = (zc = class extends Nt {
  constructor() {
    super(...arguments), this.componentFeatures = new Gt(dl), this.theme = new ce(this, Ne.styles), this.unreadConversationsController = new Qc(this), this.createConversationController = new Kc(this), this.overlay = "drawer", this.badge = "compact", this.badgePosition = "top-right", this.componentTypes = [rt.ChatRoom, rt.PrivateChat], this.conversationId = null, this.show = !1, this.maximized = !1, this.conversationQuery = new ve(this), this.persistState = new Hs(this), this.messengerRef = J(), this.conversationListRef = J(), this.conversationNewRef = J();
  }
  /** Check visibility on the Messenger */
  get visibilityElement() {
    return this.messengerRef.value;
  }
  set agent(t) {
    super.agent = t, this._agentUid ? (this.componentTypes = [rt.AgentChat], this.componentFeatures = new Gt(
      Jw,
      this.componentFeatures.allowedFeatures()
    )) : (this.componentTypes = [rt.ChatRoom, rt.PrivateChat], this.componentFeatures = new Gt(
      dl,
      this.componentFeatures.allowedFeatures()
    )), this.conversationId = null;
  }
  get agent() {
    return super.agent;
  }
  /** Current unread conversation count. */
  get unread() {
    return this.unreadConversationsController.unread;
  }
  /**
   * Creates a new conversation.
   *
   * - When in agent mode, a conversation is created instantly.
   *
   * @param members {(number|string)[] | undefined} - Optional array of member id or member uid.
   */
  async createConversation(t) {
    return await this.createConversationController.create(t);
  }
  /**
   * Opens the select member dialog to let the user select members for creating a conversation.
   *
   * @returns Any selected member ids or uids.
   */
  async selectMembers() {
    return this.conversationId = null, await this.conversationNewRef.value?.selectMembers();
  }
  async willUpdate(t) {
    await super.willUpdate(t), (t.has("weavy") || t.has("agent") || t.has("user")) && this.weavy && this.user && this.persistState.observe(
      [{ name: "conversationId", override: !1 }],
      this.agent || "messenger",
      `u${this.user?.id}`,
      (e) => {
        if (e.has("conversationId") && this.conversationId) {
          const i = new CustomEvent("wy-action", {
            detail: {
              action: me.Select,
              app: { id: this.conversationId }
            },
            bubbles: !0,
            composed: !0
          });
          this.dispatchEvent(i);
        }
      }
    ), t.has("link") && this.link?.app && (this.conversationId = this.link.app.id), (t.has("conversationId") || t.has("weavy")) && this.weavy && (this.conversationId ? this.conversationQuery.trackQuery(M1(this.weavy, this.conversationId, this.componentTypes)) : this.conversationQuery.untrackQuery()), t.has("agent") && (this.createConversationController.agent = this.agent), (t.has("componentTypes") || t.has("agent")) && await this.unreadConversationsController.track(this.componentTypes, this.agent);
  }
  render() {
    const { isPending: t } = this.weavy?.network ?? { isPending: !0 }, { data: e } = this.conversationQuery.result ?? {}, i = this.conversationListRef.value?.conversationsQuery.result.data?.pages[0].count || 0;
    return h`
      <wy-button kind="icon" ?active=${this.show} @click=${() => this.show = !this.show}>
        <wy-icon name="message-text">
          ${this.user && this.badge !== "none" ? h`
                <wy-badge
                  appearance=${this.badge}
                  position=${this.badgePosition}
                  .count=${this.unreadConversationsController.isUnreadPending ? NaN : this.unreadConversationsController.unread}
                ></wy-badge>
              ` : g}
        </wy-icon>
      </wy-button>

      ${this.overlay !== "none" ? h`<wy-overlay
            type=${this.overlay}
            .show=${this.show}
            .maximized=${this.maximized}
            @close=${() => this.show = !1}
            noHeader
          >
            <div ${K(this.messengerRef)} part="wy-messenger-layout wy-messenger-overlay-container">
              <div
                part="wy-messenger-conversation-list wy-scroll-y"
                data-conversation-id=${this.conversationId !== null && this.conversationId !== void 0 ? this.conversationId : ""}
              >
                <slot name="header">
                  <wy-titlebar floating header outer>
                    <wy-button kind="icon" slot="icon" @click=${() => this.show = !1}
                      ><wy-icon name="close"></wy-icon
                    ></wy-button>
                    <slot name="title" slot="title">${x("Conversations")}</slot>
                    <slot name="actions" slot="actions"></slot>
                    <wy-button kind="icon" slot="actions" @click=${() => this.maximized = !this.maximized}
                      ><wy-icon name=${this.maximized ? "arrow-collapse" : "arrow-expand"}></wy-icon
                    ></wy-button>
                  </wy-titlebar>
                </slot>

                <wy-conversation-list
                  ${K(this.conversationListRef)}
                  .conversationTypes=${this.componentTypes}
                  .agent=${this.agent}
                  conversationId=${ot(this.conversationId !== null ? this.conversationId : void 0)}
                  @wy-action=${(r) => {
      !r.defaultPrevented && r.detail.action === me.Select && r.detail.app !== void 0 && (this.conversationId = r.detail.app?.id);
    }}
                >
                  <wy-conversation-new
                    slot="actions"
                    .agent=${this.agent}
                    @create=${async (r) => {
      const a = await this.createConversationController.create(r.detail.members);
      a && (this.conversationId = a.id);
    }}
                    ${K(this.conversationNewRef)}
                  >
                    <slot name="conversation-new"></slot>
                  </wy-conversation-new>
                </wy-conversation-list>
              </div>

              <div
                part="wy-messenger-conversation wy-scroll-y"
                data-conversation-id=${this.conversationId !== null && this.conversationId !== void 0 ? this.conversationId : ""}
              >
                <wy-conversation-header
                  .conversationId=${this.conversationId || void 0}
                  .conversation=${e}
                  @wy-action=${(r) => {
      !r.defaultPrevented && r.detail.action === me.Select && r.detail.app !== void 0 && (this.conversationId = r.detail.app?.id);
    }}
                  ?hidden=${!this.conversationId}
                >
                  <span slot="icon" part="wy-close-conversation">
                    <wy-button kind="icon" @click=${() => this.conversationId = null}>
                      <wy-icon name="back"></wy-icon>
                    </wy-button>
                    <wy-badge
                      reveal
                      .count=${this.unreadConversationsController.isUnreadPending ? NaN : this.unreadConversationsController.unread}
                    ></wy-badge>
                  </span>
                </wy-conversation-header>

                ${this.conversationId ? h`<wy-conversation
                      .conversationId=${this.conversationId}
                      .conversation=${e}
                      .agentInstructions=${this.instructions}
                      .placeholder=${this.placeholder ?? (this.agent ? x("Ask anything...") : void 0)}
                      .header=${!this.agent}
                    ></wy-conversation>` : i ? h`<wy-empty noNetwork>${x("Select a conversation")}</wy-empty>` : g}
              </div>
              <wy-user-card .listenTo=${this.shadowRoot}></wy-user-card>
              <wy-context-data-progress></wy-context-data-progress>
            </div>
          </wy-overlay> ` : g}
    `;
  }
}, n(zc, "WyMessengerButton"), zc);
Ne.styles = [Y, Kt, Pe, hn, tf];
xi([
  y({ type: String })
], Ne.prototype, "overlay", 2);
xi([
  y({ type: String })
], Ne.prototype, "badge", 2);
xi([
  y({ type: String })
], Ne.prototype, "badgePosition", 2);
xi([
  y({ attribute: !1 })
], Ne.prototype, "componentTypes", 2);
xi([
  y({ type: String })
], Ne.prototype, "agent", 1);
xi([
  y()
], Ne.prototype, "instructions", 2);
xi([
  y()
], Ne.prototype, "placeholder", 2);
xi([
  y({ type: Number })
], Ne.prototype, "conversationId", 2);
xi([
  V()
], Ne.prototype, "show", 2);
xi([
  V()
], Ne.prototype, "maximized", 2);
Ne = xi([
  U("wy-messenger-button"),
  ut()
], Ne);
var gk = Object.getOwnPropertyDescriptor, bk = /* @__PURE__ */ n((s, t, e, i) => {
  for (var r = i > 1 ? void 0 : i ? gk(t, e) : t, a = s.length - 1, o; a >= 0; a--)
    (o = s[a]) && (r = o(r) || r);
  return r;
}, "__decorateClass$7"), Fc;
let uu = (Fc = class extends Nt {
  constructor() {
    super(...arguments), this.theme = new ce(this, uu.styles), this.createConversationController = new Kc(this), this.conversationNewRef = J();
  }
  /** 
   * Disabled link matching/consuming.
   * @internal 
   */
  matchesLink() {
    return !1;
  }
  /**
   * Creates a new conversation.
   *
   * When in agent mode, the conversation is created instantly.
   *
   * @param members - Optional array of member ids or member uids.
   */
  async createConversation(t) {
    return await this.createConversationController.create(t);
  }
  /**
   * Opens the select-member dialog.
   *
   * @returns Promise resolving to the selected member ids or uids.
   */
  async selectMembers() {
    return await this.conversationNewRef.value?.selectMembers();
  }
  async willUpdate(t) {
    await super.willUpdate(t), t.has("agent") && (this.createConversationController.agent = this.agent);
  }
  render() {
    return h`
      <wy-conversation-new
        .agent=${this.agent}
        @create=${async (t) => {
      await this.createConversationController.create(t.detail.members);
    }}
        ${K(this.conversationNewRef)}
      ></wy-conversation-new>
    `;
  }
}, n(Fc, "WyMessengerNew"), Fc);
uu.styles = [Kt, hn, tf, bi, Pe];
uu = bk([
  U("wy-messenger-new")
], uu);
var xk = Object.defineProperty, Ck = Object.getOwnPropertyDescriptor, ph = /* @__PURE__ */ n((s, t, e, i) => {
  for (var r = i > 1 ? void 0 : i ? Ck(t, e) : t, a = s.length - 1, o; a >= 0; a--)
    (o = s[a]) && (r = (i ? o(t, e, r) : o(r)) || r);
  return i && r && xk(t, e, r), r;
}, "__decorateClass$6"), Ic;
let as = (Ic = class extends Nt {
  constructor() {
    super(...arguments), this.theme = new ce(this, as.styles), this.unreadConversations = new Qc(this), this.componentTypes = [rt.ChatRoom, rt.PrivateChat], this.badge = "count", this.badgePosition = "inline";
  }
  /** 
   * Disabled link matching/consuming.
   * @internal 
   */
  matchesLink() {
    return !1;
  }
  set agent(t) {
    super.agent = t, this._agentUid ? this.componentTypes = [rt.AgentChat] : this.componentTypes = [rt.ChatRoom, rt.PrivateChat];
  }
  get agent() {
    return super.agent;
  }
  /**
   * Current unread conversation count.
   */
  get unread() {
    return this.unreadConversations.unread;
  }
  async willUpdate(t) {
    await super.willUpdate(t), (t.has("componentTypes") || t.has("agent")) && await this.unreadConversations.track(this.componentTypes, this.agent);
  }
  render() {
    return this.user && this.badge !== "none" ? h`
          <wy-badge
            appearance=${this.badge}
            position=${this.badgePosition}
            .count=${this.unreadConversations.isUnreadPending ? NaN : this.unreadConversations.unread}
          ></wy-badge>
        ` : g;
  }
}, n(Ic, "WyMessengerBadge"), Ic);
as.styles = [Wg, Kt, Pe];
ph([
  y({ attribute: !1 })
], as.prototype, "componentTypes", 2);
ph([
  y({ type: String })
], as.prototype, "badge", 2);
ph([
  y({ type: String })
], as.prototype, "badgePosition", 2);
ph([
  y({ type: String })
], as.prototype, "agent", 1);
as = ph([
  U("wy-messenger-badge")
], as);
const Yg = j`[part~=wy-conversations]{position:relative;display:flex;flex-direction:column;gap:var(--wy-gap-sm, calc(.5 * var(--wy-gap, var(--wy-padding, calc(.5 * var(--wy-size, 1rem))))))}[part~=wy-conversation-list]{display:flex;flex-direction:column;gap:var(--wy-gap-sm, calc(.5 * var(--wy-gap, var(--wy-padding, calc(.5 * var(--wy-size, 1rem))))))}`;
var $k = Object.defineProperty, kk = Object.getOwnPropertyDescriptor, Yu = /* @__PURE__ */ n((s, t, e, i) => {
  for (var r = i > 1 ? void 0 : i ? kk(t, e) : t, a = s.length - 1, o; a >= 0; a--)
    (o = s[a]) && (r = (i ? o(t, e, r) : o(r)) || r);
  return i && r && $k(t, e, r), r;
}, "__decorateClass$5"), Dc;
let ul = (Dc = class extends nt {
  constructor() {
    super(...arguments), this.exportParts = new W(this), this.typing = new eu(this);
  }
  willUpdate(t) {
    super.willUpdate(t), t.has("appId") && (this.typing.appId = this.appId), t.has("userId") && (this.typing.userId = this.userId);
  }
  render() {
    const { names: t, ellipsis: e } = this.typing;
    let i;
    if (t.length === 1) {
      const r = t[0];
      i = x(gt`${r} is typing${e}`, { desc: "A is typing..." });
    } else if (t.length > 1) {
      const r = new Intl.ListFormat(this.weavy?.locale, { style: "long", type: "conjunction" }).format(
        t
      );
      i = x(gt`${r} are typing${e}`, {
        desc: "A, B and C are typing..."
      });
    }
    return i ? h`<span>${i}</span>` : h`<slot></slot>`;
  }
}, n(Dc, "WyTyping"), Dc);
Yu([
  he({ context: Xt, subscribe: !0 }),
  V()
], ul.prototype, "weavy", 2);
Yu([
  y({ attribute: !0, type: Number })
], ul.prototype, "appId", 2);
Yu([
  y({ attribute: !0, type: Number })
], ul.prototype, "userId", 2);
ul = Yu([
  U("wy-typing"),
  ut()
], ul);
var _k = Object.defineProperty, Pk = Object.getOwnPropertyDescriptor, Xg = /* @__PURE__ */ n((s) => {
  throw TypeError(s);
}, "__typeError$1"), je = /* @__PURE__ */ n((s, t, e, i) => {
  for (var r = i > 1 ? void 0 : i ? Pk(t, e) : t, a = s.length - 1, o; a >= 0; a--)
    (o = s[a]) && (r = (i ? o(t, e, r) : o(r)) || r);
  return i && r && _k(t, e, r), r;
}, "__decorateClass$4"), Jg = /* @__PURE__ */ n((s, t, e) => t.has(s) || Xg("Cannot " + e), "__accessCheck$1"), xv = /* @__PURE__ */ n((s, t, e) => (Jg(s, t, "read from private field"), e ? e.call(s) : t.get(s)), "__privateGet$1"), Sk = /* @__PURE__ */ n((s, t, e) => t.has(s) ? Xg("Cannot add the same private member more than once") : t instanceof WeakSet ? t.add(s) : t.set(s, e), "__privateAdd$1"), Cv = /* @__PURE__ */ n((s, t, e, i) => (Jg(s, t, "write to private field"), t.set(s, e), e), "__privateSet$1"), On, Vc;
let _e = (Vc = class extends nt {
  constructor() {
    super(...arguments), this.exportParts = new W(this), this.unread = !1, this.avatarUrl = "", this.hideAvatar = !1, this.name = "", this.type = rt.PrivateChat, this.selected = !1, this.starred = !1, this.pinned = !1, this.handleMessageCreated = (t) => {
      !this.user || !this.weavy || (Ee(this.weavy.queryClient, ["apps", t.message.app.id], void 0, (e) => ({
        ...e,
        last_message: t.message,
        is_unread: t.message.created_by.id !== this.user?.id
      })), ht(
        this.weavy.queryClient,
        { queryKey: ["apps", "list"], exact: !1 },
        t.message.app.id,
        (e) => ({
          ...e,
          last_message: t.message,
          is_unread: t.message.created_by.id !== this.user?.id
        })
      ));
    }, this.handleConversationUpdated = () => {
      this.weavy?.queryClient.invalidateQueries({ queryKey: ["apps"], exact: !1 });
    }, this.handleConversationMarked = (t) => {
      this.user && t.actor.id === this.user.id && this.weavy?.queryClient.invalidateQueries({ queryKey: ["apps"], exact: !1 });
    }, Sk(this, On);
  }
  /**
   * Trigger `selected` event.
   *
   * @returns Whether the event was dispatched successfully.
   */
  dispatchSelected() {
    if (!this.conversationId)
      return;
    const t = new CustomEvent("selected", {
      detail: { id: this.conversationId }
    });
    return this.dispatchEvent(t);
  }
  /**
   * Trigger `star` event.
   *
   * @param star - Whether the conversation should be starred.
   * @returns Whether the event was dispatched successfully.
   */
  dispatchStar(t) {
    if (!this.conversationId)
      return;
    const e = new CustomEvent("star", {
      detail: { id: this.conversationId, star: t }
    });
    return this.dispatchEvent(e);
  }
  /**
   * Trigger `pin` event.
   *
   * @param pin - Whether the conversation should be pinned.
   * @returns Whether the event was dispatched successfully.
   *
   * @internal
   */
  dispatchPin(t) {
    if (!this.conversationId)
      return;
    const e = new CustomEvent("pin", {
      detail: { id: this.conversationId, pin: t }
    });
    return this.dispatchEvent(e);
  }
  /**
   * Trigger `mark` event for marking the conversation as read/unread.
   *
   * @param mark - Whether the conversation should be marked as read.
   * @returns Whether the event was dispatched successfully.
   *
   * @internal
   */
  dispatchMarked(t) {
    if (!this.conversationId)
      return;
    const e = new CustomEvent("mark", {
      detail: { id: this.conversationId, messageId: t ? this.lastMessage?.id : null }
    });
    return this.dispatchEvent(e);
  }
  /**
   * Triggers `leave` event when the current user is leaving the conversation.
   *
   * @returns Whether the event was dispatched successfully.
   *
   * @internal
   */
  dispatchLeaveConversation() {
    if (!this.conversationId)
      return;
    const t = new CustomEvent("leave", {
      detail: { id: this.conversationId }
    });
    return this.dispatchEvent(t);
  }
  /**
   * Triggers `remove` when the conversation should be removed.
   *
   * @returns Whether the event was dispatched successfully.
   *
   * @internal
   */
  dispatchRemoveConversation() {
    if (!this.conversationId)
      return;
    const t = new CustomEvent("remove", {
      detail: { id: this.conversationId }
    });
    return this.dispatchEvent(t);
  }
  /**
   * Triggers `trash` event when the conversation should be trashed.
   *
   * @returns Whether the event was dispatched successfully.
   *
   * @internal
   */
  dispatchTrashConversation() {
    if (!this.conversationId)
      return;
    const t = new CustomEvent("trash", {
      detail: { id: this.conversationId }
    });
    return this.dispatchEvent(t);
  }
  willUpdate(t) {
    var e;
    if (super.willUpdate(t), (t.has("weavy") || t.has("conversationId")) && this.weavy && this.conversationId) {
      (e = xv(this, On)) == null || e.call(this);
      const i = `a${this.conversationId}`;
      this.weavy.subscribe(i, "app_updated", this.handleConversationUpdated), this.weavy.subscribe(i, "member_added", this.handleConversationUpdated), this.weavy.subscribe(i, "message_created", this.handleMessageCreated), this.weavy.subscribe(i, "app_marked", this.handleConversationMarked), Cv(this, On, () => {
        this.weavy?.unsubscribe(i, "app_updated", this.handleConversationUpdated), this.weavy?.unsubscribe(i, "member_added", this.handleConversationUpdated), this.weavy?.unsubscribe(i, "message_created", this.handleMessageCreated), this.weavy?.unsubscribe(i, "app_marked", this.handleConversationMarked), Cv(this, On, void 0);
      });
    }
  }
  render() {
    const t = this.lastMessage?.created_at ? new Intl.DateTimeFormat(this.weavy?.locale, { dateStyle: "full", timeStyle: "short" }).format(
      new Date(this.lastMessage.created_at)
    ) : "", e = this.lastMessage?.created_at ? bl(this.weavy?.locale, new Date(this.lastMessage.created_at)) : "", i = this.type === rt.PrivateChat && this.user ? (this.members?.data || []).filter((r) => r.id !== this.user?.id)?.[0] ?? this.user : null;
    return h`
      <wy-item
        part="wy-conversation-item"
        size="lg"
        interactive
        outer
        status=${this.unread ? "unread" : void 0}
        ?selected=${this.selected}
        tabindex="0"
        actionsPosition="bottom"
        @click=${(r) => (r.preventDefault(), this.dispatchSelected())}
        @keydown=${ne}
        @keyup=${ke}
      >
        ${this.type !== rt.AgentChat ? this.avatarUrl ? h`<wy-avatar slot="image" .size=${48} src=${this.avatarUrl}></wy-avatar>` : this.type == rt.ChatRoom ? h` <wy-avatar-group
                  slot="image"
                  .members=${this.members?.data}
                  title=${this.name}
                  .size=${48}
                ></wy-avatar-group>` : h`
                  <wy-avatar
                    slot="image"
                    src=${ot(i?.avatar_url)}
                    name=${ot(i?.name)}
                    description=${ot(i?.comment)}
                    presence=${i?.presence || "away"}
                    ?isAgent=${i?.is_agent}
                    id=${ot(i?.id)}
                    size=${48}
                  ></wy-avatar>
                ` : g}

        <span slot="title">${this.name || this.lastMessage?.plain || x("Untitled conversation")}</span>
        ${this.lastMessage ? h`
              <time slot="meta" datetime=${this.lastMessage.created_at.toString()} title=${t}
                >${e}</time
              >
            ` : g}
        <span slot="text">
          ${this.user ? h`
                <wy-typing appId=${this.conversationId} userId=${this.user.id}>
                  ${this.lastMessage ? h`
                        ${this.user.id === this.lastMessage.created_by.id ? h`${x("You")}: ` : g}
                        ${this.members.count > 2 && this.user.id !== this.lastMessage?.created_by.id ? h`${this.lastMessage?.created_by.name}: ` : g}
                      ` : g}
                  ${this.lastMessage?.text ? h`<span>${this.lastMessage.plain}</span>` : g}
                  ${!this.lastMessage?.text && (this.lastMessage?.attachments?.count || 0) > 0 ? h`<wy-icon kind="text-icon" name="attachment"></wy-icon>` : g}
                  ${!this.lastMessage?.text && this.lastMessage?.meeting?.id ? h`<wy-icon kind="text-icon" name="video"></wy-icon>` : g}
                  ${!this.lastMessage?.text && (this.lastMessage?.options?.count || 0) > 0 ? h`<wy-icon kind="text-icon" name="poll"></wy-icon>` : g}
                  ${this.lastMessage ? g : h`&nbsp;`}
                </wy-typing>
              ` : g}
        </span>

        ${this.starred ? h`<wy-button
              small
              slot="actions"
              kind="icon"
              @click=${(r) => {
      r.stopPropagation(), this.dispatchStar(!1);
    }}
            >
              <wy-icon name="star" size=${24} color="yellow"></wy-icon>
            </wy-button>` : g}
        ${this.pinned ? h`<wy-button
              small
              slot="actions"
              kind="icon"
              @click=${(r) => {
      r.stopPropagation(), this.dispatchPin(!1);
    }}
            >
              <wy-icon name="pin" size=${20} color=""></wy-icon>
            </wy-button>` : g}

        <wy-dropdown small slot="actions" directionX="left">
          <wy-dropdown-item @click=${() => this.dispatchMarked(this.unread)}>
            <wy-icon name=${this.unread ? "read" : "unread"}></wy-icon>
            ${this.unread ? x("Mark as read") : x("Mark as unread")}
          </wy-dropdown-item>
          <wy-dropdown-item @click=${() => this.dispatchPin(!this.pinned)}>
            <wy-icon name=${this.pinned ? "unpin" : "pin"}></wy-icon>
            ${this.pinned ? x("Unpin") : x("Pin")}
          </wy-dropdown-item>
          <wy-dropdown-item @click=${() => this.dispatchStar(!this.starred)}>
            <wy-icon name=${this.starred ? "unstar" : "star"}></wy-icon>
            ${this.starred ? x("Unstar") : x("Star")}
          </wy-dropdown-item>
          ${this.type === rt.PrivateChat ? h`<wy-dropdown-item @click=${() => this.dispatchRemoveConversation()}>
                <wy-icon name="trashcan"></wy-icon>
                ${x("Delete")}
              </wy-dropdown-item>` : g}
          ${this.type === rt.ChatRoom ? h`<wy-dropdown-item @click=${() => this.dispatchLeaveConversation()}>
                <wy-icon name="account-minus"></wy-icon>
                ${x("Leave")}
              </wy-dropdown-item>` : g}
          ${this.type === rt.AgentChat ? h`
                <wy-dropdown-item @click=${() => this.dispatchTrashConversation()}>
                  <wy-icon name="trashcan"></wy-icon>
                  ${x("Delete")}
                </wy-dropdown-item>
              ` : g}
        </wy-dropdown>
      </wy-item>
    `;
  }
  connectedCallback() {
    super.connectedCallback(), this.weavy && this.requestUpdate("weavy");
  }
  disconnectedCallback() {
    var t;
    (t = xv(this, On)) == null || t.call(this), super.disconnectedCallback();
  }
}, n(Vc, "WyConversationItem"), Vc);
On = /* @__PURE__ */ new WeakMap();
_e.styles = [Yg];
je([
  he({ context: Xt, subscribe: !0 }),
  V()
], _e.prototype, "weavy", 2);
je([
  he({ context: Cu, subscribe: !0 }),
  V()
], _e.prototype, "user", 2);
je([
  y({ attribute: !0, type: Number })
], _e.prototype, "conversationId", 2);
je([
  y({ attribute: !0, type: Boolean, reflect: !0 })
], _e.prototype, "unread", 2);
je([
  y({ attribute: !0 })
], _e.prototype, "avatarUrl", 2);
je([
  y({ attribute: !0, type: Boolean, reflect: !0 })
], _e.prototype, "hideAvatar", 2);
je([
  y({ attribute: !0 })
], _e.prototype, "name", 2);
je([
  y({ attribute: !0, type: String })
], _e.prototype, "type", 2);
je([
  y({ attribute: !0, type: Boolean, reflect: !0 })
], _e.prototype, "selected", 2);
je([
  y({ attribute: !0, type: Boolean, reflect: !0 })
], _e.prototype, "starred", 2);
je([
  y({ attribute: !0, type: Boolean, reflect: !0 })
], _e.prototype, "pinned", 2);
je([
  y({ attribute: !1 })
], _e.prototype, "members", 2);
je([
  y({ attribute: !1 })
], _e.prototype, "lastMessage", 2);
_e = je([
  U("wy-conversation-item"),
  ut()
], _e);
var Mk = Object.defineProperty, Ek = Object.getOwnPropertyDescriptor, t0 = /* @__PURE__ */ n((s) => {
  throw TypeError(s);
}, "__typeError"), yh = /* @__PURE__ */ n((s, t, e, i) => {
  for (var r = i > 1 ? void 0 : i ? Ek(t, e) : t, a = s.length - 1, o; a >= 0; a--)
    (o = s[a]) && (r = (i ? o(t, e, r) : o(r)) || r);
  return i && r && Mk(t, e, r), r;
}, "__decorateClass$3"), e0 = /* @__PURE__ */ n((s, t, e) => t.has(s) || t0("Cannot " + e), "__accessCheck"), $v = /* @__PURE__ */ n((s, t, e) => (e0(s, t, "read from private field"), e ? e.call(s) : t.get(s)), "__privateGet"), Ak = /* @__PURE__ */ n((s, t, e) => t.has(s) ? t0("Cannot add the same private member more than once") : t instanceof WeakSet ? t.add(s) : t.set(s, e), "__privateAdd"), kv = /* @__PURE__ */ n((s, t, e, i) => (e0(s, t, "write to private field"), t.set(s, e), e), "__privateSet"), zn, Hc;
let os = (Hc = class extends Ae {
  constructor() {
    super(...arguments), this.exportParts = new W(this), this.conversationTypes = [rt.ChatRoom, rt.PrivateChat], this.searchText = "", this.conversationsQuery = new ar(this), this.infiniteScroll = new or(this), this.pagerRef = J(), this.handleRefresh = () => {
      this.conversationsQuery.result.refetch();
    }, this.handlePresenceChange = (t) => {
      if (!this.weavy)
        return;
      Array.isArray(t) || (t = [parseInt(t)]);
      const e = /* @__PURE__ */ n((i) => {
        const r = i.members.data ?? [];
        return r.forEach((a) => {
          a.presence = t.indexOf(a.id) != -1 ? "active" : "away";
        }), i.members.data = r, i;
      }, "updateMembersInApps");
      ht(
        this.weavy.queryClient,
        { queryKey: ["apps", "list"], exact: !1 },
        void 0,
        e
      );
    }, Ak(this, zn);
  }
  /**
   * Selects a conversation and triggers an action event.
   *
   * @param conversation - The conversation to select.
   */
  selectConversation(t) {
    this.conversationId = t?.id, this.dispatchAction(me.Select, t);
  }
  /**
   * Triggers `wy-action` event.
   * @param action - The performed action.
   * @param app - The conversation to select.
   * @returns Whether the event was successful.
   */
  dispatchAction(t, e) {
    const i = new CustomEvent("wy-action", {
      detail: { action: t, app: e },
      bubbles: !0,
      composed: !0
    });
    return this.dispatchEvent(i);
  }
  /**
   * Marks a conversation as read.
   *
   * @param appId - The id of the conversation to mark.
   * @param messageId - Optional message id to set the marker to.
   */
  async handleMark(t, e) {
    await this.markConversationMutation?.mutate({ app: t, messageId: e, userId: this.user?.id });
  }
  /**
   * Sets a conversation as starred.
   *
   * @param appId -  The id of the conversation to star.
   * @param star - Whether to make the conversation starred.
   */
  async handleStar(t, e) {
    await this.starConversationMutation?.mutate({ appId: t, star: e });
  }
  /**
   * Sets a conversation as pinned.
   *
   * @param appId - The id of the conversation to pin.
   * @param pin - Whether to make the conversation pinned.
   */
  async handlePin(t, e) {
    await this.pinConversationMutation?.mutate({ appId: t, pin: e });
  }
  /**
   * Leave a conversation (for the current user).
   *
   * @param appId - The id of the conversation to leave.
   */
  async handleLeaveConversation(t) {
    this.conversationId === t && this.selectConversation(null);
    const e = await this.whenUser();
    await this.leaveConversationMutation?.mutate({ appId: t, members: [e.id] }), this.conversationsQuery.result.refetch();
  }
  /**
   * Remove a conversation.
   *
   * @param appId - The id of the conversation to remove.
   */
  async handleRemoveConversation(t) {
    this.conversationId === t && this.selectConversation(null), await this.removeConversationMutation?.mutate({ appId: t }), this.conversationsQuery.result.refetch();
  }
  /**
   * Trash a conversation.
   *
   * @param appId - The id of the conversation to trash.
   */
  async handleTrashConversation(t) {
    this.conversationId === t && this.selectConversation(null), await this.trashConversationMutation?.mutate({ appId: t }), this.conversationsQuery.result.refetch();
  }
  async willUpdate(t) {
    var e;
    super.willUpdate(t), (t.has("weavy") || t.has("conversationTypes")) && this.weavy && (await this.conversationsQuery.trackInfiniteQuery(
      P2(
        this.weavy,
        {},
        this.conversationTypes,
        this.agent,
        () => this.searchText,
        "pinned_at desc,rev desc",
        !1
      )
    ), this.markConversationMutation = _1(this.weavy), this.starConversationMutation = b5(this.weavy), this.pinConversationMutation = x5(this.weavy), this.leaveConversationMutation = P1(this.weavy), this.removeConversationMutation = C5(this.weavy), this.trashConversationMutation = _5(this.weavy), (e = $v(this, zn)) == null || e.call(this), this.weavy.subscribe(null, "app_created", this.handleRefresh), this.weavy.subscribe(null, "message_created", this.handleRefresh), this.weavy.subscribe(null, "member_added", this.handleRefresh), this.weavy.subscribe(null, "online", this.handlePresenceChange), kv(this, zn, () => {
      this.weavy?.unsubscribe(null, "app_created", this.handleRefresh), this.weavy?.unsubscribe(null, "message_created", this.handleRefresh), this.weavy?.unsubscribe(null, "member_added", this.handleRefresh), this.weavy?.unsubscribe(null, "online", this.handlePresenceChange), kv(this, zn, void 0);
    }));
  }
  update(t) {
    super.update(t), this.infiniteScroll.observe(this.conversationsQuery.result, this.pagerRef.value);
  }
  async updated(t) {
    t.has("searchText") && t.get("searchText") !== void 0 && this.conversationsQuery.result && await this.conversationsQuery.result.refetch?.();
  }
  renderConversations(t) {
    if (t) {
      const e = pi(t);
      return Be(
        e,
        (i) => i?.id,
        (i) => [
          h`<wy-conversation-item
              conversationId=${i?.id}
              .avatarUrl=${i?.avatar_url}
              .hideAvatar=${!!this.agent}
              .name=${i.name}
              .lastMessage=${i.last_message}
              .members=${i.members}
              .unread=${i.is_unread}
              .starred=${i.is_starred}
              .pinned=${i.is_pinned}
              .type=${i.type}
              .selected=${this.conversationId == i.id}
              @selected=${() => this.selectConversation(i)}
              @mark=${(r) => this.handleMark(i, r.detail.messageId)}
              @star=${(r) => this.handleStar(r.detail.id, r.detail.star)}
              @pin=${(r) => this.handlePin(r.detail.id, r.detail.pin)}
              @leave=${(r) => this.handleLeaveConversation(r.detail.id)}
              @remove=${(r) => this.handleRemoveConversation(r.detail.id)}
              @trash=${(r) => this.handleTrashConversation(r.detail.id)}
            ></wy-conversation-item>`
        ]
      );
    }
    return g;
  }
  render() {
    const { data: t, isPending: e, hasNextPage: i } = this.conversationsQuery.result ?? {};
    return h`
      <div part="wy-conversations">
        <wy-buttons part="wy-conversations-toolbar" position=${this.agent ? "floating" : "sticky"} ?reverse=${!!this.agent}>
          <slot name="navigation"></slot>
          ${this.agent ? g : h`
                <wy-search
                  compact
                  placeholder=${x("Search for conversations...")}
                  @search=${(r) => this.searchText = r.detail.query}
                ></wy-search>
              `}
          <slot name="actions"></slot>
        </wy-buttons>

        <div part="wy-conversation-list">
          ${!e && this.user && t ? t.pages[0]?.count || this.searchText ? this.renderConversations(t) : h`
                  <div part="wy-pane-body">
                    <div part="wy-pane-group">
                      <wy-empty noNetwork>${x("Create a conversation to get started.")}</wy-empty>
                    </div>
                  </div>
                ` : h`<wy-empty><wy-progress-circular indeterminate padded></wy-progress-circular></wy-empty>`}
          ${i ? h`<div ${K(this.pagerRef)} part="wy-pager wy-pager-bottom"></div>` : g}
        </div>
      </div>
    `;
  }
  disconnectedCallback() {
    var t;
    (t = $v(this, zn)) == null || t.call(this), super.disconnectedCallback();
  }
}, n(Hc, "WyConversationList"), Hc);
zn = /* @__PURE__ */ new WeakMap();
os.styles = [Yg, dn, Y, kr];
yh([
  y({ type: Number })
], os.prototype, "conversationId", 2);
yh([
  y({ type: Array })
], os.prototype, "conversationTypes", 2);
yh([
  y()
], os.prototype, "agent", 2);
yh([
  V()
], os.prototype, "searchText", 2);
os = yh([
  U("wy-conversation-list"),
  ut()
], os);
var Rk = Object.defineProperty, Tk = Object.getOwnPropertyDescriptor, Xu = /* @__PURE__ */ n((s, t, e, i) => {
  for (var r = i > 1 ? void 0 : i ? Tk(t, e) : t, a = s.length - 1, o; a >= 0; a--)
    (o = s[a]) && (r = (i ? o(t, e, r) : o(r)) || r);
  return i && r && Rk(t, e, r), r;
}, "__decorateClass$2"), Uc;
let an = (Uc = class extends Nt {
  constructor() {
    super(...arguments), this.componentFeatures = new Gt(dl), this.theme = new ce(this, an.styles), this.unreadConversations = new Qc(this), this.createConversationController = new Kc(this), this.componentTypes = [rt.ChatRoom, rt.PrivateChat], this.conversationId = null, this.conversationQuery = new ve(this), this.persistState = new Hs(this), this.conversationNewRef = J();
  }
  set agent(t) {
    super.agent = t, this._agentUid ? (this.componentTypes = [rt.AgentChat], this.componentFeatures = new Gt(
      Jw,
      this.componentFeatures.allowedFeatures()
    )) : (this.componentTypes = [rt.ChatRoom, rt.PrivateChat], this.componentFeatures = new Gt(
      dl,
      this.componentFeatures.allowedFeatures()
    )), this.conversationId = null;
  }
  get agent() {
    return super.agent;
  }
  /** Current unread conversation count. */
  get unread() {
    return this.unreadConversations.unread;
  }
  /**
   * Creates a new conversation.
   *
   * When in agent mode, a conversation is created instantly.
   *
   * @param members - Optional array of member id or member uid.
   */
  async createConversation(t) {
    return await this.createConversationController.create(t);
  }
  /**
   * Opens the select member dialog to let the user choose members.
   *
   * @returns Promise resolving to any selected member ids or uids.
   */
  async selectMembers() {
    return await this.conversationNewRef.value?.selectMembers();
  }
  async willUpdate(t) {
    await super.willUpdate(t), (t.has("weavy") || t.has("agent") || t.has("user")) && this.weavy && this.user && this.persistState.observe(
      [{ name: "conversationId", override: !1 }],
      this.agent || "messenger",
      `u${this.user?.id}`,
      (e) => {
        if (e.has("conversationId") && this.conversationId) {
          const i = new CustomEvent("wy-action", {
            detail: {
              action: me.Select,
              app: { id: this.conversationId }
            },
            bubbles: !0,
            composed: !0
          });
          this.dispatchEvent(i);
        }
      }
    ), t.has("agent") && (this.createConversationController.agent = this.agent), t.has("link") && this.link?.app && (this.conversationId = this.link.app.id), (t.has("componentTypes") || t.has("agent")) && await this.unreadConversations.track(this.componentTypes, this.agent);
  }
  render() {
    return h`
      <wy-conversation-list
        .conversationTypes=${this.componentTypes}
        .agent=${this.agent}
        conversationId=${ot(this.conversationId !== null ? this.conversationId : void 0)}
        @wy-action=${(t) => {
      !t.defaultPrevented && t.detail.action === me.Select && t.detail.app !== void 0 && (this.conversationId = t.detail.app?.id);
    }}
      >
        <wy-conversation-new
          slot="actions"
          .agent=${this.agent}
          @wy-action=${(t) => {
      !t.defaultPrevented && t.detail.action === me.Select && t.detail.app !== void 0 && (this.conversationId = t.detail.app?.id);
    }}
          ${K(this.conversationNewRef)}
        >
          <slot name="conversation-new"></slot>
        </wy-conversation-new>
        <slot name="actions" slot="actions"></slot>
      </wy-conversation-list>
    `;
  }
}, n(Uc, "WyMessengerConversations"), Uc);
an.styles = [Kt, bi, Cr, Pe, $r, ls];
Xu([
  y({ attribute: !1 })
], an.prototype, "componentTypes", 2);
Xu([
  y({ type: String })
], an.prototype, "agent", 1);
Xu([
  y({ type: Number })
], an.prototype, "conversationId", 2);
an = Xu([
  U("wy-messenger-conversations"),
  ut()
], an);
var Lk = Object.defineProperty, Ok = Object.getOwnPropertyDescriptor, Ju = /* @__PURE__ */ n((s, t, e, i) => {
  for (var r = i > 1 ? void 0 : i ? Ok(t, e) : t, a = s.length - 1, o; a >= 0; a--)
    (o = s[a]) && (r = (i ? o(t, e, r) : o(r)) || r);
  return i && r && Lk(t, e, r), r;
}, "__decorateClass$1"), Bc;
let on = (Bc = class extends Ae {
  constructor() {
    super(...arguments), this.exportParts = new W(this), this._whenMembers = this.createMembersPromise(), this.show = !1;
  }
  /**
   * Resolves when members are selected from the modal.
   *
   * @returns Promise<MemberIdType[]>
   */
  async whenMembers() {
    return this._whenMembers;
  }
  /**
   * Create an internal members promise and store resolve/reject handlers.
   *
   * @internal
   */
  createMembersPromise() {
    const t = new Promise((e, i) => {
      this._resolveMembers = e, this._rejectMembers = i;
    });
    return this._whenMembers = t, t;
  }
  /**
   * Open the new conversation dialog and return selected members.
   *
   * @returns Promise<MemberIdType[]>
   */
  async selectMembers() {
    return this.show = !0, await this.whenMembers();
  }
  /**
   * Close the new conversation dialog and resolve or reject the selection promise.
   *
   * @internal
   * @param members - Optional selected member ids to resolve the promise with.
   */
  close(t) {
    this.show = !1, t ? this._resolveMembers?.(t) : this._rejectMembers?.("Selection aborted by user; no members selected"), this.createMembersPromise();
  }
  /**
   * Create a conversation by dispatching a `create` event and closing the dialog.
   *
   * @param members - Array of members by id/uid for a new conversation.
   */
  async submit(t = []) {
    await this.whenUser(), this.close(t);
    const e = new CustomEvent("create", {
      detail: { members: t }
    });
    this.dispatchEvent(e), this.createMembersPromise();
  }
  render() {
    return h`
      ${this.slotElements.length ? g : h`
            <wy-button
              part="wy-conversation-new-button"
              kind="icon"
              @click=${async () => {
      if (this.agent)
        await this.submit();
      else
        try {
          await this.selectMembers();
        } catch {
        }
    }}
            >
              <wy-icon name="plus"></wy-icon>
            </wy-button>
          `}
      <slot></slot>

      ${!this.agent && this.weavy && this.user ? h`<wy-overlay
            part="wy-conversation-new-dialog"
            .show=${this.show}
            @close=${() => {
      this.show = !1;
    }}
          >
            <wy-titlebar header slot="header">
              <wy-button slot="icon" kind="icon" @click=${() => this.close()}>
                <wy-icon name="close"></wy-icon>
              </wy-button>
              <span slot="title">${x("New conversation")}</span>
            </wy-titlebar>
            ${this.show ? h`
                  <wy-users-search
                    @submit=${(t) => this.submit(t.detail.members.map((e) => e.id))}
                  ></wy-users-search>
                ` : g}
          </wy-overlay>` : g}
    `;
  }
}, n(Bc, "WyConversationNew"), Bc);
on.styles = [Y];
Ju([
  y()
], on.prototype, "agent", 2);
Ju([
  V()
], on.prototype, "show", 2);
Ju([
  yl({ flatten: !0, selector: ":not(slot)" })
], on.prototype, "slotElements", 2);
on = Ju([
  U("wy-conversation-new"),
  ut()
], on);
const zk = j`[part~=wy-editor-message-inputs]{display:flex;flex-direction:row;align-items:flex-end;padding:var(--wy-padding, calc(.5 * var(--wy-size, 1rem))) var(--wy-padding-sm, calc(.75 * var(--wy-padding, calc(.5 * var(--wy-size, 1rem)))));min-height:calc(var(--wy-input-font-size, var(--wy-font-size, 1em)) * 1.5 * var(--wy-size, 1rem) + var(--wy-input-padding-y, var(--wy-padding, calc(.5 * var(--wy-size, 1rem)))) * 2 + var(--wy-padding-sm, calc(.75 * var(--wy-padding, calc(.5 * var(--wy-size, 1rem))))) * 2)}[part~=wy-editor-message-text]{flex:1 1 100%;display:flex;flex-direction:column;margin-inline:var(--wy-padding-sm, calc(.75 * var(--wy-padding, calc(.5 * var(--wy-size, 1rem)))))}[part~=wy-editor-parts]{padding:var(--wy-padding, calc(.5 * var(--wy-size, 1rem)));min-height:0}`;
var Fk = Object.getOwnPropertyDescriptor, Ik = /* @__PURE__ */ n((s, t, e, i) => {
  for (var r = i > 1 ? void 0 : i ? Fk(t, e) : t, a = s.length - 1, o; a >= 0; a--)
    (o = s[a]) && (r = o(r) || r);
  return r;
}, "__decorateClass"), Nc;
let pu = (Nc = class extends Tt {
  constructor() {
    super(), this.editorType = "messages", this.editorClass = "wy-editor-message";
  }
  /**
   * Render content that appears above the message editor.
   *
   * By default returns the same lists section used by the base editor.
   *
   * @internal
   */
  renderTopSlot() {
    return [this.renderLists()];
  }
  /**
   * Render the primary middle slot containing the add-menu, editor and send button.
   *
   * Overrides the base implementation to provide message-specific controls and layout.
   *
   * @internal
   */
  renderMiddleSlot() {
    return h` <div part="wy-editor-message-inputs">
      <!-- Add -->
      ${this.componentFeatures?.allowsAnyFeature(
      _.Attachments,
      _.CloudFiles,
      _.Polls,
      _.Meetings,
      _.ZoomMeetings,
      _.GoogleMeet,
      _.MicrosoftTeams
    ) ? h`<wy-dropdown icon="plus" directionY="up" ?disabled=${this.disabled}>
            ${this.componentFeatures?.allowsFeature(_.Attachments) ? h`
                  <wy-dropdown-item @click=${() => this.openFileInput()} title=${x("From device")}>
                    <wy-icon name="attachment"></wy-icon>
                    <span>${x("From device")}</span>
                  </wy-dropdown-item>
                  <input
                    type="file"
                    ${K(this.fileInputRef)}
                    @click=${(t) => t.stopPropagation()}
                    @change=${(t) => this.handleUploadFiles(
      Array.from(t.target.files || []),
      t.target
    )}
                    multiple
                    hidden
                    tabindex="-1"
                  />
                ` : g}
            ${this.componentFeatures?.allowsFeature(_.CloudFiles) ? h`
                  <wy-dropdown-item @click=${() => this.openCloudFiles()} title=${x("From cloud")}>
                    <wy-icon name="cloud"></wy-icon>
                    <span>${x("From cloud")}</span>
                  </wy-dropdown-item>
                ` : g}
            ${this.componentFeatures?.allowsFeature(_.Polls) ? h`
                  <wy-dropdown-item @click=${() => this.togglePolls()} title=${x("Poll")}>
                    <wy-icon name="poll"></wy-icon>
                    <span>${x("Poll")}</span>
                  </wy-dropdown-item>
                ` : g}
            ${this.componentFeatures?.allowsAnyFeature(_.Meetings, _.ZoomMeetings) ? h`
                  <wy-dropdown-item @click=${() => this.handleMeetingClick("zoom")} title=${x("Zoom meeting")}>
                    <wy-icon svg="zoom-meetings"></wy-icon>
                    <span>${x("Zoom meeting")}</span>
                  </wy-dropdown-item>
                ` : g}
            ${this.componentFeatures?.allowsAnyFeature(_.Meetings, _.GoogleMeet) ? h`
                  <wy-dropdown-item @click=${() => this.handleMeetingClick("google")} title=${x("Google Meet")}>
                    <wy-icon svg="google-meet"></wy-icon>
                    <span>${x("Google Meet")}</span>
                  </wy-dropdown-item>
                ` : g}
            ${this.componentFeatures?.allowsAnyFeature(_.Meetings, _.MicrosoftTeams) ? h`
                  <wy-dropdown-item
                    @click=${() => this.handleMeetingClick("microsoft")}
                    title=${x("Microsoft Teams")}
                  >
                    <wy-icon svg="microsoft-teams"></wy-icon>
                    <span>${x("Microsoft Teams")}</span>
                  </wy-dropdown-item>
                ` : g}
          </wy-dropdown>` : g}

      <!-- Input -->

     ${this.renderEditor()}

      <!-- Button -->
      <wy-button
        kind="icon"
        color="primary-text"
        title=${x("Send", { desc: "Button action to send" })}
        @click="${() => this.submit()}"
        ?disabled=${this.disabled}
      >
        <wy-icon name="send"></wy-icon>
      </wy-button>
    </div>`;
  }
  /**
   * Render the bottom slot for message editor with no content.
   *
   * @internal
   */
  renderBottomSlot() {
    return g;
  }
}, n(Nc, "WyEditorMessage"), Nc);
pu.styles = [...Tt.styles, zk];
pu = Ik([
  U("wy-editor-message"),
  ut()
], pu);
const qk = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  get WyAnnotation() {
    return sd;
  },
  get WyAnnotationList() {
    return nd;
  },
  get WyAttachment() {
    return rd;
  },
  get WyAttachmentList() {
    return il;
  },
  get WyAvatar() {
    return Oi;
  },
  get WyAvatarGroup() {
    return Ns;
  },
  get WyAvatarHeader() {
    return Xl;
  },
  get WyBadge() {
    return fr;
  },
  get WyButton() {
    return Ye;
  },
  get WyButtons() {
    return qs;
  },
  get WyCloudFiles() {
    return Js;
  },
  get WyComment() {
    return Ii;
  },
  get WyCommentList() {
    return ur;
  },
  get WyContainer() {
    return zi;
  },
  get WyContextDataProgress() {
    return hu;
  },
  get WyConversation() {
    return Oe;
  },
  get WyConversationHeader() {
    return Hi;
  },
  get WyConversationItem() {
    return _e;
  },
  get WyConversationList() {
    return os;
  },
  get WyConversationNew() {
    return on;
  },
  get WyDropdown() {
    return Xe;
  },
  get WyDropdownDivider() {
    return cu;
  },
  get WyDropdownItem() {
    return ed;
  },
  get WyDropdownOption() {
    return el;
  },
  get WyEditorComment() {
    return du;
  },
  get WyEditorMessage() {
    return pu;
  },
  get WyEditorMsg() {
    return Tt;
  },
  get WyEmbed() {
    return rl;
  },
  get WyEmbedSelect() {
    return ad;
  },
  get WyEmpty() {
    return Jc;
  },
  get WyFileGrid() {
    return en;
  },
  get WyFileItem() {
    return Di;
  },
  get WyFileMenu() {
    return Xs;
  },
  get WyFileTable() {
    return ss;
  },
  get WyFileVersions() {
    return ud;
  },
  get WyFilesHeader() {
    return rs;
  },
  get WyFilesList() {
    return mr;
  },
  get WyIcon() {
    return Wt;
  },
  get WyIconDisplay() {
    return Yl;
  },
  get WyIconStack() {
    return ru;
  },
  get WyImageGrid() {
    return Ks;
  },
  get WyItem() {
    return le;
  },
  get WyItemList() {
    return Zr;
  },
  get WyMeetingCard() {
    return od;
  },
  get WyMessage() {
    return de;
  },
  get WyMessageTyping() {
    return si;
  },
  get WyNotificationHeader() {
    return sn;
  },
  get WyNotificationList() {
    return cl;
  },
  get WyNotificationListItem() {
    return ns;
  },
  get WyOverlay() {
    return Xr;
  },
  get WyPdfViewer() {
    return Ys;
  },
  get WyPoll() {
    return ld;
  },
  get WyPollOption() {
    return Qs;
  },
  get WyPost() {
    return is;
  },
  get WyPostEdit() {
    return fd;
  },
  get WyPostForm() {
    return md;
  },
  get WyPostList() {
    return tn;
  },
  get WyPostTrashed() {
    return wd;
  },
  get WyPostView() {
    return pr;
  },
  get WyPresence() {
    return Yc;
  },
  get WyPreview() {
    return jt;
  },
  get WyPreviewEmbed() {
    return Jr;
  },
  get WyPreviewIcon() {
    return Gs;
  },
  get WyPreviewImage() {
    return Zs;
  },
  get WyPreviewItem() {
    return nl;
  },
  get WyPreviewMedia() {
    return hr;
  },
  get WyPreviewText() {
    return Fi;
  },
  get WyProgressCircular() {
    return yi;
  },
  get WyProgressLinear() {
    return ni;
  },
  get WyReactionItem() {
    return dd;
  },
  get WyReactions() {
    return Yt;
  },
  get WySearch() {
    return es;
  },
  get WySkeleton() {
    return id;
  },
  get WyTitlebar() {
    return dr;
  },
  get WyTitlebarText() {
    return cd;
  },
  get WyToast() {
    return vr;
  },
  get WyToasts() {
    return gd;
  },
  get WyTyping() {
    return ul;
  },
  get WyUpload() {
    return hl;
  },
  get WyUserCard() {
    return ts;
  },
  get WyUsersSearch() {
    return vi;
  }
}, Symbol.toStringTag, { value: "Module" }));
export {
  Jh as AgentAppTypeGuids,
  Uk as AgentAppTypeStrings,
  bu as AppContext,
  v2 as AppTypeGuids,
  g2 as AppTypeStrings,
  Qv as AppsContext,
  Gt as ComponentFeatures,
  gm as ContextController,
  Gv as ContextIdContext,
  Kc as CreateConversationController,
  Zv as DataBlobsContext,
  Jw as DefaultMessengerAgentFeatures,
  dl as DefaultMessengerFeatures,
  tu as DropZoneController,
  _ as Feature,
  xu as FeaturePolicyContext,
  $m as HistoryController,
  ar as InfiniteQueryController,
  or as InfiniteScrollController,
  Xv as LinkContext,
  Nk as MessengerAgentTypes,
  Bk as MessengerTypes,
  Ue as MutationController,
  Wc as MutationStateController,
  Hs as PersistStateController,
  ve as QueryController,
  km as ResizeController,
  Ny as ReverseInfiniteScrollController,
  W as ShadowPartsController,
  Wy as SwipeScrollController,
  ce as ThemeController,
  eu as TypingController,
  Ti as UnknownApp,
  Qc as UnreadConversationsController,
  Ql as UnreadNotificationsController,
  Cu as UserContext,
  Vt as Weavy,
  ae as WeavyAppComponent,
  Qt as WeavyClient,
  jc as WeavyComponent,
  Xh as WeavyComponentSettings,
  Jv as WeavyComponentSettingsContext,
  qk as WeavyComponents,
  Xt as WeavyContext,
  Us as WeavyOptionalAppComponent,
  qt as WeavySubAppComponent,
  Ae as WeavySubComponent,
  Nt as WeavyTypeComponent,
  al as WyChat,
  pd as WyComments,
  Gc as WyComponent,
  Zt as WyContext,
  ol as WyCopilot,
  yr as WyFeed,
  rn as WyFiles,
  br as WyMessenger,
  as as WyMessengerBadge,
  Ne as WyMessengerButton,
  an as WyMessengerConversations,
  uu as WyMessengerNew,
  nn as WyNotificationBadge,
  mi as WyNotificationButton,
  gr as WyNotificationToasts,
  ll as WyNotifications,
  vd as WyPostEditor,
  wr as WyPosts,
  Sh as allFeatures,
  Gb as createWeavyContextProvider,
  Cw as featureConfigFromList,
  Yv as featureListFromString,
  Rl as globalContextProvider,
  Fy as hasAbort
};
